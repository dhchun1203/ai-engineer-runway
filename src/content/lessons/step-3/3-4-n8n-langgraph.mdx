---
title: "n8n·LangGraph 자동화 실습 개요"
stepId: 3
moduleId: "3-4"
order: 3
depth: "개요"
estimatedMinutes: 90
hasContent: true
slug: "3-4-n8n-langgraph"
---

## 1. 학습 목표

이 레슨을 마치면 다음을 알아듣습니다.

- n8n과 LangGraph가 각각 무엇인지 한 문장으로 말할 수 있다
- 어떤 자동화에 노코드가 맞고 어떤 자동화에 코드가 맞는지 판단 기준을 말할 수 있다
- 두 도구가 전체 자동화 파이프라인에서 어디에 놓이는지 안다

## 2. 왜 배우나

개강 후 실습에서 이 두 도구로 직접 자동화를 만들게 됩니다. 도구 이름과 성격을 그때 처음 만나면 배울 게 두 배가 됩니다.

이 편은 도구를 설치해 돌려 보는 편이 아니라, 개강 후 실습할 두 도구가 무엇이고 언제 어느 쪽을 고르는지 미리 알아 두는 편입니다. **지금은 설치하지 않습니다.**

## 3. 개념 설명

### 🧱 노코드 워크플로 도구 — n8n

n8n은 화면에서 블록을 이어 붙여 자동화를 만드는 도구입니다. 레고 블록을 조립하듯 "이 일이 생기면 → 이걸 하고 → 저걸 한다"를 화면에서 이어 붙입니다.

### 📐 코드 프레임워크 — LangGraph

LangGraph는 코드로 그래프를 선언해 자동화를 만드는 프레임워크입니다. 화면에서 이어 붙이는 대신, 도면을 그리듯 코드로 노드와 연결을 적습니다.

### 🔗 공통 개념 — 노드와 연결

두 도구 모두 "무엇을 하는 상자(노드)"와 "다음으로 가는 선(연결)"으로 자동화를 표현합니다. 이는 앞 편에서 본 여러 AI 구조의 노드·연결 개념과 같습니다.

### ⚖️ 언제 어느 쪽을 고르나

두 도구 중 무엇을 쓸지는 상황에 따라 팀이 판단하는 결정 지점입니다.

| 상황 | 어느 쪽이 맞나 |
|---|---|
| 변경이 잦고 비개발자도 손대야 함 | 노코드(n8n) |
| 조건 분기가 복잡하고 테스트가 필요함 | 코드(LangGraph) |
| 화면 구성은 노코드로, 복잡한 판단만 코드로 | 둘을 섞어서 사용 |

어느 쪽을 쓰든 무엇이 바뀌었는지 기록(버전 관리)하고, 실패했을 때 무엇이 잘못됐는지 알 수 있어야 합니다.

이 레슨에서도 앞 두 편과 같은 시나리오를 씁니다.

`트리거 → 노드 여러 개 → 분기 → 사람 승인 → 종료`

## 4. 실무 예제

### 실무에서 이게 어떻게 쓰이나

공유 시나리오("문의 도착 → 분류 → 검색 → 초안 → 승인 → 발송")를 n8n으로 만들면, 화면에서 "메일 도착" 블록에 "분류" 블록, "문서 검색" 블록을 순서대로 이어 붙이고, 마지막에 "담당자 승인" 블록을 놓습니다.

같은 시나리오를 LangGraph로 만들면, 각 단계를 함수로 정의하고 코드로 노드와 연결을 선언합니다. 조건에 따라 다른 노드로 가지를 치는 로직도 코드로 직접 표현합니다.

두 방식 모두 결과물은 같은 흐름을 만들지만, 화면에서 조립하는지 코드로 선언하는지가 다릅니다.

> 아래 코드는 개념을 눈으로 보기 위한 예시입니다 — 지금 실행할 필요는 없습니다.
> 개강 후 본 과정에서 실제로 돌려보게 됩니다.

```json
{
  "workflow": "customer-inquiry-response",
  "nodes": [
    { "id": "trigger", "type": "webhook", "label": "문의 메일 도착" },
    { "id": "classify", "type": "ai", "label": "문의 분류" },
    { "id": "search", "type": "ai", "label": "관련 문서 검색" },
    { "id": "draft", "type": "ai", "label": "답변 초안 작성" },
    { "id": "approve", "type": "human", "label": "담당자 승인" },
    { "id": "send", "type": "action", "label": "발송 및 기록" }
  ],
  "edges": [
    { "from": "trigger", "to": "classify" },
    { "from": "classify", "to": "search" },
    { "from": "search", "to": "draft" },
    { "from": "draft", "to": "approve" },
    { "from": "approve", "to": "send" }
  ]
}
```

## 5. 실무 팁

- 노코드도 결국 버전 관리가 필요합니다. 화면에서 고친 내용을 기록으로 남겨두어야 나중에 무엇이 바뀌었는지 알 수 있습니다.
- 워크플로가 커지면 하나로 몰아넣지 않고 여러 개로 쪼갭니다.
- 도구가 무엇이든 실패했을 때 어디까지 처리됐는지 알 수 있어야 합니다.

### 해보기

자동화 후보 업무 세 가지 — "매일 아침 재고 리포트 보내기", "결제 실패 시 조건별로 다르게 대응하기", "신규 문의 접수하면 담당자에게 알리기" — 각각 노코드와 코드 중 무엇이 맞는지 고르고 이유를 적어보세요.

<details>
<summary>정답 보기</summary>

"매일 아침 재고 리포트 보내기"와 "신규 문의 접수하면 담당자에게 알리기"는 노코드(n8n)가 맞습니다. 정해진 절차를 그대로 반복하는 일이라 화면에서 블록을 이어 붙이는 것만으로 충분합니다.

"결제 실패 시 조건별로 다르게 대응하기"는 코드(LangGraph)가 더 맞습니다. 실패 사유에 따라 분기가 여러 갈래로 복잡해지고, 그 로직을 테스트로 검증해야 하는 경우가 많기 때문입니다.

</details>

### 해보기

공유 시나리오를 노드 6~8개로 쪼개 표로 적어보세요. 각 노드의 입력과 출력도 함께 적습니다.

<details>
<summary>정답 보기</summary>

모범 답안 예:

| 노드 | 입력 | 출력 |
|---|---|---|
| 문의 도착 | 메일 원문 | 이벤트 |
| 분류 | 이벤트 | 카테고리 |
| 문서 검색 | 카테고리 | 관련 문서 |
| 초안 작성 | 문의 + 관련 문서 | 답변 초안 |
| 담당자 승인 | 답변 초안 | 승인/반려 |
| 발송 | 승인된 답변 | 발송 결과 |

노드를 이렇게 쪼개 두면, 앞 편(3-4-multi-agent-structure)에서 본 역할 구분과 거의 그대로 겹친다는 것을 알 수 있습니다.

</details>

### 해보기

n8n과 LangGraph의 성격 차이를 아래 대조표로 직접 채워보세요. (만드는 방식 / 잘 맞는 상황 / 변경할 때 손대는 대상)

<details>
<summary>정답 보기</summary>

| 항목 | n8n | LangGraph |
|---|---|---|
| 만드는 방식 | 화면에서 블록을 이어 붙임 | 코드로 노드·연결을 선언 |
| 잘 맞는 상황 | 절차가 정해져 있고 변경이 잦음 | 조건 분기가 복잡하고 테스트가 필요함 |
| 변경할 때 손대는 대상 | 화면의 블록 배치 | 소스 코드 |

</details>

## 6. 핵심 정리 및 스스로 점검

**핵심 정리**

- n8n은 화면에서 블록을 이어 붙이는 노코드 워크플로 도구다
- LangGraph는 코드로 그래프를 선언하는 프레임워크다
- 두 도구 모두 노드와 연결로 자동화를 표현한다는 공통점이 있다
- 변경이 잦고 비개발자도 손대야 하면 노코드, 조건 분기가 복잡하고 테스트가 필요하면 코드가 어울린다
- 이 레슨의 코드는 읽기용이며, 실제로 손으로 돌려보는 것은 개강 후 본 과정에서 다룬다

**이 레슨의 단어**

| 단어 | 뜻 |
|------|-----|
| 노코드 | 화면에서 블록을 이어 붙여 만드는 방식, 코드를 직접 쓰지 않음 |
| 워크플로 | 자동화가 실행되는 전체 흐름 |
| 노드 | 워크플로에서 하나의 작업을 나타내는 상자 |
| 연결 | 한 노드에서 다음 노드로 가는 선 |
| 그래프 | 노드와 연결로 이루어진 전체 구조 |
| 분기 | 조건에 따라 다른 노드로 갈라지는 지점 |
| 실행 이력 | 워크플로가 언제 어디까지 실행됐는지 남긴 기록 |

**스스로 점검**

1. n8n과 LangGraph는 겉모습이 다른데도 왜 "같은 노드·연결 개념"이라고 말할 수 있을까요?

<details>
<summary>정답 보기</summary>

두 도구 모두 결국 "무엇을 하는 상자(노드)"와 "다음으로 가는 선(연결)"으로 자동화를 표현하기 때문입니다. 화면에서 이어 붙이는지 코드로 선언하는지는 만드는 방식의 차이일 뿐, 표현하는 구조 자체는 같습니다.

</details>

2. 제목에 "실습 개요"라고 적혀 있는데, 왜 이 레슨에는 설치·계정 생성 안내가 없을까요?

<details>
<summary>정답 보기</summary>

이 레슨은 개강 후 본 과정에서 진행할 실습의 "개요"를 미리 훑어보는 자리이지, 지금 직접 설치해 실습하는 자리가 아니기 때문입니다. n8n·LangGraph 인스턴스 구축은 시간이 많이 들어 사전학습 범위에서 제외되었습니다.

</details>
