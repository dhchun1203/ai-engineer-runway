---
title: "프롬프트 패턴과 구조화 출력"
stepId: 2
moduleId: "2-7"
order: 1
depth: "심화"
estimatedMinutes: 150
hasContent: true
slug: "2-7-prompt-patterns"
---

## 1. 학습 목표

이 레슨을 마치면 다음을 할 수 있습니다.

- 프롬프트를 역할·지시·예시·제약 네 부분으로 나눠 쓸 수 있다
- LLM 응답을 정해진 JSON 형식으로 받아내도록 요청을 설계할 수 있다
- 응답이 형식을 어겼을 때 코드가 이를 감지하고 재시도하도록 만들 수 있다
- API 키를 환경변수로만 다루고 실제 서비스 코드에 안전하게 배치할 수 있다

## 2. 왜 배우나

"잘 되는 것 같은데 가끔 이상한 답이 온다"는 LLM 서비스에서 가장 흔하고 가장 치명적인 문제입니다. 화면에 그대로 붙일 텍스트인데 형식이 흔들리면 그 화면이 깨집니다.

2-4·2-6 프로젝트에서는 LLM 응답을 상품 카드나 채팅 말풍선에 그대로 렌더링합니다. 응답 형식이 예측 가능하지 않으면 프론트엔드 코드는 항상 방어 코드로 뒤덮이게 됩니다.

이 레슨에서는 프롬프트를 구조적으로 설계하고, 응답을 스키마로 검증하는 습관을 만듭니다. 이 습관이 실무에서 LLM을 안정적으로 쓰는 첫 번째 조건입니다.

## 3. 개념 설명

### 🧩 프롬프트의 네 부분

프롬프트는 신입 사원에게 일을 시킬 때 주는 안내문과 같습니다. "당신은 카피라이터입니다"(역할), "홍보 문구를 만드세요"(지시), "이런 형식으로"(예시), "해시태그는 3개까지"(제약) 네 조각으로 나눠서 씁니다.

네 조각을 뒤섞어 한 문장으로 몰아 쓰면 모델이 어느 것이 진짜 규칙인지 헷갈립니다. 조각을 나눠 쓰면 나중에 한 조각만 바꿔서 결과를 조정하기도 쉬워집니다.

<svg data-diagram viewBox="0 0 520 306" role="img" aria-labelledby="dia-prompt-parts-t">
  <title id="dia-prompt-parts-t">프롬프트 한 장은 역할·지시·예시·제약 네 조각으로 나뉜다. 조각을 나눠 쓰면 한 조각만 바꿔서 결과를 조정할 수 있다.</title>
  <text x="258" y="24" textAnchor="middle" fontSize="15" fill="var(--diagram-accent)">프롬프트 한 장</text>
  <rect x="64" y="40" width="396" height="236" fill="var(--diagram-line)" />
  <rect x="60" y="36" width="396" height="236" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <rect x="78" y="54" width="104" height="46" fill="var(--diagram-accent)" />
  <text x="130" y="83" textAnchor="middle" fontSize="16" fill="var(--diagram-on-accent)">역할</text>
  <text x="196" y="83" textAnchor="start" fontSize="15" fill="var(--diagram-ink)">당신은 카피라이터입니다</text>
  <rect x="78" y="110" width="104" height="46" fill="var(--diagram-accent)" />
  <text x="130" y="139" textAnchor="middle" fontSize="16" fill="var(--diagram-on-accent)">지시</text>
  <text x="196" y="139" textAnchor="start" fontSize="15" fill="var(--diagram-ink)">홍보 문구를 만드세요</text>
  <rect x="78" y="166" width="104" height="46" fill="var(--diagram-accent)" />
  <text x="130" y="195" textAnchor="middle" fontSize="16" fill="var(--diagram-on-accent)">예시</text>
  <text x="196" y="195" textAnchor="start" fontSize="15" fill="var(--diagram-ink)">이런 식으로 써 주세요</text>
  <rect x="78" y="222" width="104" height="46" fill="var(--diagram-accent)" />
  <text x="130" y="251" textAnchor="middle" fontSize="16" fill="var(--diagram-on-accent)">제약</text>
  <text x="196" y="251" textAnchor="start" fontSize="15" fill="var(--diagram-ink)">해시태그는 3개까지</text>
  <text x="258" y="296" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">한 조각만 바꿔 결과를 조정한다</text>
</svg>

### 🎯 예시 제공(few-shot)

예시 제공은 원하는 답을 몇 개 미리 보여주고 "이런 식으로"라고 맡기는 방법입니다. 말로 설명하기 어려운 톤이나 형식은 규칙을 나열하는 것보다 예시 두세 개를 보여주는 쪽이 훨씬 정확합니다.

반대로 결과가 이미 안정적이라면 예시 없이도 충분합니다. 예시는 프롬프트를 길게 만들므로 필요한 만큼만 추가합니다.

<svg data-diagram viewBox="0 0 560 236" role="img" aria-labelledby="dia-fewshot-t">
  <title id="dia-fewshot-t">예시 없이 지시만 하면 출력의 형식이 매번 달라진다. 원하는 답 두세 개를 먼저 보여주면 출력이 같은 틀로 고정된다.</title>
  <text x="140" y="24" textAnchor="middle" fontSize="15" fill="var(--diagram-ink)">예시 없이 지시만 하면</text>
  <rect x="34" y="44" width="200" height="36" fill="var(--diagram-line)" />
  <rect x="30" y="40" width="200" height="36" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="130" y="63" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">짧은 한 줄</text>
  <rect x="34" y="88" width="130" height="36" fill="var(--diagram-line)" />
  <rect x="30" y="84" width="130" height="36" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="95" y="107" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">형식이 또 다름</text>
  <rect x="34" y="132" width="220" height="36" fill="var(--diagram-line)" />
  <rect x="30" y="128" width="220" height="36" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="140" y="151" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">설명이 길게 붙는다</text>
  <text x="140" y="214" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">형식이 매번 달라진다</text>
  <line x1="282" y1="14" x2="282" y2="226" stroke="var(--diagram-line)" strokeWidth="2" strokeDasharray="6 6" />
  <text x="415" y="24" textAnchor="middle" fontSize="15" fill="var(--diagram-accent)">예시 2개를 보여주면</text>
  <rect x="314" y="44" width="210" height="36" fill="var(--diagram-line)" />
  <rect x="310" y="40" width="210" height="36" fill="var(--diagram-accent)" />
  <text x="415" y="64" textAnchor="middle" fontSize="16" fill="var(--diagram-on-accent)">예시 1</text>
  <rect x="314" y="88" width="210" height="36" fill="var(--diagram-line)" />
  <rect x="310" y="84" width="210" height="36" fill="var(--diagram-accent)" />
  <text x="415" y="108" textAnchor="middle" fontSize="16" fill="var(--diagram-on-accent)">예시 2</text>
  <line x1="415" y1="124" x2="415" y2="136" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="409,136 421,136 415,146" fill="var(--diagram-ink)" />
  <rect x="314" y="154" width="210" height="40" fill="var(--diagram-line)" />
  <rect x="310" y="150" width="210" height="40" fill="var(--diagram-soft)" stroke="var(--diagram-accent)" strokeWidth="2" />
  <text x="415" y="176" textAnchor="middle" fontSize="16" fill="var(--diagram-ink)">출력이 같은 틀로</text>
  <text x="415" y="214" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">예시가 형식을 고정한다</text>
</svg>

### 📐 구조화 출력(structured output)

구조화 출력은 모델의 답을 자연어 문장이 아니라 정해진 틀(JSON 등)로 받는 것입니다. "JSON으로 답해주세요"라고 부탁만 하고 나중에 텍스트를 파싱하는 방식은 위험합니다 — 모델이 설명을 덧붙이면 파싱이 그 자리에서 깨집니다.

더 안전한 방법은 스키마(zod 같은 도구로 필드 형식을 미리 정의한 것)로 응답을 강제로 검사하는 것입니다. 파싱이 아니라 검증이 핵심입니다.

<svg data-diagram viewBox="0 0 560 252" role="img" aria-labelledby="dia-structured-output-t">
  <title id="dia-structured-output-t">같은 모델 응답을 두 가지로 다르게 다룬다. 텍스트에서 JSON을 잘라내는 방식은 설명 한 줄만 붙어도 깨지지만, 스키마로 검증하면 형식이 어긋난 순간 그 자리에서 잡아낸다.</title>
  <text x="140" y="22" textAnchor="middle" fontSize="15" fill="var(--diagram-ink)">말로만 부탁하고 파싱</text>
  <rect x="24" y="40" width="240" height="56" fill="var(--diagram-line)" />
  <rect x="20" y="36" width="240" height="56" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="140" y="60" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">물론이죠! 결과는 아래와 같습니다</text>
  <text x="140" y="82" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">&#34;name&#34;: &#34;머그컵&#34;</text>
  <line x1="140" y1="96" x2="140" y2="106" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="134,106 146,106 140,116" fill="var(--diagram-ink)" />
  <rect x="24" y="124" width="240" height="44" fill="var(--diagram-line)" />
  <rect x="20" y="120" width="240" height="44" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="140" y="148" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">텍스트에서 JSON 잘라내기</text>
  <line x1="140" y1="168" x2="140" y2="178" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="134,178 146,178 140,188" fill="var(--diagram-ink)" />
  <rect x="24" y="192" width="240" height="48" fill="var(--diagram-line)" />
  <rect x="20" y="188" width="240" height="48" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="140" y="212" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">설명 한 줄이 붙는 순간</text>
  <text x="140" y="232" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">그 자리에서 깨진다</text>
  <line x1="282" y1="12" x2="282" y2="244" stroke="var(--diagram-line)" strokeWidth="2" strokeDasharray="6 6" />
  <text x="420" y="22" textAnchor="middle" fontSize="15" fill="var(--diagram-accent)">스키마로 검증</text>
  <rect x="304" y="40" width="240" height="56" fill="var(--diagram-line)" />
  <rect x="300" y="36" width="240" height="56" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="420" y="60" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">물론이죠! 결과는 아래와 같습니다</text>
  <text x="420" y="82" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">&#34;name&#34;: &#34;머그컵&#34;</text>
  <line x1="420" y1="96" x2="420" y2="106" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="414,106 426,106 420,116" fill="var(--diagram-ink)" />
  <rect x="304" y="124" width="240" height="44" fill="var(--diagram-line)" />
  <rect x="300" y="120" width="240" height="44" fill="var(--diagram-accent)" />
  <text x="420" y="148" textAnchor="middle" fontSize="15" fill="var(--diagram-on-accent)">스키마와 대조한다</text>
  <line x1="420" y1="168" x2="420" y2="178" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="414,178 426,178 420,188" fill="var(--diagram-ink)" />
  <rect x="304" y="192" width="240" height="48" fill="var(--diagram-line)" />
  <rect x="300" y="188" width="240" height="48" fill="var(--diagram-soft)" stroke="var(--diagram-accent)" strokeWidth="2" />
  <text x="420" y="212" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">형식이 어긋나면</text>
  <text x="420" y="232" textAnchor="middle" fontSize="14" fill="var(--diagram-ink)">그 자리에서 잡아낸다</text>
</svg>

### 🔁 검증 실패 시의 선택지

스키마 검증에 실패했다는 것은 모델이 약속을 어겼다는 뜻입니다. 이때 선택지는 세 가지입니다 — 같은 요청을 한 번 더 보내 재시도하기, 미리 정해둔 기본값으로 대체하기, 사람에게 확인을 넘기기입니다.

이 레슨의 예제는 재시도를 씁니다. 전체 흐름은 다음과 같습니다.

```text
요청 조립 → 모델 호출 → 스키마 검증 → 통과하면 사용 / 실패하면 재시도
```

<svg data-diagram viewBox="0 0 560 262" role="img" aria-labelledby="dia-validate-retry-t">
  <title id="dia-validate-retry-t">스키마 검증을 통과하면 결과를 그대로 쓴다. 실패는 모델이 약속을 어겼다는 뜻이고, 이때 다시 호출하거나, 기본값으로 대체하거나, 사람에게 넘기는 세 가지 중 하나를 고른다. 이 레슨의 예제는 재시도를 쓴다.</title>
  <rect x="194" y="18" width="180" height="44" fill="var(--diagram-line)" />
  <rect x="190" y="14" width="180" height="44" fill="var(--diagram-soft)" stroke="var(--diagram-accent)" strokeWidth="2" />
  <text x="280" y="42" textAnchor="middle" fontSize="16" fill="var(--diagram-ink)">스키마 검증</text>
  <line x1="280" y1="58" x2="280" y2="76" stroke="var(--diagram-ink)" strokeWidth="2" />
  <line x1="130" y1="76" x2="430" y2="76" stroke="var(--diagram-ink)" strokeWidth="2" />
  <line x1="130" y1="76" x2="130" y2="88" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="124,88 136,88 130,98" fill="var(--diagram-ink)" />
  <line x1="430" y1="76" x2="430" y2="88" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="424,88 436,88 430,98" fill="var(--diagram-ink)" />
  <text x="122" y="70" textAnchor="end" fontSize="13" fill="var(--diagram-accent)">통과</text>
  <text x="438" y="70" textAnchor="start" fontSize="13" fill="var(--diagram-accent)">실패</text>
  <rect x="44" y="102" width="180" height="44" fill="var(--diagram-line)" />
  <rect x="40" y="98" width="180" height="44" fill="var(--diagram-accent)" />
  <text x="130" y="126" textAnchor="middle" fontSize="16" fill="var(--diagram-on-accent)">그대로 사용</text>
  <rect x="344" y="102" width="180" height="44" fill="var(--diagram-line)" />
  <rect x="340" y="98" width="180" height="44" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="430" y="126" textAnchor="middle" fontSize="16" fill="var(--diagram-ink)">약속을 어겼다</text>
  <line x1="430" y1="142" x2="430" y2="160" stroke="var(--diagram-ink)" strokeWidth="2" />
  <line x1="90" y1="160" x2="470" y2="160" stroke="var(--diagram-ink)" strokeWidth="2" />
  <line x1="90" y1="160" x2="90" y2="172" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="84,172 96,172 90,182" fill="var(--diagram-ink)" />
  <line x1="280" y1="160" x2="280" y2="172" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="274,172 286,172 280,182" fill="var(--diagram-ink)" />
  <line x1="470" y1="160" x2="470" y2="172" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="464,172 476,172 470,182" fill="var(--diagram-ink)" />
  <rect x="14" y="190" width="160" height="60" fill="var(--diagram-line)" />
  <rect x="10" y="186" width="160" height="60" fill="var(--diagram-soft)" stroke="var(--diagram-accent)" strokeWidth="2" />
  <text x="90" y="212" textAnchor="middle" fontSize="15" fill="var(--diagram-ink)">다시 호출한다</text>
  <text x="90" y="234" textAnchor="middle" fontSize="13" fill="var(--diagram-accent)">이 예제의 방식</text>
  <rect x="204" y="190" width="160" height="60" fill="var(--diagram-line)" />
  <rect x="200" y="186" width="160" height="60" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="280" y="212" textAnchor="middle" fontSize="15" fill="var(--diagram-ink)">기본값으로</text>
  <text x="280" y="234" textAnchor="middle" fontSize="15" fill="var(--diagram-ink)">대체한다</text>
  <rect x="394" y="190" width="160" height="60" fill="var(--diagram-line)" />
  <rect x="390" y="186" width="160" height="60" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="470" y="212" textAnchor="middle" fontSize="15" fill="var(--diagram-ink)">사람에게</text>
  <text x="470" y="234" textAnchor="middle" fontSize="15" fill="var(--diagram-ink)">확인을 넘긴다</text>
</svg>

## 4. 실무 예제

**실행: PC 로컬 VS Code.** 아래 명령으로 필요한 패키지를 설치합니다. Node가 아직 없다면 `2-3-typescript-setup` 레슨을 먼저 확인하세요.

```powershell
npm install @anthropic-ai/sdk zod dotenv
npm install -D typescript tsx @types/node
```

`.env` 파일에 아래처럼 키 이름만 저장해둡니다. 값 자리에는 실제 키 대신 자리표시자를 씁니다 — 실제 키 형태 문자열을 코드나 이 문서 어디에도 적지 않습니다.

```text
ANTHROPIC_API_KEY=<발급받은-키>
```

상품 정보를 받아 정해진 JSON 형식(제목, 한 줄 소개, 해시태그 3개)의 홍보 문구를 만들고, 응답을 zod 스키마로 검증한 뒤 실패하면 한 번만 재시도하는 완결 코드입니다.

```typescript
// src/product-copy.ts
import Anthropic from "@anthropic-ai/sdk";
import { z } from "zod";
import "dotenv/config";

// 응답이 반드시 지켜야 하는 형식을 스키마로 강제한다 — 파싱이 아니라 검증
const ProductCopySchema = z.object({
  title: z.string(),
  tagline: z.string(),
  hashtags: z.array(z.string()).length(3),
});

type ProductCopy = z.infer<typeof ProductCopySchema>;

interface ProductInfo {
  name: string;
  features: string[];
}

if (!process.env.ANTHROPIC_API_KEY) {
  console.error("ANTHROPIC_API_KEY가 설정되지 않았습니다. .env 파일을 확인하세요.");
  process.exit(1);
}

const client = new Anthropic({ apiKey: process.env.ANTHROPIC_API_KEY });

// 모델 이름은 날짜 접미사 없이 씁니다.
// 모델 이름은 바뀔 수 있으니 공식 문서에서 현재 이름을 확인하세요.
const MODEL_NAME = "claude-sonnet-5";

function buildPrompt(product: ProductInfo): string {
  return [
    "당신은 이커머스 카피라이터입니다.", // 역할
    "아래 상품 정보로 홍보 문구를 만드세요.", // 지시
    `상품명: ${product.name}`,
    `특징: ${product.features.join(", ")}`,
    "반드시 JSON 형식으로만 답하세요: { title, tagline, hashtags(3개) }", // 제약
  ].join("\n");
}

async function requestProductCopy(product: ProductInfo): Promise<ProductCopy> {
  const message = await client.messages.create({
    model: MODEL_NAME,
    max_tokens: 300,
    messages: [{ role: "user", content: buildPrompt(product) }],
  });

  const textBlock = message.content.find((block) => block.type === "text");
  const raw = textBlock && "text" in textBlock ? textBlock.text : "";
  const parsed = JSON.parse(raw);
  return ProductCopySchema.parse(parsed); // 실패하면 여기서 예외가 던져진다
}

async function generateProductCopy(product: ProductInfo): Promise<ProductCopy> {
  try {
    return await requestProductCopy(product);
  } catch {
    // 검증 실패 시 선택지 중 "재시도"를 택한다 — 딱 한 번만
    try {
      return await requestProductCopy(product);
    } catch (error) {
      throw new Error(`형식 검증에 두 번 연속 실패했습니다: ${error}`);
    }
  }
}

const sample: ProductInfo = {
  name: "무선 이어폰",
  features: ["노이즈 캔슬링", "20시간 배터리", "방수"],
};

generateProductCopy(sample).then((copy) => console.log(copy));
```

기대 응답은 아래와 비슷한 형태입니다.

```json
{
  "title": "하루 종일 조용한 나만의 시간",
  "tagline": "노이즈 캔슬링과 20시간 배터리로 어디서든 몰입하세요",
  "hashtags": ["무선이어폰", "노이즈캔슬링", "방수"]
}
```

실행은 아래 한 줄입니다.

```powershell
npx tsx src/product-copy.ts
```

<details>
<summary>실행 결과 예측해 보기</summary>

**실행 결과**

```text
{
  title: '하루 종일 조용한 나만의 시간',
  tagline: '노이즈 캔슬링과 20시간 배터리로 어디서든 몰입하세요',
  hashtags: [ '무선이어폰', '노이즈캔슬링', '방수' ]
}
```

문구는 모델이 실행마다 새로 지어내므로 매번 다릅니다 — 위 내용은 한 예시입니다. 키 3개와 해시태그 3개라는 형태만 스키마 검증이 보장합니다.

작은따옴표가 붙은 이 모양은 `console.log`가 JavaScript 객체를 출력하는 방식입니다 — JSON 문자열이 아니라 검증을 통과한 객체가 찍힌 것입니다.

</details>

### 해보기

`buildPrompt`에서 "반드시 JSON 형식으로만 답하세요" 제약 한 줄을 지우고 다시 실행해보세요. 응답 형식이 어떻게 흔들리는지 관찰합니다. API 키가 없다면 `buildPrompt(sample)`만 호출해 프롬프트 문자열이 어떻게 조립되는지 콘솔에 출력해보는 것으로 대신할 수 있습니다.

<details>
<summary>정답 보기</summary>

제약을 지우면 모델이 종종 설명을 JSON 앞뒤에 덧붙이거나, 순수 JSON이 아닌 마크다운 코드블록으로 감싸서 답합니다. `JSON.parse(raw)`가 그 자리에서 실패하고 `catch` 블록의 재시도 경로가 실행되는 것을 확인할 수 있습니다.

제약 문장은 "형식을 지켜라"는 지시를 모델이 가장 마지막에 읽는 규칙으로 만들어주는 역할을 합니다. 프롬프트 끝에 둘수록 모델이 더 잘 지키는 경향이 있습니다.

</details>

### 해보기

`ProductCopySchema`의 `hashtags`에 각 해시태그가 10자 이내여야 한다는 제약을 추가하고, 상품 특징을 일부러 길게 바꿔 검증 실패를 만들어보세요. 재시도 경로가 실제로 실행되는지 콘솔 로그로 확인합니다.

<details>
<summary>정답 보기</summary>

```typescript
const ProductCopySchema = z.object({
  title: z.string(),
  tagline: z.string(),
  hashtags: z.array(z.string().max(10)).length(3),
});
```

모델이 만든 해시태그가 10자를 넘으면 검증이 예외를 던지고, `generateProductCopy`의 `catch` 블록이 `requestProductCopy`를 한 번 더 호출합니다. 재시도에서도 실패하면 오류 메시지와 함께 함수가 종료됩니다.

</details>

### 해보기

`buildPrompt` 안에 예시 제공(few-shot)을 두 개 추가해보세요. 이미 완성된 홍보 문구 JSON 예시 두 개를 프롬프트 문자열 끝에 붙이고, 톤이 어떻게 더 일관되게 바뀌는지 비교합니다.

<details>
<summary>정답 보기</summary>

```typescript
function buildPrompt(product: ProductInfo): string {
  const examples = [
    '{"title": "손끝에서 시작하는 몰입", "tagline": "가볍고 정확한 타건감", "hashtags": ["키보드", "타건감", "휴대용"]}',
    '{"title": "책상 위 작은 정원", "tagline": "낮은 소음, 넓은 화면", "hashtags": ["모니터", "저소음", "넓은화면"]}',
  ];
  return [
    "당신은 이커머스 카피라이터입니다.",
    "아래는 예시 두 개입니다:",
    examples.join("\n"),
    `상품명: ${product.name}`,
    `특징: ${product.features.join(", ")}`,
    "반드시 JSON 형식으로만 답하세요: { title, tagline, hashtags(3개) }",
  ].join("\n");
}
```

예시 두 개를 넣으면 문장 길이·어조·해시태그 스타일이 예시와 비슷하게 맞춰지는 경향이 나타납니다. 예시가 곧 암묵적인 규칙이 되는 것입니다.

</details>

## 5. 실무 팁

- 키는 코드가 아니라 환경변수에 둡니다. `.env` 파일은 저장소에 절대 올리지 않고 `.gitignore`에 추가해둡니다.
- 사용자 입력을 프롬프트에 그대로 이어 붙이면 앞서 준 지시가 덮어써질 수 있습니다. 사용자 입력은 구분자나 별도 역할로 감싸서 지시와 분리합니다.
- 모델 응답을 화면에 그대로 넣기 전에 항상 스키마로 검증합니다. 검증 없이 렌더링하면 필드 누락이 화면 깨짐으로 바로 이어집니다.
- 재시도는 무한정 반복하지 않습니다. 한두 번으로 제한하고, 그래도 실패하면 사용자에게 알리거나 기본값으로 대체합니다.

## 6. 핵심 정리 및 스스로 점검

**핵심 정리**

- 프롬프트는 역할·지시·예시·제약 네 부분으로 나눠 쓰면 모델이 규칙을 더 잘 지킨다
- 구조화 출력은 파싱이 아니라 스키마 검증으로 강제해야 안전하다
- 검증 실패 시 선택지는 재시도·기본값 대체·사람에게 넘기기 세 가지다
- LLM API 키는 항상 환경변수로만 다루고 코드에 리터럴로 적지 않는다
- 모델 이름은 날짜 접미사 없이 쓰고 공식 문서에서 현재 이름을 확인한다

**이 레슨의 단어**

| 단어 | 뜻 |
|------|-----|
| 프롬프트 | 모델에게 보내는 요청 텍스트 전체 |
| 시스템 지시 | 모델의 역할과 행동 규칙을 정하는 프롬프트 조각 |
| 예시 제공(few-shot) | 원하는 답의 예시를 미리 보여주고 나머지를 맡기는 방법 |
| 구조화 출력 | 자연어 대신 정해진 형식(JSON 등)으로 응답을 받는 것 |
| 스키마 검증 | 응답이 정해진 형식을 지켰는지 코드로 확인하는 것 |
| 토큰 | 모델이 텍스트를 처리하는 최소 단위, 비용·길이 제한의 기준 |
| 온도(temperature) | 응답의 다양성·무작위성을 조절하는 값 |

**스스로 점검**

1. 구조화 출력을 "JSON으로 답해주세요"라는 요청만으로 끝내지 않고 `zod` 스키마로 다시 검증하는 이유는 무엇일까요?

<details>
<summary>정답 보기</summary>

모델이 요청을 대체로 따르더라도 항상 완벽하게 지킨다는 보장은 없기 때문입니다. 설명을 덧붙이거나 필드를 빠뜨리는 등 형식이 어긋나는 경우가 실제로 발생하고, 그 응답을 검증 없이 그대로 화면에 넣으면 서비스가 깨집니다.

스키마 검증은 "모델을 믿지 않고 코드로 확인한다"는 방어선입니다.

</details>

2. 검증 실패 시 재시도 대신 곧바로 사람에게 넘기는 편이 더 나은 상황은 어떤 경우일지 생각해보세요.

<details>
<summary>정답 보기</summary>

재시도해도 계속 같은 이유로 실패할 가능성이 높은 상황이거나, 잘못된 응답이 사용자에게 그대로 노출됐을 때 피해가 큰 상황(결제 금액, 법적 문구 등)에서는 사람 검토로 넘기는 편이 안전합니다.

전자는 프롬프트 자체의 설계 문제일 가능성이 크므로 재시도로는 해결되지 않습니다.

</details>
