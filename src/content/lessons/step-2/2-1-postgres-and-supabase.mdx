---
title: "PostgreSQL과 Supabase 활용"
stepId: 2
moduleId: "2-1"
order: 1
depth: "심화"
estimatedMinutes: 150
hasContent: true
slug: "2-1-postgres-and-supabase"
---

## 1. 학습 목표

이 레슨을 마치면 다음을 할 수 있습니다.

- PostgreSQL이 파일 저장과 어떻게 다른지, 왜 서버로 동작하는 데이터베이스가 필요한지 설명할 수 있다
- Supabase가 관리형 PostgreSQL 위에 무엇을 자동으로 얹어주는지 말할 수 있다
- 스키마로 테이블을 구분해서 만들고, 진도 데이터와 연습 데이터를 안전하게 분리할 수 있다
- 행 수준 보안(RLS)이 왜 필요한지 설명하고, 어떤 상황에서 켜야 하는지 판단할 수 있다

## 2. 왜 배우나

1-4에서 배운 SQL은 실제 서비스에서도 그대로 씁니다. 다만 지금까지는 브라우저 SQL 에디터에서 연습했다면, 실제 서비스는 관리형 PostgreSQL 위에서 이 쿼리들이 돌아갑니다.

Step 2 뒤쪽에서 만들 AI 쇼핑몰 프로젝트(2-4·2-5·2-6)는 전부 이 데이터베이스 위에 데이터를 쌓습니다. 지금 스키마와 보안 개념을 잡아두면 프로젝트에서 헤매지 않습니다.

## 3. 개념 설명

### 🐘 PostgreSQL은 서버로 도는 데이터베이스

PostgreSQL(포스트그레스, 오픈소스 관계형 데이터베이스)은 컴퓨터 한 대에 계속 켜져 있는 프로그램입니다. 파일과 달리 여러 사람·여러 서비스가 동시에 접속해 데이터를 읽고 씁니다.

1-4에서 이미 테이블·행·열·기본키를 다뤘으므로 여기서는 반복하지 않습니다. 이 레슨은 그 위에서 "어디에 놓고 어떻게 관리하는가"를 다룹니다.

<svg data-diagram viewBox="0 0 560 220" role="img" aria-labelledby="dia-server-db-t">
  <title id="dia-server-db-t">파일로 저장하면 한 번에 한쪽만 다룰 수 있어 다른 앱은 기다려야 하지만, 서버로 도는 데이터베이스는 여러 앱이 동시에 접속해 읽고 쓴다.</title>
  <text x="134" y="24" textAnchor="middle" fontSize="15" fill="var(--diagram-accent)">파일로 저장하면</text>
  <rect x="24" y="52" width="90" height="36" fill="var(--diagram-line)" />
  <rect x="20" y="48" width="90" height="36" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="65" y="71" textAnchor="middle" fontSize="15" fill="var(--diagram-ink)">앱 1</text>
  <rect x="24" y="112" width="90" height="36" fill="var(--diagram-line)" />
  <rect x="20" y="108" width="90" height="36" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="65" y="131" textAnchor="middle" fontSize="15" fill="var(--diagram-ink)">앱 2</text>
  <line x1="114" y1="66" x2="144" y2="66" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="144,60 156,66 144,72" fill="var(--diagram-ink)" />
  <line x1="114" y1="126" x2="156" y2="126" stroke="var(--diagram-line)" strokeWidth="2" strokeDasharray="6 6" />
  <line x1="128" y1="119" x2="142" y2="133" stroke="var(--diagram-ink)" strokeWidth="2" />
  <line x1="142" y1="119" x2="128" y2="133" stroke="var(--diagram-ink)" strokeWidth="2" />
  <rect x="164" y="52" width="100" height="96" fill="var(--diagram-line)" />
  <rect x="160" y="48" width="100" height="96" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="210" y="104" textAnchor="middle" fontSize="18" fill="var(--diagram-ink)">파일</text>
  <text x="134" y="196" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">여럿이 동시에 다루기 어렵다</text>
  <text x="420" y="24" textAnchor="middle" fontSize="15" fill="var(--diagram-accent)">서버로 도는 데이터베이스</text>
  <rect x="300" y="52" width="90" height="32" fill="var(--diagram-line)" />
  <rect x="296" y="48" width="90" height="32" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="341" y="69" textAnchor="middle" fontSize="15" fill="var(--diagram-ink)">앱 1</text>
  <rect x="300" y="100" width="90" height="32" fill="var(--diagram-line)" />
  <rect x="296" y="96" width="90" height="32" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="341" y="117" textAnchor="middle" fontSize="15" fill="var(--diagram-ink)">앱 2</text>
  <rect x="300" y="148" width="90" height="32" fill="var(--diagram-line)" />
  <rect x="296" y="144" width="90" height="32" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="341" y="165" textAnchor="middle" fontSize="15" fill="var(--diagram-ink)">앱 3</text>
  <line x1="390" y1="64" x2="420" y2="64" stroke="var(--diagram-line)" strokeWidth="2" />
  <line x1="390" y1="112" x2="420" y2="112" stroke="var(--diagram-line)" strokeWidth="2" />
  <line x1="390" y1="160" x2="420" y2="160" stroke="var(--diagram-line)" strokeWidth="2" />
  <line x1="420" y1="64" x2="420" y2="160" stroke="var(--diagram-line)" strokeWidth="2" />
  <line x1="420" y1="112" x2="438" y2="112" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="438,106 450,112 438,118" fill="var(--diagram-ink)" />
  <rect x="458" y="50" width="96" height="132" fill="var(--diagram-line)" />
  <rect x="454" y="46" width="96" height="132" fill="var(--diagram-accent)" />
  <text x="502" y="108" textAnchor="middle" fontSize="15" fill="var(--diagram-on-accent)">Postgres</text>
  <text x="502" y="132" textAnchor="middle" fontSize="14" fill="var(--diagram-on-accent)">서버</text>
  <text x="420" y="196" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">여럿이 동시에 읽고 쓴다</text>
</svg>

### ☁️ Supabase = 관리형 Postgres + API + 인증

Supabase(서비스형 백엔드 플랫폼)는 PostgreSQL 서버를 대신 운영해주고, 테이블을 만들면 자동으로 API와 인증 기능까지 붙여줍니다.

직접 서버를 설치하고 백업하고 보안 패치를 챙기는 대신, 대시보드에서 테이블만 설계하면 나머지는 Supabase가 관리합니다.

<svg data-diagram viewBox="0 0 560 266" role="img" aria-labelledby="dia-supabase-t">
  <title id="dia-supabase-t">Supabase는 맨 아래 PostgreSQL 서버를 대신 운영하고, 그 위에 표를 만들면 자동으로 붙는 API와 인증을 얹어준다.</title>
  <text x="180" y="26" textAnchor="middle" fontSize="15" fill="var(--diagram-accent)">Supabase가 얹어주는 것</text>
  <rect x="64" y="48" width="240" height="50" fill="var(--diagram-line)" />
  <rect x="60" y="44" width="240" height="50" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="180" y="76" textAnchor="middle" fontSize="17" fill="var(--diagram-ink)">인증</text>
  <rect x="64" y="110" width="240" height="50" fill="var(--diagram-line)" />
  <rect x="60" y="106" width="240" height="50" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="180" y="138" textAnchor="middle" fontSize="17" fill="var(--diagram-ink)">자동 API</text>
  <rect x="64" y="172" width="240" height="54" fill="var(--diagram-line)" />
  <rect x="60" y="168" width="240" height="54" fill="var(--diagram-accent)" />
  <text x="180" y="202" textAnchor="middle" fontSize="18" fill="var(--diagram-on-accent)">PostgreSQL</text>
  <line x1="304" y1="69" x2="336" y2="69" stroke="var(--diagram-line)" strokeWidth="2" />
  <text x="344" y="74" textAnchor="start" fontSize="13" fill="var(--diagram-ink)">로그인 처리까지 자동</text>
  <line x1="304" y1="131" x2="336" y2="131" stroke="var(--diagram-line)" strokeWidth="2" />
  <text x="344" y="136" textAnchor="start" fontSize="13" fill="var(--diagram-ink)">표를 만들면 API가 생김</text>
  <line x1="304" y1="195" x2="336" y2="195" stroke="var(--diagram-line)" strokeWidth="2" />
  <text x="344" y="200" textAnchor="start" fontSize="13" fill="var(--diagram-ink)">설치·백업·보안 패치 대행</text>
  <text x="280" y="252" textAnchor="middle" fontSize="13" fill="var(--diagram-accent)">우리는 테이블 설계만 하면 된다</text>
</svg>

### 🗄️ 스키마 — 테이블을 담는 서랍

스키마(schema, 여러 테이블을 묶어 담는 이름 붙은 서랍)는 1-4에서 이미 다룬 개념입니다.

이 사이트의 진도 데이터는 `public` 스키마에, 이 레슨의 연습용 테이블은 `practice` 스키마에 나눠 둡니다.

<svg data-diagram viewBox="0 0 560 278" role="img" aria-labelledby="dia-schema-t">
  <title id="dia-schema-t">데이터베이스 하나 안에 서랍이 둘 있다. public 서랍에는 손대지 않을 진도 데이터가, practice 서랍에는 이 레슨의 연습용 표가 들어간다.</title>
  <text x="280" y="26" textAnchor="middle" fontSize="15" fill="var(--diagram-accent)">데이터베이스 하나 안의 서랍 둘</text>
  <rect x="94" y="48" width="380" height="190" fill="var(--diagram-line)" />
  <rect x="90" y="44" width="380" height="190" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <rect x="110" y="68" width="340" height="66" fill="var(--diagram-line)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="280" y="96" textAnchor="middle" fontSize="17" fill="var(--diagram-ink)">public</text>
  <text x="280" y="120" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">진도 데이터 — 건드리지 않는다</text>
  <rect x="110" y="150" width="340" height="66" fill="var(--diagram-accent)" />
  <text x="280" y="178" textAnchor="middle" fontSize="17" fill="var(--diagram-on-accent)">practice</text>
  <text x="280" y="202" textAnchor="middle" fontSize="13" fill="var(--diagram-on-accent)">연습용 표 — 여기서만 실습한다</text>
  <text x="280" y="264" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">서랍이 다르면 이름이 겹쳐도 섞이지 않는다</text>
</svg>

### 🔒 행 수준 보안(RLS) 소개

행 수준 보안(RLS, Row Level Security)은 테이블의 행 하나하나에 "누가 이 행을 볼 수 있는가"라는 규칙을 거는 기능입니다.

RLS를 켜지 않으면 접속 권한이 있는 모두가 모든 행을 볼 수 있습니다. 실제 서비스 테이블을 공개하기 전에는 반드시 RLS부터 켭니다.

<svg data-diagram viewBox="0 0 560 246" role="img" aria-labelledby="dia-rls-t">
  <title id="dia-rls-t">행 수준 보안을 끄면 접속한 사용자에게 표의 모든 행이 그대로 보이고, 켜면 규칙에 맞는 자기 행만 보이고 나머지는 가려진다.</title>
  <text x="140" y="24" textAnchor="middle" fontSize="15" fill="var(--diagram-accent)">RLS 꺼짐</text>
  <rect x="24" y="108" width="86" height="40" fill="var(--diagram-line)" />
  <rect x="20" y="104" width="86" height="40" fill="var(--diagram-accent)" />
  <text x="63" y="130" textAnchor="middle" fontSize="14" fill="var(--diagram-on-accent)">사용자 B</text>
  <line x1="110" y1="124" x2="134" y2="124" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="134,118 146,124 134,130" fill="var(--diagram-ink)" />
  <rect x="160" y="48" width="104" height="30" fill="var(--diagram-line)" />
  <rect x="156" y="44" width="104" height="30" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="208" y="64" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">A의 행</text>
  <rect x="160" y="86" width="104" height="30" fill="var(--diagram-line)" />
  <rect x="156" y="82" width="104" height="30" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="208" y="102" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">B의 행</text>
  <rect x="160" y="124" width="104" height="30" fill="var(--diagram-line)" />
  <rect x="156" y="120" width="104" height="30" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="208" y="140" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">C의 행</text>
  <rect x="160" y="162" width="104" height="30" fill="var(--diagram-line)" />
  <rect x="156" y="158" width="104" height="30" fill="var(--diagram-soft)" stroke="var(--diagram-ink)" strokeWidth="2" />
  <text x="208" y="178" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">D의 행</text>
  <text x="140" y="224" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">모든 행이 다 보인다</text>
  <text x="420" y="24" textAnchor="middle" fontSize="15" fill="var(--diagram-accent)">RLS 켜짐</text>
  <rect x="304" y="108" width="86" height="40" fill="var(--diagram-line)" />
  <rect x="300" y="104" width="86" height="40" fill="var(--diagram-accent)" />
  <text x="343" y="130" textAnchor="middle" fontSize="14" fill="var(--diagram-on-accent)">사용자 B</text>
  <line x1="390" y1="124" x2="414" y2="124" stroke="var(--diagram-ink)" strokeWidth="2" />
  <polygon points="414,118 426,124 414,130" fill="var(--diagram-ink)" />
  <rect x="436" y="44" width="104" height="30" fill="var(--diagram-line)" stroke="var(--diagram-ink)" strokeWidth="2" strokeDasharray="5 5" />
  <text x="488" y="64" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">가려짐</text>
  <rect x="440" y="86" width="104" height="30" fill="var(--diagram-line)" />
  <rect x="436" y="82" width="104" height="30" fill="var(--diagram-accent)" />
  <text x="488" y="102" textAnchor="middle" fontSize="13" fill="var(--diagram-on-accent)">B의 행</text>
  <rect x="436" y="120" width="104" height="30" fill="var(--diagram-line)" stroke="var(--diagram-ink)" strokeWidth="2" strokeDasharray="5 5" />
  <text x="488" y="140" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">가려짐</text>
  <rect x="436" y="158" width="104" height="30" fill="var(--diagram-line)" stroke="var(--diagram-ink)" strokeWidth="2" strokeDasharray="5 5" />
  <text x="488" y="178" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">가려짐</text>
  <text x="420" y="224" textAnchor="middle" fontSize="13" fill="var(--diagram-ink)">내 행만 보인다</text>
</svg>

## 4. 실무 예제

**실행: 각 블록의 "실행" 버튼.** 이 레슨은 브라우저 안 임시 데이터베이스에서 전부 실습합니다 — **위에서 아래로 순서대로** 누르세요. 처음부터 다시 하려면 맨 아래 "실습 되돌리기" 블록을 먼저 실행합니다. (Supabase 대시보드의 SQL 에디터에 붙여넣어도 똑같이 동작합니다.)

⚠️ 이 레슨의 모든 SQL은 `practice` 스키마 안에서만 실행합니다 — 이 사이트의 진도 데이터가 들어 있는 `public.progress`는 건드리지 않습니다.

<RunSQL>

```sql
-- 이전 실습이 남아 있어도 깨끗이 비우고 시작합니다(여러 번 눌러도 안전).
drop schema if exists practice cascade;
create schema practice;

create table practice.ai_products (
  id serial primary key,
  name text not null,
  price int not null,
  category text,
  created_at timestamptz default now()
);
```

</RunSQL>

이 블록을 실행하면 `practice` 서랍 안에 `ai_products` 표가 생깁니다("실행 완료 — 표와 데이터가 준비되었습니다."). 진도 데이터가 있는 `public` 스키마는 전혀 건드리지 않습니다.

<RunSQL>

```sql
insert into practice.ai_products (name, price, category) values
  ('AI 스피커', 89000, '전자기기'),
  ('무선 이어폰', 129000, '전자기기'),
  ('원두 커피 500g', 18000, '식품'),
  ('요가 매트', 32000, '생활용품'),
  ('스마트워치', 259000, '전자기기');
```

</RunSQL>

상품 5개가 들어갑니다.

<RunSQL>

```sql
select name, price, category
from practice.ai_products
where category = '전자기기'
order by price desc
limit 5;

select category, count(*) as 상품수, avg(price) as 평균가격
from practice.ai_products
group by category
order by 평균가격 desc;
```

</RunSQL>

첫 번째 조회는 전자기기 중 비싼 순으로 상품을 보여줍니다. 두 번째 조회는 카테고리별 상품 수와 평균 가격을 계산합니다. (결과 표가 순서대로 두 개 나옵니다.)

<details>
<summary>실행 결과 예측해 보기</summary>

**실행 결과**

```text
-- 첫 번째 조회
name        | price  | category
------------+--------+----------
스마트워치  | 259000 | 전자기기
무선 이어폰 | 129000 | 전자기기
AI 스피커   |  89000 | 전자기기

-- 두 번째 조회
category | 상품수 | 평균가격
---------+--------+------------------------
전자기기 |      3 | 159000.000000000000
생활용품 |      1 | 32000.000000000000
식품     |      1 | 18000.0000000000000000
```

`limit 5`를 걸었지만 전자기기가 3개뿐이라 3행만 나옵니다. `avg`의 긴 소수 자릿수는 Postgres가 계산 과정에서 정한 표시 자릿수입니다.

두 문장을 한 번에 실행하면 에디터는 마지막 조회 결과만 보여줍니다 — 하나씩 실행하면 각각 확인할 수 있습니다.

</details>

### 해보기

전자기기 카테고리만 골라 평균 가격을 구하는 쿼리를 작성해보세요.

<details>
<summary>정답 보기</summary>

<RunSQL>

```sql
select avg(price) as 평균가격
from practice.ai_products
where category = '전자기기';
```

</RunSQL>

`where`로 먼저 전자기기만 걸러낸 뒤 `avg`로 평균을 계산합니다. 준비 데이터 기준 약 159,000원이 나옵니다.

</details>

### 해보기

`practice.ai_products`에 `discount_rate`(할인율, int) 컬럼을 추가하고 전체 상품에 10을 채워보세요.

<details>
<summary>정답 보기</summary>

<RunSQL>

```sql
alter table practice.ai_products add column discount_rate int;

update practice.ai_products set discount_rate = 10;
```

</RunSQL>

`alter table`로 컬럼을 먼저 추가한 뒤, `update`로 기존 행 전체에 같은 값을 채웁니다. `where` 없이 실행하면 모든 행이 대상이 됩니다.

</details>

### 해보기

가격이 비싼 순으로 상위 3개 상품만 조회해보세요.

<details>
<summary>정답 보기</summary>

<RunSQL>

```sql
select name, price
from practice.ai_products
order by price desc
limit 3;
```

</RunSQL>

`order by price desc`로 비싼 순 정렬한 뒤 `limit 3`으로 앞의 3행만 남깁니다. 스마트워치·무선 이어폰·AI 스피커 순으로 나옵니다.

</details>

## 5. 실무 팁

- 실제 애플리케이션 코드에서는 사용자 입력을 문자열로 이어 붙여 쿼리를 만들지 않습니다 — 값을 따로 넘기는 파라미터 바인딩 방식을 씁니다.
- 테이블을 공개 API로 열기 전에는 반드시 행 수준 보안(RLS)부터 켭니다. 나중에 켜면 그 사이 데이터가 이미 노출됐을 수 있습니다.
- Supabase 무료 티어는 일정 기간 접속이 없으면 프로젝트가 일시정지될 수 있고, 저장 용량에도 한도가 있습니다.
- 연습이 끝나면(또는 처음부터 다시 하려면) 아래 "실습 되돌리기" 블록을 실행해 `practice` 서랍을 비웁니다.

<RunSQL>

```sql
drop schema if exists practice cascade;
```

</RunSQL>

이 문장은 `practice` 스키마만 대상으로 하므로 사이트 진도 데이터에는 아무 영향이 없습니다.

## 6. 핵심 정리 및 스스로 점검

**핵심 정리**

- PostgreSQL은 파일이 아니라 서버로 계속 켜져 있는 관계형 데이터베이스다
- Supabase는 PostgreSQL을 관리해주고 API·인증까지 자동으로 붙여준다
- 스키마로 진도 데이터(`public`)와 연습 데이터(`practice`)를 분리한다
- 행 수준 보안(RLS)은 행 단위로 접근 권한을 거는 기능이며, 공개 전 반드시 켠다
- 애플리케이션 코드는 문자열 결합 대신 파라미터 바인딩으로 쿼리를 만든다

**이 레슨의 단어**

| 단어 | 뜻 |
|------|-----|
| 관리형 데이터베이스 | 서버 설치·백업·보안 패치를 대신 관리해주는 데이터베이스 서비스 |
| 스키마 | 여러 테이블을 묶어 담는 이름 붙은 서랍 |
| 행 수준 보안(RLS) | 행 단위로 누가 볼 수 있는지 규칙을 거는 기능 |
| 집계 함수 | COUNT·AVG처럼 여러 행을 하나의 값으로 계산하는 함수 |
| 파라미터 바인딩 | 쿼리와 사용자 입력값을 분리해서 안전하게 넘기는 방식 |
| 커넥션 | 애플리케이션과 데이터베이스 사이의 연결 하나 |
| 무료 티어 | 비용 없이 쓸 수 있는 제한된 사용량 구간 |

**스스로 점검**

1. 이 레슨의 모든 SQL 앞에 `practice.` 접두사를 붙이는 이유는 무엇일까요?

<details>
<summary>힌트 보기</summary>

접두사를 붙이지 않으면 테이블이 기본으로 어느 서랍(스키마)에 만들어질지 떠올려보세요. 그 서랍에는 원래 무엇이 들어 있었나요?

</details>

<details>
<summary>정답 보기</summary>

접두사가 없으면 테이블이 `public` 스키마에 만들어져, 이 사이트의 진도 데이터가 담긴 표들과 같은 공간에 섞입니다.

`practice.` 접두사는 연습용 테이블을 별도 서랍에 가둬, 실수로 진도 데이터를 건드리거나 이름이 겹치는 사고를 막습니다.

</details>

2. RLS를 켜지 않은 테이블을 공개 API로 노출하면 어떤 문제가 생길 수 있을까요?

<details>
<summary>힌트 보기</summary>

Supabase는 테이블을 만들면 자동으로 API를 붙여준다고 했습니다. 행 단위로 "누가 볼 수 있는지" 규칙이 아예 없다면, 그 API에 접근하는 사람이 무엇까지 할 수 있을지 떠올려보세요.

</details>

<details>
<summary>정답 보기</summary>

Supabase는 테이블을 만들면 자동으로 API를 붙여주므로, RLS 없이 공개하면 그 API에 접근할 수 있는 누구나 테이블의 모든 행을 읽거나 바꿀 수 있습니다.

행 단위 권한 규칙이 없다는 뜻이라, 다른 사용자의 데이터까지 그대로 노출되는 정보 유출로 이어질 수 있습니다.

</details>
