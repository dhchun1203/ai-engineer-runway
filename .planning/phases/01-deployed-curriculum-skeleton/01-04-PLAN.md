---
phase: 01-deployed-curriculum-skeleton
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/app/page.tsx
  - src/app/step/[stepId]/page.tsx
  - src/app/lesson/[lessonId]/page.tsx
  - src/app/not-found.tsx
  - src/components/step-card.tsx
  - src/components/depth-badge.tsx
  - src/components/estimated-time.tsx
  - src/components/module-accordion.tsx
  - src/components/lesson-nav.tsx
autonomous: true
requirements: [CONT-01, CONT-04, UX-01, UX-02]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "학습자가 홈에서 Step 카드 3장을 보고, Step 페이지에서 모듈 아코디언을 열어 그 모듈의 레슨 목록을 확인하고, 레슨 페이지로 이동할 수 있다 (CONT-01, D-07)"
    - "35개 레슨 전부가 실제로 접근 가능한 정적 라우트를 가진다 — 본문이 없는 34개도 404가 아니다 (RESEARCH Pitfall 2)"
    - "본문이 없는 레슨 페이지는 UI-SPEC Copywriting Contract의 empty state 문구(안내 제목 + 파일럿 유도 본문)를 렌더한다"
    - "모든 레슨 카드·헤더에 깊이 배지(심화/개요)와 예상 소요시간이 함께 표시된다 (CONT-04, D-13)"
    - "심화 배지는 해당 레슨이 속한 Step의 상징 색을, 개요 배지는 Step 색과 무관한 중성 슬레이트 색을 쓴다 — 깊이와 Step 정체성이 시각적으로 구분된다 (D-04, UI-SPEC)"
    - "홈의 Step 카드 3장은 고정 카디널리티이며 진행률 슬롯이 0%로 렌더된다 — Phase 2가 이 자리에 실데이터를 넣는다 (D-07)"
    - "Step 카드 그리드는 좁은 화면에서 3열이 1열로 스택되며 컨테이너를 넘치지 않는다 (UX-02)"
    - "잘못된 stepId나 존재하지 않는 lessonId로 접근하면 UI-SPEC Copywriting Contract의 error state 문구와 커리큘럼 홈 복귀 CTA가 있는 not-found 페이지가 렌더된다"
    - "404 페이지와 모든 정적 요소는 런타임 데이터 로드 없이 렌더되므로 데이터 로드 실패 상태가 존재하지 않는다"
    - "첫 레슨의 이전 버튼과 마지막(35번째) 레슨의 다음 버튼은 숨기지 않고 비활성 상태로 렌더되어 레이아웃이 흔들리지 않는다 (UI-SPEC 사용자 확정)"
    - "19개 모듈 아코디언과 35개 레슨 목록이 표준 볼륨으로 배치되고, 배지 값은 심화/개요와 약 N분·약 N시간 표기 계약을 따른다"
    - "탭 가능한 모든 요소(아코디언 헤더, 레슨 링크, 이전/다음 버튼)의 히트 영역이 최소 44px다 (UX-01)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "홈 대시보드 — Step 카드 3장 드릴다운 진입점"
    - path: "src/app/step/[stepId]/page.tsx"
      provides: "Step 페이지 — 모듈 아코디언 + 레슨 목록 (generateStaticParams 3개)"
      contains: "generateStaticParams"
    - path: "src/app/lesson/[lessonId]/page.tsx"
      provides: "레슨 페이지 — 35개 정적 경로, 브레드크럼, 이전/다음, 준비 중 상태"
      contains: "generateStaticParams"
    - path: "src/app/not-found.tsx"
      provides: "잘못된 라우트의 에러 상태 + 커리큘럼 홈 복귀 CTA"
    - path: "src/components/step-card.tsx"
      provides: "Step 카드 (상징 색, 모듈/레슨 개수, 0% 진행률 슬롯)"
    - path: "src/components/depth-badge.tsx"
      provides: "깊이 배지 — 심화는 Step 색 틴트, 개요는 중성색"
    - path: "src/components/estimated-time.tsx"
      provides: "예상 소요시간 표기 (약 N분 / 약 N시간 변환 규칙)"
    - path: "src/components/module-accordion.tsx"
      provides: "네이티브 details/summary 기반 모듈 아코디언"
    - path: "src/components/lesson-nav.tsx"
      provides: "브레드크럼 + 이전/다음 레슨 페이저 (경계에서 비활성)"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/content/curriculum-helpers.ts"
      via: "getModulesByStep / getLessonCounts로 Step 카드 요약 수치 계산"
      pattern: "getLessonCounts"
    - from: "src/app/step/[stepId]/page.tsx"
      to: "src/content/curriculum-helpers.ts"
      via: "getModulesByStep + getLessonsByModule으로 아코디언 트리 구성"
      pattern: "getModulesByStep"
    - from: "src/app/lesson/[lessonId]/page.tsx"
      to: "src/content/curriculum-helpers.ts"
      via: "getLessonBySlug + getAdjacentLessons로 본문과 이전/다음 결정"
      pattern: "getAdjacentLessons"
    - from: "src/components/depth-badge.tsx"
      to: "src/app/globals.css"
      via: "Step 상징 색 토큰과 개요 중성색 토큰 참조"
      pattern: "step"
---

<objective>
학습자가 홈 → Step → 모듈 → 레슨으로 실제로 걸어 들어가고, 레슨 페이지에서 브레드크럼과 이전/다음으로 35개를 순회한다.

Purpose: Plan 03이 채운 매니페스트가 처음으로 화면이 되는 지점이다. CONT-01("3단 구조로 전체 커리큘럼을 탐색할 수 있다")과 CONT-04(레슨마다 깊이 배지)가 여기서 사용자 눈에 닿는다.

Output: 홈 대시보드, Step 페이지(모듈 아코디언 + 레슨 목록), 확장된 레슨 페이지(35 정적 경로 · 브레드크럼 · 이전/다음 · 준비 중 상태), not-found 페이지, 그리고 이들이 공유하는 5개 표현 컴포넌트.
</objective>

## Phase Goal

**ROADMAP `**Goal:**` 원문 (verbatim):** 학습자가 배포된 URL에 아이패드로 접속해 커리큘럼 3단 구조를 탐색하고 실제 레슨을 읽을 수 있다

> ⚠ MVP_MODE 주의: Goal 문장에 정규 유저 스토리의 **so that** 절이 없다. 플래너는 스토리를 창작하지 않는다 — 정규 형식이 필요하면 `/gsd mvp-phase 1`을 먼저 실행할 것.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deployed-curriculum-skeleton/01-CONTEXT.md
@.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md
@.planning/phases/01-deployed-curriculum-skeleton/01-RESEARCH.md
@.planning/phases/01-deployed-curriculum-skeleton/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Plan 03이 만든 계약. 탐색 없이 이 시그니처를 그대로 소비할 것. -->

src/content/modules.ts:
  type StepId = 1 | 2 | 3
  interface Step { id: StepId; title: string; shortTitle: string; keywords: string[]; goal: string; courseHours: number }
  interface Module { id: string; stepId: StepId; title: string; order: number; isProject: boolean }
  export const steps: readonly Step[]      // 길이 3
  export const modules: readonly Module[]  // 길이 19

src/content/curriculum-helpers.ts:
  getStep(stepId: StepId): Step | undefined
  getModulesByStep(stepId: StepId): Module[]                              // module.order 오름차순
  getLessonsByModule(moduleId: string): Lesson[]                          // lesson.order 오름차순
  getOrderedLessons(): Lesson[]                                           // 길이 35
  getLessonBySlug(slug: string): Lesson | undefined
  getAdjacentLessons(slug: string): { prev: Lesson | null; next: Lesson | null }
  getLessonCounts(stepId?: StepId): { total: number; withContent: number }

Velite Lesson 필드: title, stepId, moduleId, order, depth('심화'|'개요'), estimatedMinutes, slug, hasContent, code

src/components/mdx-content.tsx (Plan 01):
  MDXContent(props: { code: string; components?: Record<string, React.ComponentType> }): JSX.Element
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 홈 대시보드 Step 카드 3장과 공용 배지 컴포넌트</name>
  <read_first>
    - `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` — Color 표의 정확한 hex 값과 Step identity colors, Depth badge colors, Typography 4사이즈/2웨이트, Spacing Scale과 44px 예외, Copywriting Contract 전체
    - `.planning/phases/01-deployed-curriculum-skeleton/01-CONTEXT.md` — D-03, D-04, D-07, `<specifics>`의 진행률 슬롯 지침
    - `src/content/modules.ts`, `src/content/curriculum-helpers.ts` — Plan 03이 만든 실제 시그니처
    - `src/app/globals.css` — Plan 01이 등록한 테마 토큰 이름
    - `src/app/layout.tsx` — 현재 루트 레이아웃 상태 (Plan 05가 내비를 얹는다, 여기서는 건드리지 않는다)
  </read_first>
  <files>src/app/page.tsx, src/components/step-card.tsx, src/components/depth-badge.tsx, src/components/estimated-time.tsx</files>
  <action>
홈을 커리큘럼 드릴다운의 진입점으로 만들고, 이후 두 태스크가 재사용할 배지 컴포넌트 두 개를 먼저 확정한다.

**(1) `src/components/depth-badge.tsx`.** props는 `{ depth: '심화' | '개요'; stepId: 1 | 2 | 3 }`. 심화일 때는 해당 Step의 상징 색을 배경 틴트·테두리·텍스트에 쓴다 — Step 1은 light `#3B82F6` / dark `#60A5FA`, Step 2는 `#8B5CF6` / `#A78BFA`, Step 3은 `#F59E0B` / `#FBBF24`. 개요일 때는 Step 색을 쓰지 않고 중성 슬레이트를 쓴다 — light 텍스트 `#64748B` 배경 `#F1F5F9`, dark 텍스트 `#94A3B8` 배경 `#1E293B`. 이 구분이 있어야 깊이와 Step 정체성이 서로 섞이지 않는다(UI-SPEC Depth badge colors). 배지 텍스트는 심화/개요 두 값만 쓰고 다른 라벨을 만들지 않는다. 배지는 서버 렌더 가능한 순수 표현 컴포넌트다 — 클라이언트 로직이 필요 없다.

**(2) `src/components/estimated-time.tsx`.** props는 `{ minutes: number }`. 60분 미만이면 "약 N분", 60분 이상이면 시간 단위로 환산해 "약 N시간"으로 표기한다(UI-SPEC Copywriting Contract). 나누어떨어지지 않으면 소수 첫째 자리까지 쓴다 — 270분은 "약 4.5시간", 150분은 "약 2.5시간", 180분은 "약 3시간", 120분은 "약 2시간"이 된다. 타이포는 Label 14px / 400(UI-SPEC).

**(3) `src/components/step-card.tsx`.** props는 `{ step: Step }`. 카드에는 Step 번호와 축약 제목(`shortTitle`), 커리큘럼 원 시수, 키워드, 모듈 수와 레슨 수(`getModulesByStep`·`getLessonCounts`로 계산), 그리고 진행률 바 자리가 들어간다. **진행률 바는 실제 컴포넌트로 렌더하되 값이 0%다** — Phase 2가 이 자리에 Supabase 진도 데이터를 연결한다(CONTEXT `<specifics>`). 진행률 바 채움색은 UI-SPEC의 전역 accent(light `#0D9488` / dark `#2DD4BF`)를 쓴다. 카드 전체가 `/step/{id}`로 가는 링크이고 히트 영역 높이가 최소 44px 이상이다. 카드 배경은 UI-SPEC의 Secondary 표면색(light `#E7ECF3` / dark `#131B2E`), 좌측 또는 상단 강조선에 Step 상징 색을 쓴다.

**(4) `src/app/page.tsx`.** 페이지 `h1`은 Display 28px / 600으로 사이트명 "AI Engineer Runway"를 쓴다(D-01). 그 아래에 Step 카드 3장을 그리드로 배치한다 — 넓은 화면 3열, 좁은 화면 1열로 스택되며 컨테이너를 넘치지 않는다(UX-02). 카드 3장은 고정 개수이므로 빈 상태·개수 가변 분기를 만들지 않는다. 개강일 "2026-09-30 개강" 정적 표기를 헤더 영역에 넣어도 된다(CONTEXT `<specifics>`, Claude 재량) — D-day 계산은 Phase 3 범위이므로 정적 문자열로만 쓴다. 교육기관 이름은 쓰지 않는다: 과정은 "AI Engineer 교육과정"으로만 지칭한다(D-02).
  </action>
  <verify>
    <automated>npm run build && node scripts/check-manifest.mjs && test -f .next/server/app/index.html && grep -q 'AI Engineer Runway' .next/server/app/index.html && grep -q '3B82F6\|--color-step-1\|step-1' src/components/step-card.tsx && grep -qE 'min-h-11|min-w-11|44px' src/components/step-card.tsx && grep -q '개요' src/components/depth-badge.tsx && grep -q '심화' src/components/depth-badge.tsx && grep -qE '64748B|slate' src/components/depth-badge.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`가 0으로 종료하고 홈 페이지가 정적 산출물로 생성된다
    - 홈 산출물 HTML에 사이트명 `AI Engineer Runway`가 존재한다
    - `depth-badge.tsx`가 심화 경로에서 Step 상징 색을, 개요 경로에서 Step과 무관한 중성 슬레이트 색을 사용한다
    - `estimated-time.tsx`가 60분 미만은 분, 60분 이상은 시간으로 환산하며 270분에 대해 `약 4.5시간`을 반환한다
    - `step-card.tsx`에 44px 이상 히트 영역 지정(`min-h-11`/`min-w-11` 또는 동등 값)이 있다
    - Step 카드가 3장 고정으로 렌더되고, 진행률 바가 0% 상태로 실제 존재한다
    - 홈에 금지 브랜드 문자열이 0건이다
  </acceptance_criteria>
  <done>홈에서 Step 카드 3장이 상징 색·모듈/레슨 수·0% 진행률 슬롯과 함께 보이고, 각 카드가 Step 페이지로 가는 44px 이상 터치 타깃이다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Step 페이지 — 19개 모듈 아코디언과 레슨 목록</name>
  <read_first>
    - `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` — Design System 표(아코디언은 네이티브 details/summary, 아이콘은 lucide-react), Spacing Scale, Typography 적용 맵, Copywriting Contract의 "Primary CTA"
    - `.planning/phases/01-deployed-curriculum-skeleton/01-CONTEXT.md` — D-07(드릴다운 모델), D-04
    - `src/content/curriculum-helpers.ts` — `getModulesByStep`, `getLessonsByModule`, `getStep`
    - `src/components/depth-badge.tsx`, `src/components/estimated-time.tsx` — Task 1이 만든 실제 props
    - `src/app/page.tsx` — Task 1의 레이아웃·간격 관례
  </read_first>
  <files>src/app/step/[stepId]/page.tsx, src/components/module-accordion.tsx</files>
  <action>
Step 페이지를 만들어 드릴다운의 두 번째 칸을 채운다.

**(1) `src/components/module-accordion.tsx`.** 네이티브 `<details>`/`<summary>`로 구현한다 — Radix나 헤드리스 UI 라이브러리를 도입하지 않는다(UI-SPEC Design System 결정). `<summary>`가 모듈 제목과 그 모듈의 레슨 수를 보여주고, 여는 방향을 알리는 lucide-react 셰브론 아이콘을 곁들인다. `<summary>` 자체가 탭 대상이므로 히트 영역 높이를 최소 44px로 준다(UX-01). 열린 패널 안에는 레슨 목록이 들어간다 — 각 항목은 레슨 제목, `DepthBadge`, `EstimatedTime`을 한 줄에 두고 `/lesson/{slug}`로 가는 링크이며, 이 링크의 히트 영역도 최소 44px다. 링크 텍스트는 UI-SPEC Copywriting Contract의 "Primary CTA" 행을 따른다. 프로젝트 모듈(`isProject: true`)은 제목에 그 사실이 드러나므로 별도 배지를 새로 만들지 않는다. 아코디언 헤더 배경/강조에 그 Step의 상징 색을 쓴다(D-04). 열림 상태는 클라이언트 상호작용뿐이고 트리 구조 자체는 빌드 타임에 결정된다 — 상태를 서버로 올리지 않는다.

**(2) `src/app/step/[stepId]/page.tsx`.** `generateStaticParams`가 stepId 1, 2, 3 세 개를 반환한다. 페이지 `h1`은 Display 28px / 600으로 Step 번호와 제목을 쓰고, 그 아래에 Step의 목표·키워드·커리큘럼 원 시수를 요약한다. 본문은 `getModulesByStep(stepId)` 순서대로 `ModuleAccordion`을 나열한다. 첫 번째 모듈만 기본으로 열어 두고 나머지는 닫아 둔다 — 19개 모듈 전체가 펼쳐진 채로 시작하면 아이패드 화면에서 방향을 잃는다. `getStep`이 undefined를 반환하면(잘못된 stepId) `notFound()`를 호출한다. 목록·배지·아코디언이 소비하는 데이터는 전부 빌드 타임 매니페스트에서 오므로 로딩 스켈레톤이나 빈 상태 분기를 만들지 않는다.
  </action>
  <verify>
    <automated>npm run build && test -d .next/server/app/step || { echo "step prerender dir missing"; exit 1; }; test "$(find .next/server/app/step -name '*.html' | wc -l)" = "3" && grep -q '<details' src/components/module-accordion.tsx && grep -qE 'min-h-11|min-w-11|44px' src/components/module-accordion.tsx && grep -q 'lucide-react' src/components/module-accordion.tsx && test "$(grep -c 'DepthBadge\|depth-badge' src/components/module-accordion.tsx)" -ge "1" && node -e "const fs=require('fs');const h=fs.readFileSync('.next/server/app/step/1.html','utf8');const m=(h.match(/<details/g)||[]).length; if(m!==5) throw new Error('step 1 should render 5 module accordions, got '+m);" && node -e "const fs=require('fs');const h=fs.readFileSync('.next/server/app/step/3.html','utf8');const m=(h.match(/<details/g)||[]).length; if(m!==7) throw new Error('step 3 should render 7 module accordions, got '+m);"</automated>
  </verify>
  <acceptance_criteria>
    - `/step/1`, `/step/2`, `/step/3` 세 경로만 정적 생성된다 (HTML 3개)
    - `/step/1` 산출물에 `<details>` 요소가 정확히 5개(Step 1 모듈 수), `/step/3` 산출물에 7개 존재한다
    - `module-accordion.tsx`가 네이티브 `<details>`를 사용하고 Radix·헤드리스 UI 라이브러리를 import하지 않는다
    - `<summary>`와 레슨 링크 모두에 44px 이상 히트 영역 지정이 있다
    - 각 레슨 항목이 깊이 배지와 예상 소요시간을 함께 표시한다 (CONT-04)
    - 첫 번째 모듈만 기본 열림 상태다
    - `npm run build`가 0으로 종료하고 `node scripts/check-manifest.mjs`가 계속 통과한다
  </acceptance_criteria>
  <done>Step 페이지에서 19개 모듈이 아코디언으로 나열되고, 열면 그 모듈의 레슨이 깊이 배지·예상 시간과 함께 44px 이상 터치 타깃 링크로 보인다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 레슨 페이지 확장 — 35 정적 경로, 브레드크럼, 이전/다음, 준비 중 상태, not-found</name>
  <read_first>
    - `src/app/lesson/[lessonId]/page.tsx` — Plan 01이 만든 현재 구현 (트레이서 버전)
    - `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` — Copywriting Contract 전체(Empty state heading/body, Error state, Secondary CTA 이전/다음 문구), UI Considerations의 zero-one-many 행(경계 버튼은 숨기지 않고 비활성)
    - `.planning/phases/01-deployed-curriculum-skeleton/01-CONTEXT.md` — D-08(브레드크럼 + 본문 끝 큰 이전/다음, 44px+)
    - `src/content/curriculum-helpers.ts` — `getLessonBySlug`, `getAdjacentLessons`, `getOrderedLessons`
    - `src/components/depth-badge.tsx`, `src/components/estimated-time.tsx`
    - `.planning/phases/01-deployed-curriculum-skeleton/01-RESEARCH.md` — Pitfall 2(35 경로가 아니면 CONT-01이 깨진다), Anti-Patterns
  </read_first>
  <files>src/app/lesson/[lessonId]/page.tsx, src/app/not-found.tsx, src/components/lesson-nav.tsx</files>
  <action>
트레이서가 만든 레슨 라우트를 35개 전체와 순회 장치로 확장하고, 잘못된 경로의 착지점을 만든다.

**(1) `src/components/lesson-nav.tsx`.** 두 조각을 export한다.
  - 브레드크럼: `Step {n} > {모듈 제목}` 형태로 각각 `/step/{stepId}`와 해당 Step 페이지로 링크한다(D-08). 각 링크의 히트 영역은 최소 44px다. 타이포는 Label 14px.
  - 페이저: 본문 끝에 놓이는 큰 이전/다음 버튼 두 개. 문구는 UI-SPEC Copywriting Contract의 "Secondary CTA (previous lesson)"·"Secondary CTA (next lesson)" 행을 그대로 쓰고, lucide-react 화살표 아이콘을 곁들인다. 각 버튼의 최소 높이는 44px다. 이전/다음 레슨이 없는 경계(전역 첫 레슨의 이전, 35번째 레슨의 다음)에서는 **버튼을 숨기지 않고 비활성 회색 상태로 렌더한다** — 숨기면 레이아웃이 흔들린다(UI-SPEC UI Considerations, 사용자 확정 사항). 비활성 요소는 링크가 아닌 요소로 렌더하고 `aria-disabled` 속성을 준다.
  - 두 버튼 모두 검증 가능한 데이터 속성을 갖는다: 이전 버튼에는 `data-pager="prev"`, 다음 버튼에는 `data-pager="next"`를 주고, 비활성 상태일 때만 추가로 `data-pager-disabled="true"`를 준다.

**(2) `src/app/lesson/[lessonId]/page.tsx` 확장.** `generateStaticParams`가 `getOrderedLessons()`의 35개 slug 전부를 반환하도록 바꾼다 — 본문 유무와 무관하다. 페이지 상단은 브레드크럼, 레슨 제목(Display 28px/600), `DepthBadge`, `EstimatedTime` 순으로 배치한다. 본문은 `hasContent` 값으로 갈린다: true면 `MDXContent`로 렌더하고 `prose` 래퍼를 유지한다. false면 UI-SPEC Copywriting Contract의 "Empty state heading" 행 문구를 제목으로, "Empty state body" 행 문구를 본문으로 렌더한다 — 문구를 새로 짓지 않는다. 두 경우 모두 본문 끝에 페이저를 둔다. 매니페스트에 없는 slug면 `notFound()`를 호출한다.

**(3) `src/app/not-found.tsx`.** UI-SPEC Copywriting Contract의 "Error state" 행에 적힌 제목·본문·CTA 라벨을 그대로 쓴다. CTA는 커리큘럼 홈(`/`)으로 가는 44px 이상 링크다. 이 페이지는 런타임 데이터를 전혀 읽지 않는 정적 컴포넌트다.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-manifest.mjs && test -d .next/server/app/lesson || { echo "lesson prerender dir missing"; exit 1; }; test "$(find .next/server/app/lesson -name '*.html' | wc -l)" = "35" && grep -q 'data-pager="prev"' .next/server/app/lesson/1-1-course-orientation.html && grep -q 'data-pager-disabled="true"' .next/server/app/lesson/1-1-course-orientation.html && grep -q 'data-pager-disabled="true"' .next/server/app/lesson/3-7-project-ax-launch.html && test "$(grep -c 'data-pager-disabled' .next/server/app/lesson/1-3-python-functions-and-io.html)" = "0" && grep -q '콘텐츠 준비 중입니다' .next/server/app/lesson/1-1-course-orientation.html && test "$(grep -c '콘텐츠 준비 중입니다' .next/server/app/lesson/1-3-python-variables-and-types.html)" = "0" && grep -qE 'min-h-11|min-w-11|44px' src/components/lesson-nav.tsx && test -f src/app/not-found.tsx</automated>
  </verify>
  <acceptance_criteria>
    - 레슨 정적 산출물 HTML이 정확히 35개 생성된다 (CONT-01, RESEARCH Pitfall 2)
    - 전역 첫 레슨(`1-1-course-orientation`)과 마지막 레슨(`3-7-project-ax-launch`) 산출물에 비활성 페이저 표식이 존재한다
    - 중간 레슨(`1-3-python-functions-and-io`) 산출물에는 비활성 페이저 표식이 0건이다
    - 본문 없는 레슨 산출물에 UI-SPEC empty state 제목 문구가 존재하고, 파일럿 레슨 산출물에는 그 문구가 0건이다
    - `lesson-nav.tsx`에 44px 이상 히트 영역 지정이 있고 비활성 요소에 `aria-disabled`가 붙는다
    - `src/app/not-found.tsx`가 존재하고 커리큘럼 홈으로 가는 CTA를 포함한다
    - `npm run build`와 `node scripts/check-manifest.mjs`가 모두 0으로 종료한다
  </acceptance_criteria>
  <done>35개 레슨이 모두 실제 페이지로 존재하고, 브레드크럼과 이전/다음으로 처음부터 끝까지 순회할 수 있으며, 본문 없는 레슨은 404가 아니라 준비 중 안내를 보여준다.</done>
</task>

</tasks>

<!-- planner-discipline-allow: data-pager-disabled -->
<!-- planner-discipline-allow: 콘텐츠 준비 중입니다 -->
<!-- planner-discipline-allow: kant -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 공개 인터넷 → 정적 라우트 파라미터 (`stepId`, `lessonId`) | 익명 사용자가 URL 세그먼트를 임의로 조작해 접근한다 |
| 빌드 타임 매니페스트 → 렌더된 HTML | 매니페스트 값이 그대로 공개 표면 텍스트가 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-11 | Information Disclosure | 라우트 파라미터 조작 (`/step/{임의값}`, `/lesson/{임의값}`) | medium | mitigate | `generateStaticParams`로 허용 경로를 3개·35개로 열거하고, 매니페스트에 없는 값은 `notFound()`로 보낸다. 파라미터가 파일 경로나 데이터 조회 키로 직접 쓰이지 않으므로 경로 순회 표면이 없다 |
| T-01-12 | Information Disclosure | 렌더된 HTML에 노출되는 매니페스트 텍스트 | high | mitigate | Plan 03의 콘텐츠 브랜드 스캔 + Plan 05의 상시 게이트가 이 표면을 덮는다 (D-02) |
| T-01-13 | Tampering | 아코디언 열림 상태 등 클라이언트 상호작용 | low | accept | 표시 전용 상태이고 서버로 전송되지 않으며 데이터 접근 권한과 무관하다 |
| T-01-03 | Tampering | 빌드 타임 MDX 컴파일 | low | accept | 모든 MDX가 저장소 소유자 작성이며 외부 입력 경로가 없다 (RESEARCH Security Domain) |
</threat_model>

## Artifacts this phase produces

이 Plan이 새로 만드는 심볼·경로:

- 파일: `src/app/page.tsx`, `src/app/step/[stepId]/page.tsx`, `src/app/not-found.tsx`
- 파일 확장: `src/app/lesson/[lessonId]/page.tsx` (35 정적 경로, 브레드크럼, 페이저, 준비 중 분기)
- 파일: `src/components/step-card.tsx`, `src/components/depth-badge.tsx`, `src/components/estimated-time.tsx`, `src/components/module-accordion.tsx`, `src/components/lesson-nav.tsx`
- export: `StepCard`, `DepthBadge`, `EstimatedTime`, `ModuleAccordion`, `LessonBreadcrumb`, `LessonPager`
- export: `generateStaticParams` (step 라우트: 3개 / lesson 라우트: 35개)
- 라우트: `/`, `/step/1`, `/step/2`, `/step/3`, `/lesson/{35개 slug}`, 404 폴백
- DOM 데이터 속성 계약: `data-pager="prev"`, `data-pager="next"`, `data-pager-disabled="true"`

## Flagged Assumptions

Spec-less 엣지 프로브 폴백 결과 중 이 Plan이 소유한 미해결 항목 (묵시적 누락 금지 — 명시 플래그):

- **CONT-01 / category: unclassified / status: unresolved** — 프로브가 이 요구사항을 엣지 범주로 분류하지 못했다("unclassified — review manually"). 자동으로 `must_haves.truths`에 승격하지 않았다. 플래너의 판단: 탐색 요구사항의 실질 경계(35개 경로 전부 존재, 잘못된 파라미터의 착지점, 첫·마지막 레슨의 순회 경계)는 Task 3의 정적 산출물 개수 게이트와 페이저 경계 게이트가 덮는다.
- **UX-02 / category: unclassified / status: unresolved** — 동일하게 미분류 상태다. 플래너의 판단: 반응형의 실질 경계(좁은 화면에서의 그리드 스택, 컨테이너 초과 없음)는 UI-SPEC UI Considerations의 overflow 행에서 lift한 `must_haves.truths` 항목이 덮고, 실기기 확인은 Plan 06의 human-check가 맡는다. 분류를 확정하려면 `/gsd-spec-phase 1`로 SPEC의 `## Edge Coverage`를 채우는 것이 정공법이다.

<verification>
- 홈·Step 3개·레슨 35개가 모두 정적 산출물로 생성된다
- Step 1 산출물의 모듈 아코디언 5개, Step 3 산출물의 7개가 확인된다
- 전역 첫/마지막 레슨의 페이저가 비활성 표식을 갖고 중간 레슨은 갖지 않는다
- 본문 없는 레슨은 준비 중 안내를, 파일럿 레슨은 실제 본문을 렌더한다
- `node scripts/check-manifest.mjs`가 계속 통과한다
</verification>

<success_criteria>
- CONT-01의 "3단 구조로 전체 커리큘럼 탐색"이 실제 클릭 경로로 성립한다
- CONT-04의 깊이 배지와 예상 소요시간이 35개 레슨 전부의 목록과 헤더에 표시된다
- 탭 가능한 모든 요소가 44px 이상 히트 영역을 갖는다 (UX-01의 구조적 전제)
</success_criteria>

<output>
Create `.planning/phases/01-deployed-curriculum-skeleton/01-04-SUMMARY.md` when done
</output>
