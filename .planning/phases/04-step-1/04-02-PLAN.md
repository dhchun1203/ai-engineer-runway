---
phase: 04-step-1
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/content/lessons/step-1/1-1-course-orientation.mdx
  - src/content/lessons/step-1/1-1-dev-environment-setup.mdx
autonomous: true
requirements: [CONT-02, CONT-03]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`/lesson/1-1-course-orientation`이 3 Step · 19 모듈 · 프로젝트 5종이 어떻게 이어지는지의 큰 그림, 이 사이트로 5주 공부하는 법(오늘의 학습·완료 체크·일정표·페이스), 하루 학습 루틴을 담은 본문을 렌더한다 (D-53)"
    - "1-1-course-orientation의 ④ 실무 예제 자리가 코드가 아니라 복사 가능한 하루 학습 기록 템플릿(`text` 펜스)을 제공한다 — 비코드 산출물이지만 복사 버튼은 그대로 동작한다 (D-53, CONT-03)"
    - "`/lesson/1-1-dev-environment-setup`이 VS Code · Python · Git · GitHub 계정 · Supabase 계정 다섯 가지의 설치·가입 순서와 확인 명령을 담고, 확인 명령을 `powershell` 펜스로 제공한다 (D-58)"
    - "1-1-dev-environment-setup이 Python 설치 확인 명령으로 `py --version`을 1차 안내하고, ⑤ 실무 팁에 Windows 앱 실행 별칭 때문에 `python`이 스토어 스텁으로 잡히는 증상과 해결책을 적는다 (RESEARCH Pitfall 2 — 이 개발 환경에서 실제 재현됨)"
    - "두 레슨 모두 6개 헤딩이 원문 그대로 순서대로 있고, `### 해보기` 2~3개와 접힌 `<details><summary>정답 보기</summary>` 정답 블록, `**이 레슨의 단어**` 5~8행 표를 갖는다 (D-47, D-49, D-50, D-61)"
    - "두 레슨의 `hasContent`가 `false`에서 `true`로 바뀌고, 그 외 프론트매터 7개 필드는 바이트 단위로 동일하다 (D-13)"
    - "`node scripts/check-lesson-structure.mjs`와 `node scripts/check-brand.mjs`가 exit 0이고, `npm run build`가 성공한다"
    - statement: "두 레슨의 읽기 산문이 30분 안쪽으로 읽히고 나머지 시간은 해보기 과제가 채운다 (D-49)"
      verification: backstop
  artifacts:
    - src/content/lessons/step-1/1-1-course-orientation.mdx
    - src/content/lessons/step-1/1-1-dev-environment-setup.mdx
  key_links:
    - "1-1-course-orientation → 홈 `/`(오늘의 학습) · `/schedule` · 레슨 하단 완료 버튼 — 레슨이 설명하는 사이트 사용법이 실제 화면과 어긋나면 첫 레슨부터 신뢰를 잃는다"
    - "1-1-dev-environment-setup의 `py` 안내 → 1-3·1-5 Python 실습 → 1-2 Claude API 예제 — 여기서 실행 경로를 잘못 알려주면 뒤 레슨 전부가 막힌다"
    - "설치 대상 5종 → D-55(로컬 Python) · D-56(Supabase SQL 에디터) · D-57(git + GitHub PR) — 뒤 레슨의 실습 환경 전제를 이 레슨 하나가 전부 세운다"
  prohibitions:
    - statement: "레슨 본문·코드 주석·메타데이터 어디에도 교육기관명(`scripts/check-brand.mjs`의 `FORBIDDEN_BRAND_STRINGS`에 하드코딩된 문자열)을 노출해서는 안 된다 — 항상 'AI Engineer 교육과정'으로 표기한다 (D-02)"
      status: unverified
    - statement: "1-1-course-orientation에 개강 OT 자료·기관 소개·강사진·수강 규정 같은 기관 운영 정보를 넣어서는 안 된다 — 이 레슨은 커리큘럼 지도 + 사이트 사용법 + 하루 루틴만 다룬다 (D-53)"
      status: unverified
    - statement: "Mermaid·SVG·이미지·ASCII 다이어그램·새 MDX 컴포넌트를 도입해서는 안 된다 (D-48)"
      status: unverified
    - statement: "프론트매터의 `hasContent` 외 어떤 필드도 바꿔서는 안 된다 (D-13)"
      status: unverified
    - statement: "브라우저 기반 온라인 노트북 실행 환경을 Python 실습 경로로 안내해서는 안 된다 — 실습은 PC 로컬 Python으로 통일한다 (D-55)"
      status: unverified
    - statement: "학습 일정의 날짜→레슨 배정을 레슨 본문에 하드코딩해서는 안 된다 — `/schedule`과 홈이 단일 진실 원천이고 레슨은 그쪽을 가리키기만 한다"
      status: unverified
---

<objective>
Step 1 모듈 1-1(온보딩 & 학습 환경 세팅) 2편을 승인된 집필 표준으로 쓴다 — 학습자가 5주 여정의 지도를 보고, 실습 환경을 갖추고, 첫날을 시작할 수 있게 한다.

Purpose: 이 두 편은 커리큘럼상 학습자가 처음 여는 레슨이자, 뒤따르는 8편의 실습 환경 전제를 전부 세우는 레슨이다. 여기서 `py` 실행 경로를 잘못 알려주면 1-3·1-5의 Python 실습이 모두 막히고, 사이트 사용법을 잘못 설명하면 진행률·오늘의 학습 루프가 시작되지 않는다.

Output: 읽고 실습할 수 있는 레슨 2편, `hasContent: true` 전환 완료.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-step-1/04-CONTEXT.md
@.planning/phases/04-step-1/04-RESEARCH.md
@.planning/phases/04-step-1/04-PATTERNS.md
@.planning/phases/04-step-1/04-01-SUMMARY.md
</context>

<wave_2_shared_rules>
## Wave 2 공통 규칙 (Plan 02~06 동일)

1. **표준의 출처는 Plan 01의 결과물이다.** `04-01-SUMMARY.md`와 승인된 `src/content/lessons/step-1/1-3-python-variables-and-types.mdx`가 이 Plan이 따라야 할 유일한 실물 기준이다. SUMMARY에 사용자 수정 요청이 기록되어 있다면 그것이 최우선이다.
2. **`node scripts/check-manifest.mjs`는 이 Plan에서 실행하지 않는다.** Invariant 10의 기대 개수는 아직 2인데 이 Plan이 `hasContent: true`를 늘리므로 **의도적으로 red다.** Plan 07이 상수를 2 → 11로 올릴 때 다시 green이 된다. 이 red를 "고치려고" `scripts/check-manifest.mjs`를 건드리면 다른 Wave 2 Plan과 충돌한다 (RESEARCH Pitfall 5).
3. **이 Plan의 게이트는 세 개다:** `npm run build`, `node scripts/check-brand.mjs`, `node scripts/check-lesson-structure.mjs`. 레슨 1편을 끝낼 때마다 세 개를 모두 돌린다.
4. **`src/` 아래 `.mdx` 외 어떤 파일도 바꾸지 않는다.** 컴포넌트·CSS·스크립트·설정은 이 Plan의 범위 밖이다 (D-48 "플랫폼 변경 0").
5. **사람 검토 체크포인트를 두지 않는다** (D-59). 품질 방어선은 위 세 게이트와, 실행자가 예제 코드를 직접 실행해 보는 습관 두 가지뿐이다 — 예제를 돌려 보지 않고 커밋하면 이 phase에는 그것을 잡을 다른 장치가 없다.
6. **6개 헤딩은 이 문자열 그대로:** `## 1. 학습 목표` · `## 2. 왜 배우나` · `## 3. 개념 설명` · `## 4. 실무 예제` · `## 5. 실무 팁` · `## 6. 핵심 정리 및 스스로 점검`. `##` 레벨 헤딩은 이 6개뿐이다.
7. **절 구성 표준(Plan 01에서 확정):** ①은 동사로 끝나는 결과 3~4개 · ②는 최대 4문장 1문단 · ③은 이모지 붙은 `###` 소제목 3~4개(비유 먼저, 정의 나중, 필요하면 "일상 비유" 열이 있는 표) · ④는 완결 실행 가능 예제 + 실행 방법 펜스 + 예상 출력 설명 + `### 해보기` 2~3개(각각 접힌 정답 블록) · ⑤는 불릿 3~4개(각 2문장 이내) · ⑥은 `**핵심 정리**` 불릿 4~5개 + `**이 레슨의 단어**` 5~8행 표(`| 단어 | 뜻 |`) + `**스스로 점검**` 질문 2개(각각 접힌 정답 블록).
8. **정답 블록 형식(빈 줄 필수):** `<details>` 줄 → `<summary>정답 보기</summary>` 줄 → **빈 줄** → 내용 → **빈 줄** → `</details>` 줄. 빈 줄이 빠지면 정답 안의 백틱·별표가 그대로 노출된다 (RESEARCH Pitfall 3, 게이트 L4가 잡는다).
9. **용어 표기(D-50):** 첫 등장은 "한글(English, 한 줄 풀이)", 이후는 한글만, 코드 안에서는 영어 그대로.
10. **OS 기준(D-58):** Windows PowerShell이 기본. `python`·`git`처럼 공통인 명령은 그대로 쓰고, 경로·설치·환경변수처럼 다른 부분만 macOS 한 줄 병기.
11. **모든 코드펜스에 언어 태그를 단다.** 허용 태그는 `python` · `sql` · `bash` · `powershell` · `text`. 벌거벗은 펜스는 게이트 L6이 잡는다.
</wave_2_shared_rules>

<artifacts_produced>
## Artifacts this phase produces

**Plan 02 (이 Plan)**
- `src/content/lessons/step-1/1-1-course-orientation.mdx` — 본문 신규 작성, `hasContent: false → true`
- `src/content/lessons/step-1/1-1-dev-environment-setup.mdx` — 본문 신규 작성, `hasContent: false → true`

**Phase 4 전체 산출물 목록은 `04-01-PLAN.md`의 `<artifacts_produced>` 참고** — 레슨 10편, `scripts/check-manifest.mjs` 상수 2→11, `scripts/check-lesson-structure.mjs` 신설, `globals.css` `<details>` 규칙, `lesson/[lessonId]/page.tsx` 빈 상태 문구, `01-UI-SPEC.md` Copywriting Contract, `docs/making-of.md` 4단계.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 1-1-course-orientation — 5주 여정의 지도와 하루 루틴</name>
  <files>src/content/lessons/step-1/1-1-course-orientation.mdx</files>
  <read_first>
    - `src/content/lessons/step-1/1-1-course-orientation.mdx` — 현재 스텁. 프론트매터 8줄(`title: "과정 운영 방식과 학습 준비"`, `moduleId: "1-1"`, `order: 1`, `slug: "1-1-course-orientation"`)이 유지 대상이다.
    - `src/content/lessons/step-1/1-3-python-variables-and-types.mdx` — **승인된 파일럿.** 6단 골격·톤·해보기 형식·정답 블록·용어 표의 실물 기준. 구조를 그대로 베낀다.
    - `.planning/phases/04-step-1/04-01-SUMMARY.md` — 승인 과정에서 굳은 확정값과 사용자 수정 요청.
    - `docs/making-of.md` — eli5 톤 선례. 특히 "한 문장으로 말하면"·"큰 그림" 절의 서술 방식이 이 레슨의 커리큘럼 지도 부분과 형태가 겹친다.
    - `.planning/curriculum.md` 전체 — 3 Step · 19 모듈 · 실습 프로젝트 5종의 원문. 이 레슨이 그리는 지도의 유일한 출처다.
    - `src/content/modules.ts` — 실제 모듈 19개와 `isProject: true`인 5개가 무엇인지. 레슨이 말하는 숫자와 사이트가 렌더하는 숫자가 어긋나면 안 된다.
    - `src/app/page.tsx` — 홈 "오늘의 학습"이 실제로 무엇을 보여주는지(D-day, 오늘 배정 레슨, 페이스, 밀린 레슨). 레슨이 설명할 화면의 실물.
    - `src/app/schedule/page.tsx` — `/schedule`이 실제로 보여주는 것(8/25~9/29 36행, 주차 그룹, 9/30 개강일 행).
    - `src/components/complete-button.tsx` — 완료 버튼의 실제 위치와 동작(본문 끝, 페이저 위 — D-21).
    - `.planning/phases/04-step-1/04-RESEARCH.md` §Per-Lesson Content Outline의 `1-1-course-orientation` 행.
  </read_first>
  <action>
`1-1-course-orientation.mdx`의 본문을 새로 쓰고 프론트매터의 `hasContent`를 `false`에서 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다.

이 레슨은 커리큘럼 지도 + 이 사이트 사용법 + 하루 루틴 세 가지만 다룬다. 개강 OT 자료·기관 소개·강사진·수강 규정은 넣지 않는다 (D-53, D-02). 교육과정을 부를 때는 항상 "AI Engineer 교육과정"으로 쓴다.

비유의 방향은 **5주짜리 여정을 담은 지도**다. 지금 어디에 서 있고, 어디로 가며, 하루에 얼마나 걷는지를 지도 비유로 꿴다.

절별 목표:
- ① 학습 목표 — "전체 커리큘럼이 어떤 순서로 이어지는지 설명할 수 있다", "이 사이트에서 오늘 배울 레슨을 찾아 완료 표시할 수 있다", "하루 학습 루틴을 자기 일정에 맞춰 계획할 수 있다" 계열 4개.
- ② 왜 배우나 — 사전학습의 목적이 마스터가 아니라 개강 전 기초 다지기라는 것, 지도를 먼저 보면 각 레슨이 왜 거기 있는지 알게 된다는 것. 4문장 이내.
- ③ 개념 설명 — 이모지 붙은 `###` 소제목 3~4개.
  - "커리큘럼 지도" — Step 1(개발 기반 구축) → Step 2(LLM 기반 풀스택 AI 서비스 구현) → Step 3의 흐름을 `A → B → C` 한 줄로 먼저 보여주고, 3열 표(Step · 무엇을 배우나 · 이 사이트에서 몇 편)로 정리한다. 모듈 총 19개, 레슨 총 35편, 실습 프로젝트 5종이라는 실제 수치를 `curriculum.md`·`modules.ts`에서 확인해 쓴다.
  - "실습 프로젝트 5종이 지도에서 하는 일" — 프로젝트는 재현이 아니라 개요·사전 준비 가이드로 제공된다는 사실 한 문단.
  - "이 사이트를 쓰는 법" — 홈(오늘의 학습, D-day, 페이스) → 레슨 읽기 → 본문 끝 완료 버튼 → 진행률·일정표 반영의 흐름을 순서대로. 날짜별 배정을 본문에 나열하지 말고 `/schedule`을 가리킨다.
  - "하루 루틴" — 하루 1레슨, 읽기 약 30분 + 실습 약 120분이라는 배분과, 밀렸을 때 어떻게 따라잡는지(사이트가 계산해 알려준다).
- ④ 실무 예제 — 코드가 아니라 **하루 학습 기록 템플릿**을 `text` 펜스로 준다(복사 버튼이 붙는다). 3줄 양식: 오늘 배운 것 / 막힌 것 / 내일 할 일. 각 줄에 어떻게 채우는지 예시를 한 줄씩 붙인 형태로 만들고, 펜스 아래에 이 템플릿을 어디에 두고 쓰는지(메모 앱·노션·`.md` 파일 아무거나) 산문 2~3줄.
  - `### 해보기` 2개: ① 홈을 열어 오늘 배정된 레슨이 무엇인지 확인하고 D-day가 며칠인지 적어 본다 (정답 블록에는 어디를 보면 되는지와 예상 화면 설명), ② 위 템플릿으로 오늘 하루치를 실제로 기록해 본다 (정답 블록에는 채워진 예시 한 벌).
- ⑤ 실무 팁 — 불릿 3~4개: 완료 체크는 미루지 말 것(진행률·페이스가 이 신호로만 계산된다), 밀려도 일정이 자동으로 밀리지 않는다는 것(일정은 고정), 이해가 안 되는 절은 표시만 하고 넘어가고 개강 후 다시 볼 것, 읽기는 아이패드·실습은 PC로 나눠 쓰는 게 편하다는 것.
- ⑥ 핵심 정리 + `**이 레슨의 단어**` 표(Step, 모듈, 레슨, 심화/개요, D-day, 진행률, 페이스 계열에서 5~8개) + `**스스로 점검**` 질문 2개(각각 정답 블록).
  </action>
  <verify>
    <automated>npm run build && node scripts/check-brand.mjs && node scripts/check-lesson-structure.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `node scripts/check-brand.mjs`, `node scripts/check-lesson-structure.mjs` 세 명령이 모두 exit 0이다.
    - 파일 프론트매터에 `hasContent: true`가 있고, `git diff`에서 `hasContent` 줄 외의 프론트매터 줄이 변경되지 않았다.
    - `grep -n '^## ' 파일`의 출력이 정확히 6줄이고 `## 1. 학습 목표`부터 `## 6. 핵심 정리 및 스스로 점검`까지 순서대로다.
    - `grep -c '^### 해보기$' 파일`이 2 또는 3이다.
    - `grep -c '<details>'` · `grep -c '<summary>정답 보기</summary>'` · `grep -c '</details>'` 세 값이 같고 `### 해보기` 개수 + 2 이상이다.
    - 파일에 ```` ```text ```` 펜스가 최소 1개 있고, 그 안에 "오늘 배운 것" · "막힌 것" · "내일 할 일" 세 문자열이 모두 들어 있다.
    - 파일에 `**이 레슨의 단어**`가 1회 등장하고 그 뒤 표의 데이터 행이 5~8개다.
    - `grep -ciE 'kant' 파일`이 0이다.
    - `grep -cE '개강 ?OT|강사|수강료|출석|기관' 파일`이 0이다 (D-53 — 기관 운영 정보 배제).
    - `grep -cE '2026-0[89]-[0-9]{2}' 파일`이 0이다 — 일정 날짜를 본문에 하드코딩하지 않고 `/schedule`을 가리킨다.
    - 본문이 언급하는 Step 수·모듈 수·레슨 수·프로젝트 수가 `src/content/modules.ts`의 실제 값(3 / 19 / 35 / 5)과 일치한다.
    - 코드펜스 중 언어 태그가 없는 것이 0개다.
  </acceptance_criteria>
  <done>학습자가 첫 레슨을 열면 5주 전체 지도와 이 사이트 쓰는 법, 하루 루틴을 알게 되고, 복사해 쓸 학습 기록 템플릿을 손에 넣는다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 1-1-dev-environment-setup — 공구 챙기기(VS Code · Python · Git · GitHub · Supabase)</name>
  <files>src/content/lessons/step-1/1-1-dev-environment-setup.mdx</files>
  <read_first>
    - `src/content/lessons/step-1/1-1-dev-environment-setup.mdx` — 현재 스텁. 프론트매터(`title: "GitHub·학습 도구 환경 세팅"`, `moduleId: "1-1"`, `order: 2`) 유지 대상.
    - `src/content/lessons/step-1/1-1-course-orientation.mdx` (Task 1 완료본) — 같은 모듈 앞 레슨. 중복 설명을 피하고 자연스럽게 이어지게 한다.
    - `src/content/lessons/step-1/1-3-python-variables-and-types.mdx` — 승인된 파일럿. 특히 `powershell` 펜스와 `py` 실행 안내의 정확한 형태를 여기서 그대로 가져온다.
    - `.planning/phases/04-step-1/04-RESEARCH.md` — `## Common Pitfalls` **Pitfall 2 전문**(이 개발 환경에서 `where python`이 `WindowsApps` 스텁을 먼저 잡고 `py --version`이 3.12.10을 가리킨 실측 결과), `## Environment Availability` 표(Python 3.12.10 / Git 2.52.0.windows.1), §Per-Lesson Content Outline의 `1-1-dev-environment-setup` 행.
    - `.planning/phases/04-step-1/04-CONTEXT.md` D-55 · D-56 · D-57 · D-58 — 이 레슨이 세워야 할 실습 환경 전제 전부.
    - `.planning/phases/04-step-1/04-01-SUMMARY.md` — 확정된 펜스 태그 문자열과 사용자 수정 요청.
  </read_first>
  <action>
`1-1-dev-environment-setup.mdx`의 본문을 새로 쓰고 `hasContent`를 `true`로 바꾼다.

설치·가입 대상은 다섯 가지로 고정한다: **VS Code · Python · Git · GitHub 계정 · Supabase 계정.** 순서는 "뒤 레슨에서 필요해지는 순서"로 잡는다 — VS Code(모든 실습의 편집기) → Python(1-3·1-5) → Git(1-2) → GitHub 계정(1-2 PR 실습) → Supabase 계정(1-4 SQL 에디터). 각 항목마다 "왜 필요한가 한 줄"을 먼저 주고 설치·가입을 안내한다.

비유의 방향은 **작업대에 공구를 챙기는 것**이다. 다섯 공구가 각각 어떤 작업에 쓰이는지가 비유의 축이다.

**Python 실행 경로가 이 레슨의 핵심 실무 정보다 (RESEARCH Pitfall 2 — 이 개발 환경에서 실제 재현됨).** 설치 확인 명령의 1차 안내는 `py --version`이다. <!-- planner-discipline-allow: python --version --> 실행 확인 펜스에 `python --version`을 넣지 않는다(이 문자열은 ⑤ 실무 팁의 문제 상황 설명 산문에서만 쓴다) — Windows 11이 기본으로 켜 두는 앱 실행 별칭 때문에 `python`이 실제 설치본이 아니라 Microsoft Store 스텁으로 먼저 해석되는 경우가 흔하고, 그러면 명령이 아무 반응 없이 끝나거나 스토어가 열린다. macOS는 이 문제가 없으므로 `python3 --version` 한 줄만 병기한다 (D-58 "다를 때만 병기"에 정확히 해당).

절별 목표:
- ① 학습 목표 — 다섯 도구를 설치·가입하고 각각이 정상 동작하는지 확인 명령으로 검증할 수 있다 계열 4개. 그중 하나는 "명령이 안 될 때 무엇을 의심해야 하는지 안다".
- ② 왜 배우나 — 이 다섯 가지가 뒤따르는 8편 실습의 전제라는 것. 4문장 이내.
- ③ 개념 설명 — 이모지 `###` 소제목 3~4개. "다섯 공구와 각자의 일"을 3열 표(도구 · 하는 일 · 어느 레슨에서 쓰나)로 정리한다. 편집기·런타임·버전 관리·원격 저장소·데이터베이스라는 개념을 각각 한 줄 비유로 푼다. 용어 첫 등장은 D-50 형식.
- ④ 실무 예제 — 설치 확인 명령 묶음을 `powershell` 펜스 하나로 준다. 내용: `code --version`, `py --version`, `git --version` 세 줄. 펜스 아래에 각 명령이 어떤 출력을 내면 정상인지 산문으로 설명하고(예: `py --version`이 `Python 3.12.x` 형태), macOS 병기 한 줄(`python3 --version`)을 덧붙인다. GitHub·Supabase 계정은 명령이 아니라 "가입 후 로그인된 화면이 보이면 완료"라는 확인 기준을 준다.
  - `### 해보기` 2~3개: ① 위 세 명령을 실행해 각 버전을 확인한다(정답 블록에 정상 출력 형태와 "안 나올 때 무엇을 의심하나"), ② GitHub에서 연습용 빈 저장소를 하나 만든다 — 1-2 레슨에서 그대로 쓴다(정답 블록에 저장소 생성 후 보이는 화면과 clone 주소 위치), ③(선택) VS Code에서 `.py` 파일 하나를 만들어 저장하고 열어 본다.
- ⑤ 실무 팁 — 불릿 3~4개. 반드시 포함: `python`을 쳤는데 스토어가 열리거나 아무 반응이 없으면 `py`를 쓰거나 Windows 설정의 앱 실행 별칭에서 python.exe를 끄라는 안내. 나머지는 설치 시 PATH 옵션 체크, VS Code Python 확장, 계정 비밀번호 관리 계열에서 고른다.
- ⑥ 핵심 정리 + `**이 레슨의 단어**` 표(편집기, 런타임, 버전 관리, 저장소, 터미널, PATH, 앱 실행 별칭 계열에서 5~8개) + `**스스로 점검**` 질문 2개(각각 정답 블록). 질문 하나는 반드시 "`python` 명령이 아무 반응 없이 끝났다. 무엇을 확인해야 할까?" 형태로 Pitfall 2를 복습시킨다.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-brand.mjs && node scripts/check-lesson-structure.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `node scripts/check-brand.mjs`, `node scripts/check-lesson-structure.mjs` 세 명령이 모두 exit 0이다.
    - 파일 프론트매터에 `hasContent: true`가 있고, `git diff`에서 `hasContent` 줄 외의 프론트매터 줄이 변경되지 않았다.
    - `grep -n '^## ' 파일`의 출력이 정확히 6줄이고 순서가 1→6이다.
    - `grep -c '^### 해보기$' 파일`이 2 또는 3이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이다.
    - 파일에 ```` ```powershell ```` 펜스가 최소 1개 있고 그 안에 `py --version`이 있다.
    - 파일에 `code --version`과 `git --version`이 모두 있다.
    - **(영역 한정 네거티브 게이트)** `powershell` 코드펜스 **안**에 `python --version`이 0건이다 — 이 문자열은 ⑤ 실무 팁의 문제 상황 설명 산문에는 등장해도 되고, 오히려 등장해야 한다(Pitfall 2를 학습자에게 알리는 문장이 그것이다). 파일 전체 0건 검사가 아니라 펜스 내부 검사임에 주의한다.
    - `python3 --version`이 macOS 병기로 정확히 1회 등장한다.
    - 파일에 VS Code · Python · Git · GitHub · Supabase 다섯 도구 이름이 모두 등장한다.
    - `**이 레슨의 단어**` 표의 데이터 행이 5~8개다.
    - `grep -ciE 'kant' 파일`이 0이다.
    - `grep -ciE 'colab|ollama' 파일`이 0이다 (D-55, D-54).
    - 코드펜스 중 언어 태그가 없는 것이 0개다.
    - 실행자가 `py --version`, `git --version`, `code --version`을 실제로 실행해 본문의 예상 출력 설명이 실제와 맞는지 확인했고, 결과를 SUMMARY에 적었다.
  </acceptance_criteria>
  <done>학습자가 다섯 공구를 갖추고 각각 정상 동작하는지 명령으로 확인할 수 있으며, Windows에서 `python`이 먹통일 때 무엇을 해야 하는지 안다. 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레슨 본문 → 공개 프로덕션 URL | 본문 문자열이 그대로 인터넷에 노출된다 |
| 레슨의 설치·가입 안내 → 학습자의 실제 계정·머신 | 잘못된 안내가 학습자 환경을 망가뜨리거나 계정 정보를 위험하게 다루게 만들 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | 레슨 본문(공개 표면) | high | mitigate | `node scripts/check-brand.mjs`가 브랜드 금지 문자열·이메일을 0건으로 상시 검사한다. 두 Task의 verify와 acceptance criteria에 포함 |
| T-04-05 | Information Disclosure | 1-1-dev-environment-setup의 계정 가입 안내 | medium | mitigate | Supabase·GitHub 안내에 실제 이메일 주소·비밀번호·토큰 값을 예시로도 적지 않는다. `check-brand.mjs`의 이메일 정규식이 본문 전체를 검사하며, acceptance criteria가 exit 0을 요구한다 |
| T-04-06 | Tampering | 잘못된 실행 경로 안내(`python` 스토어 스텁) | medium | mitigate | `py --version`을 1차 명령으로 고정하고 `powershell` 펜스 안에 `python --version`이 없음을 acceptance criteria가 확인한다. ⑤ 실무 팁이 증상과 해결책을 명시 |
| T-04-07 | Tampering | 일정 날짜 하드코딩으로 인한 이중 진실 원천 | low | mitigate | 본문에 `2026-0N-NN` 형태 날짜가 0건임을 acceptance criteria가 확인하고, 레슨은 `/schedule`을 가리키기만 한다 |
| T-04-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 저장소에 패키지를 설치하지 않는다. RESEARCH.md `## Package Legitimacy Audit`이 `[SLOP]`/`[SUS]` 0건을 확정했고, 이 두 레슨은 패키지 설치를 안내하지도 않는다 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

Spec-less probe fallback §A의 6개 edge 프로브(CONT-02·CONT-03 × empty/ordering/adjacency)는 전부 `unresolved`이며, 이 phase에서의 해석과 처리는 `04-01-PLAN.md`의 `<flagged_assumptions>`에 한 번 기록되어 있다 — 요지: empty는 `scripts/check-lesson-structure.mjs` L1·L2·L5와 Velite 스키마가, ordering·adjacency는 프론트매터 `order`와 `check-manifest.mjs` 기존 불변식이 담당하며 이 Plan은 `order`를 건드리지 않는다.

이 Plan 고유의 가정:
- **A4 승계 — `py` 런처 일반화:** RESEARCH는 한 대의 개발 머신에서만 Pitfall 2를 재현했다. `py`는 python.org Windows 설치에 표준 포함되므로 일반화 가능성이 높고, 이 프로젝트는 사실상 1인·같은 머신 사용이라 실질 리스크가 낮다.
- **사이트 사용법 설명의 정합성:** 1-1-course-orientation이 설명하는 홈·일정표·완료 버튼 동작은 실행자가 `src/app/page.tsx`·`src/app/schedule/page.tsx`·`src/components/complete-button.tsx`를 읽고 쓴 것이다. Phase 5 이후 UI가 바뀌면 이 레슨의 설명이 낡는다 — 그때는 quick task로 고친다.
- **`check-manifest`는 이 Plan 종료 시점에 의도적으로 red다** (Wave 2 공통 규칙 2). Plan 07이 green으로 되돌린다.
</flagged_assumptions>

<verification>
1. `npm run build`
2. `node scripts/check-brand.mjs`
3. `node scripts/check-lesson-structure.mjs` — 검사 대상이 3편으로 늘어난다(파일럿 + 이 Plan 2편)
4. 실행자가 `py --version` · `git --version` · `code --version`을 직접 실행해 본문 설명과 대조
5. `node scripts/check-manifest.mjs`는 **실행하지 않는다** — Plan 07이 상수를 갱신할 때까지 의도적 red
</verification>

<success_criteria>
- 1-1 모듈 2편이 승인된 eli5 × 6단 표준으로 작성되어 읽힌다
- 두 레슨 모두 해보기 2~3개와 접힌 정답 블록, 5~8행 용어 표를 갖는다
- 커리큘럼 지도의 수치(3 Step / 19 모듈 / 35 레슨 / 5 프로젝트)가 실제 매니페스트와 일치한다
- 설치 확인 명령이 `py --version`을 1차로 안내하고 macOS를 한 줄 병기한다
- 두 레슨의 `hasContent`가 `true`이고 나머지 프론트매터는 불변이다
</success_criteria>

<output>
Create `.planning/phases/04-step-1/04-02-SUMMARY.md` when done.
</output>
