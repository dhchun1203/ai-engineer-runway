---
phase: 04-step-1
plan: 07
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04", "04-05", "04-06"]
files_modified:
  - scripts/check-manifest.mjs
  - src/app/lesson/[lessonId]/page.tsx
  - .planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md
  - docs/making-of.md
autonomous: true
requirements: [CONT-02, CONT-03]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node scripts/check-manifest.mjs`가 다시 exit 0으로 통과한다 — `EXPECTED_HAS_CONTENT_COUNT`가 11이고 `EXPECTED_HAS_CONTENT_SLUGS`가 Step 1 10편 + `2-3-react-components` 11개를 담는다 (RESEARCH Pitfall 5)"
    - "아직 작성되지 않은 Step 2·3 레슨 24편의 빈 상태 안내 문구가 Step 1 완성 사실을 반영한다 — 더 이상 'Step 1 Python 변수·자료형 파일럿 레슨'을 가리키지 않는다"
    - "`01-UI-SPEC.md` Copywriting Contract의 Empty state 행이 새 문구와 새 레슨 수(24)로 갱신되어, 사이트 카피와 계약 문서가 어긋나지 않는다"
    - "`docs/making-of.md`에 Phase 4(4단계) 기록이 기존 1~3단계와 같은 eli5 형식으로 추가되고 마지막 갱신 줄이 갱신된다 (PLAT-03)"
    - "`node --env-file=.env.local scripts/e2e-progress.mjs`가 통과해, 늘어난 Step 1 레슨을 대상으로 완료 토글이 브라우저 → 쿠키 게이트 → Server Action → Supabase → 재렌더까지 왕복한다 (ROADMAP 성공 기준 4)"
    - "`node --env-file=.env.local scripts/e2e-today.mjs`가 통과해, 오늘의 학습·일정표 뷰가 Step 1 실콘텐츠 위에서 동작한다 (ROADMAP 성공 기준 4)"
    - "Phase 3에서 세운 상시 게이트 5종(`check-progress-math` · `check-schedule` · `check-pace` · `check-progress-gates` · `check-brand`)이 Step 1 콘텐츠 추가 후에도 전부 통과한다 — 콘텐츠 추가가 플랫폼을 회귀시키지 않았다"
    - "프로덕션 URL에서 Step 1 10편이 모두 실콘텐츠로 렌더되고, 아직 안 쓴 레슨은 새 빈 상태 문구를 보여준다"
    - "이 Plan이 어떤 레슨 `.mdx` 파일도 수정하지 않는다 — 콘텐츠는 Wave 1·2가 끝냈고 이 Plan은 게이트·카피·기록만 맞춘다"
  artifacts:
    - scripts/check-manifest.mjs
    - src/app/lesson/[lessonId]/page.tsx
    - .planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md
    - docs/making-of.md
  key_links:
    - "10편의 `hasContent: true` → `.velite/lessons.json` → `check-manifest.mjs` Invariant 10 — 상수와 실제가 맞아야 이 phase가 닫힌다"
    - "`.velite/lessons.json` → `getOrderedLessons()` → 진행률 · 오늘의 학습 · 일정표 — 콘텐츠가 늘면 이 세 화면이 자동으로 채워진다(코드 변경 없음). e2e 두 개가 그것을 실측한다"
    - "`e2e-progress.mjs`의 프로브 레슨 선택(`LESSONS.find(l => l.hasContent)`) → Step 1 첫 실콘텐츠 레슨 — 매니페스트에서 동적으로 뽑으므로 스크립트 수정이 필요 없다"
    - "`page.tsx` 빈 상태 문구 ↔ `01-UI-SPEC.md` Copywriting Contract — 둘이 어긋나면 다음 phase가 어느 쪽을 진실로 볼지 알 수 없다"
  prohibitions:
    - statement: "공개 표면(레슨 본문 · making-of · 페이지 문구 · 메타데이터) 어디에도 교육기관명(`scripts/check-brand.mjs`의 `FORBIDDEN_BRAND_STRINGS`에 하드코딩된 문자열)을 노출해서는 안 된다 — 항상 'AI Engineer 교육과정'으로 표기한다 (D-02)"
      status: unverified
    - statement: "레슨 `.mdx` 파일의 본문이나 프론트매터를 이 Plan에서 수정해서는 안 된다 — 콘텐츠 수정이 필요하면 quick task로 분리한다 (D-13, Wave 경계)"
      status: unverified
    - statement: "`check-manifest.mjs`의 Invariant 10 외 다른 12개 불변식이나 그 기대 상수(`EXPECTED_TOTAL_MINUTES` · `EXPECTED_MINUTES_DISTRIBUTION` · `EXPECTED_PROJECT_MODULE_COUNT`)를 손대서는 안 된다 — 이 phase는 프론트매터 수치를 바꾸지 않았으므로 그 상수들은 여전히 맞다"
      status: unverified
    - statement: "빈 상태 문구를 바꾸면서 UI-SPEC의 4-사이즈 타이포 시스템을 벗어나거나 JSX 구조(`h2` + `p`, `text-[20px]`/`text-[16px]`)를 바꿔서는 안 된다"
      status: unverified
    - statement: "e2e 스크립트가 남긴 프로브 행을 Supabase에 남겨 두어서는 안 된다 — 실행 후 잔여 데이터를 확인한다"
      status: unverified
---

<objective>
Step 1 10편이 다 쓰인 상태를 게이트·카피·기록에 반영하고, 학습 루프가 실제로 끝까지 도는지 종단 검증한 뒤 한 번에 배포해 Phase 4를 닫는다.

Purpose: Wave 2의 다섯 Plan은 각자 `hasContent: true`를 늘렸지만 `check-manifest.mjs`의 기대 상수는 아직 2라 **지금 이 저장소의 매니페스트 게이트는 red다.** 이 Plan이 그것을 green으로 되돌리고, 이제 맞지 않게 된 "준비 중" 안내 문구를 고치고, e2e 두 개로 ROADMAP 성공 기준 4(진행률·오늘의 학습 루프가 Step 1 콘텐츠로 실제로 채워짐)를 실측한다. D-52("9편은 한 번에 배포")와 D-59("사람 검토 없이 자동 게이트로만 완료")가 만나는 지점이다.

Output: green 상태의 전체 게이트 스위트, 갱신된 빈 상태 카피와 UI 계약, Phase 4 기록이 담긴 making-of, 그리고 프로덕션 배포.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-step-1/04-CONTEXT.md
@.planning/phases/04-step-1/04-RESEARCH.md
@.planning/phases/04-step-1/04-PATTERNS.md
@.planning/phases/04-step-1/04-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces

**Plan 07 (이 Plan)**
- `scripts/check-manifest.mjs` — `EXPECTED_HAS_CONTENT_COUNT` 2 → 11, `EXPECTED_HAS_CONTENT_SLUGS` 2개 → 11개, 해당 주석 갱신
- `src/app/lesson/[lessonId]/page.tsx` — `hasContent === false` 분기의 안내 문구 교체 (JSX 구조·className 불변)
- `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` — Copywriting Contract의 Empty state heading·body 행, Edge-state 표의 "비-파일럿 레슨 본문" 개수 33 → 24
- `docs/making-of.md` — "4단계" 기록 섹션 신설 + 마지막 갱신 줄 갱신

**이 Plan이 바꾸지 않는 것:** 레슨 `.mdx` 10편(Wave 1·2가 완료), `velite.config.ts`, `package.json`, `supabase/`, e2e·게이트 스크립트 5종(`check-progress-math` · `check-schedule` · `check-pace` · `check-progress-gates` · `check-brand`), `src/app/globals.css`, `scripts/check-lesson-structure.mjs`.

**Phase 4 전체 산출물 목록은 `04-01-PLAN.md`의 `<artifacts_produced>` 참고.**
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 매니페스트 게이트 상수 갱신 + 빈 상태 카피·UI 계약 정합</name>
  <files>scripts/check-manifest.mjs, src/app/lesson/[lessonId]/page.tsx, .planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md</files>
  <read_first>
    - `scripts/check-manifest.mjs` 1~35행 — `EXPECTED_HAS_CONTENT_COUNT`와 `EXPECTED_HAS_CONTENT_SLUGS` 선언부와 그 위 주석("Plan 06이 파일럿 2를 채우면서 1에서 2로 올렸다"). 갱신 대상은 이 두 상수와 주석뿐이다.
    - `scripts/check-manifest.mjs` 176~195행 — Invariant 10 검사 로직. 개수 비교 후 정렬된 슬러그 배열을 `JSON.stringify`로 비교하므로 배열의 **순서는 무관**하지만 철자는 정확해야 한다.
    - `src/app/lesson/[lessonId]/page.tsx` 54~66행 — `lesson.hasContent` 삼항 분기와 현재 빈 상태 JSX(`h2.text-[20px]` + `p.text-[16px]`, 본문에 `&quot;` 이스케이프 사용).
    - `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` — §Copywriting Contract의 "Empty state heading" · "Empty state body" 두 행(약 110~111행), 그리고 Edge-state 표에서 빈 상태 레슨 본문의 개수를 적어 둔 행(약 137행). 세 곳이 갱신 대상이다.
    - `.velite/lessons.json` (직전 빌드 산출물) — 실제로 `hasContent: true`인 슬러그가 무엇인지 이 파일에서 직접 확인해 상수를 만든다. 손으로 기억해 적지 않는다.
    - `.planning/phases/04-step-1/04-RESEARCH.md` `## Common Pitfalls` Pitfall 5 — 파일럿 단계와 9편 단계의 상수 규칙 구분.
    - `.planning/phases/04-step-1/04-PATTERNS.md` §`scripts/check-manifest.mjs` — 갱신 후 상수의 정확한 형태(11개 슬러그 배열) 예시.
  </read_first>
  <action>
**(1) `scripts/check-manifest.mjs` 상수 갱신.** `EXPECTED_HAS_CONTENT_COUNT`를 `2`에서 `11`로 올리고, `EXPECTED_HAS_CONTENT_SLUGS`에 Step 1 9편의 슬러그를 추가한다 — 기존 두 개(`1-3-python-variables-and-types`, `2-3-react-components`)는 유지하고, `1-1-course-orientation` · `1-1-dev-environment-setup` · `1-2-git-branch-and-pr` · `1-2-generative-ai-basics` · `1-3-python-functions-and-io` · `1-4-relational-db-basics` · `1-4-sql-queries-and-joins` · `1-5-ml-model-types` · `1-5-ml-metrics-and-pipeline` 아홉 개를 더해 총 11개로 만든다. 슬러그 철자는 기억이 아니라 `.velite/lessons.json`에서 직접 읽어 확인한다.

상수 위 주석을 현재 사실에 맞게 다시 쓴다 — 지금 주석은 "Plan 06이 파일럿 2를 채우면서 1에서 2로 올렸다"라고 되어 있는데, 이제는 Phase 4가 Step 1 10편을 채워 Step 1 10 + Step 2 파일럿 1 = 11이 되었다는 사실을 적는다.

**Invariant 10 외에는 아무것도 건드리지 않는다.** `EXPECTED_TOTAL_MINUTES`(4200) · `EXPECTED_MINUTES_DISTRIBUTION` · `EXPECTED_PROJECT_MODULE_COUNT`(5)와 나머지 12개 불변식 로직은 그대로 둔다 — 이 phase는 프론트매터의 `estimatedMinutes` · `depth` · `order`를 바꾸지 않았으므로 그 상수들은 여전히 맞다. 마지막 요약 출력의 "all 13 invariants passed" 문구도 그대로 둔다(불변식 개수가 늘지 않았다).

**(2) `src/app/lesson/[lessonId]/page.tsx` 빈 상태 문구 교체.** `lesson.hasContent`가 거짓일 때 렌더되는 `<p>` 안의 문장을 아래 문자열로 교체한다:

`이 레슨은 아직 작성되지 않았습니다. Step 1의 레슨 10편과 Step 2 "React 컴포넌트" 파일럿 레슨은 모두 작성되어 있으니 먼저 그쪽부터 학습해보세요.`

JSX 안이므로 큰따옴표는 현재 코드와 같은 방식(`&quot;`)으로 이스케이프한다. `<h2>` 문구("콘텐츠 준비 중입니다")는 바꾸지 않는다. `className`·요소 구조(바깥 `div.flex.flex-col.gap-3` + `h2.text-[20px]` + `p.text-[16px]`)도 그대로 둔다 — UI-SPEC의 4-사이즈 타이포 시스템을 벗어나지 않는다.

**(3) `01-UI-SPEC.md` Copywriting Contract 정합.** 세 곳을 고친다.
- "Empty state heading" 행의 괄호 안 레슨 수를 `33` 에서 `24` 로 바꾼다 (35편 중 실콘텐츠가 11편이 되었으므로 남은 빈 상태는 24편이다).
- "Empty state body" 행의 문구를 (2)에서 쓴 문자열과 **글자 그대로 같게** 바꾼다(이스케이프만 다르다).
- Edge-state 표에서 빈 상태 레슨 본문의 개수를 적어 둔 행(현재는 실콘텐츠 2편 기준의 옛 숫자가 적혀 있다)의 숫자도 `24`로 바꾸고, 그 행의 설명이 여전히 사실인지 확인한다.
숫자 `24`는 손으로 세지 말고 `.velite/lessons.json`에서 `hasContent`가 거짓인 레슨 수를 실제로 세어 확인한다.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-manifest.mjs && node scripts/check-brand.mjs && node scripts/check-lesson-structure.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-manifest.mjs`가 exit 0이고 "all 13 invariants passed"를 출력한다.
    - `scripts/check-manifest.mjs`에 `const EXPECTED_HAS_CONTENT_COUNT = 11;` 이 있다.
    - `EXPECTED_HAS_CONTENT_SLUGS` 배열의 원소가 정확히 11개이고, `.velite/lessons.json`에서 `hasContent === true`인 슬러그 집합과 정확히 일치한다: `node -e "const l=require('./.velite/lessons.json');const a=l.filter(x=>x.hasContent).map(x=>x.slug).sort();console.log(a.length,a.join(','));process.exit(a.length===11?0:1)"` 가 exit 0이다.
    - `EXPECTED_TOTAL_MINUTES` · `EXPECTED_MINUTES_DISTRIBUTION` · `EXPECTED_PROJECT_MODULE_COUNT` 세 상수의 값이 변경되지 않았다 (`git diff scripts/check-manifest.mjs`에 이 세 줄이 나타나지 않는다).
    - `git diff --stat scripts/check-manifest.mjs`의 변경 줄이 상수 2개 + 주석 블록에 한정된다 — Invariant 로직 줄이 변경되지 않았다.
    - `src/app/lesson/[lessonId]/page.tsx`의 빈 상태 `<p>` 문구에 `Step 1의 레슨 10편`이 포함되고, 기존 문구의 `Step 1 &quot;Python 변수·자료형&quot;` 부분이 더 이상 존재하지 않는다.
    - `src/app/lesson/[lessonId]/page.tsx`의 `<h2>` 문구가 여전히 `콘텐츠 준비 중입니다`이고, `text-[20px]` · `text-[16px]` 클래스가 그대로다.
    - `01-UI-SPEC.md`에 `33 non-pilot lessons` 및 `비-파일럿 레슨 본문 33개` 문자열이 0건이고, 대응 위치에 `24`가 들어 있다.
    - `01-UI-SPEC.md`의 "Empty state body" 행 문구와 `page.tsx`의 실제 문구가 이스케이프를 제외하고 글자 단위로 같다.
    - `.velite/lessons.json`에서 `hasContent`가 거짓인 레슨 수가 정확히 24다.
    - `npm run build`가 exit 0이고, `node scripts/check-brand.mjs`와 `node scripts/check-lesson-structure.mjs`가 exit 0이다.
    - `git diff --name-only`에 `src/content/lessons/` 하위 파일이 나타나지 않는다.
  </acceptance_criteria>
  <done>매니페스트 게이트가 다시 green이고, 아직 안 쓴 레슨의 안내 문구가 현재 사실을 말하며, 카피 계약 문서가 사이트와 일치한다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: making-of 4단계 기록 (PLAT-03)</name>
  <files>docs/making-of.md</files>
  <read_first>
    - `docs/making-of.md` 전체 — 특히 `## 🏗️ 1단계 — 뼈대와 배포`, `## ✅ 2단계 — 진도 체크`, `## 🗓 3단계 — 일정표와 오늘의 학습` 세 섹션. 새 섹션은 이 셋과 같은 형식(이모지 + "N단계 — 제목 (날짜)" 헤딩, 불릿 4~6개, 짧은 문장, 전문 용어에 괄호 풀이)을 따른다. 문서 맨 끝의 `*마지막 갱신: ...*` 줄도 갱신 대상이다.
    - `.planning/phases/04-step-1/04-01-SUMMARY.md` ~ `04-06-SUMMARY.md` — 실제로 무엇이 만들어졌는지의 사실 출처. 없는 일을 적지 않는다.
    - `.planning/phases/04-step-1/04-CONTEXT.md` `<decisions>` — 4단계 기록에서 이야기할 만한 결정(파일럿 먼저 확인, 정답 접기, 실습 환경 분리).
    - `scripts/check-brand.mjs` — `docs/`가 검사 대상이므로 이 파일에 쓰는 모든 문장이 브랜드 게이트를 통과해야 한다.
  </read_first>
  <action>
`docs/making-of.md`에 4단계 기록 섹션을 추가한다. 위치는 `## 🗓 3단계 — 일정표와 오늘의 학습` 섹션의 마지막 불릿 다음, `### 6️⃣ 확인하기 🔜` 앞이다. 헤딩은 앞 세 단계와 같은 꼴로 짓는다 — 이모지 하나 + `4단계 — {한 줄 제목} ({날짜})`.

기존 세 단계와 같은 톤으로 쓴다: 완료된 사실을 짧은 문장으로, 전문 용어는 괄호 한 줄 풀이, 이 문서를 처음 보는 사람이 무슨 일이 있었는지 알 수 있게. 불릿 4~6개로 아래를 담는다(모두 `04-0N-SUMMARY.md`에서 확인한 사실만).
- Step 1 레슨 10편의 본문을 다 썼다는 것과, 그 결과 사이트에서 실제로 읽고 완료 체크할 수 있게 됐다는 것
- 한 편(Python 변수·자료형)을 먼저 써서 아이패드로 직접 확인받고, 그 표준으로 나머지 아홉 편을 한꺼번에 썼다는 순서
- 글쓰기 방식이 바뀌었다는 것 — 짧은 문장, 비유 먼저, 용어마다 괄호 풀이. 이 소개 페이지와 같은 방식을 레슨에 옮긴 것이라는 연결
- 읽는 시간보다 직접 해 보는 시간이 길도록 "해보기" 과제와 접히는 정답을 넣었다는 것
- 실습 환경을 나눈 것 — 읽기는 아이패드, 파이썬은 PC, SQL은 브라우저. 연습용 데이터는 사이트 진도 데이터와 완전히 분리했다는 것
- 레슨 구조를 자동으로 검사하는 장치를 새로 만들어, 아홉 편을 사람이 일일이 안 보고도 완료 처리할 수 있게 했다는 것

교육기관명을 쓰지 않고 "AI Engineer 교육과정"으로만 표기한다(`check-brand.mjs`가 `docs/`를 검사한다). 이메일 주소를 넣지 않는다.

마지막으로 문서 끝의 `*마지막 갱신: ...*` 줄을 오늘 날짜와 현재 상태에 맞게 고친다 — 4단계까지 완료됐고 Step 1 학습이 실서비스에서 가능하다는 사실을 담는다.
  </action>
  <verify>
    <automated>node scripts/check-brand.mjs && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-brand.mjs`가 exit 0이다.
    - `docs/making-of.md`에 `4단계` 를 포함한 `## ` 레벨 헤딩이 정확히 1개 추가되었고, 그 위치가 `3단계` 섹션 다음이며 `### 6️⃣ 확인하기` 앞이다.
    - 새 섹션의 불릿 개수가 4개 이상 6개 이하다.
    - `*마지막 갱신:` 로 시작하는 줄이 이번 커밋에서 변경되었다.
    - `grep -ciE 'kant' docs/making-of.md`가 0이다.
    - `grep -cE '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}' docs/making-of.md`가 0이다.
    - 새 섹션이 언급하는 사실이 전부 `04-01-SUMMARY.md` ~ `04-06-SUMMARY.md`에서 확인 가능하다 (없는 일이 적혀 있지 않다).
    - `git diff --name-only`에 `docs/making-of.md` 외의 파일이 나타나지 않는다.
  </acceptance_criteria>
  <done>소개 페이지가 4단계까지의 이야기를 담고, 처음 보는 사람도 이 프로젝트가 지금 어디까지 왔는지 알 수 있다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Phase 4 종단 스위트 + 프로덕션 배포</name>
  <precondition>저장소 루트에 `.env.local`이 존재하고 `scripts/e2e-progress.mjs`·`scripts/e2e-today.mjs`가 요구하는 Supabase 환경변수를 담고 있다 (`node --env-file=.env.local`로 로드된다). 그리고 `git remote -v`에 `origin`이 있고 현재 브랜치가 `master`이며 푸시 권한이 있다. 둘 중 하나라도 없으면 중단하고 사용자에게 알린다 — 이 Task는 e2e 실측과 배포가 전부다.</precondition>
  <files>(없음 — 이 Task는 검증·배포만 하고 파일을 만들거나 고치지 않는다)</files>
  <read_first>
    - `scripts/e2e-progress.mjs` 1~30행과 85~95행 — 실행 방법(`node --env-file=.env.local`), 프로브 레슨을 `LESSONS.find(l => l.hasContent)`로 매니페스트에서 동적으로 뽑는다는 사실(그래서 스크립트 수정이 불필요하다), 개발 서버를 직접 띄운다는 것과 포트(`E2E_PORT`, 기본 3210).
    - `scripts/e2e-today.mjs` — 시나리오 목록과 매니페스트 독립 재파싱 방식.
    - `scripts/check-supabase-progress.mjs` — e2e가 남긴 프로브 행이 있는지 확인하는 방법.
    - `.planning/phases/03-schedule-and-today/03-04-PLAN.md` `<verification>` — Phase 3이 세운 종단 스위트의 구성. 이번 스위트는 그 위에 `check-lesson-structure`를 더한 것이다.
    - `.planning/phases/04-step-1/04-VALIDATION.md` §Sampling Rate·§Per-Task Verification Map — 이 Task가 만족시켜야 할 phase gate 정의.
    - `docs/making-of.md` — 프로덕션 URL(`https://ai-engineer-runway.vercel.app`)과 기본 브랜치가 `master`라는 사실.
  </read_first>
  <action>
Phase 4 종단 스위트를 순서대로 돌린다. 하나라도 red면 거기서 멈추고 원인을 SUMMARY에 기록한다 — red를 우회하거나 게이트를 느슨하게 고쳐서 통과시키지 않는다.

1. `npm run build`
2. `node scripts/check-manifest.mjs` — 13 불변식, 이제 기대 개수 11
3. `node scripts/check-lesson-structure.mjs` — Step 1 실콘텐츠 10편의 6단 구조·해보기·정답 블록·용어 표
4. `node scripts/check-brand.mjs` — `src/`·`docs/`·`public/`·`README.md` 전체
5. `node scripts/check-progress-math.mjs`
6. `node scripts/check-schedule.mjs`
7. `node scripts/check-pace.mjs`
8. `node scripts/check-progress-gates.mjs`
9. `node --env-file=.env.local scripts/e2e-progress.mjs`
10. `node --env-file=.env.local scripts/e2e-today.mjs`
11. `node --env-file=.env.local scripts/check-supabase-progress.mjs` — e2e가 남긴 프로브 행이 없는지 확인

5~8번은 Phase 3이 세운 상시 게이트다. 이 phase가 플랫폼 코드를 거의 건드리지 않았으므로 통과해야 정상이며, 만약 깨진다면 콘텐츠 추가가 진행률·일정·페이스 계산에 영향을 준 것이므로 반드시 원인을 찾는다.

9·10번이 이 phase의 핵심 검증이다 — ROADMAP 성공 기준 4("Step 1 레슨을 진행하면 Step 1 진행률과 오늘의 학습 뷰가 실제로 채워지며 학습 루프가 끝까지 동작한다")를 자동으로 실측하는 유일한 수단이다. `e2e-progress.mjs`는 프로브 레슨을 매니페스트에서 동적으로 뽑으므로 스크립트를 고칠 필요가 없다. 실행 결과에서 프로브로 선택된 레슨 슬러그가 무엇이었는지 SUMMARY에 기록한다.

스위트가 전부 green이면 `git push origin master`로 배포한다. Vercel 배포가 끝날 때까지 기다린 뒤 프로덕션에서 확인한다:
- Step 1 10편 각각의 `/lesson/{slug}`가 200을 반환하고, 본문에 `## 1. 학습 목표`에 대응하는 렌더 결과(예: "학습 목표" 텍스트)와 `정답 보기` 문자열이 포함된다.
- 아직 안 쓴 레슨 하나(예: Step 3의 아무 레슨)를 열어 새 빈 상태 문구가 나오는지 확인한다.
- 홈(`/`)과 `/curriculum`이 200을 반환하고, Step 1 카드의 진행률 표시가 렌더된다.

마지막으로 STATE.md 갱신·ROADMAP 체크박스 처리는 실행 워크플로가 담당하므로 이 Task에서 손대지 않는다. SUMMARY에 스위트 11개 항목의 결과와 프로덕션 확인 결과를 표로 남긴다.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-manifest.mjs && node scripts/check-lesson-structure.mjs && node scripts/check-brand.mjs && node scripts/check-progress-math.mjs && node scripts/check-schedule.mjs && node scripts/check-pace.mjs && node scripts/check-progress-gates.mjs && node --env-file=.env.local scripts/e2e-progress.mjs && node --env-file=.env.local scripts/e2e-today.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 위 verify의 10개 명령이 전부 exit 0으로 끝난다.
    - `node --env-file=.env.local scripts/check-supabase-progress.mjs`가 exit 0이고, e2e가 남긴 프로브 행이 Supabase `progress` 테이블에 남아 있지 않다.
    - `e2e-progress.mjs` 출력에 프로브 레슨 슬러그가 나타나고 그것이 Step 1의 실콘텐츠 레슨 중 하나다.
    - `git log origin/master -1 --oneline`이 이 Plan의 마지막 커밋을 가리킨다 (푸시 완료).
    - Step 1 10편 각각에 대해 `curl -s -o /dev/null -w "%{http_code}" https://ai-engineer-runway.vercel.app/lesson/{slug}` 가 `200`을 반환한다 (10회 전부).
    - 위 10개 URL 각각의 본문에 `정답 보기` 문자열이 포함된다.
    - 아직 작성되지 않은 레슨 URL 하나의 본문에 `콘텐츠 준비 중입니다`와 `Step 1의 레슨 10편`이 포함된다.
    - `https://ai-engineer-runway.vercel.app/` 와 `/curriculum` 이 각각 `200`을 반환한다.
    - `git status --porcelain`이 비어 있다 (임시 파일·빌드 산출물이 남지 않았다).
    - `git diff --name-only HEAD~1 HEAD`에 `package.json`이 없다.
  </acceptance_criteria>
  <done>Phase 4의 전체 게이트가 green이고, Step 1 10편이 프로덕션에서 읽히며, 완료 체크 → 진행률 → 오늘의 학습 루프가 실제 Supabase 데이터로 왕복하는 것이 자동으로 확인됐다. 배포 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트 상수 ↔ 실제 매니페스트 | 상수가 실제보다 느슨해지면 이후 phase에서 콘텐츠 누락이 조용히 통과한다 |
| `page.tsx` 빈 상태 문구 → 익명 방문자 | 공개 표면. 카피 계약 문서와 어긋나면 다음 phase가 어느 쪽을 진실로 볼지 알 수 없다 |
| e2e 스크립트 → 실제 Supabase `progress` 테이블 | 검증용 스크립트가 실데이터에 쓰기를 한다. 프로브 행이 남으면 진행률이 오염된다 |
| `docs/making-of.md` → 공개 소개 페이지 | 브랜드 게이트 검사 대상 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-20 | Tampering | `check-manifest.mjs` 상수를 실제에 맞추는 대신 게이트를 느슨하게 고침 | high | mitigate | 상수를 기억이 아니라 `.velite/lessons.json`에서 직접 읽어 만들고, acceptance criteria가 상수 배열과 실제 슬러그 집합의 정확한 일치를 `node -e`로 검증한다. Invariant 로직 줄과 다른 3개 상수의 무변경도 `git diff`로 확인한다 |
| T-04-21 | Tampering | e2e 프로브 행이 Supabase에 잔존해 진행률 오염 | medium | mitigate | Task 3이 `check-supabase-progress.mjs`로 잔여 행을 확인하는 것을 acceptance criteria에 넣는다 (Phase 3에서 확립된 관행) |
| T-04-04 | Information Disclosure | `docs/making-of.md`·`page.tsx`(공개 표면)의 브랜드·이메일 | high | mitigate | `node scripts/check-brand.mjs`가 `src/`·`docs/`·`public/`·`README.md`를 상시 검사하며 세 Task 모두의 verify에 포함된다. Task 2는 별도로 파일 단위 grep 0건을 확인 |
| T-04-22 | Information Disclosure | `.env.local`의 Supabase 시크릿이 로그·커밋에 노출 | high | mitigate | e2e 스크립트는 설계상 쿠키 값·시크릿을 출력하지 않는다(스크립트 헤더 주석에 명시). 이 Plan은 `.env.local`을 읽거나 옮기거나 커밋하지 않고 `--env-file`로만 넘긴다. `git status --porcelain`이 비어 있음을 확인한다 |
| T-04-23 | Tampering | 이 Plan이 레슨 본문을 "조금만" 고쳐 Wave 2의 결과를 덮어씀 | medium | mitigate | prohibitions에 기록하고, Task 1·2의 acceptance criteria가 `git diff --name-only`에 `src/content/lessons/` 하위 파일이 없음을 확인한다 |
| T-04-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 패키지를 설치하지 않는다 — RESEARCH.md `## Package Legitimacy Audit`이 이 phase의 설치 대상 없음을 확정했다. acceptance criteria가 `package.json` 무변경을 확인한다 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

Spec-less probe fallback §A의 6개 edge 프로브(CONT-02·CONT-03 × empty/ordering/adjacency)는 전부 `unresolved`이며, 이 phase에서의 해석은 `04-01-PLAN.md`의 `<flagged_assumptions>`에 기록되어 있다. 이 Plan은 그 셋 모두를 자동으로 닫는 지점이기도 하다 — empty는 `check-lesson-structure.mjs`가 실콘텐츠 10편 전부를 검사하며, ordering·adjacency는 `check-manifest.mjs` 13 불변식이 이 Plan에서 다시 green으로 확인된다.

이 Plan 고유의 가정:
- **`e2e-progress.mjs`·`e2e-today.mjs`가 수정 없이 동작한다:** RESEARCH `## Validation Architecture`가 두 스크립트 모두 프로브 레슨을 매니페스트에서 동적으로 뽑는 것을 코드로 확인했다(`e2e-progress.mjs:91`). 만약 실행이 실패하면 스크립트를 고치기 전에 원인을 먼저 확인한다 — 콘텐츠가 늘어 첫 실콘텐츠 레슨이 바뀐 것이 원인일 수 있고, 그것 자체는 정상 동작이다.
- **Step 2·3 스텁 `.mdx`의 본문 텍스트는 손대지 않는다:** 24개 스텁 파일의 본문에는 여전히 옛 안내 문장("Step 1 Python 변수·자료형 ... 파일럿 레슨에서")이 남아 있다. 그러나 `hasContent: false`인 레슨은 본문이 렌더되지 않고 `page.tsx`의 JSX 문구가 대신 나오므로 사용자에게는 보이지 않는다. 24개 파일을 건드리는 비용 대비 이득이 없어 그대로 둔다 — Phase 5가 그 본문들을 어차피 교체한다.
- **Vercel 자동 배포:** `master` 푸시가 프로덕션 배포를 트리거한다는 것은 Phase 1~3에서 반복 확인된 사실이다. 배포가 실패하면 Task 3은 프로덕션 확인 항목에서 멈추고 그 사실을 SUMMARY에 남긴다.
- **D-59의 대가:** 이 phase는 9편에 사람 검토를 두지 않았다. 자동 게이트는 구조·브랜드·빌드·루프 동작을 확인하지만 **프로즈의 품질과 예제 코드의 교육적 정확성은 확인하지 못한다.** 사용자는 실제 사전학습 중 발견하는 문제를 quick task로 고치게 된다 — 이것이 D-59에서 명시적으로 받아들인 트레이드오프다.
</flagged_assumptions>

<verification>
Phase 4 종단 스위트 (전부 green이어야 phase가 닫힌다):
1. `npm run build`
2. `node scripts/check-manifest.mjs` — 13 불변식, 기대 개수 11
3. `node scripts/check-lesson-structure.mjs` — Step 1 실콘텐츠 10편
4. `node scripts/check-brand.mjs`
5. `node scripts/check-progress-math.mjs`
6. `node scripts/check-schedule.mjs`
7. `node scripts/check-pace.mjs`
8. `node scripts/check-progress-gates.mjs`
9. `node --env-file=.env.local scripts/e2e-progress.mjs`
10. `node --env-file=.env.local scripts/e2e-today.mjs`
11. `node --env-file=.env.local scripts/check-supabase-progress.mjs` — 프로브 행 잔여 확인
12. 프로덕션 확인 — Step 1 10편 200 + `정답 보기` 포함, 미작성 레슨 1편의 새 빈 상태 문구, `/`·`/curriculum` 200
</verification>

<success_criteria>
- `check-manifest.mjs` 기대 상수가 실제 매니페스트(11편)와 일치하고 게이트가 green이다
- 빈 상태 안내 문구가 Step 1 완성 사실을 반영하고 UI-SPEC 계약과 일치한다
- making-of가 4단계까지의 이야기를 담는다
- e2e 두 개가 통과해 진행률·오늘의 학습 루프가 Step 1 콘텐츠로 실제 왕복함이 실측됐다
- Phase 3의 상시 게이트 5종이 회귀 없이 통과한다
- Step 1 10편이 프로덕션에서 읽힌다
</success_criteria>

<output>
Create `.planning/phases/04-step-1/04-07-SUMMARY.md` when done.

SUMMARY에 반드시 담을 것:
- 종단 스위트 12개 항목의 결과 표
- `e2e-progress.mjs`가 뽑은 프로브 레슨 슬러그
- 프로덕션 확인 결과(10편 상태 코드, 빈 상태 문구 확인)
- Wave 2 다섯 Plan의 SUMMARY에 기록된 "실행자가 확인하지 못한 것"(예: Supabase SQL 에디터 미접근)의 취합 — 사용자가 실제 학습 중 무엇을 눈여겨봐야 하는지
</output>
