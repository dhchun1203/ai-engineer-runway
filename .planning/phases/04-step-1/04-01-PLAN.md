---
phase: 04-step-1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/lessons/step-1/1-3-python-variables-and-types.mdx
  - src/app/globals.css
  - scripts/check-lesson-structure.mjs
autonomous: false
requirements: [CONT-02, CONT-03]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "프로덕션 URL `/lesson/1-3-python-variables-and-types`가 eli5 톤으로 재작성된 6단 구조 본문을 렌더한다 — 6개 헤딩이 `## 1. 학습 목표`부터 `## 6. 핵심 정리 및 스스로 점검`까지 원문 그대로 순서대로 존재한다 (D-47, D-10)"
    - "본문이 변수·자료형뿐 아니라 조건문·반복문까지 다뤄 `.planning/curriculum.md` 1-3 첫 불릿(변수, 자료형, 조건문, 반복문)의 범위를 전부 채운다 (RESEARCH Pitfall 1 / Open Question 1)"
    - "`### 해보기` 과제가 2~3개 있고 각각 바로 아래 접힌 `<details><summary>정답 보기</summary>` 블록에 예상 출력이 들어 있으며, 아이패드 터치로 펼쳐진다 (D-49, D-61)"
    - "`<details>` 안의 마크다운(굵게·인라인 코드·리스트)이 raw 텍스트가 아니라 정상 서식으로 렌더된다 — `<summary>` 뒤·`</details>` 앞 빈 줄 규칙이 이 저장소의 Velite/MDX 파이프라인에서 실제로 통한다 (RESEARCH Pitfall 3 / Assumption A3)"
    - "`powershell` 코드펜스가 이 저장소의 rehype-pretty-code/Shiki 설정에서 색상 하이라이팅되어 렌더된다 — 단색 폴백이 아니다 (RESEARCH Pitfall 4 / Assumption A2, D-58)"
    - "`## 6. 핵심 정리 및 스스로 점검` 안에 `**이 레슨의 단어**` 표가 있고 5~8개 용어의 한 줄 뜻을 담는다 (D-50)"
    - "`scripts/check-lesson-structure.mjs`가 존재하고, `hasContent: true`인 Step 1 레슨의 6단 헤딩·해보기 개수·`<details>` 짝·용어 표 행수를 정규식으로 검사해 exit 0/1로 판정한다 (D-59가 9편에서 사람 검토를 없앤 대가로 필요한 구조 안전망)"
    - "`node scripts/check-manifest.mjs`가 이 Plan 이후에도 통과한다 — 파일럿은 이미 `hasContent: true`이므로 Invariant 10의 기대 개수 2와 슬러그 2개가 그대로다 (RESEARCH Pitfall 5)"
    - "`src/content/lessons/step-1/1-3-python-variables-and-types.mdx`의 프론트매터 8줄(title·stepId·moduleId·order·depth·estimatedMinutes·hasContent·slug)이 이 Plan 전후로 바이트 단위 동일하다 (D-13)"
    - statement: "재작성된 본문의 읽기 산문(② 왜 배우나 · ③ 개념 설명 · ⑤ 실무 팁)이 구 버전보다 짧고, 늘어난 분량은 해보기·정답 블록·용어 표가 차지한다 — 글이 150분을 채우지 않는다 (D-49)"
      verification: backstop
    - statement: "아이패드 Safari 세로·가로 모드에서 `<summary>` 터치 타깃이 44px 이상이고 다크모드에서도 대비가 유지된다"
      verification: backstop
  artifacts:
    - src/content/lessons/step-1/1-3-python-variables-and-types.mdx
    - src/app/globals.css
    - scripts/check-lesson-structure.mjs
  key_links:
    - "`.mdx` 본문 → Velite(Zod 스키마) → rehype-pretty-code/Shiki → `.velite/lessons.json` → `MDXContent` → `/lesson/[lessonId]` — 이 Plan의 tracer가 통과하는 전체 경로. 어느 한 마디라도 끊기면 빌드가 깨지거나 하이라이팅이 폴백된다"
    - "`<details>` 앞뒤 빈 줄 → CommonMark HTML-block 경계 → MDX 내부 마크다운 재진입 — 빈 줄이 빠지면 정답 텍스트에 백틱·별표가 그대로 노출된다"
    - "`.prose details/summary` CSS → 아이패드 터치 타깃 — `.prose pre button.rehype-pretty-copy` 규칙 쌍(라이트 기본 + `.dark` 오버라이드)과 같은 형태로 붙어야 specificity가 충분하다"
    - "`scripts/check-lesson-structure.mjs` → Wave 2의 9편 — 이 스크립트가 파일럿에서 green이어야 9편 executor가 같은 게이트를 신뢰할 수 있다"
  prohibitions:
    - statement: "레슨 본문·코드 주석·메타데이터 어디에도 교육기관명(`scripts/check-brand.mjs`의 `FORBIDDEN_BRAND_STRINGS`에 하드코딩된 문자열)을 노출해서는 안 된다 — 항상 'AI Engineer 교육과정'으로 표기한다 (프로젝트 HARD RULE, D-02)"
      status: unverified
    - statement: "Mermaid·SVG·이미지·ASCII 다이어그램·새 MDX 컴포넌트·퀴즈 엔진을 도입해서는 안 된다 — 큰 그림은 이모지 헤더·표·`A → B → C` 한 줄로만 표현한다 (D-48, PITFALLS Pitfall 4)"
      status: unverified
    - statement: "프론트매터의 `hasContent` 외 어떤 필드도 바꿔서는 안 된다 — title·moduleId·order·depth·estimatedMinutes·slug는 Phase 1에서 확정된 값이다 (D-13)"
      status: unverified
    - statement: "브라우저 기반 온라인 노트북 실행 환경을 실습 경로로 안내해서는 안 된다 — 실습은 PC 로컬 Python으로 통일한다 (D-55)"
      status: unverified
---

<objective>
Step 1 레슨 10편의 집필 표준을 파일럿 1편으로 실증하고, 그 표준이 실제 프로덕션·아이패드에서 통하는지 사람 눈으로 확인받는다.

Purpose: 이 Plan은 이 phase의 tracer다. 레슨 하나를 `.mdx` 본문부터 Velite·Shiki 파이프라인, `<details>` 렌더, `.prose` 터치 스타일, 프로덕션 배포, 아이패드 화면까지 **모든 층을 한 번 관통**시킨다. D-47(eli5 × 6단 표준)은 reversibility `costly` — 이 표준으로 Step 1 10편이 쓰이고 Phase 5가 같은 표준을 25편에 적용하므로, 틀린 채로 9편을 병렬 집필하면 10편 재작업이 된다. 승인 전에는 다른 레슨을 한 줄도 쓰지 않는다 (D-51).

Output: eli5로 재작성된 파일럿 레슨 1편, `<details>` prose 스타일, Step 1 구조 게이트 스크립트 1개, 그리고 아이패드 실기기 승인.
</objective>

<mvp_note>
MVP 모드가 켜져 있으나 ROADMAP의 Phase 4 `**Goal**` 줄이 "As a … I want to … so that …" 사용자 스토리 형식이 아니다 (현재: "학습자가 Step 1(개발 기반 구축) 전체를 사이트에서 실제로 학습하고 완료 체크할 수 있다 — 사전학습이 여기서 시작된다"). 규칙에 따라 스토리를 지어내지 않고 사실만 남긴다 — 사용자 스토리 형식이 필요하면 `/gsd mvp-phase 4`를 먼저 돌린다. MVP 모드의 실질(수직 슬라이스 · tracer 우선 분해)은 이 Plan 구조에 그대로 적용되어 있다.
</mvp_note>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-step-1/04-CONTEXT.md
@.planning/phases/04-step-1/04-RESEARCH.md
@.planning/phases/04-step-1/04-PATTERNS.md
</context>

<source_audit>
## Multi-Source Coverage Audit (Phase 4 전체)

**GOAL (ROADMAP 성공 기준 4개)**

| 항목 | 담당 Plan | 상태 |
|------|-----------|------|
| SC1 Step 1 5개 모듈 전 레슨 작성 | 01(1편) + 02·03·04·05·06(9편) | COVERED |
| SC2 비유·핵심 정리 포함 쉬운 개념 설명 | 01~06 (D-47 표준) | COVERED |
| SC3 실행 가능 실무 예제 + 언어별 하이라이팅 | 01~06 (④ 실무 예제 + 언어 펜스) | COVERED |
| SC4 진행률·오늘의 학습 루프가 실제로 채워짐 | 07 (e2e-progress · e2e-today) | COVERED |

**REQ (REQUIREMENTS.md)**

| ID | 담당 Plan | 상태 |
|----|-----------|------|
| CONT-02 | 01, 02, 03, 04, 05, 06, 07 | COVERED |
| CONT-03 | 01, 02, 03, 04, 05, 06, 07 | COVERED |

**RESEARCH (04-RESEARCH.md 발견·제약)**

| 항목 | 담당 Plan | 상태 |
|------|-----------|------|
| Pitfall 1 — 1-3 curriculum 범위 갭(조건문·반복문) | 01 (본문 확장 + 체크포인트 확인) | COVERED |
| Pitfall 2 — Windows `python` 스토어 스텁 | 02 (1-1 dev-environment-setup) | COVERED |
| Pitfall 3 — `<details>` 빈 줄 규칙 | 01 (실증) + 게이트 L4 (상시) | COVERED |
| Pitfall 4 — `sql`/`powershell` 펜스 미실증 | 01 (powershell 실증 + 언어 번들 확인), 05 (sql 빌드 확인) | COVERED |
| Pitfall 5 — check-manifest 상수 단계 구분 | 01 (무변경) + 07 (2→11) | COVERED |
| Open Q1 — 1-3 범위 결정 | 01 (체크포인트 항목 6) | COVERED |
| Open Q2 — check-brand 실행 시점 | 각 레슨 task verify + 07 최종 1회 | COVERED |
| Package Legitimacy Audit (`anthropic`, `scikit-learn` OK) | 03, 06 (정확한 패키지명 인용) | COVERED |
| Security Domain 위협 3종 | 03 (API 키), 05 (practice 스키마 · 문자열 결합 경고) | COVERED |
| Per-Lesson Content Outline 10행 | 01(1) · 02(2) · 03(2) · 04(1) · 05(2) · 06(2) | COVERED |

**CONTEXT (D-47 ~ D-61 + Claude's Discretion)**

| ID | 담당 Plan | 상태 |
|----|-----------|------|
| D-47 eli5 × 6단 | 01 표준 확정 → 02~06 적용 | COVERED |
| D-48 마크다운만 | 01~06 prohibitions | COVERED |
| D-49 150분 배분 | 01~06 (해보기 2~3개) | COVERED |
| D-50 용어 표기 + 단어 표 | 01~06 (게이트 L5) | COVERED |
| D-51 파일럿 = 1-3 재작성 | 01 | COVERED |
| D-52 9편 병렬 한 번에 | 02~06 (wave 2) + 07 단일 배포 | COVERED |
| D-53 1-1 커리큘럼 지도 | 02 | COVERED |
| D-54 1-2 Claude API 예제 | 03 | COVERED |
| D-55 PC 로컬 Python | 02, 04, 06 | COVERED |
| D-56 SQL은 Supabase 에디터 + practice 스키마 | 05 | COVERED |
| D-57 Git 실습 흐름 | 03 | COVERED |
| D-58 Windows 우선 | 01, 02, 03, 06 | COVERED |
| D-59 파일럿만 사람 검토 | 01만 autonomous:false, 02~07 autonomous:true | COVERED |
| D-60 master 푸시 → 프로덕션 아이패드 | 01 Task 2·3 | COVERED |
| D-61 `<details>` 정답 보기 | 01 (CSS + 실증) → 02~06 적용 | COVERED |
| 재량 — 9편 wave 분할 | 01에 기록: 모듈별 5 Plan 병렬 | COVERED |
| 재량 — 해보기 개수·표·이모지 | 01에서 표준 확정(해보기 2~3, 스스로 점검 2) | COVERED |
| 재량 — check-manifest 갱신 방식 | 07 | COVERED |
| 재량 — "준비 중" 카피 + UI-SPEC | 07 | COVERED |
| 재량 — `<details>` prose CSS | 01 Task 1 | COVERED |
| 재량 — ML 데이터셋 | 06 (`load_iris`, `load_diabetes`) | COVERED |
| 재량 — 1-1 설치 범위 | 02 (VS Code · Python · Git · GitHub · Supabase 계정) | COVERED |
| 재량 — making-of 4단계 | 07 | COVERED |
| 재량 — `check-lesson-structure.mjs` 채택 여부 | **채택** — 01 Task 2 | COVERED |
| Deferred Ideas | 없음 (CONTEXT.md `<deferred>`가 None) | N/A |

**MISSING 항목: 없음.** 네 소스의 모든 항목이 Plan에 배정되었다.
</source_audit>

<artifacts_produced>
## Artifacts this phase produces

**Plan 01 (이 Plan)**
- `src/content/lessons/step-1/1-3-python-variables-and-types.mdx` — 본문 전면 교체 (프론트매터 8줄 불변)
- `src/app/globals.css` — `.prose details`, `.prose summary`, `.dark .prose summary` 규칙 신설
- `scripts/check-lesson-structure.mjs` — 신설. Step 1 레슨 구조 게이트 (L1~L6)

**Phase 4 전체가 만들거나 바꾸는 것 (드리프트 검증에서 오탐하지 말 것)**
- 레슨 `.mdx` 10편: `1-1-course-orientation`, `1-1-dev-environment-setup`, `1-2-git-branch-and-pr`, `1-2-generative-ai-basics`, `1-3-python-variables-and-types`, `1-3-python-functions-and-io`, `1-4-relational-db-basics`, `1-4-sql-queries-and-joins`, `1-5-ml-model-types`, `1-5-ml-metrics-and-pipeline` (9편은 `hasContent: false → true`)
- `scripts/check-manifest.mjs` — `EXPECTED_HAS_CONTENT_COUNT` 2 → 11, `EXPECTED_HAS_CONTENT_SLUGS` 2개 → 11개 (Plan 07)
- `scripts/check-lesson-structure.mjs` — 신설 (Plan 01)
- `src/app/globals.css` — `<details>/<summary>` prose 규칙 (Plan 01)
- `src/app/lesson/[lessonId]/page.tsx` — 빈 상태 안내 문구 교체 (Plan 07)
- `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` — Copywriting Contract Empty state 행 + 빈 상태 레슨 수 33 → 24 (Plan 07)
- `docs/making-of.md` — "4단계" 기록 섹션 + 마지막 갱신 줄 (Plan 07)
- `COVERAGE.md` — 이 phase는 외부 API를 연동하지 않으므로 작성하지 않는다 (아래 `<api_coverage_note>` 참고)
</artifacts_produced>

<api_coverage_note>
외부 API 연동 없음: 1-2 레슨은 `anthropic` SDK 호출을 **교육용 예시로 보여줄** 뿐이고, 이 사이트 자체는 이 phase에서 어떤 외부 API도 호출하지 않는다. 실행자는 레슨 본문의 SDK 코드를 보고 API 통합 작업으로 오인하지 말 것 — `src/` 아래에 `anthropic` import가 추가되면 그것이 범위 이탈이다.
</api_coverage_note>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: 파일럿 레슨 eli5 재작성 + `<details>` prose 스타일 (tracer — 콘텐츠부터 화면까지 한 경로 관통)</name>
  <reversibility rating="costly">D-47(eli5 × 6단 집필 표준)을 확정하는 태스크다. 이 표준으로 Step 1 10편이 쓰이고 Phase 5가 25편에 그대로 적용하므로, 나중에 바꾸면 전 레슨 재작업이다. 게이트는 Task 3의 human-verify 체크포인트 하나이며 별도 `checkpoint:decision`을 두지 않는다 (CONTEXT.md에 이미 costly로 기록됨).</reversibility>
  <files>src/content/lessons/step-1/1-3-python-variables-and-types.mdx, src/app/globals.css</files>
  <read_first>
    - `src/content/lessons/step-1/1-3-python-variables-and-types.mdx` — 교체 대상 구 버전 전체. 프론트매터 8줄과 6개 헤딩 문구를 여기서 그대로 가져온다. 구 버전의 산문 길이가 "이보다 짧게"(D-49)의 기준선이다.
    - `docs/making-of.md` — eli5 톤의 유일한 선례이자 기준. 이모지 섹션 헤더, "한 문장으로 말하면" 같은 도입, 괄호 한 줄 풀이, 짧은 문장, 표 사용을 그대로 재료로 삼는다. 문서 전체를 읽는다.
    - `.planning/curriculum.md` §Step 1 §1-3 — 원문 불릿 두 줄. 첫 불릿이 이 레슨의 범위 원천이다.
    - `.planning/phases/04-step-1/04-RESEARCH.md` — `## Architecture Patterns` Pattern 1(6단 스켈레톤 원문 헤딩)·Pattern 2(`<details>` 빈 줄 규칙), `## Common Pitfalls` Pitfall 1·3·4, `## Architecture Patterns` §Per-Lesson Content Outline의 `1-3-python-variables-and-types` 행.
    - `.planning/phases/04-step-1/04-PATTERNS.md` — 프론트매터 패턴, 6단 헤딩 패턴, 학습 목표 패턴, 일상 비유 표 패턴, 핵심 정리 + 용어 표 + `<details>` 패턴.
    - `src/app/globals.css` 75~110행 — `.prose pre button.rehype-pretty-copy` 규칙과 그 `.dark` 오버라이드 쌍, `.prose { line-height: 1.6 }` 블록. 새 규칙을 어디에 어떤 형태로 붙일지의 정확한 본보기.
    - `velite.config.ts` — 이 파일은 **바꾸지 않는다**. rehype-pretty-code 설정과 테마 이름만 확인해 코드펜스가 어떤 경로로 처리되는지 파악한다.
    - `src/app/lesson/[lessonId]/page.tsx` 54~66행 — 본문이 `.prose dark:prose-invert` 래퍼 안에서 렌더된다는 사실 확인(새 CSS 선택자가 이 래퍼 하위에 걸린다).
  </read_first>
  <action>
`src/content/lessons/step-1/1-3-python-variables-and-types.mdx`의 **본문을 전면 교체**한다. 프론트매터 8줄(`title: "Python 변수·자료형"`, `stepId: 1`, `moduleId: "1-3"`, `order: 1`, `depth: "심화"`, `estimatedMinutes: 150`, `hasContent: true`, `slug: "1-3-python-variables-and-types"`)은 한 글자도 바꾸지 않는다. `scripts/check-manifest.mjs`도 이 Task에서 건드리지 않는다 — 파일럿은 이미 `hasContent: true`라 Invariant 10의 기대값 2가 그대로 맞다.

**헤딩 6개는 아래 문자열을 그대로, 이 순서로 쓴다:** `## 1. 학습 목표`, `## 2. 왜 배우나`, `## 3. 개념 설명`, `## 4. 실무 예제`, `## 5. 실무 팁`, `## 6. 핵심 정리 및 스스로 점검`. `##` 레벨 헤딩은 이 6개뿐이며 다른 `##`을 추가하지 않는다.

**범위 확장 (RESEARCH Pitfall 1 해소):** 이 레슨은 `curriculum.md`의 "변수, 자료형, 조건문, 반복문" 네 주제를 전부 다룬다. 제목이 앞 두 개만 담고 있지만 제목은 확정값이라 바꾸지 않는다. 학습 목표에 조건문·반복문 항목을 넣고, ③에 조건문(`if`/`elif`/`else`)과 반복문(`for`/`while`) 절을 각각 1~2단락 짧게 넣고, ④ 예제 코드에 조건 분기 확장과 회원 리스트 `for` 순회를 포함시킨다.

**각 단의 목표 형태:**
- ① 학습 목표 — "이 레슨을 마치면 다음을 할 수 있습니다." 뒤에 동사로 끝나는 관찰 가능한 결과 4개. 그중 최소 1개는 조건문, 1개는 반복문에 관한 것.
- ② 왜 배우나 — 최대 4문장 1문단. 전문 용어 없이, 이 개념이 커리큘럼의 어디(함수·API 응답 처리·데이터 전처리)로 이어지는지만 말한다. 구 버전의 이 절보다 짧게 줄인다.
- ③ 개념 설명 — `###` 소제목 3~4개. 각 소제목 앞에 이모지 하나를 붙인다(D-48이 허용하는 유일한 시각 장치). 비유를 먼저 주고 정의는 뒤에 준다: 변수=이름표 붙은 상자, 자료형 4종=일상 비유 표, 조건문=갈림길, 반복문=컨베이어벨트. 자료형 표는 구 버전의 4열(`자료형` · `이름` · `예시` · `일상 비유`)을 유지한다. 용어는 첫 등장 시 "한글(English, 한 줄 풀이)" 형태로 풀고(예: 동적 타이핑(dynamic typing, 값을 넣는 순간 자료형이 정해지는 방식)) 이후 등장은 한글만 쓴다.
- ④ 실무 예제 — 완결된 단일 `python` 코드펜스 하나. 파일명은 `variables.py`, 첫 줄은 파일명 주석. 내용: 회원 정보 변수 4개 → `type()` 확인 → f-string 조합 → `if`/`elif`/`else` 3분기(프리미엄/일반/휴면 등) → 회원 3명 리스트를 `for`로 순회하며 출력. 그대로 복사해 붙이면 돌아가야 한다. 코드펜스 바로 뒤에 실행 방법을 `powershell` 펜스로 준다 — 내용은 `py variables.py` 한 줄. 그 아래 macOS 병기 한 줄을 산문으로 덧붙인다(`python3 variables.py`). `python 파일명`을 1차 안내로 쓰지 않는다(RESEARCH Pitfall 2 — Windows에서 스토어 스텁이 먼저 잡힌다). 그다음 예상 출력을 산문 2~3줄로 설명한다.
- ④ 안의 `### 해보기` — 소제목 `### 해보기`를 **2개 또는 3개** 만든다. 각 해보기는 위 예제를 고쳐 돌려 보는 과제 한 문단이고, 바로 아래에 접힌 정답 블록을 둔다. 정답 블록 형식은 `<details>` 한 줄 → `<summary>정답 보기</summary>` 한 줄 → **빈 줄** → 예상 출력/풀이 → **빈 줄** → `</details>` 한 줄. 빈 줄 두 개는 필수다(CommonMark HTML-block 경계 — 빠지면 정답 안의 백틱·별표가 그대로 노출된다). 과제 소재: 조건 분기 값을 바꿔 실행, `for` 순회 대상을 늘려 출력 확인, (선택) 자료형을 일부러 섞어 `TypeError`를 만들어 보기.
- ⑤ 실무 팁 — 불릿 3~4개, 각 불릿 2문장 이내. 구 버전 4개 팁을 압축하고 `py` 런처 관련 한 줄을 넣는다.
- ⑥ 핵심 정리 및 스스로 점검 — `**핵심 정리**` 굵은 라벨 뒤 요약 불릿 4~5개 → `**이 레슨의 단어**` 굵은 라벨 뒤 2열 표(헤더 `| 단어 | 뜻 |`, 데이터 행 5~8개, 각 뜻은 한 줄) → `**스스로 점검**` 굵은 라벨 뒤 번호 매긴 질문 **2개**, 각 질문 바로 아래에 위와 같은 형식의 접힌 정답 블록.

**분량 기준(D-49):** 읽기 산문은 구 버전보다 짧아야 한다. 늘어난 분량은 해보기·정답 블록·용어 표가 차지한다. 문장은 짧게 끊고, 한 문장에 쉼표를 세 번 이상 넣지 않는다.

`src/app/globals.css`의 `.prose { line-height: 1.6 }` 블록 **바로 아래**에 `<details>/<summary>` 규칙을 추가한다. `.prose pre button.rehype-pretty-copy` 규칙 쌍과 같은 형태로 쓴다 — 라이트 기본 규칙 하나 + `.dark` 오버라이드 하나, 선택자는 `.prose` 하위로 한정. 요구 사항: `.prose summary`가 `min-height: 44px`(D-61 아이패드 터치) 이상이고 `cursor: pointer`, `list-style` 기본 마커를 유지하거나 대체하되 펼침 여부가 눈에 보여야 하며, `.prose details`가 접힌 상태와 펼친 상태 모두 본문과 구분되는 얕은 배경/테두리를 갖는다. 색은 이미 있는 토큰(`--color-surface`, `--color-badge-neutral-bg`, `--color-badge-neutral-bg-dark`, `--color-accent`)만 쓰고 새 색을 만들지 않는다. 규칙 위에 왜 이렇게 쓰는지(D-61, 44px, 다크 쌍) 주석을 남긴다.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-manifest.mjs && node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`가 exit 0으로 끝난다.
    - `node scripts/check-manifest.mjs`가 exit 0이고 "all 13 invariants passed" 를 출력한다 (기대 개수는 여전히 2).
    - `node scripts/check-brand.mjs`가 exit 0이다.
    - `git diff src/content/lessons/step-1/1-3-python-variables-and-types.mdx`의 변경 범위가 11행(본문 시작) 이후로만 한정된다 — 프론트매터 8줄과 앞뒤 `---` 구분선은 diff에 나타나지 않는다.
    - 파일에 `## 1. 학습 목표`, `## 2. 왜 배우나`, `## 3. 개념 설명`, `## 4. 실무 예제`, `## 5. 실무 팁`, `## 6. 핵심 정리 및 스스로 점검`이 각각 정확히 1회씩 등장하고, `grep -n '^## ' 파일`의 출력 줄 수가 6이며 순서가 1→6이다.
    - `grep -c '^### 해보기$' 파일`의 결과가 2 또는 3이다.
    - `grep -c '<details>' 파일` · `grep -c '<summary>정답 보기</summary>' 파일` · `grep -c '</details>' 파일` 세 값이 모두 같고, 그 값이 `### 해보기` 개수 + 2 이상이다.
    - 모든 `<summary>정답 보기</summary>` 줄의 다음 줄이 빈 줄이고, 모든 `</details>` 줄의 이전 줄이 빈 줄이다.
    - 파일에 ```` ```powershell ```` 펜스가 최소 1개 있고 그 안에 `py variables.py`가 있다. ```` ```bash ```` 펜스로 Python 실행을 안내하지 않는다.
    - 파일에 ```` ```python ```` 펜스가 있고 그 안에 `if `, `elif `, `else:`, `for ` 가 모두 등장한다.
    - `**이 레슨의 단어**` 라벨이 정확히 1회 등장하고, 그 뒤 표의 헤더가 `| 단어 | 뜻 |`이며 구분선 아래 데이터 행이 5개 이상 8개 이하다.
    - `grep -cE '^\*\*스스로 점검\*\*$' 파일`이 1이고 그 아래 번호 매긴 항목이 2개다.
    - `grep -ciE 'kant' 파일`이 0이다.
    - `grep -c 'sk-ant-' 파일`이 0이다.
    - `src/app/globals.css`에 `.prose summary` 규칙과 `.dark .prose summary`(또는 `.dark .prose details`) 오버라이드가 모두 존재하고, `.prose summary` 규칙 본문에 `min-height: 44px` 이상의 값이 들어 있다.
    - `git diff --name-only`에 `velite.config.ts`, `package.json`, `scripts/check-manifest.mjs`가 나타나지 않는다.
    - 빌드 산출물에서 `powershell` 코드블록이 단색이 아니다: `node -e "const l=require('./.velite/lessons.json');const p=l.find(x=>x.slug==='1-3-python-variables-and-types');const m=p.code.match(/language-powershell/);process.exit(m?0:1)"` 가 exit 0이고, 같은 블록의 렌더 HTML에 `--shiki-light` 또는 `style` 색 스팬이 2개 이상 포함된다.
  </acceptance_criteria>
  <done>파일럿 레슨이 eli5 × 6단 구조로 다시 쓰였고, 조건문·반복문까지 범위를 채우며, `powershell` 펜스와 `<details>` 정답 블록이 실제 빌드에서 살아 있다. 기존 13개 불변식은 그대로 통과한다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Step 1 구조 게이트 신설 + 언어 번들 확인 + 프로덕션 배포</name>
  <precondition>`git remote -v`에 `origin`이 있고 그 URL이 `github.com/dhchun1203/ai-engineer-runway`이며, 현재 브랜치가 `master`이고 푸시 권한이 있다. 없으면 중단하고 사용자에게 알린다 — 이 Task의 목적 자체가 프로덕션 배포다 (D-60).</precondition>
  <files>scripts/check-lesson-structure.mjs</files>
  <read_first>
    - `scripts/check-brand.mjs` 1~40행 — 의존성 0, 파일 순회, `ROOT = process.cwd()`, 대상 경로 상수, 헤더 주석이 "왜 이 대상만 검사하는가"를 설명하는 방식. 새 스크립트가 그대로 따를 형식.
    - `scripts/check-manifest.mjs` 1~35행과 260~278행 — 기대값 상수를 파일 상단에 모아 두는 관례, `const errors = []` + `fail(message)` 누적, 마지막에 오류 개수 출력 후 `process.exit(1)`, 통과 시 요약 한 줄 + `process.exit(0)`.
    - `src/content/lessons/step-1/1-3-python-variables-and-types.mdx` (Task 1 완료본) — 이 게이트가 통과시켜야 하는 유일한 실제 입력.
    - `src/content/lessons/step-2/2-3-react-components.mdx` — Step 2 파일럿. 이 게이트의 **검사 대상이 아니다**(구 표준으로 쓰였고 해보기·`<details>`가 없다). 스코프를 Step 1로 한정하는 이유를 확인한다.
    - `.planning/phases/04-step-1/04-VALIDATION.md` §Wave 0 Requirements — 이 스크립트가 왜 필요한지(D-59가 9편의 사람 검토를 없앴다)의 근거.
  </read_first>
  <action>
`scripts/check-lesson-structure.mjs`를 신설한다. Node 표준 모듈만 쓰고 외부 의존성 0이며, `.velite/` 빌드 산출물이 아니라 **원본 `.mdx` 파일을 직접 읽는다**(`<details>` 마크업과 빈 줄은 컴파일 후에는 확인할 수 없다). 검사 대상은 `src/content/lessons/step-1/*.mdx` 중 프론트매터에 `hasContent: true`가 있는 파일만이다 — `false`인 스텁은 건너뛴다. Step 2·3 디렉터리는 검사하지 않는다(구 표준으로 쓰인 `2-3-react-components`가 오탐으로 걸리므로). 이 스코프 결정의 이유를 파일 상단 주석에 남긴다.

기대값을 파일 상단 상수로 모은다: 6개 헤딩 문자열 배열(`## 1. 학습 목표` … `## 6. 핵심 정리 및 스스로 점검`), 해보기 최소·최대(2, 3), 용어 표 행 최소·최대(5, 8), 정답 요약 라벨(`정답 보기`), 용어 표 라벨(`이 레슨의 단어`), 표 헤더(`| 단어 | 뜻 |`).

파일마다 아래 6개 검사를 돌리고 실패를 `errors` 배열에 누적한다(첫 실패에서 멈추지 않는다 — 9편을 한 번에 고칠 수 있어야 한다). 각 오류 메시지는 slug와 무엇이 몇 개였는지를 담는다.
- **L1 헤딩** — 6개 문자열이 각각 정확히 1회 등장하고, 파일 내 등장 위치(인덱스)가 1→6 순으로 증가한다. `##` 레벨 헤딩의 총 개수가 6인지도 함께 본다.
- **L2 해보기** — 줄 전체가 `### 해보기`인 줄의 개수가 2 이상 3 이하다.
- **L3 정답 블록 짝** — `<details>` · `<summary>정답 보기</summary>` · `</details>` 세 토큰의 개수가 서로 같고, 그 값이 L2에서 센 해보기 개수 + 2 이상이다(해보기 정답 + 스스로 점검 2문항 정답).
- **L4 빈 줄 규칙** — 모든 `<summary>` 줄의 바로 다음 줄이 빈 줄이고, 모든 `</details>` 줄의 바로 이전 줄이 빈 줄이다. 위반한 줄 번호를 오류에 적는다(RESEARCH Pitfall 3의 유일한 자동 방어선).
- **L5 용어 표** — `이 레슨의 단어` 라벨이 정확히 1회 등장하고, 그 아래 첫 표의 헤더 줄이 기대 헤더와 일치하며, 구분선(`|---|`) 다음부터 표가 끝날 때까지의 데이터 행 개수가 5 이상 8 이하다.
- **L6 코드펜스 언어** — 파일에 등장하는 모든 여는 코드펜스가 언어 태그를 갖는다(태그 없는 벌거벗은 펜스 금지 — 하이라이팅이 조용히 사라지는 경로). 허용 언어 목록은 상수로 두고 `python`·`sql`·`bash`·`powershell`·`text`로 시작한다. 목록 밖 언어가 나오면 오류로 잡되, 메시지에 "허용 목록에 추가하려면 이 상수를 고치라"고 적는다.

`hasContent: true`인 대상 파일이 0개면 그것 자체를 오류로 처리한다(스크립트가 아무것도 검사하지 않고 조용히 통과하는 경로를 막는다). 마지막에 `check-manifest.mjs`와 같은 형식으로 오류 개수를 출력하고 `process.exit(1)`, 통과 시 "N개 레슨, 6개 검사 통과" 요약 한 줄과 `process.exit(0)`.

스크립트를 만든 뒤 **일부러 한 번 red를 확인한다**: 파일럿의 `<summary>` 다음 빈 줄을 임시로 지워 L4가 exit 1로 잡는지 보고, 되돌린 뒤 다시 green을 확인한다. 이 확인 없이는 게이트가 실제로 무언가를 막는지 알 수 없다.

그다음 `sql`/`powershell` 문법이 이 저장소에 설치된 Shiki 번들에 실제로 들어 있는지 확인한다 — 임시 레슨 파일을 만들지 않고, 설치된 `shiki`에서 하이라이터를 만들어 두 언어를 로드해 본다(`createHighlighter`에 두 언어를 넘기고 `getLoadedLanguages()` 결과에 둘 다 있는지 확인하는 일회성 `node -e` 실행). `powershell`은 Task 1의 파일럿이 이미 실제 빌드로 증명했으므로, 여기서 확인이 필요한 것은 주로 `sql`이다(Plan 05가 의존한다). 실패하면 별칭(`postgres`, `ps1`)으로 재시도하고, 어느 쪽이 통하는지 SUMMARY에 기록한다 — Plan 05·02·03·06이 그 결과를 그대로 쓴다.

마지막으로 전체 게이트를 돌려 green을 확인하고 `git push origin master`로 프로덕션에 배포한다. Vercel 배포가 끝날 때까지 기다린 뒤 프로덕션 URL `https://ai-engineer-runway.vercel.app/lesson/1-3-python-variables-and-types`가 200으로 응답하고 본문에 `정답 보기` 문자열이 포함되는지 확인한다. 배포가 실패하면 여기서 멈추고 원인을 SUMMARY에 남긴다 — 다음 Task는 프로덕션에서만 확인할 수 있다.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-lesson-structure.mjs && node scripts/check-manifest.mjs && node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-lesson-structure.mjs`가 exit 0이고 검사한 레슨 수 1을 출력한다.
    - 파일럿의 `<summary>정답 보기</summary>` 다음 빈 줄을 임시로 제거하면 같은 명령이 exit 1로 끝나고 오류 메시지에 그 줄 번호가 포함된다 (되돌린 뒤 재실행해 exit 0 확인).
    - 파일럿에서 `## 3. 개념 설명` 헤딩을 임시로 지우면 같은 명령이 exit 1이다 (되돌린 뒤 exit 0 확인).
    - `scripts/check-lesson-structure.mjs`가 `import` 문에서 `node:` 접두사 모듈만 참조하고 `package.json` dependencies의 어떤 패키지도 import하지 않는다.
    - `scripts/check-lesson-structure.mjs`에 `src/content/lessons/step-2` 또는 `step-3` 경로 문자열이 등장하지 않는다.
    - `hasContent: false`인 9개 스텁 파일이 이 게이트에서 검사 대상에 포함되지 않는다 — 스텁을 하나 `true`로 바꾸면 exit 1이 되고(구조 미충족), 되돌리면 다시 exit 0이다.
    - Shiki 언어 확인 실행이 `sql`과 `powershell`(또는 확인된 별칭)이 로드된 언어 목록에 포함됨을 출력한다. 결과 문자열이 SUMMARY에 기록된다.
    - `npm run build && node scripts/check-manifest.mjs && node scripts/check-brand.mjs && node scripts/check-lesson-structure.mjs` 4개 명령이 모두 exit 0이다.
    - `git log origin/master -1 --oneline`이 이 Task의 커밋을 가리킨다 (푸시 완료).
    - `curl -s -o /dev/null -w "%{http_code}" https://ai-engineer-runway.vercel.app/lesson/1-3-python-variables-and-types`가 `200`을 반환한다.
    - 같은 URL의 본문에 `정답 보기` 문자열이 포함된다.
    - `git diff --name-only HEAD~1 HEAD`에 `package.json`이 없다.
  </acceptance_criteria>
  <done>Step 1 레슨 구조를 자동으로 지키는 게이트가 생겼고 실제로 red를 낼 수 있음이 증명됐다. `sql`/`powershell` 문법 지원이 확인됐고, 파일럿이 프로덕션에 올라가 아이패드로 열 수 있다. 커밋·푸시 완료.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 아이패드 프로덕션 확인 — 집필 표준 승인 (D-51, D-59, D-60)</name>
  <what-built>
파일럿 레슨 `Python 변수·자료형`이 eli5 × 6단 표준으로 다시 쓰여 프로덕션에 배포됐습니다.

- 구 버전과 같은 6단 구조(학습 목표 → 왜 배우나 → 개념 설명 → 실무 예제 → 실무 팁 → 핵심 정리·스스로 점검)를 유지하고 글쓰기만 eli5 톤으로 바꿨습니다 — 짧은 문장, 비유 먼저, 용어마다 괄호 한 줄 풀이
- `curriculum.md` 원문 범위에 맞춰 **조건문·반복문**을 새로 넣었습니다 (구 버전에는 없었습니다)
- 실습 시간을 채우는 `해보기` 과제가 생겼고, 정답은 접힌 "정답 보기"를 눌러야 보입니다
- 실행 명령을 `python` 대신 `py`로 안내합니다 (Windows에서 `python`이 Microsoft Store 스텁으로 잡히는 문제를 개발 환경에서 실제로 재현했습니다). macOS는 한 줄로 병기했습니다
- 6단 구조·해보기 개수·정답 블록·용어 표를 자동으로 검사하는 게이트(`scripts/check-lesson-structure.mjs`)를 새로 만들었습니다 — 나머지 9편은 사람 검토 없이 이 게이트로만 완료 처리됩니다 (D-59)

자동 검증은 전부 통과했습니다: `npm run build` · `check-manifest`(13 불변식) · `check-brand` · `check-lesson-structure`. 사람 눈으로만 확인되는 것은 아래 6가지입니다.

**이 승인이 잠그는 것:** 여기서 승인하면 같은 표준으로 나머지 9편이 한 번에 병렬 집필되고, Phase 5가 Step 2·3의 25편에도 같은 표준을 적용합니다. 표준을 나중에 바꾸면 전 레슨 재작업입니다 (D-47 reversibility: costly).
  </what-built>
  <how-to-verify>
**아이패드 Safari**로 아래 주소를 엽니다 (PC로 먼저 봐도 되지만, 최종 판단은 아이패드로 해주세요).

`https://ai-engineer-runway.vercel.app/lesson/1-3-python-variables-and-types`

1. **읽기 쉬운가 (가장 중요)** — 세로 모드로 처음부터 끝까지 읽어봅니다. Python을 전혀 모른다고 가정했을 때 막히는 문장이 있는지, 용어가 처음 나올 때 괄호 풀이가 붙어 있는지 봅니다. 구 버전이 궁금하면 같은 화면에서 `docs/making-of.md`가 아니라 이전 커밋 내용을 비교해도 되지만, 판단 기준은 "지금 이 글이 읽히는가" 하나입니다.

2. **글이 시간을 채우고 있지는 않은가 (D-49)** — 읽는 데 30분 안쪽으로 끝나야 합니다. 너무 길면 어느 절을 더 줄여야 하는지 알려주세요.

3. **해보기가 실습 시간을 채우는가** — `4. 실무 예제` 안의 `해보기` 과제 2~3개를 봅니다. 실제로 코드를 고쳐 돌려 보게 만드는 과제인지, 아니면 그냥 읽고 넘어가게 되는지 판단합니다. 각 과제 아래 **"정답 보기"를 손가락으로 눌러** 펼쳐지는지, 펼친 내용의 굵은 글씨·코드 서식이 정상인지(별표나 백틱이 그대로 보이면 실패), 터치 타깃이 충분히 큰지 확인합니다.

4. **코드가 실제로 돌아가는가** — `4. 실무 예제`의 Python 코드 블록 우상단 복사 버튼을 눌러 복사하고, PC에서 `variables.py`로 저장한 뒤 PowerShell에서 `py variables.py`를 실행합니다. 본문이 설명한 예상 출력과 실제 출력이 맞는지 봅니다. 그 아래 `powershell` 블록이 **색이 입혀져** 보이는지도 함께 확인합니다(검정 단색이면 하이라이팅 폴백입니다).

5. **다크모드 + 가로 모드** — 아이패드 설정에서 다크모드로 바꿔 같은 페이지를 다시 봅니다. 접힌 "정답 보기" 블록이 배경과 구분되는지, 대비가 충분한지 봅니다. 그다음 가로로 돌려 표와 코드 블록이 깨지지 않는지 확인합니다.

6. **1-3 레슨의 범위 (결정 필요)** — 이 레슨은 제목이 "Python 변수·자료형"이지만 커리큘럼 원문 불릿은 "변수, 자료형, 조건문, 반복문" 네 개입니다. 그래서 **조건문·반복문을 짧게 넣었습니다**. Step 1의 다른 어떤 레슨도 조건문·반복문을 다루지 않기 때문입니다. 이 판단이 맞는지 확인해주세요 — (a) 지금처럼 이 레슨에 포함, (b) 빼고 다른 곳에서 다룸, (c) 더 자세히.
  </how-to-verify>
  <resume-signal>"승인"이라고 입력하거나, 위 6항목 중 어긋난 부분을 항목 번호와 함께 알려주세요. 6번은 (a)/(b)/(c) 중 하나로 답해주세요. 수정 요청이 오면 파일럿만 고쳐 같은 경로로 다시 배포하고 재확인받습니다 (D-51) — 승인 전에는 나머지 9편을 한 줄도 쓰지 않습니다.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레슨 본문 → 공개 프로덕션 URL | 레슨 콘텐츠는 설계상 익명 공개(D-14/D-20). 본문에 들어간 문자열은 그대로 인터넷에 노출된다 |
| 레슨 코드 예제 → 학습자 로컬 실행 환경 | 학습자가 복사해 자기 머신에서 실행한다. 나쁜 습관을 가르치면 그대로 재생산된다 |
| `.mdx` 원본 → Velite 빌드 파이프라인 | 스키마 위반·MDX 문법 오류는 빌드 실패로 차단된다(런타임 빈 상태 불가) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | 레슨 본문(공개 표면) | high | mitigate | `node scripts/check-brand.mjs`가 브랜드 금지 문자열·이메일 정규식을 `src/`·`docs/` 전체에서 0건으로 상시 검사한다. Task 1·2의 verify와 acceptance criteria에 포함되며 Task 1은 별도로 파일 단위 대소문자 무시 grep 0건을 확인한다 |
| T-04-02 | Tampering | `scripts/check-manifest.mjs` 기대 상수 오조작 | medium | mitigate | 이 Plan의 `files_modified`에서 제외했고, Task 1의 acceptance criteria가 `git diff --name-only`에 이 파일이 없음을 확인한다. 파일럿은 이미 `hasContent: true`라 상수 갱신이 불필요하다(RESEARCH Pitfall 5) |
| T-04-03 | Information Disclosure | 파일럿의 프로덕션 조기 공개 (D-60) | low | accept | 레슨 콘텐츠는 원래 공개 대상이고, 진도 데이터는 unlock 쿠키 게이트 뒤에 그대로 남는다. 이 Plan은 인증·게이트 코드를 건드리지 않는다 |
| T-04-04 | Tampering | `src/app/globals.css` 전역 규칙 추가로 인한 기존 스타일 회귀 | low | mitigate | 새 선택자를 `.prose` 하위로만 한정하고 기존 토큰만 쓴다. Task 3 체크포인트 5번이 다크모드·가로 모드에서 회귀를 실측한다 |
| T-04-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 저장소에 패키지를 설치하지 않는다. RESEARCH.md `## Package Legitimacy Audit`이 레슨 안내 대상 `anthropic`(PyPI, 공식 SDK)·`scikit-learn`(PyPI, 10년+ 표준)을 직접 조회로 OK 판정했고 `[SLOP]`/`[SUS]` 0건이다. Task 1·2 acceptance criteria가 `package.json` 무변경을 확인한다 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

**Spec-less probe fallback §A — SPEC.md 부재로 결정론적 edge 프로브 6건이 전부 `unclassified → unresolved`로 남았다.** 프로즈 콘텐츠 phase라 대부분 형식상 부적용이지만, 조용히 버리지 않고 아래처럼 명시적 가정으로 남긴다.

| Req | 범주 | 프로브 질문 | 이 phase에서의 해석과 처리 |
|-----|------|------------|---------------------------|
| CONT-02 / CONT-03 | empty | 빈·단일 원소·null 입력의 결과는? | 레슨 본문에는 "빈 입력"이 없다. 대응되는 실패 모드는 **`hasContent: true`인데 본문이 비었거나 6단이 덜 찬 레슨**이며, `scripts/check-lesson-structure.mjs`(L1·L2·L5)와 Velite 스키마·`npm run build`가 이를 막는다. 게이트가 대상 0건일 때 조용히 통과하는 경로도 스크립트가 오류로 처리한다 |
| CONT-02 / CONT-03 | ordering | 값이 같을 때 출력 순서가 정해져 있고 안정적인가? | 레슨 순서는 본문이 아니라 프론트매터 `order`와 `getOrderedLessons()`가 정한다. 이 phase는 `hasContent` 외 프론트매터를 바꾸지 않으므로 순서가 움직일 경로가 없다 (`must_haves` 프론트매터 불변 truth로 고정) |
| CONT-02 / CONT-03 | adjacency | 두 값이 정확히 같거나 맞닿으면 병합·충돌·분리 중 무엇인가? | 한 모듈 안에 `order`가 같은 레슨 두 개가 생기는 경우가 이에 해당하며 `scripts/check-manifest.mjs`의 기존 불변식이 잡는다. 이 phase는 `order`를 건드리지 않는다 |

**추가 가정 (RESEARCH Assumptions Log 승계)**
- **A2 — `sql`/`powershell` 하이라이팅:** 이 저장소에서 아직 실증되지 않았다. Task 1이 `powershell`을 실제 빌드로, Task 2가 Shiki 번들 로드로 `sql`을 확인해 이 가정을 해소한다. 실패해도 빌드는 깨지지 않고 단색 폴백이 되므로, 체크포인트 4번이 사람 눈으로 최종 확인한다.
- **A3 — `<details>` 빈 줄 규칙:** CommonMark 규칙이 이 저장소의 Velite MDX 컴파일러에도 적용된다는 가정. Task 1이 실제 렌더로, 체크포인트 3번이 사람 눈으로 확인한다. 게이트 L4가 이후 상시 방어한다.
- **backstop 2건은 자동 확인이 불가능하다:** "읽기 산문이 구 버전보다 짧다"(D-49)와 "아이패드 터치 타깃 44px·다크모드 대비"는 Task 3 체크포인트 2번·3번·5번이 그 자리를 맡는다. 체크포인트가 생략되면 두 항목은 미검증으로 남는다.
- **MVP 사용자 스토리 부재:** ROADMAP Goal 줄이 사용자 스토리 형식이 아니어서 스토리를 지어내지 않았다 (`<mvp_note>` 참고).
</flagged_assumptions>

<verification>
Wave 1 게이트 (전부 green이어야 체크포인트로 넘어간다):
1. `npm run build`
2. `node scripts/check-manifest.mjs` — 13 불변식, 기대 개수 2 유지
3. `node scripts/check-brand.mjs`
4. `node scripts/check-lesson-structure.mjs` — 신설, 검사 대상 1편
5. 게이트 red 실증 — `<summary>` 빈 줄 제거 시 4번이 exit 1
6. `git push origin master` → Vercel 배포 성공 → 프로덕션 URL 200
7. Task 3 체크포인트 승인 (6항목 + 1-3 범위 결정)
</verification>

<success_criteria>
- 파일럿 레슨이 eli5 × 6단 구조로 재작성되어 프로덕션에서 읽힌다
- 조건문·반복문이 포함되어 `curriculum.md` 1-3 첫 불릿 범위를 전부 채운다
- `해보기` 2~3개와 접힌 정답 블록이 실제로 렌더·펼쳐진다
- `powershell` 코드펜스가 색상 하이라이팅된다
- Step 1 구조 게이트가 존재하고 실제로 red를 낼 수 있음이 증명됐다
- 사용자가 아이패드에서 확인하고 집필 표준을 승인했다 (Wave 2 착수 조건)
</success_criteria>

<output>
Create `.planning/phases/04-step-1/04-01-SUMMARY.md` when done.

SUMMARY에 반드시 담을 것 (Wave 2의 5개 Plan이 이것을 읽고 집필한다):
- 승인된 6단 구조의 실제 골격 (각 절이 어떤 형태로 굳었는지)
- 해보기·스스로 점검 개수와 정답 블록 형식의 최종 확정값
- 체크포인트 6번의 1-3 범위 결정 결과
- 사용자가 요청한 수정 사항과 그 반영 결과 (있다면 — 9편이 같은 실수를 반복하지 않도록)
- `sql`/`powershell` 언어 확인 결과와 실제로 쓸 펜스 태그 문자열
- `scripts/check-lesson-structure.mjs`가 검사하는 6개 항목의 정확한 기대값
</output>
