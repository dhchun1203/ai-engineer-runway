---
phase: 08-performance-and-mobile
plan: 08
type: execute
wave: 6
depends_on: ["08-04", "08-07"]
files_modified:
  - docs/making-of.md
  - .planning/phases/08-performance-and-mobile/08-MEASUREMENTS.md
autonomous: false
requirements: [SC1, SC2, SC3, SC4, SC5, PLAT-03, UX-01]
user_setup: []

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "게이트 19종(기존 16 + 신규 3)이 한 세션에서 순차로 전부 통과한다"
    - "이 페이즈의 전후 숫자(라우트별 TTFB, 첫 방문 전송량, 폰트 바이트, 프레임 예산, 375px 지표)가 한 파일에 표로 모인다"
    - "프로덕션 배포 후 실제 배포 URL에서 정적 라우트 3종의 TTFB가 측정되고 기록된다"
    - "Making-of 소개 페이지가 이 페이즈의 작업을 반영한다 — PLAT-03의 살아있는 문서 계약"
    - "웹에 공개되는 어떤 문구에도 금지 기관명이 없다"
    - statement: "아이패드 Safari 세로·가로에서 레슨·Step·커리큘럼·일정 스크롤과 완료 토글, 페이지 전환이 끊김 없이 동작한다"
      verification: backstop
    - statement: "폰에서 6종 화면이 좁아서 참고 쓰는 느낌 없이 읽힌다"
      verification: backstop
    - statement: "메모장 하단 시트가 아이패드 실기기에서 키보드·툴바와 틈 없이 붙는다 — Phase 6에서 게이트 전부 초록불인 채로 나왔던 결함이 재발하지 않았다"
      verification: backstop
  artifacts:
    - .planning/phases/08-performance-and-mobile/08-MEASUREMENTS.md
    - docs/making-of.md
  key_links:
    - "로컬 게이트 19종 → 프로덕션 배포 → 배포 URL 실측 TTFB"
    - "08-01 기준선 숫자 → 08-08 최종 숫자 → 전후 비교표"
    - "자동 게이트 → 실기기 UAT(Phase 6 교훈: 게이트 16종 초록불 상태로도 실기기 결함이 나왔다)"
---

<objective>
페이즈를 닫는다. 세 가지를 한다: **전부 초록불인지 한 세션에서 확인**, **숫자를 한 곳에 모으기**,
**실기기에서 사람이 직접 보기**.

세 번째가 이 플랜의 존재 이유다. Phase 6은 게이트 16종이 전부 초록불인 상태로 배포했고, 그 뒤
아이패드 실기기에서 메모장 하단 틈이라는 실제 결함이 나왔다(커밋 b878491). 자동 게이트는 실기기를
대체하지 못한다. 이 페이즈는 스크롤 프레임·터치 타깃·정적 전환처럼 **실기기 감각과 직결된** 것을
많이 건드렸으므로 실기기 확인이 특히 필요하다.

Purpose: "숫자가 좋아졌다"와 "실제로 좋아졌다"를 둘 다 확인하고 기록한다.
Output: 측정 종합 파일, 갱신된 Making-of 문서, 사람이 확인한 실기기 UAT 결과.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-performance-and-mobile/08-01-SUMMARY.md
@.planning/phases/08-performance-and-mobile/08-04-SUMMARY.md
@.planning/phases/08-performance-and-mobile/08-05-SUMMARY.md
@.planning/phases/08-performance-and-mobile/08-06-SUMMARY.md
@.planning/phases/08-performance-and-mobile/08-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 게이트 19종 전량 실행 + 배포 + 측정 종합표 작성</name>
  <files>.planning/phases/08-performance-and-mobile/08-MEASUREMENTS.md</files>
  <read_first>
    - `.planning/phases/08-performance-and-mobile/08-01-SUMMARY.md` — 이 페이즈의 기준선 숫자 전부
    - `.planning/phases/08-performance-and-mobile/08-04-SUMMARY.md` — 폰트 전후 숫자
    - `.planning/phases/08-performance-and-mobile/08-05-SUMMARY.md` — 375px 지표 전후, 실기기로 넘긴 항목
    - `.planning/phases/08-performance-and-mobile/08-06-SUMMARY.md` — 라우트별 TTFB, 라우트 렌더 모드 최종 상태
    - `.planning/phases/08-performance-and-mobile/08-07-SUMMARY.md` — 프레임 예산 전후
    - `.planning/phases/08-performance-and-mobile/08-FONT-DECISION.md` — 폰트 판단 근거
    - `scripts/` 디렉터리 전체 — 실행할 게이트 목록을 실제 파일 목록에서 확정한다(문서의 개수가 아니라 실물 기준)
    - `.planning/ROADMAP.md` § Phase 8 "이미 해결된 것" — icn1 리전 이동으로 확보한 238~252ms → 59~68ms. 이 페이즈의 숫자는 그 위에 쌓인다
    - `vercel.json` — 리전 설정
  </read_first>
  <action>
**(a) 게이트 전량 실행.** `scripts/` 아래 모든 `check-*.mjs`와 `e2e-*.mjs`를 한 세션에서 순차로
실행한다. 정적 게이트를 먼저, e2e를 나중에 돌린다(빠른 실패 우선). 각 게이트의 종료 코드와 소요
시간을 기록한다. `e2e-*` 게이트는 저마다 다른 포트를 쓰지만 순차 실행이 원칙이다 — 병렬로 돌리면
Supabase 프로브 행이 서로 간섭한다.

하나라도 실패하면 이 태스크는 실패다. "알려진 실패"로 넘기지 않는다.

**(b) 프로덕션 배포.** `git push`로 배포를 트리거하고 배포가 성공했는지 확인한다. 배포 URL을
기록한다.

**(c) 배포 URL 실측.** 배포된 사이트에서 6종 라우트의 TTFB를 측정한다. 로컬 `e2e-perf-budget.mjs`는
로컬 `next start`를 대상으로 하므로, 프로덕션 실측은 별도로 한다 — ROADMAP의 "이미 해결된 것"
항목이 쓴 것과 같은 종류의 측정(요청을 여러 번 보내 중앙값)이면 된다. 각 라우트에 대해 캐시 없는
요청을 5회 보내고 중앙값을 낸다. `x-nextjs-cache` 응답 헤더 값(`HIT`/`STALE`/`MISS`/`REVALIDATED`)도
함께 기록한다 — 정적 라우트가 실제로 캐시에서 나오는지 보여주는 직접 증거다.

**(d) `.planning/phases/08-performance-and-mobile/08-MEASUREMENTS.md` 작성.** 이 페이즈의 모든
숫자를 한 파일에 모은다. 표 다섯 개:
  1. 라우트별 TTFB — 08-01 기준선(로컬) / 최종(로컬) / 프로덕션 실측 / `x-nextjs-cache` / 렌더 모드
  2. 첫 방문 전송량 — 총 바이트 / 폰트 바이트 / 폰트 비중, 페이즈 전후
  3. 스크롤 프레임 예산 — 총 프레임 / 25ms 초과 / 비율, 페이즈 전후
  4. 375px 가독성 — 라우트별 M1/M2/M3 위반 수, 페이즈 전후
  5. 게이트 실행 결과 — 게이트 이름 / 종료 코드 / 소요 시간

각 표 아래에 한 줄 해석을 붙인다. SC1~SC5 각각이 어느 표의 어느 숫자로 충족되는지 명시한다.
이 파일에도 금지 기관명을 쓰지 않는다.
  </action>
  <verify>
    <automated>for f in scripts/check-*.mjs; do echo "== $f"; node "$f" || exit 1; done &amp;&amp; for f in scripts/e2e-*.mjs; do echo "== $f"; node --env-file=.env.local "$f" || exit 1; done &amp;&amp; node -e "const fs=require('node:fs');const p='.planning/phases/08-performance-and-mobile/08-MEASUREMENTS.md';if(!fs.existsSync(p)){console.error('MISSING '+p);process.exit(1)};const s=fs.readFileSync(p,'utf8');for(const sc of ['SC1','SC2','SC3','SC4','SC5']){if(!s.includes(sc)){console.error('MEASUREMENTS MISSING '+sc);process.exit(1)}};console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/` 아래 모든 `check-*.mjs` 와 `e2e-*.mjs` 가 각각 exit code 0으로 종료한다
    - 실행한 게이트 개수가 19개 이상이다 (기존 16 + `check-route-rendering` + `check-font-glyph-coverage` + `e2e-perf-budget` + `e2e-mobile-readability`)
    - `.planning/phases/08-performance-and-mobile/08-MEASUREMENTS.md` 에 표 5개가 전부 있다
    - 그 파일에 SC1·SC2·SC3·SC4·SC5 각각이 어느 숫자로 충족되는지 명시돼 있다
    - 프로덕션 배포 URL에서 측정한 정적 라우트 3종의 TTFB 중앙값이 기록돼 있다
    - 프로덕션 정적 라우트 응답의 `x-nextjs-cache` 헤더 값이 기록돼 있다
    - `node scripts/check-brand.mjs` 가 0으로 종료한다
    - `git status` 가 깨끗하고 배포 커밋이 원격에 반영돼 있다
  </acceptance_criteria>
  <done>이 페이즈가 무엇을 얼마나 바꿨는지가 한 파일의 숫자로 남고, 그 상태가 실제로 배포돼 있다.</done>
</task>

<task type="auto">
  <name>Task 2: Making-of 소개 페이지에 이 페이즈 반영 (PLAT-03)</name>
  <files>docs/making-of.md</files>
  <read_first>
    - `docs/making-of.md` 전체 — 현재 구조와 서술 톤. 이 문서는 eli5 방식(아무것도 모르는 사람 눈높이)으로 재작성된 이력이 있다(quick 260825-2xv)
    - `.planning/phases/08-performance-and-mobile/08-MEASUREMENTS.md` — Task 1이 만든 숫자
    - `scripts/check-brand.mjs` — 금지 기관명 검사 대상 범위
    - `src/app/about/page.tsx` — 이 문서를 렌더하는 페이지(Velite `pages` 스키마, frontmatter 요구사항)
    - `.planning/REQUIREMENTS.md` PLAT-03 — "작업 진행에 따라 계속 갱신된다"
  </read_first>
  <action>
`docs/making-of.md`에 이 페이즈의 이야기를 더한다. 기존 문서의 eli5 톤을 유지한다 — 도구 이름과
전문 용어를 나열하는 게 아니라 "무슨 문제가 있었고, 어떻게 알아냈고, 무엇을 바꿨더니 어떻게 됐는지"를
아무것도 모르는 사람이 읽을 수 있게 쓴다.

담을 것:
  - 페이지를 요청마다 새로 그리던 것을 미리 만들어 두는 방식으로 바꾼 이야기, 그리고 그렇게 하면
    "누가 무엇을 완료했는지"를 어떻게 따로 가져와야 하는지
  - 오늘 날짜가 걸린 화면들은 왜 다르게 처리했는지 — 미리 만들어 두면 날짜가 얼어붙는다는 문제와,
    브라우저가 직접 날짜를 계산하게 한 해법
  - 글꼴 파일 이야기 — 왜 컸고, 재보니 어땠고, 어떻게 줄였는지(또는 왜 안 줄였는지)
  - 버튼을 눌렀을 때 눌리는 느낌이 없던 문제와 CSS만으로 해결한 이야기, 그리고 애니메이션
    라이브러리를 굳이 안 넣은 이유
  - 스크롤할 때 화면이 버벅이던 원인(매번 모든 제목의 위치를 다시 재고 있었다)과 고친 방법
  - 폰에서 일정표 글자가 너무 좁게 눌리던 문제
  - "숫자로 확인하는 자동 검사"를 새로 세 개 만든 이야기와, 그럼에도 아이패드로 직접 봐야 하는 이유

한국어 장문 규칙을 지킨다: 2~3문장마다 끊고, 문단 사이 간격이 줄 간격보다 크게 보이도록 문단을
나눈다.

**금지 기관명을 절대 쓰지 않는다** — 항상 "AI Engineer 교육과정"으로 표기한다. 이 문서는
`/about`으로 웹에 공개된다.

frontmatter(`title`/`slug`)는 건드리지 않는다 — Velite `pages` 스키마가 요구한다.

숫자를 인용할 때는 `08-MEASUREMENTS.md`의 실측값을 쓴다. 반올림해도 되지만 지어내지 않는다.
  </action>
  <verify>
    <automated>node scripts/check-brand.mjs &amp;&amp; npm run build &amp;&amp; node -e "const fs=require('node:fs');const s=fs.readFileSync('docs/making-of.md','utf8');if(!/^---/.test(s)){console.error('FRONTMATTER MISSING');process.exit(1)};if(!/(title:)/.test(s.slice(0,400))){console.error('TITLE KEY MISSING');process.exit(1)};console.log('OK')" &amp;&amp; node --env-file=.env.local scripts/e2e-mobile-overflow.mjs &amp;&amp; node --env-file=.env.local scripts/e2e-typography.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-brand.mjs` 가 0으로 종료한다
    - `npm run build` 가 0으로 종료한다 (Velite `pages` 스키마 통과)
    - `docs/making-of.md` 에 이 페이즈의 일곱 항목(정적 전환·날짜 처리·글꼴·탭 피드백·스크롤 버벅임·폰 일정표·새 자동 검사)이 전부 서술돼 있다
    - 인용된 숫자가 `08-MEASUREMENTS.md` 의 값과 일치한다
    - 추가된 문단 중 연속 4문장 이상인 문단이 없다 (한국어 장문 규칙)
    - `/about` 을 375px·768px·1024px에서 열었을 때 가로 오버플로가 0이다 (`e2e-mobile-overflow.mjs` 통과)
    - `node --env-file=.env.local scripts/e2e-typography.mjs` 가 0으로 종료한다
  </acceptance_criteria>
  <done>PLAT-03의 살아있는 문서 계약이 이 페이즈에서도 지켜지고, 공개 페이지에 금지 기관명이 0건이다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 아이패드·폰 실기기 UAT (사람 확인)</name>
  <files>(코드 변경 없음 — 실기기 확인 결과만 SUMMARY에 기록)</files>
  <read_first>
    - `.planning/phases/08-performance-and-mobile/08-MEASUREMENTS.md` — Task 1이 만든 숫자와 배포 URL
    - `.planning/phases/08-performance-and-mobile/08-05-SUMMARY.md` — 반응형 클래스만으로 해소하지 못해 실기기 UAT로 넘긴 항목 목록. 아래 C-5가 그 항목을 가리킨다
    - `.planning/phases/08-performance-and-mobile/08-07-SUMMARY.md` — 탭 피드백이 붙은 표면 전체 목록. 아래 A-3이 그 목록을 가리킨다
    - 커밋 b878491 — Phase 6의 아이패드 메모장 하단 틈 결함과 그 수정. 아래 A-4가 이 결함의 재발을 본다
  </read_first>
  <acceptance_criteria>
    - 사람이 아이패드 실기기에서 A(세로)와 B(가로)를 전부 수행했다
    - 사람이 폰에서 C를 수행하고 6종 화면을 실제로 읽었다
    - 사람이 D(캐시 지운 첫 방문)와 E(브랜딩)를 수행했다
    - 사람이 "승인" 또는 구체적 결함 목록(어느 항목의 무엇)을 회신했다
    - 결함이 보고되면 그 전체가 SUMMARY에 원문 그대로 기록됐다 — 요약하거나 취사선택하지 않는다
    - 승인 전에는 이 페이즈를 완료로 표시하지 않는다
  </acceptance_criteria>
  <what-built>
Phase 8이 끝났다. 자동으로 확인된 것은 다음과 같다.

- 레슨·Step·커리큘럼 페이지가 요청마다 새로 그려지지 않고 미리 만들어진 상태로 나간다. 진도(완료
  체크·진행률)와 메모는 페이지가 열린 직후 따로 가져온다. 가져오는 동안에는 최종 레이아웃과 같은
  크기의 회색 자리표시가 보인다.
- 홈과 일정표는 일부러 예전 방식 그대로 두었다. 오늘 날짜가 곧 그 페이지의 내용이라, 미리 만들어
  두면 매일 첫 방문에 어제 화면을 보게 되기 때문이다.
- 글꼴 파일 전송량을 재고, 기준에 따라 줄였다(또는 줄일 이유가 없다고 판단하고 근거를 남겼다).
- 버튼·카드·행·토글을 누를 때 살짝 눌리는 변형이 생겼다. 움직임 줄이기 설정을 켜면 전부 꺼진다.
- 레슨 화면 스크롤이 매 프레임마다 모든 제목 위치를 다시 재던 것을 고쳤다.
- 폰(375px)에서 일정표 레슨 제목이 좁게 눌리던 것을 두 줄 배치로 풀었다. 아이패드·데스크톱 배치는
  건드리지 않았다.
- 자동 검사 19종이 전부 통과했고, 프로덕션에 배포됐다.

수치는 `.planning/phases/08-performance-and-mobile/08-MEASUREMENTS.md`에 표로 정리돼 있다.

**그런데 Phase 6에서 이 자동 검사가 전부 초록불인 상태로도 아이패드 실기기에서 메모장 하단 틈
결함이 나왔다.** 그래서 아래 항목은 사람이 직접 봐야 한다.
  </what-built>
  <how-to-verify>
**A. 아이패드 Safari — 세로 모드**

1. 배포 URL을 아이패드 Safari에서 연다(URL은 이 플랜의 SUMMARY 또는 Task 1 기록 참조).
2. 홈 → 커리큘럼 → Step 1 → 아무 레슨 → 일정표 순으로 이동한다. 각 화면에서:
   - 페이지가 뜨는 순간 완료 버튼·진행률 자리가 **회색 자리표시로 있다가 내용으로 채워지는지**,
     그때 **아래 내용이 밀리지 않는지**(글이 위아래로 튀면 결함)
   - 스크롤을 위아래로 빠르게 여러 번 했을 때 **끊기거나 계단처럼 튀지 않는지**
   - 특히 레슨 화면 상단의 구간 테이프(가로 막대)가 스크롤에 맞춰 **부드럽게** 현재 구간을
     따라가는지
3. 레슨 화면에서 "레슨 완료하기"를 누른다:
   - 누르는 순간 버튼이 **살짝 눌리는 느낌**이 있는지
   - 완료 표시가 **즉시** 바뀌는지
   - 페이지를 새로고침해도 완료 상태가 유지되는지
   - 한 번 더 눌러 완료를 취소하고, 새로고침 후에도 취소 상태가 유지되는지
4. 레슨 화면 하단 메모장을 연다:
   - **핸들과 화면 맨 아래 사이에 틈이 보이지 않는지** (Phase 6 결함 재발 확인)
   - 메모를 입력하고 잠시 기다렸다가 다른 레슨으로 갔다 돌아왔을 때 **입력한 내용이 그대로 있는지**
   - 특히 **이미 메모가 있는 레슨을 열었을 때 메모가 사라지거나 비지 않는지**
   - 키보드를 올렸다 내렸을 때 시트가 튀지 않는지

**B. 아이패드 Safari — 가로 모드**

기기를 가로로 돌리고 A의 2번과 4번을 반복한다. 일정표 화면에서 날짜·제목·배지 열이 흔들림 없이
정렬돼 있는지 본다.

**C. 폰**

1. 같은 URL을 폰에서 연다.
2. 6종 화면(홈 / 커리큘럼 / 일정표 / Step / 레슨 / 소개)을 차례로 열고 **실제로 읽어본다.**
3. 특히 **일정표**에서 레슨 제목이 몇 줄로 나오는지, 읽기 답답하지 않은지 본다.
4. 좁아서 참고 쓰는 느낌이 남는 지점이 있으면 **어느 화면의 어느 부분인지** 적어 준다.
5. `08-05-SUMMARY.md`에 "실기기 UAT로 넘김"으로 기록된 항목이 있으면 그 지점을 특히 본다.

**D. 첫 방문 체감**

Safari 설정에서 방문 기록·웹 사이트 데이터를 지우고 다시 연다. 글자가 나타나기까지 **글꼴이 늦게
바뀌며 깜빡이는지** 본다.

**E. 브랜딩 확인**

소개(`/about`) 페이지를 끝까지 훑어 교육기관 이름이 노출되지 않는지 확인한다.
  </how-to-verify>
  <resume-signal>전부 이상 없으면 "승인"이라고 알려주세요. 문제가 있으면 어느 항목(A-2, C-3처럼)의 무엇이 어땠는지 적어 주세요 — 화면 이름과 증상만 있으면 충분합니다.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 로컬 저장소 → 프로덕션 배포 | 이 페이즈의 모든 변경이 공개 URL로 나간다 |
| Making-of 문서 → 공개 웹페이지 | `/about`으로 렌더되는 사용자 대면 콘텐츠 |
| 게이트 실행 → Supabase 프로덕션 테이블 | e2e 게이트가 실제 진도 테이블에 프로브 행을 쓴다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-08-01 | Information Disclosure | `/about` 공개 문구 | high | mitigate | `check-brand.mjs`가 금지 기관명을 상시 검사하고 Task 2 수용 기준에 명시. 배포 전 게이트 통과가 조건 |
| T-08-08-02 | Tampering | e2e 게이트의 Supabase 프로브 행 | medium | mitigate | 기존 게이트들이 이미 프로브 행을 쓰고 정리하는 관례를 갖고 있다. 게이트를 **순차** 실행해 프로브 간 간섭을 막는다(Task 1에 명시). 각 시나리오가 자기 프로브를 정리한다 |
| T-08-08-03 | Information Disclosure | 프로덕션 TTFB 측정 로그 | medium | mitigate | 측정에 잠금 쿠키를 쓰지 않는다 — 정적 셸의 첫 바이트만 잰다. 배포 URL은 기록하되 잠금 시크릿은 어떤 기록에도 남기지 않는다 |
| T-08-08-04 | Information Disclosure | `08-MEASUREMENTS.md` | low | mitigate | 숫자와 라우트 경로만 담는다. 쿠키 값·시크릿·진도 내용을 담지 않는다 |
| T-08-08-05 | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다 |
</threat_model>

<verification>
- `scripts/check-*.mjs` 전량 → 각각 0
- `scripts/e2e-*.mjs` 전량 → 각각 0
- `npm run build` → 0
- 프로덕션 배포 성공 + 배포 URL에서 6종 라우트 200 응답
- `08-MEASUREMENTS.md`에 표 5개와 SC1~SC5 매핑
- `docs/making-of.md` 갱신 + `check-brand.mjs` 0
- 사람이 A~E 항목을 실기기에서 확인하고 승인
</verification>

<success_criteria>
- 게이트 19종이 한 세션에서 전부 통과했다
- 이 페이즈의 전후 숫자가 한 파일에 표로 모였고 SC1~SC5 각각에 매핑됐다
- 프로덕션에 배포됐고 배포 URL에서 TTFB가 실측됐다
- Making-of 문서가 갱신됐고 공개 문구에 금지 기관명이 0건이다
- 아이패드 세로·가로와 폰에서 사람이 직접 확인하고 승인했다
- Phase 6의 메모장 하단 틈 같은 실기기 결함이 재발하지 않았음이 확인됐다
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (이 플랜 몫)

**신규 파일**
- `.planning/phases/08-performance-and-mobile/08-MEASUREMENTS.md` — 이 페이즈의 전후 측정 종합표 5종 + SC1~SC5 매핑

**변경된 표면**
- `docs/making-of.md` — Phase 8 서술 추가(PLAT-03 살아있는 문서). `/about`으로 공개 렌더된다

**사람 확인 산출물**
- 아이패드 세로·가로 + 폰 실기기 UAT 결과(승인 또는 결함 목록). 결함이 나오면 gap-closure 대상이 된다
</artifacts_produced>

<output>
Create `.planning/phases/08-performance-and-mobile/08-08-SUMMARY.md` when done.
SUMMARY에 반드시 포함할 것: 게이트 19종 실행 결과표(이름/종료코드/소요시간), 프로덕션 배포 URL과
실측 TTFB, SC1~SC5 각각의 충족 근거 한 줄, 실기기 UAT에서 사람이 지적한 항목 전체(승인이면 그 사실),
그리고 다음 마일스톤으로 넘길 백로그 후보(`cacheComponents` 마이그레이션 — D8-A의 재론 조건 참조).
</output>
