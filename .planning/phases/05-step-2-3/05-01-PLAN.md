---
phase: 05-step-2-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/lessons/step-2/2-3-react-components.mdx
  - scripts/check-lesson-structure.mjs
  - src/content/lessons/step-3/3-1-vector-search-basics.mdx
  - src/content/lessons/step-2/2-4-project-ai-shop-frontend.mdx
  - scripts/check-manifest.mjs
autonomous: false
requirements: [CONT-05]

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node scripts/check-lesson-structure.mjs`가 step-1·step-2·step-3 세 디렉터리를 모두 순회하며 `hasContent: true`인 레슨만 검사한다 — Step 1 전용 스코프가 사라졌다 (D-71)"
    - "`2-3-react-components.mdx`가 신표준을 만족한다 — `### 해보기` 2~3개, 각 해보기와 스스로 점검 2문항에 접힌 `<details><summary>정답 보기</summary>` 블록, `**이 레슨의 단어**` 표 5~8행이 존재하고 확대된 게이트를 통과한다 (D-70)"
    - "`2-3-react-components.mdx`의 프론트매터 8줄(title·stepId·moduleId·order·depth·estimatedMinutes·hasContent·slug)이 이 Plan 전후로 바이트 단위 동일하다 — `hasContent`는 이미 `true`라 변경 대상이 아니다 (D-70, D-13)"
    - "`ALLOWED_FENCE_LANG_PREFIXES`가 `typescript`·`tsx`·`javascript`·`jsx`·`json`·`html`·`css`·`yaml` 8개를 추가로 허용하고, 이 목록은 스크립트 안의 단 한 곳에서만 정의된다 (D-72)"
    - "프로덕션 URL `/lesson/3-1-vector-search-basics`가 Step 3 개요 형식 본문을 렌더한다 — 학습 목표가 정의·용도·파이프라인 위치 세 요소로 쓰였고, ④에 읽기용 스니펫과 '지금 실행할 필요 없음' 안내가 붙어 있다 (D-62, D-63, D-65)"
    - "프로덕션 URL `/lesson/2-4-project-ai-shop-frontend`가 프로젝트 준비 가이드 형식 본문을 렌더한다 — ④가 코드가 아니라 사전 준비 체크리스트 표이고, ⑥에 준비 완료 판정 체크박스가 있다 (D-66, D-67, D-68)"
    - "`scripts/check-manifest.mjs`의 `EXPECTED_HAS_CONTENT_COUNT`와 `EXPECTED_HAS_CONTENT_SLUGS`가 `.velite/lessons.json`에서 실측한 `hasContent: true` 레슨 집합과 정확히 일치하고 13개 불변식이 전부 통과한다 (D-78)"
    - "신규 8개 코드펜스 언어 중 이 Plan에서 실제로 쓰인 언어가 빌드 산출물에서 단색 폴백이 아니라 하이라이팅된 스팬으로 렌더된다 (RESEARCH Pitfall 5 / Assumption A2)"
    - "사용자가 아이패드 프로덕션에서 세 형식(심화 승계 · 개요 신규 · 프로젝트 가이드 신규)을 한 화면에서 확인하고 승인했다 — 승인 전에는 나머지 22편을 한 줄도 쓰지 않는다 (D-76)"
    - statement: "게이트 확대가 `2-3-react-components.mdx` 재작성보다 먼저 일어나지 않았다 — 확대 시점에 step-2/step-3에서 `hasContent: true`인 유일한 파일이 이미 신표준이었다 (RESEARCH Pitfall 3)"
      verification: backstop
    - statement: "세 파일럿의 읽기 산문 분량이 각 레슨의 시간 배분(개요 = 읽기 약 40분 + 판단형 해보기 약 50분, 프로젝트 가이드 = 읽기 약 20분 + 준비 실행 약 40분)에 맞고, 글이 시간을 채우지 않는다 (D-64, D-68, D-49)"
      verification: backstop
  artifacts:
    - src/content/lessons/step-2/2-3-react-components.mdx
    - scripts/check-lesson-structure.mjs
    - src/content/lessons/step-3/3-1-vector-search-basics.mdx
    - src/content/lessons/step-2/2-4-project-ai-shop-frontend.mdx
    - scripts/check-manifest.mjs
  key_links:
    - "`2-3-react-components.mdx` 재작성 → `check-lesson-structure.mjs` 디렉터리 확대 — 순서가 뒤집히면 확대 직후 이 파일이 L2·L3·L5 세 규칙에 동시에 걸려 게이트가 red 상태로 방치된다 (RESEARCH Pitfall 3)"
    - "`.mdx` 본문 → Velite(Zod 스키마) → rehype-pretty-code/Shiki → `.velite/lessons.json` → `MDXContent` → `/lesson/[lessonId]` → Vercel 프로덕션 → 아이패드 Safari — 이 Plan의 tracer가 관통하는 전체 경로"
    - "`.velite/lessons.json`의 실측 `hasContent: true` 개수 → `EXPECTED_HAS_CONTENT_COUNT` — CONTEXT.md D-78 원문의 '14'를 그대로 박으면 Invariant 10이 실패한다. 상수는 빌드 실측에서만 나온다 (RESEARCH Open Question 1)"
    - "이 Plan의 세 파일럿 → Wave 2·3의 22편 — 세 형식이 여기서 굳는다. 승인 없이 진행하면 22편 재작업이다 (D-76, reversibility costly)"
  prohibitions:
    - statement: "레슨 본문·코드 주석·메타데이터 어디에도 교육기관명(`scripts/check-brand.mjs`의 `FORBIDDEN_BRAND_STRINGS`에 하드코딩된 문자열)을 노출해서는 안 된다 — 항상 'AI Engineer 교육과정' 또는 '본 과정'으로 표기한다 (프로젝트 HARD RULE, D-02)"
      status: unverified
    - statement: "다이어그램 플러그인·SVG·이미지·새 MDX 컴포넌트·퀴즈 엔진을 도입해서는 안 된다 — 큰 그림은 이모지 헤더·표·`A → B → C` 한 줄 도식으로만 표현한다 (D-48, PITFALLS Pitfall 4). `mermaid`는 허용 펜스 목록에 넣지 않는다 (D-72)"
      status: unverified
    - statement: "25편 어느 레슨에서도 프론트매터의 `hasContent` 외 어떤 필드도 바꿔서는 안 된다 — title·stepId·moduleId·order·depth·estimatedMinutes·slug는 Phase 1에서 확정된 값이고 `check-manifest.mjs` Invariant 6·12·13이 총합 4200분을 강제한다 (D-13, D-31)"
      status: unverified
    - statement: "프로젝트 준비 가이드 5편에 완성 구현 코드·완성 DB 스키마 전체·단계별 튜토리얼·'이렇게 만들면 됩니다' 형태의 정답 아키텍처를 실어서는 안 된다 — 본 과정 200~520시간 팀 프로젝트를 60분 레슨이 앞질러 풀어 버리면 개강 후 학습이 무의미해진다 (D-67, RESEARCH Pitfall 1)"
      status: unverified
    - statement: "Step 3 개요 레슨에 설치·실행 안내를 붙이거나 해보기 과제로 실행을 요구해서는 안 된다 — 스니펫은 읽기용이며 실행은 개강 후 본 과정이 담당한다 (D-63)"
      status: unverified
    - statement: "`check-lesson-structure.mjs`의 L1~L6 검사 로직에 Step별 예외 분기를 넣어 프로젝트 가이드나 개요 레슨을 우회시켜서는 안 된다 — D-69가 형식 예외를 명시적으로 금지한다"
      status: unverified
    - statement: "레슨 본문에 실제 API 키 형태의 문자열을 적어서는 안 된다 — 키는 항상 환경변수 참조로만 보여준다 (Phase 4 D-54 승계, T-05-01)"
      status: unverified
---

<objective>
Phase 5의 세 집필 형식(Step 2 심화 승계 · Step 3 개요 신규 · 프로젝트 준비 가이드 신규)을 대표 3편으로 실증하고, 그 3편이 통과할 수 있도록 자동 구조 게이트를 Step 2·3까지 확대한 뒤, 사용자가 아이패드 프로덕션에서 세 형식을 한 화면에서 판정하게 한다.

Purpose: 이 Plan은 이 phase의 tracer다. `.mdx` 본문 → Velite → Shiki → 확대된 게이트 → 매니페스트 불변식 → Vercel 프로덕션 → 아이패드까지 **모든 층을 한 경로로 관통**시킨다. D-62(Step 3 깊이 기준)와 D-66(프로젝트 가이드 단별 매핑)은 둘 다 reversibility `costly` — 이 3편이 나머지 22편의 형식을 확정하므로, 틀린 채로 22편을 병렬 집필하면 25편 재작업이 된다. 승인 전에는 다른 레슨을 한 줄도 쓰지 않는다 (D-76).

Output: 신표준으로 재작성된 심화 레슨 1편, 개요 형식 신규 1편, 프로젝트 가이드 형식 신규 1편, step-1/2/3 전체를 검사하는 구조 게이트, 실측으로 맞춘 매니페스트 상수, 그리고 아이패드 실기기 승인.
</objective>

<mvp_note>
MVP 모드가 켜져 있으나 ROADMAP의 Phase 5 `**Goal**` 줄이 "As a … I want to … so that …" 사용자 스토리 형식이 아니다 (현재: "학습자가 커리큘럼 전체(Step 2 풀스택·LLM 심화 + Step 3 RAG·오케스트레이션 개요)와 실습 프로젝트 5종 준비 가이드를 사이트에서 학습할 수 있다"). 규칙에 따라 스토리를 지어내지 않고 사실만 남긴다 — 사용자 스토리 형식이 필요하면 `/gsd mvp-phase 5`를 먼저 돌린다. MVP 모드의 실질(수직 슬라이스 · tracer 우선 분해)은 이 Plan 구조에 그대로 적용되어 있다: Task 1이 한 레슨을 콘텐츠부터 프로덕션까지 관통시키고, 나머지 Plan이 그 위에서 수평 확장한다.
</mvp_note>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-step-2-3/05-CONTEXT.md
@.planning/phases/05-step-2-3/05-RESEARCH.md
@.planning/phases/05-step-2-3/05-PATTERNS.md
@.planning/phases/04-step-1/04-01-SUMMARY.md
</context>

<source_audit>
## Multi-Source Coverage Audit (Phase 5 전체)

**GOAL (ROADMAP 성공 기준 4개)**

| 항목 | 담당 Plan | 상태 |
|------|-----------|------|
| SC1 Step 2 7개 모듈 전 레슨 심화 콘텐츠 | 01(2-3-react, 2-4) + 02(2-1×2) + 03(2-2×2) + 04(2-5×2) + 05(2-3-ts, 2-6) + 06(2-7×2) | COVERED |
| SC2 Step 3 7개 모듈 개요 콘텐츠 | 01(3-1-vector) + 08(3-1-hybrid, 3-2) + 09(3-3×2) + 10(3-4×3) + 11(3-5, 3-7) + 12(3-6×3) | COVERED |
| SC3 프로젝트 5종 준비 가이드 (재현 아님) | 01(2-4) + 05(2-6) + 08(3-2) + 11(3-5, 3-7) | COVERED |
| SC4 진행률 100% 도달 + Making-of 마감 | 13 (e2e-progress · e2e-today · making-of) | COVERED |

**REQ (REQUIREMENTS.md)**

| ID | 담당 Plan | 상태 |
|----|-----------|------|
| CONT-05 | 01, 05, 08, 11 (프로젝트 가이드 5편 실물) — 나머지 Plan은 동일 6단 표준을 공유해 게이트 한 벌을 유지 | COVERED |

**RESEARCH (05-RESEARCH.md 발견·제약)**

| 항목 | 담당 Plan | 상태 |
|------|-----------|------|
| Pitfall 1 — "재현 아님" 경계 위반 | 01(2-4), 05(2-6), 08(3-2), 11(3-5·3-7) prohibitions | COVERED |
| Pitfall 2 — Step 3 개요가 심화로 팽창 | 01(3-1), 08~12 (목표 3요소 강제 + 스니펫 30줄 상한) | COVERED |
| Pitfall 3 — 재작성 → 게이트 확대 순서 | 01 Task 1 (한 태스크 안에서 순서 고정) | COVERED |
| Pitfall 4 — `EXPECTED_HAS_CONTENT_COUNT` 산수 | 01 Task 2 · 07 · 13 (전부 `.velite/lessons.json` 실측) | COVERED |
| Pitfall 5 — 신규 8개 펜스 언어 미실증 | 01 Task 2 (스모크 확인) | COVERED |
| Pitfall 6 — Prisma CLI latest 태그가 RC | 04 (`^7` 명시 고정) | COVERED |
| Open Q1 — D-78 "11→14" 수치 | 01 Task 2 (실측 우선, 불일치를 SUMMARY에 기록) | COVERED |
| Open Q2 — Phase 4 UAT 미정산 | 01 `<flagged_assumptions>` + Task 3 체크포인트 항목 7(선택) | COVERED |
| Open Q3 — 2-1의 `practice` 스키마 네이밍 | 02 (스키마 접두사만 승계, 테이블명은 AI 서비스 맥락으로) | COVERED |
| Package Legitimacy [SUS] 3종 | 01 Task 3 체크포인트 항목 8 (blocking-human, 사용 Plan 04·06보다 앞) | COVERED |
| Security Domain 위협 6종 | 02(SQL 문자열 결합), 04(bcrypt·JWT 시크릿), 06(API 키), 12(PII/인젝션 출처) | COVERED |
| A1 — Node LTS 버전 재확인 | 05 (`2-3-typescript-setup` 집필 직전 실측) | COVERED |
| A2 — 신규 펜스 언어 하이라이팅 | 01 Task 2 | COVERED |
| A4 — Step 2 npm 패키지 조합 | 04, 06 (재량 범위, 커리큘럼 스택 내) | COVERED |
| 25편 slug·모듈·시간 확정 표 | 01~13 전 Plan이 이 표를 원본으로 사용 | COVERED |

**CONTEXT (D-62 ~ D-81 + Claude's Discretion)**

| ID | 담당 Plan | 상태 |
|----|-----------|------|
| D-62 Step 3 "알아듣기" 기준 | 01(3-1), 08, 09, 10, 12 | COVERED |
| D-63 읽기용 스니펫만 | 01(3-1), 08, 09, 10, 12 | COVERED |
| D-64 90분 = 읽기 40 + 판단형 50 | 01(3-1), 08, 09, 10, 12 | COVERED |
| D-65 6단 유지, ④ 재해석 | 01(3-1), 08, 09, 10, 12 | COVERED |
| D-66 프로젝트 가이드 단별 매핑 | 01(2-4), 05(2-6), 08(3-2), 11(3-5·3-7) | COVERED |
| D-67 "재현 아님" 경계 | 동일 4개 Plan | COVERED |
| D-68 60분 = 읽기 20 + 준비 40 | 동일 4개 Plan | COVERED |
| D-69 단어 표·해보기 형식 예외 없음 | 동일 4개 Plan + 게이트 L2/L3/L5가 상시 강제 | COVERED |
| D-70 `2-3-react-components` 재작성 | 01 Task 1 | COVERED |
| D-71 게이트 디렉터리 확대 + 주석 갱신 | 01 Task 1 | COVERED |
| D-72 펜스 언어 8개 확장 | 01 Task 1 | COVERED |
| D-73 Step 2 = PC 로컬, 2-1만 예외 | 02, 03, 04, 05, 06 | COVERED |
| D-74 Node 설치는 `2-3-typescript-setup`, 2-2는 브라우저만 | 03, 05 | COVERED |
| D-75 Windows(PowerShell) 우선, macOS 한 줄 병기 | 02, 03, 04, 05, 06 | COVERED |
| D-76 Wave 1 대표 3편 → 1회 배포 → 아이패드 확인 | 01 | COVERED |
| D-77 Step 2 배치 → Step 3 배치 (사이 승인 없음) | 02~07(Step 2), 08~13(Step 3) | COVERED |
| D-78 `EXPECTED_HAS_CONTENT_COUNT` wave별 갱신 | 01 Task 2(13), 07(23), 13(35) — 전부 실측 | COVERED |
| D-79 "준비 중" 분기 유지 + UI-SPEC 기록 | 13 | COVERED |
| D-80 Making-of 마감 (Wave 3) | 13 | COVERED |
| D-81 전체 진행률 100% e2e 검증 | 13 | COVERED |
| 재량 — Wave 2·3 executor 분할 | 01에 기록: Step 2는 모듈별 5 Plan(02~06), Step 3는 모듈별 5 Plan(08~12) | COVERED |
| 재량 — 이모지·비유·해보기 개수·단어 표 행수 | 01에서 표준 확정(해보기 2~3, 스스로 점검 2, 단어 표 5~8) → 02~12 승계 | COVERED |
| 재량 — Step 3 스니펫 언어·길이 | 01(3-1: `typescript`), 08~12에 언어 지정 | COVERED |
| 재량 — Step 2 라이브러리 버전·구조 | 04(express·prisma·jsonwebtoken·bcryptjs·zod), 06(@anthropic-ai/sdk·zod) | COVERED |
| 재량 — 프로젝트 가이드 복습 포인터 | 01, 05, 08, 11 각각에 대상 slug 지정 | COVERED |
| 재량 — Invariant 10 슬러그 배열 단순화 | 13 (최종 35 시점에 판단) | COVERED |
| 재량 — Step별 규칙 분기 필요 여부 | 01 Task 1: **분기하지 않는다** (한 벌 규칙 유지, D-69) | COVERED |
| 재량 — 파일럿 3편 집필 순서 | 01: 재작성 1편(Task 1) → 신규 형식 2편(Task 2) 순차 | COVERED |
| Deferred Ideas | 없음 — CONTEXT.md `<deferred>` 4항목(다이어그램·온라인 IDE·Step 3 실습 환경·검색/북마크/퀴즈)은 어느 Plan에도 배정하지 않았다 | N/A (의도적 제외) |

**MISSING 항목: 없음.** 네 소스의 모든 항목이 Plan에 배정되었다.
</source_audit>

<artifacts_produced>
## Artifacts this phase produces

이 절은 plan-review 소스 그라운딩이 **새로 생기는 심볼**을 드리프트로 오탐하지 않게 하기 위한 목록이다. 아래 이름들은 현재 저장소에 없거나 값이 다르며, Phase 5가 만든다.

**Plan 01 (이 Plan)이 만드는 것**
- `src/content/lessons/step-2/2-3-react-components.mdx` — 본문 전면 재작성 (프론트매터 8줄 불변, `hasContent` 이미 true)
- `src/content/lessons/step-3/3-1-vector-search-basics.mdx` — 본문 신규, `hasContent: false → true`
- `src/content/lessons/step-2/2-4-project-ai-shop-frontend.mdx` — 본문 신규, `hasContent: false → true`
- `scripts/check-lesson-structure.mjs` — 신규 상수 `LESSON_DIRS`(`STEP1_DIR` 대체), `ALLOWED_FENCE_LANG_PREFIXES`에 `typescript`·`tsx`·`javascript`·`jsx`·`json`·`html`·`css`·`yaml` 8개 추가, 상단 주석 재작성
- `scripts/check-manifest.mjs` — `EXPECTED_HAS_CONTENT_COUNT` 11 → 13(실측), `EXPECTED_HAS_CONTENT_SLUGS`에 `3-1-vector-search-basics`·`2-4-project-ai-shop-frontend` 2개 추가

**Phase 5 전체가 만들거나 바꾸는 것**
- 신규 본문 `.mdx` 24편 + 재작성 1편 = 25편. Step 2 slug 12개: `2-1-postgres-and-supabase`, `2-1-ai-data-modeling`, `2-2-html-css-js`, `2-2-browser-and-ui`, `2-3-typescript-setup`, `2-3-react-components`, `2-4-project-ai-shop-frontend`, `2-5-express-rest-api`, `2-5-auth-and-prisma`, `2-6-project-ai-shop-backend`, `2-7-prompt-patterns`, `2-7-promptops`. Step 3 slug 13개: `3-1-vector-search-basics`, `3-1-hybrid-search-reranking`, `3-2-project-rag-agent`, `3-3-peft-lora-qlora`, `3-3-tuning-evaluation`, `3-4-multi-agent-structure`, `3-4-webhook-schedule-hitl`, `3-4-n8n-langgraph`, `3-5-project-orchestration`, `3-6-prompt-versioning-eval`, `3-6-monitoring-governance`, `3-6-structured-output-canary`, `3-7-project-ax-launch`
- `scripts/check-lesson-structure.mjs` — `STEP1_DIR` 상수는 **삭제되고** `LESSON_DIRS` 배열로 대체된다 (Plan 01)
- `scripts/check-manifest.mjs` — `EXPECTED_HAS_CONTENT_COUNT`가 11 → 13(Plan 01) → 23(Plan 07) → 35(Plan 13)로 3단계 변한다. `EXPECTED_HAS_CONTENT_SLUGS`는 최종 35개
- `src/app/lesson/[lessonId]/page.tsx` — `hasContent` 분기의 빈 상태 안내 문구 교체 (Plan 13, JSX 구조·className 불변)
- `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` §Copywriting Contract — 빈 상태 카피의 "v1 완성 후 도달 불가 — 안전망으로 유지" 행 추가 (Plan 13)
- `docs/making-of.md` — Phase 4·5 기록 섹션 추가 (Plan 13)
- `.planning/phases/05-step-2-3/COVERAGE.md` — 외부 API 미연동 선언 (Plan 01 Task 1)
</artifacts_produced>

<api_coverage_note>
외부 API 연동 없음. `node gsd-core/bin/lib/api-coverage.cjs`를 이 phase 범위에 돌린 결과 `detected: false`다. Step 2·3 레슨은 Express·Prisma·LLM SDK 호출을 **교육용 예시로 보여줄** 뿐이고, 이 사이트 자체는 이 phase에서 어떤 외부 API도 새로 호출하지 않는다. 실행자는 레슨 본문의 SDK 코드를 보고 API 통합 작업으로 오인하지 말 것 — `src/` 아래(레슨 `.mdx` 제외)에 새 SDK import가 추가되면 그것이 범위 이탈이다. Task 1이 `COVERAGE.md`에 이 사실을 선언으로 남긴다.
</api_coverage_note>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: `2-3-react-components` 신표준 재작성 → 게이트 Step 2·3 확대 → 프로덕션 배포 (tracer — 콘텐츠부터 화면까지 한 경로 관통)</name>
  <reversibility rating="reversible">게이트 상수 확대(D-71·D-72)와 본문 재작성(D-70)은 모두 되돌릴 수 있다. 되돌리기 어려운 것은 이 Task가 아니라 Task 3에서 승인되는 형식 표준이다.</reversibility>
  <files>src/content/lessons/step-2/2-3-react-components.mdx, scripts/check-lesson-structure.mjs, .planning/phases/05-step-2-3/COVERAGE.md</files>
  <read_first>
    - `src/content/lessons/step-2/2-3-react-components.mdx` — 재작성 대상 전체(현재 151줄). 프론트매터 8줄과 기존 6개 헤딩 문구, `tsx` 코드펜스 예제(`components/like-card.tsx`, `LikeCard`)를 여기서 그대로 가져온다. 이미 신표준 톤에 가까운 ①~⑤는 살리고 ⑥만 교체·보강하는 것이 기본 전략이다.
    - `src/content/lessons/step-1/1-3-python-variables-and-types.mdx` — Phase 4 파일럿(신표준 실물). 6단 × eli5 × `### 해보기` × `<details>` × 단어 표가 실제로 어떻게 생겼는지 보는 기준 샘플. 전체를 읽는다.
    - `scripts/check-lesson-structure.mjs` — 수정 대상 전체. 상단 주석 1~13행(스코프 근거), 32행 `ALLOWED_FENCE_LANG_PREFIXES`, 40행 `STEP1_DIR`, 52~56행 `readdirSync`, 194~211행 순회 루프, 218~221행 `checkedCount === 0` 처리. L1~L6 검사 함수(73~213행)는 **읽되 고치지 않는다**.
    - `.planning/phases/05-step-2-3/05-PATTERNS.md` §`scripts/check-lesson-structure.mjs` — 변경 지점 3곳(디렉터리 배열, 언어 목록, 상단 주석)의 정확한 형태.
    - `.planning/phases/05-step-2-3/05-RESEARCH.md` §Common Pitfalls Pitfall 3(순서 의존성)·Pitfall 5(신규 펜스 미실증), §Architecture Patterns Pattern 5.
    - `.planning/phases/04-step-1/04-01-SUMMARY.md` — Phase 4 파일럿에서 확정된 6단 골격·해보기 개수·정답 블록 형식의 최종값. 이 Plan은 그 값을 바꾸지 않고 승계한다.
  </read_first>
  <action>
**순서를 지킨다 — 이 순서가 이 Task의 핵심이다 (RESEARCH Pitfall 3):** ① 본문 재작성 → ② 게이트 확대 → ③ 확대된 게이트로 검증 → ④ 배포. 게이트를 먼저 확대하면 그 시점에 `2-3-react-components.mdx`가 L2·L3·L5 세 규칙에 동시에 걸려 게이트가 red 상태로 방치된다.

**① `src/content/lessons/step-2/2-3-react-components.mdx` 본문 재작성 (D-70)**

프론트매터 8줄(`title: "React 컴포넌트"`, `stepId: 2`, `moduleId: "2-3"`, `order: 2`, `depth: "심화"`, `estimatedMinutes: 150`, `hasContent: true`, `slug: "2-3-react-components"`)은 한 글자도 바꾸지 않는다. `hasContent`는 이미 `true`라 이 Task에서 `scripts/check-manifest.mjs`를 건드릴 이유가 없다 — 기대 개수 11이 그대로 맞다.

`##` 레벨 헤딩은 아래 6개뿐이며 문자열과 순서를 그대로 쓴다: `## 1. 학습 목표`, `## 2. 왜 배우나`, `## 3. 개념 설명`, `## 4. 실무 예제`, `## 5. 실무 팁`, `## 6. 핵심 정리 및 스스로 점검`. 다른 `##`을 추가하지 않는다 — L1이 `## ` 시작 줄 총 개수가 정확히 6인지 센다.

구 버전에 **없는 것만** 새로 만든다:
- `## 4. 실무 예제` 안에 `### 해보기` 소제목을 **2개 또는 3개** 만든다. 각 해보기는 기존 `LikeCard` 예제를 고쳐 돌려 보는 과제 한 문단이고, 바로 아래에 접힌 정답 블록을 둔다. 과제 소재: props를 하나 더 추가해 카드에 표시하기, `useState` 초기값을 부모에서 내려 여러 카드가 각자 상태를 갖게 하기, `Link`로 상세 페이지 라우팅 붙이기.
- `## 6. 핵심 정리 및 스스로 점검`을 `**핵심 정리**` 요약 불릿 4~5개 → `**이 레슨의 단어**` 라벨 + 2열 표(헤더는 `| 단어 | 뜻 |`, 데이터 행 5~8개) → `**스스로 점검**` 라벨 + 번호 매긴 질문 **2개** 형태로 교체한다. 현재 147~151행의 "스스로 점검" 번호 목록 3개를 질문 2개로 정리하고 각각 접힌 정답 블록을 붙인다. 단어 표에 넣을 후보: 컴포넌트, props, 상태(state), 훅(hook), 클라이언트 컴포넌트, 라우팅, 리렌더.
- 정답 블록 형식은 `<details>` 한 줄 → `<summary>정답 보기</summary>` 한 줄 → **빈 줄** → 풀이 → **빈 줄** → `</details>` 한 줄. 빈 줄 두 개는 필수다(L4가 줄 단위로 검사한다).
- `<details>` 총 개수는 해보기 개수 + 2 이상이어야 한다(L3).

용어는 첫 등장 시 "한글(English, 한 줄 풀이)" 형태로 풀고 이후는 한글만 쓴다(D-50). ④에 "실행: PC 로컬 VS Code" 한 줄을 명시한다(D-73). 기존 `tsx` 코드펜스는 유지한다.

**② `scripts/check-lesson-structure.mjs` 확대 (D-71, D-72)**

검사 로직(L1~L6 함수 본문)은 **한 줄도 바꾸지 않는다.** Step별 규칙 분기를 넣지 않는다 — D-69가 형식 예외를 금지하므로 35편이 한 벌 규칙으로 검사되어야 한다. 바꾸는 것은 셋뿐이다.

- **상수 1 (32행 부근):** `ALLOWED_FENCE_LANG_PREFIXES`에 `typescript`, `tsx`, `javascript`, `jsx`, `json`, `html`, `css`, `yaml` 8개를 추가한다. 기존 5개(`python`, `sql`, `bash`, `powershell`, `text`)는 그대로 둔다. 최종 13개. 이 배열은 파일 안에서 단 한 곳에만 존재해야 한다(D-72). 다이어그램 플러그인 언어는 넣지 않는다(D-48).
- **상수 2 (40행 부근):** `STEP1_DIR` 단일 상수를 삭제하고 `LESSON_DIRS` 배열로 대체한다 — `['step-1', 'step-2', 'step-3']`를 `path.join(ROOT, 'src', 'content', 'lessons', d)`로 매핑한 배열. 48~50행의 `existsSync` 검사와 52~56행의 `readdirSync`, 194~211행의 순회 루프를 디렉터리 배열 순회로 바꾼다. 파일 순회 순서는 디렉터리 순(step-1 → step-2 → step-3), 각 디렉터리 안에서는 기존과 같이 `.sort()`. 오류 메시지가 slug를 담고 있으므로 디렉터리별 구분은 따로 필요 없다. `checkedCount === 0`이면 실패로 처리하는 성질과, `.velite/` 산출물이 아니라 원본 `.mdx`를 읽는 성질, CRLF → LF 정규화는 그대로 유지한다.
- **상단 주석 (1~13행):** 현재 주석은 "Step 2·3 디렉터리는 검사하지 않는다 — Step 2 파일럿이 구 표준이라 오탐으로 걸린다"는 근거를 담고 있다. ①이 그 근거를 없앴으므로 주석을 다시 쓴다: 검사 대상이 step-1/2/3 세 디렉터리 전체이며 `hasContent: true`인 파일만 본다는 것, 세 형식(심화·개요·프로젝트 준비 가이드)에 예외 없이 같은 규칙을 적용한다는 것(D-69), 허용 펜스 언어 목록이 D-72로 13개가 되었다는 것을 적는다. 주석과 코드가 어긋난 채 남지 않게 한다.

**③ 확대된 게이트 red/green 실증**

`node scripts/check-lesson-structure.mjs`를 돌려 통과시킨다. 이 시점에 step-2/step-3에서 `hasContent: true`인 파일은 `2-3-react-components` 하나뿐이므로 검사 대상은 Step 1 10편 + 1편 = 11편이다. 그다음 **일부러 red를 한 번 확인한다**: `2-3-react-components.mdx`의 `<summary>정답 보기</summary>` 다음 빈 줄을 임시로 지워 L4가 exit 1로 잡는지 보고 되돌린다. 이 확인 없이는 확대된 게이트가 실제로 step-2를 보고 있는지 알 수 없다.

**④ `COVERAGE.md` 선언 + 배포**

`.planning/phases/05-step-2-3/COVERAGE.md`를 만들고 첫 줄에 외부 API 미연동 선언을 남긴다: `No external API integration:` 뒤에 "레슨 본문이 SDK 호출을 교육용 예시로 보여줄 뿐, 사이트 자체는 이 phase에서 어떤 외부 API도 새로 호출하지 않는다"는 취지의 한 줄 이유. 표 행은 만들지 않는다.

전체 게이트를 green으로 확인하고 `git push origin master`로 배포한다. Vercel 배포 완료 후 프로덕션 URL `https://ai-engineer-runway.vercel.app/lesson/2-3-react-components`가 200으로 응답하고 본문에 `정답 보기` 문자열이 포함되는지 확인한다. 배포가 실패하면 여기서 멈추고 원인을 SUMMARY에 남긴다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-manifest.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`가 exit 0으로 끝난다.
    - `node scripts/check-lesson-structure.mjs`가 exit 0이고 검사한 레슨 수로 `11`을 출력한다 (Step 1 10편 + `2-3-react-components` 1편).
    - `node scripts/check-manifest.mjs`가 exit 0이다 — 기대 개수는 여전히 11이며 이 Task에서 상수를 바꾸지 않았다.
    - `node scripts/check-brand.mjs`가 exit 0이다.
    - `git diff src/content/lessons/step-2/2-3-react-components.mdx`의 변경 범위가 11행(본문 시작) 이후로만 한정된다 — 프론트매터 8줄과 앞뒤 `---` 구분선이 diff에 나타나지 않는다.
    - `grep -c '^## ' src/content/lessons/step-2/2-3-react-components.mdx`가 `6`이고, `grep -n '^## ' `로 본 순서가 `1. 학습 목표` → `6. 핵심 정리 및 스스로 점검`이다.
    - `grep -c '^### 해보기$' src/content/lessons/step-2/2-3-react-components.mdx`의 결과가 `2` 또는 `3`이다.
    - `grep -c '<details>'` · `grep -c '<summary>정답 보기</summary>'` · `grep -c '</details>'` 세 값이 같고, 그 값이 `### 해보기` 개수 + 2 이상이다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고, 그 아래 표 헤더가 `| 단어 | 뜻 |`이며 구분선 아래 데이터 행이 5개 이상 8개 이하다.
    - `grep -c "'step-1', 'step-2', 'step-3'" scripts/check-lesson-structure.mjs`가 `1` 이상이다 (디렉터리 배열 리터럴이 존재한다).
    - `grep -v '^//' scripts/check-lesson-structure.mjs | grep -c 'STEP1_DIR'`가 `0`이다 (주석을 제외한 코드에 구 상수가 남아 있지 않다).
    - `grep -v '^//' scripts/check-lesson-structure.mjs | grep -c 'ALLOWED_FENCE_LANG_PREFIXES = \['`가 `1`이다 (목록이 한 곳에서만 정의된다).
    - `node -e "import('./scripts/check-lesson-structure.mjs')"` 없이도, `node -e "const s=require('fs').readFileSync('scripts/check-lesson-structure.mjs','utf8');const m=s.match(/ALLOWED_FENCE_LANG_PREFIXES = \[([^\]]*)\]/s);const langs=m[1].match(/'[a-z]+'/g).map(x=>x.slice(1,-1));const want=['python','sql','bash','powershell','text','typescript','tsx','javascript','jsx','json','html','css','yaml'];process.exit(want.every(w=>langs.includes(w)) && !langs.includes('mermaid') ? 0 : 1)"`가 exit 0이다.
    - `scripts/check-lesson-structure.mjs`가 `import` 문에서 `node:` 접두사 모듈만 참조한다 (외부 의존성 0 유지).
    - 확대된 게이트가 실제로 step-2를 본다는 증거: `2-3-react-components.mdx`의 `<summary>정답 보기</summary>` 다음 빈 줄을 임시로 제거하면 `node scripts/check-lesson-structure.mjs`가 exit 1이고 오류 메시지에 `2-3-react-components`와 해당 줄 번호가 포함된다. 되돌린 뒤 재실행해 exit 0을 확인한다.
    - `hasContent: false` 스텁 24편이 검사 대상에 포함되지 않는다: 게이트 출력의 검사 레슨 수가 11이지 35가 아니다.
    - 파일 상단 주석에 `step-2` 문자열이 등장한다 — 주석이 새 스코프를 설명하도록 갱신되었다.
    - `grep -ciE 'kant' src/content/lessons/step-2/2-3-react-components.mdx`가 `0`이다.
    - `.planning/phases/05-step-2-3/COVERAGE.md`가 존재하고 `No external API integration:` 으로 시작하는 줄과 그 뒤 이유 문장을 담는다. 파일에 `| INTEGRATE |` 또는 `| OPT-OUT |` 행이 없다.
    - `git diff --name-only`에 `velite.config.ts`, `package.json`, `scripts/check-manifest.mjs`가 나타나지 않는다.
    - `git log origin/master -1 --oneline`이 이 Task의 커밋을 가리킨다 (푸시 완료).
    - `curl -s -o /dev/null -w "%{http_code}" https://ai-engineer-runway.vercel.app/lesson/2-3-react-components`가 `200`을 반환한다.
    - 같은 URL의 응답 본문에 `정답 보기` 문자열이 포함된다.
  </acceptance_criteria>
  <done>구 표준 파일럿이 신표준으로 재작성됐고, 구조 게이트가 step-1/2/3 세 디렉터리를 한 벌 규칙으로 검사하며 실제로 step-2에서 red를 낼 수 있음이 증명됐다. 확대된 게이트·매니페스트·브랜드 게이트가 전부 green이고 프로덕션에 배포됐다. 커밋·푸시 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 신규 형식 2편 집필 (개요 · 프로젝트 준비 가이드) + 펜스 언어 실증 + 매니페스트 상수 실측 갱신 + 배포</name>
  <reversibility rating="costly">D-62(Step 3 "알아듣기" 깊이 기준)와 D-66(프로젝트 가이드 단별 매핑)을 실물로 확정하는 Task다. 이 두 형식으로 나머지 21편이 쓰이므로 나중에 바꾸면 대규모 재작업이다. 게이트는 Task 3의 blocking-human 체크포인트 하나이며, CONTEXT.md에 이미 costly로 기록되어 있으므로 별도 `checkpoint:decision`을 두지 않는다.</reversibility>
  <files>src/content/lessons/step-3/3-1-vector-search-basics.mdx, src/content/lessons/step-2/2-4-project-ai-shop-frontend.mdx, scripts/check-manifest.mjs</files>
  <read_first>
    - `src/content/lessons/step-3/3-1-vector-search-basics.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "벡터 검색과 메타데이터 설계"`, `stepId: 3`, `moduleId: "3-1"`, `order: 1`, `depth: "개요"`, `estimatedMinutes: 90`, `hasContent: false`, `slug: "3-1-vector-search-basics"`)을 확인한다.
    - `src/content/lessons/step-2/2-4-project-ai-shop-frontend.mdx` — 스텁 현재 상태. `estimatedMinutes: 60`, `depth: "심화"`, `moduleId: "2-4"`, `title: "[Project 1] AI 쇼핑몰 프론트엔드 준비 가이드"`를 확인한다.
    - `src/content/lessons/step-2/2-3-react-components.mdx` (Task 1 완료본) — 이 Plan에서 확정된 신표준 골격. 두 신규 형식이 구조를 여기서 그대로 가져온다.
    - `.planning/phases/05-step-2-3/05-RESEARCH.md` §Architecture Patterns Pattern 2(Step 3 재해석 표 + 읽기용 스니펫 안내 문구)·Pattern 3(프로젝트 가이드 스켈레톤)·Pattern 6(Invariant 10 wave별 갱신), §Code Examples 전체, §Common Pitfalls Pitfall 1·2·4·5.
    - `.planning/phases/05-step-2-3/05-PATTERNS.md` §프로젝트 준비 가이드 5편 · §Step 3 개요 레슨 13편 — 단별 매핑 표와 금지 항목.
    - `.planning/curriculum.md` §Step 3 §3-1(불릿: 벡터 검색·메타데이터 설계 / 하이브리드 검색·re-ranking)과 §Step 2 §2-4(불릿: LLM API와 Supabase 연동 / AI 쇼핑몰 UI/UX 구현) — 두 레슨의 범위 원천.
    - `scripts/check-manifest.mjs` 6~22행 — `EXPECTED_HAS_CONTENT_COUNT`와 `EXPECTED_HAS_CONTENT_SLUGS`의 현재 값(11개)과 그 위 주석. 194~206행 — Invariant 10이 개수와 슬러그 집합을 둘 다 정확히 대조한다는 사실.
  </read_first>
  <action>
**① `src/content/lessons/step-3/3-1-vector-search-basics.mdx` — Step 3 개요 형식 신규 (D-62~D-65)**

프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩 6개는 Task 1과 동일한 문자열·순서로 쓴다.

- **① 학습 목표** — "이 레슨을 마치면 다음을 알아듣습니다." 뒤에 항목 3~4개. 각 항목은 (a) 용어를 한 문장으로 정의할 수 있다, (b) 언제 쓰는지·왜 필요한지 말할 수 있다, (c) RAG 파이프라인 전체에서 어디에 놓이는지 안다 — 이 세 형식 중 하나여야 한다. "직접 구현할 수 있다", "직접 튜닝할 수 있다", "성능을 비교 측정할 수 있다"는 쓰지 않는다 (D-62, Pitfall 2).
- **② 왜 배우나** — 최대 4문장 1문단. 개강 첫 시간에 강사가 "임베딩", "벡터 검색"을 말할 때 알아듣기 위한 준비라는 자리매김.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 비유 먼저 정의 뒤: 임베딩(embedding, 글을 숫자 배열로 바꾼 것) = 뜻이 비슷한 글을 지도 위 가까운 점으로 옮기기, 벡터 검색 = 지도에서 가장 가까운 점 찾기, 청크(chunk) = 긴 문서를 검색 단위로 자르기, 메타데이터 필터 = 지도에서 구역을 먼저 좁히기. 수식·알고리즘·차원 축소 이론은 넣지 않는다.
- **④ 실무 예제** — `### 실무에서 이게 어떻게 쓰이나` 소제목으로 시작한다. 전자상거래의 "이 상품과 비슷한 상품" 추천을 사례로 2~3단락 서술한다. 그다음 인용문(`>`) 두 줄로 "아래 코드는 개념을 눈으로 보기 위한 예시입니다 — 지금 실행할 필요는 없습니다. / 개강 후 본 과정에서 실제로 돌려보게 됩니다."를 넣는다(D-63, 문구는 이 취지를 유지하는 선에서 다듬어도 된다). 그 아래 `typescript` 펜스 하나에 Supabase `rpc`로 유사 상품 5개를 찾는 10~20줄 스니펫을 넣는다. 코드 블록은 이 하나뿐이며 30줄을 넘기지 않는다. 설치 명령·실행 명령 안내를 어떤 형태로도 붙이지 않는다.
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 청크가 너무 크면/작으면 생기는 일, 메타데이터를 미리 붙여야 나중에 필터가 가능하다는 점, 임베딩 모델을 바꾸면 기존 벡터를 다시 만들어야 한다는 점.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표(5~8행: 임베딩, 벡터, 유사도, 청크, 메타데이터, 벡터 DB, pgvector 등) → `**스스로 점검**` 질문 2개 + 각각 접힌 정답 블록.
- **`### 해보기` 2~3개는 판단·설계형이다** (D-64). 실행을 요구하지 않는다. 소재: 주어진 시나리오에서 청크 크기를 얼마로 잡고 왜 그런지 적기, 어떤 질문이 벡터 검색으로 잘 풀리고 어떤 질문이 안 풀리는지 분류하기, 상품 데이터에 붙일 메타데이터 필드 3개를 고르고 이유 적기. 각 해보기 아래 `<details>` 정답은 "모범 답안 + 왜 그렇게 보는가" 형태로 쓴다.
- 90분 = 읽기 약 40분 + 판단형 해보기 약 50분을 기준으로 분량을 잡는다 (D-64). Step 1의 읽기 30 + 실습 120과 비율이 다르다는 점을 의식한다.

**② `src/content/lessons/step-2/2-4-project-ai-shop-frontend.mdx` — 프로젝트 준비 가이드 형식 신규 (D-66~D-69)**

`hasContent: false → true`. 6단 헤딩은 동일하게 유지하고 **각 단의 내용만** 프로젝트용으로 재해석한다(D-66).

- **① 학습 목표** — 이 프로젝트에서 무엇을 만들고 무엇을 증명하는가. "AI 쇼핑몰 프론트엔드를 만들면서 Supabase 데이터와 LLM API 응답을 화면에 붙이는 능력을 증명한다" 취지. 구현 능력이 아니라 프로젝트 이해가 목표임을 명확히 한다.
- **② 왜 배우나** — 앞선 어느 모듈들이 여기서 합쳐지는지. 복습 포인터로 구체 slug를 언급한다: `2-1-postgres-and-supabase`(데이터), `2-2-html-css-js`·`2-2-browser-and-ui`(화면 기초), `2-3-typescript-setup`·`2-3-react-components`(컴포넌트). 이들이 아직 작성 전이어도 slug 이름으로 지칭하는 것은 문제없다.
- **③ 개념 설명** — `### 무엇을 만드나 — 전체 구조` 소제목 아래 `상품 데이터(Supabase) → Next.js 화면 → LLM API 추천 문구 → 장바구니` 형태의 한 줄 도식(일반 텍스트, 화살표 문자만 사용)과 화면/데이터/API 흐름을 정리한 표. 다이어그램 플러그인·이미지를 쓰지 않는다(D-48).
- **④ 실무 예제** — `### 사전 준비 체크리스트` 소제목으로 시작한다. **코드가 아니라 준비물이다** (D-67). 3열 표(`준비물` / `왜 필요한가` / `지금 할 일`)로 Supabase 프로젝트, LLM API 키, 샘플 상품 데이터 3~5개, Node·VS Code 확인 네 행을 만든다. 표 아래 인용문 한 줄로 "이 체크리스트는 무엇을 준비할지만 알려줍니다 — 실제 구현 방법은 개강 후 본 과정에서 팀과 함께 설계합니다" 취지를 남긴다. 코드 펜스가 필요하면 준비물 확인용 설정 파일 발췌(환경변수 키 이름만 담은 `text` 펜스)로 제한하고, 완성 구현 코드·완성 DB 스키마 전체·단계별 튜토리얼을 넣지 않는다.
- **⑤ 실무 팁** — 팀 프로젝트에서 자주 막히는 지점(환경변수를 팀원끼리 공유하는 방법, 디자인 합의가 늦어져 화면 작업이 밀리는 패턴, API 키를 저장소에 올려 버리는 사고), 시간 배분, 역할 분담. 불릿 3~4개.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**`를 **준비 완료 판정 기준 체크박스 목록**으로 쓴다(`- [ ]` 형태 4~5개). 그다음 `**이 레슨의 단어**` 표(5~8행: 이 프로젝트에서 처음 나오는 용어 — 예: 프론트엔드, 환경변수, 시드 데이터, 목업, 상태 관리). 그다음 `**스스로 점검**` 질문 2개 + 접힌 정답 블록.
- **`### 해보기` 2~3개는 "실제로 준비해 보기"다** (D-68). "이 함수를 구현해보세요" 형태가 나오면 이미 선을 넘은 것이다. 소재: Supabase 프로젝트를 하나 만들고 테이블 1개 생성해 보기, LLM API 키를 발급받아 `.env.local`에 넣어 두기(코드에 직접 쓰지 않기), 팔고 싶은 상품 3~5개를 표로 정리해 두기. 각 해보기의 `<details>` 안에는 "정답"이 아니라 **"성공했다면 이런 화면/출력이 나옵니다"**를 담는다.
- 60분 = 읽기 약 20분 + 준비 실행 약 40분을 기준으로 분량을 잡는다 (D-68).

두 파일 모두 `### 해보기` 2~3개, `<details>`/`<summary>정답 보기</summary>`/`</details>` 개수 일치 + 해보기 개수 + 2 이상, `**이 레슨의 단어**` 표 5~8행, `<summary>` 다음 줄과 `</details>` 이전 줄 빈 줄 규칙을 지킨다 — D-69가 형식 예외를 금지하고 게이트가 그대로 검사한다.

**③ 신규 펜스 언어 실증 (Pitfall 5 / Assumption A2)**

빌드 후 `.velite/lessons.json`에서 `3-1-vector-search-basics`의 컴파일 결과에 `language-typescript` 클래스와 색 스팬이 실제로 들어갔는지 확인한다. 단색 폴백이면 언어 태그나 Shiki 번들 문제이므로 SUMMARY에 기록하고 대안 태그(`ts`)를 시도한다. 나머지 미실증 언어(`javascript`, `jsx`, `json`, `html`, `css`, `yaml`)는 설치된 `shiki`에서 하이라이터를 만들어 6개를 한 번에 로드해 보는 일회성 `node -e` 실행으로 확인한다 — `getLoadedLanguages()` 결과에 6개가 모두 있는지 본다. 결과를 SUMMARY에 그대로 적는다(Plan 02~12가 이 결과를 보고 펜스 태그를 고른다).

**④ `scripts/check-manifest.mjs` Invariant 10 상수 실측 갱신 (D-78, Pitfall 4, Open Question 1)**

**CONTEXT.md D-78 원문의 "14"를 그대로 박지 않는다.** `npm run build` 후 `.velite/lessons.json`에서 `hasContent === true`인 레슨의 개수와 slug 목록을 실제로 세어서 그 값을 상수에 넣는다. 재작성만 한 `2-3-react-components`는 이미 `hasContent: true`이자 이미 슬러그 목록에 있으므로 개수를 올리지 않는다 — 이번 wave에서 실제로 늘어나는 것은 `3-1-vector-search-basics`와 `2-4-project-ai-shop-frontend` 두 건이다. 계산상 11 + 2 = 13이 나와야 하지만, **계산이 아니라 실측이 상수의 근거다.** 실측값과 CONTEXT.md 원문 수치가 다르면 그 사실을 SUMMARY에 명시적으로 기록한다(Plan 07·13이 같은 방식으로 반복한다).

`EXPECTED_HAS_CONTENT_SLUGS`에 두 slug를 추가한다 — Invariant 10은 개수뿐 아니라 슬러그 집합을 정렬해 정확히 대조하므로 하나라도 빠지면 실패한다. 상수 위 주석도 함께 갱신해 "Phase 5 Wave 1이 신규 형식 2편을 추가해 11에서 N이 되었다"는 근거를 남긴다.

**⑤ 배포**

전체 게이트를 green으로 확인하고 `git push origin master`. Vercel 배포 완료 후 두 프로덕션 URL이 200으로 응답하는지 확인한다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-manifest.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 네 게이트 명령(`npm run build`, `check-lesson-structure`, `check-manifest`, `check-brand`)이 모두 exit 0이다.
    - `node scripts/check-lesson-structure.mjs`가 검사한 레슨 수로 `13`을 출력한다.
    - `node -e "const l=require('./.velite/lessons.json');process.stdout.write(String(l.filter(x=>x.hasContent).length))"`의 출력이 `scripts/check-manifest.mjs`의 `EXPECTED_HAS_CONTENT_COUNT` 값과 같다.
    - `scripts/check-manifest.mjs`의 `EXPECTED_HAS_CONTENT_SLUGS` 배열이 `3-1-vector-search-basics`와 `2-4-project-ai-shop-frontend`를 포함하고 원소 개수가 `EXPECTED_HAS_CONTENT_COUNT`와 같다.
    - 두 신규 파일의 `git diff`에서 프론트매터 변경이 `hasContent: false` → `hasContent: true` 한 줄뿐이다 — `title`·`stepId`·`moduleId`·`order`·`depth`·`estimatedMinutes`·`slug` 줄은 diff에 나타나지 않는다.
    - 두 파일 각각에서 `grep -c '^## '`가 `6`이고 순서가 1→6이다.
    - 두 파일 각각에서 `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - 두 파일 각각에서 `grep -c '<details>'` · `grep -c '<summary>정답 보기</summary>'` · `grep -c '</details>'`가 서로 같고 `### 해보기` 개수 + 2 이상이다.
    - 두 파일 각각에서 `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 그 아래 표 데이터 행이 5~8개다.
    - `3-1-vector-search-basics.mdx`에서 여는 코드펜스 개수가 `1`이고 그 언어 태그가 `typescript`다: `grep -c '^```typescript$'`가 `1`, `grep -c '^```'`가 `2`(여는 1 + 닫는 1).
    - `3-1-vector-search-basics.mdx`의 코드 블록 본문이 30줄 이하다.
    - `3-1-vector-search-basics.mdx`에 `npm install` 문자열이 0건이고, `실행 방법`·`실행: PC 로컬` 안내 문구가 0건이다 (D-63 — Step 3는 실행을 요구하지 않는다).
    - `3-1-vector-search-basics.mdx`의 `## 1. 학습 목표` 절에 `구현할 수 있다`·`튜닝할 수 있다`·`비교 측정할 수 있다` 문자열이 0건이다 (D-62 금지 목표).
    - `2-4-project-ai-shop-frontend.mdx`의 `## 4. 실무 예제` 절이 3열 마크다운 표를 담고, 그 절 안에 `typescript`·`tsx`·`javascript`·`jsx` 펜스가 0건이다 (D-67 — 완성 코드 금지).
    - `2-4-project-ai-shop-frontend.mdx`의 `## 6. 핵심 정리 및 스스로 점검` 절에 `- [ ]` 체크박스 항목이 4개 이상 있다.
    - `.velite/lessons.json`에서 `3-1-vector-search-basics`의 컴파일된 코드에 `language-typescript`(또는 확인된 대안 태그)가 존재하고, 같은 블록에 색 스타일 스팬이 2개 이상 포함된다: `node -e "const l=require('./.velite/lessons.json');const p=l.find(x=>x.slug==='3-1-vector-search-basics');process.exit(/language-typescript/.test(p.code)?0:1)"`가 exit 0이다.
    - Shiki 언어 로드 확인 실행이 `javascript`·`jsx`·`json`·`html`·`css`·`yaml` 6개가 모두 로드된 언어 목록에 있음을 출력한다. 결과 문자열이 SUMMARY에 기록된다.
    - `grep -ciE 'kant' src/content/lessons/step-3/3-1-vector-search-basics.mdx src/content/lessons/step-2/2-4-project-ai-shop-frontend.mdx`가 `0`이다.
    - `grep -c 'sk-ant-' src/content/lessons/step-2/2-4-project-ai-shop-frontend.mdx`가 `0`이다.
    - `git diff --name-only`에 `velite.config.ts`, `package.json`, `src/app/` 아래 파일이 나타나지 않는다.
    - `curl -s -o /dev/null -w "%{http_code}" https://ai-engineer-runway.vercel.app/lesson/3-1-vector-search-basics`와 `.../lesson/2-4-project-ai-shop-frontend`가 각각 `200`을 반환한다.
    - 두 프로덕션 URL의 응답 본문에 `정답 보기` 문자열이 포함된다.
    - SUMMARY에 CONTEXT.md D-78 원문 수치와 실측값의 일치/불일치가 명시적으로 기록되어 있다.
  </acceptance_criteria>
  <done>Step 3 개요 형식과 프로젝트 준비 가이드 형식이 실물 1편씩으로 확정됐고, 확대된 게이트를 통과하며, 매니페스트 상수가 빌드 실측과 일치한다. 신규 펜스 언어의 하이라이팅 여부가 확인됐다. 세 파일럿이 전부 프로덕션에 올라가 아이패드로 열 수 있다. 커밋·푸시 완료.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: 아이패드 프로덕션 확인 — 세 형식 승인 + 패키지 정당성 확인 (D-76)</name>
  <what-built>
Phase 5의 대표 레슨 3편이 프로덕션에 배포됐습니다. 이 3편이 나머지 22편의 형식을 확정합니다.

1. **React 컴포넌트** (심화 — Phase 4에서 승인하신 표준을 Step 2에 그대로 승계) — Phase 1에 구 표준으로 쓰였던 레슨을 신표준으로 다시 썼습니다. 6단 구조는 그대로였고 없던 것(`해보기` 과제, 접힌 "정답 보기", "이 레슨의 단어" 표)을 채웠습니다.
2. **벡터 검색과 메타데이터 설계** (개요 — 이번에 처음 만든 형식) — Step 3는 "직접 구현"이 아니라 **"개강 첫 시간에 강사가 이 단어를 썼을 때 알아듣기"**가 목표입니다. 그래서 코드는 읽기용 한 덩어리만 넣고 "지금 실행할 필요 없습니다"라고 적었으며, `해보기`는 손을 움직이는 실습이 아니라 **판단·설계 과제**입니다.
3. **[Project 1] AI 쇼핑몰 프론트엔드 준비 가이드** (프로젝트 가이드 — 이번에 처음 만든 형식) — 본 과정의 팀 프로젝트를 **미리 풀어 주지 않습니다.** 대신 "개강 첫날에 안 막히려면 지금 뭘 준비해 둬야 하나"만 답합니다. ④가 코드가 아니라 준비물 체크리스트 표이고, ⑥이 준비 완료 판정 체크박스입니다.

함께 바뀐 것: 레슨 구조 자동 게이트가 Step 1 전용에서 **Step 1·2·3 전체**로 확대됐고(허용 코드 언어도 5개 → 13개), 세 형식 모두에 예외 없이 같은 규칙이 적용됩니다.

자동 검증은 전부 통과했습니다: `npm run build` · `check-manifest`(13 불변식) · `check-brand` · `check-lesson-structure`(13편 검사). 사람 눈으로만 확인되는 것은 아래입니다.

**이 승인이 잠그는 것:** 승인하면 같은 세 형식으로 나머지 22편이 Step 2 배치(10편) → Step 3 배치(12편) 순으로 집필·배포됩니다. 배치 사이에는 다시 여쭙지 않습니다(D-77). 형식을 나중에 바꾸면 25편 재작업입니다.
  </what-built>
  <how-to-verify>
**아이패드 Safari**로 아래 세 주소를 순서대로 엽니다. PC로 먼저 봐도 되지만 최종 판단은 아이패드로 해주세요.

1. `https://ai-engineer-runway.vercel.app/lesson/2-3-react-components`
2. `https://ai-engineer-runway.vercel.app/lesson/3-1-vector-search-basics`
3. `https://ai-engineer-runway.vercel.app/lesson/2-4-project-ai-shop-frontend`

확인 항목:

1. **(1번 레슨) Step 1과 같은 느낌으로 읽히는가** — Phase 4에서 승인하신 Step 1 레슨들과 톤·구조가 이어지는지 봅니다. `해보기` 아래 "정답 보기"를 손가락으로 눌러 펼쳐지는지, 펼친 내용의 굵은 글씨·코드 서식이 정상인지(별표나 백틱이 그대로 보이면 실패), 코드 블록이 색이 입혀져 보이는지 확인합니다.

2. **(2번 레슨) 개요는 이 정도 깊이가 맞는가 — 가장 중요한 판단** — Step 3 13편이 전부 이 깊이로 쓰입니다. 다 읽고 나서 **"개강 첫 시간에 강사가 '임베딩', '벡터 검색', '청크'라고 말하면 무슨 얘기인지 알아듣겠다"**는 느낌이 드는지가 기준입니다. (a) 지금이 딱 맞다 / (b) 너무 얕다, 좀 더 깊게 / (c) 너무 깊다, 더 줄여도 된다 중에 골라 주세요.

3. **(2번 레슨) 읽기용 코드가 부담스럽지 않은가** — ④의 코드 한 덩어리 위에 "지금 실행할 필요 없습니다"가 붙어 있습니다. 그 안내가 실제로 안심이 되는지, 아니면 코드가 아예 없는 게 나은지 봅니다.

4. **(2번 레슨) 판단형 `해보기`가 90분 중 50분어치가 되는가** — 손으로 코드를 돌리는 대신 "청크 크기를 얼마로 잡을지 정하고 이유 적기" 같은 과제입니다. 실제로 앉아서 생각하게 만드는지, 아니면 그냥 읽고 넘어가게 되는지 판단합니다.

5. **(3번 레슨) 프로젝트를 미리 풀어 버리지 않았는가 — 두 번째로 중요한 판단** — 프로젝트 가이드 5편이 전부 이 형식으로 쓰입니다. ④의 체크리스트가 "무엇을 준비할지"까지만 말하고 "어떻게 만들지"로 넘어가지 않았는지 봅니다. 개강 후 본 과정에서 배울 게 남아 있다고 느껴지면 통과입니다. 반대로 "이거 읽으면 그냥 만들 수 있겠는데"가 되면 선을 넘은 것입니다.

6. **(3번 레슨) 지금 실제로 준비할 수 있는가** — `해보기`의 "Supabase 프로젝트 하나 만들고 테이블 1개 생성해 보기" 같은 과제를 실제로 한 개만 해봅니다. 안내만으로 막힘 없이 되는지 확인합니다.

7. **다크모드 + 가로 모드** — 아이패드를 다크모드로 바꿔 세 페이지를 다시 봅니다. 접힌 "정답 보기" 블록이 배경과 구분되는지, 표가 가로 모드에서 깨지지 않는지 확인합니다.

8. **(선택 — Phase 4 정산)** 이왕 아이패드를 여신 김에, Phase 4 UAT로 남아 있는 "아직 사람이 안 본 Step 1 레슨 5편 훑어보기"를 같이 보셔도 됩니다. 이 항목은 Phase 5 진행을 막지 않습니다 — 넘어가셔도 됩니다.

9. **(확인 필요 — 패키지 정당성)** Step 2 레슨에서 학습자에게 설치를 안내할 npm 패키지 중 셋(`prisma`, `@prisma/client`, `@anthropic-ai/sdk`)이 자동 감사에서 `too-new` 신호로 주의 표시를 받았습니다. 근거는 "최근 배포 시각"(활발히 유지보수되는 대형 패키지의 정상적 특징)이고, 주간 다운로드 1,560만~3,480만 회에 공식 조직(prisma, anthropics) 저장소가 뚜렷해 슬롭스쿼팅 신호는 없습니다. 그래도 규칙상 사람 확인이 필요합니다 — 아래 세 주소를 열어 공식 패키지가 맞는지 눈으로 확인해 주세요.
   - `https://www.npmjs.com/package/prisma`
   - `https://www.npmjs.com/package/@prisma/client`
   - `https://www.npmjs.com/package/@anthropic-ai/sdk`
   (참고: `prisma` CLI의 `latest` 태그가 현재 정식판이 아닌 후보판을 가리켜서, 레슨에는 `^7` 안정 라인을 명시해 안내할 예정입니다.)
  </how-to-verify>
  <resume-signal>"승인"이라고 입력하시거나, 어긋난 부분을 항목 번호와 함께 알려주세요. **2번은 (a)/(b)/(c) 중 하나로, 9번은 세 패키지 확인 결과를 함께 답해 주세요** — 9번이 확인되기 전에는 `prisma`·`@prisma/client`·`@anthropic-ai/sdk`를 안내하는 Plan 04·06을 실행하지 않습니다. 수정 요청이 오면 파일럿만 고쳐 같은 경로로 다시 배포하고 재확인받습니다 — 승인 전에는 나머지 22편을 한 줄도 쓰지 않습니다 (D-76).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레슨 본문 → 공개 프로덕션 URL | 레슨 콘텐츠는 설계상 익명 공개. 본문에 들어간 문자열은 그대로 인터넷에 노출된다 |
| 레슨 코드 예제 → 학습자 로컬 실행 환경 | 학습자가 복사해 자기 머신에서 실행한다. 나쁜 습관을 가르치면 그대로 재생산된다 |
| 레슨이 안내하는 npm 패키지 → 학습자의 `npm install` | 사이트가 아니라 학습자 환경에 설치된다. 오타·슬롭스쿼팅 패키지명을 안내하면 학습자가 그걸 설치한다 |
| `.mdx` 원본 → Velite 빌드 파이프라인 | 스키마 위반·MDX 문법 오류는 빌드 실패로 차단된다 |
| 게이트 기대 상수 → CI/실행자 판정 | 상수가 실제와 어긋나면 게이트가 거짓 red 또는 거짓 green을 낸다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | 레슨 본문(공개 표면) — 교육기관명·이메일 | high | mitigate | `node scripts/check-brand.mjs`가 `src/`·`docs/` 전체에서 금지 문자열·이메일 정규식을 0건으로 상시 검사한다. Task 1·2 verify와 acceptance criteria에 포함되며 파일 단위 대소문자 무시 grep 0건도 별도로 확인한다 |
| T-05-02 | Information Disclosure | 프로젝트 가이드 ④ 체크리스트의 API 키 안내 | high | mitigate | 키는 `.env.local`에 두고 코드에 직접 쓰지 않는다는 문구를 체크리스트 행에 넣는다. 실제 키 형태 문자열이 본문에 0건임을 acceptance criteria가 grep으로 확인한다 |
| T-05-03 | Tampering | `scripts/check-manifest.mjs` 기대 상수와 실제 매니페스트의 불일치 | medium | mitigate | Task 2가 `.velite/lessons.json` 실측으로 상수를 정하고, acceptance criteria가 실측 개수와 상수의 동일성을 `node -e`로 대조한다. CONTEXT.md 원문 수치를 근거로 삼지 않는다 |
| T-05-04 | Tampering | 게이트 스코프 확대 시 검사 로직 훼손 | medium | mitigate | L1~L6 함수 본문 무변경을 명시하고, red 실증(빈 줄 제거 → exit 1)으로 게이트가 여전히 물린다는 것을 증명한다. 외부 의존성 0(`node:` 접두사만)도 확인한다 |
| T-05-SC | Tampering | 레슨이 안내하는 npm 패키지 (`prisma`, `@prisma/client`, `@anthropic-ai/sdk` = `[SUS]`) | high | mitigate | 이 phase는 저장소에 패키지를 설치하지 않는다. 그러나 학습자에게 설치를 안내하므로, Task 3의 `blocking-human` 체크포인트 항목 9가 세 패키지의 공식 레지스트리 페이지를 사람이 직접 확인한 뒤에야 Plan 04·06이 실행된다. `[SLOP]` 판정 0건. CLI `latest` 태그가 후보판을 가리키는 문제는 `^7` 명시 고정으로 회피한다 |
| T-05-05 | Information Disclosure | 파일럿 3편의 프로덕션 조기 공개 | low | accept | 레슨 콘텐츠는 원래 공개 대상이고 진도 데이터는 unlock 쿠키 게이트 뒤에 그대로 남는다. 이 Plan은 인증·게이트 코드를 건드리지 않는다 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

**Spec-less probe fallback §A — 이 phase에 SPEC.md가 없어 결정론적 edge 프로브가 `CONT-05` 1건을 `unclassified → unresolved`로 남겼다.** 조용히 버리지 않고 명시적 가정으로 남긴다.

| Req | 범주 | 프로브 질문 | 이 phase에서의 해석과 처리 |
|-----|------|------------|---------------------------|
| CONT-05 | unclassified (미해결) | — (분류 불가: 프로즈 콘텐츠 요구사항이라 결정론적 edge 범주에 매핑되지 않는다) | 대응되는 실패 모드는 **"프로젝트 가이드 5편이 형식상 존재하지만 내용이 '재현 아님' 경계를 넘어 튜토리얼이 되는 것"**이다. 자동 측정이 불가능하므로 (1) `must_haves.prohibitions`에 D-67 경계를 못 박고, (2) Task 2의 acceptance criteria가 ④ 절 안의 실행 코드 펜스 0건을 grep으로 확인하며, (3) Task 3 체크포인트 항목 5가 사람 눈으로 최종 판정한다. 이 세 겹이 `unresolved` 상태의 대체 방어선이며, 그럼에도 **내용 경계 자체는 여전히 자동 검증 불가**임을 여기 남긴다 |

**추가 가정 (RESEARCH Assumptions Log · Open Questions 승계)**

- **Open Question 2 — Phase 4가 아직 로드맵상 미완료다.** STATE.md 기준 Phase 4는 7/7 실행·프로덕션 배포를 마쳤으나 `/gsd-verify-work 04` UAT 2건(SQL 레슨 2편 Supabase 실행 확인, 미검토 5편 훑어보기)이 열려 있고 ROADMAP 체크박스가 6/7이다. **이 Plan은 Phase 4 UAT 완료를 블로킹 선행 조건으로 두지 않는다** — Phase 5는 새 레슨 파일과 게이트 상수만 건드리고 Phase 4가 만든 코드·CSS·스크립트 로직에 의존적 변경을 하지 않으므로 UAT 결과가 이 Plan의 산출물을 무효화하지 않는다. 대신 Task 3 체크포인트 항목 8에 "선택" 항목으로 얹어 사용자가 같은 세션에서 정산할 수 있게 했다. UAT에서 Step 1 레슨의 톤·형식 수정 요구가 나오면 그것은 Phase 5의 25편에도 파급되므로, **Task 3 승인 시점에 Phase 4 UAT가 미해결이면 그 리스크를 사용자가 인지한 상태로 진행하는 것**이 이 Plan의 가정이다.
- **A2 — 신규 8개 펜스 언어 하이라이팅:** `typescript`는 Task 2가 실제 빌드로, 나머지 6개(`javascript`/`jsx`/`json`/`html`/`css`/`yaml`)는 Shiki 번들 로드로 확인해 이 가정을 해소한다. `tsx`는 이미 프로덕션에서 실증됐다. 실패해도 빌드는 깨지지 않고 단색 폴백이 되므로 체크포인트 항목 1이 사람 눈으로 최종 확인한다.
- **A3 / Open Question 1 — D-78의 wave별 카운트 수치:** CONTEXT.md 원문은 `11 → 14 → 24 → 35`, RESEARCH 재계산은 `11 → 13 → 23 → 35`다. 이 Plan은 **어느 쪽도 하드코딩하지 않고** `.velite/lessons.json` 실측으로 상수를 정한다. 최종 목적지 35(전 레슨)는 양쪽이 일치하므로 중간 경계값만 실측 대상이다.
- **A1 — Node LTS 버전:** 이 Plan에서는 쓰이지 않는다. `2-3-typescript-setup`을 쓰는 Plan 05가 집필 직전 실측으로 해소한다.
- **backstop 2건은 자동 확인이 불가능하다:** "게이트 확대가 재작성보다 먼저 일어나지 않았다"(사후 순서 증명)와 "세 파일럿의 읽기 분량이 시간 배분에 맞는다"(D-64/D-68). 앞의 것은 Task 1의 red 실증이 간접 증거이고, 뒤의 것은 Task 3 체크포인트 항목 2·4·5가 그 자리를 맡는다. 체크포인트가 생략되면 두 항목은 미검증으로 남는다.
- **MVP 사용자 스토리 부재:** ROADMAP Goal 줄이 사용자 스토리 형식이 아니어서 스토리를 지어내지 않았다 (`<mvp_note>` 참고).
</flagged_assumptions>

<verification>
Wave 1 게이트 (전부 green이어야 체크포인트로 넘어간다):

1. `npm run build`
2. `node scripts/check-lesson-structure.mjs` — 확대된 스코프, 검사 레슨 수 13
3. `node scripts/check-manifest.mjs` — 13 불변식, 기대 개수 = 빌드 실측값
4. `node scripts/check-brand.mjs`
5. 게이트 red 실증 — `2-3-react-components`의 `<summary>` 다음 빈 줄 제거 시 2번이 exit 1
6. `git push origin master` → Vercel 배포 성공 → 세 프로덕션 URL 200
7. Task 3 체크포인트 승인 (9항목 — 2번은 (a)/(b)/(c), 9번은 패키지 3종 확인)
</verification>

<success_criteria>
- 구 표준 파일럿이 신표준으로 재작성되어 확대된 게이트를 통과한다
- 구조 게이트가 step-1/2/3 전체를 한 벌 규칙으로 검사하고 step-2에서 실제로 red를 낼 수 있다
- 허용 코드펜스 언어가 13개이며 목록이 한 곳에서만 정의된다
- Step 3 개요 형식과 프로젝트 준비 가이드 형식이 실물 1편씩으로 확정된다
- 매니페스트 상수가 CONTEXT.md 원문이 아니라 빌드 실측과 일치한다
- 사용자가 아이패드에서 세 형식을 확인하고 승인했으며, `[SUS]` 패키지 3종의 정당성을 확인했다 (Wave 2 착수 조건)
</success_criteria>

<output>
Create `.planning/phases/05-step-2-3/05-01-SUMMARY.md` when done.

SUMMARY에 반드시 담을 것 (Plan 02~13이 이것을 읽고 집필한다):
- 승인된 세 형식의 실제 골격 — 심화·개요·프로젝트 가이드 각각에서 6단이 어떤 형태로 굳었는지
- 해보기 개수, 스스로 점검 개수, 단어 표 행수, 정답 블록 형식의 최종 확정값
- 체크포인트 항목 2의 Step 3 깊이 판정 결과 (a/b/c)와 그에 따른 조정 사항
- 체크포인트 항목 5의 "재현 아님" 경계 판정 결과
- 체크포인트 항목 9의 패키지 3종 확인 결과 (Plan 04·06 착수 조건)
- 사용자가 요청한 수정 사항과 반영 결과 (있다면 — 22편이 같은 실수를 반복하지 않도록)
- 신규 펜스 언어 실증 결과 — 실제로 쓸 수 있는 태그 문자열 목록
- `EXPECTED_HAS_CONTENT_COUNT` 실측값과 CONTEXT.md D-78 원문 수치의 일치/불일치, 그리고 Plan 07·13이 쓸 예상 다음 값
- 확대된 게이트가 검사하는 6개 항목의 정확한 기대값
</output>
