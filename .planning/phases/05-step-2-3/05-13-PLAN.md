---
phase: 05-step-2-3
plan: 13
type: execute
wave: 5
depends_on: ["05-08", "05-09", "05-10", "05-11", "05-12"]
files_modified:
  - scripts/check-manifest.mjs
  - src/app/lesson/[lessonId]/page.tsx
  - .planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md
  - docs/making-of.md
autonomous: true
requirements: [CONT-05]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`scripts/check-manifest.mjs`의 `EXPECTED_HAS_CONTENT_COUNT`가 `.velite/lessons.json` 실측 개수와 같고 그 값이 **전체 레슨 수 35**다 — 35편 전부가 `hasContent: true`다 (D-78, ROADMAP 성공 기준 4)"
    - "`node scripts/check-manifest.mjs`가 13개 불변식을 모두 통과하고, `estimatedMinutes` 총합 4200분 등식과 분포(Invariant 6·12·13)가 그대로 유지된다 (D-13, D-31)"
    - "`node scripts/check-lesson-structure.mjs`가 35편을 검사하고 exit 0으로 끝난다 — step-1/2/3 전 디렉터리, 세 형식 예외 없이 한 벌 규칙이다 (D-71, D-69)"
    - "`src/app/lesson/[lessonId]/page.tsx`의 `hasContent` 분기 코드가 **삭제되지 않고 유지**되며, 빈 상태 문구만 도달 불가 상태에 맞게 갱신됐다 — JSX 구조·className·타이포 크기는 그대로다 (D-79)"
    - "`01-UI-SPEC.md` §Copywriting Contract에 빈 상태 카피가 v1 완성 후 도달 불가이며 안전망으로 유지된다는 기록이 남았다 (D-79)"
    - "`docs/making-of.md`에 Phase 4·5 기록(콘텐츠 집필 방식, eli5 표준 확립, 파일럿 검증 리듬)이 기존 eli5 톤으로 추가됐다 — 구현→검증→배포 과정까지 기록이 마쳐진다 (D-80, PLAT-03, ROADMAP 성공 기준 4)"
    - "`node --env-file=.env.local scripts/e2e-progress.mjs`가 통과하고, 전 레슨 완료 시 전체 진행률이 반올림·집계 경계에서 99%가 아니라 **100%**로 표시됨이 실제로 확인된다 (D-81, ROADMAP 성공 기준 4)"
    - "`node --env-file=.env.local scripts/e2e-today.mjs`가 통과한다 — 오늘의 학습 루프가 35편 매니페스트에서도 동작한다"
    - "Step 3 13편의 프로덕션 URL이 모두 200으로 응답하고 각 페이지 본문에 `정답 보기`가 포함되며 `콘텐츠 준비 중`이 없다 (D-77 Wave 3 배포)"
    - "`node scripts/check-brand.mjs`가 25편 신규 본문과 갱신된 `docs/making-of.md`를 포함해 exit 0으로 끝난다 (D-02)"
    - statement: "실측 35와 CONTEXT.md D-78 원문 최종값(`35`)의 일치가 SUMMARY에 기록됐고, Wave 1·2 중간 경계값의 불일치 이력도 함께 정리됐다 (RESEARCH Open Question 1 종결)"
      verification: backstop
    - statement: "프로젝트 준비 가이드 5편이 형식·톤이 일관되고 '재현 아님' 경계를 지킨다 — 사람이 훑어 확인한다 (CONT-05, D-67)"
      verification: backstop
    - statement: "Making-of 페이지를 아이패드에서 열었을 때 Phase 1~5 흐름이 끊김 없이 읽힌다 (D-80)"
      verification: backstop
  artifacts:
    - scripts/check-manifest.mjs
    - src/app/lesson/[lessonId]/page.tsx
    - .planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md
    - docs/making-of.md
  key_links:
    - "Plan 08~12의 `hasContent` 전환 12건 → `.velite/lessons.json` → 이 Plan의 실측 → `EXPECTED_HAS_CONTENT_COUNT` = 35 — 한 편이라도 빠지면 Invariant 10이 slug 집합 불일치로 실패한다"
    - "35편 `hasContent: true` → `getOrderedLessons()` → 진행률·오늘의 학습·일정표 — 성공 기준 4가 여기서 자동 충족되지만, 반올림 경계에서 99%가 나오는지는 e2e로만 확인된다 (D-81)"
    - "`hasContent` 분기 유지 → 나중에 레슨이 추가될 때의 안전망 — 분기를 제거하면 그때 빈 화면이 나온다 (D-79)"
    - "`docs/making-of.md` → `/about` 페이지 → PLAT-03 살아있는 문서 — Phase 6(디자인 정리)은 자기 기록을 스스로 추가한다"
  prohibitions:
    - statement: "레슨 본문·`docs/making-of.md`·페이지 문구·메타데이터 어디에도 교육기관명(`scripts/check-brand.mjs`의 금지 문자열)을 노출해서는 안 된다 — Making-of는 공개 페이지다 (D-02, 프로젝트 HARD RULE)"
      status: unverified
    - statement: "`src/app/lesson/[lessonId]/page.tsx`의 `hasContent` 분기 코드를 삭제하거나 단순화해서는 안 된다 — 35편 완성으로 도달 불가가 되어도 안전망으로 유지한다 (D-79)"
      status: unverified
    - statement: "빈 상태 블록의 JSX 구조·className·타이포 크기를 바꿔서는 안 된다 — 문구만 갱신한다 (01-UI-SPEC 4-사이즈 타이포 시스템)"
      status: unverified
    - statement: "`EXPECTED_HAS_CONTENT_COUNT`를 손계산이나 CONTEXT.md 원문 수치로 정해서는 안 된다 — `.velite/lessons.json` 실측값만 근거다 (D-78)"
      status: unverified
    - statement: "e2e를 통과시키기 위해 `scripts/e2e-progress.mjs`·`scripts/e2e-today.mjs`나 진행률 계산 코드를 수정해서는 안 된다 — 실패하면 원인을 고치지 검사를 고치지 않는다 (D-81)"
      status: unverified
    - statement: "레슨 본문을 이 Plan에서 새로 쓰거나 고쳐서는 안 된다 — 본문 수정이 필요하면 해당 Plan으로 되돌려 처리한다"
      status: unverified
    - statement: "Phase 6(디자인 정리) 기록을 `docs/making-of.md`에 미리 써서는 안 된다 — PLAT-03의 살아있는 문서 규칙상 각 Phase가 자기 기록을 쓴다 (D-80)"
      status: unverified
---

<objective>
Phase 5를 마감한다 — Step 3 배치 배포, 매니페스트 최종 상수(35) 실측 갱신, "콘텐츠 준비 중" 카피 정리와 UI-SPEC 기록, Making-of 마감, 그리고 전체 진행률 100% 도달 e2e 검증.

Purpose: ROADMAP 성공 기준 4("전체 커리큘럼 진행률이 100%까지 도달 가능한 상태가 되고, Making-of 페이지가 구현→검증→배포 과정까지 기록을 마친다")를 충족하는 Plan이다. 35편 전부가 `hasContent: true`가 되면 진행률 100%는 자동으로 따라오지만, **반올림·집계 경계에서 99%가 나오지 않는지는 e2e로만 확인된다**(D-81). 이 Plan이 그 확인을 담당한다.

Output: 최종 매니페스트 상수, 갱신된 빈 상태 문구와 UI-SPEC 기록, 마감된 Making-of, Step 3 13편 배포, 100% 진행률 e2e 통과.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-step-2-3/05-CONTEXT.md
@.planning/phases/05-step-2-3/05-01-SUMMARY.md
@.planning/phases/05-step-2-3/05-07-SUMMARY.md
@.planning/phases/05-step-2-3/05-11-SUMMARY.md
@.planning/phases/05-step-2-3/05-12-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces

**Plan 13 (이 Plan)이 만드는 것**
- `scripts/check-manifest.mjs` — `EXPECTED_HAS_CONTENT_COUNT`를 Wave 2 값에서 최종 실측값 **35**로 갱신, `EXPECTED_HAS_CONTENT_SLUGS`에 Step 3 신규 12개 slug 추가(`3-1-hybrid-search-reranking`, `3-2-project-rag-agent`, `3-3-peft-lora-qlora`, `3-3-tuning-evaluation`, `3-4-multi-agent-structure`, `3-4-webhook-schedule-hitl`, `3-4-n8n-langgraph`, `3-5-project-orchestration`, `3-6-prompt-versioning-eval`, `3-6-monitoring-governance`, `3-6-structured-output-canary`, `3-7-project-ax-launch`), 상수 위 주석 갱신. 재량 사항: 최종 35 시점에는 슬러그 배열을 "매니페스트 전체 슬러그와 동일" 형태로 단순화할 수 있다(D-78) — 단순화 여부는 이 Plan이 판단한다
- `src/app/lesson/[lessonId]/page.tsx` — `hasContent` 분기의 빈 상태 문구 교체(분기 코드·JSX 구조·className 불변)
- `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` — §Copywriting Contract에 빈 상태 카피의 "v1 완성 후 도달 불가 — 안전망으로 유지" 기록 추가
- `docs/making-of.md` — Phase 4·5 기록 섹션 추가, 마지막 갱신 줄 수정

**Phase 5 전체가 만들거나 바꾸는 것** — `05-01-PLAN.md` §Artifacts this phase produces 참조. 이 Plan은 `EXPECTED_HAS_CONTENT_COUNT`의 3단계 변화 중 마지막 단계를 담당한다.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 매니페스트 최종 상수(35) 실측 갱신 + 전체 게이트 green 복귀</name>
  <precondition>`.planning/phases/05-step-2-3/`에 `05-08-SUMMARY.md`부터 `05-12-SUMMARY.md`까지 5개 파일이 모두 존재한다 — Wave 3의 다섯 Plan이 전부 끝나지 않은 상태에서 상수를 맞추면 다음 Plan이 다시 red를 만든다. 하나라도 없으면 중단한다.</precondition>
  <files>scripts/check-manifest.mjs</files>
  <read_first>
    - `scripts/check-manifest.mjs` 1~30행 — `EXPECTED_HAS_CONTENT_COUNT`·`EXPECTED_HAS_CONTENT_SLUGS`의 현재 값(Plan 07이 맞춘 Wave 2 값)과 그 위 주석, 그리고 함께 있는 `EXPECTED_TOTAL_MINUTES` 4200·`EXPECTED_MINUTES_DISTRIBUTION`·`EXPECTED_PROJECT_MODULE_COUNT`. **뒤의 세 상수는 건드리지 않는다.**
    - `scripts/check-manifest.mjs` 190~206행 — Invariant 10이 개수와 slug 배열을 정렬해 완전 일치 비교한다는 사실.
    - `.planning/phases/05-step-2-3/05-07-SUMMARY.md` — Wave 2 종료 시점의 실측 카운트와 CONTEXT.md D-78 원문 수치의 일치/불일치 기록. 이 Plan이 최종값에서 같은 대조를 반복한다.
    - `.planning/phases/05-step-2-3/05-12-SUMMARY.md` — Step 3 13편·25편 전체 `hasContent: true` 확인 결과.
    - `.planning/phases/05-step-2-3/05-CONTEXT.md` §D-78 — "최종 35는 전 레슨이므로 Invariant 10의 슬러그 배열은 '매니페스트 전체 슬러그와 동일' 형태로 단순화해도 좋다(재량)"는 근거.
  </read_first>
  <action>
**먼저 실측한다.** `npm run build`를 돌린 뒤 `.velite/lessons.json`에서 `hasContent === true`인 레슨의 **개수**와 **slug 목록**, 그리고 **전체 레슨 개수**를 실제로 뽑는다. 세 값 중 앞의 둘이 상수의 근거이고, 세 번째는 "전 레슨이 채워졌는가"의 확인용이다. 실측 `hasContent: true` 개수와 전체 레슨 개수가 같아야 한다 — 둘 다 35가 나와야 정상이다. 다르면 어느 편이 `hasContent: false`로 남았다는 뜻이므로 상수를 맞추지 말고 그 편을 쓴 Plan으로 되돌린다.

`scripts/check-manifest.mjs`에서 정확히 세 가지만 바꾼다:
1. `EXPECTED_HAS_CONTENT_COUNT` — 실측 개수로 대입.
2. `EXPECTED_HAS_CONTENT_SLUGS` — 실측 slug 목록으로 대체. **재량 판단(D-78):** 이제 모든 레슨이 `hasContent: true`이므로 이 배열을 "매니페스트 전체 slug와 동일"이라는 의미로 단순화할 수 있다. 다만 배열을 지우고 동적 비교로 바꾸면 **Invariant 10이 아무것도 검사하지 않게 되는 위험**이 있다(자기 자신과 비교하게 된다). 따라서 **명시 배열을 유지하는 쪽을 기본으로 하고**, 단순화한다면 "매니페스트 전체 개수와 `hasContent: true` 개수가 같다"는 검사를 **추가**하는 형태여야 한다 — 기존 검사를 대체해서는 안 된다. 어느 쪽을 골랐는지와 이유를 SUMMARY에 기록한다.
3. 두 상수 위 주석 — "Phase 5 Wave 3이 Step 3 잔여 12편을 채워 최종 35(전 레슨)가 되었다"는 근거를 남긴다. Phase 4가 2에서 11로 올린 이력과 Wave 2 이력을 지우지 말고 이어 붙인다.

`EXPECTED_TOTAL_MINUTES`, `EXPECTED_MINUTES_DISTRIBUTION`, `EXPECTED_PROJECT_MODULE_COUNT`와 Invariant 10 이외의 검사 로직은 **한 줄도 바꾸지 않는다.** 총합 4200분·분포·프로젝트 모듈 수가 깨지면 어느 Plan이 프론트매터를 건드린 것이므로, 상수가 아니라 그 Plan을 되돌려 원인을 고친다.

그다음 게이트 4종을 돌려 전부 green을 확인한다: `npm run build`, `node scripts/check-manifest.mjs`, `node scripts/check-brand.mjs`, `node scripts/check-lesson-structure.mjs`. `check-lesson-structure`가 검사한 레슨 수가 35여야 한다 — 이 값이 Phase 5가 세 형식을 한 벌 규칙으로 검사한다는 D-69·D-71의 최종 증거다.

마지막으로 실측 35와 CONTEXT.md D-78 원문 최종값(`35`)의 일치를 확인하고, Wave 1·2 중간 경계값의 불일치 이력(`05-01-SUMMARY.md`·`05-07-SUMMARY.md` 기록)과 함께 SUMMARY에 정리한다 — RESEARCH Open Question 1이 여기서 종결된다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-manifest.mjs &amp;&amp; node scripts/check-brand.mjs &amp;&amp; node scripts/check-lesson-structure.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 네 게이트 명령이 모두 exit 0이다.
    - `node scripts/check-manifest.mjs`의 출력이 13개 불변식 통과를 알리고 Invariant 10 실패 메시지가 없다.
    - `node -e "const l=require('./.velite/lessons.json');const n=l.filter(x=>x.hasContent).length;process.exit(n===l.length&&n===35?0:1)"`가 exit 0이다 (35편 전부가 `hasContent: true`).
    - `node -e "const l=require('./.velite/lessons.json');const s=require('fs').readFileSync('scripts/check-manifest.mjs','utf8');const n=Number(s.match(/EXPECTED_HAS_CONTENT_COUNT = (\d+)/)[1]);process.exit(n===l.filter(x=>x.hasContent).length?0:1)"`가 exit 0이다 (상수 = 실측 개수).
    - `node scripts/check-lesson-structure.mjs`가 출력한 검사 레슨 수가 `35`다.
    - `git diff scripts/check-manifest.mjs`의 변경이 `EXPECTED_HAS_CONTENT_COUNT` 줄, `EXPECTED_HAS_CONTENT_SLUGS` 배열, 그 위 주석에만 한정된다 — `EXPECTED_TOTAL_MINUTES`·`EXPECTED_MINUTES_DISTRIBUTION`·`EXPECTED_PROJECT_MODULE_COUNT` 줄이 diff에 나타나지 않는다.
    - Invariant 10이 여전히 실질적으로 검사한다는 증거: 아무 레슨 하나의 `hasContent`를 임시로 `false`로 바꾸면 `node scripts/check-manifest.mjs`가 exit 1이고 오류 메시지에 개수 또는 slug 불일치가 나온다. 되돌린 뒤 재실행해 exit 0을 확인한다.
    - `git diff --name-only`에 `src/content/lessons/` 아래 파일이 나타나지 않는다.
    - SUMMARY에 실측 35와 CONTEXT.md D-78 원문 최종값의 일치, 그리고 Wave 1·2 중간 경계값 이력이 기록되어 있다.
  </acceptance_criteria>
  <done>매니페스트 기대 상수가 실측 35와 일치하고 13개 불변식이 전부 green이다. 구조 게이트가 35편을 한 벌 규칙으로 검사한다. Invariant 10이 여전히 red를 낼 수 있음이 증명됐다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: "콘텐츠 준비 중" 카피 정리 + UI-SPEC 기록 + Making-of 마감 (D-79, D-80)</name>
  <files>src/app/lesson/[lessonId]/page.tsx, .planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md, docs/making-of.md</files>
  <read_first>
    - `src/app/lesson/[lessonId]/page.tsx` 50~70행 — `lesson.hasContent ? <MDXContent .../> : <빈 상태 블록>` 삼항 분기 전체. 빈 상태 블록의 JSX 구조(`div.flex.flex-col.gap-3` 안에 `h2` + `p`), className, 타이포 크기(`text-[20px]`/`text-[16px]`), 현재 문구("Step 1의 레슨 10편과 Step 2 React 컴포넌트 파일럿 레슨은 모두 작성되어 있으니…")를 정확히 확인한다.
    - `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` §Copywriting Contract — 기존 카피 계약 서술 형식과 Empty state 행. 어디에 어떤 형태로 한 줄을 더할지의 본보기.
    - `docs/making-of.md` — **전체를 읽는다.** 기존 Phase 1~3 기록 항목의 형식(이모지 헤더, "한 문장으로 말하면" 같은 도입, 괄호 한 줄 풀이, 짧은 문단, 표), 문서 마지막의 갱신 줄, 그리고 프론트매터(`title`/`slug` 2줄, Velite pages 스키마 요구사항이므로 건드리지 않는다). 이 문서는 `/about` 페이지로 공개되므로 교육기관명이 절대 들어가면 안 된다.
    - `.planning/phases/04-step-1/04-07-SUMMARY.md` — Phase 4가 무엇을 했고 무엇을 배웠는지. Making-of의 Phase 4 기록 원천이다.
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md`·`05-07-SUMMARY.md`·`05-12-SUMMARY.md` — Phase 5의 파일럿 검증 리듬, 배치 분할 배포, 게이트 확대 이야기. Making-of의 Phase 5 기록 원천이다.
  </read_first>
  <action>
**① `src/app/lesson/[lessonId]/page.tsx` 빈 상태 문구 갱신 (D-79)**

`hasContent` 삼항 분기 **코드 자체를 삭제하거나 단순화하지 않는다.** 35편 전부가 `hasContent: true`가 되어 이 경로는 도달 불가가 되지만, 분기를 없애면 나중에 레슨이 추가될 때 빈 화면이 나온다 — 안전망으로 남긴다.

JSX 구조·className·타이포 크기(`text-[20px]`, `text-[16px]`)는 그대로 두고 **문구만** 바꾼다. 현재 문구는 Step 1 레슨 수와 Step 2 파일럿 레슨을 구체적으로 가리키는데, 이제 모든 레슨이 채워졌으므로 사실과 어긋난다. 새 문구는 특정 레슨 수를 언급하지 않고 시간이 지나도 틀리지 않는 형태로 쓴다 — 제목은 "콘텐츠 준비 중입니다"를 유지하고, 본문은 이 레슨이 아직 작성되지 않았으며 커리큘럼 목록에서 다른 레슨을 골라 학습하면 된다는 취지 한두 문장. 교육기관명을 쓰지 않고 "AI Engineer 교육과정" 또는 "본 과정"으로 지칭한다.

**② `01-UI-SPEC.md` §Copywriting Contract 기록 추가 (D-79)**

기존 서술 형식을 따라 Empty state 항목에 한 줄을 더한다: 이 카피가 v1 완성(전 레슨 `hasContent: true`) 이후 **도달 불가 상태**가 되지만 나중에 레슨이 추가될 때를 위한 **안전망으로 유지**된다는 사실, 그리고 문구가 특정 레슨 수를 언급하지 않는 이유. 기존 행을 지우지 말고 덧붙인다.

**③ `docs/making-of.md` 마감 (D-80, PLAT-03)**

기존 Phase 1~3 기록과 **같은 형식·같은 eli5 톤**으로 Phase 4·5 기록을 추가한다. 새 형식을 만들지 않는다. 담을 것:
- **Phase 4 — Step 1 콘텐츠:** 레슨을 어떻게 쓰기로 했는지(6단 구조 × 눈높이 설명), 파일럿 한 편을 먼저 쓰고 사람이 확인한 뒤 나머지를 한 번에 쓴 이유, 그리고 사람 검토를 줄인 대가로 만든 자동 구조 검사 이야기.
- **Phase 5 — Step 2·3 콘텐츠와 프로젝트 가이드:** 세 가지 글 형식(따라 하며 배우는 심화 / 용어를 알아듣는 개요 / 프로젝트를 미리 준비하는 가이드)을 왜 나눴는지, 대표 3편을 한 번에 배포해 형식을 확정한 리듬, Step 2를 먼저 배포해 학습이 끊기지 않게 한 이유, 그리고 구조 검사를 전 레슨으로 확대한 이야기.
- **구현 → 검증 → 배포로 이어지는 흐름**이 읽히도록 쓴다(ROADMAP 성공 기준 4). 기술 용어는 괄호 한 줄 풀이를 붙이거나 일상어로 바꾼다.
- **Phase 6(디자인 정리) 기록은 쓰지 않는다** — 살아있는 문서 규칙상 각 Phase가 자기 기록을 쓴다.
- 문서 마지막의 갱신 줄이 있으면 날짜를 오늘로 맞춘다. 프론트매터 `title`/`slug` 2줄은 건드리지 않는다.

세 파일 모두에서 교육기관명이 0건이어야 한다 — `docs/making-of.md`는 `/about`으로 공개되고 `page.tsx` 문구는 화면에 그대로 나온다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-brand.mjs &amp;&amp; node scripts/check-manifest.mjs &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - 다섯 명령(`npm run build`, `check-brand`, `check-manifest`, `check-lesson-structure`, `npm run lint`)이 모두 exit 0이다.
    - `src/app/lesson/[lessonId]/page.tsx`에 `lesson.hasContent ?` 삼항 분기가 그대로 존재한다: `grep -c 'hasContent'`가 `1` 이상이다 (D-79 — 분기 삭제 금지).
    - 같은 파일에 `콘텐츠 준비 중입니다` 문자열이 그대로 존재한다.
    - 같은 파일의 빈 상태 블록에 `text-[20px]`와 `text-[16px]`가 그대로 존재한다 (타이포 시스템 불변).
    - `git diff src/app/lesson/[lessonId]/page.tsx`의 변경이 빈 상태 `p` 요소의 텍스트에만 한정된다 — `className` 문자열과 `h2` 텍스트가 diff에 나타나지 않는다.
    - 새 문구가 특정 레슨 수를 언급하지 않는다: 빈 상태 `p` 요소의 텍스트에 숫자 뒤에 편수 단위가 붙은 표기가 0건이다.
    - `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md`에 `도달 불가`와 `안전망` 문자열이 각각 `1` 이상 등장하고, 기존 §Copywriting Contract 행이 삭제되지 않았다(`git diff`가 추가만 보인다).
    - `docs/making-of.md`의 프론트매터 `title`·`slug` 2줄이 diff에 나타나지 않는다.
    - `docs/making-of.md`에 Phase 4와 Phase 5를 가리키는 새 섹션이 각각 1개 이상 추가됐고, 기존 Phase 1~3 섹션이 삭제되지 않았다.
    - `docs/making-of.md`에 Phase 6·디자인 정리를 다루는 새 섹션이 0건이다 (D-80 — 각 Phase가 자기 기록을 쓴다).
    - `grep -ciE 'kant' src/app/lesson/\[lessonId\]/page.tsx docs/making-of.md .planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md`가 `0`이다.
    - `node scripts/check-brand.mjs`가 exit 0이다 (`docs/` 전체를 검사하므로 Making-of 갱신본이 포함된다).
  </acceptance_criteria>
  <done>빈 상태 분기가 안전망으로 유지되면서 문구만 시간에 견디는 형태로 바뀌었고, 그 결정이 UI-SPEC에 기록됐다. Making-of가 Phase 4·5 기록으로 마감되어 구현→검증→배포 흐름이 읽힌다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Step 3 배치 배포 + 전체 진행률 100% e2e 검증 (D-81) + Phase 5 최종 확인</name>
  <precondition>`git remote -v`에 `origin`이 있고 현재 브랜치가 `master`이며 푸시 권한이 있다. 그리고 `.env.local`이 존재하거나 `vercel env pull`로 가져올 수 있다 — D-81의 e2e 검증은 Supabase 자격증명 없이는 실행할 수 없고, 이 Task의 핵심 목적이 그 검증이다. 자격증명을 확보할 수 없으면 중단하고 사용자에게 알린다.</precondition>
  <files>scripts/check-manifest.mjs</files>
  <read_first>
    - `scripts/e2e-progress.mjs` — **전체를 읽는다.** 이 스크립트가 진행률을 어떻게 확인하는지, 프로브 레슨을 어떻게 고르는지(85~95행 부근에서 `hasContent`인 것 중 동적 선택), 어떤 화면의 어떤 숫자를 읽는지. 이 스크립트는 **고치지 않는다.**
    - `scripts/e2e-today.mjs` — 오늘의 학습 루프 e2e가 무엇을 확인하는지. 역시 고치지 않는다.
    - `src/content/curriculum-helpers.ts`와 진행률 계산 코드 — 반올림이 어디서 일어나는지 확인한다. **읽기만 한다** — 100%가 안 나오면 원인을 파악해 보고하되, 이 Plan에서 계산 로직을 고칠지는 원인에 따라 판단한다.
    - `.planning/phases/05-step-2-3/05-11-SUMMARY.md` — 프로젝트 가이드 5편의 형식 일관성 확인 결과. `<human-check>` 항목이 참조한다.
    - `.planning/phases/05-step-2-3/05-07-SUMMARY.md` — Step 2 배치에서 e2e를 실행했는지 건너뛰었는지. 건너뛴 항목이 있으면 여기서 반드시 정산한다.
  </read_first>
  <action>
Task 1·2의 커밋을 포함해 로컬에 밀려 있는 커밋이 없는지 `git log origin/master..HEAD --oneline`으로 먼저 확인하고, 있으면 전부 포함해 `git push origin master`로 배포한다.

Vercel 배포가 끝날 때까지 기다린 뒤 **Step 3 13편 전체**의 프로덕션 URL을 확인한다. 기준 주소는 `https://ai-engineer-runway.vercel.app/lesson/{slug}` 이고 slug 13개는 `3-1-vector-search-basics`, `3-1-hybrid-search-reranking`, `3-2-project-rag-agent`, `3-3-peft-lora-qlora`, `3-3-tuning-evaluation`, `3-4-multi-agent-structure`, `3-4-webhook-schedule-hitl`, `3-4-n8n-langgraph`, `3-5-project-orchestration`, `3-6-prompt-versioning-eval`, `3-6-monitoring-governance`, `3-6-structured-output-canary`, `3-7-project-ax-launch` 다. 각 URL이 200으로 응답하고 응답 본문에 `정답 보기`가 포함되며 `콘텐츠 준비 중`이 **없어야** 한다. 어느 하나라도 실패하면 여기서 멈추고 원인을 SUMMARY에 남긴다.

그다음 **D-81의 핵심 검증**을 한다. `node --env-file=.env.local scripts/e2e-progress.mjs`를 돌린다. 35편 전부가 `hasContent: true`이므로 성공 기준 4는 형식상 자동 충족이지만, 실제 확인 대상은 **반올림·집계 경계에서 99%가 나오지 않는지**다. e2e가 전 레슨 완료 상태의 전체 진행률을 확인하지 않는다면, 스크립트를 고치지 말고 **별도의 일회성 계산으로 확인**한다: `.velite/lessons.json`의 전체 레슨 수와 진행률 계산 함수(`src/content/curriculum-helpers.ts` 계열)를 그대로 써서 "전 레슨 완료" 입력에 대해 전체·Step별·모듈별 퍼센트가 모두 정확히 100인지 계산해 본다. 하나라도 99가 나오면 그것이 성공 기준 4의 실제 결함이므로, 원인(정수 나눗셈·내림·부동소수 비교 중 어디인지)을 특정해 SUMMARY에 적고 사용자에게 보고한다 — **e2e나 검사 쪽을 고쳐서 통과시키지 않는다.**

이어서 `node --env-file=.env.local scripts/e2e-today.mjs`를 돌려 오늘의 학습 루프가 35편 매니페스트에서도 동작하는지 확인한다. `05-07-SUMMARY.md`에서 건너뛴 e2e가 있었다면 여기서 반드시 실행해 정산한다.

마지막으로 게이트 4종(`npm run build`, `check-manifest`, `check-brand`, `check-lesson-structure`)이 푸시 후에도 전부 green인지 재확인한다.

이 Task는 `scripts/check-manifest.mjs`를 새로 고치지 않는다 — `files_modified`에 있는 것은 Task 1의 커밋을 이 Plan이 푸시하기 때문이다.
  </action>
  <verify>
    <automated>node --env-file=.env.local scripts/e2e-progress.mjs &amp;&amp; node --env-file=.env.local scripts/e2e-today.mjs &amp;&amp; node -e "const s=['3-1-vector-search-basics','3-1-hybrid-search-reranking','3-2-project-rag-agent','3-3-peft-lora-qlora','3-3-tuning-evaluation','3-4-multi-agent-structure','3-4-webhook-schedule-hitl','3-4-n8n-langgraph','3-5-project-orchestration','3-6-prompt-versioning-eval','3-6-monitoring-governance','3-6-structured-output-canary','3-7-project-ax-launch'];(async()=>{for(const x of s){const r=await fetch('https://ai-engineer-runway.vercel.app/lesson/'+x);const t=await r.text();if(r.status!==200||!t.includes('정답 보기')||t.includes('콘텐츠 준비 중')){console.error('FAIL',x,r.status);process.exit(1)}}console.log('13/13 ok')})()"</automated>
    <human-check>
아이패드 Safari로 프로덕션에서 아래 넷을 확인한다 (Phase 5 전체의 사람 확인 항목을 여기 모았다 — `human_verify_mode: end-of-phase`).

1. **프로젝트 준비 가이드 5편의 일관성과 "재현 아님" 경계** — `/lesson/2-4-project-ai-shop-frontend`, `/lesson/2-6-project-ai-shop-backend`, `/lesson/3-2-project-rag-agent`, `/lesson/3-5-project-orchestration`, `/lesson/3-7-project-ax-launch`를 차례로 훑는다. 다섯 편이 같은 형식으로 읽히는지, 그리고 어느 편도 "이거 읽으면 그냥 만들 수 있겠는데" 상태가 아닌지 본다. 개강 후 배울 것이 남아 있으면 통과다.
2. **2-1 모듈 SQL 실제 실행** — `05-07-SUMMARY.md`에서 미확인으로 남았다면 여기서 정산한다. `/lesson/2-1-postgres-and-supabase`와 `/lesson/2-1-ai-data-modeling`의 SQL을 Supabase SQL 에디터에 붙여 넣어 준비 블록 → `insert` → 조회 → 정리용 `drop` 순으로 오류 없이 실행되는지, 그리고 `public` 스키마의 진도 데이터가 그대로인지 확인한다.
3. **Making-of 흐름** — `/about`을 열어 Phase 1~5가 끊김 없이 읽히는지, 새로 추가된 Phase 4·5 기록의 톤이 앞부분과 같은지 본다.
4. **전체 진행률 100%** — 실제로 전 레슨 완료 상태를 만들 수는 없으므로, 홈과 `/curriculum`에서 현재 진행률 표시가 정상인지와 Step 2·3 카드가 더 이상 0%로 비어 있지 않은지만 확인한다. 100% 도달 여부는 Task 3의 자동 계산이 판정한다.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `git log origin/master..HEAD --oneline`의 출력이 비어 있다 (밀린 커밋 없음).
    - Step 3 13개 slug 각각에 대해 프로덕션 URL이 `200`을 반환하고 응답 본문에 `정답 보기`가 포함되며 `콘텐츠 준비 중`이 없다.
    - `node --env-file=.env.local scripts/e2e-progress.mjs`가 exit 0이다.
    - `node --env-file=.env.local scripts/e2e-today.mjs`가 exit 0이다.
    - **전 레슨 완료 시 전체 진행률이 정확히 100이다** (D-81): 진행률 계산 경로에 "전 레슨 완료" 입력을 넣었을 때 전체·Step별·모듈별 퍼센트가 모두 `100`이고 `99`가 하나도 없다. 계산 결과가 SUMMARY에 숫자로 기록된다.
    - `git diff --name-only HEAD~2 HEAD`에 `scripts/e2e-progress.mjs`·`scripts/e2e-today.mjs`가 나타나지 않는다 (e2e 통과를 위해 검사를 고치지 않았다).
    - 푸시 후 `npm run build`·`node scripts/check-manifest.mjs`·`node scripts/check-brand.mjs`·`node scripts/check-lesson-structure.mjs`가 모두 exit 0이고 `check-lesson-structure`가 검사한 레슨 수가 `35`다.
    - Step 2·3 전체의 프로덕션 도달 확인: Plan 07이 확인한 Step 2 12편과 이 Task가 확인한 Step 3 13편을 합한 25개 URL 모두에서 `콘텐츠 준비 중`이 0건이다 (Step 1 10개는 Phase 4에서 이미 확인됐다).
    - SUMMARY에 `<human-check>` 4항목의 확인 결과가 기록되거나, 아직 확인 전이면 그 사실이 명시된다.
  </acceptance_criteria>
  <done>Step 3 13편이 프로덕션에서 읽히고 25편 전부에서 빈 상태 카피가 사라졌다. 전 레슨 완료 시 전체 진행률이 100%로 표시됨이 숫자로 확인됐다. 오늘의 학습 루프가 35편 매니페스트에서 동작한다. Phase 5의 사람 확인 항목이 한자리에 모였다. 푸시 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `docs/making-of.md` → `/about` 공개 페이지 | Making-of는 공개 문서다. 여기 들어간 문자열은 그대로 인터넷에 노출된다 |
| 빈 상태 문구 → 화면 | 사용자에게 그대로 보이는 텍스트다 |
| 게이트 기대 상수 → CI/실행자 판정 | 최종 35 시점에 배열을 단순화하면 Invariant 10이 자기 자신과 비교해 아무것도 검사하지 않게 될 수 있다 |
| 진행률 계산 → 성공 기준 4 판정 | 반올림 한 번이 99%를 만들면 ROADMAP 성공 기준이 형식상 미충족이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | `docs/making-of.md`·`page.tsx` 문구 — 교육기관명·이메일 | high | mitigate | `node scripts/check-brand.mjs`가 `src/`·`docs/` 전체를 검사하고 Task 2의 verify에 포함된다. acceptance criteria가 세 파일에 대해 대소문자 무시 grep 0건을 확인한다. Making-of는 공개 페이지이므로 이 위협이 이 Plan에서 가장 크다 |
| T-05-32 | Tampering | Invariant 10 단순화로 검사가 무력화 | high | mitigate | Task 1의 action이 "동적 비교로 대체 금지, 추가만 허용"을 명시하고, acceptance criteria가 **레슨 하나의 `hasContent`를 임시로 끄면 exit 1이 되는지**를 실증으로 확인한다. 자기 자신과 비교하는 형태는 이 실증을 통과하지 못한다 |
| T-05-33 | Tampering | 빈 상태 분기 삭제로 안전망 상실 | medium | mitigate | prohibitions에 명시하고 acceptance criteria가 `hasContent` 분기와 `콘텐츠 준비 중` 문자열 존재, JSX 구조·타이포 클래스 불변을 확인한다 (D-79) |
| T-05-20 | Tampering | e2e·진행률 계산을 고쳐 100%를 만들어 냄 | high | mitigate | prohibitions에 명시하고 acceptance criteria가 `git diff --name-only`에 e2e 스크립트가 없음을 확인한다. 100%가 안 나오면 원인을 특정해 보고하고 사용자 판단에 맡긴다 |
| T-05-34 | Information Disclosure | Making-of가 내부 계획 문서 내용을 그대로 노출 | medium | mitigate | Making-of는 기존 Phase 1~3 기록과 같은 형식·톤으로 쓰며, 계획 문서의 결정 ID·내부 경로를 그대로 옮기지 않고 일상어로 풀어 쓴다. 기술 용어에는 괄호 한 줄 풀이를 붙인다 |
| T-05-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 저장소에 패키지를 설치하지 않는다. `package.json`은 변경되지 않는다 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

- **CONT-05 edge 프로브가 `unclassified → unresolved`로 남아 있다** (SPEC.md 부재, spec-less fallback §A). **이 Plan이 그 미해결 항목의 마지막 방어선**이다: `<human-check>` 항목 1이 프로젝트 가이드 5편을 사람 눈으로 훑어 "재현 아님" 경계를 최종 판정한다. 자동 검증은 각 authoring Plan의 grep(④ 절 실행 코드 펜스 0건, `구현해` 0건)까지가 한계이며, **내용 경계 자체는 끝까지 자동 측정 불가**임을 여기 명시한다. 사람 확인이 생략되면 이 항목은 미검증으로 남는다.
- **D-81의 실제 확인 대상이 e2e 스크립트의 범위를 넘을 수 있다.** `scripts/e2e-progress.mjs`는 프로브 레슨 하나를 완료 처리해 진행률이 움직이는지 보는 구조이지, "전 레슨 완료 시 100%"를 보는 구조가 아닐 수 있다. Task 3은 그 경우 **스크립트를 고치지 않고 별도 계산으로 확인**하게 했다. 99%가 나오면 그것은 성공 기준 4의 실제 결함이며, 이 Plan은 원인을 특정해 보고할 뿐 계산 로직을 임의로 고치지 않는다 — 고칠지 여부는 사용자 판단이다.
- **Phase 4 UAT 2건이 여전히 열려 있을 수 있다.** `05-01-PLAN.md`의 flagged assumption에서 Phase 5는 Phase 4 UAT를 블로킹 선행 조건으로 두지 않기로 했다. Phase 5가 끝나는 이 시점에도 Phase 4 UAT가 미해결이면, `/gsd-verify-work 04`와 `/gsd-verify-work 05`가 겹쳐서 남는다. **Step 1 레슨에 톤·형식 수정 요구가 나오면 Phase 5의 25편에도 파급된다**는 점을 SUMMARY에 남겨 사용자가 인지한 상태로 판단하게 한다.
- **`.env.local` 자격증명 의존.** Task 3의 `<precondition>`이 이를 강제한다 — 자격증명 없이는 D-81 검증 자체가 불가능하므로, 다른 Task처럼 "건너뛰고 기록"으로 넘기지 않고 중단하게 했다.
- **Making-of의 서술 품질은 자동 검증 불가다.** acceptance criteria는 섹션 존재·기존 섹션 보존·Phase 6 미포함·브랜드 0건까지만 확인한다. "Phase 1~5 흐름이 끊김 없이 읽히는가"는 `<human-check>` 항목 3이 맡는다.
</flagged_assumptions>

<verification>
1. Wave 3의 5개 SUMMARY 존재 (Task 1 `<precondition>`)
2. `npm run build`
3. `node scripts/check-manifest.mjs` — 13 불변식, 기대 개수 = 실측 35
4. Invariant 10 red 실증 — 레슨 하나의 `hasContent`를 임시로 끄면 exit 1
5. `node scripts/check-brand.mjs` — `docs/making-of.md` 갱신본 포함
6. `node scripts/check-lesson-structure.mjs` — 검사 레슨 수 35
7. `npm run lint`
8. `git push origin master` → Vercel 배포 → Step 3 13개 URL 200 + `정답 보기` 포함 + `콘텐츠 준비 중` 부재
9. `node --env-file=.env.local scripts/e2e-progress.mjs` (D-81)
10. `node --env-file=.env.local scripts/e2e-today.mjs`
11. 전 레슨 완료 시 전체·Step별·모듈별 진행률 = 100 (숫자로 기록)
12. `<human-check>` 4항목 (phase 종료 시 확인)
</verification>

<success_criteria>
- 35편 전부가 `hasContent: true`이고 매니페스트 기대 상수가 실측과 일치하며 13 불변식이 green이다
- 구조 게이트가 35편을 세 형식 예외 없이 한 벌 규칙으로 검사한다 (D-69, D-71)
- 빈 상태 분기가 안전망으로 유지되고 그 결정이 UI-SPEC에 기록됐다 (D-79)
- Making-of가 Phase 4·5 기록으로 마감되어 구현→검증→배포 흐름이 읽힌다 (D-80, 성공 기준 4)
- 전 레슨 완료 시 전체 진행률이 100%로 표시됨이 숫자로 확인됐다 (D-81, 성공 기준 4)
- Step 2·3 25편이 전부 프로덕션에서 읽히고 빈 상태 카피가 사라졌다
- Phase 5의 사람 확인 항목이 한자리에 모여 사용자에게 제시됐다
</success_criteria>

<output>
Create `.planning/phases/05-step-2-3/05-13-SUMMARY.md` when done.

담을 것:
- 실측 `hasContent: true` 개수(35)와 전체 레슨 수의 일치, CONTEXT.md D-78 원문 최종값과의 대조, Wave 1·2 중간 경계값 불일치 이력 정리 (RESEARCH Open Question 1 종결)
- Invariant 10 슬러그 배열 단순화 여부와 그 판단 근거, red 실증 결과
- 전 레슨 완료 시 전체·Step별·모듈별 진행률 숫자 (D-81) — 99가 나온 지점이 있으면 원인과 위치
- Step 3 13개 URL 확인 결과와 25편 전체의 빈 상태 카피 부재 확인
- `<human-check>` 4항목의 확인 결과 또는 미확인 상태
- Phase 4 UAT 잔여 상태와 그것이 Phase 5 25편에 미칠 수 있는 파급 (사용자 판단용)
- Phase 6(디자인 정리)이 물려받을 것 — 35편 레슨 본문이 전부 존재하는 상태, 개선될 읽기 화면의 대상 범위
</output>
