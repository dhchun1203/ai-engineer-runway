---
phase: 05-step-2-3
plan: 12
type: execute
wave: 4
depends_on: ["05-07"]
files_modified:
  - src/content/lessons/step-3/3-6-prompt-versioning-eval.mdx
  - src/content/lessons/step-3/3-6-monitoring-governance.mdx
  - src/content/lessons/step-3/3-6-structured-output-canary.mdx
autonomous: true
requirements: [CONT-05]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`/lesson/3-6-prompt-versioning-eval`이 프롬프트 버전관리와 평가 자동화를 **개요 깊이**로 렌더한다 — `.planning/curriculum.md` §3-6 첫 불릿 범위를 채운다 (D-62)"
    - "`/lesson/3-6-monitoring-governance`가 모니터링·알림과 보안·거버넌스를 개요 깊이로 렌더한다 — §3-6 둘째 불릿 범위를 채운다"
    - "`/lesson/3-6-structured-output-canary`가 구조화 출력 파이프라인·카나리 배포·비용/지연/성공률 지표를 개요 깊이로 렌더한다 — §3-6 셋째 불릿 범위를 채운다"
    - "세 레슨 모두 코드가 읽기용 스니펫 하나씩뿐이고 '지금 실행할 필요 없습니다' 취지의 안내가 붙어 있으며 설치·실행 명령이 0건이다 (D-63)"
    - "세 레슨의 `### 해보기`가 판단·설계형이다 — 도구 설치나 실행을 요구하지 않는다 (D-64)"
    - "`3-6-prompt-versioning-eval`이 `yaml` 코드펜스를 사용하고 그 언어가 빌드 산출물에서 하이라이팅된다 — D-72가 추가한 8개 언어 중 마지막 미실증 언어가 여기서 실사용된다 (RESEARCH Pitfall 5)"
    - "세 레슨 모두 6단 헤딩 원문 유지 + `### 해보기` 2~3개 + 해보기 개수 + 2 이상의 접힌 정답 블록 + `**이 레슨의 단어**` 표 5~8행을 갖고 `node scripts/check-lesson-structure.mjs`를 통과한다 (D-65, D-69)"
    - "세 레슨의 프론트매터에서 `hasContent: false → true` 한 줄 외에는 어떤 필드도 바뀌지 않았다 (D-13)"
    - "이 Plan이 끝나면 Step 3 13편 전부와 25편 전체가 `hasContent: true`가 된다 — 남은 것은 Plan 13의 마감뿐이다"
    - statement: "개요 3편을 읽고 나서 개강 첫 시간에 'LLMOps', '카나리', '개인정보 마스킹', '프롬프트 인젝션', '성공률 지표'를 들었을 때 무슨 얘기인지 알아들을 수 있다 (D-62)"
      verification: backstop
    - statement: "보안·거버넌스 서술이 공신력 있는 분류 체계를 참고했고 검증되지 않은 구체 수치·임계값을 단정하지 않는다 (RESEARCH Security Domain `[ASSUMED]` 항목)"
      verification: backstop
  artifacts:
    - src/content/lessons/step-3/3-6-prompt-versioning-eval.mdx
    - src/content/lessons/step-3/3-6-monitoring-governance.mdx
    - src/content/lessons/step-3/3-6-structured-output-canary.mdx
  key_links:
    - "`2-7-promptops`(Plan 06, 손으로 해 본 골든셋 실습) → `3-6-prompt-versioning-eval`(같은 개념을 운영 관점으로) — 용어를 일치시켜야 학습자가 같은 것임을 알아본다"
    - "`2-7-prompt-patterns`(구조화 출력 실습) → `3-6-structured-output-canary`(운영에서의 구조화 출력) — 같은 관계"
    - "`3-6-monitoring-governance` → `3-7-project-ax-launch`(Plan 11) — 프로젝트 가이드의 정책 초안 체크리스트가 이 레슨의 개념을 전제한다"
    - "`yaml` 펜스 → `ALLOWED_FENCE_LANG_PREFIXES` — Plan 01이 목록에 넣지 않았다면 L6가 즉시 red를 낸다"
    - "이 Plan의 3편 → Plan 13의 매니페스트 실측 (최종 35)"
  prohibitions:
    - statement: "레슨 본문·코드 주석·메타데이터 어디에도 교육기관명(`scripts/check-brand.mjs`의 금지 문자열)을 노출해서는 안 된다 (D-02)"
      status: unverified
    - statement: "Step 3 개요 레슨에 설치·실행 안내를 붙이거나 해보기 과제로 실행을 요구해서는 안 된다 (D-63)"
      status: unverified
    - statement: "개요 레슨의 학습 목표에 `직접 구현할 수 있다`·`직접 튜닝할 수 있다`·`성능을 비교 측정할 수 있다`를 써서는 안 된다 (D-62)"
      status: unverified
    - statement: "검증되지 않은 보안 임계값·비용 수치·성공률 기준을 사실처럼 단정해서는 안 된다 — 이 세션에서 확인되지 않은 실무 지식 영역이다 (RESEARCH Security Domain `[ASSUMED]`)"
      status: unverified
    - statement: "레슨 본문에 실제 개인정보 예시(실명·주민번호 형식·실제 이메일 주소)를 적어서는 안 된다 — 마스킹을 설명할 때도 형식만 보여 준다 (T-05-29)"
      status: unverified
    - statement: "6단 헤딩 텍스트를 바꾸거나 단어 표·해보기 개수 규칙을 예외 처리해서는 안 된다 (D-65, D-69)"
      status: unverified
    - statement: "프론트매터의 `hasContent` 외 어떤 필드도 바꿔서는 안 된다 (D-13, D-31)"
      status: unverified
---

<objective>
Step 3 배치의 3-6(LLMOps) 모듈 3편을 집필한다 — 프롬프트 버전관리·평가 자동화, 모니터링·알림·보안 거버넌스, 구조화 출력 파이프라인·카나리 배포·비용 지표.

Purpose: **Step 2에서 손으로 해 본 것들을 운영 관점으로 다시 보는 모듈**이다. `2-7-promptops`의 골든셋 실습과 `2-7-prompt-patterns`의 구조화 출력이 여기서 "서비스로 돌릴 때 무엇이 더 필요한가"로 확장된다. 이 Plan이 끝나면 25편 전부가 `hasContent: true`가 된다.

Output: 개요 형식 레슨 3편, `hasContent: true` 전환 3건, Step 3 13편 완성.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-step-2-3/05-CONTEXT.md
@.planning/phases/05-step-2-3/05-RESEARCH.md
@.planning/phases/05-step-2-3/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces

**Plan 12 (이 Plan)이 만드는 것**
- `src/content/lessons/step-3/3-6-prompt-versioning-eval.mdx` — 본문 신규, `hasContent: false → true`
- `src/content/lessons/step-3/3-6-monitoring-governance.mdx` — 본문 신규, `hasContent: false → true`
- `src/content/lessons/step-3/3-6-structured-output-canary.mdx` — 본문 신규, `hasContent: false → true`

**Phase 5 전체가 만들거나 바꾸는 것** — `05-01-PLAN.md` §Artifacts this phase produces 참조.
</artifacts_produced>

<manifest_gate_note>
**이 wave 동안 `node scripts/check-manifest.mjs`는 의도적으로 red다.** `hasContent`를 3건 켜면 실제 개수가 Plan 07이 맞춰 둔 상수를 넘어 Invariant 10이 실패한다. 상수는 Wave 3 마감 Plan 13이 실측으로 되돌린다(D-78). **실행자는 `check-manifest`를 verify에서 제외하고 상수를 손대지 않는다.**
</manifest_gate_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: `3-6-prompt-versioning-eval` 집필 — 프롬프트 버전관리와 평가 자동화 (개요 깊이, `yaml` 펜스 실증)</name>
  <files>src/content/lessons/step-3/3-6-prompt-versioning-eval.mdx</files>
  <read_first>
    - `src/content/lessons/step-3/3-6-prompt-versioning-eval.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "프롬프트 버전관리와 평가 자동화"`, `stepId: 3`, `moduleId: "3-6"`, `order: 1`, `depth: "개요"`, `estimatedMinutes: 90`, `hasContent: false`)을 확인한다.
    - `src/content/lessons/step-3/3-1-vector-search-basics.mdx` (Plan 01 완료본) — **개요 형식의 승인된 실물.** 학습 목표 3요소 형식, 읽기용 스니펫 안내 문구, 판단형 해보기 문장을 그대로 가져온다. 전체를 읽는다.
    - `src/content/lessons/step-2/2-7-promptops.mdx` (Plan 06 완료본) — 학습자가 골든셋·평가 스크립트를 이미 손으로 만들어 봤다. **여기서 쓴 용어(골든셋·회귀·버전 태그·형식 통과율)를 그대로 이어 쓴다.** 반복 설명하지 않고 "손으로 해 본 그것을 서비스 규모로 돌리면" 형태로 연결한다.
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md` — 체크포인트 항목 2의 Step 3 깊이 판정 결과, 그리고 `yaml` 펜스 실증 결과.
    - `.planning/curriculum.md` §Step 3 §3-6 첫 불릿("프롬프트 버전관리, 평가 자동화") — 이 레슨의 범위 원천.
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩은 아래 6개뿐이며 문자열과 순서를 그대로 쓴다: `## 1. 학습 목표`, `## 2. 왜 배우나`, `## 3. 개념 설명`, `## 4. 실무 예제`, `## 5. 실무 팁`, `## 6. 핵심 정리 및 스스로 점검`(D-65).

- **① 학습 목표** — "알아듣습니다" 형태 3~4개, 각 항목이 3요소 형식 중 하나. `성능을 비교 측정할 수 있다`류 금지(D-62).
- **② 왜 배우나** — 최대 4문장. `2-7-promptops`에서 손으로 만든 평가 스크립트가 사람이 기억해야 돌아간다면 결국 안 돌아간다는 점, 그래서 배포 흐름에 묶어 자동으로 돌게 만든다는 점.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 다룰 것: 프롬프트 버전 관리(파일과 태그로 관리하고 어떤 버전이 언제 운영에 있었는지 남기기 — 비유: 계약서 개정 이력), 평가 자동화가 붙는 위치(변경이 올라올 때 자동으로 평가가 돌고 기준 미달이면 막힌다 — 비유: 공장 출하 전 검사), 평가 결과를 어떻게 보관하고 비교하나(버전별 점수 이력), 무엇이 자동화되고 무엇이 사람 몫인지의 경계. `프롬프트 변경 → 자동 평가 실행 → 기준 통과 여부 → 통과하면 배포 / 실패하면 차단` 한 줄 도식(D-48).
- **④ 실무 예제** — `### 실무에서 이게 어떻게 쓰이나` 소제목으로 시작한다. 실제 사례를 2~3단락 서술한다: 프롬프트 한 줄을 고쳤다가 특정 유형 질문에서만 답이 나빠졌는데 자동 평가가 막아 준 경우. 그다음 인용문(`>`) 두 줄로 "지금 실행할 필요 없습니다" 안내(D-63, Plan 01과 같은 문구). 그 아래 **`yaml` 펜스 하나**에 프롬프트 변경 시 평가를 돌리는 자동화 설정 10~20줄 발췌를 넣는다 — 어떤 파일이 바뀌면 무엇이 도는지 알아볼 수 있는 최소 형태, 각 키가 무슨 뜻인지 주석 한 줄씩. 코드 블록은 이 하나뿐이며 30줄을 넘기지 않는다. **설치·실행 안내를 붙이지 않는다.**
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 평가 기준선을 처음부터 100점으로 잡으면 아무것도 배포되지 않는다, 골든셋은 실제 실패 사례로 채운다(2-7과 같은 원칙), 어떤 버전이 언제 운영에 있었는지 남겨야 사고 원인을 찾을 수 있다.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표 5~8행(버전 태그, 골든셋, 회귀, 기준선, 자동 평가, 배포 차단, 실행 이력 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.
- **`### 해보기` 2~3개는 판단·설계형**(D-64). 소재: 우리 프로젝트에서 자동으로 잴 수 있는 기준 3개와 사람이 봐야 하는 기준 2개를 나눠 표로 적기, 평가 기준선을 몇 점으로 잡을지 정하고 근거 쓰기, 프롬프트 사고가 났을 때 원인을 찾으려면 무엇이 기록돼 있어야 하는지 목록으로 적기. 각 해보기 아래 `<details>` 정답은 "모범 답안 + 왜 그렇게 보는가".

90분 = 읽기 약 40분 + 판단형 해보기 약 50분(D-64). 용어 표기는 D-50을 따른다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `3-6-prompt-versioning-eval`이 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent: false` → `hasContent: true` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`이고 순서가 1→6이며 `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며 빈 줄 규칙을 지킨다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - `grep -c '^```yaml$'`가 `1`이고 `grep -c '^```'`가 `2`다 (코드 블록 정확히 하나, 언어는 `yaml`), 본문이 30줄 이하다.
    - `grep -c 'npm install'`·`grep -c 'pip install'`·`grep -ci '실행 방법'`가 모두 `0`이다 (D-63).
    - `## 1. 학습 목표` 절에 `구현할 수 있다`·`튜닝할 수 있다`·`비교 측정할 수 있다` 문자열이 0건이다 (D-62).
    - 코드 블록 바로 앞 5줄 안에 `실행할 필요는 없습니다` 취지의 인용문(`>`로 시작하는 줄)이 있다.
    - `grep -ci '골든셋'`가 `2` 이상이다 (`2-7-promptops`와 용어가 이어진다).
    - `grep -ciE 'kant'`가 `0`이다.
    - `node -e "const l=require('./.velite/lessons.json');const p=l.find(x=>x.slug==='3-6-prompt-versioning-eval');process.exit(/language-yaml/.test(p.code)?0:1)"`가 exit 0이다 (D-72가 추가한 마지막 미실증 언어가 실제 빌드에서 살아 있다).
  </acceptance_criteria>
  <done>3-6 첫 편이 개요 깊이로 작성됐고 `yaml` 펜스가 실제 빌드에서 하이라이팅된다. `2-7-promptops`와 용어가 이어진다. 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: `3-6-monitoring-governance` 집필 — 모니터링·알림과 보안 거버넌스 (개요 깊이, 단정 금지)</name>
  <files>src/content/lessons/step-3/3-6-monitoring-governance.mdx</files>
  <read_first>
    - `src/content/lessons/step-3/3-6-monitoring-governance.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "모니터링·알림과 보안 거버넌스"`, `moduleId: "3-6"`, `order: 2`, `depth: "개요"`, `estimatedMinutes: 90`)을 확인한다.
    - `src/content/lessons/step-3/3-6-prompt-versioning-eval.mdx` (Task 1 완료본) — 같은 모듈 앞 편. 실행 이력·기록 개념을 이어 쓰고 반복하지 않는다.
    - `src/content/lessons/step-2/2-7-prompt-patterns.mdx` (Plan 06 완료본) — ⑤ 실무 팁에 "입력을 구분자·역할 분리로 감싼다"가 이미 나왔다. 이 레슨이 그것을 인젝션 개념으로 정식화한다.
    - `.planning/phases/05-step-2-3/05-RESEARCH.md` §Security Domain — 이 모듈의 PII·인젝션 정책 설명이 이 세션에서 검증되지 않은 실무 지식(`[ASSUMED]`)임을 명시한 항목. **단정하지 않는 서술 방식의 근거다.**
    - `.planning/curriculum.md` §Step 3 §3-6 둘째 불릿("모니터링, 알림, 보안/거버넌스 설계") — 이 레슨의 범위 원천.
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩 6개는 Task 1과 동일한 문자열·순서.

**서술 원칙(이 레슨에만 적용되는 추가 제약):** 보안·거버넌스는 이 세션에서 검증되지 않은 실무 지식 영역이다(RESEARCH `## Security Domain` 마지막 행). 따라서 **구체 임계값·비율·규정 조항을 단정하지 않는다.** "보통 이렇게 나눈다", "무엇을 정해야 하는지"까지만 쓰고, 숫자가 필요한 자리에는 "팀이 정한다"로 남긴다. 공신력 있는 분류 체계를 참고했다면 이름만 언급하고 조항을 인용하지 않는다.

- **① 학습 목표** — "알아듣습니다" 형태 3~4개, 3요소 형식. 금지 목표 없음(D-62).
- **② 왜 배우나** — 최대 4문장. LLM 서비스는 "에러 없이 돌지만 답이 이상한" 상태가 가능하다는 점(일반 서버 모니터링만으로는 안 잡힌다), 그리고 개인정보가 프롬프트·로그를 타고 흘러 나갈 수 있다는 점.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 다룰 것: 무엇을 보는가를 표로(시스템 지표: 오류율·지연·비용 / 품질 지표: 형식 통과율·거절률·사용자 피드백 — 둘이 다르다는 점이 핵심), 알림 설계(임계값을 어떻게 정하는가의 **원칙**만 — 평소 값을 먼저 재고 그 위에서 정한다, 알림이 너무 잦으면 아무도 안 본다), 개인정보 취급(어디에 남고 어디에 남으면 안 되는가 — 프롬프트·로그·평가 데이터 세 곳을 짚고 마스킹은 형식만 예시), 프롬프트 인젝션(사용자 입력이 지시를 덮어쓰려 하는 것 — `2-7`에서 본 구분자 감싸기가 그 대응이라는 연결)과 감사 추적. `요청 → 처리 → 지표 수집 → 임계값 비교 → 알림 → 사람 판단 → 기록` 한 줄 도식(D-48).
- **④ 실무 예제** — `### 실무에서 이게 어떻게 쓰이나` 소제목으로 시작한다. 실제 사례를 2~3단락 서술한다: 오류율은 정상인데 사용자 불만이 늘어 품질 지표를 보고서야 원인을 찾은 경우, 또는 로그에 남은 사용자 입력에 개인정보가 섞여 있던 경우. 그다음 인용문(`>`) 두 줄로 "지금 실행할 필요 없습니다" 안내(D-63). 그 아래 `json` 펜스 **하나**에 로그·지표 레코드의 형태 10~20줄 발췌를 넣는다 — 어떤 항목을 남기고 어떤 항목을 마스킹했는지 보이는 최소 형태. **실제 개인정보 예시를 쓰지 않는다** — 이름·연락처 자리에는 마스킹된 형식만 넣는다. 코드 블록은 이 하나뿐이며 30줄 이하. **설치·실행 안내를 붙이지 않는다.**
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 반드시 포함: 남길 항목과 남기지 않을 항목을 설계 단계에서 정한다(나중에 지우기 어렵다), 알림은 "사람이 지금 행동할 수 있는 것"에만 건다, 사용자 입력과 지시를 구분해 감싼다.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표 5~8행(관측, 지표, 임계값, 마스킹, 개인정보, 프롬프트 인젝션, 감사 추적 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.
- **`### 해보기` 2~3개는 판단·설계형**(D-64). 소재: 우리 서비스에서 로그에 남기면 안 되는 항목 5개를 적고 각각 왜인지 쓰기, 시스템 지표 3개와 품질 지표 3개를 나눠 표로 적고 각각 어떻게 잴지 한 줄씩, 사용자 입력이 지시를 덮어쓰려는 문장 예시를 하나 만들어 보고 어떻게 막을지 적기. 각 해보기 아래 `<details>` 정답은 "모범 답안 + 왜 그렇게 보는가".

90분 = 읽기 약 40분 + 판단형 해보기 약 50분(D-64).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `3-6-monitoring-governance`가 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`, `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며 빈 줄 규칙을 지킨다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - `grep -c '^```'`가 `2`다 (코드 블록 정확히 하나), 언어 태그가 `json`이며 본문이 30줄 이하다.
    - `grep -c 'npm install'`·`grep -c 'pip install'`·`grep -ci '실행 방법'`가 모두 `0`이다 (D-63).
    - `## 1. 학습 목표` 절에 `구현할 수 있다`·`튜닝할 수 있다`·`비교 측정할 수 있다` 문자열이 0건이다.
    - 코드 블록 바로 앞 5줄 안에 `실행할 필요는 없습니다` 취지의 인용문이 있다.
    - `## 3. 개념 설명` 구간에 시스템 지표와 품질 지표를 나눈 마크다운 표가 1개 이상 있다.
    - 실제 개인정보 형식이 0건이다: `grep -cE '[0-9]{6}-[0-9]{7}'`가 `0`이고 `grep -cE '[a-zA-Z0-9._%+-]+@[a-zA-Z0-9.-]+\.(com|net|kr|org)'`가 `0`이다.
    - `grep -ciE '인젝션'`가 `2` 이상이고 `grep -ciE '마스킹'`가 `2` 이상이다 (커리큘럼 §3-6 둘째 불릿 범위를 실제로 채운다).
    - `grep -ciE 'kant'`가 `0`이다.
    - `node -e "const l=require('./.velite/lessons.json');const p=l.find(x=>x.slug==='3-6-monitoring-governance');process.exit(/language-json/.test(p.code)?0:1)"`가 exit 0이다.
  </acceptance_criteria>
  <done>3-6 둘째 편이 개요 깊이로 작성됐고, 보안·거버넌스를 "무엇을 정해야 하는가"까지만 다루며 검증되지 않은 수치를 단정하지 않는다. 실제 개인정보 형식이 0건이다. 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: `3-6-structured-output-canary` 집필 — 구조화 출력·카나리 배포·비용 지표 (개요 깊이, Step 3 13편 완성)</name>
  <files>src/content/lessons/step-3/3-6-structured-output-canary.mdx</files>
  <read_first>
    - `src/content/lessons/step-3/3-6-structured-output-canary.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "구조화 출력·카나리 배포·비용 지표"`, `moduleId: "3-6"`, `order: 3`, `depth: "개요"`, `estimatedMinutes: 90`)을 확인한다.
    - `src/content/lessons/step-3/3-6-prompt-versioning-eval.mdx`·`3-6-monitoring-governance.mdx` (Task 1·2 완료본) — 같은 모듈 앞 두 편. 지표·기록 용어를 이어 쓰고 반복하지 않는다.
    - `src/content/lessons/step-2/2-7-prompt-patterns.mdx` (Plan 06 완료본) — 구조화 출력과 스키마 검증을 손으로 해 봤다. **여기서 쓴 용어를 그대로 이어 쓰고**, 이 레슨은 "그것을 서비스로 돌릴 때 파싱이 실패하면 무슨 일이 생기나"로 확장한다.
    - `.planning/curriculum.md` §Step 3 §3-6 셋째 불릿("구조화 출력 파이프라인, 카나리/롤백, 비용/지연/성공률 지표") — 이 레슨의 범위 원천. **세 항목 모두 다뤄야 한다.**
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩 6개는 Task 1과 동일한 문자열·순서.

- **① 학습 목표** — "알아듣습니다" 형태 3~4개, 3요소 형식. `성능을 비교 측정할 수 있다`류 금지(D-62).
- **② 왜 배우나** — 최대 4문장. `2-7`에서 만든 구조화 출력이 서비스에서 하루 수천 번 돌 때 실패가 반드시 섞인다는 점, 그리고 프롬프트·모델을 바꿀 때 전부 한 번에 바꾸면 되돌릴 수 없다는 점.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 다룰 것: 구조화 출력 파이프라인(요청 → 응답 → 검증 → 실패 시 무엇을 하나: 재시도·기본값·사람에게 넘기기 세 선택지를 표로 비교), 카나리 배포(새 버전을 전체가 아니라 일부 요청에만 먼저 태우기 — 비유: 새 메뉴를 한 테이블에만 먼저 내 보기)와 롤백 조건을 **미리 정해 두는 이유**, 지표 세 축(비용·지연·성공률)이 서로 맞바꿔진다는 점(더 정확한 모델은 느리고 비싸다)과 무엇을 우선할지는 서비스가 정한다는 점. `요청 → 모델 → 스키마 검증 → 실패 처리 → 지표 기록 → 카나리 비율 조정 / 롤백` 한 줄 도식(D-48).
- **④ 실무 예제** — `### 실무에서 이게 어떻게 쓰이나` 소제목으로 시작한다. 실제 사례를 2~3단락 서술한다: 새 프롬프트를 전체에 적용했다가 특정 입력에서 형식이 깨져 화면이 비었던 경우, 그리고 카나리로 10%만 태웠다면 어떻게 달랐을지. 그다음 인용문(`>`) 두 줄로 "지금 실행할 필요 없습니다" 안내(D-63). 그 아래 `typescript` 펜스 **하나**에 카나리 비율에 따라 어느 버전을 쓸지 고르고 결과 지표를 기록하는 10~20줄 스니펫 — 실패 처리 분기를 주석으로 표시한다. 코드 블록은 이 하나뿐이며 30줄 이하. **설치·실행 안내를 붙이지 않는다.**
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 반드시 포함: 롤백 조건을 배포 전에 숫자로 정해 둔다(사고 중에는 판단이 흐려진다), 검증 실패도 지표로 남긴다(조용히 재시도하면 문제가 안 보인다), 비용은 요청당으로 보지 말고 하루 총액으로도 본다.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표 5~8행(구조화 출력, 스키마 검증, 카나리, 롤백, 성공률, 지연, 토큰 비용 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.
- **`### 해보기` 2~3개는 판단·설계형**(D-64). 소재: 우리 서비스에서 구조화 출력이 실패했을 때 세 선택지(재시도·기본값·사람) 중 무엇이 맞는지 상황별로 고르고 이유 쓰기, 카나리 비율과 롤백 조건을 숫자로 정하고 근거 적기, 비용·지연·성공률 중 우리 서비스가 무엇을 우선할지 정하고 나머지 둘을 어디까지 포기할지 쓰기. 각 해보기 아래 `<details>` 정답은 "모범 답안 + 왜 그렇게 보는가".

90분 = 읽기 약 40분 + 판단형 해보기 약 50분(D-64). 마지막 문단에서 이 편이 Step 3 마지막 개요 레슨임을 한 줄로 짚어 준다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `3-6-structured-output-canary`가 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`, `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며 빈 줄 규칙을 지킨다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - `grep -c '^```'`가 `2`다 (코드 블록 정확히 하나), 언어 태그가 `typescript`이며 본문이 30줄 이하다.
    - `grep -c 'npm install'`·`grep -ci '실행 방법'`가 모두 `0`이다 (D-63).
    - `## 1. 학습 목표` 절에 `구현할 수 있다`·`튜닝할 수 있다`·`비교 측정할 수 있다` 문자열이 0건이다.
    - 코드 블록 바로 앞 5줄 안에 `실행할 필요는 없습니다` 취지의 인용문이 있다.
    - `## 3. 개념 설명` 구간에 검증 실패 시 세 선택지를 비교한 마크다운 표가 1개 이상 있다.
    - `grep -ciE '카나리'`가 `3` 이상, `grep -ciE '롤백'`가 `2` 이상, `grep -ciE '성공률'`가 `2` 이상이다 (커리큘럼 §3-6 셋째 불릿 세 항목을 모두 채운다).
    - `grep -ciE 'kant'`가 `0`이고 `grep -c 'sk-ant-'`가 `0`이다.
    - **Step 3 13편 전부가 `hasContent: true`다**: `node -e "const l=require('./.velite/lessons.json');const s3=l.filter(x=>x.stepId===3);process.exit(s3.length===13&&s3.every(x=>x.hasContent)?0:1)"`가 exit 0이다.
    - **25편 전체(Step 2 12편 + Step 3 13편)가 `hasContent: true`다**: `node -e "const l=require('./.velite/lessons.json');const t=l.filter(x=>x.stepId===2||x.stepId===3);process.exit(t.length===25&&t.every(x=>x.hasContent)?0:1)"`가 exit 0이다.
  </acceptance_criteria>
  <done>3-6 모듈 3편이 모두 개요 깊이로 작성됐고 Step 3 13편이 완성됐다. Phase 5의 레슨 25편 전부가 `hasContent: true`다. 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레슨 본문 → 공개 프로덕션 URL | 본문 문자열이 그대로 인터넷에 노출된다 |
| 로그·지표 예시 → 학습자의 실제 로그 설계 | 예시에 개인정보를 넣으면 그 형태가 그대로 복제된다 |
| 보안·거버넌스 서술 → 학습자의 정책 판단 | 검증되지 않은 수치를 단정하면 잘못된 기준이 굳는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | 레슨 본문 — 교육기관명·이메일·API 키 | high | mitigate | `node scripts/check-brand.mjs` 상시 검사 + acceptance criteria의 대소문자 무시 grep 0건 + 키 형태 문자열 0건 |
| T-05-29 | Information Disclosure | 로그·지표 예시에 실제 개인정보 형식이 등장 | high | mitigate | Task 2의 `json` 예시에서 이름·연락처 자리에 마스킹된 형식만 쓴다. acceptance criteria가 주민번호 형식·이메일 주소 정규식 0건을 확인한다 |
| T-05-30 | Tampering | 검증되지 않은 보안 임계값·비용 수치를 사실처럼 제시 | medium | mitigate | Task 2에 별도 서술 원칙(단정 금지, "팀이 정한다"로 남기기)을 명시하고 prohibitions에 넣었다. RESEARCH `## Security Domain`이 이 영역을 `[ASSUMED]`로 표시한 것이 근거다 |
| T-05-16 | Tampering | 프롬프트 인젝션 대응을 안 가르침 | medium | mitigate | Task 2의 ③ 개념 설명과 ⑤ 실무 팁 양쪽에서 다루고, acceptance criteria가 `인젝션` 2회 이상 등장을 확인한다. `2-7-prompt-patterns`의 구분자 감싸기와 연결한다 |
| T-05-31 | Tampering | 검증 실패를 조용히 삼키는 습관 | medium | mitigate | Task 3의 ⑤ 실무 팁에 "검증 실패도 지표로 남긴다"를 필수 항목으로 넣고, ③에 세 선택지 비교 표를 강제한다 |
| T-05-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 저장소에 패키지를 설치하지 않고, Step 3 레슨은 설치 자체를 안내하지 않는다(D-63). acceptance criteria가 설치 명령 0건을 확인한다 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

- **CONT-05 edge 프로브가 `unclassified → unresolved`로 남아 있다** (SPEC.md 부재, spec-less fallback §A). 이 Plan의 세 레슨은 프로젝트 가이드가 아니지만, `3-7-project-ax-launch`(Plan 11)의 정책 초안 체크리스트가 `3-6-monitoring-governance`의 개념을 전제한다 — **여기서 개념이 흔들리면 프로젝트 가이드의 준비 항목이 근거를 잃는다.** 두 Plan이 같은 wave에서 병렬 집필되므로 용어 일치는 Plan 13의 사람 확인에서 최종 점검된다.
- **RESEARCH `## Sources` Tertiary — 3-6/3-7의 PII·인젝션 정책 설명에 참고할 출처를 이 세션에서 직접 조사하지 않았다(`[ASSUMED]`).** Task 2가 이 위험을 **단정하지 않는 서술 원칙**으로 처리한다: 구체 임계값·비율·규정 조항을 쓰지 않고 "무엇을 정해야 하는가"까지만 다룬다. 공신력 있는 분류 체계를 참고하면 이름만 언급하고 조항을 인용하지 않는다. 그럼에도 개념 수준의 정확성은 자동 검증 불가이며, 이것이 이 Plan에서 가장 큰 미해소 위험이다.
- **RESEARCH Pitfall 2 — 개요 팽창 위험.** LLMOps는 실무에서 넓은 주제라 지표·도구 나열로 팽창하기 쉽다. acceptance criteria가 코드 블록 1개·30줄 이하·설치 안내 0건·학습 목표 금지어 0건으로 팽창 경로를 막지만 산문 깊이는 자동 측정 불가다.
- **A2 — `yaml` 펜스가 이 저장소에서 실증되는 것은 Task 1이 처음이다.** Plan 01이 Shiki 번들 로드로 사전 확인했더라도 실제 MDX 컴파일 결과에 `language-yaml`이 붙는지는 여기서 처음 확인된다. 실패해도 빌드는 깨지지 않고 단색 폴백이 되므로, 폴백이 나오면 SUMMARY에 기록하고 Plan 13의 사람 확인 항목으로 넘긴다.
- **`check-manifest.mjs`는 이 wave 동안 의도적으로 red다** — `<manifest_gate_note>` 참조.
</flagged_assumptions>

<verification>
1. `npm run build`
2. `node scripts/check-lesson-structure.mjs` — 세 레슨 모두 오류 목록에 없음
3. `node scripts/check-brand.mjs`
4. Step 3 13편·25편 전체 `hasContent: true` 확인 (`node -e` 2종, Task 3)
5. `yaml` 펜스 하이라이팅 확인 (`node -e`, Task 1)
6. `node scripts/check-manifest.mjs`는 **실행하지 않는다** (이 wave 동안 의도적 red, Plan 13이 되돌린다)
</verification>

<success_criteria>
- 3-6 모듈 3편이 개요 깊이로 작성됐고 커리큘럼 §3-6 세 불릿을 모두 덮는다 (D-62~D-65)
- 보안·거버넌스 서술이 검증되지 않은 수치를 단정하지 않고 실제 개인정보 형식이 0건이다
- `yaml` 펜스가 실제 빌드에서 하이라이팅된다 (D-72 마지막 미실증 언어 해소)
- Step 3 13편과 Phase 5의 25편 전부가 `hasContent: true`다
- 확대된 구조 게이트와 브랜드 게이트를 통과한다
</success_criteria>

<output>
Create `.planning/phases/05-step-2-3/05-12-SUMMARY.md` when done.

담을 것: `yaml` 펜스 하이라이팅 결과(폴백 여부), 세 레슨의 스니펫 언어와 길이, `2-7` 모듈과 맞춘 용어 목록, `3-7-project-ax-launch`(Plan 11)와의 정책 용어 일치 여부, 확정된 해보기 개수, 게이트 통과 시점의 `hasContent: true` 총 개수(Plan 13이 실측과 대조한다 — 35가 예상값).
</output>
