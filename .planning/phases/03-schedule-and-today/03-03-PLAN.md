---
phase: 03-schedule-and-today
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/lib/pace.ts
  - scripts/check-pace.mjs
  - src/components/pace-status.tsx
  - src/components/behind-lessons-list.tsx
  - src/components/today-lesson-card.tsx
  - src/app/page.tsx
  - scripts/check-progress-gates.mjs
  - scripts/e2e-today.mjs
autonomous: true
requirements: [SCHED-02, SCHED-04]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "시크릿 쿠키 보유 시 홈에 ahead · on-track · behind 세 상태 중 하나가 정확히 하나 렌더된다 (D-42)"
    - "페이스 판정이 어제까지 배정된 레슨의 estimatedMinutes 합과 그중 완료된 분의 합을 비교해 나온다 — 오늘 배정 레슨은 판정에서 제외된다 (D-40/D-41)"
    - "미래 배정 레슨을 미리 완료해도 어제까지 배정분에 미완료가 있으면 behind로 판정된다 — 전체 완료 합계가 어제까지 배정분을 가리지 않는다 (RESEARCH Pitfall 3)"
    - "일정 첫날(2026-08-25)에 완료가 0건이어도 어제까지 배정분이 0이므로 behind가 아니다 (D-41 경계)"
    - "버퍼일 행(lessonSlug=null)은 배정 합계에도 완료 합계에도 밀린 레슨 목록에도 들어가지 않는다"
    - "behind일 때 '약 {N}시간 분량({M}개 레슨) 밀렸어요.'와 '하루 30분씩 추가하면 {K}일이면 따라잡아요.' 두 문장이 함께 렌더되고 K = ceil(밀린 분 ÷ 30)이다 (D-43)"
    - "behind 안내 문구가 값이 커져도 문장 구조가 고정된 채 숫자만 늘어나며 말줄임 없이 자연 줄바꿈된다 (UI고려 #11, #12)"
    - "behind가 아니면 밀린 레슨 섹션이 화면에 흐릿하게가 아니라 DOM에서 완전히 없다 (UI고려 #14, D-20 부재 원칙)"
    - "밀린 레슨 행이 날짜 · 레슨 제목 · 소요시간과 '레슨 시작하기' CTA를 갖고 /lesson/{slug}로 이동한다 (UI고려 #17, D-39)"
    - "밀린 레슨이 0개면 섹션 생략, 1개면 단일 행, 여러 개면 같은 행 구조 반복 — 개수별 특수 분기가 없다 (UI고려 #19)"
    - statement: "여러 주가 밀려 행이 많아져도 전부 나열하며 접기(collapse) UI를 두지 않고 스크롤로 해결한다"
      verification: backstop
    - "오늘 배정 레슨을 완료했거나 앞서 있으면 '오늘 학습을 모두 마쳤어요!' 축하 상태와 내일 레슨 미리보기가 렌더되고, 이동은 사용자가 링크를 눌러야만 일어난다 (D-38, UI고려 #2)"
    - "오늘이 9/28이라 내일이 버퍼일이거나 9/29라 내일 배정이 없을 때도 축하 상태가 빈 문장이나 깨진 링크를 내지 않는다 (D-38 경계)"
    - "오늘 배정 레슨은 완료·미완료·쿠키 없음 세 경우 모두 정확히 하나의 상태로 렌더되며 '여러 개 배정'은 발생하지 않는다 (UI고려 #6, D-32)"
    - "진도 조회가 실패하면 페이스 패널 자리에 기존 ProgressReadError 배너가 뜨고 밀린 레슨 섹션은 통째로 생략되며, 어디에도 0%나 on-track 같은 추측 상태가 표시되지 않는다 (UI고려 #4, #13, #16)"
    - "쿠키가 없으면 페이스 패널과 밀린 레슨 섹션이 DOM에 없고, 오늘 배정 레슨의 제목·소요시간·깊이 배지·D-day는 그대로 보인다 (D-37)"
  artifacts:
    - src/lib/pace.ts
    - scripts/check-pace.mjs
    - src/components/pace-status.tsx
    - src/components/behind-lessons-list.tsx
  key_links:
    - "getScheduleRows() + getLessonMinutesBySlug() + completedIds + todayInSeoul() → computePace() — 페이스 판정의 유일한 입력 경로"
    - "computePace().missedSlugs → BehindLessonsList — 밀린 레슨 목록은 페이스 계산 결과에서만 나온다 (별도 재계산 금지)"
    - "hasUnlockCookie() → readCompletedLessonIds() → completedIds !== null 일 때만 PaceStatus/BehindLessonsList 렌더 (D-37 게이트)"
    - "computePace().gapMinutes → catchUpDays() → 따라잡기 안내 문구의 K 값"
  prohibitions:
    - statement: "페이스·밀린 레슨 표현이 질책·수치심·마감 압박 톤이 되어서는 안 된다 — destructive 색, 경고/오류 아이콘, '실패·뒤처짐·게으름' 류 어휘를 쓰지 않고 안내 톤을 유지한다 (D-43)"
      status: unverified
    - statement: "스트릭 카운터 · 연속 학습일 · 배지 수집 같은 게이미피케이션 장치를 도입해 학습을 강박화해서는 안 된다 — 긍정 피드백은 문구와 상태 표시로만 준다 (CONTEXT specifics)"
      status: unverified
    - statement: "축하 상태나 밀린 레슨 안내가 사용자를 다음 레슨으로 자동 이동시키거나 오늘 레슨 완료를 강제해서는 안 된다 — 이동은 항상 사용자가 링크를 누를 때만 (D-38)"
      status: unverified
---

<objective>
"개강까지 페이스가 맞는가"에 답하는 층을 얹는다 — 시간 가중 3단계 판정, 밀린 레슨 목록, 축하 상태. D-37의 게이트된 절반이다.

Purpose: Plan 01이 뚫은 공개 경로 위에 진도 파생 UI를 올린다. 이 판정은 Phase 2의 D-26이 "시간 가중은 Phase 3 몫"이라고 예고한 바로 그 계산이며, 잘못 구현하면(전체 완료 합계와 어제까지 배정분을 섞으면) 밀렸는데 순항 중이라고 알려주는, 있으나 마나 한 것보다 나쁜 상태가 된다.

Output: 의존성 0 순수 판정 모듈 `pace.ts`와 그 단위 게이트, 3단계 페이스 패널·밀린 레슨 목록 컴포넌트, 완료 상태와 축하 상태까지 갖춘 오늘 레슨 카드, 그리고 이 전부를 쿠키 유/무 두 경로로 왕복 확인하는 확장된 종단 게이트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-schedule-and-today/03-CONTEXT.md
@.planning/phases/03-schedule-and-today/03-RESEARCH.md
@.planning/phases/03-schedule-and-today/03-PATTERNS.md
@.planning/phases/03-schedule-and-today/03-UI-SPEC.md
@.planning/phases/03-schedule-and-today/03-01-SUMMARY.md

<interfaces>
Plan 01이 만든 계약 (이 Plan이 소비):
- `src/lib/schedule.ts` — `type ScheduleRow = { date: string; lessonSlug: string | null; isBuffer: boolean }`, `SCHEDULE_START = '2026-08-25'`, `COURSE_START_DATE = '2026-09-30'`, `buildSchedule(...)`, `scheduleTotalDays(n)`
- `src/lib/today.ts` — `todayInSeoul(now?: Date): string`, `daysUntil(targetDateISO, fromDateISO): number`
- `src/lib/schedule-data.ts` — `getScheduleRows(): ScheduleRow[]`, `getLessonMinutesBySlug(): Map<string, number>`
- `src/components/today-lesson-card.tsx` — `TodayLessonCard({ todayLesson, state })`, `type TodayCardState = 'before-start' | 'assigned' | 'buffer' | 'after-range'` (이 Plan이 props를 확장한다)
- `src/components/dday-countdown.tsx` — `DDayCountdown({ daysUntil })`

기존 계약:
- `src/lib/progress-store.ts` — `readCompletedLessonIds(): Promise<{ ok: true; completedIds: Set<string> } | { ok: false; error: string }>`
- `src/lib/auth.ts` — `hasUnlockCookie(): Promise<boolean>`
- `src/components/progress-error.tsx` — `<ProgressReadError className?="" />`
- `src/components/estimated-time.tsx` — `formatEstimatedTime(minutes): string` (60분 미만 "약 N분", 이상 "약 N시간")
- `src/components/depth-badge.tsx` — `<DepthBadge depth stepId />`
- `src/content/curriculum-helpers.ts` — `getLessonBySlug(slug)`

RESEARCH.md Pattern 2가 `computePace`의 3분기 알고리즘 코드를 그대로 담고 있다 — 그 코드가 이 Plan의 출발점이다.
</interfaces>
</context>

<artifacts_produced>
## Artifacts this phase produces

**이 Plan이 새로 만드는 심볼 (전부 신규 — 드리프트 검증 대상 아님)**
- `src/lib/pace.ts` — `computePace(rows, minutesBySlug, completedIds, todayStr): PaceResult`, `catchUpDays(gapMinutes: number): number`, `type PaceStatus = 'ahead' | 'on-track' | 'behind'`, `type PaceResult = { status: PaceStatus; gapMinutes: number; missedSlugs: string[] }`
- `src/components/pace-status.tsx` — `PaceStatusPanel({ pace })`
- `src/components/behind-lessons-list.tsx` — `BehindLessonsList({ rows })`
- `scripts/check-pace.mjs` — 실행형 게이트 (export 없음)

**이 Plan이 확장하는 심볼**
- `src/components/today-lesson-card.tsx` — `TodayLessonCard`의 props에 `completed: boolean | null`, `tomorrow` 추가. `TodayCardState`에 `'celebration'` 추가
- `src/app/page.tsx` — 기본 export `Home()` 유지
- `scripts/check-progress-gates.mjs` — G18 대상 확대 + G19 추가
- `scripts/e2e-today.mjs` — 쿠키 보유 시나리오 추가

**Phase 3 전체 신규 심볼 (앞선/이후 Plan 포함):** `todayInSeoul`, `daysUntil`, `SCHEDULE_START`, `COURSE_START_DATE`, `scheduleTotalDays`, `buildSchedule`, `ScheduleRow`, `getScheduleRows`, `getLessonMinutesBySlug`, `DDayCountdown`, `TodayLessonCard`, `TodayCardState`, `CurriculumPage`, `computePace`, `catchUpDays`, `PaceStatus`, `PaceResult`, `PaceStatusPanel`, `BehindLessonsList`, `ScheduleTable`, `ScheduleAutoScroll`, `SchedulePage`, `scripts/check-schedule.mjs`, `scripts/check-pace.mjs`, `scripts/e2e-today.mjs`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 시간 가중 페이스 판정 순수 모듈과 단위 게이트</name>
  <files>src/lib/pace.ts, scripts/check-pace.mjs</files>
  <behavior>
    - 오늘=2026-08-25(첫날), 완료 0건 → status `on-track` (어제까지 배정분이 0이므로 밀린 것이 없다)
    - 어제까지 배정 3건이 전부 완료 → `on-track`, gapMinutes 0, missedSlugs 빈 배열
    - 어제까지 배정 3건 중 1건만 완료 → `behind`, gapMinutes = 미완료 2건의 분 합, missedSlugs = 그 2건의 slug (날짜 오름차순)
    - 어제까지 배정 전부 완료 + 오늘 배정도 완료 → `ahead`, gapMinutes 0, missedSlugs 빈 배열
    - 어제까지 배정에 미완료가 있는데 미래 배정 레슨을 미리 완료 → `behind` (전체 완료 합계가 어제까지 배정분을 가리지 않는다 — Pitfall 3)
    - 버퍼일 행(lessonSlug=null)이 섞여 있어도 배정 합계·완료 합계·missedSlugs 어디에도 들어가지 않는다
    - minutesBySlug에 없는 slug는 0분으로 취급하고 예외를 던지지 않는다
    - 오늘이 2026-09-30 이후여서 모든 행이 과거일 때도 판정이 정상 동작한다
    - 호출 후 입력 rows 배열 · minutesBySlug Map · completedIds Set이 변형되지 않는다
    - catchUpDays(0)=0, catchUpDays(1)=1, catchUpDays(30)=1, catchUpDays(150)=5, catchUpDays(151)=6
  </behavior>
  <read_first>
    - `src/lib/progress-math.ts` — 의존성 0 순수 모듈의 상단 주석 관례, 타입-옆-함수 export 스타일, "입력을 변형하지 않는다" 계약 문서화 방식.
    - `scripts/check-progress-math.mjs` — `runCase` 수집기 구조와 `import(pathToFileURL(...))` 로딩 방식. `check-pace.mjs`가 그대로 복제한다.
    - `scripts/check-schedule.mjs` (Plan 01 산출물) — 같은 phase 안에서 확립된 게이트 문체.
    - `src/lib/schedule.ts` (Plan 01 산출물) — `ScheduleRow` 타입 정의.
    - `.planning/phases/03-schedule-and-today/03-RESEARCH.md` — Pattern 2의 `computePace` 코드와 Pitfall 3.
  </read_first>
  <action>
`src/lib/pace.ts`를 만든다. import 문을 하나도 쓰지 않는다 — `progress-math.ts`·`today.ts`·`schedule.ts`와 같은 이유이고, 상단 주석에 그 이유를 한 줄로 남긴다. `ScheduleRow`를 import하지 않고 구조적 타입(`readonly { date: string; lessonSlug: string | null }[]`)으로 받는다.

`computePace(rows, minutesBySlug, completedIds, todayStr)`는 RESEARCH.md Pattern 2 코드를 출발점으로 삼되 두 스코프의 변수명을 반드시 분리해서 유지한다 — 어제까지 배정분 합계와 전체 배정 완료 합계는 이름이 다른 별개 변수여야 한다(Pitfall 3의 실패 모드가 정확히 이 둘을 하나로 합치는 것이다). `date < todayStr` 문자열 사전순 비교로 과거 행을 고른다(두 값 모두 같은 포맷 규칙으로 생성되므로 Date 객체 왕복이 필요 없다). `lessonSlug`가 null인 행은 어느 합계에도 넣지 않는다. 반환은 `{ status, gapMinutes, missedSlugs }`이며 `behind`가 아닐 때 `gapMinutes`는 0, `missedSlugs`는 빈 배열이다. `missedSlugs`는 `rows` 순서를 그대로 따라 날짜 오름차순이 된다.

`catchUpDays(gapMinutes)`도 같은 파일에 둔다 — `gapMinutes`가 0 이하면 0, 아니면 `Math.ceil(gapMinutes / 30)`. 하루 추가 30분이라는 상수는 이 함수 안에 명명 상수로 두고 UI 문구가 다시 계산하지 않게 한다.

`scripts/check-pace.mjs`를 만든다. `check-progress-math.mjs` 구조 그대로, `src/lib/pace.ts`를 동적 로드하고 위 `<behavior>`의 케이스를 전부 `runCase`로 덮는다. 더미 데이터로 충분하다 — 실제 매니페스트를 읽지 않는다. 실패 시 실패 케이스 이름을 모아 exit 1.
  </action>
  <verify>
    <automated>node scripts/check-pace.mjs && node scripts/check-schedule.mjs && node scripts/check-progress-math.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-pace.mjs`가 exit 0으로 끝나고 통과 케이스 수를 출력하며 그 수가 12개 이상이다.
    - `src/lib/pace.ts`에 `import` 키워드로 시작하는 문장이 한 줄도 없다 (Task 3의 확장된 G18이 상시 검사한다).
    - "미래 레슨 선완료 + 어제까지 미완료" 케이스가 `behind`를 반환한다 — 이 한 케이스를 통과시키지 못하면 D-40 구현이 틀린 것이다.
    - `computePace` 호출 뒤 입력 배열·Map·Set의 내용이 스냅샷과 `deepStrictEqual`로 같다.
    - `check-pace.mjs`가 `node:assert/strict` 외의 외부 패키지를 쓰지 않고 `package.json`에 새 의존성이 없다.
  </acceptance_criteria>
  <done>페이스 판정이 3초 안에 도는 단독 명령으로 검증되고, Pitfall 3의 오판 경로가 케이스로 고정됐다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 페이스 패널 · 밀린 레슨 · 축하 상태를 홈에 얹기</name>
  <files>src/components/pace-status.tsx, src/components/behind-lessons-list.tsx, src/components/today-lesson-card.tsx, src/app/page.tsx</files>
  <read_first>
    - `src/app/page.tsx` (Plan 01 재작성본) — 게이트 3줄과 `ProgressSummary`/`ProgressReadError`/`null` 3분기, `todayRow` 판정 로직. 이 위에 얹는다.
    - `src/components/today-lesson-card.tsx` (Plan 01 산출물) — 확장 대상. 기존 4개 상태 문구를 유지한 채 props를 넓힌다.
    - `src/components/progress-summary.tsx` — 상태별 heading/body 분기와 `min-h-11` CTA 버튼 클래스. `pace-status.tsx`가 이 형태를 그대로 따른다.
    - `src/components/module-accordion.tsx` — 완료 표시(`CheckCircle2` + 톤다운 텍스트)와 "다시 보기"/"레슨 시작하기" CTA 문구의 원본. 오늘 레슨 카드와 밀린 레슨 행이 같은 시각 언어를 쓴다.
    - `src/components/step-card.tsx` — 전체 행이 하나의 `<Link>`가 되는 목록 항목 형태(`flex min-h-11 ...`). `behind-lessons-list.tsx`가 이 형태를 따른다.
    - `src/components/progress-error.tsx` — 조회 실패 배너. 새 카피를 만들지 않고 그대로 재사용한다.
    - `.planning/phases/03-schedule-and-today/03-UI-SPEC.md` — Copywriting Contract 전 행(문구 원본), Color의 "on-track과 behind는 accent를 쓰지 않는다" 규칙과 destructive 미사용 유지, Typography application map, Design System 표의 아이콘 4종.
  </read_first>
  <action>
`src/components/pace-status.tsx`를 만든다. 서버 렌더 순수 컴포넌트, props는 `{ pace: PaceResult }` 하나. 루트에 `data-schedule-ui="pace"`와 `data-pace-status={pace.status}`를 단다. `progress-summary.tsx`의 3분기 heading/body 패턴을 그대로 써서 UI-SPEC Copywriting Contract의 문구를 넣는다 — ahead는 헤딩 "앞서가고 있어요" / 본문 "계획보다 빠르게 진행하고 있어요.", on-track은 "순항 중이에요" / "계획대로 잘 따라가고 있어요.", behind는 "조금 밀렸어요" / "약 {N}시간 분량({M}개 레슨) 밀렸어요."에 이어 "하루 30분씩 추가하면 {K}일이면 따라잡아요."를 별도 문장으로 렌더한다. N은 `formatEstimatedTime(pace.gapMinutes)`의 결과를 그대로 쓰고(중복 포맷 로직을 새로 만들지 않는다), M은 `pace.missedSlugs.length`, K는 `catchUpDays(pace.gapMinutes)`다.

색 규칙을 정확히 지킨다: accent 색은 **ahead 상태에만** 쓴다(상태 문구와 아이콘). on-track은 중성 텍스트, behind도 중성 텍스트다 — destructive 토큰은 이 phase에서도 계속 미사용 예약 상태로 남긴다. 아이콘은 `lucide-react`에서 ahead는 `TrendingUp`, behind는 `Clock`을 `aria-hidden="true"`로 넣되, 설치된 버전에서 이름이 해석되지 않으면 아이콘을 빼고 텍스트만 남긴다 — 이해가 아이콘에 의존하지 않는다.

`src/components/behind-lessons-list.tsx`를 만든다. props는 `{ rows }`이며 각 원소는 `{ date, slug, title, depth, stepId, estimatedMinutes }` 형태의 이미 조립된 평범한 데이터다(컴포넌트가 스스로 매니페스트를 조회하지 않는다). 루트 `<section>`에 `data-schedule-ui="behind-list"`, 헤딩은 "밀린 레슨 {M}개"(Heading 20px/600). 각 행은 `step-card.tsx`처럼 전체가 하나의 `<Link href={/lesson/{slug}}>`이고 `min-h-11`을 갖는다. 행 내용은 날짜 · 레슨 제목 · `<EstimatedTime>` · `<DepthBadge>` · CTA "레슨 시작하기". 개수 상한이나 접기 UI를 넣지 않는다 — 길어지면 스크롤로 해결한다(RESEARCH Open Question 2 권장안). 목록이 비면 이 컴포넌트를 렌더하지 않는 것은 호출자의 책임이며, 컴포넌트 스스로도 빈 배열이면 null을 돌려준다.

`src/components/today-lesson-card.tsx`를 확장한다. props에 `completed: boolean | null`(null = 쿠키 없음/조회 실패)과 `tomorrow`(내일 행에서 파생한 `{ kind: 'lesson', slug, title } | { kind: 'buffer' } | { kind: 'none' }`)를 추가하고, `TodayCardState`에 `'celebration'`을 더한다. `assigned` 상태에서 `completed === true`면 CTA 문구가 "다시 보기", `false`면 "레슨 시작하기", `null`이면 완료 마커 없이 "레슨 시작하기"를 쓴다(조회 실패를 미완료로 오인시키지 않는다 — 마커만 생략한다). `completed === true`이거나 호출자가 ahead를 알려주면 `celebration` 상태로 렌더한다: 헤딩 "오늘 학습을 모두 마쳤어요!", 본문은 내일 행 종류에 따라 — 레슨이면 `내일은 "{제목}"을 배워요.`와 CTA "내일 레슨 미리 보기" → `/lesson/{slug}`, 버퍼일이면 "내일은 복습·정리일이에요."와 CTA 생략, 없으면 "사전학습 일정을 모두 마쳤어요."와 CTA "커리큘럼 보기" → `/curriculum`. 어떤 경우에도 자동 이동·리다이렉트를 걸지 않는다(D-38).

`src/app/page.tsx`를 통합한다. 게이트 순서를 바꾸지 않는다 — `hasUnlockCookie()`가 여전히 최우선이다. `completedIds`가 non-null일 때만 `computePace(rows, minutesBySlug, completedIds, today)`를 호출하고 그 결과로 `<PaceStatusPanel>`을 렌더한다. `pace.status === 'behind'`이고 `missedSlugs`가 비어 있지 않을 때만 `<BehindLessonsList>`를 렌더하며, 행 데이터는 `missedSlugs`를 `getLessonBySlug`와 `rows`로 조합해 만든다(밀린 목록을 별도로 재계산하지 않는다). `completedIds`가 null이고 `progressRead`가 실패였다면 페이스 패널 자리에 `<ProgressReadError>`를 렌더하고 밀린 레슨 섹션은 통째로 생략한다. 쿠키가 아예 없으면 두 섹션 모두 DOM에서 없다. `TodayLessonCard`에는 `completed`(`completedIds?.has(slug) ?? null`)와 내일 행 파생값을 넘긴다. 렌더 순서는 D-day → 오늘 레슨 카드 → 페이스 패널 → 밀린 레슨 → 기존 전체 진행률 요약이다.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-pace.mjs && node scripts/check-progress-gates.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`가 성공하고 타입 오류가 없다.
    - 쿠키 없이 렌더한 홈 HTML에 `data-schedule-ui="pace"`와 `data-schedule-ui="behind-list"`가 각각 0건이다 (Task 3의 e2e가 이를 실측한다).
    - `src/app/page.tsx`에서 `hasUnlockCookie` 첫 등장 문자 위치가 `readCompletedLessonIds`보다 앞선다.
    - `src/components/pace-status.tsx`에 destructive 색 토큰(`text-destructive`/`bg-destructive` 계열)이 쓰이지 않는다 — behind 상태도 중성 텍스트다.
    - `src/components/behind-lessons-list.tsx`에 개수 상한 상수나 `<details>` 접기 요소가 없다.
    - `src/components/pace-status.tsx`가 소요시간 포맷 문자열을 직접 조립하지 않고 `formatEstimatedTime`을 호출한다.
    - 밀린 레슨 행과 오늘 레슨 카드 CTA가 모두 `min-h-11` 클래스를 갖는다.
    - `node scripts/check-brand.mjs`가 통과한다.
  </acceptance_criteria>
  <done>쿠키를 켠 상태로 홈에 들어가면 지금 앞서가는지 순항 중인지 밀렸는지, 밀렸다면 무엇을 따라잡아야 하는지가 한 화면에 보인다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 게이트 확장 — 순수성·게이트 순서 상시화와 쿠키 유/무 종단 검증</name>
  <files>scripts/check-progress-gates.mjs, scripts/e2e-today.mjs</files>
  <read_first>
    - `scripts/check-progress-gates.mjs` — Plan 01이 추가한 G17·G18의 형태를 확인하고 같은 문체로 잇는다. 기존 G2(클라이언트 컴포넌트의 서버 전용 모듈 import 금지)와 G14(게이트 순서)의 어설션 방식도 참고한다.
    - `scripts/e2e-today.mjs` (Plan 01 산출물) — 확장 대상. 기존 t1~t5 시나리오, 매니페스트 독립 재파싱 방식, 쿠키 헤더 조립 관례를 확인한다.
    - `scripts/e2e-progress.mjs` — 쿠키 포함 요청과 service_role로 프로브 행을 만들었다 지우는 정리 패턴, 시크릿을 출력하지 않는 규칙.
    - `src/lib/pace.ts`, `src/app/page.tsx` — 이번에 검사 대상이 되는 파일들의 실제 형태.
  </read_first>
  <action>
`scripts/check-progress-gates.mjs`를 확장한다. G18의 검사 대상 목록에 `src/lib/pace.ts`를 더해 의존성 0 순수 모듈 세 개(today · schedule · pace)를 모두 덮는다. G19를 새로 추가한다: `src/lib/pace.ts`와 `src/lib/schedule.ts`가 Supabase 계열·`progress-store`·Velite 매니페스트 식별자를 참조하지 않는지 확인한다 — G13이 `progress.ts`에 대해 하는 것과 같은 형태의 계층 분리 검사이며, 주석은 `stripJsLineComments()`로 걷어낸 뒤 검사해 게이트가 자기 설명 문장에 걸려 오탐하는 일을 막는다.

`scripts/e2e-today.mjs`를 확장한다. `e2e-progress.mjs`처럼 service_role 클라이언트로 프로브 행을 직접 만들었다 지우는 방식을 쓴다. 프로브 대상은 "어제까지 배정된 레슨" 중 하나여야 판정이 의미를 갖는데, 실행 시점(2026-08-24 전후)에는 과거 배정분이 없을 수 있다 — 그러므로 시나리오를 오늘 날짜에 따라 분기한다.

추가 시나리오: (t6) 쿠키 포함 `GET /` → 200이고, 오늘이 일정 범위 안이면 `data-schedule-ui="pace"`가 정확히 1건이며 `data-pace-status` 값이 ahead·on-track·behind 셋 중 하나다. 오늘이 범위 밖이면 페이스 패널이 있어도 없어도 되지만 응답은 200이고 오늘 레슨 카드는 범위 밖 상태 문구를 담는다. (t7) 쿠키 없이 `GET /` → `data-schedule-ui="pace"` 0건, `data-schedule-ui="behind-list"` 0건, `data-schedule-ui="dday"`와 `data-schedule-ui="today-card"`는 그대로 1건씩 (D-37 공개/게이트 분리의 핵심 어설션). (t8) 과거 배정분이 하나라도 존재하는 날에만 도는 조건부 시나리오 — 과거 배정 레슨 하나를 service_role로 미완료로 만든 뒤 쿠키 포함 `GET /`이 `data-pace-status="behind"`와 `data-schedule-ui="behind-list"` 1건을 내는지 확인하고, 끝나면 프로브 행을 원래대로 되돌린다. 과거 배정분이 없는 날에는 이 시나리오를 건너뛰되 조용히 넘어가지 말고 건너뛴 이유를 출력한다.

기존 t1~t5는 그대로 두고, 실패 수집·일괄 보고·서버 트리 종료 처리도 그대로 쓴다. 어떤 출력에도 쿠키 값·시크릿을 찍지 않는다.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-progress-gates.mjs && node --env-file=.env.local scripts/e2e-today.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-progress-gates.mjs`가 exit 0으로 끝나고 "all gates passed"를 출력한다.
    - `src/lib/pace.ts` 상단에 아무 import 문이나 하나 임시로 넣으면 G18이 exit 1로 실패한다 (되돌린 뒤 다시 통과 확인).
    - `node --env-file=.env.local scripts/e2e-today.mjs`가 exit 0으로 끝나고 t1~t7 통과를 출력한다. t8은 통과하거나, 건너뛴 사유를 명시적으로 출력한다.
    - t7이 쿠키 없는 응답에서 페이스·밀린 레슨 마커 0건과 D-day·오늘 카드 마커 1건씩을 동시에 확인한다 — 둘 중 하나만 보는 검사가 아니다.
    - e2e 실행 후 Supabase `progress` 테이블에 스크립트가 만든 프로브 행이 남아 있지 않다.
    - 스크립트 출력 어디에도 `UNLOCK_SECRET` 값이나 service_role 키가 찍히지 않는다.
  </acceptance_criteria>
  <done>진도 파생 UI가 쿠키 없이는 DOM에 존재하지 않는다는 사실이 정적 게이트와 실제 서버 왕복 두 층으로 고정됐다. 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 익명 브라우저 → `/` (오늘의 학습) | 신뢰되지 않은 요청. 이 Plan이 붙이는 페이스·밀린 레슨은 전부 진도 파생이라 쿠키 없이는 존재해선 안 된다 |
| RSC 서버 → Supabase `progress` | 기존 서버 전용 경계. 이 Plan은 새 쓰기 경로를 만들지 않고 읽기만 한다 |
| e2e 게이트 스크립트 → Supabase `progress` | 검증용 프로브 행을 만들었다 지우는 임시 쓰기 경계 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Information Disclosure | `src/components/pace-status.tsx`, `src/components/behind-lessons-list.tsx` | high | mitigate | 두 컴포넌트를 `completedIds !== null`일 때만 렌더해 쿠키 없는 응답의 DOM에서 완전히 제거한다(시각적 숨김이 아니다). `e2e-today.mjs` t7이 쿠키 없는 응답에서 두 마커가 0건임을 실측 |
| T-03-11 | Information Disclosure | `src/lib/pace.ts` 계층 분리 | medium | mitigate | `pace.ts`가 Supabase·`progress-store`·Velite 매니페스트를 import하지 않는다 — 완료 집합을 인자로 받을 뿐 스스로 읽지 않는다. Task 3의 G18·G19가 상시 검사 |
| T-03-12 | Tampering | `scripts/e2e-today.mjs`의 프로브 행 쓰기 | medium | mitigate | 프로브 행을 시나리오 전후로 명시적으로 되돌리고, 되돌림 여부를 acceptance criteria로 확인한다. 프로덕션과 같은 테이블을 공유하므로(Phase 2 결정) 잔여 행이 실제 진도를 오염시킬 수 있다 |
| T-03-13 | Spoofing | 페이스 판정 입력 위조 | low | accept | 완료 집합은 서버가 service_role로 직접 읽고, 클라이언트가 페이스 값을 제출하는 경로가 없다. 클라이언트 입력 자체가 존재하지 않으므로 별도 검증을 두지 않는다 |
| T-03-14 | Information Disclosure | 로그·스크립트 출력의 시크릿 유출 | medium | mitigate | e2e 스크립트가 쿠키 값·service_role 키를 어떤 출력에도 찍지 않는다(Phase 2 규칙 승계). acceptance criteria로 확인 |
| T-03-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 패키지를 설치하지 않는다 — RESEARCH.md Package Legitimacy Audit이 "설치 대상 없음"으로 확정. `lucide-react`는 Phase 1에서 이미 설치된 기존 의존성이며 새 아이콘 이름만 사용한다 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe fallback §A)

SPEC.md 부재로 결정론적 edge 프로브가 4건 전부 `unclassified → unresolved`를 냈다. 이 Plan이 소유하는 2건:

| Req | 프로브 결과 | 이 Plan의 처리 |
|-----|------------|---------------|
| SCHED-02 | unclassified / unresolved | 완료 상태 경계(완료·미완료·쿠키 없음·조회 실패 4경우)와 축하 상태 경계(내일이 레슨/버퍼일/없음 3경우)를 수동 도출해 `must_haves.truths`로 명시하고 Task 2 acceptance criteria로 고정 |
| SCHED-04 | unclassified / unresolved | 판정 경계(첫날 0건, 미래 선완료, 버퍼일 제외, 범위 밖 접속)를 수동 도출해 truths와 `check-pace.mjs` 케이스로 명시 |

추가 가정:
- **A1 (RESEARCH Assumptions Log):** 따라잡기 안내의 K = `ceil(gapMinutes ÷ 30)`, 하루 추가 30분 상수. CONTEXT.md가 이 계산식을 Claude's Discretion으로 남겼고 UI-SPEC Copywriting Contract가 같은 식을 채택했다. 틀려도 문구·상수 한 줄 수정으로 복구된다.
- **T-03-12 잔여 위험:** e2e 프로브가 프로덕션과 같은 Supabase 테이블을 쓴다(Phase 2 결정). 되돌림을 acceptance criteria로 고정했으나 스크립트가 중간에 죽으면 프로브 행이 남을 수 있다 — 실행 후 `node scripts/check-supabase-progress.mjs`로 확인할 것.
- **UI 아이콘 해석:** `lucide-react` 1.33에서 `TrendingUp`·`Clock`·`PartyPopper` 이름이 해석되지 않을 수 있다. UI-SPEC 지시대로 그 경우 아이콘을 빼고 텍스트만 남긴다 — 이해가 아이콘에 의존하지 않는다.
</flagged_assumptions>

<verification>
1. `npm run build`
2. `node scripts/check-pace.mjs` — 3분기 판정과 Pitfall 3 오판 경로
3. `node scripts/check-schedule.mjs`, `node scripts/check-manifest.mjs`, `node scripts/check-progress-math.mjs` — 무회귀
4. `node scripts/check-progress-gates.mjs` — 순수성·계층 분리·게이트 순서
5. `node scripts/check-brand.mjs`
6. `node --env-file=.env.local scripts/e2e-today.mjs` — 쿠키 유/무 두 경로 왕복
7. `node --env-file=.env.local scripts/check-supabase-progress.mjs` — 프로브 행 잔여 확인
</verification>

<success_criteria>
- 쿠키 보유 시 ahead / on-track / behind 중 정확히 하나가 표시되고, behind면 정량 + 따라잡기 안내와 밀린 레슨 목록이 함께 나온다
- 쿠키가 없으면 페이스·밀린 레슨이 DOM에 없고 D-day·오늘 레슨은 그대로 보인다
- 진도 조회 실패 시 추측 상태 대신 기존 오류 배너가 뜬다
- `pace.ts`가 의존성 0을 유지하고 판정 경계가 단위 게이트로 고정됐다
</success_criteria>

<output>
Create `.planning/phases/03-schedule-and-today/03-03-SUMMARY.md` when done
</output>
