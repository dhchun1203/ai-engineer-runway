---
phase: 06-site-wide-design-polish
plan: 07
type: execute
wave: 3
depends_on: ["06-02", "06-04"]
files_modified:
  - src/app/curriculum/page.tsx
  - src/app/page.tsx
autonomous: true
requirements: [SC2, SC4]
user_setup: []

estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "/curriculum이 Step 카드 3장 그리드만 있는 상태에서 벗어나, 사이트가 이미 계산하고 있는 값(전체 진행률, D-day)을 끌어와 첫 화면을 채운다 — 새 데이터·새 쿼리·새 DB 필드를 만들지 않는다 (D-90)"
    - "홈 재배치는 06-02의 D-97 실측 판정에 따라 실행되거나 명시적으로 종료된다 — 쿠키 있는 상태의 첫 화면 빈 영역이 뷰포트 높이의 30% 미만이면 홈을 작업하지 않는다 (D-97)"
    - "/curriculum의 빈 캔버스는 쿠키 유무와 무관하게 유효하므로 D-97 판정과 관계없이 처리된다"
    - "재배치 후에도 쿠키가 없으면 진행률·페이스·밀린레슨 섹션이 렌더되지 않는 기존 조건부 구조가 그대로 유지된다"
    - "재배치는 기존 서버 컴포넌트가 이미 계산한 props를 다른 자리에 놓는 것뿐이며, 컴포넌트의 props 시그니처와 계산 함수를 바꾸지 않는다"
    - "2트랙 폭 규칙(그리드형 max-w-5xl)과 컨테이너 공통 패딩·gap-8이 유지된다 (D-99)"
    - "기존 자동 게이트 10종이 전부 통과한다 (D-94)"
  artifacts:
    - src/app/curriculum/page.tsx
    - "src/app/page.tsx (D-97 판정이 홈 작업을 여는 경우에만)"
  key_links:
    - "06-02의 D-97 판정 → 이 플랜의 홈 태스크 실행 여부 — 판정 없이 홈을 고치면 실사용자가 겪지 않는 상태를 고치게 된다"
    - "/curriculum의 기존 completedIds 조회 → stepProgress → StepCard — 재배치는 이 흐름에 새 쿼리를 얹지 않고 같은 값을 추가 컴포넌트에 넘긴다"
  prohibitions:
    - statement: "새 데이터·새 쿼리·새 DB 필드를 만들지 않는다 — 이미 계산되고 있는 값만 재배치한다 (D-90)"
      status: active
      verification: flagged
    - statement: "새 기능을 추가하지 않는다 — 빈 공간을 채우는 수단은 기존 정보의 재배치뿐이다 (D-90)"
      status: active
      verification: flagged
    - statement: "새 색 토큰·새 폰트 파일을 도입하지 않는다 (D-R4K-2, D-R4K-3)"
      status: active
      verification: flagged
    - statement: "새 라우트를 만들지 않고 렌더링 전략(force-dynamic)을 바꾸지 않는다"
      status: active
      verification: flagged
    - statement: "375px 대응을 이유로 truncate나 고정폭 클래스를 새로 도입하지 않는다"
      status: active
      verification: flagged
    - statement: "쿠키가 없을 때 진행률·페이스·밀린레슨을 렌더하지 않는 기존 조건부 분기를 없애지 않는다"
      status: active
      verification: flagged
    - statement: "사용자에게 보이는 어떤 문자열에도 교육기관명(K로 시작하는 4글자 브랜드)을 쓰지 않는다"
      status: active
      verification: flagged
---

<objective>
04-UI-REVIEW가 지적한 빈 캔버스를 **새 기능이 아니라 기존 정보의 재배치**로 푼다(D-90).
`/curriculum`은 쿠키 유무와 무관하게 `StepCard` 3장 그리드뿐이라 아이패드 세로에서 약 40%가 비어
있고, 이건 두 쿠키 상태 모두에서 유효한 문제다. 홈은 다르다 — 04-UI-REVIEW의 "55% 빈 캔버스"는
잠금 해제 쿠키가 없는 세션에서 나온 값이고, 실사용자는 10년 만료 쿠키를 항상 갖고 있어 일상적인
홈은 이미 5개 섹션을 렌더한다(D-97). 그래서 홈 작업은 06-02의 실측 판정에 걸려 있다.

**타임박스 상한 (명시).** 이 플랜이 Phase 6의 2일 타임박스를 위협하면 **이 플랜 전체가 v2로
밀려나는 첫 번째 후보다.** 성공 기준 2는 "같은 셸로 읽힌다"이지 "빈 공간이 없다"가 아니다 —
06-03/06-04/06-05/06-06이 이미 셸 통일을 끝냈으므로 이 플랜을 드롭해도 성공 기준 2는 성립한다.
드롭하는 경우 그 사실과 이유를 요약에 남기고 v2(CONV-01~04) 후보로 기록한다.

Purpose: 6종 화면 중 가장 비어 보이는 화면을 이미 있는 정보로 채운다.
Output: 라우트 페이지 최대 2개 수정.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-site-wide-design-polish/06-CONTEXT.md
@.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md
@.planning/phases/06-site-wide-design-polish/06-02-SUMMARY.md
@.planning/phases/06-site-wide-design-polish/06-D97-MEASUREMENT.md
@.planning/phases/06-site-wide-design-polish/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: /curriculum 재배치 — 이미 계산된 진행률·D-day를 끌어온다 (D-90)</name>
  <files>src/app/curriculum/page.tsx</files>
  <read_first>
    - `src/app/curriculum/page.tsx` 전체(36줄) — 이미 `hasUnlockCookie()` → `readCompletedLessonIds()`
      → `stepProgress(step.id, completedIds)`를 호출하고 있다. **필요한 데이터가 이미 이 파일 안에
      있다** — 새 조회가 필요 없다는 것이 D-90의 전제다
    - `src/app/page.tsx` — 홈이 같은 값들을 어떤 컴포넌트에 어떻게 넘기는지: `DDayCountdown`에
      `daysUntil(COURSE_START_DATE, today)`, `ProgressSummary`에 `overallProgress(completedIds)`와
      `nextIncompleteLesson(completedIds)?.slug`. import 경로와 호출 형태를 그대로 복제한다
    - `src/components/progress-summary.tsx`, `src/components/dday-countdown.tsx` — props 시그니처
      (이 플랜은 두 컴포넌트를 **수정하지 않는다**, 06-04가 이미 정리했다)
    - `.planning/ui-reviews/06-d97/curriculum-768-with-cookie.png` — 06-02가 남긴 실제 화면.
      무엇이 비어 있는지 눈으로 확인하고 채울 자리를 정한다
    - `.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md` § 컨테이너 폭·셸 계약
  </read_first>
  <action>
`/curriculum` 페이지에 **이미 계산 가능한 값만** 끌어와 첫 화면을 채운다. 새 쿼리를 만들지 않는다 —
이 파일은 이미 `completedIds`를 갖고 있고, 나머지는 홈이 쓰는 것과 같은 순수 계산 함수 호출이다.

넣을 것(우선순위 순, 스크린샷을 보고 필요한 만큼만):
1. **전체 진행률** — 홈과 같은 `ProgressSummary` 컴포넌트를 `overallProgress(completedIds)`와
   `nextIncompleteLesson(completedIds)?.slug ?? null`로 렌더한다. Step별 진행률은 이미 `StepCard`가
   보여주므로, 전체 진행률이 그 위에 얹히는 요약이 된다. `completedIds`가 `null`일 때(쿠키 없음)는
   홈과 똑같이 렌더하지 않는다 — 조건부 구조를 홈에서 그대로 복제한다.
2. **D-day** — 홈과 같은 `DDayCountdown`을 같은 인자로 렌더한다. 개강까지 남은 날이 커리큘럼
   전체 지도 위에 놓이면 "이 분량을 언제까지"라는 맥락이 생긴다.

배치는 `<header>` 아래, Step 카드 그리드 위. 컨테이너 className(`max-w-5xl`, `gap-8`, 공통 패딩)은
그대로 둔다 — 새 섹션은 기존 `flex flex-col gap-8`의 자식으로 들어가면 자동으로 같은 간격을 받는다.

`readCompletedLessonIds()` 실패 분기가 이 페이지엔 아직 없다 — 홈은 `ProgressReadError`를 렌더한다.
같은 처리를 복제해 실패 시 잘못된 0%를 보여주지 않게 한다(홈의 3분기 구조를 그대로 가져온다).

**손대지 않을 것:** `export const dynamic = "force-dynamic"`과 `hasUnlockCookie()` 선호출 순서
(주석에 이유가 있다), `StepCard` 그리드 구조, `stepProgress` 호출. `ProgressSummary`/`DDayCountdown`
컴포넌트 파일 자체.
  </action>
  <verify>
    <automated>npx next build &amp;&amp; node scripts/check-design-tokens.mjs --strict --only src/app/curriculum/page.tsx &amp;&amp; node scripts/check-progress-math.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npx next build` 성공
    - `grep -c 'force-dynamic' src/app/curriculum/page.tsx`가 1 — 렌더링 전략 유지
    - `grep -c 'max-w-5xl' src/app/curriculum/page.tsx`가 1, `grep -c 'gap-8'`가 1 이상 — 셸 계약 유지
    - `git diff src/lib src/content` 결과가 비어 있다 — 새 계산 함수·새 쿼리를 만들지 않았다
    - `git diff src/components` 결과가 비어 있다 — 기존 컴포넌트를 수정하지 않았다
    - 쿠키 있는 상태로 `/curriculum`을 요청하면 응답 HTML에 전체 진행률과 D-day 요소가 있고,
      쿠키 없는 상태로 요청하면 진행률 요소가 없다(홈과 같은 조건부 동작)
    - Playwright 768×1024 스크린샷에서 첫 화면 하단 빈 영역이 06-02가 기록한 이전 값보다 작다
    - `node scripts/check-progress-math.mjs` exit 0, `node scripts/check-brand.mjs` exit 0
    - `node scripts/check-design-tokens.mjs --strict --only src/app/curriculum/page.tsx` exit 0
    - 기존 게이트 10종 전부 exit 0 (D-94)
  </acceptance_criteria>
  <done>/curriculum 첫 화면이 Step 카드 3장 외에 이미 계산되고 있던 진행률·D-day를 함께 보여주고, 새 데이터 경로는 0개다.</done>
</task>

<task type="auto">
  <name>Task 2: 홈 재배치 — D-97 판정에 따른 조건부 실행</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - `.planning/phases/06-site-wide-design-polish/06-D97-MEASUREMENT.md` — **이 태스크를 시작하기 전에
      가장 먼저 읽는다.** "홈 작업함/안 함" 판정 한 줄이 이 태스크의 실행 여부를 결정한다
    - `.planning/ui-reviews/06-d97/home-768-with-cookie.png` — 실제 화면
    - `src/app/page.tsx` — 컨테이너(101줄)와 5개 섹션 배치(107-129줄), `completedIds` 3분기
    - `.planning/phases/06-site-wide-design-polish/06-CONTEXT.md` D-90, D-97
  </read_first>
  <action>
**먼저 판정을 읽는다.** `06-D97-MEASUREMENT.md`의 판정이 **"홈 작업하지 않음"**(쿠키 있는 상태의
첫 화면 빈 영역이 뷰포트 높이의 30% 미만)이면:
- `src/app/page.tsx`를 **수정하지 않는다.**
- 이 태스크는 no-op으로 종료하고, 요약에 "D-97 실측 결과 쿠키 있는 홈의 빈 영역이 N%로 30% 미만 —
  D-90의 홈 범위를 종료한다"를 근거 숫자와 함께 기록한다.
- 이것은 스코프 축소가 아니라 **D-97이 미리 정한 판정 절차의 정상 결과**다. 04-UI-REVIEW의 55%
  측정이 실사용자가 거의 겪지 않는 쿠키 없는 세션에서 나왔다는 것이 근거다.

판정이 **"홈 재배치 진행"**(30% 이상)이면:
- `06-D97-MEASUREMENT.md`가 적어둔 재배치 후보를 그대로 구현한다. 규칙은 `/curriculum`과 동일하다:
  **이미 계산되고 있는 값만** 옮긴다 — 전체·Step별 진행률, D-day, 페이스 상태, 다음 레슨.
  새 데이터·새 쿼리·새 DB 필드·새 기능을 만들지 않는다.
- 배치 변경은 `<main>`의 자식 순서와 그룹화(예: D-day와 오늘 카드를 한 행으로 묶기)까지이며,
  컴포넌트 내부·props 시그니처·계산 함수는 건드리지 않는다.
- `completedIds`가 `null`일 때 진행률·페이스·밀린레슨 3개 섹션이 렌더되지 않는 기존 조건부
  구조를 **없애지 않는다** — 쿠키 없는 방문자에게 빈 상자를 보여주는 것이 재배치의 목적이 아니다.
- 컨테이너 className(`max-w-5xl`, `gap-8`, 공통 패딩)은 유지한다.

어느 쪽이든 `export const dynamic = "force-dynamic"`과 `hasUnlockCookie()` 선호출 순서는 그대로 둔다.
  </action>
  <verify>
    <automated>npx next build &amp;&amp; node scripts/check-design-tokens.mjs &amp;&amp; node --env-file=.env.local scripts/e2e-today.mjs &amp;&amp; node --env-file=.env.local scripts/e2e-progress.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 요약에 D-97 판정 인용과 이 태스크의 실행/no-op 여부가 **숫자 근거와 함께** 기록되어 있다
    - no-op인 경우: `git diff src/app/page.tsx`가 비어 있다
    - 실행한 경우: `git diff src/lib src/content src/components`가 비어 있다(새 계산·새 쿼리·
      컴포넌트 수정 0), `grep -c 'force-dynamic' src/app/page.tsx`가 1,
      `grep -c 'max-w-5xl' src/app/page.tsx`가 1
    - 실행한 경우: 쿠키 없는 상태로 홈을 요청했을 때 진행률·페이스·밀린레슨 요소가 여전히
      렌더되지 않는다(조건부 구조 유지)
    - `npx next build` 성공
    - `node --env-file=.env.local scripts/e2e-today.mjs`와 `... e2e-progress.mjs`가 exit 0 —
      오늘 카드·진행률 DOM 계약 무회귀
    - 기존 게이트 10종 전부 exit 0 (D-94)
  </acceptance_criteria>
  <done>홈의 D-90 범위가 실측 판정에 따라 처리되었거나 명시적 근거와 함께 종료되었다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase 진행률 조회 → 서버 컴포넌트 렌더 | `/curriculum`에 진행률 요약이 추가되면서 이미 조회 중인 값이 화면에 한 군데 더 표시된다. 새 조회 경로는 생기지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-19 | Information Disclosure | `/curriculum`의 진행률 노출 | low | accept | 진행률은 이미 같은 페이지의 `StepCard`가 Step 단위로 보여주고 있고, 쿠키가 없으면 렌더되지 않는 기존 조건부 구조를 그대로 복제한다. 1인 사용 프로젝트이며 새 정보가 드러나지 않는다 |
| T-06-20 | Tampering | 홈/커리큘럼의 조건부 렌더 분기 | medium | mitigate | 재배치가 `completedIds === null` 분기를 뭉개면 쿠키 없는 방문자에게 빈 진행률 상자가 노출된다. acceptance criteria가 쿠키 없는 요청의 응답 HTML을 직접 확인한다 |
| T-06-21 | Denial of Service | 추가 Supabase 조회 | low | accept | 조회가 늘지 않는다 — 이미 호출된 `readCompletedLessonIds()` 결과를 재사용한다. `git diff src/lib`가 비어 있음을 acceptance criteria가 확인한다 |
</threat_model>

<verification>
1. `npx next build` → 성공
2. `node scripts/check-design-tokens.mjs`, `node scripts/check-progress-math.mjs`,
   `node scripts/check-brand.mjs` → exit 0
3. `node --env-file=.env.local scripts/e2e-today.mjs`, `... e2e-progress.mjs` → exit 0
4. `git diff --name-only`가 `src/app/curriculum/page.tsx`와 `src/app/page.tsx` 밖으로 나가지 않는다
5. 쿠키 없음/있음 두 상태로 `/curriculum`과 `/`를 요청해 조건부 동작이 유지된다
</verification>

<success_criteria>
- `/curriculum` 첫 화면의 빈 영역이 06-02가 기록한 값보다 줄었다
- 홈의 D-90 범위가 실측 판정으로 열리거나 근거와 함께 닫혔다
- 새 데이터 경로·새 쿼리·새 기능이 0개다
- 기존 게이트 10종 무회귀
</success_criteria>

<output>
Create `.planning/phases/06-site-wide-design-polish/06-07-SUMMARY.md` when done.
D-97 판정 인용, 홈 태스크의 실행/no-op 여부, 그리고 이 플랜 전체를 타임박스 사유로 드롭한 경우
그 사실과 v2(CONV-01~04) 이관 기록을 남긴다.
</output>
