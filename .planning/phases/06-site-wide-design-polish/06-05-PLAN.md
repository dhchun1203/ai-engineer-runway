---
phase: 06-site-wide-design-polish
plan: 05
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/site-nav.tsx
  - src/components/lesson-nav.tsx
  - src/components/complete-button.tsx
  - src/components/progress-badge.tsx
  - src/components/progress-error.tsx
  - src/components/estimated-time.tsx
  - src/components/depth-badge.tsx
autonomous: true
requirements: [SC1, SC2, SC4]
user_setup: []

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "내비·페이저·배지·버튼 7종에 Tailwind 임의값 대괄호 타이포 문법이 남아 있지 않다 (D-95)"
    - "레슨 페이저 라벨에 리터럴 방향 화살표 문자가 없고 Lucide chevron 아이콘만 방향을 표시한다 — 이중 글리프가 사라진다 (D-R4K-7)"
    - "375px에서 로고와 4개 링크와 테마 토글이 한 줄에 안 들어가면 줄바꿈으로 흡수한다 — 가로 스크롤이나 햄버거 메뉴를 도입하지 않는다 (06-UI-SPEC Nav Shell Contract)"
    - "375px에서 이전과 다음 두 페이저 링크가 세로로 쌓여도 가로 오버플로가 없다 — 06-08의 D-91 게이트가 레슨 라우트에서 검증한다"
    - "소요시간과 브레드크럼이 ui-monospace 계열(Tailwind font-mono)로 렌더되어 본문 Pretendard와 계기 mono의 페어링이 성립한다 — 새 폰트 파일 0개 (D-R4K-3)"
    - "깊이 배지의 시각 처리만 정리되고 어떤 레슨이 어느 depth인지는 건드리지 않는다 — 프론트매터 재분류는 이 Phase 범위 밖이다 (D-92)"
    - "내비의 활성 상태 표시(accent 밑줄 + accent 글자색)와 터치 타깃 min-h-11이 그대로 유지된다"
    - "완료 버튼의 accent 배경 CTA 타이포는 body 역할 + font-semibold(16px/600)로 06-04의 다른 두 CTA와 같은 값을 쓴다"
    - "임의값 치환은 멱등이다 — 이미 시맨틱 클래스로 바뀐 곳을 다시 치환해도 변화가 없고, 역방향 치환이 발생하지 않는다"
    - statement: "긴 레슨 제목이 페이저 라벨에서 줄바꿈되는지 시각 확인한다 — 리터럴 화살표 제거 후 chevron과 제목 조합의 줄바꿈 지점이 바뀔 수 있다"
      verification: backstop
    - "기존 자동 게이트 10종이 전부 통과한다 (D-94)"
  artifacts:
    - src/components/site-nav.tsx
    - src/components/lesson-nav.tsx
    - src/components/complete-button.tsx
    - src/components/progress-badge.tsx
    - src/components/progress-error.tsx
    - src/components/estimated-time.tsx
    - src/components/depth-badge.tsx
  key_links:
    - "lesson-nav.tsx의 `data-pager` 속성 → globals.css의 완료 후 다음 레슨 강조 선택자 — 라벨 문자열만 바꾸고 이 DOM 계약을 건드리면 완료 연출이 조용히 죽는다"
    - "depth-badge.tsx의 Step 색 리터럴 맵 → Tailwind JIT 스캔 — 동적 문자열 조합으로 바꾸면 클래스가 생성되지 않아 배지 색이 통째로 사라진다"
  prohibitions:
    - statement: "새 색 토큰을 도입하지 않는다 (D-R4K-2)"
      status: active
      verification: flagged
    - statement: "새 폰트 파일을 추가하지 않는다 — mono 승격은 이미 스택에 있는 ui-monospace를 쓴다 (D-R4K-3)"
      status: active
      verification: flagged
    - statement: "depth 프론트매터를 재분류하지 않는다 — 배지 컴포넌트의 시각 처리만 이 Phase 범위다 (D-92)"
      status: active
      verification: flagged
    - statement: "내비에 햄버거 메뉴나 가로 스크롤을 도입하지 않는다 — flex-wrap 줄바꿈으로 흡수한다"
      status: active
      verification: flagged
    - statement: "375px 대응을 이유로 truncate나 고정폭 클래스를 새로 도입하지 않는다"
      status: active
      verification: flagged
    - statement: "lesson-nav.tsx의 chevron 아이콘 JSX 구조와 data-pager 속성을 제거하지 않는다 — 라벨 문자열만 바꾼다"
      status: active
      verification: flagged
    - statement: "새 라우트를 만들지 않고 DB 스키마를 바꾸지 않으며 렌더링 전략을 바꾸지 않는다"
      status: active
      verification: flagged
    - statement: "사용자에게 보이는 어떤 문자열에도 교육기관명(K로 시작하는 4글자 브랜드)을 쓰지 않는다"
      status: active
      verification: flagged
---

<objective>
셸의 나머지 층 — 전역 내비, 레슨 페이저, 배지·버튼·상태 표시 — 7개 컴포넌트의 임의값 타이포
13곳을 치환하고, 그 김에 같은 파일 안에 있는 확정 결정 두 건을 처리한다:
페이저의 이중 방향 글리프 제거(D-R4K-7)와 계기 요소의 mono 승격(D-R4K-3).

페이저는 지금 Lucide chevron **과** 리터럴 화살표 문자를 둘 다 렌더해 `‹ ← 이전 레슨`으로 읽힌다.
라벨 문자열에서 리터럴만 지우고 chevron은 남긴다.

Purpose: 성공 기준 1·2를 셸 컴포넌트 층에서 완성한다.
Output: 7개 컴포넌트 정리.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-site-wide-design-polish/06-CONTEXT.md
@.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md
@.planning/phases/06-site-wide-design-polish/06-PATTERNS.md
@.planning/phases/06-site-wide-design-polish/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 전역 내비 + 레슨 페이저 — 타이포 치환 + 이중 글리프 제거 + mono 승격 (6곳)</name>
  <files>src/components/site-nav.tsx, src/components/lesson-nav.tsx</files>
  <read_first>
    - `src/components/site-nav.tsx` 전체 — 컨테이너(37줄, `flex-wrap gap-y-2`가 375px 줄바꿈을
      담당한다 — 구조 불변), `NavBadge`(24줄), 로고(41줄, 옛 20px 조합), 내비 항목(52·64줄),
      활성 상태 로직(64-68줄, `border-b-2 border-accent text-accent` — 변경 없음),
      `href: null` 비활성 분기(49-56줄)
    - `src/components/lesson-nav.tsx` 전체 — `PagerButton`의 `label` 상수(35줄, 리터럴 화살표가
      들어 있는 정확한 지점), chevron JSX 분기(38-49줄, **유지**), `data-pager` 속성,
      타이포 2곳(37줄 포함), `LessonBreadcrumb` 구조
    - `.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md` § Nav Shell Contract +
      § Copywriting Contract(페이저 라벨 확정 문구) + § Focus-visible 계약(내비·페이저는 변경 없음)
    - `src/app/globals.css` — `[data-progress-controls]:has(...) [data-pager="next"]` 선택자
      (파일 끝) — `data-pager` 속성이 CSS 계약이라는 증거
    - `.planning/phases/06-site-wide-design-polish/06-01-SUMMARY.md` — 확정된 유틸리티 클래스 이름
  </read_first>
  <action>
**`lesson-nav.tsx` — 이중 글리프 제거 (D-R4K-7).** `PagerButton`의 `label` 상수가 만드는 두 문자열에서
리터럴 방향 문자만 지워 06-UI-SPEC.md § Copywriting Contract가 확정한 값("이전 레슨" / "다음 레슨")으로
만든다. `ChevronLeft`/`ChevronRight` JSX와 `aria-hidden` 속성, `data-pager` 속성, 배치 순서
(이전은 아이콘이 앞, 다음은 아이콘이 뒤)는 **하나도 바꾸지 않는다** — `data-pager`는 완료 후
다음 레슨 강조 CSS 선택자가 의존하는 DOM 계약이다.

**`lesson-nav.tsx` — mono 승격 (D-R4K-3).** `LessonBreadcrumb`의 경로 텍스트에 Tailwind `font-mono`
유틸리티를 더해 "계기판 목소리"로 만든다. 새 폰트 파일을 추가하지 않는다 — `font-mono`는 이미
스택에 있는 `ui-monospace`로 해석된다. 페이저 라벨은 본문 목소리이므로 mono를 적용하지 않는다.

**타이포 치환.** 두 파일의 임의값 6곳을 06-UI-SPEC.md § Typography 대응표대로 치환한다:
`site-nav.tsx`의 로고는 옛 20px/600 조합이므로 **heading 역할 + `font-bold`**(22px/700),
`NavBadge`와 내비 항목 3곳은 label 역할 + `font-semibold`. `lesson-nav.tsx`의 페이저 라벨은
body 역할 + `font-normal`, 브레드크럼은 label 역할. `leading-[...]` 짝은 함께 지운다.

**손대지 않을 것:** `site-nav.tsx`의 컨테이너 className(`flex-wrap`과 `gap-y-2`가 375px에서
줄바꿈을 만드는 유일한 장치다 — 이걸 건드리면 D-91 게이트가 깨진다), `max-w-5xl` 폭,
`min-h-11` 터치 타깃, 활성 상태 로직, `href: null` 비활성 분기(현재 4개 라우트가 다 존재해
도달하지 않을 수 있으나 안전망으로 유지). 햄버거 메뉴나 가로 스크롤을 도입하지 않는다.
  </action>
  <verify>
    <automated>node scripts/check-design-tokens.mjs --strict --only src/components/site-nav.tsx src/components/lesson-nav.tsx &amp;&amp; npx next build &amp;&amp; node scripts/check-manifest.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 위 `--strict --only` 실행이 exit 0
    - `grep -c 'ChevronLeft\|ChevronRight' src/components/lesson-nav.tsx`가 2 이상 — chevron이 남아 있다
    - `grep -c 'data-pager' src/components/lesson-nav.tsx`가 1 이상 — CSS 계약이 살아 있다
    - `grep -c 'font-mono' src/components/lesson-nav.tsx`가 1 이상 — 브레드크럼 mono 승격
    - `grep -c 'flex-wrap' src/components/site-nav.tsx`가 1 이상, `grep -c 'min-h-11' src/components/site-nav.tsx`가 1 이상
    - `grep -c 'text-heading' src/components/site-nav.tsx`가 1 이상 — 로고가 heading 역할을 쓴다
    - dev 서버를 띄우고 레슨 페이지 HTML을 받았을 때 페이저 링크 텍스트가 06-UI-SPEC.md
      § Copywriting Contract의 확정 문구와 정확히 일치한다(리터럴 방향 문자 없음).
      `node --env-file=.env.local scripts/e2e-today.mjs`가 서버를 띄우므로 그 과정에서 확인 가능하며,
      06-08의 최종 스위트가 이 문구를 다시 검증한다
    - `npx next build` 성공, `node scripts/check-manifest.mjs` exit 0(인접 레슨 계산 회귀 없음)
    - 기존 게이트 10종 전부 exit 0 (D-94)
  </acceptance_criteria>
  <done>내비와 페이저에 임의값 타이포가 0건이고, 페이저가 방향을 한 번만 표시한다.</done>
</task>

<task type="auto">
  <name>Task 2: 배지·버튼·상태 표시 5종 타이포 치환 + mono 승격 (7곳)</name>
  <files>src/components/complete-button.tsx, src/components/progress-badge.tsx, src/components/progress-error.tsx, src/components/estimated-time.tsx, src/components/depth-badge.tsx</files>
  <read_first>
    - `src/components/complete-button.tsx` — 버튼 className(54줄, 옛 16px/600 CTA 조합),
      `data-complete-state` 속성과 `complete-check-icon`/`complete-ring-glow` 클래스
      (globals.css의 완료 연출이 의존하는 DOM 계약 — 유지)
    - `src/components/depth-badge.tsx` — Step 색 리터럴 맵(7-9줄, Tailwind JIT이 템플릿 문자열
      조합을 스캔하지 못하는 이유가 주석에 있다), 배지 className 1곳
    - `src/components/estimated-time.tsx` — 1곳, 소요시간 표시
    - `src/components/progress-badge.tsx` — 2곳. React SSR이 인접 표현식 사이에 삽입하는 주석
      마커 때문에 `e2e-today.mjs`가 `stripSsrComments`를 쓰는 파일이므로 DOM 구조를 바꾸지 않는다
    - `src/components/progress-error.tsx` — 1곳
    - `.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md` § Typography 대응표
    - `.planning/phases/06-site-wide-design-polish/06-01-SUMMARY.md` — 확정된 유틸리티 클래스 이름
  </read_first>
  <action>
**타이포 치환(7곳).** 06-UI-SPEC.md § Typography 대응표대로 치환한다.
`complete-button.tsx`의 옛 16px/600 CTA 조합 → **body 역할 + `font-semibold`**(16px/600) —
이 값은 06-04가 not-found·unlock/done·progress-summary의 CTA에 적용하는 값과 **정확히 같아야
한다**(세 CTA와 완료 버튼이 서로 다른 크기로 갈리면 성공 기준 2가 깨진다). 나머지 배지·상태 문구는
label 역할 + 기존 굵기 유틸리티, 14px/16px 값 자체는 바뀌지 않는다. `leading-[...]` 짝은 함께 지운다.

**mono 승격 (D-R4K-3).** `estimated-time.tsx`의 소요시간 숫자에 Tailwind `font-mono` 유틸리티를
더한다 — 소요시간은 D-R4K-3이 명시한 "계기판 목소리" 대상이다. 새 폰트 파일을 추가하지 않는다.

**깊이 배지 (D-92).** `depth-badge.tsx`는 **시각 처리만** 정리한다: 타이포 치환 + 필요하면 배지
padding/radius를 같은 배지 원형(`progress-badge.tsx`)과 맞추는 정도. Step 색 리터럴
`Record<StepId, string>` 맵을 동적 클래스 조합으로 바꾸지 않는다(Tailwind JIT이 스캔하지 못해
색이 통째로 사라진다). **어떤 레슨이 어느 depth인지는 건드리지 않는다** — 프론트매터 재분류는
콘텐츠 작업으로 이관되었다.

**손대지 않을 것:** `complete-button.tsx`의 `data-complete-state` 속성, `complete-check-icon`·
`complete-ring-glow` 클래스, Server Action 호출 로직. `progress-badge.tsx`의 DOM 구조
(`e2e-today.mjs`가 SSR 주석 마커를 제거하고 문자열 매칭하는 대상이다). 각 컴포넌트의 props
시그니처와 조건부 렌더 분기.
  </action>
  <verify>
    <automated>node scripts/check-design-tokens.mjs --strict --only src/components/complete-button.tsx src/components/progress-badge.tsx src/components/progress-error.tsx src/components/estimated-time.tsx src/components/depth-badge.tsx &amp;&amp; npx next build &amp;&amp; node scripts/check-progress-gates.mjs &amp;&amp; node --env-file=.env.local scripts/e2e-progress.mjs &amp;&amp; node --env-file=.env.local scripts/e2e-today.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 위 `--strict --only` 실행이 exit 0
    - `grep -c 'data-complete-state' src/components/complete-button.tsx`가 1 이상
    - `grep -c 'font-mono' src/components/estimated-time.tsx`가 1 이상
    - `grep -c 'Record<StepId' src/components/depth-badge.tsx`가 1 이상 — Step 색 리터럴 맵 유지
    - `node scripts/check-progress-gates.mjs`가 exit 0
    - `node --env-file=.env.local scripts/e2e-progress.mjs`와 `... e2e-today.mjs`가 exit 0 —
      완료 버튼·진행률 배지의 DOM 계약이 살아 있다
    - `git diff` 결과 이 5개 파일에서 props 타입·함수 시그니처·조건부 분기·`data-*` 속성이
      변경되지 않았다(변경 라인이 className 문자열 안이다)
    - `npx next build` 성공
    - 기존 게이트 10종 전부 exit 0 (D-94)
  </acceptance_criteria>
  <done>배지·버튼·상태 표시 5종에 임의값 타이포가 0건이고, 완료 버튼이 다른 CTA와 같은 타이포 값을 쓴다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 (신규) | className과 라벨 문자열 편집만이다. `complete-button.tsx`가 호출하는 Server Action의 권한 검증 로직에는 손대지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-11 | Tampering | `complete-button.tsx`의 `data-complete-state` / Server Action 호출부 | high | mitigate | 이 컴포넌트는 진행률 쓰기 경로의 진입점이다. className만 바꾸고 호출 로직·속성을 건드리지 않는다는 것을 `git diff` 검사와 `check-progress-gates.mjs`·`e2e-progress.mjs` 통과로 이중 확인한다 (D-94) |
| T-06-12 | Tampering | `lesson-nav.tsx`의 `data-pager` DOM 계약 | medium | mitigate | 라벨 문자열만 편집하고 속성·JSX 구조를 유지한다. acceptance criteria가 `data-pager` 존재를 직접 확인한다 |
| T-06-13 | Spoofing | 내비 활성 상태 표시 | low | accept | 활성 상태는 시각 힌트일 뿐 권한 경계가 아니다. 로직을 건드리지 않는다 |
</threat_model>

<verification>
1. `node scripts/check-design-tokens.mjs --strict --only <이 플랜이 소유한 7개 파일>` → exit 0
2. `npx next build` → 성공
3. `node scripts/check-progress-gates.mjs`, `node scripts/check-manifest.mjs` → exit 0
4. `node --env-file=.env.local scripts/e2e-progress.mjs`, `... e2e-today.mjs` → exit 0
5. `git diff --name-only`가 이 플랜의 `files_modified` 7개 밖으로 나가지 않는다
</verification>

<success_criteria>
- 내비·페이저·배지·버튼 7종에 임의값 대괄호 타이포가 0건이다
- 페이저가 방향 표시를 chevron 하나로만 한다
- 소요시간과 브레드크럼이 mono로 렌더된다 (새 폰트 파일 0개)
- 완료 버튼 CTA 타이포가 다른 세 CTA와 같은 값이다
- 기존 게이트 10종 무회귀
</success_criteria>

<output>
Create `.planning/phases/06-site-wide-design-polish/06-05-SUMMARY.md` when done.
`complete-button.tsx`에 적용한 CTA 타이포 값을 요약에 명시한다 — 같은 웨이브의 06-04가 적용한
값과 일치해야 하며, 어긋나면 06-08의 시각 검수에서 잡아야 한다.
</output>
