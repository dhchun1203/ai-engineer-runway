---
phase: 01-deployed-curriculum-skeleton
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/content/modules.ts
  - src/content/curriculum-helpers.ts
  - src/content/lessons/step-1/1-1-course-orientation.mdx
  - src/content/lessons/step-1/1-1-dev-environment-setup.mdx
  - src/content/lessons/step-1/1-2-git-branch-and-pr.mdx
  - src/content/lessons/step-1/1-2-generative-ai-basics.mdx
  - src/content/lessons/step-1/1-3-python-functions-and-io.mdx
  - src/content/lessons/step-1/1-4-relational-db-basics.mdx
  - src/content/lessons/step-1/1-4-sql-queries-and-joins.mdx
  - src/content/lessons/step-1/1-5-ml-model-types.mdx
  - src/content/lessons/step-1/1-5-ml-metrics-and-pipeline.mdx
  - src/content/lessons/step-2/2-1-postgres-and-supabase.mdx
  - src/content/lessons/step-2/2-1-ai-data-modeling.mdx
  - src/content/lessons/step-2/2-2-html-css-js.mdx
  - src/content/lessons/step-2/2-2-browser-and-ui.mdx
  - src/content/lessons/step-2/2-3-typescript-setup.mdx
  - src/content/lessons/step-2/2-3-react-components.mdx
  - src/content/lessons/step-2/2-4-project-ai-shop-frontend.mdx
  - src/content/lessons/step-2/2-5-express-rest-api.mdx
  - src/content/lessons/step-2/2-5-auth-and-prisma.mdx
  - src/content/lessons/step-2/2-6-project-ai-shop-backend.mdx
  - src/content/lessons/step-2/2-7-prompt-patterns.mdx
  - src/content/lessons/step-2/2-7-promptops.mdx
  - src/content/lessons/step-3/3-1-vector-search-basics.mdx
  - src/content/lessons/step-3/3-1-hybrid-search-reranking.mdx
  - src/content/lessons/step-3/3-2-project-rag-agent.mdx
  - src/content/lessons/step-3/3-3-peft-lora-qlora.mdx
  - src/content/lessons/step-3/3-3-tuning-evaluation.mdx
  - src/content/lessons/step-3/3-4-multi-agent-structure.mdx
  - src/content/lessons/step-3/3-4-webhook-schedule-hitl.mdx
  - src/content/lessons/step-3/3-4-n8n-langgraph.mdx
  - src/content/lessons/step-3/3-5-project-orchestration.mdx
  - src/content/lessons/step-3/3-6-prompt-versioning-eval.mdx
  - src/content/lessons/step-3/3-6-monitoring-governance.mdx
  - src/content/lessons/step-3/3-6-structured-output-canary.mdx
  - src/content/lessons/step-3/3-7-project-ax-launch.mdx
  - scripts/check-manifest.mjs
autonomous: true
requirements: [CONT-01, CONT-04]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "빌드 후 매니페스트가 정확히 3개 Step, 19개 모듈, 35개 레슨을 공급한다 (CONT-01)"
    - "35개 레슨 전부에 깊이 배지 값(심화 또는 개요)과 예상 소요시간이 확정되어 있다 — 본문이 없는 레슨도 예외가 없다 (CONT-04, D-13)"
    - "Step 3의 모든 레슨은 개요이고 Step 1·2의 모든 레슨은 심화다 (CONT-04, curriculum.md 깊이 방침)"
    - "35개 레슨 예상 소요시간 총합이 7,200~10,800분(120~180시간) 범위에 들어온다 — 8/25~9/29 36일 × 하루 4~6시간이라는 Phase 3 일정 배분의 입력 제약 (D-13)"
    - "레슨의 (moduleId, order) 쌍이 전역 유일해 전체 정렬 순서가 결정적이고 안정적이다 — 동률로 인한 순서 흔들림이 없다"
    - "레슨의 moduleId 집합이 modules.ts의 19개 모듈 id 집합과 정확히 일치한다 — 어느 쪽에도 고아 항목이 없다"
    - "Step 카드·모듈 아코디언·깊이 배지가 소비하는 데이터는 빌드 타임 매니페스트가 고정 공급하므로 런타임 빈 상태가 발생할 수 없다 — Velite Zod 스키마가 누락 필드를 빌드 실패로 차단한다"
    - "부분 데이터(일부 필드만 채워진 레슨)는 Velite 스키마 검증이 빌드 시점에 차단하므로 런타임 부분 상태가 존재하지 않는다"
    - "본문이 있는 레슨(hasContent: true)은 이 시점에 파일럿 1개뿐이며, 나머지 34개는 실 라우트를 가진 메타데이터 행으로 존재한다 — 404가 아니다 (RESEARCH Pitfall 2)"
  artifacts:
    - path: "src/content/modules.ts"
      provides: "3 Step 메타 + 19 모듈 정의 (id, stepId, title, order, isProject)"
      exports: ["steps", "modules"]
      min_lines: 60
    - path: "src/content/curriculum-helpers.ts"
      provides: "매니페스트 조회·전역 정렬·인접 레슨 계산 (Plan 04의 브레드크럼/이전-다음 입력)"
      exports: ["getStep", "getModulesByStep", "getLessonsByModule", "getOrderedLessons", "getLessonBySlug", "getAdjacentLessons", "getLessonCounts"]
    - path: "scripts/check-manifest.mjs"
      provides: "매니페스트 불변식 11종 자동 검증 게이트 (의존성 0)"
      min_lines: 40
  key_links:
    - from: "src/content/curriculum-helpers.ts"
      to: ".velite/lessons.json"
      via: "#site/content에서 lessons import 후 modules와 조인"
      pattern: "#site/content"
    - from: "src/content/curriculum-helpers.ts"
      to: "src/content/modules.ts"
      via: "moduleId → module → stepId 조회 및 전역 정렬 키 구성"
      pattern: "modules"
    - from: "scripts/check-manifest.mjs"
      to: ".velite/lessons.json + src/content/modules.ts"
      via: "빌드 산출물과 모듈 정의를 교차 대조해 불변식 검증"
      pattern: "lessons\\.json"
---

<objective>
커리큘럼 원문(3 Step / 19 모듈)을 사이트가 소비할 수 있는 35행 매니페스트로 확정한다 — 본문 없이도 전 레슨의 깊이 배지와 예상 소요시간이 결정된다.

Purpose: Plan 01의 트레이서가 파이프라인을 증명했으니, 이제 그 파이프라인에 실제 데이터를 채운다(트레이서 다음 순서인 "real data layer" 단계). CONT-01의 "전체 레슨 목록 탐색"은 35행이 다 있어야 성립하고, D-13의 예상 소요시간은 Phase 3 일정 배분의 입력값이라 콘텐츠 집필보다 먼저 확정되어야 한다.

Output: `modules.ts`(19 모듈), 34개 레슨 MDX 메타데이터 행(파일럿 1은 Plan 01이 이미 작성), 조회·정렬·인접 계산 헬퍼, 그리고 매니페스트 불변식을 기계 검증하는 의존성 0 게이트.
</objective>

## Phase Goal

**ROADMAP `**Goal:**` 원문 (verbatim):** 학습자가 배포된 URL에 아이패드로 접속해 커리큘럼 3단 구조를 탐색하고 실제 레슨을 읽을 수 있다

> ⚠ MVP_MODE 주의: Goal 문장에 정규 유저 스토리의 **so that** 절이 없다. 플래너는 스토리를 창작하지 않는다 — 정규 형식이 필요하면 `/gsd mvp-phase 1`을 먼저 실행할 것.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/curriculum.md
@.planning/phases/01-deployed-curriculum-skeleton/01-CONTEXT.md
@.planning/phases/01-deployed-curriculum-skeleton/01-RESEARCH.md
@.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md
@.planning/phases/01-deployed-curriculum-skeleton/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Plan 01이 확정한 Velite lessons 스키마 — 이 Plan의 34개 파일이 그대로 따른다. -->

velite.config.ts lessons 스키마 필드:
  title: string
  stepId: number (1..3)
  moduleId: string        // "1-3" 형태
  order: number           // 모듈 내 순서
  depth: '심화' | '개요'
  estimatedMinutes: number (>= 1)
  slug: string            // s.slug('lessons') — 파일명에서 파생
  hasContent: boolean     // 기본 true
  code: mdx

import alias:
  "#site/content" -> ".velite"   (lessons 배열이 여기서 나온다)

<!-- 이 Plan이 생성하는 계약 — Plan 04가 여기에 맞춰 라우트를 구현한다. -->

src/content/modules.ts:
  type StepId = 1 | 2 | 3
  interface Step { id: StepId; title: string; shortTitle: string; keywords: string[]; goal: string; courseHours: number }
  interface Module { id: string; stepId: StepId; title: string; order: number; isProject: boolean }
  export const steps: readonly Step[]      // 길이 3
  export const modules: readonly Module[]  // 길이 19

src/content/curriculum-helpers.ts:
  export function getStep(stepId: StepId): Step | undefined
  export function getModulesByStep(stepId: StepId): Module[]              // module.order 오름차순
  export function getLessonsByModule(moduleId: string): Lesson[]          // lesson.order 오름차순
  export function getOrderedLessons(): Lesson[]                           // 길이 35, stepId → module.order → lesson.order
  export function getLessonBySlug(slug: string): Lesson | undefined
  export function getAdjacentLessons(slug: string): { prev: Lesson | null; next: Lesson | null }
  export function getLessonCounts(stepId?: StepId): { total: number; withContent: number }
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 19개 모듈 정의와 커리큘럼 조회·정렬 헬퍼</name>
  <read_first>
    - `.planning/curriculum.md` — 3 Step의 제목·키워드·목표·시수와 19개 모듈 제목의 원문 (source of truth)
    - `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` — Step identity colors 표의 Step 축약 라벨("개발 기반 구축" / "풀스택·LLM 심화" / "RAG·오케스트레이션 개요")
    - `.planning/phases/01-deployed-curriculum-skeleton/01-RESEARCH.md` — Curriculum Manifest Derivation(35행 파생 근거), Recommended Project Structure
    - `velite.config.ts` — Plan 01이 확정한 lessons 스키마 필드와 타입
    - `tsconfig.json` — `#site/content` 별칭 확인
  </read_first>
  <files>src/content/modules.ts, src/content/curriculum-helpers.ts</files>
  <action>
커리큘럼 원문 구조를 타입이 붙은 정적 데이터와 조회 함수로 옮긴다. 값은 아래 "## Curriculum Manifest — 35행 확정 표" 섹션이 전부 정해 두었으니 그 표를 그대로 옮긴다.

**(1) `src/content/modules.ts`.** `<interfaces>` 블록의 `Step`/`Module` 타입과 `steps`/`modules` export를 정확히 그 이름으로 만든다. `steps`는 3개 항목이며 각 항목의 `title`·`keywords`·`goal`·`courseHours`는 `.planning/curriculum.md`의 Step 헤더와 그 아래 "키워드:"·"목표:" 줄에서 그대로 가져온다(Step 1은 200시간, Step 2는 336시간, Step 3은 520시간). `shortTitle`은 UI-SPEC의 Step identity colors 표에 적힌 축약 라벨을 쓴다. `modules`는 19개 항목이며 `id`는 `"1-1"`~`"3-7"` 형태, `title`은 curriculum.md의 `### N-M.` 뒤 제목 원문, `order`는 Step 내 1부터의 일련번호, `isProject`는 curriculum.md에서 `[Project N]` 태그가 붙은 5개 모듈(`2-4`, `2-6`, `3-2`, `3-5`, `3-7`)만 true다. Step별 모듈 수는 5 / 7 / 7이다.

**(2) 소거 가능한 TypeScript만 사용.** `modules.ts`에는 `enum`, 네임스페이스, 생성자 파라미터 프로퍼티를 쓰지 않는다 — interface, type alias, `as const`, 일반 export만 쓴다. Task 2의 검증 스크립트가 이 파일을 정규식으로 읽어 모듈 id를 추출하므로 `id: '1-1'` 형태의 단순 리터럴을 유지한다.

**(3) `src/content/curriculum-helpers.ts`.** `#site/content`에서 `lessons`를, `./modules`에서 `steps`/`modules`를 import해 `<interfaces>` 블록에 나열된 7개 함수를 그 시그니처 그대로 export한다. 핵심 규칙:
  - `getOrderedLessons()`의 전역 정렬 키는 (레슨의 stepId, 그 레슨이 속한 모듈의 order, 레슨의 order) 3단이다. 세 값 조합이 유일하므로 동률 비교가 발생하지 않고 정렬 결과가 결정적이다 — 이 성질이 이전/다음 버튼 순서의 근거다.
  - `getAdjacentLessons(slug)`는 전역 정렬 배열에서 인덱스를 찾아 앞뒤를 반환하고, 첫 레슨의 `prev`와 마지막 레슨의 `next`는 `null`을 반환한다. 존재하지 않는 slug면 `{ prev: null, next: null }`을 반환한다.
  - `getLessonCounts(stepId?)`는 인자가 없으면 전체 35개 기준, 있으면 해당 Step 기준으로 `{ total, withContent }`를 반환한다. Plan 04의 Step 카드가 이 값을 쓴다.
  - 모듈을 찾지 못한 레슨이 있으면 조용히 건너뛰지 말고 예외를 던진다 — 고아 레슨은 빌드가 실패해야 하는 상태다.
  </action>
  <verify>
    <automated>npm run build && test "$(grep -cE "id: *'[0-9]-[0-9]'" src/content/modules.ts)" = "19" && node -e "const s=require('fs').readFileSync('src/content/curriculum-helpers.ts','utf8'); for (const f of ['getStep','getModulesByStep','getLessonsByModule','getOrderedLessons','getLessonBySlug','getAdjacentLessons','getLessonCounts']) { if (!new RegExp('export (function|const) '+f+'\\\\b').test(s)) throw new Error('missing export: '+f); }" && test "$(grep -cE '\benum\b' src/content/modules.ts)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/modules.ts`에 `id: '<숫자>-<숫자>'` 형태의 모듈 리터럴이 정확히 19개 있다
    - `steps` 배열의 길이가 3이고 각 항목의 `courseHours`가 각각 200 / 336 / 520이다
    - `isProject: true`인 모듈이 정확히 5개이며 그 id가 `2-4`, `2-6`, `3-2`, `3-5`, `3-7`이다
    - Step별 모듈 개수가 5(Step 1) / 7(Step 2) / 7(Step 3)이다
    - `src/content/modules.ts`에 `enum` 키워드가 0건이다
    - `src/content/curriculum-helpers.ts`가 `getStep`, `getModulesByStep`, `getLessonsByModule`, `getOrderedLessons`, `getLessonBySlug`, `getAdjacentLessons`, `getLessonCounts` 7개를 모두 export한다
    - `npm run build`가 타입 오류 없이 0으로 종료한다
  </acceptance_criteria>
  <done>19개 모듈이 타입 붙은 정적 데이터로 존재하고, 레슨을 Step/모듈로 조인하고 전역 순서와 인접 레슨을 계산하는 헬퍼가 빌드를 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: 34개 레슨 매니페스트 행 생성과 불변식 검증 게이트</name>
  <read_first>
    - 이 PLAN의 `## Curriculum Manifest — 35행 확정 표` 섹션 — 35행 전부의 slug·제목·모듈·순서·깊이·예상시간이 확정되어 있다
    - `src/content/lessons/step-1/1-3-python-variables-and-types.mdx` — Plan 01이 만든 파일럿 1의 frontmatter 형태(그대로 따를 것)
    - `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` — Copywriting Contract의 "Empty state heading"·"Empty state body" 원문
    - `.planning/phases/01-deployed-curriculum-skeleton/01-RESEARCH.md` — Pitfall 2(35행이 아니면 CONT-01이 깨진다), Open Questions 2(예상 소요시간 파생 근거)
    - `.planning/PROJECT.md` — Constraints의 금지 브랜드 HARD RULE
    - `velite.config.ts` — 스키마 필드와 기본값
  </read_first>
  <files>src/content/lessons/step-1/*.mdx (9 new), src/content/lessons/step-2/*.mdx (12 new), src/content/lessons/step-3/*.mdx (13 new), scripts/check-manifest.mjs</files>
  <action>
아래 "## Curriculum Manifest — 35행 확정 표"의 34개 행(파일럿 1 제외)을 MDX 파일로 만들고, 매니페스트 전체가 지켜야 할 불변식을 자동 검증 스크립트로 고정한다.

**(1) 34개 레슨 파일 생성.** 파일 경로는 `src/content/lessons/step-{stepId}/{slug}.mdx`다. 34개는 형태가 동일하므로 표를 데이터로 삼아 스크립트로 일괄 생성한 뒤 결과를 검토한다 — 파일마다 손으로 다른 판단을 내리는 작업이 아니다. 각 파일의 frontmatter는 표의 `title`, `stepId`, `moduleId`, `order`, `depth`, `estimatedMinutes`를 그대로 담고 `hasContent: false`를 명시한다. 단, 파일럿 2에 해당하는 `2-3-react-components.mdx`도 이번에는 다른 33개와 동일하게 `hasContent: false` 메타데이터 행으로 만든다 — Plan 06이 이 파일의 본문을 D-10 6단 구성 실콘텐츠로 채우고 그때 `hasContent: true`로 올린다.

**(2) 본문 처리.** Velite의 `s.mdx()`는 빈 본문도 컴파일하지만, 각 파일의 본문에는 UI-SPEC Copywriting Contract의 "Empty state body" 문장을 한 줄 넣어 렌더 결과가 비어 보이지 않게 한다. 표시용 제목·안내 문구를 별도로 지어내지 않는다 — 카피는 UI-SPEC이 소유한다. 34개 파일 어디에도 교육기관 이름을 쓰지 않는다: 과정은 항상 "AI Engineer 교육과정"이다(D-02, PROJECT.md HARD RULE).

**(3) `scripts/check-manifest.mjs`.** 외부 의존성 없이 Node 표준 모듈만 써서 다음 11개 불변식을 검사하고, 하나라도 깨지면 비어 있지 않은 오류 메시지와 함께 비정상 종료한다. 입력은 `.velite/lessons.json`(빌드 산출물)과 `src/content/modules.ts`(정규식으로 `id: '<숫자>-<숫자>'` 리터럴을 추출)다:
  1. 레슨 수가 정확히 35다
  2. `modules.ts`에서 추출한 모듈 id가 정확히 19개이고 모두 유일하다
  3. 모든 레슨의 `depth`가 심화 또는 개요다
  4. `stepId`가 3인 레슨은 전부 개요, 1 또는 2인 레슨은 전부 심화다
  5. 모든 레슨의 `estimatedMinutes`가 1 이상의 정수다
  6. `estimatedMinutes` 총합이 7200 이상 10800 이하다
  7. 레슨의 `moduleId` 집합이 모듈 id 집합과 정확히 같다(양방향 차집합이 공집합)
  8. `moduleId`와 `order`를 이어 붙인 키가 전역 유일하다
  9. `slug`가 전역 유일하다
  10. `hasContent`가 true인 레슨이 정확히 1개이고 그 slug가 `1-3-python-variables-and-types`다 (Plan 06이 파일럿 2를 채우면 이 기대값을 2로 올린다 — 스크립트 상단에 기대값 상수로 노출해 둘 것)
  11. (stepId, 모듈 order, 레슨 order) 3단 정렬 결과의 첫 항목 slug가 `1-1-course-orientation`이고 마지막 항목 slug가 `3-7-project-ax-launch`다
  검사 대상 파일이 없으면 기본값으로 넘어가지 말고 즉시 오류로 종료한다 — 파일이 없어서 0건이 된 상태가 통과로 둔갑하면 게이트가 아무것도 측정하지 않게 된다.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-manifest.mjs && node -e "const l=JSON.parse(require('fs').readFileSync('.velite/lessons.json','utf8')); if(l.length!==35) throw new Error('expected 35 lessons, got '+l.length); const sum=l.reduce((a,x)=>a+x.estimatedMinutes,0); if(sum<7200||sum>10800) throw new Error('estimatedMinutes total out of band: '+sum); if(l.filter(x=>x.stepId===3&&x.depth!=='개요').length) throw new Error('step3 depth wrong'); if(l.filter(x=>x.stepId!==3&&x.depth!=='심화').length) throw new Error('step1/2 depth wrong'); if(new Set(l.map(x=>x.slug)).size!==35) throw new Error('slug not unique'); if(new Set(l.map(x=>x.moduleId+'#'+x.order)).size!==35) throw new Error('(moduleId,order) not unique');" && test "$(ls src/content/lessons/step-1/*.mdx | wc -l)" = "10" && test "$(ls src/content/lessons/step-2/*.mdx | wc -l)" = "12" && test "$(ls src/content/lessons/step-3/*.mdx | wc -l)" = "13" && test "$(grep -rIil 'kant' src/content | wc -l)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `.velite/lessons.json`의 항목 수가 정확히 35다
    - Step별 MDX 파일 수가 10 / 12 / 13이다
    - `stepId`가 3인 13개 레슨의 `depth`가 모두 개요, 1·2인 22개 레슨의 `depth`가 모두 심화다
    - 35개 레슨의 `estimatedMinutes` 총합이 7200 이상 10800 이하다
    - `slug` 35개가 전역 유일하고, `moduleId`+`order` 조합 35개도 전역 유일하다
    - `node scripts/check-manifest.mjs`가 0으로 종료하고, 입력 파일을 일부러 지우면 0이 아닌 코드로 종료한다
    - `hasContent`가 true인 레슨이 정확히 1개이며 slug가 `1-3-python-variables-and-types`다
    - `src/content` 전체에 금지 브랜드 문자열이 0건이다
    - `npm run build`가 0으로 종료한다
  </acceptance_criteria>
  <done>35행 매니페스트가 완성되고, 그 불변식이 의존성 없는 스크립트로 고정되어 이후 Plan에서 깨지면 즉시 드러난다.</done>
</task>

</tasks>

<!-- planner-discipline-allow: kant -->

## Curriculum Manifest — 35행 확정 표

파생 규칙(RESEARCH.md Curriculum Manifest Derivation, CONTEXT.md "Claude's Discretion"으로 위임된 영역):
비프로젝트 모듈의 불릿 하나 = 레슨 하나 / `[Project N]` 태그가 붙은 5개 모듈 = 각 개요·사전 준비 가이드 레슨 하나 (CONT-05).
`estimatedMinutes` 파생 규칙: 심화 비프로젝트 270 / 심화 프로젝트 개요 150 / 개요 비프로젝트 180 / 개요 프로젝트 개요 120.
근거: 사전학습 기간 8/25~9/29 = 36일 × 하루 4~6시간 = 144~216시간에 대해 총합 7,860분(131시간)으로 복습 여유를 남긴 값. 커리큘럼 원 시수(200/336/520h)의 비례 분할이 아니다 — 원 시수는 개강 후 본 과정의 실제 소요이고 이 사이트는 5주 사전 통독용이다 (RESEARCH Open Question 2).

| # | slug | title | moduleId | order | stepId | depth | estimatedMinutes | hasContent |
|---|------|-------|----------|-------|--------|-------|------------------|------------|
| 1 | 1-1-course-orientation | 과정 운영 방식과 학습 준비 | 1-1 | 1 | 1 | 심화 | 270 | false |
| 2 | 1-1-dev-environment-setup | GitHub·학습 도구 환경 세팅 | 1-1 | 2 | 1 | 심화 | 270 | false |
| 3 | 1-2-git-branch-and-pr | Git 브랜치와 PR 협업 흐름 | 1-2 | 1 | 1 | 심화 | 270 | false |
| 4 | 1-2-generative-ai-basics | 생성형 AI 개념과 활용 윤리 | 1-2 | 2 | 1 | 심화 | 270 | false |
| 5 | 1-3-python-variables-and-types | Python 변수·자료형 | 1-3 | 1 | 1 | 심화 | 270 | **true** (Plan 01 작성 완료) |
| 6 | 1-3-python-functions-and-io | Python 함수·예외·파일 입출력 | 1-3 | 2 | 1 | 심화 | 270 | false |
| 7 | 1-4-relational-db-basics | 관계형 데이터베이스 구조 | 1-4 | 1 | 1 | 심화 | 270 | false |
| 8 | 1-4-sql-queries-and-joins | SQL 쿼리·JOIN·집계 | 1-4 | 2 | 1 | 심화 | 270 | false |
| 9 | 1-5-ml-model-types | 분류·회귀·군집 모델 이해 | 1-5 | 1 | 1 | 심화 | 270 | false |
| 10 | 1-5-ml-metrics-and-pipeline | 평가 지표와 Scikit-learn Pipeline | 1-5 | 2 | 1 | 심화 | 270 | false |
| 11 | 2-1-postgres-and-supabase | PostgreSQL과 Supabase 활용 | 2-1 | 1 | 2 | 심화 | 270 | false |
| 12 | 2-1-ai-data-modeling | AI 서비스 데이터 구조 설계 | 2-1 | 2 | 2 | 심화 | 270 | false |
| 13 | 2-2-html-css-js | HTML·CSS·JavaScript 핵심 | 2-2 | 1 | 2 | 심화 | 270 | false |
| 14 | 2-2-browser-and-ui | 브라우저 동작 원리와 UI 구현 | 2-2 | 2 | 2 | 심화 | 270 | false |
| 15 | 2-3-typescript-setup | TypeScript 개발 환경 구성 | 2-3 | 1 | 2 | 심화 | 270 | false |
| 16 | 2-3-react-components | React 컴포넌트 | 2-3 | 2 | 2 | 심화 | 270 | false → Plan 06이 true로 |
| 17 | 2-4-project-ai-shop-frontend | [Project 1] AI 쇼핑몰 프론트엔드 준비 가이드 | 2-4 | 1 | 2 | 심화 | 150 | false |
| 18 | 2-5-express-rest-api | Express RESTful API 구현 | 2-5 | 1 | 2 | 심화 | 270 | false |
| 19 | 2-5-auth-and-prisma | 인증·인가와 Prisma ORM | 2-5 | 2 | 2 | 심화 | 270 | false |
| 20 | 2-6-project-ai-shop-backend | [Project 2] AI 쇼핑몰 백엔드 준비 가이드 | 2-6 | 1 | 2 | 심화 | 150 | false |
| 21 | 2-7-prompt-patterns | 프롬프트 패턴과 구조화 출력 | 2-7 | 1 | 2 | 심화 | 270 | false |
| 22 | 2-7-promptops | PromptOps로 안정적인 LLM 활용 | 2-7 | 2 | 2 | 심화 | 270 | false |
| 23 | 3-1-vector-search-basics | 벡터 검색과 메타데이터 설계 | 3-1 | 1 | 3 | 개요 | 180 | false |
| 24 | 3-1-hybrid-search-reranking | 하이브리드 검색과 re-ranking | 3-1 | 2 | 3 | 개요 | 180 | false |
| 25 | 3-2-project-rag-agent | [Project 3] RAG Agent 준비 가이드 | 3-2 | 1 | 3 | 개요 | 120 | false |
| 26 | 3-3-peft-lora-qlora | PEFT·LoRA·QLoRA 개념 | 3-3 | 1 | 3 | 개요 | 180 | false |
| 27 | 3-3-tuning-evaluation | 모델 튜닝 전후 성능 비교 | 3-3 | 2 | 3 | 개요 | 180 | false |
| 28 | 3-4-multi-agent-structure | 여러 AI가 함께 일하는 구조 | 3-4 | 1 | 3 | 개요 | 180 | false |
| 29 | 3-4-webhook-schedule-hitl | Webhook·스케줄·HITL 설계 | 3-4 | 2 | 3 | 개요 | 180 | false |
| 30 | 3-4-n8n-langgraph | n8n·LangGraph 자동화 실습 개요 | 3-4 | 3 | 3 | 개요 | 180 | false |
| 31 | 3-5-project-orchestration | [Project 4] AI 업무 자동화 준비 가이드 | 3-5 | 1 | 3 | 개요 | 120 | false |
| 32 | 3-6-prompt-versioning-eval | 프롬프트 버전관리와 평가 자동화 | 3-6 | 1 | 3 | 개요 | 180 | false |
| 33 | 3-6-monitoring-governance | 모니터링·알림과 보안 거버넌스 | 3-6 | 2 | 3 | 개요 | 180 | false |
| 34 | 3-6-structured-output-canary | 구조화 출력·카나리 배포·비용 지표 | 3-6 | 3 | 3 | 개요 | 180 | false |
| 35 | 3-7-project-ax-launch | [Project 5] AX 서비스 런칭 준비 가이드 | 3-7 | 1 | 3 | 개요 | 120 | false |

합계 검산: 심화 비프로젝트 20 × 270 = 5,400 · 심화 프로젝트 2 × 150 = 300 · 개요 비프로젝트 10 × 180 = 1,800 · 개요 프로젝트 3 × 120 = 360 → **총 7,860분 (131시간)**. 게이트 허용 범위 7,200~10,800분 안에 든다.

19개 모듈 id: `1-1` `1-2` `1-3` `1-4` `1-5` / `2-1` `2-2` `2-3` `2-4` `2-5` `2-6` `2-7` / `3-1` `3-2` `3-3` `3-4` `3-5` `3-6` `3-7`. 프로젝트 모듈(`isProject: true`): `2-4` `2-6` `3-2` `3-5` `3-7`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 콘텐츠 파일(MDX frontmatter) → Velite Zod 검증 | 신뢰되지 않는 형식의 텍스트가 타입 계약으로 승격되는 지점 |
| 매니페스트 메타데이터 → Phase 3 일정 계산 | 이 Phase의 숫자가 다음 Phase의 계산 입력이 된다 (하류 신뢰 경계) |
| 저장소 콘텐츠 → 공개 배포 산출물 | 34개 신규 파일이 그대로 공개 웹 표면이 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | 매니페스트 메타데이터 무결성 (`depth`, `estimatedMinutes`, `moduleId`, `order`) | high | mitigate | `scripts/check-manifest.mjs`의 11개 불변식 게이트 — 35행 개수, 깊이 정책, 시간 총합 범위, 모듈 id 집합 일치, (moduleId, order) 유일성을 매 커밋 검증. 손상된 값이 Phase 3 일정 계산으로 전파되기 전에 빌드에서 차단 |
| T-01-02 | Information Disclosure | 34개 신규 콘텐츠 파일의 공개 표면 | high | mitigate | Task 2 verify의 `src/content` 전체 브랜드 문자열 0건 게이트 (D-02 HARD RULE) |
| T-01-10 | Denial of Service | 35개 정적 경로 생성으로 인한 빌드 시간 | low | accept | 35개는 정적 생성에서 사소한 규모다. 빌드 시간이 문제되면 그때 측정한다 |
| T-01-03 | Tampering | 빌드 타임 MDX 컴파일 | low | accept | 모든 MDX는 저장소 소유자 작성이며 사용자 제출·원격 fetch 경로가 없다 (RESEARCH Security Domain) |
</threat_model>

## Artifacts this phase produces

이 Plan이 새로 만드는 심볼·경로:

- 파일: `src/content/modules.ts`, `src/content/curriculum-helpers.ts`, `scripts/check-manifest.mjs`
- 파일: `src/content/lessons/step-1/` 아래 9개 MDX (`1-1-course-orientation`, `1-1-dev-environment-setup`, `1-2-git-branch-and-pr`, `1-2-generative-ai-basics`, `1-3-python-functions-and-io`, `1-4-relational-db-basics`, `1-4-sql-queries-and-joins`, `1-5-ml-model-types`, `1-5-ml-metrics-and-pipeline`)
- 파일: `src/content/lessons/step-2/` 아래 12개 MDX (`2-1-postgres-and-supabase`, `2-1-ai-data-modeling`, `2-2-html-css-js`, `2-2-browser-and-ui`, `2-3-typescript-setup`, `2-3-react-components`, `2-4-project-ai-shop-frontend`, `2-5-express-rest-api`, `2-5-auth-and-prisma`, `2-6-project-ai-shop-backend`, `2-7-prompt-patterns`, `2-7-promptops`)
- 파일: `src/content/lessons/step-3/` 아래 13개 MDX (`3-1-vector-search-basics`, `3-1-hybrid-search-reranking`, `3-2-project-rag-agent`, `3-3-peft-lora-qlora`, `3-3-tuning-evaluation`, `3-4-multi-agent-structure`, `3-4-webhook-schedule-hitl`, `3-4-n8n-langgraph`, `3-5-project-orchestration`, `3-6-prompt-versioning-eval`, `3-6-monitoring-governance`, `3-6-structured-output-canary`, `3-7-project-ax-launch`)
- 타입: `StepId`, `Step`, `Module`
- export: `steps`, `modules` (src/content/modules.ts)
- export: `getStep`, `getModulesByStep`, `getLessonsByModule`, `getOrderedLessons`, `getLessonBySlug`, `getAdjacentLessons`, `getLessonCounts` (src/content/curriculum-helpers.ts)
- 검증 커맨드: `node scripts/check-manifest.mjs`

## Flagged Assumptions

Spec-less 엣지 프로브 폴백 결과 중 이 Plan이 소유한 미해결 항목 (묵시적 누락 금지 — 명시 플래그):

- **CONT-04 / category: unclassified / status: unresolved** — 프로브가 이 요구사항을 엣지 범주로 분류하지 못했다("unclassified — review manually"). 따라서 자동으로 `must_haves.truths`에 승격하지 않았다. 플래너의 판단: 깊이 배지의 실질 경계(모든 레슨이 두 값 중 하나를 갖는가, Step 3이 예외 없이 개요인가)는 위 `check-manifest.mjs` 불변식 3·4가 기계적으로 덮는다. 분류를 확정하려면 `/gsd-spec-phase 1`로 SPEC의 `## Edge Coverage`를 채우는 것이 정공법이다.

추가 플래너 가정 (RESEARCH Assumption A2를 그대로 계승):
- **35행 파생 규칙은 확정된 사용자 결정이 아니라 권고안이다.** CONTEXT.md가 매니페스트 구조를 Claude's Discretion으로 위임했고, 프로젝트 모듈 5개를 각각 개요 레슨 1개로 접는 규칙은 CONT-05 문구("재현 아님, 개요·사전 준비 가이드")에서 도출했다. 사용자가 프로젝트 모듈의 불릿을 각각 별도 레슨으로 원한다면 행 수와 lessonId가 바뀌고 Phase 3 일정 파생이 함께 바뀐다.
- **`estimatedMinutes` 수치는 파생 규칙의 산물이다.** RESEARCH Open Question 2가 "커리큘럼 원 시수의 비례 분할이 아니라 독립 산정"을 권고했고 CONTEXT.md가 수치 산정을 Claude's Discretion으로 위임했으므로 체크포인트 없이 확정했다. 총합 게이트(7,200~10,800분)가 이후 어떤 조정에도 자릿수 오류를 잡아 준다.

<verification>
- `npm run build`가 0으로 종료하고 `.velite/lessons.json`이 35개 항목을 담는다
- `node scripts/check-manifest.mjs`가 11개 불변식을 모두 통과한다
- Step별 MDX 파일 수가 10 / 12 / 13이다
- `src/content` 전체에 금지 브랜드 문자열이 0건이다
</verification>

<success_criteria>
- 본문 집필 이전에 3 Step / 19 모듈 / 35 레슨의 전체 구조와 메타데이터가 확정된다 (CONT-01, CONT-04, D-13)
- Phase 3의 일정 배분이 소비할 예상 소요시간 데이터가 총합 제약을 만족한 상태로 준비된다
- 매니페스트가 깨지는 순간을 사람 눈이 아니라 스크립트가 잡는다
</success_criteria>

<output>
Create `.planning/phases/01-deployed-curriculum-skeleton/01-03-SUMMARY.md` when done
</output>
