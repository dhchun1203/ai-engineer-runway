---
phase: 01-deployed-curriculum-skeleton
plan: 05
type: execute
wave: 5
depends_on: ["01-01", "01-04"]
files_modified:
  - src/app/layout.tsx
  - src/components/site-nav.tsx
  - src/components/theme-toggle.tsx
  - src/app/about/page.tsx
  - velite.config.ts
  - scripts/check-brand.mjs
autonomous: true
requirements: [PLAT-03, UX-01, UX-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "모든 페이지 상단에 글로벌 내비 4항목(오늘의 학습 · 커리큘럼 · 일정표 · 소개)이 고정 노출되고, Phase 1에서는 커리큘럼과 소개만 이동 가능하며 나머지 둘은 준비 중 표시로 비활성이다 (D-09)"
    - "브라우저 탭 제목과 OG 태그에 사이트명 AI Engineer Runway가 들어간다 (D-01)"
    - "시스템 다크모드 설정이 첫 방문에 자동 반영되고, 수동 토글로 덮어쓴 선택이 새로고침 후에도 유지된다 (D-05)"
    - "테마가 첫 페인트 이전에 결정되어 잘못된 테마가 번쩍이지 않는다 — React 컨텍스트 프로바이더가 아니라 하이드레이션 이전 인라인 스크립트가 결정한다"
    - "Making-of 페이지가 docs/making-of.md를 렌더해 자료 수집 → 리서치 → 요구사항 → 로드맵 → 구현·배포까지의 단계별 기록과 스택 선택 이유를 보여주며, 문서가 갱신되면 다음 배포에 그대로 반영된다 (PLAT-03)"
    - "Making-of 페이지에 공개 GitHub 저장소로 가는 링크가 있다 (D-14)"
    - "내비 라벨·버튼 라벨·배지 텍스트는 고정 카피 템플릿을 쓰므로 가변 장문 텍스트로 인한 레이아웃 붕괴가 없다"
    - "Step 카드 3장과 내비 4항목은 고정 카디널리티라 개수 가변 상태가 존재하지 않는다 — 한국어 카피이므로 단복수 분기도 불필요하다"
    - statement: "글로벌 내비(로고 + 4항목 + 테마 토글)가 최소 지원 폭 375px에서 가로 스크롤·겹침 없이 44px 이상 터치 타깃을 유지한다"
      verification: backstop
    - "공개 표면(src, docs, public, README.md)에 금지 브랜드 문자열과 개인 이메일 주소가 0건임을 스크립트가 매번 검증한다 (D-02)"
  prohibitions:
    - statement: "공개되는 어떤 표면에도 교육기관명이 노출되어서는 안 된다 — 페이지 제목, 메타데이터, OG 태그, 레슨 본문, Making-of 렌더 결과, 코드 주석, 저장소명, 배포 URL 전부 포함. 과정은 항상 AI Engineer 교육과정으로만 지칭한다"
      category: values
      requirement_id: PLAT-03
      status: resolved
      verification: judgment
    - statement: "Making-of 페이지와 공개 저장소가 .planning/ 내부 산출물, 개인 메모, 개인 이메일 주소 등 사적 정보를 공개 웹 페이지로 렌더해서는 안 된다 — 소개 페이지의 콘텐츠 소스는 docs/making-of.md 하나뿐이다"
      category: privacy
      requirement_id: PLAT-03
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/components/site-nav.tsx"
      provides: "글로벌 내비 4항목 (2 활성 / 2 준비 중 비활성)"
    - path: "src/components/theme-toggle.tsx"
      provides: "다크모드 수동 토글 (localStorage.theme + .dark 클래스)"
    - path: "src/app/layout.tsx"
      provides: "루트 레이아웃 확장 — 내비, 테마 토글, 사이트 메타데이터/OG"
      contains: "AI Engineer Runway"
    - path: "src/app/about/page.tsx"
      provides: "Making-of 소개 페이지 — 단계별 타임라인 렌더"
    - path: "scripts/check-brand.mjs"
      provides: "공개 표면 브랜드·개인정보 노출 상시 게이트 (의존성 0)"
      min_lines: 30
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/site-nav.tsx"
      via: "모든 라우트가 공유하는 헤더 슬롯"
      pattern: "SiteNav"
    - from: "src/components/theme-toggle.tsx"
      to: "src/app/layout.tsx 인라인 테마 스크립트"
      via: "동일한 localStorage.theme 키와 documentElement의 dark 클래스를 읽고 쓴다"
      pattern: "localStorage"
    - from: "src/app/about/page.tsx"
      to: "docs/making-of.md"
      via: "velite pages 컬렉션 → MDXContent 렌더"
      pattern: "making-of"
    - from: "velite.config.ts"
      to: "docs/making-of.md"
      via: "pages 컬렉션 pattern"
      pattern: "docs/making-of"
---

<objective>
학습자가 어느 페이지에 있든 같은 껍데기 안에 있게 만든다 — 4항목 글로벌 내비, 다크모드 토글, 사이트 정체성(제목·OG), 그리고 이 사이트가 어떻게 만들어졌는지 보여주는 Making-of 페이지.

Purpose: D-09의 내비 구조는 Phase 3에서 "오늘의 학습"·"일정표"가 켜질 자리를 지금 잡아 두는 일이고, PLAT-03의 Making-of는 매 Phase마다 갱신되는 살아있는 포트폴리오 문서라 첫 Phase에 렌더 경로가 서 있어야 한다. D-02의 브랜드 금지는 되돌릴 수 없는 공개 노출이라 사람 눈 대신 스크립트가 지킨다.

Output: 글로벌 내비와 테마 토글이 얹힌 루트 레이아웃, 사이트 메타데이터·OG, `/about` Making-of 타임라인 페이지, 그리고 공개 표면을 매번 훑는 브랜드·개인정보 게이트.
</objective>

## Phase Goal

**ROADMAP `**Goal:**` 원문 (verbatim):** 학습자가 배포된 URL에 아이패드로 접속해 커리큘럼 3단 구조를 탐색하고 실제 레슨을 읽을 수 있다

> ⚠ MVP_MODE 주의: Goal 문장에 정규 유저 스토리의 **so that** 절이 없다. 플래너는 스토리를 창작하지 않는다 — 정규 형식이 필요하면 `/gsd mvp-phase 1`을 먼저 실행할 것.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-deployed-curriculum-skeleton/01-CONTEXT.md
@.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md
@.planning/phases/01-deployed-curriculum-skeleton/01-RESEARCH.md
@.planning/phases/01-deployed-curriculum-skeleton/01-01-SUMMARY.md
@.planning/phases/01-deployed-curriculum-skeleton/01-04-SUMMARY.md
@docs/making-of.md
</context>

<interfaces>
<!-- Plan 01이 확정한 계약 -->

src/components/mdx-content.tsx:
  MDXContent(props: { code: string; components?: Record<string, React.ComponentType> }): JSX.Element

velite.config.ts (현재): root '.', output.data '.velite', collections = { lessons }
tsconfig paths: "#site/content" -> ["./.velite"]

src/app/layout.tsx (현재): html lang="ko" + Pretendard 폰트 변수 + globals.css + 하이드레이션 이전 인라인 테마 스크립트

globals.css 토큰: 배경/표면/accent/destructive, Step 1·2·3 상징 색, 개요 배지 중성색, 타이포 14/16/20/28 · 400/600

<!-- 이 Plan이 생성하는 계약 -->

velite.config.ts (확장 후): collections = { lessons, pages }
  pages 스키마: title(string), slug(slug), code(mdx)
  pages pattern: 'docs/making-of.md'  — 이 하나만. .planning 이하를 절대 포함하지 않는다.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 글로벌 내비 4항목, 다크모드 토글, 사이트 메타데이터</name>
  <read_first>
    - `src/app/layout.tsx` — Plan 01이 만든 현재 루트 레이아웃과 인라인 테마 스크립트
    - `.planning/phases/01-deployed-curriculum-skeleton/01-CONTEXT.md` — D-01(사이트명), D-05(다크모드 자동 + 수동 토글), D-09(내비 4항목, 2개만 활성)
    - `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` — Design System(아이콘은 lucide-react), Typography 적용 맵(내비 라벨 = Label 14px, 배지 변형은 600), Color의 accent 사용처("Active/focused state: focus ring outline, active nav item underline"), Copywriting Contract의 "Nav placeholder badge" 행
    - `.planning/phases/01-deployed-curriculum-skeleton/01-RESEARCH.md` — Pattern 5(Tailwind v4 수동 다크모드), Don't Hand-Roll 표의 다크모드 행
    - `src/app/globals.css` — 등록된 토큰 이름
  </read_first>
  <files>src/app/layout.tsx, src/components/site-nav.tsx, src/components/theme-toggle.tsx</files>
  <action>
루트 레이아웃을 사이트 껍데기로 확장한다.

**(1) `src/components/site-nav.tsx`.** `<nav>` 안에 좌측 로고("AI Engineer Runway", `/`로 링크, D-01)와 4개 항목을 둔다. 항목과 목적지는 D-09 그대로다 — 오늘의 학습(비활성), 커리큘럼(`/`), 일정표(비활성), 소개(`/about`). 비활성 두 항목은 링크가 아닌 요소로 렌더하고 `aria-disabled`를 주며, UI-SPEC Copywriting Contract의 "Nav placeholder badge" 행 문구를 작은 배지로 곁들인다. 항목을 숨기지 않는 이유는 Phase 3에서 켜질 자리를 미리 인지시키기 위함이다. 현재 경로와 일치하는 활성 항목에는 UI-SPEC이 accent 사용처로 지정한 활성 밑줄을 준다. 모든 항목의 히트 영역은 최소 44px이며, 폭 375px에서도 가로 스크롤이나 겹침 없이 배치된다(좁은 화면에서는 로고를 축약하거나 항목을 2행으로 흘려도 된다 — 항목을 없애지는 않는다). 내비 라벨은 전부 고정 카피이므로 가변 장문 텍스트 분기를 만들지 않는다.

**(2) `src/components/theme-toggle.tsx`.** 클라이언트 컴포넌트다. lucide-react의 해/달 아이콘으로 현재 테마를 표시하고, 클릭하면 `localStorage.theme` 값을 뒤집고 `document.documentElement`의 `dark` 클래스를 토글한다 — 레이아웃의 인라인 스크립트와 정확히 같은 키와 같은 클래스를 쓴다. React 컨텍스트 기반 테마 프로바이더는 만들지 않는다(RESEARCH Don't Hand-Roll: 하이드레이션 불일치로 인한 첫 페인트 깜빡임의 원인). 버튼 히트 영역은 최소 44px이고 `aria-label`로 현재 동작을 알린다.

**(3) `src/app/layout.tsx` 확장.** 인라인 테마 스크립트는 그대로 유지하고(첫 페인트 이전 실행이 핵심), `<body>` 최상단에 `SiteNav`와 `ThemeToggle`을 담은 헤더를 넣고 그 아래를 페이지 콘텐츠 슬롯으로 둔다. `metadata`를 export한다: `title`은 기본값 "AI Engineer Runway"와 하위 페이지용 템플릿("%s | AI Engineer Runway")을 함께 두고, `description`은 사전학습 사이트임을 한국어 한 문장으로 설명하며, `openGraph`에 `siteName` "AI Engineer Runway", `locale` "ko_KR", `type` "website"와 같은 제목·설명을 준다(D-01). `metadataBase`는 `process.env.NEXT_PUBLIC_SITE_URL`이 있으면 그 값을, 없으면 `https://ai-engineer-runway.vercel.app`을 쓴다 — 이 값은 비밀이 아니고 환경변수가 없어도 빌드가 성공해야 한다. 제목·설명·OG 어디에도 교육기관 이름을 쓰지 않는다: 과정은 "AI Engineer 교육과정"으로만 지칭한다(D-02, PROJECT.md HARD RULE).
  </action>
  <verify>
    <automated>npm run build && grep -q 'AI Engineer Runway' .next/server/app/index.html && grep -q 'og:site_name\|property="og:' .next/server/app/index.html && grep -q '준비 중' .next/server/app/index.html && grep -q "'use client'\|\"use client\"" src/components/theme-toggle.tsx && grep -q 'localStorage' src/components/theme-toggle.tsx && grep -q 'localStorage' src/app/layout.tsx && test "$(grep -c 'createContext' src/components/theme-toggle.tsx src/app/layout.tsx | awk -F: '{s+=$2} END {print s+0}')" = "0" && grep -qE 'min-h-11|min-w-11|44px' src/components/site-nav.tsx && grep -qE 'min-h-11|min-w-11|44px' src/components/theme-toggle.tsx && test "$(grep -c 'aria-disabled' src/components/site-nav.tsx)" -ge "1" && grep -q 'metadataBase' src/app/layout.tsx</automated>
    <human-check>iPad Safari(또는 Safari 반응형 모드 375px 폭)에서 세로·가로 모두 열어 내비 4항목과 테마 토글이 가로 스크롤 없이 보이고, 각 항목을 손가락으로 정확히 누를 수 있는지 확인. 테마 토글을 누른 뒤 새로고침해 선택이 유지되고 첫 페인트에 잘못된 테마가 번쩍이지 않는지 확인</human-check>
  </verify>
  <acceptance_criteria>
    - 렌더된 페이지에 내비 4항목이 모두 존재하고 그중 2개가 `aria-disabled` 상태이며 준비 중 배지 문구를 갖는다
    - 홈 산출물 HTML에 `AI Engineer Runway`와 OG 메타 태그가 존재한다
    - `theme-toggle.tsx`가 클라이언트 컴포넌트이고 `localStorage`를 읽고 쓴다
    - `theme-toggle.tsx`와 `layout.tsx`에 `createContext` 사용이 0건이다 (컨텍스트 기반 테마 프로바이더 금지)
    - `layout.tsx`의 인라인 테마 스크립트가 그대로 남아 있고 `metadataBase`가 설정되어 있다
    - `site-nav.tsx`와 `theme-toggle.tsx` 모두 44px 이상 히트 영역 지정을 갖는다
    - 환경변수를 하나도 설정하지 않은 상태에서 `npm run build`가 0으로 종료한다
    - 렌더된 페이지 제목·설명·OG 태그에 금지 브랜드 문자열이 0건이다
  </acceptance_criteria>
  <done>어느 라우트로 들어와도 같은 헤더(로고 + 4항목 내비 + 테마 토글)가 보이고, 탭 제목과 OG에 사이트명이 실리며, 다크모드가 시스템 설정과 수동 선택 양쪽으로 동작한다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Making-of 소개 페이지 — 단계별 타임라인 렌더</name>
  <read_first>
    - `docs/making-of.md` — 전문. `### N단계 — ...` 제목 구조와 표 사용 방식을 파악하고, 금지 브랜드 문자열이 이미 들어 있지 않은지 전체를 확인할 것 (발췌만 보지 말 것)
    - `velite.config.ts` — Plan 01이 만든 lessons 컬렉션 정의와 MDX 옵션
    - `src/components/mdx-content.tsx` — 재사용할 런타임 렌더러
    - `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` — Copywriting Contract의 "Making-of page CTA (GitHub link)" 행, Typography 적용 맵
    - `.planning/phases/01-deployed-curriculum-skeleton/01-CONTEXT.md` — D-14(공개 저장소 링크), Claude's Discretion의 "단계별 타임라인 형태 권장"
    - `.planning/phases/01-deployed-curriculum-skeleton/01-RESEARCH.md` — Architecture Patterns의 `/about` 라우트 설명
  </read_first>
  <files>velite.config.ts, src/app/about/page.tsx</files>
  <action>
Making-of 원천 문서를 레슨과 같은 파이프라인으로 사이트에 올린다.

**(1) `velite.config.ts` 확장.** 기존 `lessons` 컬렉션은 그대로 두고 `pages` 컬렉션을 추가한다. `pattern`은 `docs/making-of.md` 하나만 지정한다 — 글로브를 넓혀 `.planning` 이하를 빨아들이지 않는다. 스키마는 `title`(string), `slug`(s.slug), `code`(s.mdx) 세 필드면 충분하다. MDX 옵션(rehype-pretty-code, 복사 버튼 트랜스포머)은 레슨과 동일하게 적용되도록 한다 — Making-of 문서에도 표와 코드가 등장할 수 있다.

**(2) `src/app/about/page.tsx`.** `#site/content`에서 `pages`를 import해 making-of 항목을 찾아 `MDXContent`로 렌더한다. 페이지 `h1`은 Display 28px / 600이고, 본문 래퍼에 `prose` 계열 클래스를 적용해 표·목록·인용의 타이포그래피를 `@tailwindcss/typography`에 맡긴다. 원문의 `### N단계 — ...` 구조를 시각적 타임라인으로 보이게 만든다 — 각 단계 제목 앞에 순서 표식과 세로 연결선을 주는 정도로 충분하고, 애니메이션이나 스크롤 연동은 넣지 않는다(RESEARCH Pitfall 6: 이 Phase에서 시각 다듬기에 시간을 태우지 않는다). 원문을 요약하거나 재작성하지 않는다 — 문서가 갱신되면 다음 배포에 그대로 반영되는 것이 PLAT-03의 요구다.

**(3) 저장소 링크.** 페이지 하단(또는 상단 요약부)에 공개 GitHub 저장소로 가는 링크를 둔다. 링크 문구는 UI-SPEC Copywriting Contract의 "Making-of page CTA (GitHub link)" 행 그대로 쓰고, lucide-react의 외부 링크 아이콘을 곁들이며, 히트 영역은 최소 44px, 새 탭으로 열 때 `rel="noopener noreferrer"`를 준다.

**(4) 콘텐츠 소스 제한.** 이 페이지가 렌더하는 소스는 `docs/making-of.md` 단 하나다. `.planning/` 이하의 어떤 문서도 사이트에 렌더하지 않는다 — GSD 산출물에는 개인 메모와 계획 과정이 담겨 있고 소개 페이지의 대상이 아니다.
  </action>
  <verify>
    <automated>npm run build && test -f .next/server/app/about.html && grep -q '단계' .next/server/app/about.html && grep -q 'github.com' .next/server/app/about.html && grep -q 'docs/making-of' velite.config.ts && test "$(grep -c '\.planning' velite.config.ts)" = "0" && test "$(grep -c '\.planning' src/app/about/page.tsx)" = "0" && grep -q 'pages' velite.config.ts && grep -q 'MDXContent' src/app/about/page.tsx && grep -q 'noopener' src/app/about/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `/about`이 정적 산출물로 생성되고 `docs/making-of.md`의 단계별 제목이 그 HTML에 나타난다
    - `velite.config.ts`의 pages 컬렉션 pattern이 `docs/making-of.md` 하나로 한정된다
    - `velite.config.ts`와 `src/app/about/page.tsx` 어디에도 `.planning` 경로 참조가 0건이다
    - `/about` 산출물에 `github.com` 저장소 링크가 존재하고 `rel`에 `noopener`가 포함된다
    - `/about` 산출물에 금지 브랜드 문자열이 0건이다
    - `npm run build`가 0으로 종료한다
  </acceptance_criteria>
  <done>`/about`에서 자료 수집 → 리서치 → 요구사항 → 로드맵 → 구현·배포까지의 단계별 기록과 스택 선택 이유가 타임라인으로 읽히고, 원천 문서를 고치면 다음 배포에 그대로 반영된다.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 공개 표면 브랜드·개인정보 노출 상시 게이트</name>
  <read_first>
    - `.planning/PROJECT.md` — Constraints의 금지 브랜드 HARD RULE 원문 (스캔 대상 문자열의 출처)
    - `.claude/CLAUDE.md` — 같은 규칙의 프로젝트 가이드 표현
    - `.planning/phases/01-deployed-curriculum-skeleton/01-CONTEXT.md` — D-02(one-way: 공개 노출 후 회수 불가), D-14
    - `scripts/check-manifest.mjs` — Plan 03이 만든 검증 스크립트의 오류 처리·종료 코드 관례(같은 관례를 따를 것)
    - `package.json` — 현재 스크립트 목록 (이 태스크는 package.json을 수정하지 않는다)
  </read_first>
  <files>scripts/check-brand.mjs</files>
  <action>
되돌릴 수 없는 공개 노출을 사람 기억이 아니라 스크립트가 막게 만든다.

**(1) `scripts/check-brand.mjs`.** 외부 의존성 없이 Node 표준 모듈만 쓴다. 검사 대상은 공개 표면인 `src`, `docs`, `public`, `README.md` 네 곳이다 — `.planning`, `.claude`, `node_modules`, `.next`, `.velite`는 검사 대상이 아니다(전자 둘은 규칙 원문을 담고 있고 후자 셋은 생성물이다). 텍스트 파일만 읽고 바이너리는 건너뛴다.

검사 항목은 두 가지다:
  - **금지 브랜드 문자열.** PROJECT.md Constraints에 HARD RULE로 적힌 교육기관명을 대소문자 무관으로 찾는다. 문자열 값은 `.planning/PROJECT.md`에서 읽어 스크립트 상단 상수로 하드코딩한다.
  - **개인 이메일 주소.** 일반적인 이메일 주소 정규식으로 찾는다. 특정 주소를 스크립트에 적어 넣지 말 것 — 그것 자체가 공개 저장소에 개인정보를 남기는 행위다. 정규식 매칭이면 충분하다.

**(2) 실패 동작.** 위반이 하나라도 있으면 위반 파일 경로와 줄 번호를 출력하고 0이 아닌 코드로 종료한다. 검사 대상 경로가 존재하지 않으면 "0건이므로 통과"로 넘어가지 말고 즉시 오류로 종료한다 — 대상이 없어서 0이 된 상태가 통과로 둔갑하면 게이트가 아무것도 측정하지 않는다. 위반이 없으면 검사한 파일 수를 출력하고 0으로 종료한다.

**(3) 자기 자신 제외 금지 확인.** 스크립트는 `scripts/` 아래에 있으므로 검사 대상 네 곳에 포함되지 않는다. 검사 대상을 넓히려는 유혹을 피한다 — 규칙 원문이 담긴 문서까지 검사하면 게이트가 스스로를 무효화한다.
  </action>
  <verify>
    <automated>node scripts/check-brand.mjs && test "$(grep -c '\.planning' scripts/check-brand.mjs)" = "0" && test "$(grep -oE '@[A-Za-z][A-Za-z0-9.-]*\.[A-Za-z]{2,}' scripts/check-brand.mjs | wc -l)" = "0" && test "$(grep -oE "from ['\"][^'\"]+['\"]" scripts/check-brand.mjs | grep -vc 'node:' || true)" = "0" && TMPD=$(mktemp -d) && (cd "$TMPD" && ! node "$OLDPWD/scripts/check-brand.mjs" >/dev/null 2>&1) && rm -rf "$TMPD"</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-brand.mjs`가 현재 저장소 상태에서 0으로 종료한다
    - 스크립트 소스에 리터럴 이메일 주소가 0건이다 (정규식 패턴은 허용, 실제 주소는 금지)
    - 스크립트 소스에 `.planning` 경로 참조가 0건이다 (규칙 원문 문서를 검사 대상에 넣지 않는다)
    - 검사 대상 경로가 없는 디렉터리에서 실행하면 0이 아닌 코드로 종료한다 (부재를 통과로 처리하지 않는다)
    - 스크립트가 외부 npm 의존성을 import하지 않는다
    - 위반 발견 시 파일 경로와 줄 번호를 출력한다
  </acceptance_criteria>
  <done>공개 표면 네 곳에 대한 브랜드·개인정보 노출 검사가 명령 한 줄로 재현 가능하고, 이후 모든 Plan과 Phase가 같은 게이트를 재사용한다.</done>
</task>

</tasks>

<!-- planner-discipline-allow: kant -->
<!-- planner-discipline-allow: .planning -->

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 저장소 문서(`docs/making-of.md`) → 공개 웹 페이지 | 문서 내용이 그대로 렌더되어 누구나 읽는다 |
| 저장소 전체(`.planning/` 포함, D-14 공개) → 검색 가능한 공개 표면 | 계획 산출물이 저장소에는 있지만 사이트에는 렌더되지 않아야 한다 |
| 브라우저 `localStorage` → 렌더 결정 | 사용자가 조작 가능한 값이 첫 페인트 클래스를 정한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Information Disclosure | 공개 표면(메타데이터, OG, `/about`, 레슨 본문)의 브랜드명 노출 | high | mitigate | `scripts/check-brand.mjs` 상시 게이트 + Task 1·2 verify의 산출물 스캔. D-02가 one-way로 표기한 회수 불가 위험이라 자동 게이트로 고정 |
| T-01-14 | Information Disclosure | Velite 글로브 확장으로 `.planning/` 문서가 사이트에 렌더될 위험 | high | mitigate | pages 컬렉션 pattern을 `docs/making-of.md` 단일 파일로 고정하고, `velite.config.ts`·`about/page.tsx`에 `.planning` 참조 0건을 verify에서 기계 검증 |
| T-01-15 | Information Disclosure | 공개 표면에 개인 이메일 주소 노출 | medium | mitigate | `check-brand.mjs`가 이메일 정규식으로 검사. 스크립트 자체에는 실제 주소를 남기지 않는다 |
| T-01-04 | Tampering | 클라이언트 `localStorage.theme` 값 조작 | low | accept | 표시 전용이며 권한·데이터 접근에 관여하지 않는다. 알 수 없는 값은 시스템 설정 폴백으로 흡수된다 |
| T-01-16 | Spoofing | OG 태그의 `metadataBase` 오설정으로 잘못된 도메인이 미리보기에 노출 | low | accept | 값이 D-15로 고정된 자체 도메인이고 환경변수로 덮어쓸 수 있다. 오설정 시 미리보기 이미지가 깨질 뿐 권한 상승 경로가 없다 |
</threat_model>

## Artifacts this phase produces

이 Plan이 새로 만드는 심볼·경로:

- 파일: `src/components/site-nav.tsx`, `src/components/theme-toggle.tsx`, `src/app/about/page.tsx`, `scripts/check-brand.mjs`
- 파일 확장: `src/app/layout.tsx` (헤더 슬롯, `metadata` export), `velite.config.ts` (`pages` 컬렉션)
- export: `SiteNav`, `ThemeToggle`, `metadata` (layout)
- Velite 컬렉션: `pages` (필드 `title`, `slug`, `code`)
- 라우트: `/about`
- 검증 커맨드: `node scripts/check-brand.mjs`
- 선택적 환경변수(비밀 아님, 미설정 시 기본값): `NEXT_PUBLIC_SITE_URL`

## Flagged Assumptions

Spec-less 엣지 프로브 폴백 결과 중 이 Plan이 소유한 미해결 항목 (묵시적 누락 금지 — 명시 플래그):

- **PLAT-03 / category: unclassified / status: unresolved** — 프로브가 이 요구사항을 엣지 범주로 분류하지 못했다("unclassified — review manually"). 자동으로 `must_haves.truths`에 승격하지 않았다. 플래너의 판단: Making-of 요구사항의 실질 경계(원천 문서가 비었을 때, 단계가 하나도 없을 때, 문서가 갱신됐을 때)는 Velite 스키마 검증과 Task 2의 산출물 게이트가 부분적으로 덮지만, "갱신되면 계속 반영된다"는 시간축 속성은 이 Phase 안에서 완전히 검증되지 않는다 — Plan 02가 5단계를 갱신하고 이 페이지가 그것을 렌더하는 것이 첫 번째 실증이다. 분류를 확정하려면 `/gsd-spec-phase 1`로 SPEC의 `## Edge Coverage`를 채우는 것이 정공법이다.

## Canon referral (minted 하지 않음)

- 공개 저장소의 시크릿 커밋, OWASP 계열 인젝션·경로 순회는 canon이다 — `/gsd-secure-phase`와 `.gitignore`가 담당하며 여기서 prohibition으로 mint하지 않는다.

<verification>
- 모든 라우트에 내비 4항목(2 활성 / 2 비활성)과 테마 토글이 공통으로 렌더된다
- 탭 제목과 OG 태그에 사이트명이 들어가고 환경변수 없이 빌드가 성공한다
- `/about`이 `docs/making-of.md`를 단계별 타임라인으로 렌더하고 저장소 링크를 포함한다
- `velite.config.ts`와 about 페이지에 `.planning` 참조가 0건이다
- `node scripts/check-brand.mjs`가 0으로 종료하고, 대상이 없는 위치에서는 실패한다
</verification>

<success_criteria>
- D-09의 내비 구조가 Phase 3에서 켜질 두 항목의 자리를 포함해 완성된다
- PLAT-03의 Making-of 페이지가 살아있는 문서 렌더 경로로 서 있다
- D-02의 되돌릴 수 없는 브랜드 노출 위험이 재현 가능한 명령으로 통제된다
</success_criteria>

<output>
Create `.planning/phases/01-deployed-curriculum-skeleton/01-05-SUMMARY.md` when done
</output>
