---
phase: 03-schedule-and-today
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/content/lessons/**/*.mdx
  - scripts/check-manifest.mjs
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SCHED-03]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "35개 레슨의 estimatedMinutes 합이 정확히 4,200분(70시간)이다 (D-31)"
    - "estimatedMinutes 값 분포가 정확히 150분 20개 · 90분 10개 · 60분 5개이며 이 세 값 외의 값이 0개다 (D-31)"
    - "심화이며 프로젝트 모듈이 아닌 레슨은 150, 개요이며 프로젝트 모듈이 아닌 레슨은 90, 프로젝트 준비 가이드 모듈(2-4·2-6·3-2·3-5·3-7)의 레슨은 깊이와 무관하게 60이다 (D-31 규칙)"
    - "프론트매터 일괄 수정 전후로 title · stepId · moduleId · order · depth · slug · hasContent 값이 35개 레슨 모두에서 동일하다 (T-03-06)"
    - "모든 estimatedMinutes가 1 이상의 정수여서 velite.config.ts의 s.number().min(1) 스키마를 계속 만족한다 (SCHED-03 경계)"
    - "Velite 재빌드 후 레슨 페이지에 렌더되는 소요시간 문자열이 갱신값이다 — 심화 일반 레슨은 '약 2.5시간', 프로젝트 준비 가이드는 '약 1시간' (RESEARCH Pitfall 2)"
    - "하루 1레슨 배정에서 어떤 날의 소요시간도 150분(2.5시간)을 넘지 않아 D-35의 '하루 3시간 이내'를 만족한다"
    - "ROADMAP.md · PROJECT.md · REQUIREMENTS.md의 페이스 기준 문구에 '하루 4~6시간'이 남아 있지 않고 D-35의 기준으로 갱신되어 있다"
  artifacts:
    - scripts/check-manifest.mjs
  key_links:
    - "modules.ts의 isProject + mdx 프론트매터 depth → estimatedMinutes 목표값 — 원본 수치가 아니라 이 두 축에서 파생 계산해야 270→150→60 이중 적용 사고를 원천 차단한다"
    - "mdx 프론트매터 → Velite 재빌드 → .velite/lessons.json → check-manifest.mjs — 재빌드를 건너뛰면 게이트가 옛 값을 검사한다 (Pitfall 2)"
    - "estimatedMinutes → Plan 03의 computePace(minutesBySlug) 시간 가중 판정 입력 (D-40)"
  prohibitions:
    - statement: "소요시간 하향이 Phase 4~5 레슨 본문의 깊이·예제 품질을 줄이는 근거로 쓰여서는 안 된다 — 이 수치는 사전학습 페이스 기준일 뿐 콘텐츠 품질 기준이 아니다"
      status: unverified
    - statement: "일괄 수정 과정에서 레슨 본문(프론트매터 바깥)을 한 글자도 바꿔서는 안 된다 — 편집 대상은 프론트매터의 estimatedMinutes 한 줄로 한정한다"
      status: unverified
    - statement: "갱신하는 계획 문서(ROADMAP·PROJECT·REQUIREMENTS) 어디에도 교육기관명을 새로 적어 넣어서는 안 된다 — 항상 'AI Engineer 교육과정' 표기 (프로젝트 HARD RULE)"
      status: unverified
---

<objective>
D-31의 예상 소요시간 일괄 하향을 실행하고, 그 수치가 다시 조용히 어긋나지 않도록 매니페스트 게이트를 조인다.

Purpose: 일정 배분(D-32)과 페이스 판정(D-40)이 전부 `estimatedMinutes`를 근거로 계산된다. 총 131시간짜리 수치 위에서는 하루 1레슨 배정이 "하루 3시간 이내"라는 D-35 기준을 만족하지 못한다. 이 수치는 Phase 4~5 콘텐츠 집필 분량의 기준이 되므로 콘텐츠를 쓰기 전인 지금이 바꿀 수 있는 마지막 시점이다.

Output: 35개 레슨 프론트매터의 갱신된 `estimatedMinutes`, 총합·분포·파생 규칙을 상시 검사하는 확장된 `check-manifest.mjs`, D-35에 맞춰 갱신된 계획 문서 3종.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-schedule-and-today/03-CONTEXT.md
@.planning/phases/03-schedule-and-today/03-RESEARCH.md
@.planning/phases/03-schedule-and-today/03-PATTERNS.md

<interfaces>
From `velite.config.ts` (lessons 스키마 — **변경하지 않는다**, 값만 바꾼다):
`{ title: string, stepId: 1|2|3, moduleId: string, order: number, depth: '심화'|'개요', estimatedMinutes: number(min 1), slug: slug, hasContent: boolean(default true), code: mdx }`

From `src/content/modules.ts` — `isProject: true`인 모듈은 정확히 5개: `2-4`, `2-6`, `3-2`, `3-5`, `3-7`

현재 값 분포 (수정 전, 이 세션에서 실측): `270 × 20`, `180 × 10`, `150 × 2`, `120 × 3` = 7,860분
목표 값 분포 (수정 후): `150 × 20`, `90 × 10`, `60 × 5` = 4,200분
</interfaces>
</context>

<artifacts_produced>
## Artifacts this phase produces

이 Plan은 **새 심볼을 만들지 않는다.** 기존 파일의 값과 검사 항목만 바꾼다.

- `scripts/check-manifest.mjs` — 기존 실행형 게이트에 Invariant 12·13 추가, Invariant 6의 허용 범위 교체, 최종 출력 문구의 불변식 개수 갱신. export 없음.
- `src/content/lessons/**/*.mdx` 35개 — 프론트매터 `estimatedMinutes` 값만 변경. 신규 필드·신규 파일 없음.
- `.planning/ROADMAP.md`, `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md` — 페이스 기준 문구만 갱신. 요구사항 ID·Phase 구조는 불변.

(Plan 01이 만든 `src/lib/today.ts` · `src/lib/schedule.ts` · `src/lib/schedule-data.ts` · `src/components/dday-countdown.tsx` · `src/components/today-lesson-card.tsx` · `src/app/curriculum/page.tsx` · `scripts/check-schedule.mjs` · `scripts/e2e-today.mjs`도 이 phase 산출물이다 — 신규 심볼로 취급할 것.)
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 35개 레슨 예상 소요시간 하향 (D-31)</name>
  <reversibility rating="costly">이 수치가 Phase 4~5 레슨 본문 집필 분량의 기준이 된다 — 콘텐츠를 쓴 뒤 되돌리면 35+ 레슨 본문과 일정 전체를 다시 손봐야 한다. CONTEXT.md가 사용자 결정으로 확정했으므로 체크포인트로 재확인하지 않고, 콘텐츠 집필 전인 지금 실행한다.</reversibility>
  <files>src/content/lessons/**/*.mdx</files>
  <read_first>
    - `src/content/modules.ts` — 어떤 모듈이 `isProject: true`인지 확인 (2-4, 2-6, 3-2, 3-5, 3-7). 이 파일은 **수정 대상이 아니다**.
    - `velite.config.ts` — lessons 스키마. `estimatedMinutes`는 `s.number().min(1)`이며 스키마 자체는 손대지 않는다.
    - `src/content/lessons/` 아래 임의의 mdx 2개 (심화 1개 + 개요 1개) — 프론트매터 줄 순서·들여쓰기·구분자 형태를 눈으로 확인한 뒤 편집 방식을 정한다.
    - `.planning/phases/03-schedule-and-today/03-RESEARCH.md` — Pitfall 2(Velite 재빌드 누락), Security Domain의 "estimatedMinutes 필드만 좁게 타겟팅" 요구.
  </read_first>
  <action>
35개 레슨 mdx의 프론트매터 `estimatedMinutes` 값을 갱신한다. **목표값은 수정 전 값이 아니라 (depth, 소속 모듈의 isProject) 두 축에서 파생 계산한다** — 이것이 이 Task의 핵심 안전장치다. 순차 치환(270을 150으로 바꾼 뒤 150을 60으로 바꾸는 식)을 쓰면 방금 150이 된 20개가 다시 60으로 끌려 내려가 총합이 조용히 망가진다.

파생 규칙:
- `depth`가 심화이고 소속 모듈의 `isProject`가 false → **150**
- `depth`가 개요이고 소속 모듈의 `isProject`가 false → **90**
- 소속 모듈의 `isProject`가 true → 깊이와 무관하게 **60**

소속 모듈은 각 mdx 프론트매터의 `moduleId`를 `src/content/modules.ts`의 모듈 목록과 조인해 판별한다. 편집은 프론트매터 안에서 `estimatedMinutes` 한 줄만 좁게 겨냥한다 — `title`·`stepId`·`moduleId`·`order`·`depth`·`slug`·`hasContent`와 프론트매터 바깥 본문은 한 글자도 바뀌면 안 된다(T-03-06). 일회성 편집 스크립트를 써도 좋고 파일별 직접 편집도 좋지만, 어느 쪽이든 편집 전후로 위 7개 필드가 동일한지 스스로 확인한 뒤 커밋한다.

편집이 끝나면 반드시 `npm run build`를 한 번 돌려 `.velite/lessons.json`을 재생성한다 — Velite는 dev/build가 돌지 않으면 mdx 변경을 반영하지 않아, 파일은 바뀌었는데 화면과 게이트는 옛 값을 보는 상태가 된다(Pitfall 2).
  </action>
  <verify>
    <automated>npm run build && node -e "const l=require('./.velite/lessons.json');const t=l.reduce((s,x)=>s+x.estimatedMinutes,0);const d={};for(const x of l)d[x.estimatedMinutes]=(d[x.estimatedMinutes]||0)+1;console.log(t,JSON.stringify(d));if(t!==4200)process.exit(1);if(d[150]!==20||d[90]!==10||d[60]!==5||Object.keys(d).length!==3)process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `.velite/lessons.json`의 `estimatedMinutes` 총합이 정확히 4200이다.
    - 값 분포가 정확히 `{150: 20, 90: 10, 60: 5}`이고 그 외 값이 0개다.
    - `moduleId`가 2-4 · 2-6 · 3-2 · 3-5 · 3-7 중 하나인 레슨 5개의 `estimatedMinutes`가 전부 60이다.
    - `stepId`가 3이면서 프로젝트 모듈이 아닌 레슨 10개의 `estimatedMinutes`가 전부 90이다.
    - `git diff --stat`에 mdx 35개만 잡히고, `git diff`의 변경 줄이 파일당 `estimatedMinutes` 한 줄뿐이다.
    - `node scripts/check-brand.mjs`가 통과한다.
  </acceptance_criteria>
  <done>35개 레슨의 예상 소요시간이 D-31 기준으로 낮아졌고, Velite 산출물이 갱신값을 반영한다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 매니페스트 게이트에 총합·분포·파생 규칙 불변식 추가</name>
  <files>scripts/check-manifest.mjs</files>
  <read_first>
    - `scripts/check-manifest.mjs` — 확장 대상. Invariant 5·6의 현재 구현(총합 7200~10800 밴드), `moduleIdMatches` 정규식 추출 방식, 마지막 `console.log`의 "all 11 invariants passed" 문구, `EXPECTED_HAS_CONTENT_COUNT` 상수 관례를 확인한다.
    - `src/content/modules.ts` — `isProject` 리터럴의 표기 형태. 모듈 항목 하나가 한 줄 객체이므로 `[^}]*?`로 경계를 잡는 정규식이 안전하다 (`scripts/e2e-progress.mjs`의 `readModuleOrderMap`이 같은 방식으로 order를 뽑는다).
    - `scripts/e2e-progress.mjs`의 `readModuleOrderMap()` — modules.ts 재파싱 정규식의 기존 관례.
  </read_first>
  <action>
`scripts/check-manifest.mjs`를 확장한다. 이 스크립트는 지금도 `modules.ts`를 정규식으로 독립 재파싱한다 — 그 관례를 유지하고 `curriculum-helpers.ts`를 import하지 않는다.

먼저 모듈 id → isProject 맵을 뽑는 정규식 패스를 하나 추가한다. 기존 `moduleIdMatches` 추출과 같은 파일·같은 소스 문자열을 쓰되, 모듈 항목 하나의 중괄호 경계를 넘지 않도록 경계를 잡는다. 추출된 프로젝트 모듈이 5개가 아니면 정규식이 낡았다는 뜻이므로 그 자체를 실패로 처리한다.

Invariant 6의 허용 범위를 교체한다. 지금의 하한·상한 밴드 대신 총합이 정확히 4200이어야 한다는 등식 검사로 바꾸고, 실패 메시지에 실제 총합과 기대값을 함께 찍는다. 기대값은 파일 상단에 `EXPECTED_HAS_CONTENT_COUNT`와 같은 형태의 명명 상수로 올려 D-31 근거를 한 줄 주석으로 남긴다.

Invariant 12를 추가한다: `estimatedMinutes` 값 분포가 정확히 150이 20개, 90이 10개, 60이 5개이며 그 세 값 외의 값을 가진 레슨이 0개다. 위반 시 실제 분포를 통째로 출력한다.

Invariant 13을 추가한다: 각 레슨의 `estimatedMinutes`가 (depth, 소속 모듈 isProject) 파생 규칙과 일치한다 — 프로젝트 모듈이면 60, 심화·비프로젝트면 150, 개요·비프로젝트면 90. 어긋난 레슨의 slug와 실제/기대값을 나열한다. 이 불변식이 Invariant 12보다 강한 검사라 개수만 맞고 배정이 뒤바뀐 경우까지 잡는다.

마지막으로 성공 출력의 불변식 개수를 13으로 갱신한다.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-manifest.mjs && node scripts/check-progress-gates.mjs && node scripts/check-schedule.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-manifest.mjs`가 exit 0으로 끝나고 성공 문구가 "13"개 불변식 통과를 알린다.
    - 아무 mdx 하나의 `estimatedMinutes`를 임시로 다른 값으로 바꾸면 `check-manifest.mjs`가 exit 1로 실패하고 Invariant 6·12·13 중 최소 두 개가 실패 목록에 나온다 (되돌린 뒤 다시 통과 확인).
    - `modules.ts`에서 `isProject`를 추출한 결과가 정확히 5개 모듈이며, 4개나 6개면 게이트가 스스로 실패한다.
    - 스크립트가 여전히 Node 표준 모듈만 쓰고 `package.json`에 새 의존성이 없다.
  </acceptance_criteria>
  <done>총합·분포·파생 규칙 세 층의 불변식이 상시 게이트로 굳어, 이후 레슨이 추가되거나 값이 손대지면 즉시 빨간불이 뜬다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: D-35 페이스 기준 문구를 계획 문서 3종에 반영</name>
  <files>.planning/ROADMAP.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` — Phase 3 Success Criteria 1번 항목의 현재 문구.
    - `.planning/PROJECT.md` — 세 군데에 같은 취지의 가정이 있다: 요구사항 체크리스트 항목, "가정" 절, Key Decisions 표의 일정표 행.
    - `.planning/REQUIREMENTS.md` — SCHED-01 요구사항 본문.
    - `.planning/phases/03-schedule-and-today/03-CONTEXT.md` — D-32(하루 1레슨), D-35(기준 대체) 원문.
  </read_first>
  <action>
D-35에 따라 세 문서의 페이스 기준 문구를 갱신한다. 사용자가 직접 기준을 바꿨으므로 이 갱신은 문서를 결정에 맞추는 작업이지 결정을 재해석하는 작업이 아니다.

새 기준 표현은 "하루 3시간 이내(하루 1레슨, 평균 약 2시간)"로 통일한다. 근거 수치: 하루 1레슨 고정 배정(D-32)에서 하루 소요시간은 60·90·150분 세 값만 나오고 35일 평균은 120분이다.

`.planning/ROADMAP.md` — Phase 3 Success Criteria 1번의 시간 범위 표현만 새 기준으로 교체한다. 날짜 범위(2026-08-25 ~ 09-29)와 나머지 문장 구조는 유지한다. Phase 3 섹션 밖은 건드리지 않는다.

`.planning/PROJECT.md` — 세 군데 모두 교체한다: 요구사항 체크리스트의 일정표 항목, 가정 절의 하루 투자 가능 시간, Key Decisions 표의 일정표 설계 행. Key Decisions 행에는 "D-35로 하향 조정됨" 취지를 덧붙여 이전 가정이 왜 바뀌었는지 흔적을 남긴다.

`.planning/REQUIREMENTS.md` — SCHED-01 본문의 시간 기준 표현만 교체한다. 요구사항 ID·체크박스 상태·Phase 매핑 표는 손대지 않는다.

세 파일 모두 해당 문장만 좁게 교체한다 — 파일 전체를 다시 쓰지 않는다.
  </action>
  <verify>
    <automated>node scripts/check-brand.mjs && node -e "const fs=require('fs');const files=['.planning/ROADMAP.md','.planning/PROJECT.md','.planning/REQUIREMENTS.md'];let bad=0;for(const f of files){const s=fs.readFileSync(f,'utf8');const hits=(s.match(/4~6\s*시간/g)||[]).length;if(hits>0){console.error(f+': '+hits);bad++}}if(bad>0)process.exit(1);console.log('pace wording updated in 3 docs')"</automated>
  </verify>
  <acceptance_criteria>
    - 세 문서 어디에도 이전 페이스 범위 표현이 남아 있지 않다 (verify 명령이 이를 검사한다).
    - `.planning/ROADMAP.md` Phase 3 Success Criteria가 여전히 4개 항목이고 1번 항목의 날짜 범위 `2026-08-25 ~ 09-29`가 그대로다.
    - `.planning/REQUIREMENTS.md`의 SCHED-01 ~ SCHED-04 ID 네 개와 하단 Phase 매핑 표의 행 수가 변경 전과 같다.
    - `.planning/PROJECT.md` Key Decisions 표에 이 조정의 근거가 한 줄로 남아 있다.
    - `git diff --stat`에 세 파일만 잡히고 각 파일의 변경 줄 수가 5줄 이하다.
  </acceptance_criteria>
  <done>계획 문서의 페이스 기준이 실제 구현·일정과 어긋나지 않는다. 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 편집 스크립트/도구 → `src/content/lessons/**/*.mdx` | 35개 파일을 일괄 수정하는 쓰기 경계. 잘못된 정규식 하나가 커리큘럼 구조 전체를 조용히 훼손할 수 있다 |
| mdx 프론트매터 → Velite 빌드 → `.velite/lessons.json` | 빌드 산출물 경계. 재빌드를 건너뛰면 소스와 산출물이 어긋난 채로 검증이 통과한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | `src/content/lessons/**/*.mdx` 일괄 편집 | high | mitigate | 편집을 프론트매터의 `estimatedMinutes` 한 줄로 좁게 겨냥하고, 직후 `check-manifest.mjs` 전체(13개 불변식)를 재실행해 depth·moduleId·order·slug·hasContent·모듈 조인 무결성을 확인한다. `git diff`의 변경 줄이 파일당 한 줄인지도 acceptance criteria로 확인 |
| T-03-07 | Tampering | 순차 치환에 의한 이중 적용 | high | mitigate | 목표값을 원본 수치가 아니라 (depth, isProject)에서 파생 계산한다 — 270→150과 150→60이 같은 값 공간을 공유하므로 원본 수치 기반 치환은 구조적으로 안전하지 않다. Invariant 13이 배정 규칙 자체를 상시 검증 |
| T-03-08 | Repudiation | 소스와 `.velite/` 산출물 불일치 | medium | mitigate | Task 1·2의 verify 명령이 `npm run build`를 앞에 붙여 항상 재생성 후 검사한다. 게이트가 옛 산출물을 검사해 거짓 통과하는 경로를 없앤다 |
| T-03-09 | Tampering | `.planning/` 계획 문서 광범위 덮어쓰기 | medium | mitigate | 세 문서 모두 해당 문장만 좁게 교체하고 파일 전체 재작성을 금지한다. `git diff --stat`의 변경 줄 수 상한(파일당 5줄)을 acceptance criteria로 고정 |
| T-03-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 패키지를 설치하지 않는다 — RESEARCH.md Package Legitimacy Audit이 "설치 대상 없음"으로 확정. Task 2 acceptance criteria가 `package.json` 무변경을 확인 |

**Canon-referral breadcrumb:** "estimatedMinutes 외 필드 훼손 금지"는 위 T-03-06이 이미 소유하므로 `must_haves.prohibitions`에 중복 발행하지 않았다.
</threat_model>

<flagged_assumptions>
## Flagged Assumptions (spec-less probe fallback §A)

SPEC.md 부재로 결정론적 edge 프로브가 4건 전부 `unclassified → unresolved`를 냈다. 이 Plan이 소유하는 1건:

| Req | 프로브 결과 | 이 Plan의 처리 |
|-----|------------|---------------|
| SCHED-03 | unclassified / unresolved — 한국어 요구사항 문구가 영어 휴리스틱에 걸리지 않음 | 수치 경계(총합 정확값 4,200, 값 집합 {150,90,60}만 존재, 스키마 하한 min(1) 정수, 하루 최대 150분)를 수동 도출해 `must_haves.truths`와 Invariant 6·12·13으로 명시 |

추가 가정:
- **분포 실측 근거:** "현재 270×20 / 180×10 / 150×2 / 120×3" 분포는 RESEARCH 세션의 전수 grep 결과이며 이 계획 세션에서도 재확인했다. Task 1의 verify가 수정 후 분포를 실증하므로, 원본 분포가 다르면 목표 분포 어설션에서 즉시 빨간불이 뜬다.
- **프로젝트 모듈 5개 전제:** `isProject: true`가 2-4 · 2-6 · 3-2 · 3-5 · 3-7 다섯 개라는 전제 위에 60분 배정 5건이 성립한다. Task 2의 게이트가 추출 개수 5를 직접 검사하므로 전제가 깨지면 게이트가 먼저 실패한다.
</flagged_assumptions>

<verification>
1. `npm run build` — mdx 편집 후 Velite 재생성 (건너뛰면 이후 모든 검사가 옛 값을 본다)
2. `node scripts/check-manifest.mjs` — 13개 불변식 (총합 4,200 · 분포 · 파생 규칙 포함)
3. `node scripts/check-schedule.mjs`, `node scripts/check-progress-math.mjs`, `node scripts/check-progress-gates.mjs` — 기존 게이트 무회귀
4. `node scripts/check-brand.mjs` — 브랜드 노출 게이트
5. `node --env-file=.env.local scripts/e2e-today.mjs` — 갱신된 소요시간이 실제 렌더에 반영되는지 왕복 확인
</verification>

<success_criteria>
- 35개 레슨 예상 소요시간 총합이 정확히 4,200분이고 분포가 150×20 / 90×10 / 60×5다
- 프론트매터의 다른 필드와 본문이 전혀 바뀌지 않았다
- 매니페스트 게이트가 총합·분포·파생 규칙 세 층을 상시 검사한다
- 계획 문서 3종의 페이스 기준이 D-35와 일치한다
</success_criteria>

<output>
Create `.planning/phases/03-schedule-and-today/03-02-SUMMARY.md` when done
</output>
