---
phase: 08-performance-and-mobile
plan: 03
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - src/app/lesson/[lessonId]/page.tsx
  - src/app/api/progress/route.ts
  - src/components/progress-slots.tsx
  - src/components/progress-skeleton.tsx
  - src/components/lesson-notepad.tsx
  - src/components/complete-button.tsx
  - scripts/check-progress-gates.mjs
  - scripts/e2e-progress.mjs
  - scripts/e2e-lesson-note.mjs
autonomous: true
requirements: [SC1, SC4, SC5, TRACK-01, TRACK-02, CONV-02]
user_setup: []

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "모든 레슨 라우트(`/lesson/<slug>`)가 빌드 시점에 미리 생성되고 `.next/prerender-manifest.json`의 `routes`에 전부 등장한다"
    - "잠금 쿠키를 가진 브라우저에서 완료 버튼이 뜨고, 누르면 즉시 반영되며, 새로고침 후에도 유지된다"
    - "잠금 쿠키가 없으면 완료 버튼 대신 잠금 안내 문구가 뜨고 메모장이 DOM에 아예 등장하지 않는다"
    - "메모장은 저장된 메모를 다 불러온 뒤에만 입력 가능해진다 — 빈 값 자동 저장이 실제 메모를 덮어쓰지 않는다"
    - "잠금 쿠키가 있어도 레슨 페이지 HTML 원문에는 진도 마커와 메모 본문이 0건이다"
    - "`node --env-file=.env.local scripts/e2e-progress.mjs`와 `scripts/e2e-lesson-note.mjs`가 둘 다 0으로 종료한다"
    - statement: "완료 버튼 자리에 로딩 중 같은 크기의 스켈레톤이 보이고, 데이터가 도착해도 레이아웃이 밀리지 않는다"
      verification: backstop
  artifacts:
    - src/app/lesson/[lessonId]/page.tsx
    - src/components/progress-slots.tsx
  key_links:
    - "정적 레슨 셸 → `<ProgressProvider lessonId>` → `GET /api/progress?lesson=<slug>` → `lesson.done` + `lesson.note`"
    - "완료 토글 → `toggleLessonComplete` Server Action → `useProgress().refresh()` → 슬롯 재렌더"
    - "`globals.css`의 `[data-progress-controls]:has([data-complete-state=\"done\"]) [data-pager=\"next\"]` 규칙 → 완료 버튼과 페이저가 같은 `data-progress-controls` 컨테이너 안에 있어야 계속 동작한다"
---

<objective>
트레이서(08-02)가 증명한 구조를 레슨 라우트로 넓힌다. 레슨 페이지는 이 사이트에서 쿠키 의존
조회가 가장 많은 화면이다 — 잠금 판정, 완료 진도, 메모 본문 셋이 페이지 본문에서 읽힌다. 셋을
전부 `/api/progress` 한 번의 왕복으로 옮기고 페이지를 완전 정적으로 만든다.

메모는 진도와 함께 온다. 왕복을 둘로 나누면 레슨 화면 하나에 두 번의 대기 구간이 생기고, 게이트도
두 벌이 된다. `?lesson=<slug>`가 있을 때만 응답에 `lesson` 필드가 붙는 08-02의 설계를 그대로 쓴다.

**메모장에는 정적 전환이 만드는 고유한 위험이 있다.** 지금은 서버가 `initialBody`를 내려주므로
메모장이 처음부터 실제 내용을 갖고 시작한다. fetch로 옮기면 "빈 textarea로 시작 → 잠시 후 내용
도착"이라는 구간이 생기고, 그 사이에 자동 저장 디바운스가 돌면 **실제 메모가 빈 값으로 덮어써진다.**
이 플랜은 그 경로를 구조적으로 막는다.

Purpose: 레슨 읽기 화면 — 이 사이트에서 가장 오래 머무는 화면 — 의 첫 바이트를 정적 라우트 수준으로
내린다.
Output: 35개 레슨 라우트 전부 프리렌더, 완료·메모 아일랜드, 갱신된 게이트 3종.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-performance-and-mobile/08-RESEARCH.md
@.planning/phases/08-performance-and-mobile/08-PATTERNS.md
@.planning/phases/08-performance-and-mobile/08-02-SUMMARY.md
</context>

<recorded_decisions>
- **D8-G: 메모 읽기를 `/api/progress?lesson=`에 합친다(별도 `/api/note`를 만들지 않는다).**
  (reversibility: reversible) 08-PATTERNS.md가 Open Question으로 남긴 지점이다. 합치는 이유는 왕복
  1회와 게이트 1벌. 쓰기 경로(`saveLessonNoteAction`)는 그대로 둔다 — 읽기만 이사한다.

- **D8-H: 메모장은 메모가 도착하기 전에는 입력을 받지 않는다.** (reversibility: reversible)
  로딩 구간에 `NotepadSkeleton`을 보여주고, `lesson.note`가 도착한 뒤에 비로소 `LessonNotepad`를
  마운트한다. `initialBody`를 나중에 prop으로 갈아끼우지 않는다 — 그 형태는 `lastSavedRef`/`valueRef`
  초기값과 어긋나 빈 값 자동 저장 경로를 남긴다.
</recorded_decisions>

<tasks>

<task type="auto">
  <name>Task 1: 레슨 페이지 정적 전환 + 완료·메모 아일랜드</name>
  <files>src/app/lesson/[lessonId]/page.tsx, src/app/api/progress/route.ts, src/components/progress-slots.tsx, src/components/progress-skeleton.tsx, src/components/complete-button.tsx, src/components/lesson-notepad.tsx</files>
  <read_first>
    - `src/app/lesson/[lessonId]/page.tsx` 전체 — 전환 대상. 특히 28행(route segment config 선언), 42행(`hasUnlockCookie`), 49행(`readCompletedLessonIds`), 52행(`readLessonNote`), 88~115행(`data-progress-controls` / `data-locked-notice` 분기), 117~130행(메모장 / `data-notepad-read-error` 분기)
    - `src/app/step/[stepId]/page.tsx` — 08-02가 전환한 자매 파일(그대로 따를 형태)
    - `src/app/api/progress/route.ts` — 08-02가 만든 핸들러. `lesson` 필드를 덧붙일 자리
    - `src/components/progress-provider.tsx` — `lessonId` prop, `refresh()` 시그니처
    - `src/components/progress-slots.tsx` — 08-02가 만든 슬롯 3종의 형태
    - `src/components/progress-skeleton.tsx` — 기존 스켈레톤 3종
    - `src/components/complete-button.tsx` 전체 — `initialDone` prop 수렴 방식, `data-progress-ui`/`data-complete-state` 훅, `min-h-11` 클래스(스켈레톤 높이를 맞출 기준)
    - `src/components/lesson-notepad.tsx` 1~50행 — `initialBody`가 `useState`/`lastSavedRef`/`valueRef` 초기값으로 세 군데 들어가는 구조
    - `src/lib/note-store.ts` — `readLessonNote()`의 `NoteRead` 타입
    - `src/app/globals.css` 560~573행 — `[data-progress-controls]:has([data-complete-state="done"]) [data-pager="next"]` 규칙. 완료 버튼과 페이저가 같은 컨테이너 안에 있어야 이 규칙이 계속 동작한다
    - `scripts/check-progress-gates.mjs` G12 — 완료 버튼이 `useOptimistic`을 쓰고 별도 로컬 done 상태를 두지 않아야 한다는 계약(깨뜨리면 안 됨)
  </read_first>
  <action>
**(a) `src/app/api/progress/route.ts`에 메모를 덧붙인다.**
`lesson` 검색 파라미터가 있고 `getLessonBySlug()` 검증을 통과했을 때만 `readLessonNote(slug)`를
부른다. 잠금 해제 전에는 절대 부르지 않는다 — `hasUnlockCookie()` 판정 뒤에만 실행되는 위치에 둔다.
응답의 `lesson` 필드를 `{ slug, done, note }`로 확장한다. `note`는 읽기 성공이면
`{ ok: true, body }`, 실패면 `{ ok: false }`다 — 실패 사유 문자열을 클라이언트에 노출하지 않는다.
잠금 상태에서는 `lesson`이 통째로 `null`이므로 메모 본문이 응답에 등장할 경로 자체가 없다.

**(b) `src/components/progress-skeleton.tsx`에 두 개를 추가한다.**
`CompleteButtonSkeleton` — `complete-button.tsx`의 실제 버튼이 쓰는 `min-h-11`과 `rounded-lg`,
`border` 구성을 그대로 공유해 높이·모서리가 일치하게 한다(레이아웃 시프트 0이 목적).
`NotepadSkeleton` — 접힌 상태 메모장 핸들과 같은 높이. 두 스켈레톤 모두 `.progress-skeleton`
클래스를 쓰고 `aria-hidden="true"`를 붙인다. 스피너를 쓰지 않는다.

**(c) `src/components/progress-slots.tsx`에 세 개를 추가한다.**
  - `CompleteButtonSlot({ lessonId })` — `loading`이면 `CompleteButtonSkeleton`, `ready`이면
    `<CompleteButton lessonId initialDone={data.lesson.done} onToggled={refresh} />`, `error`면
    기존 `<ProgressReadError />`, `locked`면 기존 잠금 문구를 그대로 렌더한다(문자열과
    `data-locked-notice` 속성을 지금 페이지에서 그대로 옮겨온다 — 문구를 새로 쓰지 않는다).
  - `LessonNoteSlot({ lessonId })` — `loading`이면 `NotepadSkeleton`, `ready`이고
    `data.lesson.note.ok`가 true면 `<LessonNotepad lessonId initialBody={...} />`, `ready`이고
    `ok`가 false면 기존 `data-notepad-read-error` 문구 그대로, `locked`/`error`면 아무것도
    렌더하지 않는다(잠금 상태에서 메모장이 DOM에 등장하지 않아야 한다는 기존 계약 유지).
  - `NotePageSpacerSlot` 은 만들지 않는다 — 대신 아래 (e)를 본다.

`CompleteButton`에 `onToggled?: () => void` optional prop을 추가하고, Server Action이 성공적으로
resolve된 직후 호출한다. `useOptimistic`/`useTransition` 구조와 "별도 로컬 done 상태를 두지 않는다"는
규율은 절대 바꾸지 않는다 — G12가 검사한다. 실패 시에는 호출하지 않는다(낙관적 값이 prop으로
수렴하는 기존 동작 유지).

**(d) `src/components/lesson-notepad.tsx`.**
컴포넌트 시그니처와 내부 로직은 그대로 둔다 — `initialBody`는 여전히 필수 prop이고, 이 컴포넌트는
값이 확정된 뒤에만 마운트된다(D8-H). 바꿀 것은 하나뿐이다: 헤더 주석에 "이 컴포넌트는 메모가
도착한 뒤에만 마운트된다. `initialBody`를 나중에 갈아끼우지 않는다"는 계약을 명시한다.

**(e) `src/app/lesson/[lessonId]/page.tsx` 정적 전환.**
route segment config `dynamic` 선언 줄과 그 위 주석 블록을 지운다. `hasUnlockCookie()`,
`readCompletedLessonIds()`, `readLessonNote()` 호출과 `unlocked`/`progressRead`/`noteRead`/`showNotepad`
계산, 관련 import를 전부 제거한다. `generateStaticParams()`는 그대로 둔다. `notFound()` 분기를
`getLessonBySlug()` 직후로 옮긴다.

`<main>`의 `note-page-spacer` 조건부 클래스가 `showNotepad`에 의존했다. 서버는 이제 메모장 표시
여부를 모른다. `note-page-spacer`를 **항상** 붙인다 — 잠금 상태에서 하단 여백이 조금 남는 것이
레이아웃 시프트보다 낫고, 이 클래스는 하단 패딩만 준다. 그 이유를 주석으로 남긴다.

본문 전체를 `<ProgressProvider lessonId={lesson.slug}>`로 감싼다. 완료/잠금 분기 자리에는 항상
`<div data-progress-controls className="flex flex-col gap-6"><CompleteButtonSlot lessonId={lesson.slug} /><LessonPager prev={prev} next={next} /></div>`
를 렌더한다 — 두 갈래 div를 하나로 합친다. `data-progress-controls`가 완료 버튼과 페이저를 함께
감싸야 `globals.css` 560~573행의 `:has()` 규칙이 계속 동작한다.
`</article>` 뒤에 `<LessonNoteSlot lessonId={lesson.slug} />`를 둔다.

`SectionTape`, `MDXContent`, `LessonBreadcrumb`, 깊이 배지·소요시간, "콘텐츠 준비 중입니다" 분기는
전부 정적 셸에 그대로 남는다 — 매니페스트 파생값이라 쿠키와 무관하다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node -e "const m=require('./.next/prerender-manifest.json');const L=JSON.parse(require('node:fs').readFileSync('.velite/lessons.json','utf8'));const need=L.map(l=>'/lesson/'+l.slug);const miss=need.filter(r=>!m.routes[r]);if(miss.length){console.error('NOT PRERENDERED ('+miss.length+'):',miss.slice(0,5).join(', '));process.exit(1)};const isr=need.filter(r=>typeof m.routes[r].initialRevalidateSeconds==='number');if(isr.length){console.error('UNEXPECTED ISR:',isr.join(', '));process.exit(1)};console.log('OK: '+need.length+' lesson routes prerendered')"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` 가 0으로 종료한다
    - `.velite/lessons.json` 의 모든 슬러그에 대해 `/lesson/<slug>` 가 `.next/prerender-manifest.json` 의 `routes` 에 존재하고 `initialRevalidateSeconds` 가 숫자가 아니다
    - 잠금 쿠키를 실은 `curl -s /lesson/1-1-course-orientation` **HTML 원문**에 `data-progress-ui` 문자열이 0건, `data-notepad` 문자열이 0건이다
    - 잠금 쿠키 **없이** Chromium으로 같은 페이지를 열면 `data-locked-notice` 요소가 나타나고 `[data-notepad]` 는 끝까지 나타나지 않는다
    - 잠금 쿠키를 실어 열면 `[data-progress-island][data-progress-state="ready"]` 이후 `[data-complete-state]` 와 `[data-notepad]` 가 둘 다 나타난다
    - Supabase에 미리 저장해 둔 메모가 있는 레슨을 열면, `[data-notepad-input]` 의 최초 `value` 가 그 메모 본문과 정확히 일치한다 (빈 문자열이었다가 채워지는 중간 상태가 관측되지 않는다)
    - 잠금 쿠키를 실은 `GET /api/progress?lesson=<존재하지-않는-슬러그>` 응답의 `lesson` 이 `null` 이고 status가 200이다
    - 잠금 쿠키 **없이** `GET /api/progress?lesson=1-1-course-orientation` 응답 JSON 어디에도 메모 본문 문자열이 없다
    - `node scripts/check-progress-gates.mjs` 의 G12 항목이 실패하지 않는다
    - `node scripts/check-supabase-note.mjs` 가 0으로 종료한다
    - `git diff package.json` 이 비어 있다
  </acceptance_criteria>
  <done>레슨 라우트 전체가 프리렌더되고, 완료 토글·메모 저장이 여전히 정확하게 동작하며, 잠금 상태에서 진도와 메모가 HTML에 아예 등장하지 않는다.</done>
  <reversibility rating="costly">메모 읽기 경로를 서버 렌더에서 fetch로 옮긴다. 되돌리려면 페이지·슬롯·핸들러를 함께 되돌려야 하지만 데이터 마이그레이션이 없어 one-way는 아니다.</reversibility>
</task>

<task type="auto">
  <name>Task 2: G9 STATIC_SHELL_PAGES에 레슨 페이지 추가 + e2e-progress b~e 이행</name>
  <files>scripts/check-progress-gates.mjs, scripts/e2e-progress.mjs</files>
  <read_first>
    - `scripts/check-progress-gates.mjs` — 08-02가 만든 `STATIC_SHELL_PAGES` 상수 위치와 검사 로직
    - `scripts/e2e-progress.mjs` — 08-02가 추가한 `renderedHtml(url, { cookie })` 헬퍼와 h5 시나리오, 그리고 b·c·d·e 시나리오(약 345~410행)
    - `.planning/phases/08-performance-and-mobile/08-02-SUMMARY.md` — `STATIC_SHELL_PAGES`에 이 플랜이 추가할 항목이 명시돼 있다
  </read_first>
  <action>
**(a) `scripts/check-progress-gates.mjs`.**
`STATIC_SHELL_PAGES` 배열에 `src/app/lesson/[lessonId]/page.tsx`를 추가한다. 검사 로직은 08-02가
만든 것을 그대로 쓴다 — route segment config `dynamic` 선언 부재와 네 식별자(`hasUnlockCookie`,
`readCompletedLessonIds`, `readLessonNote`, `cookies(`) 부재를 주석 제거 후 검사한다. 다른 게이트는
손대지 않는다.

**(b) `scripts/e2e-progress.mjs`의 b·c·d·e 시나리오.**
  - b(쿠키 없는 레슨 응답): 원문 fetch로 그대로 둔다. `data-progress-ui` 0건과 레슨 제목 존재
    어설션 둘 다 정적 셸에서도 참이다. 실패 메시지 문구만 정적 셸 맥락으로 갱신한다.
  - c·d·e: `await res.text()`를 08-02가 만든 `renderedHtml(...)`로 교체한다. `data-complete-state="todo"`
    / `"done"` 어설션 문자열은 그대로 둔다.

**(c) 신규 시나리오 f 추가.** 잠금 쿠키를 **실은 채로** 레슨 라우트를 **원문 fetch**했을 때
`data-progress-ui`와 `data-notepad` 둘 다 0건임을 검사한다. h5의 레슨 버전이다 — 정적 셸이 진도도
메모도 담지 않는다는 런타임 증명(T-08-03-01·T-08-03-02).

**(d) 신규 시나리오 g 추가.** 브라우저에서 완료 버튼을 실제로 클릭해 `data-complete-state`가
`todo`에서 `done`으로 바뀌고, **페이지를 새로 열었을 때도** `done`으로 유지되는지 검사한다.
현재 c·d·e는 Supabase를 직접 조작한 뒤 페이지를 다시 여는 방식이라 "토글 → refresh → 슬롯 재렌더"
경로를 한 번도 지나가지 않는다. SC1의 "완료 토글이 즉시 반영된다"를 실제로 증명하는 시나리오다.
클릭 후 Supabase에서 프로브 행을 직접 삭제해 정리한다(기존 시나리오들의 정리 관례 그대로).

어떤 출력에도 쿠키 값·시크릿을 찍지 않는다.
  </action>
  <verify>
    <automated>node scripts/check-progress-gates.mjs &amp;&amp; node --env-file=.env.local scripts/e2e-progress.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-progress-gates.mjs` 가 `all gates passed` 를 출력하고 0으로 종료한다
    - `node --env-file=.env.local scripts/e2e-progress.mjs` 가 0으로 종료하고, 로그에 b·c·d·e·f·g·h1~h5·i1~i5 전 시나리오가 통과로 찍힌다
    - `src/app/lesson/[lessonId]/page.tsx` 에 진도를 서버에서 다시 읽는 코드를 임시로 되살려 실행하면 G9와 시나리오 f가 둘 다 실패한다 — 검사 후 원복한다
    - `CompleteButton` 의 `onToggled` 호출을 임시로 지우고 실행하면 시나리오 g가 실패한다 — 검사 후 원복한다
    - 출력 어디에도 `UNLOCK_SECRET` 값이 등장하지 않는다
  </acceptance_criteria>
  <done>레슨 정적 셸 계약이 게이트로 고정되고, 완료 토글의 즉시 반영이 클릭 경로로 실제 증명된다.</done>
</task>

<task type="auto">
  <name>Task 3: e2e-lesson-note.mjs를 fetch 지연 구간에 맞춰 조정</name>
  <files>scripts/e2e-lesson-note.mjs</files>
  <read_first>
    - `scripts/e2e-lesson-note.mjs` 전체 — 특히 251~270행(브라우저·컨텍스트·쿠키 설정), 261행(`waitForSelector('[data-notepad]')`), 355~470행(입력·자동 저장·재방문 시나리오), 425~450행(reload 후 `[data-notepad-input]` 대기), 585~600행(잠금 컨텍스트에서 `data-notepad` 부재 확인), 605~625행(뷰포트 반복)
    - `src/components/progress-provider.tsx` — `data-progress-state` 값 4종
    - `src/components/progress-slots.tsx` — `LessonNoteSlot`의 상태별 렌더
  </read_first>
  <action>
메모장이 이제 fetch 이후에 마운트되므로, 마운트를 전제하고 곧바로 조작하는 지점들이 타이밍에
민감해진다. 최소 변경으로 맞춘다.

페이지를 여는 모든 지점에서 `page.waitForSelector('[data-notepad]')` **앞에**
`page.waitForSelector('[data-progress-island]:not([data-progress-state="loading"])')`를 추가한다.
헬퍼 함수 하나(`waitForProgressSettled(page)`)로 뽑아 중복을 없앤다. 기존 `waitForSelector` 호출과
이후 어설션은 그대로 둔다.

reload 후 `[data-notepad-input]`을 기다리는 지점(약 425~450행)에도 같은 헬퍼를 먼저 넣는다.

**잠금 컨텍스트 시나리오(약 585~600행)를 강화한다.** 지금은 `page.content()`에 `data-notepad`가
없음을 한 번 확인한다. 정적 셸에서는 이것이 수화 전에도 참이라 위양성 통과가 가능하다. 그래서
`waitForProgressSettled(page)`를 먼저 부르고 `data-progress-state` 값이 `locked`인 것을 확인한
다음에 `data-notepad` 부재를 확인하도록 순서를 바꾼다 — "아직 안 그려진 것"과 "잠겨서 안 그린 것"을
구분한다.

**신규 시나리오 추가: 빈 값 덮어쓰기 방지.** Supabase에 프로브 레슨의 메모를 알아볼 수 있는 고유
문자열로 미리 심어 둔다. 그 레슨 페이지를 열고, 진도가 정착되기를 기다리지 **않은 채**
자동 저장 디바운스(`SAVE_DEBOUNCE_MS` = 1000ms)보다 긴 2500ms를 그냥 기다린다. 그다음 Supabase에서
그 행을 다시 읽어 본문이 여전히 원래 고유 문자열인지 확인한다. 빈 값으로 덮어써졌으면 실패다.
이 시나리오가 D8-H(메모 도착 전에는 마운트하지 않는다)를 검사한다. 검사 후 프로브 행을 정리한다.

어떤 출력에도 쿠키 값·시크릿·메모 본문 전체를 찍지 않는다(고유 문자열 일치 여부만 보고한다).
  </action>
  <verify>
    <automated>node --env-file=.env.local scripts/e2e-lesson-note.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --env-file=.env.local scripts/e2e-lesson-note.mjs` 가 0으로 종료한다
    - 로그에 기존 시나리오 전부와 신규 "빈 값 덮어쓰기 방지" 시나리오가 통과로 찍힌다
    - `LessonNoteSlot` 을 임시로 "로딩 중에도 빈 `initialBody` 로 `LessonNotepad` 를 마운트" 하도록 바꾸면 신규 시나리오가 실패한다 — 검사 후 원복한다
    - 잠금 컨텍스트 시나리오가 `data-progress-state` 값 `locked` 확인 후에 부재 확인을 하도록 순서가 바뀌었다
    - `node --env-file=.env.local scripts/e2e-mobile-overflow.mjs` 가 0으로 종료한다 (메모장 하단 시트 기하 회귀 없음)
  </acceptance_criteria>
  <done>메모장이 fetch 이후 마운트 구조에서도 기존 계약을 전부 지키고, 정적 전환이 새로 만든 "빈 값 덮어쓰기" 경로가 자동으로 차단됨이 증명된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 브라우저 → `GET /api/progress?lesson=<slug>` | 메모 **본문**이 처음으로 HTTP 응답 표면에 실린다 |
| 정적 레슨 셸 HTML → 엣지 캐시 | 전 사용자 공유 캐시. 메모 본문이 여기 섞이면 즉시 유출 |
| 메모장 자동 저장 → `saveLessonNoteAction` | 클라이언트 상태가 서버 데이터를 덮어쓴다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-03-01 | Information Disclosure | `GET /api/progress?lesson=` 잠금 없는 호출 | high | mitigate | `readLessonNote()`를 `hasUnlockCookie()` 판정 통과 이후에만 호출. 잠금 상태 응답은 `lesson: null`이라 메모 본문이 실릴 경로 자체가 없다. Task 1 수용 기준이 이 응답을 직접 검사한다 |
| T-08-03-02 | Information Disclosure | 정적 레슨 셸에 메모 잔류 | high | mitigate | 페이지에서 `readLessonNote()` 호출 제거(G9 `STATIC_SHELL_PAGES`가 정적 검사). e2e 시나리오 f가 "쿠키를 실어도 원문 HTML에 `data-notepad` 0건"을 런타임 검사 |
| T-08-03-03 | Tampering | 메모 빈 값 덮어쓰기 | high | mitigate | 메모가 도착한 뒤에만 `LessonNotepad`를 마운트(D8-H). Task 3의 신규 e2e 시나리오가 2500ms 대기 후 Supabase 본문 보존을 직접 확인 |
| T-08-03-04 | Information Disclosure | 메모 읽기 실패 사유 노출 | medium | mitigate | 응답의 `note`는 실패 시 `{ ok: false }`만 담는다 — DB 오류 문자열을 클라이언트로 내보내지 않는다 |
| T-08-03-05 | Spoofing | `?lesson=` 임의 슬러그 | medium | mitigate | `getLessonBySlug()` 검증 후에만 사용. 미존재 슬러그는 오류가 아니라 `lesson: null` |
| T-08-03-06 | Tampering | npm/pip/cargo installs | high | mitigate | 이 플랜은 패키지를 설치하지 않는다 — Task 1 수용 기준에 `git diff package.json` 무변경 명시 |
</threat_model>

<verification>
- `npm run build` → `.next/prerender-manifest.json`에 레슨 라우트 전량 존재
- `node scripts/check-progress-gates.mjs` → 0
- `node scripts/check-supabase-note.mjs` → 0
- `node scripts/check-supabase-progress.mjs` → 0
- `node --env-file=.env.local scripts/e2e-progress.mjs` → 0
- `node --env-file=.env.local scripts/e2e-lesson-note.mjs` → 0
- `node --env-file=.env.local scripts/e2e-mobile-overflow.mjs` → 0
- `node --env-file=.env.local scripts/e2e-section-tape.mjs` → 0
- `node --env-file=.env.local scripts/e2e-typography.mjs` → 0
- `node scripts/check-design-tokens.mjs` → 0
- `node scripts/check-brand.mjs` → 0 (스켈레톤·잠금 문구 등 신규/이전된 UI 문구에 금지 기관명 0건)
- `node scripts/check-route-rendering.mjs` → 남은 실패가 `/curriculum` 하나로 좁혀졌는지 확인
- `node --env-file=.env.local scripts/e2e-perf-budget.mjs` → `/lesson/*` TTFB 중앙값을 기록
</verification>

<success_criteria>
- 레슨 라우트 전량이 빌드 시점에 미리 생성된다
- 완료 토글이 클릭 경로로 즉시 반영되고 재방문 후에도 유지된다
- 잠금 쿠키 유무가 완료 버튼/잠금 문구/메모장 표시를 올바르게 가른다
- 저장된 메모가 빈 값으로 덮어써지지 않는다(자동 검증됨)
- 쿠키가 있어도 레슨 HTML 원문에 진도·메모가 0건이다
- 기존 게이트 16종 + 신규 게이트가 전부 통과한다
- 새 npm 패키지 0개
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (이 플랜 몫)

**신규 export**
- `src/components/progress-slots.tsx` — `CompleteButtonSlot`, `LessonNoteSlot`
- `src/components/progress-skeleton.tsx` — `CompleteButtonSkeleton`, `NotepadSkeleton`
- `src/components/complete-button.tsx` — `CompleteButton`에 optional `onToggled` prop 추가

**확장된 API 응답 필드**
- `GET /api/progress?lesson=<slug>` → `lesson: { slug, done, note: { ok: true, body } | { ok: false } } | null`

**변경된 게이트**
- `G9` `STATIC_SHELL_PAGES` += `src/app/lesson/[lessonId]/page.tsx`

**신규 e2e 시나리오**
- `e2e-progress.mjs` f — 쿠키를 실어도 레슨 정적 셸 원문에 진도·메모 마커 0건
- `e2e-progress.mjs` g — 완료 버튼 실제 클릭 → 즉시 반영 → 재방문 유지
- `e2e-lesson-note.mjs` — 메모 빈 값 덮어쓰기 방지

**제거된 표면**
- `src/app/lesson/[lessonId]/page.tsx`의 route segment config `dynamic` 선언, `hasUnlockCookie`/`readCompletedLessonIds`/`readLessonNote` 호출, `showNotepad` 조건부 `note-page-spacer` 분기
</artifacts_produced>

<output>
Create `.planning/phases/08-performance-and-mobile/08-03-SUMMARY.md` when done.
SUMMARY에 반드시 포함할 것: `/lesson/*` TTFB 전후 숫자, 프리렌더된 레슨 라우트 개수(실측),
`e2e-lesson-note.mjs`에서 조정한 대기 지점 목록, `STATIC_SHELL_PAGES`의 현재 내용.
</output>
