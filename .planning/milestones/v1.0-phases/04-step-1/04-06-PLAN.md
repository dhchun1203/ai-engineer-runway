---
phase: 04-step-1
plan: 06
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/content/lessons/step-1/1-5-ml-model-types.mdx
  - src/content/lessons/step-1/1-5-ml-metrics-and-pipeline.mdx
autonomous: true
requirements: [CONT-02, CONT-03]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`/lesson/1-5-ml-model-types`가 분류 · 회귀 · 군집 세 가지 모델 유형을 각각 비유와 실행 가능한 예제로 다룬다 (curriculum.md 1-5 첫 불릿)"
    - "`/lesson/1-5-ml-metrics-and-pipeline`이 평가 지표(정확도·정밀도·재현율 계열)와 scikit-learn Pipeline 구성을 실행 가능한 예제로 다룬다 (curriculum.md 1-5 두 번째 불릿)"
    - "두 레슨의 모든 데이터가 scikit-learn 내장 데이터셋(`load_iris` · `load_diabetes`)에서 오고, 외부 파일 다운로드·네트워크 접근이 필요한 단계가 없다 (Claude's Discretion — 재현성 확보)"
    - "두 레슨이 같은 `load_iris` 데이터를 재사용해, 학습자가 새 데이터 구조를 두 번 익히지 않는다 (D-49 — 글이 시간을 채우지 않는다)"
    - "1-5-ml-metrics-and-pipeline의 예제가 학습용과 평가용 데이터를 나눠(`train_test_split`) 지표를 계산한다 — 학습에 쓴 데이터로 자기 성적을 매기는 형태를 정상으로 가르치지 않는다"
    - "실행 방법이 `powershell` 펜스의 `pip install scikit-learn` + `py 파일명`이고 macOS는 한 줄 병기된다 (D-55, D-58, RESEARCH Pitfall 2)"
    - "설치 패키지 이름이 `scikit-learn`으로, import 이름이 `sklearn`으로 정확히 구분되어 안내된다 (RESEARCH Package Legitimacy Audit — 이름 불일치 주의)"
    - "두 레슨 모두 6개 헤딩이 원문 그대로 순서대로 있고, `### 해보기` 2~3개와 접힌 `<details><summary>정답 보기</summary>` 정답 블록, `**이 레슨의 단어**` 5~8행 표를 갖는다 (D-47, D-49, D-50, D-61)"
    - "두 레슨의 `hasContent`가 `false`에서 `true`로 바뀌고, 그 외 프론트매터 7개 필드는 바이트 단위로 동일하다 (D-13)"
    - "`node scripts/check-lesson-structure.mjs`와 `node scripts/check-brand.mjs`가 exit 0이고, `npm run build`가 성공한다"
    - statement: "두 레슨의 읽기 산문이 30분 안쪽으로 읽히고 나머지 시간은 해보기 과제가 채운다 (D-49)"
      verification: backstop
    - statement: "예제 코드가 실제 로컬 Python + scikit-learn 환경에서 오류 없이 실행되고 본문이 설명한 형태의 출력을 낸다"
      verification: backstop
  artifacts:
    - src/content/lessons/step-1/1-5-ml-model-types.mdx
    - src/content/lessons/step-1/1-5-ml-metrics-and-pipeline.mdx
  key_links:
    - "1-1-dev-environment-setup의 Python 설치 → 이 두 레슨의 `pip install scikit-learn` — 앞 레슨이 세운 실행 경로(`py`)를 그대로 쓴다"
    - "1-3 두 편(변수·자료형·조건문·반복문 / 함수·예외·파일) → 이 레슨의 코드 읽기 — 여기서 처음으로 남이 만든 라이브러리를 조립해 쓴다"
    - "`load_iris` 재사용(1-5 두 편 공통) → 학습 비용 절감 — 두 번째 레슨은 데이터 설명을 반복하지 않고 지표·파이프라인에만 집중한다"
    - "`Pipeline(StandardScaler + 분류기)` → Step 2·3의 모델 서빙 — 전처리와 모델을 한 덩어리로 묶는 습관이 여기서 시작된다"
  prohibitions:
    - statement: "레슨 본문·코드 주석·메타데이터 어디에도 교육기관명(`scripts/check-brand.mjs`의 `FORBIDDEN_BRAND_STRINGS`에 하드코딩된 문자열)을 노출해서는 안 된다 — 항상 'AI Engineer 교육과정'으로 표기한다 (D-02)"
      status: unverified
    - statement: "외부 데이터셋 다운로드(CSV 링크, 공개 데이터 플랫폼 계정, `fetch_*` 계열 네트워크 로더)를 실습 경로로 안내해서는 안 된다 — 내장 데이터셋만 쓴다"
      status: unverified
    - statement: "브라우저 기반 온라인 노트북 실행 환경을 실습 경로로 안내해서는 안 된다 — 실습은 PC 로컬 Python으로 통일한다 (D-55)"
      status: unverified
    - statement: "정확도·정밀도·재현율 같은 평가 지표를 수식으로 직접 구현해 보여서는 안 된다 — 표준 라이브러리의 지표 함수를 쓴다. 개념 설명은 비유와 말로 한다 (RESEARCH Don't Hand-Roll)"
      status: unverified
    - statement: "Mermaid·SVG·이미지·차트·ASCII 다이어그램·새 MDX 컴포넌트를 도입해서는 안 된다 — 모델 결과는 표와 출력 텍스트로만 보여준다 (D-48)"
      status: unverified
    - statement: "프론트매터의 `hasContent` 외 어떤 필드도 바꿔서는 안 된다 (D-13)"
      status: unverified
---

<objective>
Step 1 모듈 1-5(머신러닝 기초 모델링) 2편을 승인된 집필 표준으로 쓴다 — 학습자가 분류·회귀·군집을 실제로 한 번씩 돌려 보고, 모델의 성적을 어떻게 매기는지, 전처리와 모델을 어떻게 한 덩어리로 묶는지 익히게 한다.

Purpose: Step 1의 마지막 두 편이자 학습자가 처음으로 "학습된 모델"을 손으로 만들어 보는 지점이다. 여기까지 오면 Step 1이 끝나고 진행률이 100%가 되며, 사전학습의 첫 덩어리가 닫힌다.

Output: 읽고 실습할 수 있는 레슨 2편, `hasContent: true` 전환 완료.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-step-1/04-CONTEXT.md
@.planning/phases/04-step-1/04-RESEARCH.md
@.planning/phases/04-step-1/04-PATTERNS.md
@.planning/phases/04-step-1/04-01-SUMMARY.md
</context>

<wave_2_shared_rules>
## Wave 2 공통 규칙 (Plan 02~06 동일)

1. **표준의 출처는 Plan 01의 결과물이다.** `04-01-SUMMARY.md`와 승인된 `src/content/lessons/step-1/1-3-python-variables-and-types.mdx`가 유일한 실물 기준이다. SUMMARY에 사용자 수정 요청이 있으면 그것이 최우선이다.
2. **`node scripts/check-manifest.mjs`는 이 Plan에서 실행하지 않는다.** Invariant 10의 기대 개수는 아직 2인데 이 Plan이 `hasContent: true`를 늘리므로 **의도적으로 red다.** Plan 07이 상수를 2 → 11로 올릴 때 green이 된다. 이 red를 고치려고 `scripts/check-manifest.mjs`를 건드리면 다른 Wave 2 Plan과 충돌한다 (RESEARCH Pitfall 5).
3. **이 Plan의 게이트는 세 개다:** `npm run build`, `node scripts/check-brand.mjs`, `node scripts/check-lesson-structure.mjs`.
4. **`src/` 아래 `.mdx` 외 어떤 파일도 바꾸지 않는다** (D-48 "플랫폼 변경 0"). `package.json`에 Python 관련 항목을 추가하지 않는다 — `scikit-learn`은 학습자의 로컬 Python 환경 이야기이지 이 저장소의 의존성이 아니다.
5. **사람 검토 체크포인트를 두지 않는다** (D-59). 품질 방어선은 위 세 게이트와, 실행자가 예제 코드를 직접 실행해 보는 습관 두 가지뿐이다 — 이 Plan은 특히 실행이 중요하다. ML 예제는 문법이 맞아도 결과가 본문 설명과 다를 수 있다.
6. **6개 헤딩은 이 문자열 그대로:** `## 1. 학습 목표` · `## 2. 왜 배우나` · `## 3. 개념 설명` · `## 4. 실무 예제` · `## 5. 실무 팁` · `## 6. 핵심 정리 및 스스로 점검`. `##` 레벨 헤딩은 이 6개뿐이다.
7. **절 구성 표준(Plan 01에서 확정):** ①은 동사로 끝나는 결과 3~4개 · ②는 최대 4문장 1문단 · ③은 이모지 붙은 `###` 소제목 3~4개(비유 먼저, 정의 나중) · ④는 완결 실행 가능 예제 + 실행 방법 펜스 + 예상 출력 설명 + `### 해보기` 2~3개(각각 접힌 정답 블록) · ⑤는 불릿 3~4개(각 2문장 이내) · ⑥은 `**핵심 정리**` 불릿 4~5개 + `**이 레슨의 단어**` 5~8행 표(`| 단어 | 뜻 |`) + `**스스로 점검**` 질문 2개(각각 접힌 정답 블록).
8. **정답 블록 형식(빈 줄 필수):** `<details>` 줄 → `<summary>정답 보기</summary>` 줄 → **빈 줄** → 내용 → **빈 줄** → `</details>` 줄 (RESEARCH Pitfall 3, 게이트 L4가 잡는다).
9. **용어 표기(D-50):** 첫 등장은 "한글(English, 한 줄 풀이)", 이후는 한글만, 코드 안에서는 영어 그대로.
10. **OS 기준(D-58):** Windows PowerShell이 기본. 실행 명령은 `py 파일명`이 1차이고 macOS는 `python3 파일명` 한 줄 병기.
11. **모든 코드펜스에 언어 태그를 단다.** 허용 태그: `python` · `sql` · `bash` · `powershell` · `text`.
</wave_2_shared_rules>

<ml_content_contract>
## 이 Plan의 ML 콘텐츠 계약 (두 Task 공통)

- **데이터는 내장 데이터셋만 쓴다.** 분류·군집은 `sklearn.datasets.load_iris`, 회귀 스니펫은 `sklearn.datasets.load_diabetes`. 외부 CSV·URL·공개 데이터 플랫폼 계정·네트워크 로더(`fetch_*` 계열)를 쓰지 않는다 — 재현성이 이 phase의 실습 기준이다.
- **두 레슨이 같은 `load_iris`를 재사용한다.** 두 번째 레슨은 데이터 구조 설명을 반복하지 않고 "1-5 앞 레슨에서 쓴 그 데이터"라고 가리킨 뒤 지표·파이프라인에만 집중한다.
- **결과의 재현성을 확보한다.** 난수가 개입하는 곳(`KMeans`, `train_test_split`)에 `random_state`를 고정값으로 준다. 그러지 않으면 본문의 예상 출력과 학습자의 실제 출력이 매번 달라져 학습자가 자기가 틀린 줄 안다.
- **지표를 직접 구현하지 않는다.** `sklearn.metrics`의 함수를 쓰고, 개념 설명은 비유와 말로 한다.
- **모델을 튜닝하지 않는다.** 이 두 레슨의 목표는 "모델이 무엇을 하는지 감을 잡는 것"이지 성능을 올리는 것이 아니다. 하이퍼파라미터 탐색·교차검증은 Step 2 이후 범위다.
- **출력은 텍스트로만 보여준다.** 그래프·산점도·시각화 라이브러리를 쓰지 않는다 (D-48).
</ml_content_contract>

<artifacts_produced>
## Artifacts this phase produces

**Plan 06 (이 Plan)**
- `src/content/lessons/step-1/1-5-ml-model-types.mdx` — 본문 신규 작성, `hasContent: false → true`
- `src/content/lessons/step-1/1-5-ml-metrics-and-pipeline.mdx` — 본문 신규 작성, `hasContent: false → true`

**Phase 4 전체 산출물 목록은 `04-01-PLAN.md`의 `<artifacts_produced>` 참고.**
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 1-5-ml-model-types — 우편함 나누기(분류) · 선 긋기(회귀) · 끼리끼리 묶기(군집)</name>
  <files>src/content/lessons/step-1/1-5-ml-model-types.mdx</files>
  <read_first>
    - `src/content/lessons/step-1/1-5-ml-model-types.mdx` — 현재 스텁. 프론트매터(`title: "분류·회귀·군집 모델 이해"`, `moduleId: "1-5"`, `order: 1`) 유지 대상.
    - `src/content/lessons/step-1/1-3-python-variables-and-types.mdx` — 승인된 파일럿. 6단 골격·톤·`python` 펜스와 `powershell` 실행 펜스의 정확한 형태.
    - `.planning/phases/04-step-1/04-01-SUMMARY.md` — 확정값과 사용자 수정 요청.
    - `.planning/phases/04-step-1/04-RESEARCH.md` — `## Code Examples` §scikit-learn 분류+군집 최소 예제(전문: `load_iris` → `LogisticRegression(max_iter=200)` → `KMeans(n_clusters=3, n_init=10, random_state=0)` 패턴과 그 아래 `powershell` 실행 블록), `## Package Legitimacy Audit`의 `scikit-learn` 행과 그 아래 이름 불일치 주석, §Per-Lesson Content Outline의 `1-5-ml-model-types` 행, `## Common Pitfalls` Pitfall 2(`py` 런처).
    - `.planning/curriculum.md` §Step 1 §1-5 첫 불릿 — "분류, 회귀, 군집 모델 이해"가 범위 원천이다.
    - `src/content/lessons/step-1/1-3-python-functions-and-io.mdx` (Plan 04 산출물, 있으면) — 학습자가 직전에 어디까지 배웠는지 확인해 설명 수준을 맞춘다.
  </read_first>
  <action>
`1-5-ml-model-types.mdx`의 본문을 새로 쓰고 프론트매터의 `hasContent`를 `false`에서 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `<ml_content_contract>`를 예외 없이 지킨다.

비유의 세 축: **분류 = 우편함을 종류별로 나누기**(정답 이름표가 이미 있다), **회귀 = 점들 사이에 선을 긋고 다음 값을 맞히기**(정답이 숫자다), **군집 = 정답 없이 비슷한 것끼리 묶기**(이름표가 없다). 세 유형의 차이를 "정답이 있는가, 정답이 이름인가 숫자인가"라는 한 가지 질문으로 꿰면 학습자가 유형 구분을 외우지 않아도 된다.

절별 목표:
- ① 학습 목표 — "분류·회귀·군집이 각각 어떤 문제를 푸는지 구분해 설명할 수 있다", "정답 데이터가 있는 학습과 없는 학습의 차이를 말할 수 있다", "내장 데이터셋으로 분류 모델을 학습시키고 예측을 뽑을 수 있다", "군집 결과가 정답과 어떻게 다른지 관찰할 수 있다" 계열 4개.
- ② 왜 배우나 — 앞으로 만날 AI 서비스가 결국 이 세 가지 중 하나에 얹힌다는 것, 그리고 문제를 보고 어느 유형인지 판단하는 것이 첫 단추라는 것. 4문장 이내.
- ③ 개념 설명 — 이모지 `###` 소제목 3~4개.
  - "정답이 있느냐 없느냐" — 지도 학습(supervised learning)과 비지도 학습(unsupervised learning)을 한 줄 풀이와 함께 구분. 용어 첫 등장은 D-50 형식.
  - "세 유형 한눈에" — 4열 표(유형 · 무엇을 맞히나 · 일상 비유 · 이 레슨의 예). 우편함/선 긋기/끼리끼리 묶기가 비유 열에 온다.
  - "이번에 쓸 데이터" — `load_iris`가 무엇인지 짧게(꽃 150송이, 특징 4개, 품종 3종). 왜 이 데이터를 쓰는지(설치만 하면 바로 있다, 크기가 작아 즉시 돌아간다)를 한 줄. 데이터 표 예시 3~4행을 마크다운 표로 보여준다.
  - "학습과 예측" — `fit`으로 배우고 `predict`로 맞힌다는 두 단어를 축으로. 여기서 "모델은 규칙을 외우는 게 아니라 패턴을 요약한다"를 한 문단.
- ④ 실무 예제 — 완결된 단일 `python` 펜스 하나. 파일명은 `ml_types.py`, 첫 줄은 파일명 주석. 구성 순서:
  1. 내장 데이터 불러오기와 모양 확인(샘플 수·특징 수·품종 이름 출력).
  2. **분류** — 로지스틱 회귀 계열 분류기를 만들어 `fit`, 앞 5개 샘플에 대한 `predict` 결과와 실제 정답을 나란히 출력.
  3. **군집** — `KMeans`로 정답 없이 3덩어리로 묶고 앞 5개의 군집 번호를 출력. 군집 번호가 품종 번호와 일치할 이유가 없다는 것을 코드 주석과 아래 산문에서 짚는다(초심자가 가장 많이 오해하는 지점).
  4. **회귀** — 짧은 스니펫으로 `load_diabetes`를 불러 선형 회귀를 `fit`하고 앞 3개 예측값과 실제값을 출력. 여기서는 정확도가 아니라 "출력이 이름이 아니라 숫자"라는 차이만 보여준다.
  난수가 개입하는 곳에 `random_state` 고정값을 준다. 코드 전체가 100줄을 넘지 않게 압축한다.
  - 실행 방법을 `powershell` 펜스로 준다: `pip install scikit-learn` 한 줄 + `py ml_types.py` 한 줄. 패키지 이름은 `scikit-learn`으로 정확히 쓴다. 아래에 macOS 병기 한 줄과 예상 출력 설명 산문 3~4줄(무엇이 몇 개 나오면 정상인지).
  - `### 해보기` 2~3개: ① 분류기를 다른 모델(예: 결정 트리 계열)로 바꿔 예측 결과가 달라지는지 확인한다(정답 블록에 바꿀 import·한 줄과 예상 관찰), ② `KMeans`의 덩어리 개수를 3이 아닌 값으로 바꿔 결과가 어떻게 달라지는지 본다(정답 블록에 예상 변화와 왜 그런지), ③(선택) 예측이 틀린 샘플이 몇 개인지 세어 본다.
- ⑤ 실무 팁 — 불릿 3~4개. 반드시 포함: 설치할 때 쓰는 이름(`scikit-learn`)과 코드에서 부르는 이름(`sklearn`)이 다르다는 것. 나머지는 군집 번호가 정답 라벨과 일치하지 않는다는 것, 작은 내장 데이터로 잘 나온 결과가 실제 데이터에서 그대로 나오지 않는다는 것, 난수 고정을 해야 결과를 남과 비교할 수 있다는 것 계열에서 고른다.
- ⑥ 핵심 정리 + `**이 레슨의 단어**` 표(지도 학습, 비지도 학습, 분류, 회귀, 군집, 특징, 라벨, 학습/예측 계열에서 5~8개) + `**스스로 점검**` 질문 2개(각각 정답 블록). 질문 하나는 "고객 이탈 여부를 맞히는 문제는 셋 중 어느 유형인가?" 같은 유형 판단 문제로 한다.

**작성 후 반드시 예제를 실제로 실행한다.** 로컬에 `scikit-learn`을 설치해(`py -m pip install scikit-learn`) 코드를 돌리고, 실제 출력을 본문의 예상 출력 설명과 대조한다. 실행 결과를 SUMMARY에 적는다. 실행을 저장소 밖 임시 디렉터리에서 하고, `package.json`이나 저장소 파일이 변하지 않게 한다.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-brand.mjs && node scripts/check-lesson-structure.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `node scripts/check-brand.mjs`, `node scripts/check-lesson-structure.mjs` 세 명령이 모두 exit 0이다.
    - 파일 프론트매터에 `hasContent: true`가 있고, `git diff`에서 `hasContent` 줄 외의 프론트매터 줄이 변경되지 않았다.
    - `grep -n '^## ' 파일`의 출력이 정확히 6줄이고 순서가 1→6이다.
    - `grep -c '^### 해보기$' 파일`이 2 또는 3이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이다.
    - `python` 펜스 안에 `load_iris`, `load_diabetes`, `.fit(`, `.predict(`, `KMeans` 다섯 요소가 모두 등장한다.
    - 난수가 개입하는 모든 호출(`KMeans` 등)에 `random_state=`가 지정되어 있다.
    - 파일에 `fetch_` 로 시작하는 scikit-learn 네트워크 로더 호출이 0건이고, `http://`·`https://`로 시작하는 데이터 파일 URL이 0건이다.
    - 파일에 `pip install scikit-learn`이 정확히 이 문자열로 등장하고, `pip install sklearn` 형태는 0건이다.
    - 파일에 ```` ```powershell ```` 펜스가 최소 1개 있고 그 안에 `py ml_types.py`(또는 실제 사용한 파일명)가 있다. `python 파일명` 형태를 1차 실행 명령으로 쓰지 않는다.
    - `python3 ` 로 시작하는 macOS 병기가 정확히 1회 등장한다.
    - ⑤ 실무 팁에 설치 이름(`scikit-learn`)과 import 이름(`sklearn`)이 다르다는 불릿이 존재한다.
    - 군집 번호가 정답 라벨과 일치하지 않는다는 설명이 본문에 존재한다.
    - `matplotlib`·`seaborn`·`plt.` 문자열이 파일 전체에 0건이다 (D-48 — 시각화 없음).
    - `**이 레슨의 단어**` 표의 데이터 행이 5~8개다.
    - `grep -ciE 'kant' 파일`이 0이다.
    - `grep -ciE 'colab|kaggle' 파일`이 0이다.
    - 코드펜스 중 언어 태그가 없는 것이 0개다.
    - 실행자가 저장소 밖 임시 디렉터리에서 예제를 실제로 실행해 오류 없이 끝나는 것을 확인했고, 실제 출력을 SUMMARY에 적었다.
    - `git status --porcelain`에 예제 실행이 만든 파일이 나타나지 않고 `package.json`이 변경되지 않았다.
  </acceptance_criteria>
  <done>학습자가 분류·회귀·군집을 각각 한 번씩 실제로 돌려 보고, 문제를 보면 어느 유형인지 판단할 수 있다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 1-5-ml-metrics-and-pipeline — 성적표(평가 지표) · 조립 라인(Pipeline)</name>
  <files>src/content/lessons/step-1/1-5-ml-metrics-and-pipeline.mdx</files>
  <read_first>
    - `src/content/lessons/step-1/1-5-ml-metrics-and-pipeline.mdx` — 현재 스텁. 프론트매터(`title: "평가 지표와 Scikit-learn Pipeline"`, `moduleId: "1-5"`, `order: 2`) 유지 대상.
    - `src/content/lessons/step-1/1-5-ml-model-types.mdx` (Task 1 완료본) — **같은 모듈 앞 레슨.** `load_iris` 데이터 설명과 분류기 코드를 여기서 이어받고 반복하지 않는다.
    - `src/content/lessons/step-1/1-3-python-variables-and-types.mdx` — 승인된 파일럿(구조·톤 기준).
    - `.planning/phases/04-step-1/04-01-SUMMARY.md` — 확정값과 사용자 수정 요청.
    - `.planning/phases/04-step-1/04-RESEARCH.md` — `## Code Examples` §scikit-learn 예제, `## Don't Hand-Roll`의 ML 평가 지표 행(직접 구현 금지, `sklearn.metrics` 사용), §Per-Lesson Content Outline의 `1-5-ml-metrics-and-pipeline` 행.
    - `.planning/curriculum.md` §Step 1 §1-5 두 번째 불릿 — "평가 지표와 Scikit-learn Pipeline 활용"이 범위 원천이다.
  </read_first>
  <action>
`1-5-ml-metrics-and-pipeline.mdx`의 본문을 새로 쓰고 `hasContent`를 `false`에서 `true`로 바꾼다. 나머지 7개 프론트매터 필드는 손대지 않는다. `<ml_content_contract>`를 예외 없이 지킨다.

**앞 레슨을 이어받는다.** `load_iris` 데이터가 무엇인지 다시 설명하지 않고 "앞 레슨에서 쓴 그 꽃 데이터"로 가리킨다. 분류기도 같은 것을 쓴다. 이 레슨의 새것은 두 가지뿐이다: **어떻게 성적을 매기는가**와 **전처리와 모델을 어떻게 한 덩어리로 묶는가**.

비유의 두 축: **평가 지표 = 성적표**(과목마다 보는 게 다르다), **Pipeline = 조립 라인**(재료가 정해진 순서로 같은 공정을 통과한다).

**중요 — 정직한 평가:** 예제는 반드시 데이터를 학습용과 평가용으로 나눠(`train_test_split`, `random_state` 고정) 평가용 데이터로만 지표를 계산한다. 학습에 쓴 데이터로 자기 성적을 매기는 형태를 정상 패턴으로 보여주지 않는다. 왜 나눠야 하는지("시험 문제를 미리 보고 시험을 치는 것과 같다")를 ③에서 한 문단으로 설명한다.

절별 목표:
- ① 학습 목표 — "모델의 성적을 지표로 재고 그 숫자가 무엇을 뜻하는지 설명할 수 있다", "정확도만으로 판단하면 안 되는 경우를 말할 수 있다", "학습용과 평가용 데이터를 나눠 정직하게 평가할 수 있다", "전처리와 모델을 Pipeline 하나로 묶어 실행할 수 있다" 계열 4개.
- ② 왜 배우나 — 모델을 만드는 것보다 "이 모델을 믿어도 되는가"를 판단하는 게 실무의 핵심이라는 것. 4문장 이내.
- ③ 개념 설명 — 이모지 `###` 소제목 3~4개.
  - "시험지를 미리 보면 안 된다" — 학습용/평가용 분리의 이유. 여기가 이 레슨의 첫 번째 핵심이다.
  - "성적표 읽는 법" — 정확도(accuracy)·정밀도(precision)·재현율(recall)을 각각 한 줄 풀이 + 일상 비유로. 3~4열 표(지표 · 무엇을 재나 · 언제 중요한가 · 일상 비유). 정확도만 보면 안 되는 상황(한쪽 답이 압도적으로 많은 데이터)을 한 문단으로. 수식을 쓰지 않고 말로 설명한다.
  - "조립 라인 — Pipeline" — 전처리(스케일 맞추기)와 모델이 따로 놀 때 생기는 문제(평가용 데이터에까지 전처리 기준이 새어 들어가는 것)를 한 문단으로 설명하고, Pipeline이 그 순서를 한 덩어리로 묶어 준다는 것. 조립 라인 비유로 `재료 → 전처리 → 모델 → 결과` 한 줄 흐름을 보여준다.
- ④ 실무 예제 — 완결된 단일 `python` 펜스 하나. 파일명은 `ml_metrics_pipeline.py`, 첫 줄은 파일명 주석. 구성:
  1. 앞 레슨과 같은 내장 데이터 불러오기(한 줄).
  2. `train_test_split`으로 학습용/평가용 분리, `random_state` 고정.
  3. `Pipeline`으로 스케일러 + 분류기를 묶어 만들고 학습용 데이터에 `fit`.
  4. 평가용 데이터에 `predict` 후 `sklearn.metrics`의 지표 함수들로 정확도와 클래스별 리포트를 출력. 지표를 직접 계산하지 않는다.
  5. 출력 아래 산문으로 각 숫자를 어떻게 읽는지 3~4줄 설명.
  - 실행 방법을 `powershell` 펜스로 준다: `pip install scikit-learn`(이미 설치했다면 넘어가도 된다는 한 줄) + `py ml_metrics_pipeline.py`. macOS 병기 한 줄.
  - `### 해보기` 2~3개: ① Pipeline에서 스케일러를 빼고 돌려 결과가 어떻게 달라지는지 비교한다(정답 블록에 예상 변화와 왜 이 데이터에서는 차이가 작은지), ② `train_test_split`의 평가용 비율이나 `random_state`를 바꿔 지표가 흔들리는 것을 관찰한다(정답 블록에 예상 관찰과 "한 번의 점수를 믿으면 안 되는 이유"), ③(선택) 다른 지표를 하나 더 출력해 본다.
- ⑤ 실무 팁 — 불릿 3~4개: 정확도 하나만 보고 판단하지 말 것, 평가용 데이터로는 절대 학습하지 말 것, 전처리는 Pipeline 안에 넣어야 새는 것을 막는다는 것, 한 번의 분할 결과는 운이 섞여 있으므로 숫자를 소수점 여러 자리까지 신뢰하지 말 것.
- ⑥ 핵심 정리 + `**이 레슨의 단어**` 표(학습용/평가용 데이터, 정확도, 정밀도, 재현율, 스케일링, Pipeline, 데이터 누수 계열에서 5~8개) + `**스스로 점검**` 질문 2개(각각 정답 블록). 질문 하나는 "100명 중 1명만 해당하는 것을 찾는 문제에서 정확도 99%는 좋은 성적인가?" 형태로 ③의 함정을 복습시킨다.

**작성 후 반드시 예제를 실제로 실행한다.** 실제 출력 숫자를 본문의 설명과 대조하고, 결과를 SUMMARY에 적는다. 실행은 저장소 밖 임시 디렉터리에서 한다.

이 레슨이 커밋되면 Step 1 10편이 모두 `hasContent: true`가 된다. Plan 07이 그 상태를 전제로 게이트 상수를 갱신하므로, SUMMARY에 이 Plan이 몇 편을 `true`로 바꿨는지 정확히 적는다.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-brand.mjs && node scripts/check-lesson-structure.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`, `node scripts/check-brand.mjs`, `node scripts/check-lesson-structure.mjs` 세 명령이 모두 exit 0이다.
    - 파일 프론트매터에 `hasContent: true`가 있고, `git diff`에서 `hasContent` 줄 외의 프론트매터 줄이 변경되지 않았다.
    - `grep -n '^## ' 파일`의 출력이 정확히 6줄이고 순서가 1→6이다.
    - `grep -c '^### 해보기$' 파일`이 2 또는 3이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이다.
    - `python` 펜스 안에 `train_test_split`, `Pipeline`, `sklearn.metrics`에서 온 지표 함수 하나 이상, `.fit(`, `.predict(`가 모두 등장한다.
    - `train_test_split` 호출에 `random_state=`가 지정되어 있다.
    - 지표를 수식으로 직접 계산하는 코드(`/ len(`, `sum(` 기반 정확도 계산 등)가 예제 안에 없다 — 전부 라이브러리 함수를 쓴다.
    - 예제가 `fit`에 넘기는 데이터와 지표 계산에 쓰는 데이터가 서로 다르다(학습용 vs 평가용).
    - 파일에 `fetch_` 네트워크 로더 호출이 0건이고 데이터 파일 URL이 0건이다.
    - 파일에 ```` ```powershell ```` 펜스가 최소 1개 있고 `py ` 로 시작하는 실행 명령을 담는다. `python 파일명` 형태를 1차 실행 명령으로 쓰지 않는다.
    - `python3 ` 로 시작하는 macOS 병기가 정확히 1회 등장한다.
    - `matplotlib`·`seaborn`·`plt.` 문자열이 파일 전체에 0건이다.
    - 본문이 `load_iris` 데이터 구조를 처음부터 다시 설명하지 않고 앞 레슨을 가리킨다.
    - `**이 레슨의 단어**` 표의 데이터 행이 5~8개다.
    - `grep -ciE 'kant' 파일`이 0이다.
    - `grep -ciE 'colab|kaggle' 파일`이 0이다.
    - 코드펜스 중 언어 태그가 없는 것이 0개다.
    - 실행자가 저장소 밖 임시 디렉터리에서 예제를 실제로 실행해 오류 없이 끝나는 것을 확인했고, 실제 지표 출력을 SUMMARY에 적었다.
    - `node -e "const l=require('./.velite/lessons.json');const n=l.filter(x=>x.stepId===1&&x.hasContent).length;console.log(n);process.exit(n===10?0:1)"` 가 exit 0이다 — 이 Plan 종료 시점에 Step 1 10편이 모두 실콘텐츠다.
  </acceptance_criteria>
  <done>학습자가 모델의 성적을 정직하게 재고, 정확도만 보면 안 되는 이유를 알고, 전처리와 모델을 한 덩어리로 묶을 수 있다. Step 1 10편의 본문이 모두 채워졌다. 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레슨 본문 → 공개 프로덕션 URL | 본문 문자열이 그대로 인터넷에 노출된다 |
| 레슨의 `pip install` 안내 → 학습자의 로컬 Python 환경 | 잘못된 패키지 이름을 안내하면 학습자가 슬롭스쿼팅 패키지를 설치할 수 있다 |
| 레슨의 예제 코드 → 학습자 로컬 실행 | 네트워크에서 데이터를 받아 오는 코드는 재현성과 신뢰 경계를 함께 흔든다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-16 | Spoofing | 잘못된/유사 패키지명 안내 (슬롭스쿼팅) | high | mitigate | RESEARCH.md `## Package Legitimacy Audit`이 `scikit-learn`(PyPI, 10년+ 업계 표준, github.com/scikit-learn/scikit-learn)을 OK 판정했고 `[SLOP]`/`[SUS]` 0건이다. 두 Task의 acceptance criteria가 `pip install scikit-learn` 문자열의 정확한 일치와 `pip install sklearn` 0건을 확인한다. ⑤ 실무 팁이 설치 이름과 import 이름의 불일치를 학습자에게 각인시킨다 |
| T-04-17 | Tampering | 예제가 외부에서 데이터를 받아 옴 | medium | mitigate | `<ml_content_contract>`가 내장 데이터셋만 허용하고, acceptance criteria가 `fetch_` 로더 0건과 데이터 URL 0건을 확인한다 |
| T-04-18 | Tampering | 학습에 쓴 데이터로 성적을 매기는 패턴을 정상으로 전달 | medium | mitigate | Task 2 예제가 `train_test_split`으로 분리하도록 고정하고, acceptance criteria가 `fit` 대상과 지표 계산 대상이 다름을 확인한다. ③·⑤·⑥이 같은 규칙을 반복 각인 |
| T-04-04 | Information Disclosure | 레슨 본문(공개 표면)의 브랜드·이메일 | high | mitigate | `node scripts/check-brand.mjs`가 상시 검사하며 두 Task의 verify에 포함 |
| T-04-19 | Denial of Service | 무거운 학습 코드로 학습자 머신이 멈춤 | low | accept | 내장 데이터가 150샘플 규모이고 모델이 단순해 초 단위로 끝난다. 하이퍼파라미터 탐색·교차검증을 금지한 것이 부수적 완화다 |
| T-04-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 저장소에 npm 패키지를 설치하지 않는다. 레슨이 안내하는 `pip install scikit-learn`은 학습자 로컬 대상이며 RESEARCH의 Package Legitimacy Audit에서 OK 판정. acceptance criteria가 `package.json` 무변경을 확인한다 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

Spec-less probe fallback §A의 6개 edge 프로브(CONT-02·CONT-03 × empty/ordering/adjacency)는 전부 `unresolved`이며, 이 phase에서의 해석은 `04-01-PLAN.md`의 `<flagged_assumptions>`에 기록되어 있다 — empty는 구조 게이트와 Velite 스키마가, ordering·adjacency는 프론트매터 `order`와 `check-manifest.mjs`가 담당하고 이 Plan은 `order`를 건드리지 않는다.

이 Plan 고유의 가정:
- **`scikit-learn` 로컬 설치 가능성:** RESEARCH `## Environment Availability`가 이 세션에서 설치를 시도하지 않았다고 기록했다. 표준 PyPI 패키지라 실패 위험은 낮지만, 실행자가 실제로 설치해 예제를 돌려 보는 것이 이 Plan의 유일한 결과 검증 경로다(D-59가 사람 검토를 없앴다). 설치가 실패하면 그 사실을 SUMMARY에 명시적으로 적는다.
- **본문의 예상 출력과 실제 출력의 일치:** 난수 고정으로 재현성을 확보했지만 scikit-learn 버전이 바뀌면 기본 하이퍼파라미터나 수렴 경고가 달라져 출력이 미묘하게 변할 수 있다. 그래서 본문의 예상 출력 설명을 "정확히 이 숫자"가 아니라 "이런 형태로 몇 개가 나온다" 수준으로 쓴다.
- **`check-manifest`는 이 Plan 종료 시점에 의도적으로 red다** (Wave 2 공통 규칙 2). 이 Plan이 마지막 2편을 채우면 Step 1 10편이 모두 실콘텐츠가 되고, Plan 07이 상수를 11로 올려 green으로 되돌린다.
</flagged_assumptions>

<verification>
1. `npm run build`
2. `node scripts/check-brand.mjs`
3. `node scripts/check-lesson-structure.mjs`
4. 실행자가 저장소 밖 임시 디렉터리에서 두 예제를 실제 실행 — 오류 없이 끝나고 출력이 본문 설명과 형태가 맞는지 확인
5. Step 1 실콘텐츠 10편 확인 — `.velite/lessons.json`에서 `stepId===1 && hasContent` 개수가 10
6. `node scripts/check-manifest.mjs`는 **실행하지 않는다** — Plan 07이 상수를 갱신할 때까지 의도적 red
</verification>

<success_criteria>
- 1-5 모듈 2편이 승인된 eli5 × 6단 표준으로 작성되어 읽힌다
- 분류 · 회귀 · 군집 · 평가 지표 · Pipeline 다섯 주제가 모두 다뤄진다
- 모든 데이터가 내장 데이터셋에서 오고 네트워크 접근이 없다
- 평가가 학습용/평가용 분리 위에서 이뤄진다
- 설치 이름과 import 이름의 불일치가 학습자에게 명시된다
- 이 Plan 종료 시점에 Step 1 10편이 모두 `hasContent: true`다
</success_criteria>

<output>
Create `.planning/phases/04-step-1/04-06-SUMMARY.md` when done.
</output>
