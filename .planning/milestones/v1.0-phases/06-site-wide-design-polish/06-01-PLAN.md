---
phase: 06-site-wide-design-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - scripts/e2e-typography.mjs
  - scripts/check-design-tokens.mjs
  - src/app/globals.css
  - src/app/lesson/[lessonId]/page.tsx
autonomous: false
requirements: [SC1, SC4]
user_setup: []

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "globals.css의 @theme 블록이 --text-display/--text-heading/--text-subhead/--text-body/--text-label 5종을 올바른 Tailwind v4 네임스페이스로 선언하고, Tailwind가 text-display·text-heading·text-subhead·text-body·text-label 유틸리티를 실제로 생성한다 (D-95, D-R4K-4)"
    - "렌더된 레슨 페이지의 getComputedStyle 크기 히스토그램에 30/22/17/16/15/14px 밖의 값이 없고, 굵기 히스토그램에 400/600/700 밖의 값이 없다 (D-89, D-R4K-4)"
    - "레슨 본문 인라인 코드에 리터럴 백틱 글리프가 렌더되지 않고, 배경 칩(--color-surface / --color-surface-dark)과 좌우 padding으로 구분된다 (D-R4K-5)"
    - "check-design-tokens.mjs가 @theme 블록 밖의 리터럴 색(hex/rgb/hsl)과 Tailwind 기본 팔레트 색 유틸리티를 위반으로 잡고, 위반 0건일 때 exit 0으로 종료한다 (D-88, D-96)"
    - "check-design-tokens.mjs의 판정선은 @theme 여는 줄부터 대응하는 닫는 중괄호까지이며, 중첩 중괄호를 세어 블록 끝을 찾는다 — 첫 닫는 중괄호에서 끊지 않는다"
    - "check-design-tokens.mjs는 CSS 주석과 JS/TS 주석 안의 색 리터럴을 위반으로 세지 않는다 — 매칭 전에 주석을 제거한다"
    - "check-design-tokens.mjs는 스캔한 파일 수를 출력하고, 스캔 대상이 0개이면 통과가 아니라 non-zero exit으로 실패한다"
    - "check-design-tokens.mjs의 Tailwind 클래스 매칭은 단어 경계 기준이라 text-whitesmoke·my-text-white를 오탐하지 않고, 임의값 대괄호는 대괄호 짝을 세어 중첩·이스케이프를 포함한 전체 토큰을 잡는다"
    - "check-design-tokens.mjs의 위반 목록은 파일 경로 오름차순, 같은 파일 안에서는 줄 번호 오름차순으로 정렬되어 출력되며 파일시스템 순회 순서에 의존하지 않는다"
    - "globals.css가 html/:root의 font-size를 재정의하지 않아 0.9375rem이 정확히 15px로 남고, 게이트는 rem 값을 px로 환산하지 않고 선언 문자열 그대로 대조한다"
    - "인라인 코드 칩은 배경과 padding만 추가하고 줄바꿈 동작을 바꾸지 않아 한국어 본문 흐름 안에서 기존과 동일하게 줄바꿈된다"
    - "인라인 코드의 font-size가 0.9375rem 절대값이라 표 셀 등 어디에 중첩돼도 em 복리 축소가 일어나지 않고, font-weight 400도 함께 지정되어 플러그인 기본값 600이 새지 않는다"
    - "기존 자동 게이트 10종이 전부 통과한다 (D-94)"
  artifacts:
    - scripts/check-design-tokens.mjs
    - scripts/e2e-typography.mjs
    - "src/app/globals.css (@theme --text-* 5종, --color-foreground 쌍, .prose h1–h4 / .prose code 오버라이드)"
  key_links:
    - "@theme의 --text-* 선언 → Tailwind v4 유틸리티 생성 → 컴포넌트 className의 text-{role} → 렌더된 DOM의 computed font-size — 이 4단 사슬 중 한 칸이라도 끊기면 66곳 치환 전체가 무의미해진다"
    - "e2e-typography.mjs의 --report-only 히스토그램 → 허용 집합 확정 → 엄격 모드 exit code — 측정 범위를 잘못 잡으면 게이트가 조용히 초록불이 된다"
  prohibitions:
    - statement: "새 색 토큰을 도입하지 않는다 — --color-foreground/--color-foreground-dark는 body/.dark body에 이미 있던 리터럴을 값 변경 없이 @theme로 옮긴 것이며, 이 Phase가 추가하는 새 색 값은 0개다 (D-R4K-2)"
      status: active
      verification: flagged
    - statement: "새 폰트 파일을 추가하지 않는다 — Pretendard 가변 폰트의 700을 소비할 뿐 다운로드가 늘지 않는다 (D-R4K-3)"
      status: active
      verification: flagged
    - statement: "렌더링 전략을 바꾸지 않는다 — force-dynamic/generateStaticParams 선언을 건드리지 않는다"
      status: active
      verification: flagged
    - statement: "새 라우트를 만들지 않고 DB 스키마를 바꾸지 않는다"
      status: active
      verification: flagged
    - statement: "375px 대응을 이유로 truncate나 고정폭 클래스를 새로 도입하지 않는다 — 넘치면 줄바꿈한다"
      status: active
      verification: flagged
    - statement: "사용자에게 보이는 어떤 문자열에도 교육기관명(K로 시작하는 4글자 브랜드)을 쓰지 않는다 — check-brand.mjs가 강제한다"
      status: active
      verification: flagged
---

<objective>
이 Phase의 **트레이서 슬라이스**다. 타이포 토큰 한 벌이 `@theme` 선언 → Tailwind 유틸리티 생성 →
컴포넌트 className → 렌더된 DOM의 computed style → 자동 게이트까지 **한 경로로 끝까지 관통**하는지를
파일 하나(레슨 페이지)로 먼저 증명한다. 이 사슬이 붙지 않으면 나머지 22개 파일 치환은 전부 헛수고다.

핵심 발견(06-RESEARCH.md 실측): `globals.css:35-38`의 `--font-size-*` 4종은 죽은 코드다. Tailwind v4가
`text-{name}` 유틸리티를 만드는 네임스페이스는 `--text-*`이기 때문이다
(`node_modules/tailwindcss/theme.css:347-372`). 코드베이스는 이미 일관된 4종/2굵기를 쓰고 있었지만
토큰이 연결이 안 돼 있어서 전부 임의값 대괄호로 우회했다 — 23개 파일 66곳.

Purpose: 네임스페이스를 고치고, 측정 도구 2종을 세우고, 한 파일로 사슬 전체를 초록불로 만든다.
Output: `--text-*` 5종 토큰, `.prose` 오버라이드, `check-design-tokens.mjs`, `e2e-typography.mjs`,
레슨 페이지 3곳 치환.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-site-wide-design-polish/06-CONTEXT.md
@.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md
@.planning/phases/06-site-wide-design-polish/06-RESEARCH.md
@.planning/phases/06-site-wide-design-polish/06-PATTERNS.md
</context>

<artifacts_this_phase_produces>
> 이 절은 Phase 6 전체가 만들어내는 심볼의 단일 목록이다. 각 항목의 소유 플랜을 괄호로 표기한다.
> 실행자는 여기 없는 이름을 새로 만들지 않는다.

**신규 파일**
- `scripts/check-design-tokens.mjs` (06-01) — 정적 토큰 게이트
- `scripts/e2e-typography.mjs` (06-01) — 런타임 타이포 히스토그램 게이트
- `scripts/e2e-mobile-overflow.mjs` (06-08) — 뷰포트 가로 오버플로 게이트
- `src/components/section-tape.tsx` (06-06) — `export function SectionTape`

**신규 CSS 커스텀 프로퍼티 (`@theme`, `src/app/globals.css`)**
- `--text-display`, `--text-display--line-height`
- `--text-heading`, `--text-heading--line-height`
- `--text-subhead`, `--text-subhead--line-height`
- `--text-body`, `--text-body--line-height`
- `--text-label`, `--text-label--line-height`
- `--color-foreground`, `--color-foreground-dark` — **새 색 값이 아니다.** `body`/`.dark body`에 이미
  있던 리터럴을 값 변경 없이 `@theme`로 이동한 것(SC1의 "한 곳에서 정의" 요구를 만족시키기 위함)

**삭제되는 토큰**
- `--font-size-label`, `--font-size-body`, `--font-size-heading`, `--font-size-display` (죽은 코드)
- `--font-weight-regular`, `--font-weight-semibold` (Tailwind 기본 유틸리티로 충분)

**Tailwind가 토큰에서 자동 생성하는 유틸리티 클래스**
- `text-display`, `text-heading`, `text-subhead`, `text-body`, `text-label`

**손으로 쓰는 신규 클래스**
- `.card-interactive` + `.card-interactive:hover` + `.dark .card-interactive:hover` (06-03)

**신규 CSS 규칙 (선택자 기준)**
- `.prose h1`, `.prose h2`, `.prose h3`, `.prose h4` — 크기·굵기 못박기 (06-01)
- `.prose code`, `.prose code::before`, `.prose code::after`, `.dark .prose code` — 백틱 제거 + 칩 (06-01)
- `.prose h2 { scroll-margin-top: 52px }` (06-06)
- `[data-section-tape] button:focus-visible` — focus-visible 아웃라인 (06-06)

**신규 컴포넌트 내부 심볼**
- `TableWrapper` — `src/components/mdx-content.tsx` 안 로컬 함수 (06-06)
- `SECTION_TAPE_STEP_CLASSES` — `Record<StepId, string>` 리터럴 맵, `section-tape.tsx` (06-06)

**신규 DOM 계약 (게이트가 셀렉터로 잡는 속성)**
- `data-section-tape` — Section Tape 루트 (06-06)
- `data-locked-notice` — 잠금 상태 문구 (06-06)

**신규 CLI 플래그**
- `check-design-tokens.mjs`: `--strict`, `--only <path...>`
- `e2e-typography.mjs`: `--report-only`
- `e2e-mobile-overflow.mjs`: `--report-only`

**신규 환경변수 (선택, 기본값 있음)**
- `E2E_TYPOGRAPHY_PORT` (기본 3212), `E2E_OVERFLOW_PORT` (기본 3213)

**신규 npm script: 없음.** 기존 컨벤션은 `node scripts/<name>.mjs` 직접 실행이다
(`package.json`의 `scripts`엔 dev/build/start/lint만 존재). 이 Phase는 그 컨벤션을 바꾸지 않는다.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: @playwright/test 패키지 정당성 확인 (SUS 판정)</name>
  <decision>`@playwright/test@1.62.1`을 devDependency로 설치할지 확인한다.</decision>
  <context>
06-RESEARCH.md의 Package Legitimacy Audit이 이 패키지를 `[SUS]`로 표기했다. 유일한 사유는
`too-new`(이 **버전**이 최근 게시됨 — Playwright는 거의 매주 패치를 낸다)이며, 나머지 신호는
슬롭스쿼팅과 정반대다: 주간 다운로드 56,416,711 / 저장소 `github.com/microsoft/playwright` /
`postinstall` 스크립트 없음 / deprecated 아님.

이 패키지 없이는 D-89(`getComputedStyle` 타이포 실측)와 D-91(`scrollWidth` 375px 오버플로)을
**원리적으로 구현할 수 없다** — 기존 `e2e-progress.mjs`/`e2e-today.mjs`는 순수 HTTP fetch라
레이아웃 엔진이 계산하는 값을 볼 수 없다. devDependency이므로 프로덕션 번들에는 들어가지 않는다.

**확인 방법:** 브라우저에서 `https://www.npmjs.com/package/@playwright/test`를 열어 (a) 게시자가
Microsoft이고 (b) 저장소 링크가 `github.com/microsoft/playwright`이며 (c) 주간 다운로드가
수천만 단위인지 본다. 또는 터미널에서 `npm view @playwright/test repository.url dist-tags.latest`.
  </context>
  <options>
    <option id="approve">
      <name>승인 — 최신 버전으로 설치</name>
      <pros>D-89/D-91 게이트를 구현할 수 있는 유일한 경로. dev 전용이라 프로덕션 표면 0</pros>
      <cons>새 devDependency 1개 + Chromium 바이너리 다운로드(약 150MB)</cons>
    </option>
    <option id="pin-older">
      <name>한 단계 이전 마이너 버전으로 고정</name>
      <pros>`too-new` 신호를 회피</pros>
      <cons>실익 없음 — 공급망 위험의 실체는 패키지 정체성이지 패치 신선도가 아니다</cons>
    </option>
  </options>
  <resume-signal>Select: approve 또는 pin-older (버전 명시)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Playwright 설치 + 런타임 타이포 측정 도구 신설 (D-89, D-98)</name>
  <files>package.json, package-lock.json, scripts/e2e-typography.mjs</files>
  <read_first>
    - `scripts/e2e-today.mjs` — 부트스트랩 절반의 유일한 analog. `waitForServerReady()`(130-141줄),
      `killServerTree()`(144-159줄), `spawn(process.execPath, [nextBin, 'dev', ...])`(171-189줄),
      `t1`/`t2` 시나리오 번호 매기기 컨벤션, `--env-file=.env.local` 실행 규약(9줄 주석)
    - `scripts/check-brand.mjs` — 위반 수집·정렬·exit 컨벤션, "0건 검사 = 성공 위장 금지"
      방어 로직(126-131줄)
    - `.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md` § Typography — 5종/3굵기 허용 집합
    - `.planning/phases/06-site-wide-design-polish/06-RESEARCH.md` § Common Pitfalls 4 —
      기존 e2e 패턴을 그대로 복제하면 측정 불가능한 값을 요구하게 되는 함정
  </read_first>
  <precondition>npm 레지스트리와 Playwright CDN에 네트워크 접근이 가능해야 한다 — `npx playwright install chromium`이 브라우저 바이너리를 받아야 하고, 이것 없이는 D-89/D-91 게이트를 실행할 수 없다 (06-RESEARCH.md Assumptions A2).</precondition>
  <action>
`npm install -D @playwright/test`로 devDependency를 추가하고 `npx playwright install chromium`으로
브라우저 바이너리를 1회 내려받는다. `playwright.config.ts`는 만들지 않는다 — 기존 게이트 컨벤션이
"설정 파일 없이 인자 없는 직접 실행"이므로 스크립트가 `chromium.launch()`를 직접 호출한다.

`scripts/e2e-typography.mjs`를 신설한다. 실행 규약은 기존 e2e 2종과 동일하게
`node --env-file=.env.local scripts/e2e-typography.mjs`.

**부트스트랩(재사용):** `e2e-today.mjs`의 `waitForServerReady`/`killServerTree`/dev 서버 spawn을
복제한다 — 기존 게이트 8종이 전부 "재사용 안 함, 복제" 원칙을 따르므로 공유 모듈로 빼지 않는다.
기본 포트는 `E2E_TYPOGRAPHY_PORT` 환경변수로 override 가능하게 하고 기본값 3212로 둬 기존
스크립트(3211대)와 충돌하지 않게 한다. 서버 종료는 `finally` 블록에서 항상 호출한다.

**검증층(신규 작성 — analog 없음):** `chromium.launch()` → `browser.newContext()` →
`page.goto(BASE_URL + route)`. 측정 전에 `networkidle`이 아니라 `page.waitForSelector('main')`처럼
명시적 셀렉터 대기를 쓴다. `page.evaluate()` 안에서 `document.querySelectorAll`로 텍스트를 가진
요소를 훑고 `getComputedStyle(el).fontSize`/`.fontWeight` 히스토그램(값 → 개수 + 대표 셀렉터)을
만든다.

**측정 범위:** `main` 하위의 텍스트 노드를 직접 가진 요소만 센다(자식 요소만 가진 래퍼 div는
중복 집계를 만든다). `pre` 및 그 하위(Shiki 코드 블록)는 제외한다 — 문법 강조 테마가 굵기를
지정하고 이 Phase는 코드 하이라이팅을 손대지 않는다. 제외 셀렉터는 추측하지 말고 아래
`--report-only` 1차 실행 결과로 확정한다.

**허용 집합:** 크기 `{30px, 22px, 17px, 16px, 15px, 14px}`, 굵기 `{400, 600, 700}`
(06-UI-SPEC.md § Typography 5종 + 인라인 코드 15px, 3굵기). 집합 밖 값이 하나라도 있으면
그 값·개수·대표 셀렉터를 출력하고 exit 1.

**`--report-only` 플래그:** 히스토그램만 출력하고 항상 exit 0. 이 태스크의 검증은 이 모드로
현재 코드의 이탈을 **탐지해내는 것**이다 — 지금 렌더되는 레슨 페이지에는
`@tailwindcss/typography`가 주입한 h2 24px와 표 안 인라인 코드 12.25px/굵기 600이 존재하고
(04-UI-REVIEW Pillar 4 실측), 히스토그램이 이 값들을 실제로 잡아내야 도구가 작동한다는 증거가 된다.

**측정 라우트:** 콘텐츠가 있는 레슨 1편(`/lesson/1-1-course-orientation`)을 기본으로 하고,
라우트 목록을 배열 상수로 두어 나중에 늘릴 수 있게 한다.

**출력 컨벤션:** 한국어 메시지 + 영어 식별자, 시나리오 번호 `t1`/`t2`, 성공 시
`console.log('e2e-typography: ...')` 후 `process.exit(0)`. 측정 대상 요소가 0개면 통과가 아니라
실패다(검사 대상 부재 = 실패, `check-brand.mjs:126-131` 방어 로직과 같은 규칙).
  </action>
  <verify>
    <automated>node --env-file=.env.local scripts/e2e-typography.mjs --report-only 2>&1 | tee /tmp/typo-baseline.txt; grep -qE '(^|[^0-9])24px' /tmp/typo-baseline.txt &amp;&amp; echo "BASELINE-DETECTED"</automated>
  </verify>
  <acceptance_criteria>
    - `npm ls @playwright/test`가 설치된 버전을 출력한다(`(empty)`가 아니다)
    - `node --env-file=.env.local scripts/e2e-typography.mjs --report-only`가 exit 0으로 끝나고
      크기·굵기 히스토그램을 출력한다
    - 그 히스토그램에 `24px`(플러그인이 주입한 h2 이탈)이 실제로 등장한다 — 도구가 알려진 결함을
      탐지한다는 증거. 등장하지 않으면 측정 범위 셀렉터가 잘못된 것이므로 범위를 고친다
    - 측정 대상 요소 수가 출력되고 0이 아니다
    - dev 서버 프로세스가 스크립트 종료 후 남아 있지 않다(`finally`의 `killServerTree` 동작 확인)
    - 기존 게이트 회귀 없음: `node scripts/check-brand.mjs`, `node scripts/check-manifest.mjs`가
      각각 exit 0 (D-94)
  </acceptance_criteria>
  <done>런타임 타이포 측정 도구가 존재하고, 현재 코드의 알려진 이탈(h2 24px)을 실제로 탐지한다.</done>
</task>

<task type="tracer" tdd="false">
  <name>Task 3: 토큰 사슬 관통 — @theme 정정 + prose 봉쇄 + 레슨 페이지 1파일 + 정적 게이트</name>
  <files>src/app/globals.css, src/app/lesson/[lessonId]/page.tsx, scripts/check-design-tokens.mjs</files>
  <read_first>
    - `src/app/globals.css` 전체(305줄) — 특히 `@theme` 블록(5-41줄), `body`/`.dark body`(43-53줄),
      복사 버튼 색 규칙(110-124줄, 라이트+다크 쌍 패턴의 원형), `.prose` 오버라이드 5곳(163-219줄,
      "명세성만으로 이긴다"의 기존 성공 사례), `.prose summary`(204줄, `var(--font-weight-semibold)`
      참조 지점 — 토큰 삭제 시 함께 고쳐야 한다), 파일 끝 `color-mix(in srgb, ...)` 사용례
    - `src/app/lesson/[lessonId]/page.tsx` — 치환 대상 3곳(48줄 h1, 60줄 h2, 61줄 p)
    - `scripts/check-brand.mjs` 전문 — `walk`/`scanFile` 순회, violations 누적, 정렬 출력,
      "0건 검사 = 성공 위장 금지"(126-131줄), 성공 시 `console.log` + `process.exit(0)`
    - `.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md` § Typography 전체 —
      `@theme` 토큰 선언 블록과 `.prose h1–h4` / `.prose code` 규칙이 그대로 최종안이다
    - `.planning/phases/06-site-wide-design-polish/06-RESEARCH.md` § Pitfall 1, § Pitfall 2
  </read_first>
  <action>
**(1) `@theme` 정정 (D-95, D-R4K-4).** 06-UI-SPEC.md § Typography의 `@theme` 토큰 선언 블록을
그대로 삽입한다: `--text-display: 1.875rem` + `--text-display--line-height: 1.2`,
`--text-heading: 1.375rem` + `1.3`, `--text-subhead: 1.0625rem` + `1.4`, `--text-body: 1rem` + `1.6`,
`--text-label: 0.875rem` + `1.4`. 죽은 `--font-size-label/body/heading/display` 4종과
`--font-weight-regular/semibold` 2종은 삭제한다. 삭제 부작용 1곳: `.prose summary`가
`font-weight: var(--font-weight-semibold)`를 참조하므로 숫자 `600`으로 바꾼다(허용 굵기 집합 안).

**(2) `@theme` 밖 리터럴 색 제거 (D-88a, SC1).** 현재 `globals.css`에는 `@theme` 밖 리터럴이 4개
있다: `body`의 밝은 테마 본문 글자색 hex, `.dark body`의 어두운 테마 본문 글자색 hex, 복사 버튼
라이트/다크 배경의 `rgba(...)` 2개. 앞의 둘은 값을 그대로 둔 채 `@theme` 안으로 옮겨
`--color-foreground` / `--color-foreground-dark`로 선언하고 사용처는 `var()`로 바꾼다 — **새 색 값을
만드는 것이 아니라 이미 있던 값을 단일 출처로 옮기는 것**이며 렌더되는 색은 1비트도 바뀌지 않는다
(D-R4K-2 준수). 뒤의 둘은 각각 `color-mix(in srgb, var(--color-background) 90%, transparent)`와
`color-mix(in srgb, var(--color-background-dark) 90%, transparent)`로 바꾼다 — 계산 결과가 기존
rgba 값과 동일하고, 이 파일은 이미 파일 끝에서 `color-mix(in srgb, ...)`를 쓰고 있다. `html`이나
`:root`의 `font-size`는 재정의하지 않는다(0.9375rem = 15px 등가가 깨진다).

**(3) `.prose` 봉쇄 (D-R4K-4, D-R4K-5).** 06-UI-SPEC.md § Typography의 `.prose h1`–`.prose h4`
규칙 4줄과 `.prose code` 블록을 그대로 넣는다. 평범한 클래스 선택자로 충분하다 —
`@tailwindcss/typography`의 모든 선택자가 `:where()`로 감싸져 명세성 0이므로 `!important`나
`@layer`가 필요 없고 소스 순서도 무관하다. 인라인 코드는 `::before`/`::after`의 `content`를 `none`으로
지우고(백틱 글리프 제거), `font-size: 0.9375rem` **과 함께 `font-weight: 400`을 반드시 같이** 지정한다
— 플러그인 기본 굵기가 600이라 크기만 덮으면 굵기 이탈이 그대로 남는다(06-RESEARCH.md Pitfall 2).
배경 칩은 `var(--color-surface)`, 다크는 `.dark .prose code`에서 `var(--color-surface-dark)`,
좌우 여백은 `padding-inline`, 모서리는 `border-radius`. 새 색 없음. 줄바꿈 관련 속성은 건드리지
않는다 — 칩은 배경과 padding만 더하고 한국어 본문의 줄바꿈 동작을 바꾸지 않는다.

**(4) 레슨 페이지 3곳 치환 (D-95).** `src/app/lesson/[lessonId]/page.tsx`의 h1은 display 역할
유틸리티 + `font-bold`, "콘텐츠 준비 중입니다" h2는 heading 역할 + `font-bold`, 그 아래 안내 문단은
body 역할 + `font-normal`로 바꾼다. `leading-[...]` 짝은 토큰의 `--line-height` 접미사가 대신하므로
같이 지운다. 이 파일의 다른 구조(`<article>` 컨테이너, `gap-6`, 잠금 분기)는 **이 태스크에서
건드리지 않는다** — 06-06이 소유한다.

**(5) `scripts/check-design-tokens.mjs` 신설 (D-88, D-96).** `check-brand.mjs`의 골격을 그대로 따른다:
shebang, 외부 의존성 0(node 표준 모듈만), `path.dirname(fileURLToPath(import.meta.url))` 기준 저장소
루트 계산, violations 배열 누적 후 일괄 출력, 성공 시 요약 `console.log` + exit 0.

검사 대상: `src/**/*.tsx`와 `src/app/globals.css`.

규칙 (a) **리터럴 색**: `@theme` 블록 **밖**의 hex(`#rgb`/`#rrggbb`/`#rrggbbaa`)·`rgb(`·`rgba(`·
`hsl(`·`hsla(`. `@theme` 블록 경계는 여는 줄부터 **중첩 중괄호를 세어** 찾은 대응 닫는 줄까지다 —
첫 `}`에서 끊으면 안 된다. 매칭 전에 CSS 주석(`/* */`)과 JS/TS 주석(`//`, `/* */`)을 제거한다 —
`globals.css`와 여러 `.tsx`의 주석에 참고용 hex가 남아 있고, 04-UI-REVIEW도 하드코딩 색 0건을
판정할 때 주석을 제외했다.

규칙 (b) **타이포 리터럴**: `font-size`/`font-weight` CSS 선언과 `text-[...]`/`font-[...]` 클래스에서
나오는 값이 크기 `{1.875rem, 1.375rem, 1.0625rem, 1rem, 0.9375rem, 0.875rem}`, 굵기 `{400, 600, 700}`
밖이면 위반. **rem 값을 px로 환산해 비교하지 않는다** — 선언된 문자열 그대로 대조한다(환산이 반올림
오차를 만든다).

규칙 (c) **임의값 대괄호 + Tailwind 기본 팔레트 색 유틸리티** (D-96): `text-[...]`, `bg-[...]`,
`leading-[...]` 등 대괄호 임의값과 `text-white`/`bg-white`/`text-black`/`bg-black` 및
`{색이름}-{숫자}` 형태의 Tailwind 기본 팔레트 유틸리티. **이 규칙만 기본 비활성**이다 — 상단에
`const ENFORCE_ARBITRARY_VALUES = false;`를 두고 "66곳 치환 완료 후 06-08 태스크에서 true로 뒤집는다"는
주석을 단다. 비활성일 때는 잔여 건수를 정보성 한 줄로 출력하고 exit code에 반영하지 않는다.
`--strict` 플래그를 주면 이 상수와 무관하게 활성화된다. 06-UI-SPEC.md 권고에 따라 `text-white`는
allowlist에 등록한다(4곳, 전부 accent 배경 위 고정 대비용 CTA 텍스트 — 토큰화해도 값이 항상
동일해 실익이 없다). allowlist 사유를 스크립트 주석에 남긴다.

allowlist 두 번째 항목: `src/app/about/page.tsx`의 장식용 점 마커 세로 정렬에 쓰이는 `em` 단위
임의값 오프셋 1건. 06-RESEARCH.md 실측상 간격 계열 임의값은 코드베이스 전체에서 이것 하나뿐이고,
값이 폰트 크기에 상대적이어야 해서 rem 기반 Tailwind 기본 스페이싱 스케일에 대응물이 없다.
파일 경로 + 정확한 토큰 문자열로 좁게 등록하고 사유를 주석에 남긴다 — 와일드카드로 열지 않는다.

클래스 매칭은 **단어 경계 기준**이라 `text-whitesmoke`나 `my-text-white`를 오탐하지 않아야 한다.
임의값 대괄호는 `[^\]]*` 같은 단순 정규식으로 끊지 말고 **대괄호 짝을 세어** 중첩·이스케이프
(`text-[calc(1rem_+_2px)]`)까지 온전히 잡는다.

`--only <path...>` 플래그: 지정한 경로만 검사한다. 뒤 웨이브의 파일 단위 치환 태스크가 자기가
소유한 파일만 정확히 검증할 수 있게 하는 용도다.

출력: 위반은 **파일 경로 오름차순, 같은 파일 안에서는 줄 번호 오름차순**으로 정렬한다 — 실행마다
순서가 달라지면 로그 diff를 읽을 수 없고 파일시스템 순회 순서에 의존해선 안 된다. 스캔한 파일 수를
항상 출력하고, **0개면 통과가 아니라 exit 1**이다(glob 오타로 아무것도 안 훑고 초록불을 내는 것이
이 게이트의 가장 위험한 실패 모드다).
  </action>
  <verify>
    <automated>node scripts/check-design-tokens.mjs &amp;&amp; node scripts/check-design-tokens.mjs --strict --only "src/app/lesson/[lessonId]/page.tsx" &amp;&amp; npx next build &amp;&amp; node --env-file=.env.local scripts/e2e-typography.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-design-tokens.mjs`가 exit 0이고, 스캔한 파일 수를 0보다 큰 값으로 출력한다
    - `node scripts/check-design-tokens.mjs --strict --only "src/app/lesson/[lessonId]/page.tsx"`가 exit 0
    - `node scripts/check-design-tokens.mjs --strict`(전체)는 아직 exit 1이며, 잔여 위반이 63건이고
      전부 아직 치환하지 않은 22개 파일에만 존재한다 — 트레이서 파일은 목록에 없다
    - `node scripts/check-design-tokens.mjs --only src`를 존재하지 않는 경로로 바꿔 실행하면
      exit 1이다("0건 검사 = 성공 위장 금지" 방어 로직 동작 확인)
    - `npx next build`가 성공한다
    - `node --env-file=.env.local scripts/e2e-typography.mjs`가 **exit 0**이다 — 크기 히스토그램에
      30/22/17/16/15/14px 밖의 값이 없고(특히 24px와 12.25px가 사라졌다), 굵기 히스토그램에
      400/600/700 밖의 값이 없다
    - 렌더된 레슨 본문에 리터럴 백틱 글리프가 없다: `--report-only` 실행 로그가 아니라
      `node --env-file=.env.local scripts/e2e-typography.mjs`가 초록불인 것으로 크기·굵기를
      확인하고, 백틱은 브라우저에서 `page.evaluate`로 인라인 코드 요소의
      `::before` content가 `none`인지 확인하는 단언을 스크립트에 추가해 덮는다
    - 기존 자동 게이트 10종 전부 exit 0 (D-94): `check-brand` `check-lesson-structure`
      `check-manifest` `check-pace` `check-progress-gates` `check-progress-math` `check-schedule`
      `check-supabase-progress` `e2e-progress` `e2e-today`
  </acceptance_criteria>
  <reversibility rating="costly">D-R4K-4의 5종/3굵기 표는 6종 화면 전체의 크기 결정을 지배하고 `01-UI-SPEC.md`의 상위 계약을 개정한다 — 되돌리려면 이후 모든 치환을 함께 되돌려야 한다. 다만 값은 CONTEXT.md와 06-UI-SPEC.md가 이미 확정했고 06-UI-SPEC.md § 상위 계약 개정 고지가 승계 경로를 명시했으므로 실행 단계에서 재결정할 여지는 없다.</reversibility>
  <done>토큰 선언 → 유틸리티 생성 → 컴포넌트 사용 → 렌더된 computed style → 정적/런타임 게이트까지 한 경로가 전부 초록불이고 커밋되어 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm 레지스트리 → 로컬 `node_modules` | 신규 devDependency 1개(`@playwright/test`)와 Chromium 바이너리가 이 경계를 넘는다. 프로덕션 번들에는 포함되지 않는다 |
| 저장소 소스 → 자동 게이트 판정 | 게이트가 조용히 통과하면 이후 Phase가 "정리됐다"는 거짓 신호 위에 쌓인다 |

이 플랜은 인증(`hasUnlockCookie`, `/unlock` 시크릿 비교)·세션·데이터 접근·렌더링 전략을 **하나도**
바꾸지 않는다. 사용자 입력을 새로 받는 표면도 없다. ASVS L1 기준 V2/V3/V5/V6은 해당 없음.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | `npm install -D @playwright/test` | high | mitigate | 06-RESEARCH.md Package Legitimacy Audit 완료(`[SUS]` 사유 = `too-new`뿐, 주간 5,640만 다운로드·Microsoft 공식 저장소·`postinstall` 없음). Task 1의 `checkpoint:decision`이 설치 전 사람 확인을 강제한다 — `workflow.auto_advance`로 자동 승인되지 않는다 |
| T-06-01 | Tampering | `scripts/check-design-tokens.mjs` | medium | mitigate | 스캔 파일 수 0 = exit 1(성공 위장 금지), 위반 정렬 출력 고정, 주석 제거 후 매칭, 단어 경계·대괄호 짝 매칭 — 게이트가 빈 결과로 초록불을 내는 경로를 막는다 |
| T-06-02 | Tampering | `scripts/e2e-typography.mjs` | medium | mitigate | 측정 대상 요소 0개 = 실패. `--report-only` 1차 실행이 알려진 이탈(24px)을 실제로 탐지하는지를 acceptance criteria로 못박아 "아무것도 안 재고 통과"를 배제한다 |
| T-06-03 | Information Disclosure | `.env.local`의 `UNLOCK_SECRET`/Supabase 키 | low | accept | 신규 e2e 스크립트가 기존 2종과 동일하게 `--env-file=.env.local`로 값을 읽되 로그·에러 메시지에 값을 출력하지 않는다. 저장소에 커밋되지 않는 파일이며 이 Phase가 노출 표면을 늘리지 않는다 |
</threat_model>

<verification>
1. `node scripts/check-design-tokens.mjs` → exit 0, 스캔 파일 수 > 0
2. `node --env-file=.env.local scripts/e2e-typography.mjs` → exit 0
3. `npx next build` → 성공
4. 기존 게이트 10종 순차 실행 → 전부 exit 0 (정적 8종 먼저, e2e 2종 나중)
5. `node scripts/check-design-tokens.mjs --strict` → exit 1, 잔여 63건, 전부 미치환 22개 파일
</verification>

<success_criteria>
- `--text-*` 5종이 `@theme`에 선언되고 Tailwind가 대응 유틸리티를 실제로 생성한다
- 레슨 페이지가 렌더될 때 크기 5종(+인라인 코드 15px)·굵기 3종 밖의 값이 하나도 없다
- `globals.css`에 `@theme` 밖 리터럴 색이 0건이다
- 정적 게이트와 런타임 게이트 2종이 존재하고 둘 다 초록불이다
- 기존 게이트 10종 무회귀
</success_criteria>

<output>
Create `.planning/phases/06-site-wide-design-polish/06-01-SUMMARY.md` when done.
`--report-only` 1차 히스토그램(치환 전 baseline)과 확정된 측정 범위 셀렉터를 요약에 남긴다 —
06-08의 최종 스위트가 같은 범위로 재실행되어야 한다.
</output>
