---
phase: 06-site-wide-design-polish
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/page.tsx
  - src/app/curriculum/page.tsx
  - src/app/schedule/page.tsx
  - src/app/step/[stepId]/page.tsx
  - src/app/about/page.tsx
  - src/app/not-found.tsx
  - src/app/unlock/done/page.tsx
  - src/components/progress-summary.tsx
  - src/components/pace-status.tsx
  - src/components/dday-countdown.tsx
  - src/components/behind-lessons-list.tsx
autonomous: true
requirements: [SC1, SC2, SC4]
user_setup: []

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "라우트 페이지 7종과 홈 대시보드 컴포넌트 4종에 Tailwind 임의값 대괄호 타이포 문법이 남아 있지 않다 (D-95)"
    - "6종 화면의 h1이 모두 display 역할(30px/700)로 통일되고, 옛 28px/600 조합이 어느 화면에도 남아 있지 않다 (D-R4K-4)"
    - "accent 배경 CTA 버튼의 텍스트는 body 역할 + font-semibold(16px/600)로 확정된다 — 06-UI-SPEC이 계획 재량으로 남긴 body+semibold 조합을 14px로 낮추지 않고 유지하며, 크기 5종·굵기 3종 집합 안에 있으므로 D-89 런타임 게이트를 통과한다"
    - "밀린 레슨이 0개면 BehindLessonsList가 렌더되지 않는다 — 빈 목록 상자가 남지 않는다 (기존 동작 유지)"
    - "readCompletedLessonIds가 실패하면 ProgressReadError가 진행률과 페이스 자리를 대신한다 — 0퍼센트나 잘못된 페이스를 표시하지 않는다 (기존 동작 유지)"
    - "쿠키가 있는 실사용 상태에서 D-day·오늘카드·페이스·밀린레슨(조건부)·전체진행률 5개 섹션이 렌더된다"
    - "쿠키가 없으면 진행률·페이스·밀린레슨 3개 섹션이 통째로 빠지고 D-day와 오늘카드만 남는다"
    - "밀린 레슨이 0개면 미렌더, 1개 이상이면 목록 렌더이며 상한은 전체 레슨 수 35다"
    - "6종 화면의 컨테이너 공통값(mx-auto flex w-full flex-1 flex-col, px-4 py-12 sm:px-6 lg:px-8)과 2트랙 max-width 분리(그리드형 5xl / 읽기형 3xl)가 그대로 유지된다 — 폭 통일은 이 Phase의 목표가 아니다 (D-99)"
    - "임의값 치환은 멱등이다 — 이미 시맨틱 클래스로 바뀐 곳을 다시 치환해도 변화가 없고, 역방향 치환이 발생하지 않는다"
    - statement: "375px에서 진행률 바와 페이스 패널이 넘치지 않음을 06-08의 D-91 게이트가 홈 라우트에서 검증한다"
      verification: backstop
    - statement: "밀린 레슨 목록의 긴 제목 줄바꿈을 시각 확인한다 — 목록 행이 카드 리스트 행 계약을 따르는지 함께 본다"
      verification: backstop
    - "기존 자동 게이트 10종이 전부 통과한다 (D-94)"
  artifacts:
    - "src/app/ 아래 라우트 페이지 7종 (page, curriculum, schedule, step/[stepId], about, not-found, unlock/done)"
    - "src/components/ 아래 홈 대시보드 컴포넌트 4종 (progress-summary, pace-status, dday-countdown, behind-lessons-list)"
  key_links:
    - "각 페이지의 h1 → display 역할 유틸리티 → @theme의 --text-display — 한 화면이라도 빠뜨리면 6종 화면의 제목 크기가 어긋나 성공 기준 2가 깨진다"
    - "progress-summary/pace-status/dday-countdown/behind-lessons-list는 홈이 이미 계산해 넘겨준 props만 표시한다 — 이 플랜은 그 데이터 흐름을 건드리지 않는다"
  prohibitions:
    - statement: "새 색 토큰을 도입하지 않는다 — accent 배경 CTA의 흰 글자색 유틸리티는 06-01 게이트 allowlist에 등록된 4곳이므로 그대로 둔다 (D-R4K-2)"
      status: active
      verification: flagged
    - statement: "새 폰트 파일을 추가하지 않는다 (D-R4K-3)"
      status: active
      verification: flagged
    - statement: "새 데이터·새 쿼리·새 DB 필드를 만들지 않는다 — 이 플랜은 className만 바꾼다"
      status: active
      verification: flagged
    - statement: "홈과 커리큘럼의 섹션 배치를 바꾸지 않는다 — 빈 캔버스 재배치(D-90)는 06-07이 소유한다"
      status: active
      verification: flagged
    - statement: "375px 대응을 이유로 truncate나 고정폭 클래스를 새로 도입하지 않는다"
      status: active
      verification: flagged
    - statement: "렌더링 전략(force-dynamic, generateStaticParams)을 바꾸지 않는다"
      status: active
      verification: flagged
    - statement: "사용자에게 보이는 어떤 문자열에도 교육기관명(K로 시작하는 4글자 브랜드)을 쓰지 않는다"
      status: active
      verification: flagged
---

<objective>
66곳 임의값 중 가장 큰 덩어리 31곳을 시맨틱 클래스로 치환한다. 라우트 페이지 7종의 `h1`과
홈 대시보드 컴포넌트 4종이 대상이다.

여기서 실제로 크기가 바뀌는 것은 두 조합뿐이다: 옛 28px/600 → display 30px/700, 옛 20px/600 →
heading 22px/700. 나머지(14px, 16px)는 값이 그대로이고 표현만 임의값에서 토큰으로 바뀐다 —
**코드베이스는 이미 일관된 크기 체계를 쓰고 있었고 토큰만 연결이 안 돼 있었다**(D-95).

Purpose: 성공 기준 1의 "모든 페이지가 그 토큰만 쓴다"를 화면 층에서 실제로 만든다.
Output: 11개 파일 정리.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-site-wide-design-polish/06-CONTEXT.md
@.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md
@.planning/phases/06-site-wide-design-polish/06-PATTERNS.md
@.planning/phases/06-site-wide-design-polish/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 라우트 페이지 7종 타이포 치환 (16곳)</name>
  <files>src/app/page.tsx, src/app/curriculum/page.tsx, src/app/schedule/page.tsx, src/app/step/[stepId]/page.tsx, src/app/about/page.tsx, src/app/not-found.tsx, src/app/unlock/done/page.tsx</files>
  <read_first>
    - `src/app/page.tsx` — 컨테이너(101줄)와 header 블록(102-107줄), `completedIds` 3분기 구조
      (이 태스크는 분기 구조를 건드리지 않는다)
    - `src/app/curriculum/page.tsx` 전체(36줄) — h1 1곳
    - `src/app/schedule/page.tsx` — 컨테이너(60줄)와 h1(62줄)
    - `src/app/step/[stepId]/page.tsx` — 컨테이너(41줄, 다른 5화면과 같은 `<main>` + `gap-8`
      원형이며 06-06의 레슨 페이지 수정이 이 형태를 따라간다), h1(44줄)
    - `src/app/about/page.tsx` — 컨테이너(18줄), h1(19줄), `prose prose-slate` 래퍼(23줄),
      장식용 점 마커의 em 단위 오프셋(25줄 — **건드리지 않는다**, 06-01 게이트 allowlist 항목)
    - `src/app/not-found.tsx` — h1(6줄)과 accent 배경 CTA 링크(12줄)
    - `src/app/unlock/done/page.tsx` — accent 배경 CTA 링크(37줄)
    - `.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md` § Typography 레거시 임의값 대응표 +
      § 컨테이너 폭·셸 계약(D-99, 2트랙 폭은 유지)
    - `.planning/phases/06-site-wide-design-polish/06-01-SUMMARY.md` — 확정된 유틸리티 클래스 이름
  </read_first>
  <action>
7개 라우트 페이지의 임의값 타이포 16곳을 06-UI-SPEC.md § Typography 레거시 대응표대로 치환한다.

**28px/semibold 조합 → display 역할 + `font-bold`** (30px/700). 홈·커리큘럼·일정표·Step·소개·
not-found의 `h1` 6곳이 여기 해당한다. 이 치환으로 6종 화면의 제목 크기가 처음으로 하나의
토큰에서 나온다.

**20px/semibold 조합 → heading 역할 + `font-bold`** (22px/700).

**14px/16px 조합 → label / body 역할 + 기존 굵기 유틸리티.** 값이 바뀌지 않는다.

**accent 배경 CTA 버튼(not-found, unlock/done)의 16px/semibold 조합 → body 역할 + `font-semibold`**
(16px/600). 06-UI-SPEC.md § Typography 대응표가 이 조합을 "재검토 대상, 계획 재량"으로 남겼는데,
14px로 낮추면 CTA 가독성이 떨어지고 D-R4K-4 표는 *크기 5종·굵기 3종*을 정하지 그 곱집합을
제한하지 않는다. 16px/600은 두 집합 안에 있으므로 D-89 런타임 게이트를 그대로 통과한다.
**이 조합을 프로젝트의 CTA 표준으로 확정한다** — 06-05의 `complete-button.tsx`와
Task 2의 `progress-summary.tsx` CTA도 같은 값을 쓴다.

CTA의 흰 글자색 유틸리티는 **그대로 둔다** — 06-01 게이트 allowlist에 등록된 4곳이며
전부 accent 배경 위 고정 대비용이다.

`leading-[...]` 짝은 토큰의 `--line-height` 접미사가 대신하므로 함께 지운다.

**손대지 않을 것:** 각 페이지의 컨테이너 className(`mx-auto flex w-full flex-1 flex-col`,
`px-4 py-12 sm:px-6 lg:px-8`, `max-w-5xl`/`max-w-3xl` 2트랙 분리, `gap-8`)은 그대로 둔다 —
D-99가 폭 통일이 목표가 아님을 명시했고, 레슨 페이지의 `gap-6` 이탈만 06-06이 고친다.
`about/page.tsx`의 `prose prose-slate` 수식어 차이도 유지한다(06-01의 전역 `.prose h1-h4`
오버라이드가 크기·굵기를 이미 통일했으므로 색 팔레트 차이만 남고 무해하다).
홈의 `completedIds` 3분기 구조와 각 페이지의 `force-dynamic` 선언도 건드리지 않는다.
  </action>
  <verify>
    <automated>node scripts/check-design-tokens.mjs --strict --only src/app/page.tsx src/app/curriculum/page.tsx src/app/schedule/page.tsx "src/app/step/[stepId]/page.tsx" src/app/about/page.tsx src/app/not-found.tsx src/app/unlock/done/page.tsx &amp;&amp; npx next build &amp;&amp; node --env-file=.env.local scripts/e2e-today.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 위 `--strict --only` 실행이 exit 0
    - `grep -c 'text-display' src/app/page.tsx src/app/curriculum/page.tsx src/app/schedule/page.tsx src/app/about/page.tsx src/app/not-found.tsx`가 각 파일에서 1 이상 —
      5개 화면의 h1이 display 역할을 쓴다(`step/[stepId]/page.tsx`도 동일)
    - `grep -c 'max-w-5xl' src/app/page.tsx src/app/curriculum/page.tsx src/app/schedule/page.tsx`가 각 1 이상,
      `grep -c 'max-w-3xl' "src/app/step/[stepId]/page.tsx" src/app/about/page.tsx`가 각 1 이상 —
      2트랙 폭 분리가 유지된다
    - `grep -c 'gap-8' src/app/page.tsx src/app/curriculum/page.tsx src/app/schedule/page.tsx "src/app/step/[stepId]/page.tsx" src/app/about/page.tsx`가 각 1 이상
    - `npx next build` 성공
    - `node --env-file=.env.local scripts/e2e-today.mjs`가 exit 0
    - 기존 게이트 10종 전부 exit 0 (D-94)
  </acceptance_criteria>
  <done>6종 화면의 제목이 하나의 display 토큰에서 나오고, 7개 라우트 페이지에 임의값 타이포가 0건이다.</done>
</task>

<task type="auto">
  <name>Task 2: 홈 대시보드 컴포넌트 4종 타이포 치환 (15곳)</name>
  <files>src/components/progress-summary.tsx, src/components/pace-status.tsx, src/components/dday-countdown.tsx, src/components/behind-lessons-list.tsx</files>
  <read_first>
    - `src/components/dday-countdown.tsx` 전체 — 28px 계기 숫자 2곳(16, 21줄), `whitespace-nowrap`과
      accent 색 유틸리티(유지 대상)
    - `src/components/progress-summary.tsx` 전체 — 28px 퍼센트 숫자(52줄), accent 배경 CTA(72줄,
      흰 글자색 allowlist 항목), 진행률 바 구조
    - `src/components/pace-status.tsx` 전체 — 3곳
    - `src/components/behind-lessons-list.tsx` 전체 — 4곳, 밀린 레슨 0개일 때 부모(홈)가
      아예 렌더하지 않는 조건부 구조(이 컴포넌트 안에서 빈 상태를 새로 만들지 않는다)
    - `.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md` § Typography 레거시 대응표
    - `.planning/phases/06-site-wide-design-polish/06-01-SUMMARY.md` — 확정된 유틸리티 클래스 이름
  </read_first>
  <action>
4개 컴포넌트의 임의값 타이포 15곳을 대응표대로 치환한다.

**28px/semibold 계기 숫자 → display 역할 + `font-bold`** (30px/700). D-day 카운트다운의 숫자 2곳과
전체 진행률 퍼센트 1곳이 여기 해당한다 — 이 숫자들이 홈의 시각적 앵커이므로 display 역할이 맞다.
`whitespace-nowrap`과 accent 색 유틸리티(`text-accent dark:text-accent-dark`)는 그대로 둔다.

**20px/semibold → heading 역할 + `font-bold`, 14px/16px → label / body 역할 + 기존 굵기.**

**`progress-summary.tsx`의 accent 배경 CTA 16px/semibold → body 역할 + `font-semibold`** —
Task 1에서 확정한 CTA 표준과 같은 값이다. 흰 글자색 유틸리티는 유지(allowlist 항목).

`leading-[...]` 짝은 함께 지운다.

**동작을 바꾸지 않는다:** 밀린 레슨 0개일 때 목록이 렌더되지 않는 것은 홈(`page.tsx`)의 조건부
분기가 담당하므로 이 컴포넌트들에 빈 상태 UI를 새로 만들지 않는다. 진행률 조회 실패 시
`ProgressReadError`가 대신 렌더되는 기존 3분기도 건드리지 않는다. props 시그니처·계산 로직·
데이터 흐름을 하나도 바꾸지 않는다 — className만 바꾼다.

`behind-lessons-list.tsx`의 레슨 제목에 `truncate`나 고정폭 클래스를 넣지 않는다 — 긴 제목은
줄바꿈되고 행 높이만 늘어난다.
  </action>
  <verify>
    <automated>node scripts/check-design-tokens.mjs --strict --only src/components/progress-summary.tsx src/components/pace-status.tsx src/components/dday-countdown.tsx src/components/behind-lessons-list.tsx &amp;&amp; npx next build &amp;&amp; node scripts/check-progress-math.mjs &amp;&amp; node scripts/check-pace.mjs &amp;&amp; node --env-file=.env.local scripts/e2e-progress.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 위 `--strict --only` 실행이 exit 0
    - `node scripts/check-progress-math.mjs`와 `node scripts/check-pace.mjs`가 exit 0 —
      진행률·페이스 계산 회귀 없음
    - `node --env-file=.env.local scripts/e2e-progress.mjs`가 exit 0 — 진행률 DOM 계약이 살아 있다
    - `git diff` 결과 이 4개 파일에서 props 타입·함수 시그니처·조건부 렌더 분기가 변경되지 않았다
      (변경 라인이 전부 className 문자열 안이다)
    - `npx next build` 성공
    - 기존 게이트 10종 전부 exit 0 (D-94)
  </acceptance_criteria>
  <done>홈 대시보드 4종에 임의값 타이포가 0건이고, 진행률·페이스·D-day의 동작과 계산이 그대로다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 (신규) | className 편집만이며 인증·세션·데이터 접근·입력 처리·라우팅에 손대지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-08 | Tampering | `progress-summary.tsx` / `behind-lessons-list.tsx`의 `data-*` DOM 계약 | medium | mitigate | `e2e-progress.mjs`가 이 속성들을 문자열 매칭으로 검증한다. Task 2의 acceptance criteria에 해당 게이트 통과를 포함해 className 편집이 `data-*`를 지우는 경로를 막는다 (D-94) |
| T-06-09 | Tampering | 11개 파일 동시 편집 중 조건부 분기 훼손 | medium | mitigate | acceptance criteria가 `git diff`의 변경 라인이 className 문자열 안에만 있을 것을 요구한다. `check-progress-math`/`check-pace`/`e2e-progress`/`e2e-today`가 분기 동작을 별도로 확인한다 |
| T-06-10 | Information Disclosure | `unlock/done/page.tsx` 편집 | low | accept | 이 파일은 결과 상태(`?state=ok|invalid`)만 읽고 시크릿을 다루지 않는다. 이 플랜은 그 로직을 건드리지 않고 CTA className만 바꾼다 |
</threat_model>

<verification>
1. `node scripts/check-design-tokens.mjs --strict --only <이 플랜이 소유한 11개 파일>` → exit 0
2. `npx next build` → 성공
3. `node scripts/check-progress-math.mjs`, `node scripts/check-pace.mjs` → exit 0
4. `node --env-file=.env.local scripts/e2e-progress.mjs`, `... e2e-today.mjs` → exit 0
5. `git diff --name-only`가 이 플랜의 `files_modified` 11개 밖으로 나가지 않는다
</verification>

<success_criteria>
- 라우트 페이지 7종과 홈 대시보드 4종에 임의값 대괄호 타이포가 0건이다
- 6종 화면의 h1이 모두 같은 display 토큰에서 나온다
- CTA 타이포 표준(body + semibold)이 확정되어 세 CTA에 같은 값이 쓰인다
- 2트랙 폭 분리와 `gap-8` 공통값이 유지된다
- 기존 게이트 10종 무회귀
</success_criteria>

<output>
Create `.planning/phases/06-site-wide-design-polish/06-04-SUMMARY.md` when done.
CTA 타이포 표준(body 역할 + `font-semibold`, 16px/600) 확정 사실을 요약에 남긴다. 같은 웨이브의
06-05가 `complete-button.tsx`에 같은 값을 독립적으로 적용하도록 지시받았으므로 두 플랜의 결과가
일치해야 한다 — 어긋나면 06-08의 런타임 히스토그램이 아니라 **시각 검수**에서만 드러나므로
요약에 명시적으로 기록한다.
</output>
