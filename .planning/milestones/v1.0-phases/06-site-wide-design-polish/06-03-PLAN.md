---
phase: 06-site-wide-design-polish
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/globals.css
  - src/components/step-card.tsx
  - src/components/today-lesson-card.tsx
  - src/components/module-accordion.tsx
  - src/components/schedule-table.tsx
autonomous: true
requirements: [SC1, SC2, SC4]
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "StepCard와 TodayLessonCard가 동일한 radius, surface, padding 계약(rounded-lg / bg-surface / p-4 / gap-3)을 따르고, 클릭 가능한 카드에만 card-interactive hover surface 톤 이동이 적용된다 (D-99, 06-UI-SPEC Card Contract)"
    - "StepCard의 border-l-4 Step 색 테두리는 TodayLessonCard에 없는 의도된 차이로 유지된다 — 이탈이 아니다"
    - "ModuleAccordion 레슨 행과 ScheduleTable 행이 min-h-11 / py-3 계약을 유지한 채 카드와 같은 hover 배경 톤을 재사용한다"
    - "ScheduleTable의 좁은 가로 padding은 의도된 예외로 유지된다 — 강제 통일하면 375px에서 날짜·배지·시간 고정폭 grid가 더 쉽게 넘친다"
    - "이 플랜이 소유한 4개 컴포넌트 파일에 Tailwind 임의값 대괄호 타이포 문법이 남아 있지 않다 (D-95)"
    - "카드가 하나도 없는 화면은 존재하지 않는다 — StepCard 3장과 TodayLessonCard 1장은 데이터와 무관하게 항상 렌더되고, 셸 계약은 카드 유무와 독립적으로 내비·컨테이너 패딩·섹션 gap에만 걸린다"
    - "StepCard는 항상 정확히 3장(Step 1, 2, 3), TodayLessonCard는 항상 정확히 1장 — 개수가 데이터에 따라 변하지 않는다"
    - "TodayLessonCard는 오늘 배정 레슨이 없을 때 before-start / after-range / buffer 상태 문구를 렌더한다 — 빈 카드가 되지 않는다 (기존 동작 유지)"
    - "Supabase 조회 실패 시 홈은 ProgressReadError를 렌더한다 — 카드가 잘못된 진행률을 보여주지 않는다 (기존 동작 유지, 이 Phase는 변경하지 않음)"
    - "쿠키가 없어 completedIds가 null이면 카드는 완료 배지 없이 렌더된다 — 카드 자체는 항상 완전한 형태를 유지한다"
    - "임의값 치환은 멱등이다 — 이미 시맨틱 클래스로 바뀐 곳을 다시 치환해도 변화가 없고, 역방향 치환이 발생하지 않는다"
    - "치환 분할 경계는 23개 파일이지 66개 매치가 아니다 — 같은 파일을 두 태스크가 동시에 수정하지 않는다"
    - statement: "375px에서 카드 내부 요소(배지, 제목, 소요시간)가 가로로 넘치지 않음을 06-08의 D-91 게이트가 scrollWidth <= clientWidth로 검증한다"
      verification: backstop
    - statement: "긴 레슨 제목이 카드 안에서 줄바꿈되고 카드 높이만 늘어나는지 시각 확인한다 — truncate 클래스를 새로 도입하지 않는다는 것이 계약이며, 실제 최장 제목 렌더는 시각 확인 대상"
      verification: backstop
    - "기존 자동 게이트 10종이 전부 통과한다 (D-94)"
  artifacts:
    - "src/app/globals.css (.card-interactive 라이트/다크 쌍 규칙)"
    - src/components/step-card.tsx
    - src/components/today-lesson-card.tsx
    - src/components/module-accordion.tsx
    - src/components/schedule-table.tsx
  key_links:
    - ".card-interactive 클래스 정의(globals.css) → 4개 컴포넌트의 <Link> className — 정의만 하고 붙이지 않으면 hover가 통째로 죽고, 붙이기만 하고 정의가 없으면 조용히 아무 일도 안 일어난다"
  prohibitions:
    - statement: "새 색 토큰을 도입하지 않는다 — hover는 기존 --color-badge-neutral-bg / --color-badge-neutral-bg-dark 안에서 명도만 이동한다 (D-R4K-2)"
      status: active
      verification: flagged
    - statement: "새 폰트 파일을 추가하지 않는다 (D-R4K-3)"
      status: active
      verification: flagged
    - statement: "375px 대응을 이유로 truncate나 고정폭 클래스를 새로 도입하지 않는다 — 넘치면 줄바꿈한다"
      status: active
      verification: flagged
    - statement: "ScheduleTable 행의 가로 padding을 ModuleAccordion에 맞춰 통일하지 않는다 — 06-UI-SPEC이 의도된 예외로 명시했다"
      status: active
      verification: flagged
    - statement: "새 라우트를 만들지 않고 DB 스키마를 바꾸지 않으며 렌더링 전략을 바꾸지 않는다"
      status: active
      verification: flagged
    - statement: "사용자에게 보이는 어떤 문자열에도 교육기관명(K로 시작하는 4글자 브랜드)을 쓰지 않는다"
      status: active
      verification: flagged
---

<objective>
"카드 하나로 통일" 요구를 원형별 계약 2개로 푼다 — 그리드에 놓이는 **카드**(StepCard,
TodayLessonCard)와 세로로 쌓이는 **행**(ModuleAccordion 레슨 행, ScheduleTable 행). 행에 카드의
padding·radius를 강제하면 표·리스트 밀도가 깨지므로 두 원형을 각각 하나의 규칙으로 모은다.

동시에 이 4개 파일이 소유한 임의값 타이포 19곳을 06-01이 세운 시맨틱 클래스로 치환한다.
치환과 계약 통일을 같은 태스크에 묶는 이유는 파일 단위 분할 경계를 지키기 위해서다 — 한 파일을
두 태스크가 나눠 만지지 않는다(06-EDGE-COVERAGE SC2/concurrency).

Purpose: 성공 기준 2의 "같은 셸"을 카드·행 층에서 실제로 만든다.
Output: `.card-interactive` 규칙 1쌍 + 4개 컴포넌트 정리.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-site-wide-design-polish/06-CONTEXT.md
@.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md
@.planning/phases/06-site-wide-design-polish/06-PATTERNS.md
@.planning/phases/06-site-wide-design-polish/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: globals.css에 .card-interactive hover 규칙 1쌍 추가</name>
  <files>src/app/globals.css</files>
  <read_first>
    - `src/app/globals.css` — 특히 복사 버튼 색 규칙 쌍(110-124줄 근처, `.prose [data-code-block] > button`
      + `.dark .prose [data-code-block] > button`)과 `.prose details` / `.dark .prose details` 쌍.
      **라이트 규칙을 먼저 쓰고 `.dark` 접두사로 다크를 오버라이드**하는 이 파일의 관례를 그대로 따른다
    - `.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md` § Card Contract — hover 규칙 원문
  </read_first>
  <action>
`globals.css`에 `.card-interactive:hover`와 `.dark .card-interactive:hover` 규칙 쌍을 추가한다.
배경색은 각각 `var(--color-badge-neutral-bg)`와 `var(--color-badge-neutral-bg-dark)` — 기존 중성
토큰 재사용이며 **새 색 값을 만들지 않는다**(D-R4K-2).

규칙 바로 위에 한국어 주석으로 남길 것: 이 클래스가 그리드 카드 2종과 리스트 행 2종에 공용으로
붙는다는 것, hover 피드백이 지금까지 전혀 없었다는 것(04-UI-REVIEW), 새 색 없이 기존 surface 톤
안에서 명도만 이동한다는 것.

포커스 아웃라인은 이 규칙에 넣지 않는다 — 브라우저 기본 아웃라인을 유지하는 것이
06-UI-SPEC.md § Focus-visible 계약의 결정이다. `transition-colors duration-150`은 CSS가 아니라
각 컴포넌트의 Tailwind 클래스로 붙인다(Task 2·3).

`@theme` 블록은 이 태스크에서 건드리지 않는다 — 06-01이 이미 확정했다.
  </action>
  <verify>
    <automated>node scripts/check-design-tokens.mjs &amp;&amp; npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'card-interactive' src/app/globals.css`가 2 이상을 반환한다(라이트 + 다크 쌍)
    - `node scripts/check-design-tokens.mjs`가 exit 0 — 새 규칙이 `@theme` 밖 리터럴 색을 도입하지 않았다
    - `npx next build`가 성공한다
    - `node scripts/check-brand.mjs`가 exit 0 (D-94)
  </acceptance_criteria>
  <done>4개 컴포넌트가 공유할 hover 계약이 CSS 한 곳에 정의되어 있다.</done>
</task>

<task type="auto">
  <name>Task 2: 그리드 카드 2종 — padding 통일 + hover + 타이포 치환 (8곳)</name>
  <files>src/components/step-card.tsx, src/components/today-lesson-card.tsx</files>
  <read_first>
    - `src/components/step-card.tsx` 전체 — 특히 `STEP_BORDER_CLASSES` 리터럴 맵(8-12줄, Tailwind JIT이
      템플릿 문자열 조합을 스캔하지 못해 동적 클래스명을 만들지 않는 이유가 주석에 있다),
      컨테이너 className(30줄), 타이포 4곳(33/36/38/41줄)
    - `src/components/today-lesson-card.tsx` 전체 — 컨테이너 className(79줄, `p-6` 이탈 지점),
      `CTA_CLASS` 상수(26줄), 타이포 4곳(91/105/106줄 부근)
    - `.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md` § Card Contract 그리드 카드 표 +
      § Typography 레거시 임의값 대응표
    - `.planning/phases/06-site-wide-design-polish/06-01-SUMMARY.md` — 확정된 유틸리티 클래스 이름
  </read_first>
  <action>
**padding 통일:** `today-lesson-card.tsx`의 카드 컨테이너 padding을 `p-6`에서 `p-4`로 낮춰
`step-card.tsx`와 맞춘다(06-UI-SPEC Card Contract 표준값 16px). radius(`rounded-lg`),
surface(`bg-surface dark:bg-surface-dark`), 내부 gap(`gap-3`)은 이미 양쪽이 부합하므로 손대지 않는다.
`step-card.tsx`의 `border-l-4` Step 색 테두리는 **의도된 차이**이므로 유지하고
`today-lesson-card.tsx`에 추가하지 않는다.

**hover:** 두 카드의 클릭 가능한 최상위 요소(`<Link>`)의 className에 `card-interactive`와
`transition-colors duration-150`을 더한다. 클릭 불가능한 상태(오늘 배정 레슨이 없어 상태 문구만
렌더되는 분기)에는 붙이지 않는다 — hover 피드백이 있는데 눌리지 않으면 거짓 아핀던스가 된다.

**타이포 치환(8곳):** 06-UI-SPEC.md § Typography 레거시 대응표대로 1:1 치환한다.
14px/normal 조합 → label 역할 + `font-normal`, 14px/semibold → label 역할 + `font-semibold`,
16px/normal → body 역할 + `font-normal`, 20px/semibold → **heading 역할 + `font-bold`**(20px가
22px로, 굵기가 600에서 700으로 바뀌는 유일한 조합이다). `leading-[...]` 짝은 토큰의
`--line-height` 접미사가 대신하므로 함께 지운다.

`today-lesson-card.tsx`의 `CTA_CLASS` 안 14px/semibold 조합도 같은 규칙으로 치환한다. 그 안의
흰 글자색 유틸리티는 **그대로 둔다** — 06-01의 게이트 allowlist에 등록된 4곳 중 하나이며
(accent 배경 위 고정 대비용), 바꾸면 대비 실측을 다시 해야 한다.

**멱등성:** 이미 시맨틱 클래스로 바뀐 지점을 되돌리지 않는다. 치환은 임의값 대괄호 형태가
남아 있는 곳에만 적용한다.

카드 안 텍스트에 `truncate`나 고정폭 클래스를 새로 넣지 않는다 — 긴 레슨 제목은 줄바꿈되고
카드 높이만 늘어나는 것이 계약이다.
  </action>
  <verify>
    <automated>node scripts/check-design-tokens.mjs --strict --only src/components/step-card.tsx src/components/today-lesson-card.tsx &amp;&amp; npx next build &amp;&amp; node --env-file=.env.local scripts/e2e-today.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-design-tokens.mjs --strict --only src/components/step-card.tsx src/components/today-lesson-card.tsx`가 exit 0
    - `grep -c 'card-interactive' src/components/step-card.tsx src/components/today-lesson-card.tsx`가 각 파일에서 1 이상
    - `grep -c 'p-4' src/components/today-lesson-card.tsx`가 1 이상이고, 카드 컨테이너의 padding이
      `step-card.tsx`와 같은 값이다
    - `grep -c 'border-l-4' src/components/step-card.tsx`가 1 이상 — Step 색 테두리가 유지된다
    - `npx next build` 성공
    - `node --env-file=.env.local scripts/e2e-today.mjs`가 exit 0 — 오늘 카드의 `data-*` 계약이
      살아 있다(카드 개수·상태 문구 회귀 없음)
    - 기존 게이트 10종 전부 exit 0 (D-94)
  </acceptance_criteria>
  <done>두 그리드 카드가 같은 padding·radius·surface·hover 규칙을 따르고 임의값 타이포가 남아 있지 않다.</done>
</task>

<task type="auto">
  <name>Task 3: 리스트 행 2종 — hover + 타이포 치환 (11곳)</name>
  <files>src/components/module-accordion.tsx, src/components/schedule-table.tsx</files>
  <read_first>
    - `src/components/module-accordion.tsx` 전체 — 특히 레슨 행 className(58줄, `min-h-11 ... py-3`),
      부모 `ul`의 가로 padding(50줄), 타이포 4곳(40/41/69/76줄 부근)
    - `src/components/schedule-table.tsx` 전체 — 행 className(54줄, `min-h-11 ... px-2 py-3`,
      오늘 행 강조 `TODAY_ROW_CLASS`), 타이포 7곳
    - `.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md` § Card Contract 리스트 행 표 —
      가로 padding을 통일하지 않는 이유(일정표의 날짜+배지+시간 고정폭 grid 정렬 제약)
    - `.planning/phases/06-site-wide-design-polish/06-01-SUMMARY.md` — 확정된 유틸리티 클래스 이름
  </read_first>
  <action>
**hover:** 두 컴포넌트의 클릭 가능한 행(`<Link>`)에 `card-interactive`와
`transition-colors duration-150`을 더한다. `schedule-table.tsx`의 오늘/개강일 행 강조
(`TODAY_ROW_CLASS`) 로직은 그대로 유지한다 — hover는 그 위에 얹히는 별개 상태다.

**유지할 것(이탈이 아니다):** 최소 높이 `min-h-11`과 세로 padding `py-3`은 이미 양쪽이 부합하므로
손대지 않는다. **가로 padding은 통일하지 않는다** — 일정표 행이 좁은 가로 padding을 쓰는 것은
날짜·배지·시간 고정폭 grid 정렬 제약 때문이고, 강제로 넓히면 375px에서 표가 더 쉽게 넘친다
(06-UI-SPEC Card Contract). divider 관례 차이(아코디언 목록의 구분선 vs 일정표의 좌측 강조)도
원형 차이로 유지한다.

**타이포 치환(11곳):** 06-UI-SPEC.md § Typography 레거시 대응표대로 1:1 치환한다.
`module-accordion.tsx`의 20px/semibold 모듈 헤더는 heading 역할 + `font-bold`(22px/700),
나머지는 label/body 역할로. `schedule-table.tsx`의 7곳은 전부 14px 계열이므로 label 역할 +
기존 굵기 유틸리티(`font-normal`/`font-semibold`)를 유지한다. `leading-[...]` 짝은 함께 지운다.

행 텍스트에 `truncate`나 고정폭 클래스를 새로 넣지 않는다.
  </action>
  <verify>
    <automated>node scripts/check-design-tokens.mjs --strict --only src/components/module-accordion.tsx src/components/schedule-table.tsx &amp;&amp; npx next build &amp;&amp; node scripts/check-schedule.mjs &amp;&amp; node scripts/check-pace.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-design-tokens.mjs --strict --only src/components/module-accordion.tsx src/components/schedule-table.tsx`가 exit 0
    - `grep -c 'card-interactive' src/components/module-accordion.tsx src/components/schedule-table.tsx`가 각 파일에서 1 이상
    - `grep -c 'min-h-11' src/components/module-accordion.tsx src/components/schedule-table.tsx`가 각 파일에서 1 이상 — 44px 터치 타깃 유지
    - `node scripts/check-schedule.mjs`와 `node scripts/check-pace.mjs`가 exit 0 — 일정표 계산·표시 회귀 없음
    - `npx next build` 성공
    - 기존 게이트 10종 전부 exit 0 (D-94)
  </acceptance_criteria>
  <done>두 리스트 행이 카드와 같은 hover 톤을 쓰면서 각자의 밀도 계약(최소 높이·세로 padding·의도된 가로 padding 차이)을 유지하고, 임의값 타이포가 남아 있지 않다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 없음 (신규) | 이 플랜은 CSS 클래스와 Tailwind 유틸리티만 바꾼다. 인증·세션·데이터 접근·입력 처리·라우팅에 손대지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Tampering | `today-lesson-card.tsx` / `schedule-table.tsx`의 `data-*` DOM 계약 | medium | mitigate | 기존 `e2e-today.mjs`·`check-schedule.mjs`가 이 속성들을 문자열 매칭으로 검증한다. 두 태스크의 acceptance criteria에 해당 게이트 통과를 포함해 className 편집이 `data-*` 속성을 실수로 지우는 경로를 막는다 (D-94) |
| T-06-07 | Information Disclosure | 신규 UI 문구 | low | accept | 이 플랜은 새 사용자 문구를 추가하지 않는다(클래스 편집만). `check-brand.mjs`가 어차피 전 소스를 훑는다 |
</threat_model>

<verification>
1. `node scripts/check-design-tokens.mjs --strict --only <이 플랜이 소유한 4개 파일>` → exit 0
2. `npx next build` → 성공
3. `node --env-file=.env.local scripts/e2e-today.mjs` → exit 0
4. `node scripts/check-schedule.mjs`, `node scripts/check-pace.mjs` → exit 0
5. `git diff --name-only`가 이 플랜의 `files_modified` 5개 밖으로 나가지 않는다
</verification>

<success_criteria>
- 그리드 카드 2종이 하나의 padding·radius·surface·hover 계약을 따른다
- 리스트 행 2종이 같은 hover 톤을 쓰되 의도된 밀도 차이를 유지한다
- 이 플랜이 소유한 4개 파일에 임의값 대괄호 타이포가 0건이다
- 기존 게이트 10종 무회귀
</success_criteria>

<output>
Create `.planning/phases/06-site-wide-design-polish/06-03-SUMMARY.md` when done.
</output>
