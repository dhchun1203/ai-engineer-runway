---
phase: 05-step-2-3
plan: 07
type: execute
wave: 3
depends_on: ["05-02", "05-03", "05-04", "05-05", "05-06"]
files_modified:
  - scripts/check-manifest.mjs
autonomous: true
requirements: [CONT-05]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`scripts/check-manifest.mjs`의 `EXPECTED_HAS_CONTENT_COUNT`가 `.velite/lessons.json`에서 실측한 `hasContent: true` 레슨 개수와 정확히 같고, `EXPECTED_HAS_CONTENT_SLUGS`가 실측 slug 집합과 정렬 후 완전히 일치한다 (D-78, RESEARCH Pitfall 4)"
    - "Step 2 12편 전부가 `hasContent: true`이며, 실측 개수가 Wave 1 종료값 + 10이다 — Plan 02~06이 각각 2편씩 켰다 (D-77 Wave 2)"
    - "`node scripts/check-manifest.mjs`가 13개 불변식을 모두 통과하고 exit 0으로 끝난다 — Wave 2 동안 의도적으로 red였던 Invariant 10이 여기서 green으로 돌아온다"
    - "`node scripts/check-lesson-structure.mjs`가 Step 1 10편 + Step 2 12편 + Step 3 1편을 검사하고 exit 0으로 끝난다"
    - "`node scripts/check-brand.mjs`가 새로 추가된 Step 2 10편 본문을 포함해 exit 0으로 끝난다 (D-02)"
    - "Step 2 12편의 프로덕션 URL이 모두 200으로 응답하고 각 페이지 본문에 `정답 보기` 문자열이 포함된다 (D-77 — Step 2 배치가 실제로 배포됐다)"
    - "`scripts/e2e-today.mjs`가 통과한다 — 오늘의 학습 루프가 Step 2 레슨이 늘어난 매니페스트에서도 정상 동작한다"
    - "`estimatedMinutes` 총합 4200분 등식과 분포(Invariant 6·12·13)가 그대로 유지된다 — 이 wave는 본문만 채웠고 시간 값을 건드리지 않았다 (D-13, D-31)"
    - statement: "실측값과 CONTEXT.md D-78 원문 수치(`24`)의 일치/불일치가 SUMMARY에 명시적으로 기록됐다 (RESEARCH Open Question 1)"
      verification: backstop
    - statement: "2-1 모듈 두 레슨의 연습 SQL이 실제 Supabase SQL 에디터에서 오류 없이 실행된다 — 자격증명이 필요해 자동화할 수 없는 유일한 확인 항목이다"
      verification: backstop
  artifacts:
    - scripts/check-manifest.mjs
  key_links:
    - "Plan 02~06의 `hasContent` 전환 10건 → `.velite/lessons.json` → 이 Plan의 실측 → `EXPECTED_HAS_CONTENT_COUNT` — 한 편이라도 빠지면 Invariant 10이 slug 집합 불일치로 실패한다"
    - "이 Plan의 green → Wave 3(Plan 08~12) 착수 — Step 3 배치는 매니페스트가 green인 상태에서 시작해야 자기 wave의 red를 구분할 수 있다"
    - "Step 2 배포 → 일정표의 실제 학습 — 사전학습이 이미 진행 중이므로(2026-08-25 = 1일차) Step 2가 먼저 배포되어야 학습이 끊기지 않는다 (D-77 근거 a)"
  prohibitions:
    - statement: "`EXPECTED_HAS_CONTENT_COUNT`를 CONTEXT.md D-78 원문 수치나 손계산으로 정해서는 안 된다 — `.velite/lessons.json` 실측값만 근거다 (RESEARCH Open Question 1)"
      status: unverified
    - statement: "Invariant 10을 통과시키기 위해 어떤 레슨의 `hasContent`를 되돌리거나 `estimatedMinutes`를 조정해서는 안 된다 — 상수를 실측에 맞추는 방향으로만 고친다 (D-13)"
      status: unverified
    - statement: "이 Plan에서 레슨 본문을 새로 쓰거나 고쳐서는 안 된다 — 본문 수정이 필요하면 해당 Plan으로 되돌려 처리한다"
      status: unverified
    - statement: "게이트 스크립트의 검사 로직이나 다른 불변식 상수(총합 4200분, 분포, 프로젝트 모듈 수)를 바꿔서는 안 된다 — 이 Plan이 건드리는 것은 Invariant 10 상수 두 개와 그 주석뿐이다"
      status: unverified
---

<objective>
Step 2 배치(D-77 Wave 2)를 마감한다 — 매니페스트 기대 상수를 빌드 실측으로 맞추고, 전체 게이트를 green으로 되돌리고, Step 2 12편을 프로덕션에 배포한다.

Purpose: Plan 02~06이 병렬로 10편을 켜는 동안 `check-manifest.mjs` Invariant 10은 의도적으로 red였다 — 같은 상수를 다섯 Plan이 동시에 고치면 충돌하기 때문이다. 이 Plan이 그 상수를 한 번에, **손계산이 아니라 빌드 실측으로** 맞춘다. CONTEXT.md D-78 원문의 중간 경계 수치는 리서치 재계산과 어긋나므로(Open Question 1) 어느 쪽도 하드코딩하지 않는다.

Output: 실측으로 맞춰진 Invariant 10 상수, 전 게이트 green, Step 2 12편 프로덕션 배포.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-step-2-3/05-CONTEXT.md
@.planning/phases/05-step-2-3/05-01-SUMMARY.md
@.planning/phases/05-step-2-3/05-02-SUMMARY.md
@.planning/phases/05-step-2-3/05-03-SUMMARY.md
@.planning/phases/05-step-2-3/05-04-SUMMARY.md
@.planning/phases/05-step-2-3/05-05-SUMMARY.md
@.planning/phases/05-step-2-3/05-06-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces

**Plan 07 (이 Plan)이 만드는 것**
- `scripts/check-manifest.mjs` — `EXPECTED_HAS_CONTENT_COUNT`를 Wave 1 종료값에서 Wave 2 실측값으로 갱신(계산상 13 → 23), `EXPECTED_HAS_CONTENT_SLUGS`에 Step 2 신규 10개 slug 추가(`2-1-postgres-and-supabase`, `2-1-ai-data-modeling`, `2-2-html-css-js`, `2-2-browser-and-ui`, `2-3-typescript-setup`, `2-5-express-rest-api`, `2-5-auth-and-prisma`, `2-6-project-ai-shop-backend`, `2-7-prompt-patterns`, `2-7-promptops`), 상수 위 주석 갱신

**Phase 5 전체가 만들거나 바꾸는 것** — `05-01-PLAN.md` §Artifacts this phase produces 참조. 이 Plan은 `EXPECTED_HAS_CONTENT_COUNT`의 3단계 변화 중 2단계를 담당한다.
</artifacts_produced>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 매니페스트 Invariant 10 상수 실측 갱신 + 전체 게이트 green 복귀</name>
  <precondition>`.planning/phases/05-step-2-3/`에 `05-02-SUMMARY.md`부터 `05-06-SUMMARY.md`까지 5개 파일이 모두 존재한다 — Wave 2의 다섯 Plan이 전부 끝나지 않은 상태에서 상수를 맞추면 다음 Plan이 다시 red를 만든다. 하나라도 없으면 중단한다.</precondition>
  <files>scripts/check-manifest.mjs</files>
  <read_first>
    - `scripts/check-manifest.mjs` 1~30행 — `EXPECTED_HAS_CONTENT_COUNT`·`EXPECTED_HAS_CONTENT_SLUGS`의 현재 값과 그 위 주석, 그리고 함께 있는 다른 기대 상수(`EXPECTED_TOTAL_MINUTES` 4200, `EXPECTED_MINUTES_DISTRIBUTION`, `EXPECTED_PROJECT_MODULE_COUNT`). **뒤의 세 상수는 건드리지 않는다.**
    - `scripts/check-manifest.mjs` 190~206행 — Invariant 10이 개수뿐 아니라 slug 배열을 정렬해 `JSON.stringify`로 완전 일치 비교한다는 사실. slug 하나만 빠져도 실패한다.
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md` — Wave 1 종료 시점의 실측 카운트와 CONTEXT.md D-78 원문 수치의 일치/불일치 기록. 이 Plan이 같은 방식을 반복한다.
    - `.planning/phases/05-step-2-3/05-02-SUMMARY.md` ~ `05-06-SUMMARY.md` — 각 Plan이 켠 slug와 게이트 통과 시점의 총 개수. 실측값과 대조할 참고치다(근거는 아니다).
  </read_first>
  <action>
**먼저 실측한다.** `npm run build`를 돌린 뒤 `.velite/lessons.json`에서 `hasContent === true`인 레슨의 **개수**와 **slug 목록**을 실제로 뽑는다. 이 두 값이 상수의 유일한 근거다 — CONTEXT.md D-78 원문의 `24`도, RESEARCH 재계산의 `23`도 하드코딩하지 않는다.

`scripts/check-manifest.mjs`에서 정확히 세 가지만 바꾼다:
1. `EXPECTED_HAS_CONTENT_COUNT` — 실측 개수로 대입.
2. `EXPECTED_HAS_CONTENT_SLUGS` — 실측 slug 목록으로 대체. 기존 항목을 지우지 말고 Step 2 신규 10개를 추가하는 형태로 쓰되, 최종 배열이 실측 집합과 정렬 후 완전히 일치해야 한다. 가독성을 위해 Step 1 / Step 2 / Step 3 그룹으로 줄바꿈해 정리해도 좋다.
3. 두 상수 위 주석 — "Phase 5 Wave 2가 Step 2 잔여 10편을 채워 N에서 M이 되었다"는 근거를 남긴다. 현재 주석은 Phase 4가 2에서 11로 올린 근거를 담고 있으므로, 그 이력을 지우지 말고 한 줄 이어 붙인다.

`EXPECTED_TOTAL_MINUTES`, `EXPECTED_MINUTES_DISTRIBUTION`, `EXPECTED_PROJECT_MODULE_COUNT`와 Invariant 10 이외의 검사 로직은 **한 줄도 바꾸지 않는다.** 이 wave는 본문만 채웠으므로 시간 총합·분포·프로젝트 모듈 수는 그대로 통과해야 한다 — 통과하지 않는다면 어느 Plan이 프론트매터를 건드린 것이므로, 상수를 맞추지 말고 그 Plan으로 되돌려 원인을 고친다.

그다음 게이트 4종을 순서대로 돌려 전부 green을 확인한다: `npm run build`, `node scripts/check-manifest.mjs`, `node scripts/check-brand.mjs`, `node scripts/check-lesson-structure.mjs`. `check-lesson-structure`가 검사한 레슨 수가 실측 `hasContent: true` 개수와 같아야 한다 — 다르면 어느 스텁이 켜졌는데 구조가 미완이거나, 켜지지 않은 편이 있다는 뜻이다.

마지막으로 실측값과 CONTEXT.md D-78 원문 수치(`24`)를 대조해 일치 여부를 SUMMARY에 명시적으로 기록한다. Plan 13이 최종 35에서 같은 대조를 반복한다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-manifest.mjs &amp;&amp; node scripts/check-brand.mjs &amp;&amp; node scripts/check-lesson-structure.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 네 게이트 명령이 모두 exit 0이다.
    - `node scripts/check-manifest.mjs`의 출력이 13개 불변식 통과를 알리고 Invariant 10 실패 메시지가 없다.
    - `node -e "const l=require('./.velite/lessons.json');const s=require('fs').readFileSync('scripts/check-manifest.mjs','utf8');const n=Number(s.match(/EXPECTED_HAS_CONTENT_COUNT = (\d+)/)[1]);process.exit(n===l.filter(x=>x.hasContent).length?0:1)"`가 exit 0이다 (상수 = 실측 개수).
    - `node -e "const l=require('./.velite/lessons.json');const s=require('fs').readFileSync('scripts/check-manifest.mjs','utf8');const arr=s.match(/EXPECTED_HAS_CONTENT_SLUGS = \[([\s\S]*?)\]/)[1].match(/'[^']+'/g).map(x=>x.slice(1,-1)).sort();const act=l.filter(x=>x.hasContent).map(x=>x.slug).sort();process.exit(JSON.stringify(arr)===JSON.stringify(act)?0:1)"`가 exit 0이다 (상수 배열 = 실측 slug 집합).
    - Step 2 12편의 slug가 모두 `EXPECTED_HAS_CONTENT_SLUGS`에 있다: `2-1-postgres-and-supabase`, `2-1-ai-data-modeling`, `2-2-html-css-js`, `2-2-browser-and-ui`, `2-3-typescript-setup`, `2-3-react-components`, `2-4-project-ai-shop-frontend`, `2-5-express-rest-api`, `2-5-auth-and-prisma`, `2-6-project-ai-shop-backend`, `2-7-prompt-patterns`, `2-7-promptops`.
    - `node scripts/check-lesson-structure.mjs`가 출력한 검사 레슨 수가 실측 `hasContent: true` 개수와 같다.
    - `git diff scripts/check-manifest.mjs`의 변경이 `EXPECTED_HAS_CONTENT_COUNT` 줄, `EXPECTED_HAS_CONTENT_SLUGS` 배열, 그 위 주석에만 한정된다 — `EXPECTED_TOTAL_MINUTES`·`EXPECTED_MINUTES_DISTRIBUTION`·`EXPECTED_PROJECT_MODULE_COUNT` 줄과 190행 이후 검사 로직이 diff에 나타나지 않는다.
    - `git diff --name-only`에 `src/content/lessons/` 아래 파일이 나타나지 않는다 (이 Plan은 본문을 고치지 않는다).
    - SUMMARY에 실측 개수와 CONTEXT.md D-78 원문 수치(`24`)의 일치/불일치가 기록되어 있다.
  </acceptance_criteria>
  <done>매니페스트 기대 상수가 빌드 실측과 정확히 일치하고, Wave 2 동안 red였던 Invariant 10이 green으로 돌아왔다. 네 게이트가 전부 통과한다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Step 2 배치 프로덕션 배포 + 전체 레슨 도달 확인 + 오늘의 학습 루프 확인</name>
  <precondition>`git remote -v`에 `origin`이 있고 현재 브랜치가 `master`이며 푸시 권한이 있다. 없으면 중단하고 사용자에게 알린다 — 이 Task의 목적 자체가 Step 2 배치 배포다 (D-77).</precondition>
  <files>scripts/check-manifest.mjs</files>
  <read_first>
    - `scripts/e2e-today.mjs` — 오늘의 학습 루프 e2e가 무엇을 확인하는지, 어떤 환경변수를 요구하는지(`--env-file=.env.local`). 이 스크립트는 **고치지 않는다** — 매니페스트가 커져도 그대로 동작해야 한다.
    - `scripts/e2e-progress.mjs` 85~95행 — 프로브 레슨을 `hasContent`인 것 중에서 동적으로 고른다는 사실. Step 2가 늘어나도 코드 변경이 필요 없다.
    - `.planning/phases/05-step-2-3/05-02-SUMMARY.md` ~ `05-06-SUMMARY.md` — 각 Plan의 커밋이 로컬에만 있는지 이미 푸시됐는지 확인한다. Phase 1에서 로컬 커밋이 밀린 채 남아 있던 전례가 있다.
  </read_first>
  <action>
Task 1의 커밋을 포함해 로컬에 밀려 있는 커밋이 없는지 `git log origin/master..HEAD --oneline`으로 먼저 확인한다. 있으면 전부 포함해 `git push origin master`로 배포한다.

Vercel 배포가 끝날 때까지 기다린 뒤 **Step 2 12편 전체**의 프로덕션 URL을 확인한다. 기준 주소는 `https://ai-engineer-runway.vercel.app/lesson/{slug}` 이고 slug 12개는 `2-1-postgres-and-supabase`, `2-1-ai-data-modeling`, `2-2-html-css-js`, `2-2-browser-and-ui`, `2-3-typescript-setup`, `2-3-react-components`, `2-4-project-ai-shop-frontend`, `2-5-express-rest-api`, `2-5-auth-and-prisma`, `2-6-project-ai-shop-backend`, `2-7-prompt-patterns`, `2-7-promptops` 다. 각 URL이 200으로 응답하고 응답 본문에 `정답 보기` 문자열이 포함되는지 확인한다 — `정답 보기`가 없으면 그 레슨은 `<details>` 블록이 렌더되지 않았거나 아직 배포되지 않은 것이다. 어느 하나라도 실패하면 여기서 멈추고 원인을 SUMMARY에 남긴다.

그다음 "콘텐츠 준비 중" 빈 상태가 Step 2에서 더 이상 나타나지 않는지 확인한다 — 12개 응답 본문 어디에도 `콘텐츠 준비 중` 문자열이 없어야 한다. (분기 코드 자체는 D-79대로 남아 있고, Step 3 스텁에서는 여전히 정상적으로 보인다.)

마지막으로 e2e 두 개를 돌린다: `node --env-file=.env.local scripts/e2e-today.mjs`로 오늘의 학습 루프가 커진 매니페스트에서도 동작하는지, `node --env-file=.env.local scripts/e2e-progress.mjs`로 진행률 집계가 깨지지 않았는지 확인한다. `.env.local`이 없으면 `vercel env pull`로 가져오고, 그래도 자격증명이 없으면 두 e2e를 건너뛰되 **건너뛴 사실과 이유를 SUMMARY에 명시한다** — Plan 13이 최종적으로 반드시 통과시켜야 하는 항목이므로 조용히 넘어가지 않는다.

이 Task는 `scripts/check-manifest.mjs`를 새로 고치지 않는다 — `files_modified`에 있는 것은 Task 1의 커밋을 이 Plan이 푸시하기 때문이다.
  </action>
  <verify>
    <automated>node -e "const s=['2-1-postgres-and-supabase','2-1-ai-data-modeling','2-2-html-css-js','2-2-browser-and-ui','2-3-typescript-setup','2-3-react-components','2-4-project-ai-shop-frontend','2-5-express-rest-api','2-5-auth-and-prisma','2-6-project-ai-shop-backend','2-7-prompt-patterns','2-7-promptops'];(async()=>{for(const x of s){const r=await fetch('https://ai-engineer-runway.vercel.app/lesson/'+x);const t=await r.text();if(r.status!==200||!t.includes('정답 보기')||t.includes('콘텐츠 준비 중')){console.error('FAIL',x,r.status);process.exit(1)}}console.log('12/12 ok')})()"</automated>
    <human-check>2-1 모듈 두 레슨(`2-1-postgres-and-supabase`, `2-1-ai-data-modeling`)의 SQL 예제를 실제 Supabase SQL 에디터에 붙여 넣어 오류 없이 실행되는지 확인한다. 자격증명이 필요해 실행자·게이트가 대신할 수 없는 유일한 항목이다. 확인 순서: 준비 블록(`create schema` + `create table`) → 샘플 `insert` → 조회 쿼리 → 마지막 정리용 `drop schema practice cascade`. `public` 스키마의 진도 데이터가 그대로인지도 함께 본다.</human-check>
  </verify>
  <acceptance_criteria>
    - `git log origin/master..HEAD --oneline`의 출력이 비어 있다 (밀린 커밋 없음).
    - Step 2 12개 slug 각각에 대해 `https://ai-engineer-runway.vercel.app/lesson/{slug}`가 `200`을 반환한다.
    - 12개 응답 본문 각각에 `정답 보기` 문자열이 포함된다.
    - 12개 응답 본문 어디에도 `콘텐츠 준비 중` 문자열이 없다.
    - `node --env-file=.env.local scripts/e2e-today.mjs`가 exit 0이다. 자격증명이 없어 실행하지 못했다면 그 사실과 이유가 SUMMARY에 기록된다.
    - `node --env-file=.env.local scripts/e2e-progress.mjs`가 exit 0이다. 같은 예외 처리 규칙이 적용된다.
    - `git diff --name-only HEAD~1 HEAD`에 `scripts/e2e-today.mjs`·`scripts/e2e-progress.mjs`·`src/app/` 아래 파일이 나타나지 않는다 (e2e 통과를 위해 코드를 고치지 않았다).
    - `node scripts/check-manifest.mjs`·`node scripts/check-brand.mjs`·`node scripts/check-lesson-structure.mjs`가 푸시 후에도 모두 exit 0이다.
  </acceptance_criteria>
  <done>Step 2 12편이 전부 프로덕션에서 읽히고 빈 상태 카피가 사라졌다. 오늘의 학습·진행률 e2e가 커진 매니페스트에서도 통과한다. 사전학습이 끊기지 않고 Step 2로 이어진다. 푸시 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 게이트 기대 상수 → CI/실행자 판정 | 상수가 실제와 어긋나면 거짓 red 또는 거짓 green이 나온다. 거짓 green이 더 위험하다 |
| 로컬 커밋 → 프로덕션 배포 | 푸시되지 않은 커밋은 배포되지 않는다. Phase 1에서 실제로 15개 커밋이 밀린 채 남아 있던 전례가 있다 |
| 레슨 본문 → 공개 프로덕션 URL | Step 2 10편이 이 Plan에서 처음 공개된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | `EXPECTED_HAS_CONTENT_COUNT`/`SLUGS`와 실제 매니페스트의 불일치 | high | mitigate | 상수를 `.velite/lessons.json` 실측에서만 도출하고, acceptance criteria가 개수와 slug 집합 양쪽을 `node -e`로 대조한다. CONTEXT.md 원문 수치를 근거로 삼지 않는다 (Open Question 1) |
| T-05-19 | Tampering | 게이트를 통과시키려 다른 불변식 상수를 조정 | high | mitigate | prohibitions에 명시하고, acceptance criteria가 `git diff` 범위를 Invariant 10 상수와 주석으로 한정함을 확인한다. 총합 4200분이 깨지면 상수가 아니라 프론트매터를 고친 Plan을 되돌린다 |
| T-05-01 | Information Disclosure | Step 2 10편의 첫 공개 | high | mitigate | `node scripts/check-brand.mjs`가 푸시 전후로 통과함을 확인한다. 각 authoring Plan에서 이미 파일 단위 grep을 통과했다 |
| T-05-20 | Tampering | e2e를 통과시키려 e2e 스크립트나 앱 코드를 수정 | medium | mitigate | acceptance criteria가 `git diff --name-only`에 e2e 스크립트와 `src/app/`이 없음을 확인한다. e2e가 실패하면 원인을 고치지 검사를 고치지 않는다 |
| T-05-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 저장소에 패키지를 설치하지 않는다. `package.json`은 변경되지 않는다 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

- **CONT-05 edge 프로브가 `unclassified → unresolved`로 남아 있다** (SPEC.md 부재, spec-less fallback §A). 이 Plan에서 대응되는 실패 모드는 **"프로젝트 가이드가 `hasContent: true`이지만 프로덕션에서 빈 상태로 보이는 것"**이며, `2-4`·`2-6` 두 slug의 200 응답과 `콘텐츠 준비 중` 문자열 부재를 acceptance criteria가 확인한다. 내용 경계(재현 아님)는 여전히 자동 검증 불가이며 각 authoring Plan의 grep과 Plan 01 체크포인트 판정에 의존한다.
- **Open Question 1이 이 Plan에서 두 번째로 정산된다.** CONTEXT.md D-78 원문은 Wave 2 종료값을 `24`로, RESEARCH 재계산은 `23`으로 본다. 이 Plan은 실측만 쓰고 불일치를 기록한다. 최종 35는 양쪽이 일치하므로 Plan 13에서 수렴한다.
- **e2e 두 개가 자격증명에 의존한다.** `.env.local`이 없는 환경에서는 실행할 수 없다. 이 Plan은 건너뛰기를 허용하되 **반드시 기록**하게 했고, Plan 13이 최종적으로 통과를 강제한다(D-81).
- **2-1 SQL의 실제 실행 확인은 자동화 불가다.** Phase 4 UAT 1번과 같은 구조적 제약(자격증명 없는 격리 환경)이다. Task 2의 `<verify><human-check>`가 이 항목을 사람 확인으로 넘긴다 — `human_verify_mode: end-of-phase` 설정에 따라 phase 종료 시 한 번에 확인된다. D-77의 "배치 사이에 사람 승인 체크포인트를 두지 않는다"와 충돌하지 않는다(블로킹 체크포인트가 아니다).
</flagged_assumptions>

<verification>
1. Wave 2의 5개 SUMMARY 존재 (Task 1 `<precondition>`)
2. `npm run build`
3. `node scripts/check-manifest.mjs` — 13 불변식, Invariant 10이 green으로 복귀
4. `node scripts/check-brand.mjs`
5. `node scripts/check-lesson-structure.mjs` — 검사 레슨 수 = 실측 `hasContent: true` 개수
6. 상수 = 실측 대조 2종 (`node -e` 개수·slug 집합)
7. `git push origin master` → Vercel 배포 → Step 2 12개 URL 200 + `정답 보기` 포함 + `콘텐츠 준비 중` 부재
8. `node --env-file=.env.local scripts/e2e-today.mjs`, `scripts/e2e-progress.mjs`
9. `<human-check>` — 2-1 SQL 실제 실행 (phase 종료 시 확인)
</verification>

<success_criteria>
- 매니페스트 기대 상수가 빌드 실측과 정확히 일치하고 Invariant 10이 green이다
- 실측값과 CONTEXT.md D-78 원문 수치의 일치/불일치가 기록됐다
- Step 2 12편이 전부 프로덕션에서 읽히고 빈 상태 카피가 사라졌다
- 오늘의 학습·진행률 e2e가 커진 매니페스트에서도 통과한다 (또는 건너뛴 이유가 기록됐다)
- Step 3 배치(Plan 08~12)가 green 상태에서 시작할 수 있다
</success_criteria>

<output>
Create `.planning/phases/05-step-2-3/05-07-SUMMARY.md` when done.

담을 것: 실측 `hasContent: true` 개수와 slug 목록, CONTEXT.md D-78 원문 수치(`24`)와의 일치/불일치, Step 2 12개 URL 확인 결과, e2e 두 개의 실행 여부와 결과(건너뛴 경우 이유), Plan 13이 쓸 최종 예상값(35)과 남은 전환 편수(12).
</output>
