---
phase: 05-step-2-3
plan: 11
type: execute
wave: 4
depends_on: ["05-07"]
files_modified:
  - src/content/lessons/step-3/3-5-project-orchestration.mdx
  - src/content/lessons/step-3/3-7-project-ax-launch.mdx
autonomous: true
requirements: [CONT-05]

estimate:
  tokens: 56000
  raw_tokens: 56000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`/lesson/3-5-project-orchestration`이 프로젝트 준비 가이드 형식으로 렌더한다 — ④가 코드가 아니라 사전 준비 체크리스트 표이고 ⑥ 핵심 정리가 준비 완료 판정 체크박스다 (D-66, CONT-05)"
    - "`/lesson/3-7-project-ax-launch`가 프로젝트 준비 가이드 형식으로 렌더한다 — 같은 단별 매핑을 따른다 (D-66, CONT-05)"
    - "두 레슨이 완성 구현 코드·완성 스키마 전체·단계별 튜토리얼을 담지 않는다 — ④ 절 안에 실행 코드 펜스가 0건이다 (D-67, RESEARCH Pitfall 1)"
    - "두 레슨의 ② 왜 배우나가 복습 포인터로 구체 slug를 언급한다 — `3-5`는 3-4 모듈 세 편을, `3-7`은 앞선 Step 3 모듈 전반을 가리킨다 (D-66)"
    - "두 레슨의 `### 해보기`가 '실제로 준비해 보기'이고 `<details>` 안이 정답이 아니라 '성공했다면 이런 화면/출력이 나옵니다'다 (D-68)"
    - "두 레슨 모두 6단 헤딩 원문 유지 + `### 해보기` 2~3개 + 해보기 개수 + 2 이상의 접힌 정답 블록 + `**이 레슨의 단어**` 표 5~8행을 갖고 `node scripts/check-lesson-structure.mjs`를 통과한다 — 프로젝트 가이드도 형식 예외가 없다 (D-69)"
    - "두 레슨의 프론트매터에서 `hasContent: false → true` 한 줄 외에는 어떤 필드도 바뀌지 않았다 (D-13)"
    - "이 Plan이 끝나면 커리큘럼 실습 프로젝트 5종 준비 가이드가 전부 존재한다 — `2-4`, `2-6`, `3-2`, `3-5`, `3-7` (CONT-05 완성)"
    - statement: "두 가이드를 읽고 나서도 개강 후 본 과정에서 배울 것이 남아 있다고 느껴진다 — 60분 가이드가 200~520시간 팀 프로젝트를 앞질러 풀지 않았다 (D-67)"
      verification: backstop
    - statement: "`3-7`이 최종 프로젝트라는 성격에 맞게 앞선 4개 프로젝트를 어떻게 이어받는지 보여 준다 — 학습자가 커리큘럼 전체의 도착점을 본다"
      verification: backstop
  artifacts:
    - src/content/lessons/step-3/3-5-project-orchestration.mdx
    - src/content/lessons/step-3/3-7-project-ax-launch.mdx
  key_links:
    - "`2-4-project-ai-shop-frontend`(Plan 01, 승인된 형식) → 이 Plan의 2편 — 프로젝트 가이드 형식의 원본이다. 단별 매핑·체크리스트 표·성공 판정 문구를 그대로 승계한다"
    - "3-4 모듈 세 편(Plan 10) → `3-5` 복습 포인터 — 같은 wave에서 병렬 집필되므로 용어를 CONTEXT·커리큘럼 원문 기준으로 맞춘다"
    - "프로젝트 5종 완성 → CONT-05 충족 — 이 Plan이 마지막 2편이다"
    - "이 Plan의 2편 → Plan 13의 매니페스트 실측"
  prohibitions:
    - statement: "레슨 본문·코드 주석·메타데이터 어디에도 교육기관명(`scripts/check-brand.mjs`의 금지 문자열)을 노출해서는 안 된다 (D-02)"
      status: unverified
    - statement: "프로젝트 준비 가이드에 완성 구현 코드·완성 DB 스키마 전체·단계별 튜토리얼·'이렇게 만들면 됩니다' 형태의 정답 아키텍처를 실어서는 안 된다 (D-67, CONT-05)"
      status: unverified
    - statement: "`### 해보기`를 '이 함수를 구현해보세요' 형태의 코드 작성 과제로 써서는 안 된다 — '실제로 준비해 보기'여야 한다 (D-68)"
      status: unverified
    - statement: "프로젝트 가이드라는 이유로 단어 표나 `### 해보기` 개수 규칙을 예외 처리해서는 안 된다 (D-69)"
      status: unverified
    - statement: "6단 헤딩 텍스트를 바꿔서는 안 된다 (D-65)"
      status: unverified
    - statement: "프론트매터의 `hasContent` 외 어떤 필드도 바꿔서는 안 된다 (D-13, D-31)"
      status: unverified
    - statement: "레슨 본문에 실제 API 키·접속 문자열·웹훅 URL 형태의 문자열을 적어서는 안 된다 — 자리표시자만 쓴다 (T-05-01)"
      status: unverified
---

<objective>
Step 3 배치의 프로젝트 준비 가이드 2편을 집필한다 — `3-5-project-orchestration`([Project 4] AI 업무 자동화)과 `3-7-project-ax-launch`([Project 5] AX 서비스 런칭).

Purpose: **이 Plan이 CONT-05를 완성한다.** 커리큘럼 실습 프로젝트 5종 중 마지막 두 편이며, 특히 `3-7`은 커리큘럼 전체의 도착점(최종 데모·포트폴리오)이라 앞선 네 프로젝트를 어떻게 이어받는지 보여 주는 자리다. 두 편 모두 D-67의 "재현 아님" 경계를 지킨다 — 60분 가이드가 200~520시간 팀 프로젝트를 앞질러 풀면 개강 후 학습이 무의미해진다.

Output: 프로젝트 준비 가이드 형식 레슨 2편, `hasContent: true` 전환 2건, CONT-05 충족 완료.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-step-2-3/05-CONTEXT.md
@.planning/phases/05-step-2-3/05-PATTERNS.md
@.planning/phases/05-step-2-3/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces

**Plan 11 (이 Plan)이 만드는 것**
- `src/content/lessons/step-3/3-5-project-orchestration.mdx` — 본문 신규, `hasContent: false → true` (프로젝트 준비 가이드 5편 중 4번째)
- `src/content/lessons/step-3/3-7-project-ax-launch.mdx` — 본문 신규, `hasContent: false → true` (프로젝트 준비 가이드 5편 중 5번째, CONT-05 완성)

**Phase 5 전체가 만들거나 바꾸는 것** — `05-01-PLAN.md` §Artifacts this phase produces 참조.
</artifacts_produced>

<manifest_gate_note>
**이 wave 동안 `node scripts/check-manifest.mjs`는 의도적으로 red다.** `hasContent`를 2건 켜면 실제 개수가 Plan 07이 맞춰 둔 상수를 넘어 Invariant 10이 실패한다. 상수는 Wave 3 마감 Plan 13이 실측으로 되돌린다(D-78). **실행자는 `check-manifest`를 verify에서 제외하고 상수를 손대지 않는다.**
</manifest_gate_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: `3-5-project-orchestration` 집필 — [Project 4] AI 업무 자동화 준비 가이드</name>
  <files>src/content/lessons/step-3/3-5-project-orchestration.mdx</files>
  <read_first>
    - `src/content/lessons/step-3/3-5-project-orchestration.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "[Project 4] AI 업무 자동화 준비 가이드"`, `stepId: 3`, `moduleId: "3-5"`, `order: 1`, `depth: "개요"`, `estimatedMinutes: 60`, `hasContent: false`)을 확인한다. `estimatedMinutes`가 60인 것이 프로젝트 가이드의 표식이다.
    - `src/content/lessons/step-2/2-4-project-ai-shop-frontend.mdx` (Plan 01 완료본) — **프로젝트 준비 가이드 형식의 승인된 실물.** 단별 매핑·체크리스트 표 형식·`<details>` 안 "성공했다면 이런 화면"·준비 완료 체크박스를 그대로 가져온다. 전체를 읽는다.
    - `src/content/lessons/step-3/3-4-multi-agent-structure.mdx`·`3-4-webhook-schedule-hitl.mdx`·`3-4-n8n-langgraph.mdx` — **같은 wave의 Plan 10이 병렬로 집필 중이다.** 이미 존재하면 읽어서 용어(에이전트·트리거·HITL·멱등성·노드)를 맞추고, 아직 스텁이면 `.planning/curriculum.md` §3-4와 `05-CONTEXT.md`를 기준으로 용어를 정한다.
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md` — 체크포인트 항목 5의 "재현 아님" 경계 판정 결과와 사용자 수정 요청 반영 사항.
    - `.planning/curriculum.md` §Step 3 §3-5 — 불릿 2줄("웹훅·스케줄·HITL 트리거 기반 자동화와 외부툴 3개 연동", "감사로그, 롤백 시나리오 설계")이 이 프로젝트의 범위 원천. "외부툴 3개"와 "감사로그·롤백"이 체크리스트의 핵심 항목이다.
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩은 아래 6개뿐이며 문자열과 순서를 그대로 쓴다: `## 1. 학습 목표`, `## 2. 왜 배우나`, `## 3. 개념 설명`, `## 4. 실무 예제`, `## 5. 실무 팁`, `## 6. 핵심 정리 및 스스로 점검`(D-65). **각 단의 내용만** 프로젝트용으로 재해석한다(D-66).

- **① 학습 목표** — 이 프로젝트에서 **무엇을 만들고 무엇을 증명하는가**. "실제 업무 하나를 자동화하면서, 트리거로 시작해 외부 도구 세 개를 거쳐 사람 승인까지 이어지는 흐름을 설계하고, 잘못됐을 때 되돌릴 수 있음을 증명한다" 취지. 구현 능력이 아니라 프로젝트 이해가 목표다.
- **② 왜 배우나** — 앞선 어느 모듈들이 여기서 합쳐지는지. **복습 포인터로 구체 slug를 언급한다**: `3-4-multi-agent-structure`(역할 분담), `3-4-webhook-schedule-hitl`(트리거와 승인 지점), `3-4-n8n-langgraph`(도구 선택). 각 포인터마다 "여기서 무엇을 가져오는지" 한 줄씩.
- **③ 개념 설명** — `### 무엇을 만드나 — 전체 구조` 소제목 아래 `트리거(웹훅·스케줄) → 워크플로 실행 → 외부 도구 3개 연동 → 사람 승인(HITL) → 결과 기록(감사로그) → 실패 시 롤백` 형태의 한 줄 도식(일반 텍스트, 화살표 문자만)과 표 하나. 표는 3열(`단계` / `하는 일` / `어느 레슨에서 나왔나`)로 여섯 행. 다이어그램 플러그인·이미지를 쓰지 않는다(D-48).
- **④ 실무 예제** — `### 사전 준비 체크리스트` 소제목으로 시작한다. **코드가 아니라 준비물이다**(D-67). 3열 표(`준비물` / `왜 필요한가` / `지금 할 일`)로 다음 행을 만든다: 자동화할 실제 업무 1개 선정(지금 사람이 손으로 하는 절차를 단계별로 적어 두기), 외부 도구 3개 계정(메신저·문서·표 계열에서 팀이 실제로 쓰는 것으로 고르고 각각 자동화 연동이 가능한지 확인), 트리거 방식 결정(무엇이 이 자동화를 시작시키는가), 승인자 지정(누가 무엇을 승인하는가), 감사로그에 남길 항목 목록(누가·언제·무엇을·결과). 표 아래 인용문 한 줄로 "이 체크리스트는 무엇을 준비할지만 알려줍니다 — 실제 구현 방법은 개강 후 본 과정에서 팀과 함께 설계합니다" 취지를 남긴다. **이 절에 실행 코드 펜스를 넣지 않는다.** 감사로그 항목을 형식으로 보여 줄 필요가 있으면 `text` 펜스 하나로 키 이름만, 또는 마크다운 표로 대체한다.
- **⑤ 실무 팁** — 팀 프로젝트에서 자주 막히는 지점을 불릿 3~4개로. 반드시 포함: 자동화 대상 업무를 너무 크게 잡으면 끝나지 않는다(한 사이클이 10분 안에 끝나는 업무를 고른다), 외부 도구 연동 권한을 받는 데 생각보다 오래 걸린다(첫 주에 신청), 롤백 시나리오를 나중에 붙이면 이미 늦다, 시간 배분과 역할 분담.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**`를 **준비 완료 판정 기준 체크박스 목록**으로 쓴다(`- [ ]` 형태 4~5개: 업무 선정·절차 문서화, 도구 3개 계정·권한 확인, 트리거 결정, 승인자 합의, 감사로그 항목 목록). 그다음 `**이 레슨의 단어**` 표 5~8행(이 프로젝트에서 처음 나오는 용어: 감사로그, 롤백, 승인 대기, 외부 연동, 실행 이력, 드라이런 등). 그다음 `**스스로 점검**` 질문 2개 + 접힌 정답 블록.
- **`### 해보기` 2~3개는 "실제로 준비해 보기"다**(D-68). 소재: 지금 손으로 하는 업무 하나의 절차를 단계별로 적고 각 단계에 걸리는 시간 표시하기, 외부 도구 3개를 고르고 각각 자동화 연동 문서가 있는지 확인해 링크나 메모 남기기, 이 자동화가 잘못 실행됐을 때 무엇을 되돌려야 하는지 목록으로 적기. 각 해보기의 `<details>` 안에는 **"성공했다면 이런 화면/출력이 나옵니다"**를 담는다 — 정답 코드가 아니다.

60분 = 읽기 약 20분 + 준비 실행 약 40분(D-68). 용어 표기는 D-50을 따른다. 단어 표와 해보기 개수 규칙에 예외를 두지 않는다(D-69).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `3-5-project-orchestration`이 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent: false` → `hasContent: true` 한 줄뿐이다 — `estimatedMinutes: 60`이 diff에 나타나지 않는다.
    - `grep -c '^## '`가 `6`이고 순서가 1→6이며 `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며 빈 줄 규칙을 지킨다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - `## 4. 실무 예제`부터 `## 5. 실무 팁` 직전 구간에 여는 코드펜스가 있다면 언어 태그가 `text` 하나뿐이고, `typescript`·`tsx`·`javascript`·`jsx`·`python`·`sql`·`json` 펜스가 그 구간에 0건이다 (D-67).
    - `## 4. 실무 예제` 구간에 3열 마크다운 표가 1개 이상 있고, 그 표에 외부 도구 3개와 감사로그 항목 행이 포함된다.
    - `## 6. 핵심 정리 및 스스로 점검` 구간에 `- [ ]` 체크박스 항목이 4개 이상 있다.
    - `grep -c '3-4-multi-agent-structure'`·`grep -c '3-4-webhook-schedule-hitl'`·`grep -c '3-4-n8n-langgraph'`가 각각 `1` 이상이다 (복습 포인터 slug, D-66).
    - `### 해보기` 과제 문장에 `구현해` 또는 `함수를 작성` 문자열이 0건이다 (D-68).
    - `grep -ciE '롤백'`가 `2` 이상이고 `grep -ciE '감사로그'`가 `2` 이상이다 (커리큘럼 §3-5 둘째 불릿 범위를 실제로 채운다).
    - `grep -c 'sk-ant-'`가 `0`, `grep -cE 'https://hooks\.[a-z]'`가 `0`, `grep -ciE 'kant'`가 `0`이다.
  </acceptance_criteria>
  <done>프로젝트 준비 가이드 5편 중 4번째가 승인된 형식으로 작성됐고, ④가 코드가 아니라 체크리스트이며 감사로그·롤백이 실제로 다뤄진다. 복습 포인터가 실제 slug를 가리킨다. 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: `3-7-project-ax-launch` 집필 — [Project 5] AX 서비스 런칭 준비 가이드 (CONT-05 완성)</name>
  <files>src/content/lessons/step-3/3-7-project-ax-launch.mdx</files>
  <read_first>
    - `src/content/lessons/step-3/3-7-project-ax-launch.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "[Project 5] AX 서비스 런칭 준비 가이드"`, `moduleId: "3-7"`, `order: 1`, `depth: "개요"`, `estimatedMinutes: 60`, `hasContent: false`)을 확인한다.
    - `src/content/lessons/step-2/2-4-project-ai-shop-frontend.mdx` (Plan 01 완료본)과 `src/content/lessons/step-3/3-5-project-orchestration.mdx` (Task 1 완료본) — 프로젝트 가이드 형식의 실물 둘. 단별 매핑·체크리스트 표·성공 판정 문구를 그대로 승계하고, `3-5`에서 쓴 용어를 이어 쓴다.
    - `.planning/curriculum.md` §Step 3 §3-7 — 불릿 3줄("JSON 구조화 출력, PII/인젝션 정책, HITL·RAG 고도화·오케스트레이션을 적용해 AX 제품 배포", "관측/알림 환경 구성, Vercel·Cloud Run 등으로 배포", "최종 데모 발표 및 포트폴리오 산출")이 이 프로젝트의 범위 원천. **세 불릿 모두 체크리스트에 반영한다.**
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md` — 체크포인트 항목 5의 "재현 아님" 경계 판정 결과.
    - `.planning/REQUIREMENTS.md` §CONT-05 — 이 Task가 요구사항을 완성한다. 원문의 "(재현 아님)"이 경계다.
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩 6개는 Task 1과 동일한 문자열·순서를 유지하고 각 단의 내용만 재해석한다(D-66).

이 편은 **커리큘럼 전체의 도착점**이다. 앞선 네 프로젝트를 어떻게 이어받는지가 ②의 핵심이고, 배포·관측·데모까지 준비 범위가 넓으므로 체크리스트가 가장 길어질 수 있다 — 그럴수록 "무엇을 준비할지"에서 멈추고 "어떻게 만들지"로 넘어가지 않게 주의한다(D-67).

- **① 학습 목표** — 이 프로젝트에서 **무엇을 만들고 무엇을 증명하는가**. "실제로 쓸 수 있는 AX 서비스를 배포하고, 안전장치(구조화 출력·개인정보·인젝션 정책)와 관측 환경을 갖춘 상태로 시연할 수 있음을 증명한다" 취지.
- **② 왜 배우나** — 앞선 네 프로젝트가 여기서 어떻게 합쳐지는지. **복습 포인터로 구체 slug를 언급한다**: `2-4-project-ai-shop-frontend`·`2-6-project-ai-shop-backend`(화면과 서버), `3-2-project-rag-agent`(지식 기반 답변), `3-5-project-orchestration`(자동화와 승인), 그리고 `2-7-prompt-patterns`(구조화 출력)·`3-6-monitoring-governance`(관측·정책). 각 포인터마다 "여기서 무엇을 가져오는지" 한 줄씩.
- **③ 개념 설명** — `### 무엇을 만드나 — 전체 구조` 소제목 아래 `사용자 → AX 서비스 화면 → 구조화 출력 + 개인정보·인젝션 정책 → RAG·오케스트레이션 → 배포 환경 → 관측·알림 → 데모` 형태의 한 줄 도식과 표 하나. 표는 3열(`구성 요소` / `맡는 일` / `어느 레슨·프로젝트에서 나왔나`)로 여섯~일곱 행. 다이어그램 플러그인·이미지를 쓰지 않는다(D-48).
- **④ 실무 예제** — `### 사전 준비 체크리스트` 소제목으로 시작한다. **코드가 아니라 준비물이다**(D-67). 3열 표(`준비물` / `왜 필요한가` / `지금 할 일`)로 커리큘럼 §3-7 세 불릿을 모두 덮는 행을 만든다: 배포 플랫폼 계정(정적·서버 실행이 각각 가능한 곳 하나씩 확인), 관측·알림 도구 계정(오류와 지표를 어디서 볼지 정하기), 개인정보·인젝션 정책 초안(어떤 데이터를 화면·로그에 남기지 않을지 목록으로 적기), 서비스 아이디어 한 문장(누구의 어떤 문제를 푸는가), 데모 시나리오 초안(3분 안에 보여 줄 흐름 5단계), 포트폴리오 틀(무엇을 어떻게 기록할지). 표 아래 인용문 한 줄로 "이 체크리스트는 무엇을 준비할지만 알려줍니다 — 실제 구현 방법은 개강 후 본 과정에서 팀과 함께 설계합니다" 취지를 남긴다. **이 절에 실행 코드 펜스를 넣지 않는다.**
- **⑤ 실무 팁** — 팀 프로젝트에서 자주 막히는 지점을 불릿 3~4개로. 반드시 포함: 배포를 마지막 주에 처음 시도하면 반드시 늦는다(첫 주에 빈 화면이라도 한 번 올려 본다), 데모 시나리오를 먼저 정하면 만들 것이 줄어든다, 개인정보·인젝션 정책은 기능이 아니라 제약이므로 설계 단계에서 정한다, 시간 배분과 역할 분담.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**`를 **준비 완료 판정 기준 체크박스 목록**으로 쓴다(`- [ ]` 형태 5개 안팎: 배포 계정 확인, 관측 도구 확인, 정책 초안, 아이디어 한 문장, 데모 시나리오 5단계). 그다음 `**이 레슨의 단어**` 표 5~8행(AX, 배포 환경, 관측, 알림 임계값, 개인정보 마스킹, 프롬프트 인젝션, 데모 시나리오 등). 그다음 `**스스로 점검**` 질문 2개 + 접힌 정답 블록.
- **`### 해보기` 2~3개는 "실제로 준비해 보기"다**(D-68). 소재: 배포 플랫폼에 빈 프로젝트를 하나 만들어 주소가 열리는 것까지 확인하기, 서비스 아이디어를 "누구의 어떤 문제를 어떻게" 한 문장으로 적고 팀원과 맞춰 보기, 화면·로그에 절대 남기면 안 되는 데이터 항목을 5개 적어 보기. 각 해보기의 `<details>` 안에는 **"성공했다면 이런 화면/출력이 나옵니다"**를 담는다.

60분 = 읽기 약 20분 + 준비 실행 약 40분(D-68). 마지막 문단에서 이 편이 커리큘럼 준비 가이드 5종의 마지막임을 한 줄로 짚어 학습자가 도착점을 인지하게 한다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `3-7-project-ax-launch`가 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`, `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며 빈 줄 규칙을 지킨다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - `## 4. 실무 예제`부터 `## 5. 실무 팁` 직전 구간에 여는 코드펜스가 있다면 언어 태그가 `text` 하나뿐이고, 실행 가능한 언어 펜스(`typescript`·`tsx`·`javascript`·`jsx`·`python`·`sql`·`yaml`)가 그 구간에 0건이다 (D-67).
    - `## 4. 실무 예제` 구간의 3열 표가 커리큘럼 §3-7 세 불릿을 모두 덮는다: 배포·관측·정책·데모·포트폴리오 관련 행이 각각 1개 이상 있다.
    - `## 6. 핵심 정리 및 스스로 점검` 구간에 `- [ ]` 체크박스 항목이 4개 이상 있다.
    - `grep -c '2-4-project-ai-shop-frontend'`·`grep -c '2-6-project-ai-shop-backend'`·`grep -c '3-2-project-rag-agent'`·`grep -c '3-5-project-orchestration'`가 각각 `1` 이상이다 (복습 포인터 slug, D-66).
    - `### 해보기` 과제 문장에 `구현해` 또는 `함수를 작성` 문자열이 0건이다 (D-68).
    - `grep -c 'sk-ant-'`가 `0`이고 `grep -ciE 'kant'`가 `0`이다.
    - 프로젝트 가이드 5편이 전부 `hasContent: true`다: `node -e "const l=require('./.velite/lessons.json');const s=new Set(l.filter(x=>x.hasContent).map(x=>x.slug));process.exit(['2-4-project-ai-shop-frontend','2-6-project-ai-shop-backend','3-2-project-rag-agent','3-5-project-orchestration','3-7-project-ax-launch'].every(x=>s.has(x))?0:1)"`가 exit 0이다.
    - 프로젝트 가이드 5편 모두 `estimatedMinutes`가 60이다: `node -e "const l=require('./.velite/lessons.json');const ids=['2-4-project-ai-shop-frontend','2-6-project-ai-shop-backend','3-2-project-rag-agent','3-5-project-orchestration','3-7-project-ax-launch'];process.exit(ids.every(x=>l.find(y=>y.slug===x).estimatedMinutes===60)?0:1)"`가 exit 0이다.
  </acceptance_criteria>
  <done>프로젝트 준비 가이드 5편이 전부 완성됐다 — CONT-05가 충족된다. 마지막 편이 커리큘럼 전체의 도착점을 보여 주고 배포·관측·정책·데모를 준비 범위로 다룬다. 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레슨 본문 → 공개 프로덕션 URL | 본문 문자열이 그대로 인터넷에 노출된다 |
| 준비 체크리스트 → 학습자의 외부 서비스 계정 | 배포 플랫폼·관측 도구·메신저 계정을 실제로 만들게 한다 |
| 정책 초안 안내 → 학습자의 실제 서비스 데이터 | 개인정보·인젝션 정책을 어떻게 가르치는지가 그대로 재생산된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | 레슨 본문 — 교육기관명·이메일·키·웹훅 URL | high | mitigate | `node scripts/check-brand.mjs` 상시 검사 + acceptance criteria의 대소문자 무시 grep 0건 + 키·URL 정규식 0건. 자리표시자만 쓴다 |
| T-05-02 | Information Disclosure | ④ 체크리스트의 계정·키 준비 안내 | high | mitigate | 키는 환경 파일에 두고 코드·대화창에 붙여넣지 않는다는 취지를 체크리스트 행에 넣는다 |
| T-05-27 | Information Disclosure | 개인정보를 화면·로그에 남기는 습관 | high | mitigate | `3-7` ④ 체크리스트에 "화면·로그에 남기지 않을 데이터 목록"을 준비물 행으로 넣고, 해보기 하나가 그 목록을 직접 5개 적게 한다. ⑤ 실무 팁이 "정책은 기능이 아니라 제약"임을 못 박는다 |
| T-05-28 | Tampering | 롤백 없는 자동화를 배포하는 습관 | medium | mitigate | `3-5` ④ 체크리스트와 ⑤ 실무 팁 양쪽에 롤백을 넣고, acceptance criteria가 `롤백`·`감사로그` 각 2회 이상 등장을 확인한다 |
| T-05-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 저장소에 패키지를 설치하지 않고, 프로젝트 가이드는 설치 명령 자체를 담지 않는다(D-67). RESEARCH.md `## Package Legitimacy Audit`의 `[SLOP]` 판정 0건 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

- **CONT-05 edge 프로브가 `unclassified → unresolved`로 남아 있다** (SPEC.md 부재, spec-less fallback §A). **이 Plan이 CONT-05를 완성하는 자리**이므로 그 미해결 상태가 가장 크게 걸린다. 대응되는 실패 모드는 "프로젝트 가이드 5편이 형식상 존재하지만 내용이 '재현 아님' 경계를 넘어 튜토리얼이 되는 것"이며, (1) `must_haves.prohibitions`의 D-67 경계, (2) ④ 절 실행 코드 펜스 0건 grep, (3) `### 해보기`의 `구현해`·`함수를 작성` 0건 grep, (4) 5편 존재·`estimatedMinutes: 60` 일치 확인 네 겹이 방어한다. **그럼에도 내용 경계 자체는 자동 검증 불가**이며, Plan 01 체크포인트 항목 5에서 사람이 판정한 기준을 그대로 따르는 것이 이 Plan의 가정이다. Phase 5 마감(Plan 13)의 사람 확인이 마지막 방어선이다.
- **3-4 세 편이 같은 wave에서 병렬 집필된다.** `3-5`의 복습 포인터가 가리키는 slug는 Phase 1에서 확정된 불변값이라 링크 대상은 안전하지만, **용어 일치는 보장되지 않는다.** 이 Plan은 3-4 파일이 이미 있으면 읽어서 맞추고, 없으면 커리큘럼·CONTEXT 원문을 기준으로 정하도록 `<read_first>`에 명시했다. 불일치가 남으면 Plan 13의 사람 확인에서 드러난다.
- **`3-7`의 PII·인젝션 정책 설명은 이 세션에서 검증되지 않은 실무 지식이다** (RESEARCH `## Sources` Tertiary, `[ASSUMED]`). 이 Plan은 정책의 **구체 규칙을 단정하지 않고** "무엇을 남기지 않을지 팀이 목록으로 정한다"는 준비 행위로만 다뤄 위험을 줄였다. 깊은 정책 설명은 `3-6-monitoring-governance`(Plan 12) 몫이며 거기서도 같은 주의가 적용된다.
- **`check-manifest.mjs`는 이 wave 동안 의도적으로 red다** — `<manifest_gate_note>` 참조.
</flagged_assumptions>

<verification>
1. `npm run build`
2. `node scripts/check-lesson-structure.mjs` — 두 레슨 모두 오류 목록에 없음
3. `node scripts/check-brand.mjs`
4. 프로젝트 가이드 5편 존재·`estimatedMinutes: 60` 일치 확인 (`node -e` 2종)
5. `node scripts/check-manifest.mjs`는 **실행하지 않는다** (이 wave 동안 의도적 red, Plan 13이 되돌린다)
</verification>

<success_criteria>
- 커리큘럼 실습 프로젝트 5종 준비 가이드가 전부 존재한다 — CONT-05 충족 (D-66~D-69)
- 두 편 모두 "재현 아님" 경계를 지키고 ④가 코드가 아니라 체크리스트다
- `3-5`가 감사로그·롤백을, `3-7`이 배포·관측·정책·데모·포트폴리오를 준비 범위로 다룬다
- 확대된 구조 게이트와 브랜드 게이트를 통과한다
</success_criteria>

<output>
Create `.planning/phases/05-step-2-3/05-11-SUMMARY.md` when done.

담을 것: 두 가이드의 체크리스트 표 행 구성, 복습 포인터 slug 목록, 프로젝트 가이드 5편의 형식 일관성 확인 결과(Plan 13의 사람 확인이 참조한다), 3-4 모듈 용어와의 일치 여부(불일치가 있으면 명시), 확정된 해보기 개수, 게이트 통과 시점의 `hasContent: true` 총 개수.
</output>
