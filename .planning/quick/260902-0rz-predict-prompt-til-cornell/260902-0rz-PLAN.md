---
phase: quick-260902-0rz
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260902120000_add_til_to_lesson_note.sql
  - src/lib/note-store.ts
  - src/app/lesson/[lessonId]/til-actions.ts
  - src/app/api/progress/route.ts
  - src/components/progress-provider.tsx
  - src/components/lesson-til.tsx
  - src/components/progress-slots.tsx
  - src/app/lesson/[lessonId]/page.tsx
  - src/components/predict-prompt.tsx
  - src/components/mdx-content.tsx
  - src/content/lessons/step-1/1-3-python-variables-and-types.mdx
  - scripts/insert-predict-prompt.mjs
  - src/content/lessons/**/*.mdx
autonomous: true
requirements:
  - QUICK-0rz-PREDICT-PROMPT
  - QUICK-0rz-TIL-CORNELL
user_setup: []

estimate:
  tokens: 78000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "코드/쿼리 예제가 있는 33편 전부의 4장(실무 예제) 첫 코드 앞에 동일한 '먼저 스스로 설계' 예측 프롬프트가 보인다 — 부분 적용 없이 경계 안 100%."
    - "예측 프롬프트는 접기(details)가 아니라 항상 보이는 콜아웃이며, 모든 레슨에서 문구가 글자 그대로 동일하다(레슨별 커스텀 저작 아님)."
    - "레슨 완료 영역에서 '오늘 배운 것 한 줄?'(코넬 큐: '내 말로 한 문장으로 설명하면?')을 입력·저장할 수 있고, 저장 후 다시 방문해도 값이 그대로 보인다."
    - "TIL은 선택 사항이며 비워 두어도 레슨 완료가 막히지 않는다(건너뛰기 가능)."
    - "TIL은 Supabase lesson_note.til 컬럼에 저장되고 기존 메모 body와 별도 컬럼이라 서로 덮어쓰지 않는다(localStorage 아님, 기기 간 유지)."
  artifacts:
    - src/components/predict-prompt.tsx
    - src/components/lesson-til.tsx
    - src/app/lesson/[lessonId]/til-actions.ts
    - supabase/migrations/20260902120000_add_til_to_lesson_note.sql
    - scripts/insert-predict-prompt.mjs
  key_links:
    - "mdx-content.tsx defaultComponents에 PredictPrompt 등록 → MDX의 `<PredictPrompt />`가 실제 컴포넌트로 렌더된다(RunPython/RunSQL과 동형)."
    - "note-store.saveLessonTil은 {lesson_id, til, updated_at}만 upsert → 기존 body 컬럼을 건드리지 않는다(WR-01: 문자열 결합 금지, 별도 컬럼)."
    - "GET /api/progress → ProgressProvider → LessonTilSlot → LessonTil로 til 문자열이 전달되어 화면에 표시된다."
---

<objective>
학습 인출 장치 2건을 붙인다.

1. **예측 프롬프트(4장 '먼저 스스로 설계' 의례)** — 코드를 보기 전 "어떤 순서로 짤지 30초 머릿속으로" 떠올리게 하는, 항상 보이는 짧은 콜아웃. 생성 효과·생산적 실패를 노린다. 리서치가 "의례 장치의 부분 적용은 유해"라고 못박았으므로 **경계 안 전량 균일 적용**한다. 경계 = "4장(실무 예제)에 코드/쿼리 예제가 있는 콘텐츠 레슨"(측정값 33편). 4장에 코드가 없는 2편(3-2-project-rag-agent, 3-7-project-ax-launch)은 "코드 보기 전"이라는 의례의 전제 자체가 없으므로 원칙적으로 제외한다(경계 안 부분 적용이 아니라 경계 밖).

2. **TIL 한 줄 + 코넬 큐** — 레슨 완료 흐름에 "오늘 배운 것 한 줄?" 입력란(건너뛰기 가능)을 통합. 코넬 큐 문구 "내 말로 한 문장으로 설명하면?"으로 자기 설명을 유도하고, 자유 회상의 인출 효과를 얻는다. 저장은 이미 라이브에 추가된 lesson_note.til 컬럼(별도 컬럼, 기존 body와 분리 — WR-01 준수).

Purpose: 읽고 넘어가는 학습을 "예측 → 확인 → 회상"의 인출 루프로 바꿔 개강 전 기초를 더 오래 남긴다.
Output: 공유 MDX 컴포넌트 `<PredictPrompt>`, 33편 균일 삽입, TIL 입력/저장/표시 흐름(Supabase 왕복), til 마이그레이션 파일.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md

# TIL 데이터 흐름 — 완료/메모와 같은 경로(정적 셸 + GET /api/progress 클라이언트 조회)
@src/app/api/progress/route.ts
@src/components/progress-provider.tsx
@src/components/progress-slots.tsx
@src/lib/note-store.ts
@src/app/lesson/[lessonId]/note-actions.ts
@src/app/lesson/[lessonId]/page.tsx

# 예측 프롬프트 — MDX 컴포넌트 매핑과 4장 구조
@src/components/mdx-content.tsx
@src/content/lessons/step-1/1-3-python-variables-and-types.mdx

# 게이트
@scripts/check-lesson-structure.mjs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: TIL 한 줄 + 코넬 큐 — 완료 흐름에 end-to-end 통합</name>
  <files>supabase/migrations/20260902120000_add_til_to_lesson_note.sql, src/lib/note-store.ts, src/app/lesson/[lessonId]/til-actions.ts, src/app/api/progress/route.ts, src/components/progress-provider.tsx, src/components/lesson-til.tsx, src/components/progress-slots.tsx, src/app/lesson/[lessonId]/page.tsx</files>
  <precondition>라이브 Supabase의 public.lesson_note 테이블에 til 컬럼이 이미 존재한다(오케스트레이터가 관리 API로 `til text not null default ''` 적용 완료). 이 태스크의 마이그레이션 파일은 repo에 남기는 검토용 사본이다.</precondition>
  <action>
    한 레슨에서 "오늘 배운 것 한 줄?"을 입력·저장·재표시하는 경로를 데이터 계층부터 UI까지 한 줄로 관통시킨다. 별도 컬럼 til에만 쓰고 기존 메모 body는 절대 건드리지 않는다(WR-01).

    (1) 마이그레이션 검토용 사본: supabase/migrations/20260902120000_add_til_to_lesson_note.sql 을 만든다. `alter table public.lesson_note add column if not exists til text not null default '';` 한 줄 + 머리 주석 — 20260827000000_create_lesson_note.sql의 톤을 따라 "라이브 테이블은 오케스트레이터가 관리 API로 이미 적용(2026-09-02), 이 파일은 검토용 사본이며 재실행 불필요, if not exists로 멱등"임을 적는다.

    (2) note-store.ts: NoteRead 성공 분기에 `til: string`을 추가하고(`{ ok: true; body: string; til: string }`), readLessonNote의 select를 `'body, til'`로 바꾸고 반환에 `til: data?.til ?? ''`를 채운다. 새 함수 `saveLessonTil(lessonSlug: string, til: string)`를 추가한다 — MAX_TIL_LENGTH 상한(2_000, 상수로 선언, 초과 시 한국어 오류 throw), upsert는 `{ lesson_id, til, updated_at }`만 담는다(body 키를 넣지 않아 기존 body 보존 — saveLessonNote가 til을 보존하는 것과 대칭). readAllLessonNotes와 saveLessonNote는 이번에 건드리지 않는다(/notes의 TIL 표시는 이번 범위 밖).

    (3) 서버 액션은 새 파일 til-actions.ts에 둔다 — actions.ts는 check-progress-gates.mjs G4가 식별자 위치를 계약으로 못박고 있어 손대지 않는다(note-actions.ts가 같은 이유로 분리된 것과 동일). saveLessonTilAction(lessonId, til): 본문 순서가 보안 계약이다 — hasUnlockCookie() 재검증 → getLessonBySlug() 존재 검증 → saveLessonTil() 순서로만 호출하고, 저장소 오류는 잡지 않고 그대로 전파한다(note-actions.ts와 동형).

    (4) api/progress/route.ts: ProgressLesson 타입에 `til: string`을 추가하고, lesson 조립부에서 `til: noteRead.ok ? noteRead.til : ''`로 채운다(읽기 실패 시 note는 기존대로 {ok:false}이고 til은 빈 문자열). 보안 순서(unlocked 우선 → 슬러그 존재 검증 후 readLessonNote)는 그대로 둔다.

    (5) progress-provider.tsx: ProgressLesson 타입 미러에 `til: string`을 추가한다(route.ts의 ProgressLesson과 형태 일치).

    (6) lesson-til.tsx (신규 'use client' 컴포넌트): props는 직렬화 스칼라 2개 {lessonId, initialTil}. 라벨 "오늘 배운 것 한 줄?" + 코넬 큐 헬퍼 문구 "내 말로 한 문장으로 설명하면?" + 한 줄 입력(input 또는 rows=1 textarea) + "저장" 버튼. IME 안전: onChange는 setValue만 하고 값의 길이/문자종류를 들여다보지 않는다(lesson-notepad.tsx의 한글 IME 원칙 그대로). 저장은 버튼 클릭 시 saveLessonTilAction 호출(자동 저장 아님), 성공 시 저장 상태 표시(role=status aria-live=polite로 "저장됨"), 실패 시 입력 글은 그대로 두고 "저장하지 못했어요" 표시. **건너뛰기 가능**: 빈 값 저장 허용, TIL은 어떤 경우에도 완료의 전제 조건이 아니다 — 완료 버튼·완료 상태와 결합하지 않는다(완료 여부와 독립적으로 항상 입력 가능). initialTil이 있으면 입력값 초기값으로 채워 재표시한다. 스타일은 기존 토큰/유틸리티만 사용한다(.panel/.btn/.btn-action/text-body/text-label/text-badge-neutral-text 등) — 리터럴 색·`text-[...]` 임의값 금지(check-design-tokens).

    (7) progress-slots.tsx: LessonTilSlot({lessonId}) 추가 — useProgress()가 status==="ready" 이고 data.lesson 존재이고 data.lesson.note.ok 일 때만 `<LessonTil lessonId initialTil={data.lesson.til} />`를 렌더, 그 외(loading/error/locked/note 실패)에는 아무것도 렌더하지 않는다(CompleteButtonSlot/LessonNoteSlot의 가드 방식과 동일 — 잠금 상태에서 TIL 입력 경로 자체를 없앤다).

    (8) page.tsx: data-progress-controls 블록 안, `<CompleteButtonSlot .../>` 바로 다음 · `<LessonPager .../>` 앞에 `<LessonTilSlot lessonId={lesson.slug} />`를 추가한다.

    브랜딩(HARD RULE): 어떤 문구에도 KANT/Kant 금지.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx velite build --clean && npm run build && node scripts/check-progress-gates.mjs</automated>
    <human-check>내장 브라우저를 아이패드 폭으로 열고 잠금 해제 상태에서 아무 레슨(/lesson/1-3-python-variables-and-types)을 연다. 완료 영역에 "오늘 배운 것 한 줄?"과 코넬 큐 "내 말로 한 문장으로 설명하면?"이 보이는지, 한 줄 입력 후 저장하면 "저장됨"이 뜨는지, 새로고침해도 값이 남는지, 라이트/다크 모두 자연스러운지 확인한다. 완료를 누르지 않아도 입력이 되는지(완료와 독립) 확인한다. 테스트로 넣은 TIL 값은 빈 값으로 다시 저장해 흔적을 남기지 않는다.</human-check>
    <automated>test -f .env.local && node --env-file=.env.local scripts/check-supabase-note.mjs || echo "check-supabase-note skipped (.env.local 없음)"</automated>
  </verify>
  <done>lesson_note.til에 한 줄이 저장·재조회되고(기존 body 보존), 완료 영역에서 코넬 큐와 함께 입력·저장·표시된다. TIL이 비어도 완료가 막히지 않는다. tsc·lint·velite·build·check-progress-gates 통과.</done>
</task>

<task type="auto">
  <name>Task 2: 공유 `<PredictPrompt>` 콜아웃 컴포넌트 + mdx 등록 + 파일럿 1편</name>
  <files>src/components/predict-prompt.tsx, src/components/mdx-content.tsx, src/content/lessons/step-1/1-3-python-variables-and-types.mdx</files>
  <action>
    의례 문구를 컴포넌트에 한 번만 담고(레슨별 커스텀 저작 아님 — 의례 장치라 모든 레슨에서 글자 그대로 동일), 파일럿 1편에 먼저 넣어 렌더·빌드·게이트를 검증한다(사용자 pilot-first 원칙).

    (1) predict-prompt.tsx: children/props 없는 자기완결 컴포넌트로 만든다(`<PredictPrompt />` 자기닫힘으로 MDX에 삽입하기 위함 — 문구가 컴포넌트 안에 baked-in이면 모든 삽입 지점이 단일 JSX 한 줄이라 저작 편차가 원천 차단된다). 항상 보이는 콜아웃이다(접기/`<details>` 아님 — 사고 실험이라 숨길 이유가 없다). 문구(한국어, KANT 금지, 한국어 장문 규칙: 2~3문장마다 끊기):
      - 제목/리드: 코드를 보기 전 잠깐 멈추라는 신호. 예 — "코드 보기 전 30초: 먼저 스스로 설계"
      - 본문: "아래 코드를 보기 전에, 어떤 순서로 짜야 할지 머릿속으로 한 번 그려 보세요. 잘 안 그려져도 괜찮습니다. 통과 여부는 중요하지 않아요. 스스로 예상해 본 다음 코드와 맞춰 보면, 그냥 읽고 넘어갈 때보다 훨씬 오래 남습니다."
      스타일은 기존 토큰/유틸리티만 사용한다(.panel 흰 면 + 잉크 테두리 표면을 재사용, text-body/text-label 등) — 리터럴 색·`text-[...]` 임의값·기본 팔레트 색 유틸리티 금지(check-design-tokens 규칙 a/b/c). 접근성: 장식 아이콘은 aria-hidden.

    (2) mdx-content.tsx: defaultComponents에 `PredictPrompt: PredictPrompt as ComponentType`를 추가하고 import한다(RunPython/RunSQL과 동형 — MDX는 이 매핑으로 `<PredictPrompt />`를 해석한다. MDX 원문에 import는 넣지 않는다).

    (3) 파일럿 삽입: 1-3-python-variables-and-types.mdx 의 "## 4. 실무 예제" 섹션에서 첫 코드 요소(이 파일은 `<RunPython>`) 바로 앞 줄에 `<PredictPrompt />`를 넣는다. 앞뒤로 빈 줄을 둔다(도입 문단 다음 빈 줄 → `<PredictPrompt />` → 빈 줄 → `<RunPython>`). `<details>`가 아니므로 L3/L4에는 무관하지만, MDX 블록 분리를 위해 빈 줄은 반드시 유지한다.
  </action>
  <verify>
    <automated>npx velite build --clean && node scripts/check-lesson-structure.mjs && node scripts/check-design-tokens.mjs && node scripts/check-brand.mjs && npx tsc --noEmit && npm run lint</automated>
    <human-check>내장 브라우저로 /lesson/1-3-python-variables-and-types 4장(실무 예제)을 연다. 첫 코드(RunPython) 바로 위에 항상 보이는 "먼저 스스로 설계" 콜아웃이 뜨는지, 실행 버튼과 겹치지 않는지, 라이트/다크 모두 읽기 좋은지 확인한다.</human-check>
  </verify>
  <done>`<PredictPrompt>`가 mdx 매핑에 등록되고 파일럿 레슨 4장 첫 코드 앞에 항상 보이는 콜아웃으로 렌더된다. velite·lesson-structure·design-tokens·brand·tsc·lint 통과.</done>
</task>

<task type="auto">
  <name>Task 3: 예측 프롬프트 전량 승격 — 나머지 32편에 멱등 코드모드로 균일 삽입</name>
  <files>scripts/insert-predict-prompt.mjs, src/content/lessons/**/*.mdx</files>
  <action>
    경계(4장에 코드/쿼리 예제가 있는 콘텐츠 레슨, 측정값 33편) 안에 `<PredictPrompt />`를 100% 균일 삽입한다. 손으로 32편을 편집하는 대신 멱등 코드모드를 써서 저작 편차·중복 삽입을 원천 차단한다(리서치: 부분 적용은 유해 — 경계 안 전량이어야 한다).

    (1) scripts/insert-predict-prompt.mjs (Node 표준 모듈만, check-lesson-structure.mjs의 파일 순회 골격 재사용):
      - 대상: src/content/lessons/step-1|2|3 의 .mdx 중 프론트매터 hasContent: true.
      - 각 파일에서 "## 4. 실무 예제" 줄을 찾고, 다음 "## " 헤딩(또는 EOF)까지를 4장 범위로 잡는다.
      - 4장 범위 안에서 첫 코드 요소 줄을 찾는다: `<RunPython>` 또는 `<RunSQL>` 또는 여는 코드펜스(정규식 `/^```[a-z]/` — 소문자 언어 태그로 시작하는 여는 펜스). 없으면 그 레슨은 건너뛴다(3-2-project-rag-agent, 3-7-project-ax-launch — 4장에 코드 없음).
      - **멱등**: 4장 범위 안에 이미 `<PredictPrompt`가 있으면 그 파일은 건너뛴다(Task 2 파일럿·재실행 안전).
      - 삽입: 첫 코드 요소 줄 바로 앞에 `<PredictPrompt />`와 빈 줄을 넣는다. 삽입 지점 직전 줄이 빈 줄이 아니면 빈 줄도 함께 넣어 `... , '', '<PredictPrompt />', '', <코드요소>` 형태를 보장한다(MDX 블록 분리).
      - CRLF/LF 모두 처리(정규화 후 처리, 저장은 원본 개행 정책과 일치 — check-lesson-structure가 CRLF도 정규화해 검사하므로 LF 저장이 안전).
      - 종료 시 요약 출력: 삽입한 파일 수 / 이미 있어 건너뛴 수 / 4장 코드 없어 건너뛴 수.

    (2) 코드모드 실행: `node scripts/insert-predict-prompt.mjs`.

    (3) 검증 스크립트(태스크 내 인라인 Node 또는 verify의 assert)로 경계 100%를 못박는다: hasContent:true 이고 4장에 코드 요소가 있는 레슨의 4장 범위에 `<PredictPrompt`가 있는 파일 수가 정확히 33이고, 있어야 하는데 없는 파일이 0인지 확인한다(누락 0). 4장 코드 없는 2편에는 없어야 한다.

    스크립트는 저장소에 남긴다(멱등·재실행 가능 — 앞으로 레슨이 추가되면 다시 돌려 경계를 유지). 머리 주석에 경계 정의와 멱등성을 적는다.
  </action>
  <verify>
    <automated>node scripts/insert-predict-prompt.mjs && node -e "const fs=require('fs'),path=require('path');const dirs=['step-1','step-2','step-3'].map(d=>path.join('src/content/lessons',d));let have=0,missing=[];for(const dir of dirs){for(const f of fs.readdirSync(dir).filter(x=>x.endsWith('.mdx'))){const c=fs.readFileSync(path.join(dir,f),'utf8').replace(/\r\n/g,'\n');const fm=c.match(/^---\n([\s\S]*?)\n---/);if(!fm||!/hasContent:\s*true/.test(fm[1]))continue;const L=c.split('\n');const s=L.indexOf('## 4. 실무 예제');if(s<0)continue;let e=L.length;for(let i=s+1;i<L.length;i++){if(L[i].startsWith('## ')){e=i;break;}}const sec=L.slice(s,e);const code=sec.some(l=>/<RunPython>|<RunSQL>/.test(l)||/^\x60\x60\x60[a-z]/.test(l.trim()));const pp=sec.some(l=>l.includes('<PredictPrompt'));if(code){if(pp)have++;else missing.push(f);}}if(missing.length){console.error('누락:',missing.join(', '));process.exit(1);}if(have!==33){console.error('예측 프롬프트 보유 레슨 수 '+have+' (기대 33)');process.exit(1);}console.log('예측 프롬프트 전량 승격 OK — '+have+'편, 누락 0');"</automated>
    <automated>npx velite build --clean && node scripts/check-lesson-structure.mjs && node scripts/check-pace.mjs && node scripts/check-review.mjs && node scripts/check-route-rendering.mjs && npx tsc --noEmit && npm run lint && npm run build</automated>
    <human-check>내장 브라우저로 정적 코드 예제 레슨 1편(예: /lesson/2-3-react-components)과 실행형 1편(/lesson/1-4-sql-queries-and-joins)을 열어 4장 첫 코드 앞에 같은 예측 프롬프트가 균일하게 뜨는지, 라이트/다크 모두 자연스러운지 확인한다.</human-check>
  </verify>
  <done>4장에 코드가 있는 콘텐츠 레슨 33편 전부(파일럿 포함)의 첫 코드 앞에 동일한 `<PredictPrompt />`가 있고 누락 0, 4장 코드 없는 2편에는 없다. velite·lesson-structure·pace·review·route-rendering·tsc·lint·build 통과.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → TIL Server Action | 사용자가 입력한 TIL 문자열과 lessonId가 saveLessonTilAction으로 서버에 넘어온다(캡처된 POST 재전송 가능). |
| server → Supabase(service_role) | note-store.saveLessonTil이 service_role로 lesson_note에 upsert(RLS 우회). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-0rz-01 | Spoofing/Elevation | saveLessonTilAction | high | mitigate | 본문 순서 계약 — hasUnlockCookie() 재검증 → getLessonBySlug() 존재 검증 → saveLessonTil() 순(note-actions.ts와 동형). 렌더 여부와 무관하게 액션이 스스로 재검증. |
| T-0rz-02 | Tampering | 임의 lessonId로 til 행 생성 | medium | mitigate | getLessonBySlug() 미존재 슬러그는 저장 전 거부('invalid lesson') — 커리큘럼 매니페스트에 없는 slug로는 행을 못 만든다. |
| T-0rz-03 | DoS | 초대형 TIL 본문 | medium | mitigate | note-store.saveLessonTil에서 MAX_TIL_LENGTH(2_000) 상한 초과 시 저장 거부 + 한국어 오류(MAX_NOTE_LENGTH 상한과 동일 방어). |
| T-0rz-04 | Information Disclosure | til이 별도 컬럼이 아니라 body에 결합 | high | mitigate | WR-01 — til은 별도 컬럼, saveLessonTil은 {lesson_id, til, updated_at}만 upsert해 body 미변경. note 읽기 실패 시 til은 ''로 처리하고 TilSlot은 note.ok에서만 렌더. |

new npm/pip/cargo 설치 없음 → 패키지 적법성 게이트 불필요.
</threat_model>

<verification>
- 전량 승격 경계 100%: hasContent:true & 4장 코드 있는 레슨의 4장에 `<PredictPrompt` 보유 = 33, 누락 0(Task 3 verify assert가 못박음).
- 게이트 회귀 없음: check-lesson-structure(L1~L7), check-design-tokens, check-brand, check-progress-gates, check-pace, check-review, check-route-rendering.
- 빌드 파이프라인: `npx velite build --clean`(매니페스트 재생성) → `npm run build`(next가 velite도 트리거) → `npx tsc --noEmit` → `npm run lint`.
- TIL Supabase 왕복: check-supabase-note(.env.local 있을 때) — til 컬럼 추가 후에도 lesson_id select/RLS 기본 차단 유지.
- 내장 브라우저(아이패드 폭, 라이트/다크): 예측 프롬프트 표시(실행 버튼과 비겹침), TIL 입력·저장·재표시(테스트 값 되돌려 흔적 제거). 아이패드 실기기 UAT는 사용자 몫.
</verification>

<success_criteria>
- [ ] 4장에 코드가 있는 콘텐츠 레슨 33편 전부에 동일한 `<PredictPrompt />`가 첫 코드 앞에 있고 누락 0(부분 적용 없음).
- [ ] 예측 프롬프트가 접기가 아니라 항상 보이는 콜아웃이며 모든 레슨에서 문구가 동일하다.
- [ ] 완료 영역에서 코넬 큐("내 말로 한 문장으로 설명하면?")와 함께 TIL 한 줄을 입력·저장·재표시할 수 있고, 비워도 완료가 막히지 않는다.
- [ ] TIL이 lesson_note.til(별도 컬럼)에 저장되고 기존 body를 덮어쓰지 않는다. til 마이그레이션 파일이 supabase/migrations/에 남는다.
- [ ] 모든 기존 게이트(lesson-structure·design-tokens·brand·progress-gates·pace·review·route-rendering)와 tsc·lint·velite·build 회귀 없음.
- [ ] 공개 문구 어디에도 KANT/Kant 없음.
</success_criteria>

<output>
Create `.planning/quick/260902-0rz-predict-prompt-til-cornell/260902-0rz-SUMMARY.md` when done
</output>
