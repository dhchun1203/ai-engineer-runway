---
phase: quick-260901-r9t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - velite.config.ts
  - scripts/check-lesson-structure.mjs
  - src/content/curriculum-helpers.ts
  - src/app/glossary/page.tsx
  - src/components/site-nav.tsx
  - scripts/check-route-rendering.mjs
autonomous: true
requirements:
  - round2-j-glossary
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "/glossary 페이지가 열리고 전 35편 레슨의 용어가 섹션별로 나열된다"
    - "라틴 시작 용어(29건)는 A–Z 섹션에, 한글 시작 용어는 ㄱㄴㄷ(초성) 섹션에 배치된다"
    - "같은 단어의 여러 정의가 자동 병합되지 않고 그룹(정의 목록)으로 보존된다 — 다의어 학습 가치 유지"
    - "각 정의에 출처 레슨 역링크(레슨 permalink 최상단)가 붙어 이동할 수 있다"
    - "내비게이션에서 /glossary로 진입할 수 있다(데스크톱·폰 햄버거 모두)"
    - "빌드 시 어떤 레슨의 용어 표를 파싱하지 못하면(0개) 빌드가 throw로 실패한다"
    - "/glossary가 완전 정적으로 프리렌더되고 check-route-rendering 게이트에 등록된다"
  artifacts:
    - "velite.config.ts — parseTermTable + lessons transform의 terms 필드"
    - "scripts/check-lesson-structure.mjs — checkTermTable에 velite 파서 상호참조 주석"
    - "src/content/curriculum-helpers.ts — getAllTerms() + 용어 그룹/섹션 타입"
    - "src/app/glossary/page.tsx — 완전 정적 단일 페이지"
    - "src/components/site-nav.tsx — /glossary 링크(용어집)"
    - "scripts/check-route-rendering.mjs — /glossary를 EXPECTED_STATIC_ROUTES에 등록"
  key_links:
    - "velite terms 필드 → getAllTerms 그룹핑 → /glossary 렌더"
    - "getAllTerms 그룹핑이 word 완전일치로만 묶어 다의어를 병합하지 않음"
    - "각 정의의 역링크 permalink가 실제 /lesson/<slug> 라우트와 일치"
    - "/glossary 정적 프리렌더 ↔ check-route-rendering EXPECTED_STATIC_ROUTES 등록"
---

<objective>
레슨 35편의 용어 표(총 247용어)를 빌드타임에 파싱해, ㄱㄴㄷ·A–Z 섹션과 다의어 그룹으로 훑고 출처 레슨으로 역링크하는 정적 `/glossary` 페이지를 만든다.

Purpose: 리서치 2단(round2-j-aggregation) 항목. L5 게이트가 이미 기계 파싱 가능한 형태를 강제하고 있어("이 레슨의 단어" 표), 그 형식에 코드가 기대어 "표가 단일 진실"로 남는다. 같은 단어가 문맥마다 다르게 쓰이는 것(회귀: ML vs 버그 / 상태: React vs 에이전트)을 그룹 표시로 드러내는 것이 학습 가치다.
Output: velite 파서 + getAllTerms 헬퍼 + /glossary 페이지 + 내비 링크 + 라우트 게이트 등록.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/research/edu-sites/round2-j-aggregation.md

# 설계 단일 소스는 위 round2-j. 아래는 이식/연결 대상 실물.
@velite.config.ts
@scripts/check-lesson-structure.mjs
@src/content/curriculum-helpers.ts
@src/content/modules.ts
@src/app/step/[stepId]/page.tsx
@src/app/curriculum/page.tsx
@src/components/site-nav.tsx
@scripts/check-route-rendering.mjs

# 인터페이스 메모(executor 참고):
# - 레슨 매니페스트는 `#site/content`의 `lessons` (velite 산출물). `Lesson` 타입은
#   velite가 스키마+transform 반환값에서 추론한다 — transform에 terms를 추가하면
#   `lesson.terms`가 자동으로 타입에 실린다(수동 타입 선언 불필요).
# - modules.ts: `modules`(19개, id "1-1"~"3-7", stepId/title/order), `steps`.
# - velite는 next build/dev 중 next.config.ts가 `import("velite").then(m=>m.build())`로
#   실행한다. velite.config.ts를 바꾸면 로컬 .velite가 stale일 수 있으므로 반드시
#   `node node_modules/velite/bin/velite.js build --clean`로 재생성 후 확인한다
#   (프로덕션 빌드는 매번 clean이라 무관 — round2-j 함정 e).
# - 게이트(check-*.mjs)는 package.json scripts에 없다. 개별 실행: `node scripts/<name>.mjs`.
# - 레슨 표 실제 형식(1-3 레슨 276줄):
#     **이 레슨의 단어**
#     (빈 줄)
#     | 단어 | 뜻 |
#     |------|-----|
#     | 변수 | 값을 담아두는 이름 붙은 공간 |
#     ... 데이터 행 5~8개, 셀은 순수 텍스트(백틱·볼드·이스케이프 0건).
</context>

<tasks>

<task type="tracer">
  <name>Task 1: velite terms 파서(L5 이식) + 스키마 terms 필드 + 게이트 상호참조 주석</name>
  <files>velite.config.ts, scripts/check-lesson-structure.mjs</files>
  <precondition>로컬에 `.velite/`가 stale일 수 있음 — 이 태스크 검증은 `--clean` 재생성 후에만 유효하다.</precondition>
  <action>
    velite.config.ts에 `parseTermTable(content: string)` 함수를 추가한다(round2-j 권장 경로 1, ~25줄). check-lesson-structure.mjs의 checkTermTable(186~215줄) 로직을 그대로 이식하되, 검사가 아니라 추출을 한다:
    (1) 본문 줄 배열에서 `**이 레슨의 단어**` 라벨을 찾는다(정확히 1회 기대). meta.content는 frontmatter를 포함한 원문이지만 라벨 탐색 기반이라 실질 영향 없음(round2-j 함정 e). CRLF 대비 `\r\n`→`\n` 정규화는 check-lesson-structure.mjs 302줄과 동일하게 처리.
    (2) 라벨 다음 빈 줄들을 건너뛰고 `| 단어 | 뜻 |` 헤더 → 구분행(`^\|[\s:-]+\|`) → `|`로 시작하는 데이터 행들을 순회.
    (3) 각 데이터 행을 `|`로 분할하고 바깥 파이프 사이 두 셀을 트림해 `{ word, definition }`로 뽑는다. 셀 트림 필수.
    (4) 레슨당 정확히 5~8개를 기대. **파싱 결과가 0개면 throw**로 빌드를 실패시킨다(L5 게이트와 동일한 방어 — round2-j 권장 경로 1).
    lessons 스키마 transform을 `.transform((data, { meta }) => ({ ...data, permalink: `/lesson/${data.slug}`, terms: data.hasContent ? parseTermTable(meta.content) : [] }))`로 확장한다. hasContent:false 스텁은 파싱하지 않고 `terms: []` — 이는 L5 게이트가 hasContent:true만 검사하는 것과 정확히 대칭이다(현재 스텁 0편이지만 미래 방어). velite 0.4의 두 번째 인자 `{ meta }`로 본문 원문(meta.content)에 접근한다 — frontmatter 이관 불필요.
    velite.config.ts의 parseTermTable 위에 상호참조 주석을 단다: 이 파서와 scripts/check-lesson-structure.mjs의 checkTermTable(L5)이 동일한 용어-표 문법(라벨→헤더→구분행→데이터 행)을 각각 구현하며 함께 바뀌어야 한다는 점을 명시한다(round2-j 함정 d, 파서-게이트 이중 구현). check-lesson-structure.mjs의 checkTermTable 함수 위(186줄 주석 근처)에도 상반되는 주석을 달아 velite.config.ts의 parseTermTable을 가리킨다.
  </action>
  <verify>
    <automated>node node_modules/velite/bin/velite.js build --clean && node -e "const l=require('./.velite/lessons.json'); const bad=l.filter(x=>x.hasContent&&(!x.terms||x.terms.length<5||x.terms.length>8)); if(bad.length) throw new Error('5-8 위반: '+bad.map(b=>b.slug+'='+(b.terms||[]).length).join(',')); const t=l.reduce((n,x)=>n+(x.terms?x.terms.length:0),0); if(t<240||t>255) throw new Error('terms 총합 '+t+' (기대 ~247)'); if(!l.every(x=>!x.hasContent||x.terms.every(z=>z.word&&z.definition))) throw new Error('빈 word/definition'); console.log('terms 총합',t)"</automated>
  </verify>
  <done>velite build가 exit 0, .velite/lessons.json의 모든 hasContent 레슨이 5~8개 terms(각 word·definition 비어있지 않음)를 가지며 총합이 ~247. 파싱 0개인 표가 있으면 빌드가 throw로 실패한다.</done>
</task>

<task type="auto">
  <name>Task 2: getAllTerms() 헬퍼 + /glossary 정적 페이지 + 내비 링크</name>
  <files>src/content/curriculum-helpers.ts, src/app/glossary/page.tsx, src/components/site-nav.tsx</files>
  <action>
    curriculum-helpers.ts에 `getAllTerms()`와 관련 타입(TermSource, TermDefinition, TermGroup, GlossarySection)을 추가한다(round2-j 권장 경로 2):
    - getOrderedLessons() 순서로 전 레슨의 terms를 순회하며 각 term에 출처 `{ lessonSlug: l.slug, lessonTitle: l.title, moduleId: l.moduleId, permalink: l.permalink }`를 부착한다.
    - **word 완전일치로만** 그룹핑한다(같은 word의 여러 정의를 definitions 배열로 유지). 정의 텍스트가 같아도 **병합·중복제거 금지** — 그룹 표시 자체가 다의어 학습 가치다(round2-j 함정 b). definitions는 순회 순서(커리큘럼 순)를 유지한다.
    - 섹션 분리: 각 그룹의 첫 글자로 판정. 라틴 문자(/[A-Za-z]/)로 시작하면 latin 섹션(대문자 버킷 A–Z), 한글 음절이면 korean 섹션(초성 버킷). 초성은 (charCodeAt(0) - 0xAC00) / 588을 내림해 19-자음 초성 배열(ㄱ ㄲ ㄴ ㄷ ㄸ ㄹ ㅁ ㅂ ㅃ ㅅ ㅆ ㅇ ㅈ ㅉ ㅊ ㅋ ㅌ ㅍ ㅎ)에서 뽑는다. 그 외(숫자·기호 시작)는 korean 쪽 '기타' 버킷으로 보내거나 latin 뒤에 붙인다 — executor 판단(29건 라틴은 반드시 A–Z, 나머지는 초성).
    - 정렬: 한글 word는 localeCompare(other, 'ko'), 라틴 word는 대소문자 무시 비교. 섹션 순서는 초성 배열 순 → A–Z 순.
    - 반환: `{ korean: GlossarySection[], latin: GlossarySection[], totalGroups, totalTerms }` 형태(executor 판단, 페이지에서 소비하기 쉬운 형태면 됨).
    src/app/glossary/page.tsx를 완전 정적 단일 페이지로 만든다(round2-j 권장 경로 3, curriculum/page.tsx·step 페이지의 크림 종이·각진 패널 디자인 재사용). ProgressProvider·쿠키·진도 읽기 없음(정적 셸). generateStaticParams 불필요(단일 라우트). 구성: 상단 제목("용어집") + 총 용어 수 안내 → (선택) 초성/알파벳 점프 인덱스 → 섹션 헤더(ㄱ/ㄴ/…/A/B/…)마다 그 섹션 용어 그룹 목록. 각 그룹: 단어(굵게) + 정의(들). 다의어(definitions.length>1)는 정의를 목록으로 나열. 각 정의에 출처 레슨 역링크를 칩/링크로 붙이되 **레슨 permalink 최상단으로만** 링크한다 — 용어 표는 6장 섹션이 아니라 별도 위치라 앵커가 없으므로 직행 앵커는 이번 범위 밖(round2-j 함정 a). 아이패드 우선: 44px+ 터치 타깃, 세로 스크롤, 기존 chip/nav-link 유틸 재사용.
    site-nav.tsx의 NAV_ITEMS에 `{ label: "용어집", href: "/glossary" }`를 커리큘럼 항목 바로 뒤에 추가한다(용어 참조 도구라 커리큘럼과 함께 두는 게 자연스럽다). 데스크톱 행·폰 햄버거 패널이 같은 NAV_ITEMS를 순회하므로 한 번 추가로 양쪽이 반영된다. 상단 D-09 주석에 "D-09 4항목 확정 이후 용어집(5번째)을 리서치 2단 round2-j로 추가"임을 한 줄 남긴다(접근성상 최소 한 곳의 진입점이 필요 — round2-j 함정 a의 미접근 우려 해소).
  </action>
  <verify>
    <automated>node node_modules/velite/bin/velite.js build --clean && npx tsc --noEmit</automated>
  </verify>
  <done>getAllTerms가 word 완전일치 그룹(다의어 배열 보존)과 ㄱㄴㄷ/A–Z 섹션을 반환하고, /glossary 페이지·내비 링크가 타입 오류 없이 컴파일된다. 역링크는 레슨 permalink로만 향한다.</done>
</task>

<task type="auto">
  <name>Task 3: /glossary 라우트 게이트 등록 + 정적 프리렌더 및 회귀 확인</name>
  <files>scripts/check-route-rendering.mjs</files>
  <action>
    check-route-rendering.mjs의 EXPECTED_STATIC_ROUTES(83줄)에 `/glossary`를 추가한다 — /glossary는 쿠키·진도를 읽지 않는 완전 정적 페이지이므로 프리렌더 대상이다. 열린 세계(미등록=무검사)이므로 신규 라우트는 반드시 게이트에 등록해 정적 계약을 검증한다(round2-j 권장 경로 5, 게이트 요구사항). 라우트 목표 계약을 적는 24줄 근처 주석의 "완전 정적" 목록에도 /glossary를 한 줄 추가한다.
    별도 check-glossary.mjs는 만들지 않는다(플래너 결정): (1) velite parseTermTable의 throw가 이미 파싱 무결성을 빌드타임에 게이트하고, (2) L5(check-lesson-structure)가 원본 표 형식을 게이트하며, (3) check-route-rendering이 라우트를 게이트한다. 총량 ~247 단언은 레슨 편집마다 흔들려 취약하고 빌드가 이미 보장하는 것을 중복한다 — 5주 타임라인 대비 과함(round2-j "과하면 생략").
  </action>
  <verify>
    <automated>npx next build && node scripts/check-route-rendering.mjs && node scripts/check-lesson-structure.mjs && node scripts/check-brand.mjs && node scripts/check-design-tokens.mjs</automated>
  </verify>
  <done>next build가 /glossary를 완전 정적으로 프리렌더하고, check-route-rendering이 /glossary 정적 계약을 통과하며, check-lesson-structure·check-brand·check-design-tokens 기존 게이트에 회귀가 없다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 저장소 MDX 저작 → velite 빌드 | 신뢰된 저자(사용자 본인)만 레슨 MDX를 작성. 외부/런타임 사용자 입력이 파서에 닿지 않음 |
| 정적 HTML → 브라우저 | /glossary는 빌드타임에 굳은 정적 마크업. 런타임 사용자 입력·쿠키·DB 읽기 없음 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-r9t-01 | Tampering | velite parseTermTable(meta.content) | low | mitigate | 파싱 실패(0개) 시 throw로 빌드 실패 — 손상된/누락된 표가 조용히 통과하지 못함. 입력은 신뢰된 저장소 MDX뿐 |
| T-r9t-02 | Denial of Service | parseTermTable throw = 빌드 중단 | low | accept | 빌드 중단은 의도된 게이트 동작(잘못된 표를 배포하지 않음). 런타임 DoS 아님 — 정적 산출물이므로 요청당 파싱 없음 |
| T-r9t-03 | Information Disclosure | /glossary 정적 페이지 | low | accept | 공개 레슨 용어만 노출(이미 /lesson 공개). KANT 브랜딩 금지는 check-brand 게이트가 강제 |
| T-r9t-SC | Tampering | npm/pip/cargo installs | low | accept | 신규 패키지 설치 없음 — 기존 velite/next만 사용. 공급망 체크포인트 불필요 |
</threat_model>

<verification>
- `node node_modules/velite/bin/velite.js build --clean` 성공 + .velite/lessons.json terms 총합 ~247, 각 hasContent 레슨 5~8개.
- `npx tsc --noEmit` 타입 통과(getAllTerms·/glossary·내비).
- `npx next build` 성공 + /glossary 완전 정적 프리렌더.
- 정적 게이트 회귀 없음: check-route-rendering(/glossary 등록), check-lesson-structure, check-brand, check-design-tokens 모두 통과.
- 로컬 확인 시 velite.config.ts 변경 후 반드시 `--clean` 재생성(round2-j 함정 e — stale .velite).
</verification>

<success_criteria>
- /glossary가 전 35편의 247용어를 ㄱㄴㄷ·A–Z 섹션으로 보여준다.
- 라틴 시작 29용어가 A–Z 섹션에, 나머지는 초성 섹션에 배치.
- 29개 중복 용어가 병합되지 않고 정의 목록(그룹)으로 보존(다의어).
- 각 정의에 출처 레슨 역링크(permalink 최상단)가 있다.
- 내비(데스크톱·폰)에서 /glossary 진입 가능.
- 잘못된 용어 표는 빌드를 실패시킨다(velite throw).
- /glossary가 정적 프리렌더 + 라우트 게이트 등록.
</success_criteria>

<output>
Create `.planning/quick/260901-r9t-glossary-247-a-z/260901-r9t-SUMMARY.md` when done.
</output>
