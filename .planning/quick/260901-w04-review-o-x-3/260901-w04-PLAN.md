---
phase: 260901-w04-review-o-x-3
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - velite.config.ts
  - supabase/migrations/20260901130000_add_missed_q_to_lesson_review.sql
  - src/lib/review.ts
  - src/lib/review-store.ts
  - scripts/check-review.mjs
  - src/app/review/page.tsx
  - src/components/review-judgment-buttons.tsx
  - src/app/review-actions.ts
  - scripts/check-route-rendering.mjs
  - src/components/site-nav.tsx
  - src/components/today-lesson-card.tsx
  - src/components/today-review-card.tsx
  - src/components/schedule-table.tsx
autonomous: false
requirements:
  - round2h-V2-review-session
  - round2h-V2-selfcheck-extraction
  - FINAL-REPORT-O-triangle-X
  - FINAL-REPORT-wrong-answers
  - round2h-F8-buffer-CTA

estimate:
  tokens: 95000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "/review를 열면 완료 레슨의 스스로 점검 문항이 약한 것 우선으로 최대 12개 표시된다"
    - "각 문항에 O(맞음)·△(맞았지만 불안)·X(틀림) 세 버튼이 있고 44px+ 터치 타깃이다"
    - "O를 누르면 그 문항이 missed_q에서 빠지고, △·X를 누르면 missed_q에 들어간다"
    - "문항은 정답을 표시하지 않고 레슨의 스스로 점검 섹션 앵커 링크만 제공한다"
    - "오답 모아보기에 missed_q가 있는 문항이 앵커 링크와 함께 한곳에 모인다"
    - "9/29 버퍼일에 홈 카드·일정표에서 /review로 가는 CTA가 뜬다"
    - "velite build가 각 레슨에서 스스로 점검 문항 2개를 추출하며, 정확히 2개가 아니면 빌드가 실패한다"
  artifacts:
    - velite.config.ts
    - supabase/migrations/20260901130000_add_missed_q_to_lesson_review.sql
    - src/lib/review.ts
    - src/lib/review-store.ts
    - src/app/review/page.tsx
    - src/components/review-judgment-buttons.tsx
    - src/app/review-actions.ts
    - scripts/check-review.mjs
    - scripts/check-route-rendering.mjs
  key_links:
    - "velite selfCheck 추출 → page가 lesson.selfCheck[questionIndex]로 소비"
    - "review-store missed_q → review.ts selectReviewQuestions 약한 것 우선 tier"
    - "recordReviewJudgment 서버 액션 → store 쓰기 → 다음 selection이 그 값을 읽음"
    - "/review force-dynamic → check-route-rendering EXPECTED_DYNAMIC_ROUTES"
---

<objective>
리서치 2단 플래그십의 마지막 조각: 완료·만기 레슨의 "스스로 점검" 문항을 한곳에 모아
풀고 O/△/X 3단으로 스스로 판정하는 `/review` 복습 세션, 약한 문항을 다시 보는 오답
모아보기, 그리고 9/29 버퍼일에서 세션으로 보내는 CTA를 만든다.

설계 단일 소스는 `.planning/research/edu-sites/round2-h-review-design.md`(V2 절) +
FINAL-REPORT의 "O/△/X 3단 판정" 항목이다. 1단(사다리 V0+V1)은 이미 완료돼
있고(review.ts·review-store.ts·홈 복습 카드·lesson_review 테이블·SELF_CHECK_ANCHOR),
이 플랜은 그 위에 문항 레이어를 얹는다.

Purpose: 회독 문화의 핵심인 "맞았지만 불안(△)"을 기록해 약한 문항을 우선 재소환하는
학습 루프를 완성한다. 정답은 세션에서 렌더하지 않는다(두 번째 마크다운 렌더러 부채
회피) — 문항만 보여주고 레슨의 스스로 점검 앵커로 확인을 넘긴다.
Output: velite selfCheck 추출, /review 라우트(force-dynamic), O/△/X 판정 서버 액션,
오답 모아보기, missed_q 마이그레이션 파일, 게이트 갱신.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md
@.planning/research/edu-sites/round2-h-review-design.md

# 이미 존재하는 재사용 대상 (읽고 패턴을 이어붙일 것)
@velite.config.ts
@src/lib/review.ts
@src/lib/review-store.ts
@src/app/review-actions.ts
@scripts/check-review.mjs
@scripts/check-route-rendering.mjs
@src/app/page.tsx
@src/content/lessons/step-1/1-3-python-variables-and-types.mdx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 데이터 척추 — selfCheck 추출 + missed_q + 순수 선택 로직 + 저장</name>
  <files>velite.config.ts, supabase/migrations/20260901130000_add_missed_q_to_lesson_review.sql, src/lib/review.ts, src/lib/review-store.ts, scripts/check-review.mjs</files>
  <read_first>
velite.config.ts의 parseTermTable(L28~75)이 이 태스크가 이식할 원형이다 — 라벨을
찾고, 그 뒤만 파싱하고, 0개면 throw하는 방어 구조를 그대로 따른다. scripts/check-lesson-structure.mjs의 L7 fence 추적(inFence 토글)이 코드펜스 내부를 건너뛰는 방법을
보여준다. src/lib/review.ts의 computeDueLessons·ReviewState·GRADUATE_COUNT,
src/lib/review-store.ts의 readReviewStates·markLessonReviewed, scripts/check-review.mjs의
runCase 하네스와 .velite 대조 케이스를 확인한다.
  </read_first>
  <behavior>
    selectReviewQuestions(순수 함수) 계약 — check-review.mjs 케이스로 먼저 고정(RED):
    - 완료+문항 있는 레슨만 후보다. 약한 것(missed_q 인덱스) → 만기(computeDueLessons) →
      나머지 3-tier 우선순위로 배열한다.
    - todayStr를 시드로 한 결정적 셔플: 같은 입력·같은 날짜면 항상 같은 순서(테스트 가능).
    - 레슨 라운드로빈: 가능한 한 연속 두 항목이 같은 레슨이 아니게 교차한다.
    - 12문항에서 자른다(상수). 후보가 12 미만이면 있는 만큼.
    - missed_q가 비어 있으면 약한 tier는 비고 만기·나머지만 나온다.
    - ReviewState에 missedQ가 없어도(undefined) []로 취급해 기존 케이스가 깨지지 않는다.
    - .velite 대조: hasContent 레슨은 selfCheck.length가 정확히 2다.
  </behavior>
  <action>
    velite.config.ts: parseTermTable과 동형인 parseSelfCheck(content) 함수를 추가한다.
    `**스스로 점검**` 라벨을 찾아(정확히 1회, 없으면 throw) 그 이후 구간만 본다.
    코드펜스 내부(삼중 백틱 토글로 추적)와 `<details>`…`</details>` 블록 내부(depth 카운터)를
    건너뛰고, 최상위에서 `/^\d+\.\s+(.+)/`에 맞는 번호 목록 항목만 문항 텍스트로 뽑는다 —
    힌트 보기·정답 보기 접기가 문항으로 오인되지 않게 하는 것이 이 파서의 핵심 방어다.
    추출 결과가 정확히 2개가 아니면(0개 포함) throw해 velite build를 실패시킨다
    (parseTermTable의 "0개면 throw"와 동형 계약). 스키마 transform에 selfCheck 필드를
    추가한다: `selfCheck: data.hasContent ? parseSelfCheck(meta.content ?? "") : []`
    (terms와 정확히 같은 패턴, string[] 타입 — 인덱스가 곧 questionIndex). parseTermTable과
    parseSelfCheck는 같은 "라벨 이후 파싱" 문법의 이중 구현이므로 한쪽 문법이 바뀌면 다른
    쪽도 함께 볼 것을 주석에 남긴다.

    supabase/migrations/20260901130000_add_missed_q_to_lesson_review.sql: 신규 파일.
    라이브 Supabase에는 오케스트레이터가 이미 컬럼을 적용했으므로 idempotent하게
    `add column if not exists missed_q int[] not null default '{}'`로 작성한다. create_lesson_review 마이그레이션의 헤더 주석 톤(default-deny·service_role 접근)을 이어
    missed_q가 "약한 문항 인덱스 배열(문항당 0/1)"임과 부패 수용(콘텐츠 편집으로 문항
    순서가 바뀌면 인덱스가 오염될 수 있으나 레슨당 2문항 고정이라 실무상 드묾 — 텍스트
    해시 키는 편집 시 더 잘 깨져 기각)을 명시한다.

    src/lib/review.ts: ReviewState에 `missedQ?: number[]`를 선택 필드로 추가한다(기존
    computeDueLessons·nextDueDate는 무시하므로 무해, check-review.mjs 기존 케이스도 유지).
    ReviewQuestionRef 타입({ lessonSlug: string; questionIndex: number })과 REVIEW_SESSION_LIMIT=12
    상수를 export한다. selectReviewQuestions를 <behavior>대로 구현한다: completedDateBySlug·
    states·todayStr·questionCountBySlug를 받아 3-tier를 만들고, todayStr 문자열 해시로 시드한
    작은 결정적 PRNG(mulberry32류)로 셔플한 뒤 레슨 라운드로빈으로 교차, 12개 컷. 의존성 0
    원칙을 지킨다(import 없음 — check-review.mjs가 그대로 로드). 안정 키 부패 수용을 주석에
    남긴다.

    src/lib/review-store.ts: readReviewStates의 select에 `missed_q`를 더하고 ReviewState에
    `missedQ: (row.missed_q as number[] | null) ?? []`를 채운다. recordReviewJudgment(lessonSlug,
    questionIndex, judgment)을 추가한다 — judgment는 'correct'|'shaky'|'wrong'. 현재 missed_q를
    읽어(maybeSingle) 새 배열을 만든다: correct면 그 인덱스를 제거, shaky·wrong이면 없을 때만
    추가(중복 방지, 정렬 유지). `upsert({ lesson_id, missed_q })`로 저장한다 — review_count·
    last_reviewed_at 컬럼을 지정하지 않아 on-conflict 시 사다리 상태를 건드리지 않고(신규 행이면
    default 0/null), markLessonReviewed의 사다리 전진과 서로 침범하지 않음을 주석에 명시한다.
    이 세션은 사다리(review_count)를 전진시키지 않는다 — 사다리 전진은 홈 카드의 "복습 완료"
    자기 신고가 유지한다(두 상호작용의 역할 분리).

    scripts/check-review.mjs: selectReviewQuestions 경계 케이스(약한 것 우선, 만기, 나머지,
    라운드로빈 교차, 12 컷, 같은 시드 결정성, missedQ undefined 무해)를 runCase로 추가한다.
    .velite 대조 블록에 "hasContent 레슨의 selfCheck.length가 정확히 2" 어설션을 추가한다
    (앵커 대조와 같은 .velite 존재 가드 안).
  </action>
  <verify>
    <automated>node node_modules/velite/bin/velite.js build --clean && node scripts/check-review.mjs</automated>
  </verify>
  <done>velite build가 35편 전부에서 selfCheck 2개씩 추출하고(임의 레슨 문항을 1개로 만들면 build throw로 실패), check-review.mjs의 selectReviewQuestions·selfCheck-count 케이스가 전부 통과한다. review-store가 missed_q를 읽고 recordReviewJudgment가 correct/shaky/wrong을 배열에 반영한다.</done>
  <reversibility rating="reversible">순수 로직·추가 컬럼·게이트 케이스 — 되돌리기 쉬움. missed_q는 add-column, 데이터 파괴 없음.</reversibility>
</task>

<task type="auto">
  <name>Task 2: /review 페이지 + O/△/X 판정 + 오답 모아보기 + 라우트 게이트</name>
  <files>src/app/review/page.tsx, src/components/review-judgment-buttons.tsx, src/app/review-actions.ts, scripts/check-route-rendering.mjs, src/components/site-nav.tsx</files>
  <read_first>
src/app/page.tsx(38~164행)이 hasUnlockCookie → readProgressRows → readReviewStates →
computeDueLessons 조립 순서와 completedDateBySlug(completedAt를 서울 날짜로 변환) 만드는
법을 보여준다 — /review도 같은 조립을 재사용한다. src/app/glossary/page.tsx가 크림 종이·
각진 패널(.panel/.chip) 정적 페이지 마크업 관례를, src/components/today-review-card.tsx가
SELF_CHECK_ANCHOR 앵커 링크(`/lesson/${slug}#${SELF_CHECK_ANCHOR}`)와 카드 톤을,
src/components/review-done-button.tsx가 use client + useTransition + 실패 표시 패턴을 준다.
src/app/review-actions.ts의 completeReview가 서버 액션 보안 계약(hasUnlockCookie 재검증 →
getLessonBySlug 존재 검증 → 저장)을, scripts/check-route-rendering.mjs의
EXPECTED_DYNAMIC_ROUTES(94행)가 동적 라우트 등록 위치를 준다.
  </read_first>
  <action>
    src/app/review-actions.ts: recordReviewJudgment(lessonId, questionIndex, judgment) 서버
    액션을 추가한다. completeReview와 동일한 순서로 스스로 재검증한다 — hasUnlockCookie()
    실패 시 throw, getLessonBySlug(lessonId) 없으면 throw, questionIndex가 해당 레슨
    selfCheck 길이 범위를 벗어나면 throw(위조 POST 방어), 그 다음에만 store의
    recordReviewJudgment를 호출한다. judgment 값도 'correct'|'shaky'|'wrong' 화이트리스트로
    검증한다.

    src/components/review-judgment-buttons.tsx: use client 컴포넌트. props는 lessonSlug·
    questionIndex. O(맞음)·△(맞았지만 불안)·X(틀림) 세 버튼을 낸다 — 각 min-h-11 min-w-11
    (44px+), btn/chip 계열 클래스 + tap-feedback 재사용, 라이트/다크 대응. useTransition으로
    recordReviewJudgment를 호출하고, 성공하면 눌린 판정을 로컬 state로 표시(예: 선택된 버튼
    강조 + "기록됨")하고 router.refresh()로 오답 모아보기 섹션을 동기화한다. 실패는
    review-done-button처럼 조용히 삼키지 않고 재시도 안내를 띄운다. △ 기준 문구("한 군데라도
    머뭇거렸으면 △")를 버튼 묶음 근처에 상주시킨다.

    src/app/review/page.tsx: `export const dynamic = "force-dynamic"` 신규 라우트. 조립 순서는
    홈과 동일 — hasUnlockCookie() 최우선 → readProgressRows → readReviewStates. getOrderedLessons에서
    완료(completedDateBySlug에 있음) + selfCheck.length>0 레슨으로 questionCountBySlug를 만들고
    selectReviewQuestions(completedDateBySlug, states, today, questionCountBySlug)로 문항 참조
    목록(최대 12)을 얻는다. 각 참조를 getLessonBySlug로 풀어 문항 텍스트(lesson.selfCheck[questionIndex])·
    레슨 제목·앵커 링크(`/lesson/${slug}#${SELF_CHECK_ANCHOR}`)·ReviewJudgmentButtons를
    카드로 렌더한다. 정답은 절대 렌더하지 않는다 — "레슨에서 확인" 앵커 링크만 준다(리서치
    절충: 두 번째 마크다운 렌더러 부채 회피). 그 아래 "오답 모아보기" 섹션: states 중 missedQ가
    비지 않은 레슨을 순회해 해당 인덱스 문항을 앵커 링크와 함께 모아 보여준다(getLessonBySlug/
    selfCheck 조회 실패는 조용히 제외 — 홈 behindRows와 같은 방어적 필터링). 완료 0건/쿠키 없음/
    후보 0건일 때 부드러운 빈 상태를 낸다. 크림 종이·각진 패널 관례(.panel/.chip)를 쓴다.

    scripts/check-route-rendering.mjs: EXPECTED_DYNAMIC_ROUTES에 '/review'를 추가한다
    (force-dynamic이라 prerender-manifest routes에 등장하면 안 됨 — /·/schedule과 동일 계약).

    src/components/site-nav.tsx: NAV_ITEMS에 "복습"({ label: "복습", href: "/review" })을
    추가한다 — "용어집"과 "일정표" 사이 등 자연스러운 위치. isActiveHref가 startsWith라
    별도 처리 불필요.
  </action>
  <verify>
    <automated>npm run build && node scripts/check-route-rendering.mjs && npm run lint</automated>
    <human-check>내장 브라우저를 아이패드 세로 크기로 /review에 열어 — 문항 카드가 뜨고, 각 문항에 O/△/X 세 버튼(44px+)이 있고, 정답은 보이지 않으며, "레슨에서 확인" 링크가 스스로 점검 섹션으로 이동하고, 오답 모아보기 섹션이 렌더되는지 확인한다. O/△/X를 눌러 기록 후 새로고침에도 상태가 유지되는지 본다.</human-check>
  </verify>
  <done>/review가 force-dynamic으로 렌더되고(check-route-rendering 통과), 완료 레슨 문항을 약한 것 우선으로 보여주며, O/△/X 판정이 missed_q에 저장되고, 오답 모아보기가 약한 문항을 모으며, 내비에 "복습" 링크가 있다. build·lint 통과.</done>
  <reversibility rating="reversible">신규 라우트·컴포넌트·액션 추가와 게이트/내비 한 줄 등록 — 되돌리기 쉬움.</reversibility>
</task>

<task type="auto">
  <name>Task 3: 9/29 버퍼 CTA — 홈 카드 + 일정표 → /review</name>
  <files>src/components/today-lesson-card.tsx, src/components/today-review-card.tsx, src/components/schedule-table.tsx</files>
  <read_first>
src/components/today-lesson-card.tsx의 buffer 분기(state === "buffer", heading "복습·정리일",
cta 미설정)와 celebration→tomorrow buffer 분기가 CTA를 붙일 자리다 — 이미 있는 cta = { href,
label } 형태를 그대로 쓴다. src/components/schedule-table.tsx의 ScheduleBufferRow(복습·정리일
텍스트 행)가 일정표 버퍼 행이다. src/components/today-review-card.tsx가 홈 복습 카드로,
세션 진입점 링크를 얹을 곳이다.
  </read_first>
  <action>
    src/components/today-lesson-card.tsx: buffer 상태에 cta = { href: "/review", label: "복습
    세션 열기" }를 설정한다(현재 문구만 있고 CTA 없음 — F8 해소). celebration 분기의
    tomorrow.kind === "buffer" 경우에도 같은 /review CTA를 붙일 수 있으면 붙인다(내일이
    복습일임을 알리며 세션으로 예열). 기존 CTA 렌더 경로/버튼 클래스를 그대로 재사용한다.

    src/components/schedule-table.tsx: ScheduleBufferRow에 /review로 가는 Link("복습하러 가기 →"
    류)를 추가한다 — 버퍼 행(9/27~9/29)에서 복습 세션으로 직행. 44px+ 터치 타깃, tap-feedback,
    라이트/다크 대응. 서버 렌더 순수 표현 컴포넌트 성격을 유지한다(데이터 조회 추가 금지).

    src/components/today-review-card.tsx: 카드 헤더/푸터에 "복습 세션 열기 →"로 /review를 여는
    Link를 추가한다 — 만기 복습이 있을 때 홈에서 세션으로 가는 상시 진입점. 기존 카드 톤·
    앵커 링크 스타일을 재사용한다. 축소형 배지(만기 0건) 분기는 건드리지 않는다.
  </action>
  <verify>
    <automated>npm run build && npm run lint</automated>
    <human-check>내장 브라우저를 아이패드 세로로 열어 — 홈 복습 카드에 "복습 세션 열기" 링크가 있고, 일정표의 9/29 복습·정리일 행에 /review 링크가 있으며, 두 링크가 실제로 /review로 이동하는지 확인한다.</human-check>
  </verify>
  <done>홈 카드(버퍼 상태 CTA + 복습 카드 진입점)와 일정표 버퍼 행에서 /review로 가는 CTA가 뜨고 이동한다. build·lint 통과, 기존 렌더 계약 회귀 없음.</done>
  <reversibility rating="reversible">표현 컴포넌트에 링크 추가만 — 되돌리기 쉬움.</reversibility>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>/review 복습 세션(O/△/X 3단 판정 + 오답 모아보기), selfCheck 문항 추출, 9/29 버퍼 CTA. 실행자는 내장 브라우저(아이패드 크기)로 1차 확인을 마친 상태.</what-built>
  <how-to-verify>
    아이패드 실기기(Safari)에서 배포/로컬 사이트를 열어 직접 확인해 주세요:
    1. 상단 내비 "복습" → /review 진입. 문항 카드가 읽기 좋게 뜨고 코드가 있으면 가로 스크롤 되는지.
    2. 각 문항의 O(맞음)·△(맞았지만 불안)·X(틀림) 버튼이 손가락으로 누르기 편한 크기(44px+)인지, 세로·가로 모드 모두.
    3. △·X를 누른 문항이 아래 "오답 모아보기"에 모이고, O를 누르면 빠지는지(새로고침 후).
    4. 문항 옆 "레슨에서 확인" 링크가 해당 레슨의 스스로 점검 섹션으로 정확히 이동하는지. 세션에는 정답이 안 보이는지.
    5. 홈 복습 카드의 "복습 세션 열기"와 일정표 9/29 행의 복습 링크가 /review로 가는지.
    6. 라이트/다크 모드 모두에서 O/△/X·패널·링크 색이 자연스러운지.
  </how-to-verify>
  <resume-signal>"승인"이라고 입력하거나 고칠 점을 알려 주세요</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → recordReviewJudgment 서버 액션 | 컴파일된 POST 엔드포인트. 캡처된 요청 재전송·위조 인자(lessonId·questionIndex·judgment)가 신뢰 경계를 넘는다 |
| MDX 원문 → velite parseSelfCheck | 빌드타임 콘텐츠 파싱. 저장소 소유 콘텐츠라 신뢰되나, 구조 어긋남이 조용히 통과하면 안 된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-w04-01 | Tampering | recordReviewJudgment 서버 액션 | medium | mitigate | hasUnlockCookie 재검증 → getLessonBySlug 존재 검증 → questionIndex 범위 검증 → judgment 화이트리스트('correct'\|'shaky'\|'wrong'), 그 다음에만 store 호출(completeReview 패턴 복제) |
| T-w04-02 | Denial of Service | velite parseSelfCheck | low | mitigate | 문항 !=2면 build throw — 잘못된 콘텐츠가 런타임 빈 세션으로 새는 대신 빌드에서 멈춘다 |
| T-w04-03 | Information Disclosure | /review 세션 | low | accept | 정답 미표시 설계라 세션에서 정답 노출 경로 자체가 없음 — 앵커 링크로만 레슨 페이지에서 확인 |
| T-w04-SC | Tampering | npm/pip/cargo installs | low | accept | 신규 패키지 설치 없음(rehype-slug·velite 등 모든 의존성 기존재) — 패키지 정당성 게이트 불필요 |
</threat_model>

<verification>
- `node node_modules/velite/bin/velite.js build --clean` — selfCheck 추출 성공, 문항 2개 아니면 throw (로컬 .velite stale 함정 회피: config 수정 후 반드시 --clean).
- `node scripts/check-review.mjs` — 선택 로직 경계 + selfCheck 개수 대조 통과.
- `npm run build` — /review 포함 전체 빌드 성공.
- `node scripts/check-route-rendering.mjs` — /review 동적 계약 통과, 기존 정적/동적 라우트 회귀 없음.
- `node scripts/check-lesson-structure.mjs` — 기존 F6/F7 구조 게이트 회귀 없음(MDX 미변경).
- `npm run lint` — 통과.
- 내장 브라우저 아이패드 크기 1차 확인 후 실기기 UAT 체크포인트.
</verification>

<success_criteria>
- /review가 force-dynamic으로 완료 레슨의 스스로 점검 문항을 약한 것 우선·12개 컷으로 보여준다.
- O/△/X 3버튼(44px+)이 판정을 missed_q에 저장한다(O 제거, △·X 추가).
- 세션은 정답을 렌더하지 않고 SELF_CHECK_ANCHOR 앵커 링크만 제공한다.
- 오답 모아보기가 missed_q 문항을 한곳에 모은다.
- 9/29 버퍼일 홈·일정표에서 /review CTA가 뜬다.
- velite가 selfCheck 2개를 추출하고 아니면 build를 실패시킨다.
- missed_q 마이그레이션 파일이 repo에 남고 idempotent하다.
- 브랜딩(KANT 금지)·아이패드 우선·한국어 UI 준수, 기존 게이트 회귀 없음.
</success_criteria>

<output>
Create `.planning/quick/260901-w04-review-o-x-3/260901-w04-SUMMARY.md` when done
</output>
