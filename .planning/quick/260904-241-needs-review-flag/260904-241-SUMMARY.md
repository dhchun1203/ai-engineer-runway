---
quick_id: 260904-241
slug: needs-review-flag
title: 레슨별 "더 공부하기" 표시 + 클로드 프롬프트 반영
status: complete
date: 2026-09-04
commits:
  - 874acf2
  - b64a2a9
  - 9626312
---

# 요약

레슨마다 "이 레슨은 더 공부해야 함"을 표시(토글)할 수 있게 했다. 표시하면:

1. 완료 영역의 토글이 켜진 상태로 남고(서버 저장 → 기기 간 유지).
2. 그 레슨의 "클로드에 물어보기" 버튼이 강조된다(btn → btn-action + 안내 문구).
3. 복사되는 질문 틀에 "# 이 레슨은 내가 더 공부해야 하는 부분이야" 블록이 붙어,
   선생님(클로드)이 막히는 지점부터 찾아 더 꼼꼼히 가르치도록 지시한다.

TIL 기능(quick 260902-0rz)을 그대로 미러링해 구현했다.

## 커밋(원자적)

- `874acf2` — Task 1: `lesson_note.needs_review` 컬럼 + `readLessonNote`/`saveLessonNeedsReview`
- `b64a2a9` — Task 2: `setLessonNeedsReviewAction`(신규 파일) + `/api/progress` `needsReview` 노출 + provider 타입
- `9626312` — Task 3: `LessonNeedsReview` 토글 + `LessonNeedsReviewSlot` + 페이지 배선 + `lesson-copy-prompt` 프롬프트 블록·버튼 강조

## 변경 파일

- `supabase/migrations/20260904160000_add_needs_review_to_lesson_note.sql` (신규, 검토용 사본)
- `src/lib/note-store.ts` — NoteRead에 needsReview, readLessonNote 확장, saveLessonNeedsReview 추가
- `src/app/lesson/[lessonId]/needs-review-actions.ts` (신규, 'use server')
- `src/app/api/progress/route.ts` — ProgressLesson.needsReview
- `src/components/progress-provider.tsx` — ProgressLesson.needsReview 타입
- `src/components/lesson-needs-review.tsx` (신규, 'use client')
- `src/components/progress-slots.tsx` — LessonNeedsReviewSlot
- `src/app/lesson/[lessonId]/page.tsx` — 슬롯 배선
- `src/components/lesson-copy-prompt.tsx` — buildPrompt 조건부 블록 + 버튼 강조

## 데이터 계층

- 라이브 Supabase(project `wxqteqiuihrgtxmztauc` / ai-news-briefing)의 `lesson_note`에
  `needs_review boolean not null default false` 컬럼을 관리 API로 적용 완료.
- til과 같은 한 행 공유(WR-01): needs_review만 upsert, body/til 보존.

## 검증

- `npm run build` — Compiled successfully.
- 게이트: 내 변경은 **신규 실패 0**. 남은 실패 G22 1건은 클린 master에서도 재현되는
  **선행 이슈**(북마크 기능의 rAF-스로틀 스크롤 리스너가 G22 허용목록에 미등재).
  별도 태스크로 분리(task_f9e3a355).
- 읽기 경로 end-to-end 확인: 언락 세션으로 `/api/progress?lesson=...` 응답에
  `needsReview` 필드가 흐르는 것 확인(curl, 시크릿 미노출).
- 쓰기 형태 확인: `lesson_note` upsert(액션과 동일 형태)로 needs_review=true 설정 →
  API가 true 반영 → 원복(false). body/til 보존 확인.
- 대화형 토글/강조/프롬프트 최종 확인은 사용자가 배포 사이트에서 아이패드로 수행.

## 후속(선택)

- 표시한 레슨 목록을 커리큘럼/홈에서 한눈에 보는 오버뷰(북마크 `/bookmarks`류)는
  범위에 넣지 않음. 필요 시 별도 quick으로.
