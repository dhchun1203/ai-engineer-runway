---
phase: quick-260901-s8b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pace.ts
  - scripts/check-pace.mjs
  - src/components/pace-status.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - round2-완료예측일
user_setup: []

estimate:
  tokens: 46000
  raw_tokens: 23000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ahead 또는 on-track이고 노이즈 가드를 통과하면 홈 페이스 패널에 '이 속도면 M월 D일 완주 예정' 한 줄이 보인다"
    - "behind일 때는 예측 줄이 어디에도 렌더되지 않는다 — 늦은 완주일을 얹어 낙담 신호로 반전시키지 않는다(리서치 2단 경고)"
    - "완료 0개·경과일 부족·남은 0개(전부 완료)일 때 예측 줄이 숨는다(속도 노이즈 가드)"
    - "ahead면 예측 줄이 accent 색, on-track이면 중성색이다(D-43 톤 규칙)"
    - "개강(9/30) 전에 끝나는 예측이면 '개강 전에 끝나요' 격려 한 조각이 붙는다"
    - "check-pace.mjs 기존 케이스가 하나도 깨지지 않고 computeProjection 신규 케이스가 모두 통과한다"
  artifacts:
    - "src/lib/pace.ts — computeProjection + Projection 타입 + 순수 날짜 헬퍼(추가, computePace/computeAheadDetail/catchUpDays 미수정)"
    - "scripts/check-pace.mjs — computeProjection 전용 케이스 블록(추가, 기존 케이스 미수정)"
    - "src/components/pace-status.tsx — projection prop + 조건부 예측 줄 렌더"
    - "src/app/page.tsx — computeProjection 호출·전달"
  key_links:
    - "page.tsx computeProjection(rows, completedIds, today, SCHEDULE_START) → PaceStatusPanel projection prop"
    - "pace-status.tsx: 예측 줄은 isAhead/on-track 분기에서만 문자열이 생기고 isBehind 분기에는 코드 경로 자체가 없다"
    - "computeProjection show 플래그(노이즈·카운트기반 behind 프록시) + 컴포넌트 pace.status 분기 = behind 미표시를 두 겹으로 보장"
    - "pace.ts 무-import 규칙 유지 — Date 전역(Date.UTC)만 쓰고 import 문은 추가하지 않는다"
---

<objective>
지금 학습 속도로 남은 레슨을 다 끝내는 예상 완주일을 계산해, 홈 페이스 패널에 "이 속도면 M월 D일 완주 예정" 한 줄을 ahead·on-track일 때만 보여준다. behind일 때는 예측일을 아예 렌더하지 않아, 늦은 완주일이 격려를 낙담으로 뒤집는 리서치 2단 경고를 구조로 차단한다.

Purpose: 리서치 2단 "완료 예측일" 항목(FINAL-REPORT 53~54행). 사전 단계에서 진행을 시각화하는 동기 장치이되, 경고("밀리면 낙담 신호로 반전 — ⑥과 짝")를 지키기 위해 표시 조건을 코드로 못박는다. ⑥(마지막 주 새 레슨 배정 금지, B안)은 이미 schedule.ts에 반영됨.
Output: computeProjection 순수 함수 + 전용 게이트 케이스 + PaceStatusPanel projection prop/렌더 + page.tsx 조립.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

@src/lib/pace.ts
@src/lib/schedule.ts
@src/components/pace-status.tsx
@src/app/page.tsx
@scripts/check-pace.mjs
</context>

<interface_context>
- `computePace(rows, minutesBySlug, completedIds, todayStr)` → `PaceResult { status: 'ahead'|'on-track'|'behind'; gapMinutes; missedSlugs }`. 절대 수정 금지 — check-pace.mjs가 반환 객체를 deepStrictEqual로 고정.
- `computeAheadDetail(...)` 는 선례: computePace를 안 건드리고 별도 순수 함수로 추가된 패턴. computeProjection도 같은 방식.
- pace.ts 규약: import 문 0개(Node가 트랜스파일러 없이 로드). 날짜 비교는 YYYY-MM-DD 문자열 사전순. 날짜 산술이 필요하면 schedule.ts처럼 `Date.UTC(...)` + `toISOString().slice(0,10)`만 쓴다(로컬 타임존 getter 금지, Pitfall 1). Date는 전역이라 import가 아니다.
- page.tsx에 이미 존재: `rows`(ScheduleRow[]), `completedIds`(Set<string>|null), `today`(YYYY-MM-DD), `SCHEDULE_START`, `COURSE_START_DATE`, `pace`(completedIds null이면 null), `ahead`. `PaceStatusPanel`은 `completedIds !== null && pace` 일 때만 렌더된다.
- pace-status.tsx에 이미 존재: `formatMonthDay(isoDate)` 헬퍼(재사용), isAhead/isBehind 분기, bodyLines 배열, 색 규칙 주석.
- schedule.ts export: `SCHEDULE_START = '2026-08-28'`, `COURSE_START_DATE = '2026-09-30'`.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pace.ts에 computeProjection 순수 함수 추가 + check-pace.mjs 전용 케이스</name>
  <files>src/lib/pace.ts, scripts/check-pace.mjs</files>
  <behavior>
    computeProjection(rows, completedIds, todayStr, scheduleStart) → Projection { show: boolean; projectedFinish: string | null; remainingCount: number }. 반환 필드는 정확히 이 셋.
    - 집계는 레슨 "개수" 기준(분 아님): assigned = lessonSlug !== null 행. total = assigned.length. completedCount = assigned 중 completedIds에 있는 수. remaining = total - completedCount.
    - 카운트기반 behind 프록시: pastDueIncomplete = assigned 중 date < todayStr 이면서 미완료인 수. > 0이면 show=false(밀린 상태에서 예측 줄을 숨기는 두 겹 방어의 함수 쪽).
    - elapsedDays = daysBetween(scheduleStart, todayStr) + 1 (포함 일수). today == scheduleStart면 1.
    - show=false(projectedFinish=null) 조건, 이 순서로 검사: completedCount < 1 → false(완료 0개는 항상 미표시). elapsedDays < 2 → false(시작 직후 속도 불안정). pastDueIncomplete > 0 → false(behind). remaining < 1 → false(전부 완료, 예측 불필요).
    - 위를 모두 통과하면 show=true. daysNeeded = Math.ceil((remaining * elapsedDays) / completedCount). projectedFinish = addDays(todayStr, daysNeeded). remainingCount 는 항상 remaining 값(show=false여도 계산된 remaining을 담는다).
    - Test 완료0개: start 2026-08-28, today 2026-08-30, rows 전부 오늘/미래(l1 8/30,l2 8/31,l3 9/01), completed 없음 → {show:false, projectedFinish:null, remainingCount:3}.
    - Test 경과일부족: start=today=2026-08-28, rows l1 8/28(완료)·l2 8/29(미완료), completed {l1} → elapsed 1 → {show:false, projectedFinish:null, remainingCount:1}.
    - Test 시작전: today 2026-08-27 < start 2026-08-28, rows l1 8/28(완료)·l2 8/29, completed {l1} → elapsed 0 → {show:false, projectedFinish:null, remainingCount:1}.
    - Test behind프록시: start 2026-08-28, today 2026-08-31, rows l1 8/28(미완료)·l2 8/29(완료)·l3 9/01, completed {l2} → pastDueIncomplete 1 → {show:false, projectedFinish:null, remainingCount:2}.
    - Test 정상예측A(딱 떨어짐): start 2026-08-28, today 2026-09-01, rows l1..l10 = 8/28~9/06 하루 1편, completed {l1..l5}(8/28~9/01) → total 10, completedCount 5, remaining 5, elapsed 5, daysNeeded ceil(5*5/5)=5 → {show:true, projectedFinish:'2026-09-06', remainingCount:5}.
    - Test 정상예측B(ceil 올림): start 2026-08-28, today 2026-08-30, rows l1 8/28,l2 8/29,l3 8/30,l4 8/31,l5 9/01, completed {l1,l2} → completedCount 2, remaining 3, elapsed 3, daysNeeded ceil(3*3/2)=ceil(4.5)=5 → {show:true, projectedFinish:'2026-09-04', remainingCount:3}.
    - Test 남은0개: start 2026-08-28, today 2026-08-31, rows l1 8/28,l2 8/29,l3 8/30 전부 완료 → remaining 0 → {show:false, projectedFinish:null, remainingCount:0}.
    - Test 개강후today: today 2026-10-15, rows l1 8/28(완료)·l2 9/28(미완료), completed {l1} → pastDueIncomplete 1(늦은 예측 안 냄) → {show:false, projectedFinish:null, remainingCount:1}.
    - Test 불변: 정상예측A 입력으로 호출 후 rows 배열·completedIds Set이 변형되지 않는다.
  </behavior>
  <action>
    pace.ts 파일 끝(catchUpDays 아래)에 추가한다. computePace/computeAheadDetail/catchUpDays 및 그 위 주석·타입은 한 글자도 건드리지 않는다 — check-pace.mjs가 그 반환을 deepStrictEqual로 고정하므로 필드를 늘리면 기존 케이스가 전부 깨진다.

    먼저 두 개의 파일 로컬 순수 날짜 헬퍼를 둔다(export 불필요): daysBetween(fromISO, toISO) 는 두 날짜를 각각 split('-').map(Number) 후 Date.UTC(y, m-1, d)로 만들어 밀리초 차를 86400000으로 나눈 정수(Math.round)를 돌려준다. addDays(baseISO, n) 은 base를 같은 방식으로 파싱해 Date.UTC(y, m-1, d + n)을 만들고 toISOString().slice(0,10)을 돌려준다. schedule.ts와 동일한 UTC 산술 패턴을 쓰는 이유(로컬 타임존 getter를 피해 Pitfall 1을 원천 차단)를 한 줄 주석으로 남긴다. import 문은 절대 추가하지 않는다 — Date는 전역이라 이 파일의 무-import 규약을 깨지 않는다.

    Projection 타입은 정확히 show(boolean)·projectedFinish(string|null)·remainingCount(number) 세 필드만 export 한다. computeProjection의 시그니처는 rows(computePace와 같은 구조적 타입: readonly { date; lessonSlug }[]), completedIds(ReadonlySet<string>), todayStr, scheduleStart. minutesBySlug는 받지 않는다 — 예측은 분이 아니라 레슨 개수로 계산한다. lessonSlug가 null인 버퍼 행은 어느 집계에도 넣지 않는다. 비-null 필터 이후 slug 접근은 computePace 선례대로 `r.lessonSlug as string`로 캐스팅한다. 입력 rows·completedIds는 변형하지 않는다(filter만 사용).

    behavior 블록의 show=false 검사 순서·수식을 그대로 구현한다. JSDoc에 (1) 이 함수는 minute 단위 behind를 모른다 — 권위 있는 판정은 computePace 소관이고, 여기 pastDueIncomplete는 카운트기반 프록시라는 점, (2) behind일 때 예측 줄을 숨기는 최종 보장은 함수 show 플래그와 호출부(page/컴포넌트) 양쪽에 있다는 점, (3) 완료 0개·경과일 <2는 시작 직후 속도 추정이 불안정해 숨긴다는 점을 명시한다. daysNeeded는 float rate를 거치지 않고 Math.ceil((remaining * elapsedDays) / completedCount) 정수식으로 계산한다.

    check-pace.mjs: 파일 하단 `if (failures.length > 0)` 블록 위에 `// --- computeProjection (완료 예측일) ---` 섹션을 신설하고 behavior의 9개 Test를 runCase로 추가한다. 상단 import 구조분해에 computeProjection을 더한다(`const { computePace, catchUpDays, computeAheadDetail, computeProjection } = await import(...)`). 기존 runCase 블록은 하나도 수정·삭제하지 않는다 — 새 함수 전용 케이스만 추가한다. 날짜 픽스처는 자기완결 더미값이라 SCHEDULE_START를 읽지 않는다.
  </action>
  <verify>
    <automated>node scripts/check-pace.mjs</automated>
  </verify>
  <done>node scripts/check-pace.mjs 가 기존 + 신규 computeProjection 케이스를 전부 통과(0 실패)한다. pace.ts에 import 문이 여전히 0개고 computePace/computeAheadDetail/catchUpDays 반환은 불변이다.</done>
</task>

<task type="auto">
  <name>Task 2: PaceStatusPanel에 예측 줄 렌더 + page.tsx 조립</name>
  <files>src/components/pace-status.tsx, src/app/page.tsx</files>
  <action>
    pace-status.tsx: import에 `Projection` 타입을 @/lib/pace에서, `COURSE_START_DATE`를 @/lib/schedule에서 추가로 가져온다(schedule.ts는 순수 모듈이라 서버 렌더 컴포넌트에서 안전). PaceStatusPanel props에 `projection?: Projection`을 더한다.

    예측 줄 문자열을 담을 지역 변수 projectionLine(string | null, 초기값 null)을 둔다. **isBehind 분기에는 projectionLine을 세팅하는 코드를 절대 넣지 않는다** — 이렇게 하면 밀린 상태에서 예측 줄이 렌더될 코드 경로 자체가 존재하지 않아 리서치 경고를 구조로 보장한다. isAhead 분기와 on-track(else) 분기에서만, `projection?.show && projection.projectedFinish` 일 때 projectionLine을 만든다: 기본 문구는 "이 속도면 {formatMonthDay(projection.projectedFinish)} 완주 예정"(기존 formatMonthDay 재사용). projection.projectedFinish < COURSE_START_DATE 이면(문자열 사전순, 개강 전 완주) 뒤에 " · 개강 전에 끝나요"를 덧붙인다.

    렌더: bodyLines 매핑 아래에, projectionLine 이 있으면 `<p data-pace-projection>` 하나를 낸다. 색은 톤 규칙(D-43)대로 isAhead면 accent(`text-body font-semibold text-accent dark:text-accent-dark`), 아니면(on-track) 중성(`text-body font-normal`). destructive 토큰은 계속 미사용. 컴포넌트 상단 주석 블록에 "예측 줄은 ahead/on-track 분기에서만 생성되고 behind 분기에는 코드 경로가 없다 — 늦은 완주일이 격려를 낙담으로 반전시키는 리서치 2단 경고를 구조로 차단한다"는 한 문단을 추가한다.

    page.tsx: import { computePace, computeAheadDetail } 줄에 computeProjection을 더하고, @/lib/schedule import에 이미 SCHEDULE_START·COURSE_START_DATE가 있는지 확인(있음). ahead 계산 근처(82~86행 블록)에 `const projection = completedIds ? computeProjection(rows, completedIds, today, SCHEDULE_START) : undefined;`를 추가한다. PaceStatusPanel 렌더(182행)에 `projection={projection}` prop을 전달한다. 다른 조립·게이트 순서는 건드리지 않는다.
  </action>
  <verify>
    <automated>npx tsc --noEmit && node scripts/check-pace.mjs && npm run lint</automated>
  </verify>
  <done>타입 체크·pace 게이트·lint 전부 통과한다. 실행자가 내장 브라우저로 홈(/)을 아이패드 크기 라이트/다크에서 열어, 완료를 몇 개 켠 상태에서 페이스 패널에 예측 줄이 뜨는지(ahead accent / on-track 중성) 눈으로 확인한다. behind 상태에서는 예측 줄이 없어야 한다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (없음, 신규) | 이 기능은 순수 함수 + 서버 렌더 표현 컴포넌트다. 새 네트워크·사용자 입력·패키지가 없다. 입력(rows·completedIds·today)은 이미 기존 화면에서 검증·표시되는 진도 파생값이다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-s8b-01 | Denial of Service | computeProjection daysNeeded 계산 | low | mitigate | completedCount < 1 가드가 나눗셈 분모 0을 원천 차단(0이면 show=false로 조기 반환). elapsedDays·remaining 가드도 그 앞에 위치. |
| T-s8b-02 | Information Disclosure | 예측 줄 문구 | low | accept | 예측일은 이미 화면에 있는 완료·일정 정보에서 파생될 뿐 새 정보를 노출하지 않는다. 1인용 사이트·공개 정적 일정. |
| T-s8b-SC | Tampering | 패키지 설치 | low | accept | npm/pip/cargo 설치 없음 — 새 의존성 0(pace.ts 무-import 규약, 컴포넌트는 기존 import만). 공급망 표면 증가 없음. |
</threat_model>

<verification>
- `node scripts/check-pace.mjs` — 기존 케이스 + computeProjection 신규 9케이스 전부 통과.
- `npx tsc --noEmit` — page.tsx ↔ PaceStatusPanel projection prop 타입 배선이 컴파일된다.
- `npm run lint` — 회귀 없음.
- pace.ts에 import 문이 0개로 유지된다(무-import 규약).
- 정적 게이트(design-tokens/brand/lesson-structure) 회귀 없음 — KANT 미언급, destructive 토큰 미사용.
- 내장 브라우저 육안: 홈 라이트/다크, ahead(accent)·on-track(중성) 예측 줄 표시, behind 미표시.
</verification>

<success_criteria>
- computeProjection이 show/projectedFinish/remainingCount 계약대로 동작하고 게이트로 고정된다.
- 홈 페이스 패널이 ahead·on-track일 때만 "이 속도면 M월 D일 완주 예정"을 보여주고 behind일 때는 렌더하지 않는다.
- computePace 계열 3함수 반환 불변, 기존 게이트 회귀 0.
</success_criteria>

<output>
Create `.planning/quick/260901-s8b-m-d/260901-s8b-SUMMARY.md` when done
</output>
