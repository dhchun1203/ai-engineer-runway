---
phase: 06-site-wide-design-polish
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - .planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md
  - .planning/phases/06-site-wide-design-polish/06-D97-MEASUREMENT.md
  - .planning/ui-reviews/06-d97/
autonomous: true
requirements: [SC1, SC2]
user_setup: []

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "01-UI-SPEC.md의 Typography 표가 D-R4K-4의 5종/3굵기 값으로 갱신되어, 저장소에 서로 모순되는 타입 계약 문서가 두 벌 남지 않는다 (D-R4K-4)"
    - "쿠키가 있는 실사용 상태의 홈이 768×1024에서 실제로 몇 퍼센트 비어 있는지가 스크린샷과 숫자로 기록되고, 그 숫자가 D-90 홈 작업의 착수/종료를 결정한다 (D-97)"
    - "쿠키가 있는 실사용 상태에서 D-day·오늘카드·페이스·밀린레슨(조건부)·전체진행률 5개 섹션이 렌더된다 — D-97의 빈 캔버스 판정은 이 상태를 기준으로 한다"
    - "쿠키가 없으면 진행률·페이스·밀린레슨 3개 섹션이 통째로 빠지고 D-day와 오늘카드만 남는다 — 04-UI-REVIEW의 55% 측정이 나온 상태가 이것임을 두 스크린샷이 증명한다"
    - "기존 자동 게이트 10종이 전부 통과한다 (D-94)"
  artifacts:
    - .planning/phases/06-site-wide-design-polish/06-D97-MEASUREMENT.md
    - ".planning/ui-reviews/06-d97/ (쿠키 있음/없음 홈 스크린샷 2장)"
  key_links:
    - "D-97 측정 결과 → 06-07의 홈 작업 착수 여부 — 이 숫자가 없으면 06-07은 실사용자가 겪지 않는 상태를 고치게 된다"
    - "01-UI-SPEC.md Typography 표 → 이후 모든 Phase의 크기 결정 근거 — 갱신하지 않으면 두 문서가 모순된 채 남는다"
  prohibitions:
    - statement: "01-UI-SPEC.md의 Design System·Spacing·Color·Copywriting 섹션은 개정하지 않는다 — 개정 대상은 Typography 표뿐이다 (06-UI-SPEC.md § 상위 계약 개정 고지)"
      status: active
      verification: flagged
    - statement: "새 색 토큰·새 폰트 파일을 도입하지 않는다 (D-R4K-2, D-R4K-3)"
      status: active
      verification: flagged
    - statement: "이 플랜은 src/ 아래 어떤 파일도 수정하지 않는다 — 같은 웨이브의 치환 플랜 3종과 파일이 겹치면 안 된다"
      status: active
      verification: flagged
    - statement: "사용자에게 보이는 어떤 문자열에도 교육기관명(K로 시작하는 4글자 브랜드)을 쓰지 않는다"
      status: active
      verification: flagged
---

<objective>
싸고 빠른 두 가지 정산. (1) `01-UI-SPEC.md`의 Typography 표를 D-R4K-4의 5종/3굵기로 갱신해 저장소에
모순되는 타입 계약이 두 벌 남지 않게 한다. (2) D-90(빈 캔버스)의 범위를 **계획이 아니라 실측으로**
확정한다 — 04-UI-REVIEW의 "홈 55% 빈 캔버스"는 잠금 해제 쿠키가 없는 세션에서 나온 값이고,
실사용자는 10년 만료 쿠키를 항상 갖고 있어 일상적인 홈은 이미 5개 섹션을 렌더한다.

Purpose: 06-07이 실사용자가 겪지도 않는 상태를 고치는 데 2일 타임박스를 쓰지 않게 막는다.
Output: 갱신된 상위 계약 문서 1개, 측정 기록 1개, 스크린샷 2장.

이 플랜은 `src/` 아래 파일을 하나도 건드리지 않으므로 같은 웨이브의 치환 플랜 3종
(06-03/06-04/06-05)과 완전히 병렬로 돈다.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-site-wide-design-polish/06-CONTEXT.md
@.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md
@.planning/phases/06-site-wide-design-polish/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 01-UI-SPEC.md Typography 표를 5종/3굵기로 개정 (D-R4K-4)</name>
  <files>.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md</files>
  <read_first>
    - `.planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md` — 전체를 읽되 특히
      `## Typography` 섹션(개정 대상)과 그 앞뒤 섹션 경계
    - `.planning/phases/06-site-wide-design-polish/06-UI-SPEC.md` § 상위 계약 개정 고지 +
      § Typography 역할별 표 — 이 표가 그대로 새 값이다
  </read_first>
  <action>
`01-UI-SPEC.md`의 `## Typography` 섹션만 06-UI-SPEC.md § Typography의 역할별 표로 교체한다:
display 30px/700(tracking -0.02em), heading 22px/700, subhead 17px/700, body 16px/400,
label 14px/400·600, inline code 15px(`0.9375rem` 절대값)/400. 각 역할의 유틸리티 클래스
(`text-display`, `text-heading`, `text-subhead`, `text-body`, `text-label`)와 line-height
(1.2 / 1.3 / 1.4 / 1.6 / 1.4)를 함께 적는다.

표 바로 위에 개정 고지 한 문단을 넣는다: 이 표가 Phase 6(D-R4K-4)에서 4종/2굵기를 대체했고,
원인은 종수가 아니라 `@tailwindcss/typography`의 선언되지 않은 런타임 이탈이었으며(04-UI-REVIEW
Pillar 4 = 2/4 실측), 근거 원문은 `06-UI-SPEC.md`와 `06-CONTEXT.md`의 D-R4K-4에 있다는 것.

`## Typography` 이외의 섹션(Design System, Spacing Scale, Color, Copywriting Contract 등)은
**한 줄도 바꾸지 않는다** — 06-UI-SPEC.md가 그 섹션들은 그대로 유효하다고 명시했다.

`## Spacing Scale`에 예외 항목이 있다면 Section Tape 셀의 폭 방향 24px 하한 예외
(06-UI-SPEC.md § Section Tape 계약 "터치 타깃 예외")를 그 예외 목록에 한 줄로만 덧붙인다 —
44px 규칙 본문은 바꾸지 않는다.
  </action>
  <verify>
    <automated>node scripts/check-brand.mjs &amp;&amp; grep -c 'text-display' .planning/phases/01-deployed-curriculum-skeleton/01-UI-SPEC.md</automated>
  </verify>
  <acceptance_criteria>
    - `01-UI-SPEC.md`의 Typography 표에 display/heading/subhead/body/label/inline code 6개 행이 있고
      각각 30/22/17/16/14/15px, 굵기 700/700/700/400/400·600/400을 명시한다
    - 굵기 종류가 400·600·700 3종이고 4종/2굵기라는 옛 문구가 남아 있지 않다
    - 개정 고지 문단이 D-R4K-4와 06-UI-SPEC.md를 근거로 인용한다
    - `git diff --stat` 결과 변경 파일이 `01-UI-SPEC.md` 하나뿐이다
    - `node scripts/check-brand.mjs`가 exit 0 (D-94)
  </acceptance_criteria>
  <done>저장소에 타입 계약 문서가 한 벌만 남고, 그 값이 코드의 `@theme` 토큰과 일치한다.</done>
</task>

<task type="auto">
  <name>Task 2: D-97 실측 — 쿠키 있는 홈의 빈 캔버스 비율을 숫자로 확정</name>
  <files>.planning/phases/06-site-wide-design-polish/06-D97-MEASUREMENT.md, .planning/ui-reviews/06-d97/</files>
  <read_first>
    - `src/app/page.tsx` — 특히 `completedIds` 3분기(쿠키 없으면 `PaceStatusPanel`,
      `BehindLessonsList`, `ProgressSummary` 3개 섹션이 통째로 렌더되지 않는 지점)
    - `src/app/unlock/route.ts` — `?key=` 검증 후 10년 만료 쿠키 발급(12, 22-30줄).
      Playwright에서 쿠키를 얻는 유일한 방법은 이 라우트를 실제로 방문하는 것이다
    - `scripts/e2e-typography.mjs`(06-01 산출물) — dev 서버 기동/대기/종료 + `chromium.launch()`
      부트스트랩을 그대로 복제할 원본
    - `.planning/phases/06-site-wide-design-polish/06-CONTEXT.md` D-97 — 판정 절차와 30% 기준선
  </read_first>
  <precondition>`.env.local`에 `UNLOCK_SECRET`이 설정되어 있어야 한다 — 쿠키 있는 상태를 만들 수 없으면 이 측정 자체가 불가능하다.</precondition>
  <action>
일회성 측정 스크립트를 임시로 작성해 실행한다(저장소에 커밋하지 않는 스크래치 스크립트여도 되고,
`scripts/`에 남기지 않는다 — 이 측정은 반복 게이트가 아니라 1회 판정이다).

절차:
1. dev 서버를 띄운다(`e2e-typography.mjs`의 부트스트랩 복제, 포트는 3212/3213과 겹치지 않게 3214).
2. Chromium 컨텍스트를 뷰포트 768×1024로 연다.
3. **쿠키 없는 상태**로 홈(`/`)을 열어 스크린샷을 `.planning/ui-reviews/06-d97/home-768-no-cookie.png`로
   저장한다(`fullPage: false` — 첫 화면만이 판정 대상이다).
4. 같은 컨텍스트에서 `/unlock?key=${process.env.UNLOCK_SECRET}`를 방문해 쿠키를 받는다.
   **키 값을 로그·파일명·스크린샷 어디에도 남기지 않는다.**
5. 다시 홈을 열어 `.planning/ui-reviews/06-d97/home-768-with-cookie.png`로 저장한다.
6. 각 상태에서 `page.evaluate()`로 **첫 화면 하단 빈 영역 비율**을 계산한다:
   `<main>`의 마지막 자식 요소의 `getBoundingClientRect().bottom`을 구하고,
   `(viewportHeight - min(bottom, viewportHeight)) / viewportHeight`를 백분율로 낸다.
   콘텐츠가 뷰포트를 넘으면 0%다.
7. 두 상태에서 렌더된 홈 섹션 목록도 함께 수집한다(D-day, 오늘 카드, 페이스, 밀린 레슨,
   전체 진행률 중 무엇이 존재하는지) — 쿠키 유무에 따른 3섹션 차이를 실제로 확인한다.

측정이 끝나면 `06-D97-MEASUREMENT.md`를 쓴다. 담을 것:
- 두 상태의 빈 영역 비율(%), 렌더된 섹션 목록, 스크린샷 경로
- **판정**: 쿠키 있는 상태의 빈 영역이 30% 미만이면 → "홈은 작업하지 않는다. D-90의 홈 범위를
  종료한다." 30% 이상이면 → "홈 재배치를 06-07에서 진행한다"와 함께 어떤 정보를 어디로 옮길지
  후보를 적는다(새 데이터·새 쿼리 금지, 이미 계산된 값만).
- `/curriculum`은 쿠키와 무관하게 `StepCard` 3장 그리드뿐이므로 **양쪽 상태 모두에서 빈 캔버스가
  유효하다** — 06-07의 `/curriculum` 작업은 이 측정 결과와 무관하게 진행된다는 것을 명시한다.
- 참고로 `/curriculum`도 쿠키 있는 상태로 768×1024 스크린샷 1장을 같은 디렉터리에 남긴다
  (`curriculum-768-with-cookie.png`) — 06-07이 무엇을 채울지 판단할 근거가 된다.

측정 후 dev 서버는 `finally`에서 반드시 종료한다.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-site-wide-design-polish/06-D97-MEASUREMENT.md &amp;&amp; ls .planning/ui-reviews/06-d97/ | wc -l &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ui-reviews/06-d97/`에 스크린샷 3장이 존재한다:
      `home-768-no-cookie.png`, `home-768-with-cookie.png`, `curriculum-768-with-cookie.png`
    - `06-D97-MEASUREMENT.md`가 두 홈 상태의 빈 영역 비율을 **숫자로** 기록한다(예: "쿠키 없음 54%,
      쿠키 있음 8%")
    - 그 문서에 "홈 작업함/안 함"의 **판정 한 줄**이 30% 기준선과 함께 명시되어 있다
    - 쿠키 있는 상태의 섹션 목록에 D-day·오늘 카드·전체 진행률이 포함되고, 쿠키 없는 상태의
      목록에는 진행률·페이스·밀린 레슨이 빠져 있다 — 두 상태의 차이가 실제로 확인된다
    - `UNLOCK_SECRET` 값이 `06-D97-MEASUREMENT.md`·파일명·스크린샷 어디에도 남아 있지 않다
    - `git status`에 새로 커밋될 `scripts/*.mjs` 파일이 없다(측정 스크립트는 남기지 않는다)
    - `node scripts/check-brand.mjs`가 exit 0 (D-94)
  </acceptance_criteria>
  <done>D-90의 홈 범위가 측정된 숫자로 열리거나 닫혔고, 그 판정이 문서에 남아 06-07이 참조할 수 있다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env.local`의 `UNLOCK_SECRET` → Playwright 브라우저 컨텍스트 | 측정을 위해 시크릿이 URL 쿼리로 한 번 오간다. 로컬 dev 서버 대상이며 값이 산출물에 남으면 안 된다 |

이 플랜은 `src/` 아래 어떤 파일도 수정하지 않는다 — 애플리케이션 표면 변화 0.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Information Disclosure | D-97 측정 스크립트의 `/unlock?key=` 방문 | medium | mitigate | 키를 `process.env.UNLOCK_SECRET`에서만 읽고 리터럴로 쓰지 않는다. 로그·파일명·측정 문서에 값을 출력하지 않는다. `unlock/route.ts`가 이미 리다이렉트 목적지에서 key를 제거하므로 스크린샷 URL 바에도 남지 않는다 |
| T-06-05 | Information Disclosure | `.planning/ui-reviews/06-d97/` 스크린샷 | low | accept | 홈 화면 스크린샷에는 학습 진행률만 보인다 — 1인 사용 개인 프로젝트이고 시크릿·자격증명이 화면에 렌더되지 않는다 |
</threat_model>

<verification>
1. `06-D97-MEASUREMENT.md`에 두 상태의 빈 영역 비율(%)과 판정이 기록되어 있다
2. 스크린샷 3장이 존재한다
3. `01-UI-SPEC.md` Typography 표가 5종/3굵기다
4. `node scripts/check-brand.mjs` exit 0
5. `git diff --name-only`에 `src/` 경로가 없다
</verification>

<success_criteria>
- 저장소에 모순되는 타입 계약 문서가 없다
- D-90의 홈 범위가 추측이 아니라 숫자로 확정되었다
- 같은 웨이브의 치환 플랜과 파일 충돌이 0건이다
</success_criteria>

<output>
Create `.planning/phases/06-site-wide-design-polish/06-02-SUMMARY.md` when done.
D-97 판정("홈 작업함/안 함")을 요약 첫 줄에 명시한다 — 06-07이 이 한 줄을 읽고 범위를 정한다.
</output>
