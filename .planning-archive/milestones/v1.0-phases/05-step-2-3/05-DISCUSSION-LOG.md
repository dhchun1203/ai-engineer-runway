# Phase 5: Step 2·3 콘텐츠와 프로젝트 가이드 - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-25
**Phase:** 5-step-2-3
**Areas discussed:** Step 3 개요 깊이 기준, 프로젝트 5종 가이드 형식, Step 2 실습 환경·게이트 확장, 집필·배포 리듬

**진행 방식:** 사용자가 영역 선택 질문에 `알아서 해줘`로 응답 → 4개 영역 전부 선택, 각 질문에서 권장안 자동 선택(auto mode). 사용자 대화형 응답 없음.

---

## Step 3 개요 깊이 기준

### Q1. 완료 판정 기준을 무엇으로 잡나?

| Option | Description | Selected |
|--------|-------------|----------|
| 알아듣기 (용어·용도·위치) | 용어를 정의하고, 언제 쓰는지 말하고, 전체 그림에서 위치를 안다 | ✓ |
| 설명하기 | 남에게 개념을 설명할 수 있는 수준 | |
| 따라 해보기 | 축소판이라도 직접 돌려 본 경험 | |

**Selection rationale:** CONT-04가 Step 3를 "개념·용어 중심 훑기"로 규정했고, ROADMAP 성공 기준 2가 "개강 후 학습에 필요한 용어를 미리 알 수 있다"로 이미 이 수준을 명시한다. STATE.md Blockers에 남아 있던 리서치 Gap("Step 3 rubric 미확정 → 범위 팽창 위험")을 닫는 결정. → **D-62**

### Q2. 개요 레슨에 실행 가능한 코드가 들어가나?

| Option | Description | Selected |
|--------|-------------|----------|
| 읽기용 스니펫만 | 개념을 보여주는 10~20줄 조각, 설치·실행 안내 없음 | ✓ |
| 전부 실행 가능 | Step 1·2와 동일하게 D-12 완결 코드 적용 | |
| 코드 완전 배제 | 산문·표만으로 개념 전달 | |

**Selection rationale:** RAG(벡터DB)·PEFT/LoRA(GPU)·n8n/LangGraph(별도 인스턴스)는 환경 구축만으로 90분 예산을 초과한다. 반대로 코드를 완전히 배제하면 개강 후 첫 노출이 벽이 된다. → **D-63**

### Q3. 90분을 어떻게 배분하나?

| Option | Description | Selected |
|--------|-------------|----------|
| 읽기 40 + 판단형 해보기 50 | 실행 대신 시나리오 판단·설계 선택 과제 | ✓ |
| 읽기 70 + 확인 문제 20 | 산문 중심, 짧은 자가 점검 | |
| Step 1과 동일 비율(읽기 30 + 실습 60) | D-49 비율을 90분에 축소 적용 | |

**Selection rationale:** D-49의 "글이 시간을 채우지 않는다" 원칙은 유지하되, 실행 실습이 불가능하므로 사고 과제가 그 자리를 대신한다. Step 1과 비율이 다르다는 점을 명시적으로 기록. → **D-64**

### Q4. 개요 레슨도 6단 헤딩을 그대로 쓰나?

| Option | Description | Selected |
|--------|-------------|----------|
| 6단 원문 유지, ④만 재해석 | ④ 실무 예제 = "실무에서 이게 어떻게 쓰이나" 사례 + 읽기용 스니펫 | ✓ |
| 개요 전용 4단 구조 | 짧은 형식에 맞춘 별도 템플릿 | |

**Selection rationale:** 자동 구조 게이트가 35편을 한 벌 규칙으로 검사할 수 있어야 한다. → **D-65**

---

## 프로젝트 5종 가이드 형식

### Q1. 6단 템플릿 유지 vs 프로젝트 전용 구조?

| Option | Description | Selected |
|--------|-------------|----------|
| 6단 유지, 각 단 내용만 재해석 | ④를 "사전 준비 체크리스트"로, ⑥을 "준비 완료 판정 기준"으로 | ✓ |
| 프로젝트 전용 구조 | 무엇을 만드나 / 준비물 / 체크리스트 3단 | |

**Selection rationale:** 전용 구조는 게이트를 두 벌로 갈라 자동 안전망(D-59)을 약화시킨다. 학습자 입장에서도 레슨 구조가 예측 가능하게 유지된다. → **D-66**

### Q2. "재현 아님"(CONT-05)의 경계를 어디에 긋나?

| Option | Description | Selected |
|--------|-------------|----------|
| 완성 코드·정답 아키텍처 금지 | 준비물·구조 스케치·용어까지만. "첫날에 안 막히게" | ✓ |
| 축소판 구현까지 허용 | 미니 버전을 한 번 만들어 보게 함 | |

**Selection rationale:** 본 과정 200~520시간짜리 팀 프로젝트를 60분 레슨이 앞질러 풀면 개강 후 학습이 무의미해진다. CONT-05 원문이 "(재현 아님)"을 괄호로 못 박았다. → **D-67**

### Q3. 해보기·단어 표를 프로젝트 가이드에도 유지하나?

| Option | Description | Selected |
|--------|-------------|----------|
| 유지 — 해보기는 "실제로 준비해 보기" | 계정 생성·키 발급·샘플 데이터 수집을 과제로 | ✓ |
| 프로젝트 가이드는 형식 예외 | 체크리스트만 두고 해보기 생략 | |

**Selection rationale:** 형식 예외를 만들면 게이트에 구멍이 생긴다. 60분 = 읽기 20 + 준비 실행 40. → **D-68, D-69**

---

## Step 2 실습 환경·게이트 확장

### Q1. 구 표준 파일럿 `2-3-react-components`를 어떻게 하나?

| Option | Description | Selected |
|--------|-------------|----------|
| 신표준으로 재작성 | 해보기·`<details>` 정답·단어 표 추가, 프론트매터 불변 | ✓ |
| 게이트 예외로 유지 | 스크립트에 슬러그 화이트리스트 추가 | |

**Selection rationale:** 게이트를 Step 2로 확대하는 순간 이 파일이 유일한 예외가 된다. 151줄 재작성 비용이 예외 유지 비용보다 싸다. → **D-70**

### Q2. 구조 게이트를 Step 2·3으로 확대하나?

| Option | Description | Selected |
|--------|-------------|----------|
| 확대 — step-1/2/3 전 디렉터리 순회 | `hasContent: true`인 파일만 검사하는 기존 규칙 유지 | ✓ |
| Step 1 전용 유지 | Step 2·3은 빌드·브랜드 게이트만 | |

**Selection rationale:** D-59가 사람 검토를 파일럿으로 제한한 대가로 만든 안전망인데, 24편에서는 Step 1의 10편보다 더 필요하다. → **D-71**

### Q3. 허용 코드 펜스 언어를 어디까지 확장하나?

| Option | Description | Selected |
|--------|-------------|----------|
| ts/tsx/js/jsx/json/html/css/yaml 추가 | 기존 python·sql·bash·powershell·text에 8종 추가, mermaid 제외 | ✓ |
| 제한 해제 | 언어 검사 자체를 제거 | |

**Selection rationale:** mermaid를 넣으면 D-48(마크다운만으로 도식)이 무너진다. 제한 해제는 오타 언어 태그를 놓친다. → **D-72**

### Q4. Step 2 예제를 어디서 실행하나?

| Option | Description | Selected |
|--------|-------------|----------|
| PC 로컬(VS Code + Node), 2-1만 브라우저 | D-55·D-56 원칙을 Node 생태계에 그대로 적용 | ✓ |
| 온라인 IDE(StackBlitz) 병기 | 아이패드에서도 실행 가능하게 | |

**Selection rationale:** 커리큘럼이 로컬 개발 환경을 전제하고 Step 1에서 이미 세팅을 마쳤다. "읽기는 아이패드, 실행은 PC" 분리는 Phase 4에서 이미 확립되어 Step 2에서 새로 생기는 제약이 아니다. → **D-73, D-75**

### Q5. Node·npm 설치는 어느 레슨이 담당하나?

| Option | Description | Selected |
|--------|-------------|----------|
| `2-3-typescript-setup` | 제목이 "TypeScript 개발 환경 구성"이라 자연스러움. 2-2는 브라우저만으로 완결 | ✓ |
| Step 1의 1-1 환경 세팅에 소급 추가 | Phase 4 레슨을 수정 | |

**Selection rationale:** Phase 4 레슨을 소급 수정하지 않는다(범위 밖). 2-2(HTML·CSS·JS)가 순서상 먼저이므로 별도 설치 없이 `.html` 파일을 브라우저로 여는 예제로 쓴다. → **D-74**

---

## 집필·배포 리듬

### Q1. 파일럿을 다시 받나?

| Option | Description | Selected |
|--------|-------------|----------|
| 대표 3편 묶어 1회 확인 | 심화 승계 1 + 개요 신규 1 + 프로젝트 신규 1을 한 배포로 | ✓ |
| 파일럿 없이 바로 병렬 | Phase 4 표준이 이미 승인됨 | |
| 형식별 3회 확인 | 개요·프로젝트를 각각 별도 확인 | |

**Selection rationale:** Step 2 심화는 이미 승인된 표준이라 단독 파일럿이 불필요하고, 신규 형식 둘(개요·프로젝트)은 검증이 필요하다. 3회로 쪼개면 리듬이 끊긴다. 파일럿 = `2-3-react-components`(재작성), `3-1-vector-search-basics`, `2-4-project-ai-shop-frontend`. → **D-76**

### Q2. 나머지 22편을 한 번에 배포하나, 나눠 배포하나?

| Option | Description | Selected |
|--------|-------------|----------|
| Step 2 배치 → Step 3 배치 | 10편 + 12편, 사이에 사람 승인 없음, 배포만 분리 | ✓ |
| 한 번에 22편 | D-52(Phase 4의 9편 일괄)의 연장 | |
| 모듈별 7배치 | 세밀한 분할 | |

**Selection rationale:** (a) 일정표가 slug 순서로 배정되므로 Step 2가 먼저 배포되면 진행 중인 사전학습이 끊기지 않는다, (b) 22편 동시 병렬은 실패 시 되돌릴 표면이 너무 크다, (c) Step 2 배치를 읽어 본 경험이 Step 3 집필에 반영된다. 모듈별 7배치는 오버헤드가 이득을 넘는다. → **D-77**

### Q3. 게이트 상수·카피·Making-of 마감 처리

| Option | Description | Selected |
|--------|-------------|----------|
| wave마다 상수 갱신, 카피 분기 유지, Making-of는 Wave 3 | 11 → 14 → 24 → 35. "준비 중" 코드는 안전망으로 남김 | ✓ |
| 마지막에 일괄 | 중간 wave는 게이트를 우회 | |

**Selection rationale:** 상수를 wave마다 맞추지 않으면 그 wave의 빌드가 실패한다 — 우회는 게이트를 무력화한다. "준비 중" 분기를 제거하면 나중에 레슨 추가 시 빈 화면이 나온다. → **D-78, D-79, D-80, D-81**

---

## Claude's Discretion

사용자가 전 영역을 위임(`알아서 해줘`)했으므로 아래는 계획·실행 단계에서도 재량으로 남긴다:

- Wave 2·3 안에서의 executor 분할 방식과 커밋 단위
- 각 레슨의 이모지 헤더·비유 소재·해보기 개수(2~3)·단어 표 행 수(5~8)
- Step 3 읽기용 스니펫의 언어·길이 선택
- Step 2 예제의 구체 라이브러리 버전·프로젝트 구조 (커리큘럼 스택 범위 안)
- 프로젝트 가이드 5편의 "복습 포인터" 대상 레슨 선정
- `check-manifest.mjs` Invariant 10 슬러그 배열 단순화 여부
- `check-lesson-structure.mjs` 확장 시 Step별 규칙 분기 필요 여부
- Wave 1 파일럿 3편의 집필 순서

## Deferred Ideas

- **Mermaid·SVG 다이어그램** — D-48·Pitfall 4에 막힘. 25편 완료 후 특정 레슨에서 필요하면 quick task로
- **온라인 IDE(StackBlitz·CodeSandbox) 대체 경로** — D-73으로 검토 후 미채택
- **Step 3 실습 환경 실제 구축 안내** — 90분 예산 초과로 D-63에서 배제. 개강 후 본 과정이 다룸
- **레슨 검색·북마크·퀴즈 엔진** — v2 CONV 범위
