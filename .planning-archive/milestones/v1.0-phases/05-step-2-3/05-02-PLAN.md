---
phase: 05-step-2-3
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/content/lessons/step-2/2-1-postgres-and-supabase.mdx
  - src/content/lessons/step-2/2-1-ai-data-modeling.mdx
autonomous: true
requirements: [CONT-05]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`/lesson/2-1-postgres-and-supabase`가 PostgreSQL·SQL 쿼리·Supabase 활용을 다루는 심화 본문을 렌더한다 — `.planning/curriculum.md` §2-1 첫 불릿 범위를 채운다 (CONT-05 표준 승계)"
    - "`/lesson/2-1-ai-data-modeling`이 AI 서비스에 필요한 데이터 구조 설계를 다루는 심화 본문을 렌더한다 — §2-1 둘째 불릿 범위를 채운다"
    - "두 레슨의 실무 예제가 브라우저 Supabase SQL 에디터에서 실행되는 완결 SQL이며, 실행 위치가 본문에 한 줄로 명시된다 — 2-1은 D-73의 유일한 PC 로컬 예외다 (D-73, D-12)"
    - "두 레슨의 연습 SQL이 전부 `practice` 스키마 안에서만 동작하고 `public.progress`를 읽거나 쓰지 않는다 — Phase 4 D-56 규칙을 그대로 승계한다 (D-73)"
    - "두 레슨 모두 `### 해보기` 2~3개 + 해보기 개수 + 2 이상의 접힌 `<details><summary>정답 보기</summary>` 블록 + `**이 레슨의 단어**` 표 5~8행을 갖고 `node scripts/check-lesson-structure.mjs`를 통과한다 (D-69, D-50, D-61)"
    - "두 레슨의 프론트매터에서 `hasContent: false → true` 한 줄 외에는 어떤 필드도 바뀌지 않았다 (D-13)"
    - statement: "실무 팁에 사용자 입력을 문자열 결합으로 쿼리에 넣지 말라는 경고가 남아 있다 — SQL 에디터 정적 쿼리라 인젝션이 해당되지 않더라도 습관을 잘못 심지 않는다 (T-05-06)"
      verification: backstop
    - statement: "읽기 산문이 150분을 채우지 않고, 늘어난 분량은 해보기·정답 블록·단어 표가 차지한다 (D-49 승계)"
      verification: backstop
  artifacts:
    - src/content/lessons/step-2/2-1-postgres-and-supabase.mdx
    - src/content/lessons/step-2/2-1-ai-data-modeling.mdx
  key_links:
    - "`practice` 스키마 격리 → `public.progress` 무결성 — 연습 SQL이 스키마 접두사 없이 `create table`을 쓰면 `public`에 만들어지고, 진도 테이블과 이름이 겹치면 실제 학습 데이터가 위험해진다"
    - "`.mdx` 본문 → Velite → `sql` 펜스 → Shiki — `sql`은 Phase 4의 1-4 레슨에서 이미 프로덕션 실증됐다. 새 언어를 시도할 이유가 없다"
    - "이 Plan의 2편 → Plan 07의 매니페스트 실측 — 여기서 `hasContent`를 켜지 않으면 Plan 07의 카운트가 어긋난다"
  prohibitions:
    - statement: "레슨 본문·코드 주석·메타데이터 어디에도 교육기관명(`scripts/check-brand.mjs`의 금지 문자열)을 노출해서는 안 된다 — 항상 'AI Engineer 교육과정' 또는 '본 과정'으로 표기한다 (D-02)"
      status: unverified
    - statement: "연습 SQL이 `public` 스키마에 테이블을 만들거나 `public.progress`를 대상으로 `select`·`insert`·`update`·`delete`·`drop`을 실행하게 해서는 안 된다 (D-56 승계, D-73)"
      status: unverified
    - statement: "프론트매터의 `hasContent` 외 어떤 필드도 바꿔서는 안 된다 (D-13, D-31)"
      status: unverified
    - statement: "다이어그램 플러그인·SVG·이미지·새 MDX 컴포넌트를 도입해서는 안 된다 — 구조는 표와 `A → B → C` 한 줄 도식으로만 표현한다 (D-48)"
      status: unverified
    - statement: "온라인 IDE(StackBlitz·CodeSandbox)나 브라우저 실행기를 대체 실습 경로로 안내해서는 안 된다 (D-73, CONTEXT.md Deferred)"
      status: unverified
    - statement: "레슨 본문에 실제 데이터베이스 접속 문자열이나 API 키 형태의 문자열을 적어서는 안 된다 — 항상 자리표시자나 환경변수 참조로만 보여준다 (T-05-01)"
      status: unverified
---

<objective>
Step 2 배치(D-77 Wave 2)의 2-1 모듈 2편을 집필한다 — PostgreSQL·Supabase 활용과 AI 서비스 데이터 구조 설계.

Purpose: 2-1은 Step 2 전체에서 **유일하게 PC 로컬이 아닌 브라우저 실행 예외**(D-73)다. Phase 4의 1-4 SQL 레슨이 확립한 `practice` 스키마 분리 패턴을 그대로 승계해, 학습자가 아이패드에서도 실습을 시도할 수 있으면서 진도 테이블을 건드리지 않게 한다.

Output: 심화 형식 레슨 2편, `hasContent: true` 전환 2건.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-step-2-3/05-CONTEXT.md
@.planning/phases/05-step-2-3/05-PATTERNS.md
@.planning/phases/05-step-2-3/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces

**Plan 02 (이 Plan)이 만드는 것**
- `src/content/lessons/step-2/2-1-postgres-and-supabase.mdx` — 본문 신규, `hasContent: false → true`
- `src/content/lessons/step-2/2-1-ai-data-modeling.mdx` — 본문 신규, `hasContent: false → true`
- 신규 연습 스키마 객체(레슨 본문 안에서만 존재하는 이름, 저장소 코드가 아님): `practice.ai_products`, `practice.ai_chat_sessions`, `practice.ai_messages`

**Phase 5 전체가 만들거나 바꾸는 것** — `05-01-PLAN.md` §Artifacts this phase produces 참조. 요약: 신규 `.mdx` 24편 + 재작성 1편, `scripts/check-lesson-structure.mjs`의 `LESSON_DIRS`(신규, `STEP1_DIR` 대체)와 13개짜리 `ALLOWED_FENCE_LANG_PREFIXES`, `scripts/check-manifest.mjs`의 `EXPECTED_HAS_CONTENT_COUNT` 3단계 변경(13 → 23 → 35), `src/app/lesson/[lessonId]/page.tsx` 빈 상태 문구, `01-UI-SPEC.md` Copywriting Contract 행, `docs/making-of.md` Phase 4·5 섹션, `.planning/phases/05-step-2-3/COVERAGE.md`.
</artifacts_produced>

<manifest_gate_note>
**이 wave 동안 `node scripts/check-manifest.mjs`는 의도적으로 red다.** 이 Plan이 `hasContent`를 2건 켜는 순간 실제 개수가 상수(Plan 01이 맞춰 둔 13)를 넘어서 Invariant 10이 실패한다. 상수는 Wave 2 마감 Plan 07이 실측으로 되돌린다(D-78). **이 Plan의 실행자는 `check-manifest`를 verify에서 제외하고, 실패를 보더라도 상수를 손대지 않는다** — 같은 wave의 Plan 03~06이 병렬로 같은 상수를 고치면 충돌한다.
</manifest_gate_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: `2-1-postgres-and-supabase` 집필 — PostgreSQL·SQL 쿼리·Supabase 활용</name>
  <files>src/content/lessons/step-2/2-1-postgres-and-supabase.mdx</files>
  <read_first>
    - `src/content/lessons/step-2/2-1-postgres-and-supabase.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "PostgreSQL과 Supabase 활용"`, `stepId: 2`, `moduleId: "2-1"`, `order: 1`, `depth: "심화"`, `estimatedMinutes: 150`, `hasContent: false`, `slug: "2-1-postgres-and-supabase"`)을 확인한다.
    - `src/content/lessons/step-1/1-4-sql-queries-and-joins.mdx` — Phase 4의 SQL 레슨. `practice` 스키마 분리(D-56)가 실제로 어떤 문구·SQL로 쓰였는지, 준비 블록이 어떻게 생겼는지의 기준 샘플. 전체를 읽는다.
    - `src/content/lessons/step-1/1-4-relational-db-basics.mdx` — 관계형 DB 기초를 어디까지 다뤘는지 확인한다. 이 레슨은 그 위에서 시작하므로 기초를 반복하지 않는다.
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md` — Plan 01에서 승인·확정된 심화 형식의 최종 골격과 사용자 수정 요청 반영 결과.
    - `.planning/curriculum.md` §Step 2 §2-1 — 불릿 2줄이 이 모듈의 범위 원천. 첫 불릿("PostgreSQL, SQL 쿼리, Supabase 활용")이 이 레슨 몫이다.
    - `.planning/phases/05-step-2-3/05-PATTERNS.md` §`src/content/lessons/step-2/*.mdx` — 심화 10편 공통 패턴과 Step 2 전용 추가 규칙(D-73/D-74/D-75).
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다.

`##` 레벨 헤딩은 아래 6개뿐이며 문자열과 순서를 그대로 쓴다: `## 1. 학습 목표`, `## 2. 왜 배우나`, `## 3. 개념 설명`, `## 4. 실무 예제`, `## 5. 실무 팁`, `## 6. 핵심 정리 및 스스로 점검`.

- **① 학습 목표** — "이 레슨을 마치면 다음을 할 수 있습니다." 뒤에 동사로 끝나는 관찰 가능한 결과 4개. Step 2는 심화이므로 "~할 수 있다"(구현·실행 능력) 형태로 쓴다.
- **② 왜 배우나** — 최대 4문장 1문단. 1-4에서 배운 SQL이 실제 서비스에서는 관리형 PostgreSQL 위에서 돌아간다는 연결, 그리고 Step 2 뒤쪽(2-4·2-5·2-6 프로젝트)이 전부 이 데이터베이스를 쓴다는 자리매김.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 비유 먼저 정의 뒤. 다룰 것: PostgreSQL이 서 있는 자리(파일이 아니라 서버로 도는 데이터베이스), Supabase = 관리형 Postgres + 자동 생성 API + 인증이라는 묶음, 스키마(schema, 테이블을 담는 폴더 같은 것)와 역할, 행 수준 보안(RLS, Row Level Security)의 개념 소개. 1-4에서 이미 다룬 테이블·행·열·기본키 정의는 한 줄 복습으로만 스치고 반복하지 않는다.
- **④ 실무 예제** — 이 레슨의 실행 위치는 **브라우저의 Supabase SQL 에디터**다. PC 로컬 설치가 아니라는 점을 한 줄로 명시한다(D-73 예외). 예제는 `sql` 펜스 2~3개로 나눈다: (a) `create schema if not exists practice;`로 시작하는 준비 블록과 `practice.ai_products` 테이블 생성(컬럼: id·name·price·category·created_at 정도), (b) 샘플 행 5개 `insert`, (c) `select` + `where` + `order by` + `limit` + 집계(`count`, `avg`) 조회. 모든 테이블 참조에 `practice.` 접두사를 반드시 붙인다. 준비 블록 앞에 "이 레슨의 모든 SQL은 `practice` 스키마 안에서만 실행합니다 — 이 사이트의 진도 데이터가 들어 있는 `public` 스키마는 건드리지 않습니다" 취지의 경고 한 줄을 넣는다. 마지막에 정리용 `drop schema practice cascade;`를 접힌 블록 또는 별도 안내로 제공한다.
- **④ 안의 `### 해보기` 2~3개** — SQL 에디터에서 직접 쿼리를 고쳐 돌려 보는 과제. 소재: 특정 카테고리만 필터해 평균 가격 구하기, 컬럼 하나를 추가(`alter table`)하고 값 채우기, `order by`를 바꿔 상위 3개만 보기. 각 해보기 아래 접힌 정답 블록(`<details>` → `<summary>정답 보기</summary>` → **빈 줄** → 정답 SQL과 예상 결과 → **빈 줄** → `</details>`).
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 반드시 포함할 것: 실제 애플리케이션 코드에서는 사용자 입력을 문자열로 이어 붙여 쿼리를 만들지 않는다는 경고(파라미터 바인딩을 쓴다), 테이블을 공개하기 전에 행 수준 보안을 켠다는 원칙, 무료 티어의 일시정지·용량 주의.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표(헤더 `| 단어 | 뜻 |`, 데이터 행 5~8개: 스키마, 관리형 데이터베이스, 행 수준 보안, 집계 함수, 마이그레이션, 커넥션 등) → `**스스로 점검**` 질문 2개 + 각각 접힌 정답 블록.

용어는 첫 등장 시 "한글(English, 한 줄 풀이)" 형태로 풀고 이후는 한글만 쓴다(D-50). 접속 문자열·키가 필요한 자리에는 자리표시자만 쓴다. OS별 차이가 없는 레슨이므로 PowerShell 명령은 필요 없다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build`가 exit 0이다.
    - `node scripts/check-lesson-structure.mjs`가 exit 0이고 오류 목록에 `2-1-postgres-and-supabase`가 나타나지 않는다.
    - `node scripts/check-brand.mjs`가 exit 0이다.
    - `git diff src/content/lessons/step-2/2-1-postgres-and-supabase.mdx`의 프론트매터 변경이 `hasContent: false` → `hasContent: true` 한 줄뿐이다.
    - `grep -c '^## ' <파일>`이 `6`이고 순서가 1→6이다.
    - `grep -c '^### 해보기$' <파일>`이 `2` 또는 `3`이다.
    - `grep -c '<details>'` · `grep -c '<summary>정답 보기</summary>'` · `grep -c '</details>'`가 서로 같고 `### 해보기` 개수 + 2 이상이다.
    - 모든 `<summary>정답 보기</summary>` 줄의 다음 줄이 빈 줄이고, 모든 `</details>` 줄의 이전 줄이 빈 줄이다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 그 아래 표 데이터 행이 5~8개다.
    - `grep -c '^```sql$' <파일>`이 2 이상이고, 파일 안 모든 여는 코드펜스의 언어 태그가 `sql` 하나뿐이다.
    - `grep -c 'create schema if not exists practice' <파일>`이 `1` 이상이다.
    - `grep -c 'practice\.' <파일>`이 `5` 이상이다 (모든 테이블 참조에 스키마 접두사가 붙었다).
    - `grep -ci 'public\.progress' <파일>`의 결과가 `1` 이하이고, 등장한다면 그 줄이 "건드리지 않는다"는 경고 문맥이다 — `select`·`insert`·`update`·`delete`·`drop` 뒤에 `public.progress`가 오는 줄이 0건이다.
    - `grep -cE 'create table [^p]' <파일>`가 `0`이다 (스키마 접두사 없는 테이블 생성이 없다).
    - `grep -ciE 'stackblitz|codesandbox' <파일>`가 `0`이다.
    - `grep -ciE 'kant' <파일>`가 `0`이다.
    - `grep -cE 'postgres(ql)?://[a-z]' <파일>`가 `0`이다 (실제 접속 문자열이 없다).
  </acceptance_criteria>
  <done>2-1 첫 레슨이 심화 형식으로 작성됐고 확대된 구조 게이트를 통과한다. 모든 연습 SQL이 `practice` 스키마 안에 갇혀 있다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: `2-1-ai-data-modeling` 집필 — AI 서비스 데이터 구조 설계</name>
  <files>src/content/lessons/step-2/2-1-ai-data-modeling.mdx</files>
  <read_first>
    - `src/content/lessons/step-2/2-1-ai-data-modeling.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "AI 서비스 데이터 구조 설계"`, `moduleId: "2-1"`, `order: 2`, `depth: "심화"`, `estimatedMinutes: 150`)을 확인한다.
    - `src/content/lessons/step-2/2-1-postgres-and-supabase.mdx` (Task 1 완료본) — 같은 모듈의 앞 레슨. `practice` 스키마 준비 블록의 문구와 경고를 여기서 그대로 가져와 일관되게 쓴다. 앞 레슨이 이미 설명한 스키마·RLS 개념을 반복하지 않는다.
    - `src/content/lessons/step-1/1-4-relational-db-basics.mdx` — 정규화·관계를 어디까지 다뤘는지 확인한다.
    - `.planning/curriculum.md` §Step 2 §2-1 둘째 불릿("AI 서비스에 필요한 데이터 구조 설계") — 이 레슨의 범위 원천.
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md` — 확정된 심화 형식 골격.
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩 6개는 Task 1과 동일한 문자열·순서.

- **① 학습 목표** — "~할 수 있다" 형태 4개. AI 서비스 특유의 테이블을 설계하고, 관계를 표현하고, 어떤 데이터를 정규화하고 어떤 것을 그대로 둘지 판단하는 능력.
- **② 왜 배우나** — 2-4·2-6 프로젝트에서 실제로 이 구조를 만든다는 자리매김. 데이터 구조를 잘못 잡으면 화면과 API를 다 고쳐야 한다는 이유.
- **③ 개념 설명** — `###` 소제목 3~4개. 다룰 것: 정규화와 비정규화의 트레이드오프(비유: 물건을 종류별 서랍에 나눠 넣기 vs 자주 쓰는 것만 앞에 꺼내 두기), 관계 표현(1:N, N:M과 연결 테이블), AI 서비스에서 자주 나오는 테이블 세 종류(대화 세션·메시지 로그, 프롬프트 버전, 임베딩 저장소 — 임베딩은 이름만 소개하고 Step 3에서 다룬다고 넘긴다), 인덱스가 왜 필요한지. `A → B → C` 한 줄 도식이나 표로 관계를 그린다(다이어그램 플러그인·이미지 없음, D-48).
- **④ 실무 예제** — 실행 위치는 앞 레슨과 같은 **브라우저 Supabase SQL 에디터**이며 한 줄로 명시한다. `sql` 펜스 2~3개: (a) `practice` 스키마에 `practice.ai_chat_sessions`(id·user_label·started_at)와 `practice.ai_messages`(id·session_id 외래키·role·content·created_at) 두 테이블 생성, (b) 세션 2개와 메시지 6개 `insert`, (c) 두 테이블을 `join`해 "세션별 메시지 수와 마지막 메시지 시각"을 뽑는 조회. 외래키(`references practice.ai_chat_sessions(id)`)와 인덱스(`create index`) 한 줄을 실제로 보여 준다. 모든 참조에 `practice.` 접두사를 붙이고, Task 1과 같은 `public` 불간섭 경고 한 줄을 넣는다. 정리용 `drop` 안내도 동일하게 제공한다.
- **④ 안의 `### 해보기` 2~3개** — 소재: 메시지 테이블에 토큰 수 컬럼을 추가하고 세션별 합계 구하기, N:M 관계가 필요한 상황(태그 등)을 연결 테이블로 만들어 보기, 인덱스를 만들기 전후로 `explain` 결과를 비교해 보기. 각각 접힌 정답 블록.
- **⑤ 실무 팁** — 불릿 3~4개. 반드시 포함: 사용자 입력을 문자열 결합으로 쿼리에 넣지 않는다는 경고, 대화 로그처럼 계속 쌓이는 테이블은 보관 기간을 처음부터 정한다, 개인정보가 들어갈 수 있는 컬럼은 설계 단계에서 표시해 둔다(Step 3의 LLMOps로 이어짐).
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표 5~8행(정규화, 외래키, 연결 테이블, 인덱스, 스키마 마이그레이션, 임베딩 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.

용어 표기·분량 기준은 Task 1과 동일하다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `2-1-ai-data-modeling`이 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`, `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며, `<summary>` 다음 줄과 `</details>` 이전 줄이 빈 줄이다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - 파일 안 모든 여는 코드펜스의 언어 태그가 `sql` 하나뿐이고 개수가 2 이상이다.
    - `grep -c 'practice\.ai_chat_sessions'`와 `grep -c 'practice\.ai_messages'`가 각각 `2` 이상이다.
    - `grep -ci 'references practice\.'`가 `1` 이상이다 (외래키가 스키마 접두사와 함께 선언됐다).
    - `grep -cE 'create (table|index) [^p]'`가 `0`이다.
    - `select`·`insert`·`update`·`delete`·`drop` 뒤에 `public.progress`가 오는 줄이 0건이다.
    - `grep -ciE 'kant'`가 `0`이고, `grep -cE 'postgres(ql)?://[a-z]'`가 `0`이다.
    - `node -e "const l=require('./.velite/lessons.json');const s=new Set(l.filter(x=>x.hasContent).map(x=>x.slug));process.exit(s.has('2-1-postgres-and-supabase')&&s.has('2-1-ai-data-modeling')?0:1)"`가 exit 0이다.
  </acceptance_criteria>
  <done>2-1 모듈 2편이 모두 심화 형식으로 작성됐고 구조 게이트를 통과한다. 두 레슨의 연습 SQL이 같은 `practice` 스키마 규칙을 공유한다. 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레슨 본문 → 공개 프로덕션 URL | 본문 문자열이 그대로 인터넷에 노출된다 |
| 레슨 SQL → 학습자의 실제 Supabase 프로젝트 | 학습자가 복사해 자기 프로젝트의 SQL 에디터에서 실행한다. 이 사이트의 진도 데이터도 같은 프로젝트에 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | 레슨 본문 — 교육기관명·이메일·접속 문자열 | high | mitigate | `node scripts/check-brand.mjs` 상시 검사 + acceptance criteria의 파일 단위 grep 0건(대소문자 무시) + 접속 문자열 정규식 0건 |
| T-05-06 | Tampering | 연습 SQL이 `public.progress`를 손상 | high | mitigate | 모든 테이블 참조에 `practice.` 접두사 강제, 스키마 접두사 없는 `create table`/`create index` 0건을 grep으로 확인, 본문 상단 경고 한 줄. Phase 4 D-56 규칙 승계 |
| T-05-07 | Tampering | 문자열 결합 쿼리 습관을 가르침 | medium | mitigate | ⑤ 실무 팁에 파라미터 바인딩 경고를 필수 항목으로 넣는다. SQL 에디터 정적 쿼리 자체는 인젝션 대상이 아니지만 습관이 2-5 백엔드 레슨으로 이어진다 |
| T-05-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 저장소에 패키지를 설치하지 않고, 레슨도 npm 패키지를 안내하지 않는다(브라우저 SQL 에디터만 쓴다). RESEARCH.md `## Package Legitimacy Audit`의 `[SLOP]` 판정 0건 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

- **CONT-05 edge 프로브가 `unclassified → unresolved`로 남아 있다** (SPEC.md 부재, spec-less fallback §A). 이 Plan의 두 레슨은 프로젝트 가이드가 아니므로 "재현 아님" 경계가 직접 걸리지는 않지만, 대응되는 실패 모드(`hasContent: true`인데 6단이 덜 찬 레슨)는 `check-lesson-structure.mjs` L1·L2·L5와 Velite 스키마·`npm run build`가 막는다. 프로즈 내용 품질 자체는 여전히 자동 검증 불가다.
- **Open Question 3 — `practice` 스키마 네이밍:** D-73이 D-56을 승계한다고 했으나 Phase 4의 구체 테이블명(`practice.students`)은 AI 서비스 맥락에 맞지 않는다. 이 Plan은 **스키마 접두사 규칙만 승계하고 테이블명은 주제에 맞게 새로 짓는다**(`practice.ai_products`, `practice.ai_chat_sessions`, `practice.ai_messages`) — Claude's Discretion 범위로 처리했고 사용자 확인을 따로 받지 않는다.
- **Phase 4 UAT 1번(SQL 레슨 2편 Supabase 실제 실행 확인)이 미해결이다.** 이 Plan의 SQL 예제도 같은 이유로 실행자·게이트가 자격증명 없이 실행할 수 없다. 게이트는 문법이 아니라 구조만 검사하므로, **SQL이 실제로 도는지는 이 Plan에서 자동으로 증명되지 않는다.** Phase 5 마감(Plan 13)의 `<verify><human-check>`가 이 항목을 사람 확인으로 넘긴다.
- **`check-manifest.mjs`는 이 wave 동안 의도적으로 red다** — `<manifest_gate_note>` 참조. 상수를 여기서 고치지 않는다.
</flagged_assumptions>

<verification>
1. `npm run build`
2. `node scripts/check-lesson-structure.mjs` — 두 레슨 모두 오류 목록에 없음
3. `node scripts/check-brand.mjs`
4. `node scripts/check-manifest.mjs`는 **실행하지 않는다** (이 wave 동안 의도적 red, Plan 07이 되돌린다)
</verification>

<success_criteria>
- 2-1 모듈 2편이 심화 형식(6단 × eli5 × 해보기 × 단어 표)으로 작성됐다
- 두 레슨의 실행 위치가 브라우저 Supabase SQL 에디터로 명시됐다 (D-73 예외)
- 모든 연습 SQL이 `practice` 스키마 안에서만 동작하고 진도 테이블을 건드리지 않는다
- 확대된 구조 게이트와 브랜드 게이트를 통과한다
</success_criteria>

<output>
Create `.planning/phases/05-step-2-3/05-02-SUMMARY.md` when done.

담을 것: 두 레슨에서 실제로 쓴 `practice` 테이블 이름과 컬럼(2-6 프로젝트 가이드가 참조할 수 있다), `sql` 펜스 개수, 확정된 해보기 개수, 게이트 통과 시점의 `hasContent: true` 총 개수(Plan 07이 실측과 대조한다).
</output>
