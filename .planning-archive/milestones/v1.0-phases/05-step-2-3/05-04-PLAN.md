---
phase: 05-step-2-3
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/content/lessons/step-2/2-5-express-rest-api.mdx
  - src/content/lessons/step-2/2-5-auth-and-prisma.mdx
autonomous: true
requirements: [CONT-05]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`/lesson/2-5-express-rest-api`가 Express 기반 RESTful API 구현을 다루는 심화 본문을 렌더한다 — `.planning/curriculum.md` §2-5 첫 불릿 범위를 채운다"
    - "`/lesson/2-5-auth-and-prisma`가 인증·인가와 Prisma ORM을 다루는 심화 본문을 렌더한다 — §2-5 둘째 불릿 범위를 채운다"
    - "두 레슨의 실행 위치가 **PC 로컬 VS Code**로 본문에 한 줄 명시되고, 설치 명령이 `powershell` 펜스로 주어지며 macOS 차이는 산문 한 줄로 병기된다 (D-73, D-75, D-12)"
    - "`2-5-auth-and-prisma`가 Prisma를 `prisma@^7`과 `@prisma/client@^7`로 **메이저 고정**해 안내한다 — 버전 없는 설치나 `latest` 태그 안내가 본문에 0건이다 (RESEARCH Pitfall 6: CLI latest 태그가 현재 후보판을 가리킨다)"
    - "비밀번호가 `bcryptjs` 해시를 거쳐 저장되는 예제만 등장하고 평문 저장·평문 비교 코드가 0건이다 (T-05-10)"
    - "JWT 시크릿과 데이터베이스 접속 정보가 항상 `process.env` 참조로만 등장하고 리터럴 값이 0건이다 (T-05-11)"
    - "두 레슨 모두 `### 해보기` 2~3개 + 해보기 개수 + 2 이상의 접힌 정답 블록 + `**이 레슨의 단어**` 표 5~8행을 갖고 `node scripts/check-lesson-structure.mjs`를 통과한다 (D-69)"
    - "두 레슨의 프론트매터에서 `hasContent: false → true` 한 줄 외에는 어떤 필드도 바뀌지 않았다 (D-13)"
    - statement: "예제가 외부 데이터베이스 서버 없이 학습자 PC에서 그대로 끝까지 돌아간다 — 접속 실패로 실습이 중단되지 않는다"
      verification: backstop
    - statement: "읽기 산문이 150분을 채우지 않고, 늘어난 분량은 해보기·정답 블록·단어 표가 차지한다 (D-49 승계)"
      verification: backstop
  artifacts:
    - src/content/lessons/step-2/2-5-express-rest-api.mdx
    - src/content/lessons/step-2/2-5-auth-and-prisma.mdx
  key_links:
    - "Plan 01 Task 3 체크포인트 항목 9(패키지 정당성 확인) → 이 Plan — `prisma`·`@prisma/client`가 `[SUS]`(too-new 신호) 판정을 받았으므로, 사용자 확인 전에는 이 Plan을 실행하지 않는다"
    - "`prisma@^7` 고정 → 학습자의 `npm install` — 고정을 빼면 CLI 후보판이 설치되어 안정판 `@prisma/client@7`과 API가 어긋난다"
    - "`process.env.JWT_SECRET` 패턴 → 학습자가 나중에 쓰는 실제 서비스 — 예제에 리터럴 시크릿을 한 번이라도 보이면 그대로 재생산된다"
    - "이 Plan의 2편 → `2-6-project-ai-shop-backend`(Plan 05) 복습 포인터 — 프로젝트 가이드가 이 두 slug를 가리킨다"
  prohibitions:
    - statement: "레슨 본문·코드 주석·메타데이터 어디에도 교육기관명(`scripts/check-brand.mjs`의 금지 문자열)을 노출해서는 안 된다 (D-02)"
      status: unverified
    - statement: "비밀번호를 평문으로 저장하거나 평문끼리 비교하는 예제를 실어서는 안 된다 — 반드시 `bcryptjs` 해시를 거친다 (T-05-10)"
      status: unverified
    - statement: "JWT 시크릿·데이터베이스 접속 문자열·API 키를 코드 리터럴로 적어서는 안 된다 — 항상 `process.env` 참조로만 보여준다 (T-05-11, Phase 4 D-54 승계)"
      status: unverified
    - statement: "Prisma를 버전 고정 없이 안내하거나 후보판 태그를 안내해서는 안 된다 — 안정 라인 `^7`을 명시한다 (RESEARCH Pitfall 6)"
      status: unverified
    - statement: "커스텀 JWT 서명이나 커스텀 해싱을 직접 구현하는 예제를 실어서는 안 된다 — 표준 라이브러리를 쓴다 (RESEARCH Don't Hand-Roll)"
      status: unverified
    - statement: "프론트매터의 `hasContent` 외 어떤 필드도 바꿔서는 안 된다 (D-13, D-31)"
      status: unverified
    - statement: "온라인 IDE(StackBlitz·CodeSandbox)를 대체 실습 경로로 안내해서는 안 된다 (D-73, CONTEXT.md Deferred)"
      status: unverified
---

<objective>
Step 2 배치의 2-5 모듈 2편을 집필한다 — Express 기반 RESTful API 구현과 인증·인가·Prisma ORM.

Purpose: Step 2에서 보안 습관이 가장 크게 걸리는 두 편이다. 비밀번호 해싱·JWT 시크릿·접속 정보를 다루는 예제가 그대로 학습자의 실제 서비스로 재생산되므로, 표준 라이브러리와 환경변수 패턴만 보여 준다. Prisma는 현재 CLI 최신 태그가 정식판이 아닌 후보판을 가리키므로 안정 라인을 명시 고정해 안내한다.

Output: 심화 형식 레슨 2편, `hasContent: true` 전환 2건.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-step-2-3/05-CONTEXT.md
@.planning/phases/05-step-2-3/05-RESEARCH.md
@.planning/phases/05-step-2-3/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces

**Plan 04 (이 Plan)이 만드는 것**
- `src/content/lessons/step-2/2-5-express-rest-api.mdx` — 본문 신규, `hasContent: false → true`
- `src/content/lessons/step-2/2-5-auth-and-prisma.mdx` — 본문 신규, `hasContent: false → true`
- 레슨 본문 안에서만 존재하는 실습 파일·심볼 이름(저장소 파일 아님): `src/server.ts`, `src/auth.ts`, `prisma/schema.prisma`, `.env.example`, 모델 `User`

**Phase 5 전체가 만들거나 바꾸는 것** — `05-01-PLAN.md` §Artifacts this phase produces 참조.
</artifacts_produced>

<manifest_gate_note>
**이 wave 동안 `node scripts/check-manifest.mjs`는 의도적으로 red다.** `hasContent`를 2건 켜면 실제 개수가 Plan 01이 맞춰 둔 상수를 넘어 Invariant 10이 실패한다. 상수는 Wave 2 마감 Plan 07이 실측으로 되돌린다(D-78). **실행자는 `check-manifest`를 verify에서 제외하고 상수를 손대지 않는다.**
</manifest_gate_note>

<package_gate_note>
**착수 전 확인:** `05-01-SUMMARY.md`에 Plan 01 Task 3 체크포인트 항목 9(패키지 정당성)의 결과가 기록되어 있어야 한다 — `prisma`와 `@prisma/client`가 `[SUS]`(too-new 신호) 판정이라 사람 확인이 선행 조건이다. 확인 기록이 없으면 이 Plan을 실행하지 않고 중단한다. 판정 근거는 "최근 배포 시각"일 뿐이며 주간 다운로드 1,560만~1,670만 회에 공식 `prisma` 조직 저장소가 뚜렷해 `[SLOP]` 신호는 0건이다.
</package_gate_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: `2-5-express-rest-api` 집필 — Express 기반 RESTful API 구현</name>
  <precondition>`.planning/phases/05-step-2-3/05-01-SUMMARY.md`가 존재하고 그 안에 Plan 01 Task 3 체크포인트 항목 9(패키지 정당성 확인) 결과가 기록되어 있다. 없으면 중단하고 사용자에게 알린다.</precondition>
  <files>src/content/lessons/step-2/2-5-express-rest-api.mdx</files>
  <read_first>
    - `src/content/lessons/step-2/2-5-express-rest-api.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "Express RESTful API 구현"`, `stepId: 2`, `moduleId: "2-5"`, `order: 1`, `depth: "심화"`, `estimatedMinutes: 150`, `hasContent: false`, `slug: "2-5-express-rest-api"`)을 확인한다.
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md` — 확정된 심화 형식 골격, 사용자 수정 요청 반영 결과, 신규 펜스 언어 실증 결과(`typescript`·`json` 사용 가능 여부), 체크포인트 항목 9 결과.
    - `.planning/curriculum.md` §Step 2 §2-5 — 불릿 2줄. 첫 불릿("Express 기반 RESTful API 구현")이 이 레슨 몫이다.
    - `.planning/phases/05-step-2-3/05-RESEARCH.md` §Standard Stack §학습자 로컬 환경 + §Package Legitimacy Audit — 안내해도 되는 정확한 패키지명(`express`, `zod`, `cors`, `dotenv`)과 각각의 판정.
    - `.planning/phases/05-step-2-3/05-PATTERNS.md` §`src/content/lessons/step-2/*.mdx` — Step 2 전용 규칙(D-73 실행 위치 명시, D-75 OS 병기).
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩은 아래 6개뿐이며 문자열과 순서를 그대로 쓴다: `## 1. 학습 목표`, `## 2. 왜 배우나`, `## 3. 개념 설명`, `## 4. 실무 예제`, `## 5. 실무 팁`, `## 6. 핵심 정리 및 스스로 점검`.

- **① 학습 목표** — "~할 수 있다" 형태 4개. 자원 기준으로 엔드포인트를 설계하고, Express로 라우터·미들웨어를 붙이고, 요청 본문을 검증하고, 상태 코드를 올바르게 돌려주는 능력.
- **② 왜 배우나** — 최대 4문장. 2-2·2-3에서 만든 화면이 데이터를 어디서 받아 오는지가 여기서 결정된다는 연결, 그리고 2-6 프로젝트가 이 구조 위에 올라간다는 자리매김.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 비유 먼저 정의 뒤. 다룰 것: 서버가 하는 일(주문 창구 비유), HTTP 메서드와 상태 코드 표(GET/POST/PUT/PATCH/DELETE × 200/201/400/401/404/500), REST 자원 설계 원칙(동사가 아니라 명사로 URL을 만든다), 미들웨어(요청이 손님이라면 미들웨어는 문 앞에서 차례로 확인하는 직원들). 요청 흐름은 `요청 → 미들웨어(로그·CORS·본문 파싱) → 라우터 → 핸들러 → 응답` 한 줄 도식으로 그린다(D-48).
- **④ 실무 예제** — **실행: PC 로컬 VS Code**를 한 줄로 명시한다(D-73). `powershell` 펜스에 프로젝트 준비 명령을 준다: `npm init -y`, `npm install express@^5 zod cors`, `npm install -D typescript tsx @types/node @types/express @types/cors`. macOS 차이는 없으므로 병기하지 않고, Node가 아직 없다면 `2-3-typescript-setup` 레슨을 먼저 보라는 포인터 한 줄을 둔다(D-74·D-75). 그다음 `typescript` 펜스 하나에 `src/server.ts` 완결 코드를 넣는다 — 첫 줄은 파일명 주석. 내용: 메모리 배열에 상품 5개, `GET /products`(전체·쿼리로 카테고리 필터), `GET /products/:id`(없으면 404), `POST /products`(`zod` 스키마로 본문 검증, 실패 시 400에 오류 목록, 성공 시 201에 생성된 자원), `cors()`와 `express.json()` 미들웨어, 포트는 `process.env.PORT` 없으면 3000. 그대로 복사해 붙이면 돌아가야 한다. 실행은 `powershell` 펜스에 `npx tsx src/server.ts` 한 줄. 호출 확인은 `powershell` 펜스에 `Invoke-RestMethod` 예시 2줄(GET 하나, POST 하나)을 주고, macOS·리눅스는 `curl`을 쓰면 된다는 산문 한 줄을 병기한다(D-75). 응답 예시는 `json` 펜스 하나로 보여 준다.
- **④ 안의 `### 해보기` 2~3개** — 소재: `DELETE /products/:id`를 추가하고 없는 id에 404를 돌려주기, `zod` 스키마에 가격 최솟값 제약을 넣고 400 응답 확인하기, 요청 시간을 재는 미들웨어를 하나 추가해 콘솔에 남기기. 각 해보기 아래 접힌 정답 블록(`<details>` → `<summary>정답 보기</summary>` → **빈 줄** → 정답 코드와 예상 응답 → **빈 줄** → `</details>`).
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 반드시 포함: 검증을 라우터 안에 흩뿌리지 말고 스키마 한 곳에 모은다, 오류 응답 형식을 처음부터 하나로 정한다, 포트·비밀 값은 코드가 아니라 환경변수에서 읽는다.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표 5~8행(엔드포인트, 미들웨어, 상태 코드, 라우터, 직렬화, CORS, 스키마 검증 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.

용어는 첫 등장 시 "한글(English, 한 줄 풀이)" 형태로 풀고 이후는 한글만 쓴다(D-50). 데이터베이스 연결은 이 레슨에서 다루지 않는다 — 메모리 배열로 끝내고 다음 레슨(2-5-auth-and-prisma)이 저장을 맡는다고 한 줄로 넘긴다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `2-5-express-rest-api`가 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent: false` → `hasContent: true` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`이고 순서가 1→6이며 `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며, `<summary>` 다음 줄과 `</details>` 이전 줄이 빈 줄이다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - 파일 안 모든 여는 코드펜스의 언어 태그가 `typescript`·`powershell`·`json` 셋 중 하나다.
    - `grep -c 'npm install express@\^5'`가 `1` 이상이다 (메이저 고정 설치 안내가 존재한다).
    - `grep -c 'src/server.ts'`가 `2` 이상이고, `typescript` 펜스 안에 `app.get(`·`app.post(`·`express.json()`·`z.object(`가 모두 등장한다.
    - `grep -c 'Invoke-RestMethod'`가 `1` 이상이다 (D-75 — Windows 기준 호출 예시).
    - `grep -ci '실행: PC 로컬'`가 `1` 이상이다 (D-73 — 실행 위치 명시).
    - `grep -cE '(secret|SECRET|password|PASSWORD)\s*[:=]\s*["'"'"'][^"'"'"']'`가 `0`이다 (리터럴 비밀 값이 없다).
    - `grep -ciE 'stackblitz|codesandbox'`가 `0`이고 `grep -ciE 'kant'`가 `0`이다.
    - `node -e "const l=require('./.velite/lessons.json');const p=l.find(x=>x.slug==='2-5-express-rest-api');process.exit(/language-typescript/.test(p.code)?0:1)"`가 exit 0이다.
  </acceptance_criteria>
  <done>2-5 첫 레슨이 심화 형식으로 작성됐고, 복사해 붙이면 도는 Express 서버 예제와 Windows 기준 호출 예시를 담는다. 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: `2-5-auth-and-prisma` 집필 — 인증·인가와 Prisma ORM (버전 고정 + 보안 습관)</name>
  <precondition>`.planning/phases/05-step-2-3/05-01-SUMMARY.md`에 Plan 01 Task 3 체크포인트 항목 9의 `prisma`·`@prisma/client` 확인 결과가 기록되어 있다. 없으면 중단한다.</precondition>
  <files>src/content/lessons/step-2/2-5-auth-and-prisma.mdx</files>
  <read_first>
    - `src/content/lessons/step-2/2-5-auth-and-prisma.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "인증·인가와 Prisma ORM"`, `moduleId: "2-5"`, `order: 2`, `depth: "심화"`, `estimatedMinutes: 150`)을 확인한다.
    - `src/content/lessons/step-2/2-5-express-rest-api.mdx` (Task 1 완료본) — 같은 모듈 앞 레슨. 이 레슨은 그 서버 위에 인증을 얹는 형태로 이어진다. 프로젝트 준비 명령·실행 명령 문구를 동일하게 맞추고, 이미 설명한 미들웨어·상태 코드를 반복하지 않는다.
    - `.planning/phases/05-step-2-3/05-RESEARCH.md` §Common Pitfalls Pitfall 6(Prisma CLI latest 태그가 후보판) + §Package Legitimacy Audit + §Security Domain §Known Threat Patterns — 이 레슨이 정확히 어떤 보안 습관을 가르쳐야 하는지의 원문.
    - `.planning/curriculum.md` §Step 2 §2-5 둘째 불릿("인증/인가, Prisma ORM, API 문서화") — 이 레슨의 범위 원천.
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md` — 확정된 심화 형식 골격, `text` 펜스 사용 가능 여부.
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩 6개는 Task 1과 동일한 문자열·순서.

- **① 학습 목표** — "~할 수 있다" 형태 4개. 인증과 인가를 구분해 설명하고, 비밀번호를 안전하게 저장하고, 토큰으로 보호된 엔드포인트를 만들고, Prisma로 데이터를 읽고 쓰는 능력.
- **② 왜 배우나** — 최대 4문장. 앞 레슨의 메모리 배열이 서버를 껐다 켜면 사라진다는 점, 그리고 "내 정보"를 다루려면 누구인지 확인해야 한다는 점.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 다룰 것: 인증(authentication, 누구인지 확인)과 인가(authorization, 무엇을 해도 되는지 확인)의 구분(비유: 신분증 확인 vs 출입 등급), 비밀번호를 왜 그대로 저장하면 안 되는가와 해시·솔트 개념, JWT(JSON Web Token, 서명된 출입증)의 구조와 만료, ORM(Object-Relational Mapping, 테이블을 코드 객체처럼 다루기)과 Prisma의 세 단계(스키마 정의 → 마이그레이션 → 타입이 붙은 클라이언트). 요청 흐름은 `로그인 요청 → 비밀번호 해시 대조 → 토큰 발급 → 이후 요청의 헤더에 토큰 → 인증 미들웨어 → 보호된 핸들러` 한 줄 도식으로 그린다.
- **④ 실무 예제** — **실행: PC 로컬 VS Code**를 한 줄로 명시한다. `powershell` 펜스에 설치 명령을 준다 — Prisma는 **메이저를 고정해** `npm install prisma@^7 @prisma/client@^7`로 안내하고, 함께 `npm install jsonwebtoken bcryptjs dotenv`, `npm install -D @types/jsonwebtoken`을 준다. 버전 없는 설치 안내나 후보판 태그 안내를 쓰지 않는다. 바로 아래 산문 한 줄로 "Prisma는 메이저 버전 전환기라 버전을 고정해 설치합니다"라고 이유를 남긴다(RESEARCH Pitfall 6). 그다음 `powershell` 펜스에 `npx prisma init --datasource-provider sqlite`를 주고, **로컬 파일 데이터베이스를 쓰는 이유**(외부 데이터베이스 서버 없이 끝까지 돌아간다)를 한 줄로 적는다. `prisma/schema.prisma` 내용은 `text` 펜스로 보여 준다 — Prisma 스키마 언어는 허용 펜스 목록에 없으므로 `text`를 쓴다. 모델은 `User`(id·email 유니크·passwordHash·createdAt) 하나면 충분하다. `.env` 예시는 `text` 펜스로 키 이름만 보여 준다(`DATABASE_URL`, `JWT_SECRET`) — 실제 값 자리에는 자리표시자를 쓴다. 마이그레이션은 `powershell` 펜스에 `npx prisma migrate dev --name init`. 마지막으로 `typescript` 펜스 하나에 `src/auth.ts` 완결 코드: 회원가입(`bcryptjs`의 해시 함수로 비밀번호를 해시해 저장, 이메일 중복이면 409), 로그인(저장된 해시와 비교 함수로 대조, 실패면 401, 성공이면 `jsonwebtoken`으로 만료 있는 토큰 발급), 인증 미들웨어(`Authorization` 헤더의 Bearer 토큰 검증, 실패면 401), 보호된 `GET /me`. 시크릿은 `process.env.JWT_SECRET`으로만 읽고 없으면 시작 시 즉시 오류를 내게 한다. 평문 비밀번호를 저장하거나 평문끼리 비교하는 코드를 어디에도 넣지 않는다. 실행은 `powershell` 펜스에 `npx tsx src/auth.ts`, 호출 확인은 `Invoke-RestMethod` 예시 2줄(회원가입·로그인)과 macOS·리눅스 `curl` 산문 병기.
- **④ 안의 `### 해보기` 2~3개** — 소재: 토큰 만료 시간을 짧게 바꿔 만료 후 401이 나오는지 확인하기, `User` 모델에 역할(role) 필드를 추가하고 특정 역할만 통과시키는 인가 검사 붙이기, 이미 가입된 이메일로 다시 가입해 409를 확인하기. 각각 접힌 정답 블록.
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 반드시 포함: 시크릿과 접속 정보는 절대 저장소에 올리지 않고 `.env`는 `.gitignore`에 넣는다, 해싱·토큰 서명을 직접 구현하지 않고 검증된 라이브러리를 쓴다, 토큰에 개인정보를 담지 않는다(누구나 내용을 열어 볼 수 있다).
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표 5~8행(인증, 인가, 해시, 솔트, JWT, ORM, 마이그레이션, 미들웨어 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.

패키지명은 정확히 `prisma`, `@prisma/client`, `jsonwebtoken`, `bcryptjs`, `dotenv`로 쓴다. 용어 표기·분량 기준은 Task 1과 동일하다. API 문서화는 ⑤ 실무 팁에서 한 줄로만 언급하고 도구 설치를 요구하지 않는다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `2-5-auth-and-prisma`가 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`, `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며 빈 줄 규칙을 지킨다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - 파일 안 모든 여는 코드펜스의 언어 태그가 `typescript`·`powershell`·`text`·`json` 넷 중 하나다 (Prisma 스키마는 `text`로 쓴다).
    - `grep -c 'prisma@\^7'`가 `1` 이상이고 `grep -c '@prisma/client@\^7'`가 `1` 이상이다.
    - `grep -c 'prisma@latest'`가 `0`이다. 그리고 Prisma 설치 안내 줄이 모두 메이저 핀을 포함한다: `grep -i install <파일> | grep -i prisma | grep -vc '@^7'`가 `0`이다 (버전 고정 없는 설치 안내가 없다).
    - `grep -c 'process.env.JWT_SECRET'`가 `1` 이상이고, `grep -cE 'JWT_SECRET\s*=\s*["'"'"'][A-Za-z0-9]'`가 `0`이다 (시크릿 리터럴 없음).
    - `grep -cE 'DATABASE_URL\s*=\s*["'"'"']?(postgres|mysql|file:/[^.])'`가 `0`이다 (실제 접속 문자열 없음).
    - `grep -c 'bcryptjs'`가 `2` 이상이고, `typescript` 펜스 안에 해시 생성과 해시 대조 호출이 각각 1회 이상 등장한다.
    - `grep -cE 'password\s*===\s*|=== *user\.password|password ==' `가 `0`이다 (평문 비밀번호 비교가 없다).
    - `grep -ciE 'passwordHash'`가 `2` 이상이다 (저장 필드 이름이 해시임을 드러낸다).
    - `grep -c 'jsonwebtoken'`가 `1` 이상이고, 커스텀 서명·커스텀 해싱 구현 코드가 없다.
    - `grep -ci '실행: PC 로컬'`가 `1` 이상이다.
    - `grep -c 'sk-ant-'`가 `0`, `grep -ciE 'stackblitz|codesandbox'`가 `0`, `grep -ciE 'kant'`가 `0`이다.
    - `node -e "const l=require('./.velite/lessons.json');const s=new Set(l.filter(x=>x.hasContent).map(x=>x.slug));process.exit(s.has('2-5-express-rest-api')&&s.has('2-5-auth-and-prisma')?0:1)"`가 exit 0이다.
  </acceptance_criteria>
  <done>2-5 모듈 2편이 심화 형식으로 작성됐고, Prisma가 안정 라인으로 고정 안내되며, 비밀번호 해싱과 환경변수 시크릿 패턴만 등장한다. 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레슨 본문 → 공개 프로덕션 URL | 본문 문자열이 그대로 인터넷에 노출된다 |
| 레슨 코드 예제 → 학습자가 나중에 만드는 실제 서비스 | 인증 예제는 특히 그대로 복제된다. 나쁜 습관 하나가 실서비스 취약점이 된다 |
| 레슨이 안내하는 npm 패키지 → 학습자의 `npm install` | 잘못된 패키지명·버전 안내가 학습자 환경에 그대로 설치된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | 레슨 본문 — 교육기관명·이메일 | high | mitigate | `node scripts/check-brand.mjs` 상시 검사 + 파일 단위 대소문자 무시 grep 0건 |
| T-05-10 | Information Disclosure / Tampering | 비밀번호 저장 예제 | high | mitigate | `bcryptjs` 해시를 거친 저장·대조만 등장한다. acceptance criteria가 평문 비교 패턴 0건과 해시 함수 호출 존재를 grep으로 확인하고, 저장 필드 이름을 `passwordHash`로 강제한다 |
| T-05-11 | Information Disclosure | JWT 시크릿·데이터베이스 접속 문자열 | high | mitigate | `process.env.JWT_SECRET`·`process.env.DATABASE_URL` 참조만 등장한다. acceptance criteria가 시크릿·접속 문자열 리터럴 0건을 정규식으로 확인한다. ⑤ 실무 팁에 `.gitignore` 원칙을 남긴다 |
| T-05-12 | Tampering | 커스텀 인증 구현을 가르침 | medium | mitigate | `jsonwebtoken`·`bcryptjs` 표준 라이브러리 사용을 명시하고, 커스텀 서명·해싱 구현 금지를 prohibitions에 넣는다 (RESEARCH Don't Hand-Roll) |
| T-05-13 | Tampering | 토큰에 개인정보를 담는 습관 | medium | mitigate | ⑤ 실무 팁에 "토큰 내용은 누구나 열어 볼 수 있다"는 경고를 필수 항목으로 넣는다 |
| T-05-SC | Tampering | 레슨이 안내하는 npm 패키지 (`prisma`, `@prisma/client` = `[SUS]` too-new) | high | mitigate | 이 Plan은 저장소에 패키지를 설치하지 않는다. Plan 01 Task 3의 `blocking-human` 체크포인트 항목 9가 두 패키지의 공식 레지스트리 페이지를 사람이 확인한 뒤에야 이 Plan이 실행된다(`<package_gate_note>`, 각 Task의 `<precondition>`). CLI `latest` 태그가 후보판을 가리키는 문제는 `^7` 명시 고정으로 회피하며 acceptance criteria가 grep으로 확인한다. `[SLOP]` 판정 0건 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

- **CONT-05 edge 프로브가 `unclassified → unresolved`로 남아 있다** (SPEC.md 부재, spec-less fallback §A). 이 Plan의 두 레슨은 프로젝트 가이드가 아니지만, 여기서 다루는 Express·Prisma·인증이 `2-6-project-ai-shop-backend` 준비 가이드의 "재현 아님" 경계와 맞닿는다 — **심화 레슨이 실무 예제를 완결 코드로 주는 것은 D-12대로 정상이고, 경계는 프로젝트 가이드 쪽에만 적용된다.** 두 규칙을 혼동하지 않는다.
- **A4 — 정확한 패키지 조합:** 커리큘럼 원문은 "Express 기반 RESTful API", "인증/인가, Prisma ORM"만 명시하고 세부 라이브러리를 지정하지 않는다. `express`·`zod`·`cors`·`jsonwebtoken`·`bcryptjs`·`dotenv` 조합은 Claude's Discretion 범위의 사실상 표준 선택이다.
- **Pitfall 6 — Prisma 버전:** 리서치 시점(2026-08-25) 기준 `prisma` CLI의 `latest` 태그가 후보판이고 `@prisma/client`는 안정판 7.x다. **집필 시점에 이 상태가 바뀌었을 수 있다** — 그래도 `^7` 고정 안내는 안전하므로 유지한다. 만약 8.x가 정식 배포된 것을 확인하면 SUMMARY에 기록하되 이 Plan에서 버전을 올리지 않는다(레슨 일관성 유지, Phase 5 밖에서 판단).
- **로컬 파일 데이터베이스 선택:** 예제가 외부 데이터베이스 서버 없이 끝까지 돌아가도록 Prisma 데이터소스를 로컬 파일로 잡았다. 커리큘럼 스택은 PostgreSQL이지만, 실습 완주율을 위해 이 레슨에서만 파일 데이터베이스를 쓰고 "실제 프로젝트에서는 PostgreSQL로 바꾼다"를 한 줄로 남긴다 — Claude's Discretion 범위.
- **`check-manifest.mjs`는 이 wave 동안 의도적으로 red다** — `<manifest_gate_note>` 참조.
</flagged_assumptions>

<verification>
1. `05-01-SUMMARY.md`의 패키지 정당성 확인 결과 존재 (각 Task `<precondition>`)
2. `npm run build`
3. `node scripts/check-lesson-structure.mjs` — 두 레슨 모두 오류 목록에 없음
4. `node scripts/check-brand.mjs`
5. `node scripts/check-manifest.mjs`는 **실행하지 않는다** (이 wave 동안 의도적 red, Plan 07이 되돌린다)
</verification>

<success_criteria>
- 2-5 모듈 2편이 심화 형식으로 작성됐고 실행 위치가 PC 로컬로 명시됐다
- Prisma가 `^7` 안정 라인으로 고정 안내되고 후보판 안내가 0건이다
- 비밀번호 해싱·환경변수 시크릿 패턴만 등장하고 평문 저장·리터럴 시크릿이 0건이다
- 확대된 구조 게이트와 브랜드 게이트를 통과한다
</success_criteria>

<output>
Create `.planning/phases/05-step-2-3/05-04-SUMMARY.md` when done.

담을 것: 두 레슨에서 안내한 정확한 패키지명과 버전 표기, Prisma 데이터소스 선택과 그 이유, `text` 펜스로 처리한 Prisma 스키마의 렌더 결과, 확정된 해보기 개수, 게이트 통과 시점의 `hasContent: true` 총 개수, 집필 시점의 Prisma 레지스트리 상태(리서치 시점과 달라졌는지).
</output>
