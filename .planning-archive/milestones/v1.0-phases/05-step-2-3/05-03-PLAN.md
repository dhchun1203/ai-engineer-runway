---
phase: 05-step-2-3
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/content/lessons/step-2/2-2-html-css-js.mdx
  - src/content/lessons/step-2/2-2-browser-and-ui.mdx
autonomous: true
requirements: [CONT-05]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`/lesson/2-2-html-css-js`가 HTML·CSS·JavaScript 핵심을 다루는 심화 본문을 렌더한다 — `.planning/curriculum.md` §2-2 첫 불릿 범위를 채운다"
    - "`/lesson/2-2-browser-and-ui`가 브라우저 동작 원리와 UI 구현을 다루는 심화 본문을 렌더한다 — §2-2 둘째 불릿 범위를 채운다"
    - "두 레슨의 예제가 별도 설치 없이 `.html` 파일 하나를 브라우저로 여는 것만으로 완결된다 — Node·npm 설치 안내는 이 레슨들에 없다. 설치는 순서상 뒤인 `2-3-typescript-setup`이 담당한다 (D-74)"
    - "두 레슨이 `html`·`css`·`javascript` 코드펜스를 쓰고 그 언어가 빌드 산출물에서 하이라이팅된다 — Plan 01이 확인한 신규 8개 언어 중 3개가 여기서 실사용된다 (D-72, RESEARCH Pitfall 5)"
    - "두 레슨 모두 `### 해보기` 2~3개 + 해보기 개수 + 2 이상의 접힌 정답 블록 + `**이 레슨의 단어**` 표 5~8행을 갖고 `node scripts/check-lesson-structure.mjs`를 통과한다 (D-69)"
    - "두 레슨의 프론트매터에서 `hasContent: false → true` 한 줄 외에는 어떤 필드도 바뀌지 않았다 (D-13)"
    - statement: "예제가 외부 네트워크 호출에 의존하지 않아 오프라인에서도 그대로 동작한다 — 학습자가 예제 실패를 자기 코드 문제로 오해하지 않는다"
      verification: backstop
    - statement: "읽기 산문이 150분을 채우지 않고, 늘어난 분량은 해보기·정답 블록·단어 표가 차지한다 (D-49 승계)"
      verification: backstop
  artifacts:
    - src/content/lessons/step-2/2-2-html-css-js.mdx
    - src/content/lessons/step-2/2-2-browser-and-ui.mdx
  key_links:
    - "`2-2` → `2-3-typescript-setup` 순서 — 2-2가 Node 설치를 요구하면 학습자가 아직 설치하지 않은 도구를 먼저 만나게 된다. 2-2는 브라우저와 파일 하나로 완결되어야 한다 (D-74)"
    - "`html`/`css`/`javascript` 펜스 → `ALLOWED_FENCE_LANG_PREFIXES` — Plan 01이 목록에 넣지 않았다면 L6가 즉시 red를 낸다"
    - "이 Plan의 2편 → Plan 07의 매니페스트 실측"
  prohibitions:
    - statement: "레슨 본문·코드 주석·메타데이터 어디에도 교육기관명(`scripts/check-brand.mjs`의 금지 문자열)을 노출해서는 안 된다 (D-02)"
      status: unverified
    - statement: "이 두 레슨에서 Node·npm·TypeScript 설치를 안내해서는 안 된다 — 설치 담당은 `2-3-typescript-setup`이다 (D-74)"
      status: unverified
    - statement: "프론트매터의 `hasContent` 외 어떤 필드도 바꿔서는 안 된다 (D-13, D-31)"
      status: unverified
    - statement: "다이어그램 플러그인·SVG·이미지·새 MDX 컴포넌트를 도입해서는 안 된다 (D-48)"
      status: unverified
    - statement: "온라인 IDE(StackBlitz·CodeSandbox)를 대체 실습 경로로 안내해서는 안 된다 (D-73, CONTEXT.md Deferred)"
      status: unverified
    - statement: "예제가 실행 시점에 외부 공개 API를 호출하도록 작성해서는 안 된다 — 네트워크 실패가 학습 실패로 오인된다. 비동기는 로컬 데이터와 타이머로 보여 준다"
      status: unverified
---

<objective>
Step 2 배치의 2-2 모듈 2편을 집필한다 — HTML·CSS·JavaScript 핵심과 브라우저 동작 원리·UI 구현.

Purpose: 2-2는 커리큘럼 순서상 `2-3-typescript-setup`(Node·npm·TypeScript 설치 담당)보다 **먼저** 온다. 그래서 이 두 레슨은 어떤 설치도 요구하지 않고 `.html` 파일 하나와 브라우저만으로 완결되어야 한다(D-74). 동시에 이 Plan은 신규 허용 펜스 언어 `html`·`css`·`javascript` 세 개를 실제 콘텐츠에서 처음 쓰는 자리이기도 하다.

Output: 심화 형식 레슨 2편, `hasContent: true` 전환 2건.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-step-2-3/05-CONTEXT.md
@.planning/phases/05-step-2-3/05-PATTERNS.md
@.planning/phases/05-step-2-3/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces

**Plan 03 (이 Plan)이 만드는 것**
- `src/content/lessons/step-2/2-2-html-css-js.mdx` — 본문 신규, `hasContent: false → true`
- `src/content/lessons/step-2/2-2-browser-and-ui.mdx` — 본문 신규, `hasContent: false → true`
- 레슨 본문 안에서만 존재하는 실습 파일 이름(저장소 파일 아님): `shop-card.html`, `shop-list.html`

**Phase 5 전체가 만들거나 바꾸는 것** — `05-01-PLAN.md` §Artifacts this phase produces 참조.
</artifacts_produced>

<manifest_gate_note>
**이 wave 동안 `node scripts/check-manifest.mjs`는 의도적으로 red다.** `hasContent`를 2건 켜면 실제 개수가 Plan 01이 맞춰 둔 상수를 넘어 Invariant 10이 실패한다. 상수는 Wave 2 마감 Plan 07이 실측으로 되돌린다(D-78). **실행자는 `check-manifest`를 verify에서 제외하고 상수를 손대지 않는다** — 같은 wave의 Plan 02·04·05·06이 병렬로 같은 파일을 고치면 충돌한다.
</manifest_gate_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: `2-2-html-css-js` 집필 — HTML·CSS·JavaScript 핵심 (설치 없이 파일 하나로)</name>
  <files>src/content/lessons/step-2/2-2-html-css-js.mdx</files>
  <read_first>
    - `src/content/lessons/step-2/2-2-html-css-js.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "HTML·CSS·JavaScript 핵심"`, `stepId: 2`, `moduleId: "2-2"`, `order: 1`, `depth: "심화"`, `estimatedMinutes: 150`, `hasContent: false`, `slug: "2-2-html-css-js"`)을 확인한다.
    - `src/content/lessons/step-2/2-3-react-components.mdx` (Plan 01 완료본) — 확정된 심화 형식의 실물. 이 레슨이 그 골격을 그대로 따른다. 또한 2-3이 React로 다룰 내용(props·state·컴포넌트)을 여기서 미리 풀어 버리지 않도록 경계를 확인한다.
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md` — 승인·확정된 심화 형식 골격과 사용자 수정 요청 반영 결과, 신규 펜스 언어 실증 결과(실제로 쓸 수 있는 태그 문자열).
    - `.planning/curriculum.md` §Step 2 §2-2 — 불릿 2줄. 첫 불릿("HTML, CSS, JavaScript 핵심 학습")이 이 레슨 몫이다.
    - `.planning/phases/05-step-2-3/05-CONTEXT.md` §D-74 — 2-2가 브라우저와 파일 하나로 완결되어야 하는 이유(설치는 2-3이 담당).
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩은 아래 6개뿐이며 문자열과 순서를 그대로 쓴다: `## 1. 학습 목표`, `## 2. 왜 배우나`, `## 3. 개념 설명`, `## 4. 실무 예제`, `## 5. 실무 팁`, `## 6. 핵심 정리 및 스스로 점검`.

- **① 학습 목표** — "~할 수 있다" 형태 4개. HTML로 구조를 만들고, CSS로 배치하고, JavaScript로 클릭에 반응하게 만드는 능력.
- **② 왜 배우나** — 최대 4문장. React·Next.js도 결국 HTML·CSS·JS로 바뀌어 브라우저에 도착한다는 점, 그래서 프레임워크를 배우기 전에 밑바닥을 한 번 보는 것이라는 자리매김.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 비유 먼저 정의 뒤. 다룰 것: HTML = 뼈대(태그·속성·시맨틱 태그), CSS = 옷(선택자·박스 모델·flex 배치), JavaScript = 움직임(변수·함수·DOM 찾기·이벤트 붙이기). 각 언어의 역할을 "뼈대 / 옷 / 움직임" 비유 표로 한 번 묶는다.
- **④ 실무 예제** — **실행: PC 로컬 VS Code** 한 줄을 명시하되, **설치가 필요 없다**는 점을 같은 줄에서 분명히 한다(D-73, D-74). 예제는 `shop-card.html` 파일 **하나**로 완결한다: `html` 펜스에 전체 문서 구조(head에 `<style>`, body에 상품 카드 마크업, 끝에 `<script>`), 그리고 `css` 펜스와 `javascript` 펜스로 그중 스타일 부분과 스크립트 부분을 각각 떼어 다시 보여 주며 설명한다. 내용: 상품 카드 하나(이미지 자리 대신 배경색 박스, 제목·가격·좋아요 버튼)를 flex로 배치하고, 좋아요 버튼을 누르면 숫자가 1씩 오르고 버튼 색이 바뀐다. 이미지 파일을 참조하지 않는다(파일 하나로 완결). 실행 방법은 산문 한 줄로 "저장한 뒤 파일을 더블클릭하면 기본 브라우저에서 열린다"고 적고, 명령으로 열고 싶은 경우를 위해 `powershell` 펜스에 `start shop-card.html` 한 줄을 주고 그 아래 macOS 병기를 산문 한 줄(`open shop-card.html`)로 덧붙인다(D-75).
- **④ 안의 `### 해보기` 2~3개** — 소재: 카드를 3개로 늘려 가로로 나열하기(flex 연습), 좋아요 수가 10을 넘으면 문구가 바뀌게 하기(조건 분기 + DOM 갱신), 색·간격을 CSS 변수로 빼기. 각 해보기 아래 접힌 정답 블록(`<details>` → `<summary>정답 보기</summary>` → **빈 줄** → 정답 코드와 화면 설명 → **빈 줄** → `</details>`).
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 개발자도구 요소 검사로 CSS를 실시간으로 만져 보는 법, 시맨틱 태그를 쓰면 접근성과 검색에 유리한 이유, 인라인 스타일보다 클래스를 쓰는 이유.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표(5~8행: 태그, 속성, 선택자, 박스 모델, DOM, 이벤트, 시맨틱 태그 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.

용어는 첫 등장 시 "한글(English, 한 줄 풀이)" 형태로 풀고 이후는 한글만 쓴다(D-50). React·컴포넌트·상태 관리는 이름만 스치고 설명하지 않는다 — 2-3이 담당한다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `2-2-html-css-js`가 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent: false` → `hasContent: true` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`이고 순서가 1→6이며, `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며, `<summary>` 다음 줄과 `</details>` 이전 줄이 빈 줄이다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - `grep -c '^```html$'`·`grep -c '^```css$'`·`grep -c '^```javascript$'`가 각각 `1` 이상이다.
    - 파일 안 모든 여는 코드펜스의 언어 태그가 `html`·`css`·`javascript`·`powershell` 넷 중 하나다.
    - `grep -c 'npm install'`·`grep -c 'npm init'`·`grep -ci 'node --version'`가 모두 `0`이다 (D-74 — 이 레슨은 설치를 요구하지 않는다).
    - `grep -c '<img'`가 `0`이다 (예제가 외부 이미지 파일을 참조하지 않는다).
    - `grep -ciE 'fetch\(|XMLHttpRequest|https?://api\.'`가 `0`이다 (실행 시점 외부 네트워크 호출이 없다).
    - `grep -c 'shop-card.html'`가 `2` 이상이다 (파일명이 본문과 실행 안내에 일관되게 등장한다).
    - `grep -ciE 'stackblitz|codesandbox'`가 `0`이고, `grep -ciE 'kant'`가 `0`이다.
    - `node -e "const l=require('./.velite/lessons.json');const p=l.find(x=>x.slug==='2-2-html-css-js');process.exit(/language-html/.test(p.code)&&/language-css/.test(p.code)&&/language-javascript/.test(p.code)?0:1)"`가 exit 0이다 (세 신규 언어가 실제로 컴파일 산출물에 반영됐다).
  </acceptance_criteria>
  <done>2-2 첫 레슨이 설치 없이 파일 하나로 완결되는 심화 형식으로 작성됐고, `html`·`css`·`javascript` 세 신규 펜스 언어가 실제 빌드에서 살아 있다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: `2-2-browser-and-ui` 집필 — 브라우저 동작 원리와 UI 구현</name>
  <files>src/content/lessons/step-2/2-2-browser-and-ui.mdx</files>
  <read_first>
    - `src/content/lessons/step-2/2-2-browser-and-ui.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "브라우저 동작 원리와 UI 구현"`, `moduleId: "2-2"`, `order: 2`, `depth: "심화"`, `estimatedMinutes: 150`)을 확인한다.
    - `src/content/lessons/step-2/2-2-html-css-js.mdx` (Task 1 완료본) — 같은 모듈 앞 레슨. 이미 설명한 태그·선택자·DOM 기초를 반복하지 않고 그 위에서 시작한다. 실행 안내 문구와 파일 열기 방식을 동일하게 맞춘다.
    - `.planning/curriculum.md` §Step 2 §2-2 둘째 불릿("브라우저 동작 원리와 UI 구현") — 이 레슨의 범위 원천.
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md` — 확정된 심화 형식 골격.
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩 6개는 Task 1과 동일한 문자열·순서.

- **① 학습 목표** — "~할 수 있다" 형태 4개. 주소창에 URL을 넣은 뒤 화면이 뜨기까지의 단계를 설명하고, 개발자도구로 그 단계를 관찰하고, 비동기 코드가 화면을 언제 바꾸는지 예측하는 능력.
- **② 왜 배우나** — 최대 4문장. 화면이 느리거나 안 뜨는 문제를 "왜 그런지 모른 채 고치는" 상태에서 벗어나는 것, 그리고 2-4 프로젝트에서 LLM 응답을 기다리는 동안 화면을 어떻게 다룰지가 여기서 나온다는 자리매김.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 다룰 것: URL 입력부터 화면까지의 흐름을 `주소 입력 → DNS 조회 → HTTP 요청 → HTML 응답 → 파싱(DOM·CSSOM) → 렌더 트리 → 레이아웃 → 페인트` 한 줄 도식과 단계별 표로 정리(D-48 — 다이어그램 플러그인·이미지 없음), 렌더링을 막는 것(동기 스크립트)과 안 막는 것, 이벤트 루프와 비동기(비유: 주문받고 음식 나올 때까지 다른 손님을 받는 카운터), 재계산(reflow)과 다시 그리기(repaint)의 차이.
- **④ 실무 예제** — **실행: PC 로컬 VS Code, 설치 불필요**를 한 줄로 명시한다(D-74). `shop-list.html` 파일 하나로 완결한다. `html` 펜스로 문서 골격을, `javascript` 펜스로 스크립트를 보여 준다. 내용: 파일 안에 상품 객체 배열 6개를 상수로 두고, "불러오기" 버튼을 누르면 로딩 문구를 먼저 표시한 뒤 타이머로 지연을 흉내 내 목록을 DOM에 그린다. **외부 API를 호출하지 않는다** — 네트워크 실패가 학습 실패로 오인되는 것을 막고 오프라인에서도 돌아가게 한다. `async`/`await`와 `Promise`를 이 지연 함수로 설명한다. 예제 아래에 개발자도구 관찰 안내를 산문으로 붙인다: Elements 탭에서 DOM이 실제로 늘어나는 것 보기, Network 탭에서 문서 요청 하나만 있는 것 확인하기, Performance 또는 Console에서 렌더 순서 확인하기. 파일을 여는 방법은 Task 1과 동일하게 산문 한 줄 + `powershell` 펜스 `start shop-list.html` + macOS 병기 산문 한 줄.
- **④ 안의 `### 해보기` 2~3개** — 소재: 지연 시간을 늘려 로딩 문구가 실제로 보이게 만들기, 목록을 그리는 방식을 한 번에 붙이기와 하나씩 붙이기로 바꿔 개발자도구에서 차이 관찰하기, `<script>` 위치를 head로 옮겼을 때 무슨 일이 생기는지 확인하고 이유 적기. 각각 접힌 정답 블록.
- **⑤ 실무 팁** — 불릿 3~4개. 스크립트를 body 끝이나 `defer`로 두는 이유, DOM을 반복문 안에서 매번 건드리면 느려지는 이유, 개발자도구의 네트워크 속도 제한(throttling)으로 느린 환경을 흉내 내는 법.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표 5~8행(렌더 트리, 파싱, 이벤트 루프, 비동기, 프로미스, 리플로우, 개발자도구 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.

용어 표기·분량 기준은 Task 1과 동일하다. React·프레임워크 이야기는 이름만 스친다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `2-2-browser-and-ui`가 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`, `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며 빈 줄 규칙을 지킨다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - 파일 안 모든 여는 코드펜스의 언어 태그가 `html`·`javascript`·`powershell` 셋 중 하나다.
    - `grep -ciE 'fetch\(|XMLHttpRequest|https?://api\.|jsonplaceholder'`가 `0`이다 (실행 시점 외부 네트워크 호출이 없다).
    - `grep -c 'npm install'`·`grep -c 'npm init'`가 모두 `0`이다 (D-74).
    - `grep -c 'shop-list.html'`가 `2` 이상이다.
    - `grep -ciE 'stackblitz|codesandbox'`가 `0`이고 `grep -ciE 'kant'`가 `0`이다.
    - `node -e "const l=require('./.velite/lessons.json');const s=new Set(l.filter(x=>x.hasContent).map(x=>x.slug));process.exit(s.has('2-2-html-css-js')&&s.has('2-2-browser-and-ui')?0:1)"`가 exit 0이다.
  </acceptance_criteria>
  <done>2-2 모듈 2편이 모두 설치 없이 완결되는 심화 형식으로 작성됐고 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레슨 본문 → 공개 프로덕션 URL | 본문 문자열이 그대로 인터넷에 노출된다 |
| 레슨 예제 → 학습자의 브라우저 | 학습자가 복사해 자기 머신에서 `.html`로 열어 실행한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | 레슨 본문 — 교육기관명·이메일 | high | mitigate | `node scripts/check-brand.mjs` 상시 검사 + acceptance criteria의 파일 단위 대소문자 무시 grep 0건 |
| T-05-08 | Tampering | 예제가 `innerHTML`로 외부 문자열을 넣는 습관을 가르침 | medium | mitigate | 예제 데이터가 파일 안 상수 배열이라 외부 입력 경로가 없다. 목록 렌더는 `textContent`/`createElement` 중심으로 쓰고, 사용자 입력을 화면에 넣을 때는 이스케이프가 필요하다는 취지를 ⑤ 실무 팁 한 줄로 남긴다 |
| T-05-09 | Denial of Service | 예제가 외부 공개 API에 의존해 실패 | low | mitigate | 예제는 로컬 상수 배열 + 타이머로 비동기를 흉내 낸다. acceptance criteria가 `fetch(`·외부 URL 0건을 grep으로 확인한다 |
| T-05-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 저장소에 패키지를 설치하지 않고, 레슨도 설치를 안내하지 않는다(D-74). RESEARCH.md `## Package Legitimacy Audit`의 `[SLOP]` 판정 0건 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

- **CONT-05 edge 프로브가 `unclassified → unresolved`로 남아 있다** (SPEC.md 부재, spec-less fallback §A). 이 Plan의 두 레슨은 프로젝트 가이드가 아니므로 "재현 아님" 경계가 직접 걸리지 않는다. 대응되는 실패 모드(`hasContent: true`인데 6단이 덜 찬 레슨)는 `check-lesson-structure.mjs` L1·L2·L5와 Velite 스키마가 막지만, 프로즈 품질 자체는 자동 검증 불가다.
- **A2 승계 — `html`·`css`·`javascript` 펜스가 이 저장소에서 실증되는 것은 이 Plan이 처음이다.** Plan 01이 Shiki 번들 로드로 사전 확인했더라도, 실제 MDX 컴파일 결과에 `language-*` 클래스가 붙는지는 Task 1의 acceptance criteria가 처음 확인한다. 실패해도 빌드는 깨지지 않고 단색 폴백이 되므로, 폴백이 나오면 SUMMARY에 기록하고 Plan 13의 사람 확인 항목으로 넘긴다.
- **`check-manifest.mjs`는 이 wave 동안 의도적으로 red다** — `<manifest_gate_note>` 참조.
</flagged_assumptions>

<verification>
1. `npm run build`
2. `node scripts/check-lesson-structure.mjs` — 두 레슨 모두 오류 목록에 없음
3. `node scripts/check-brand.mjs`
4. `node scripts/check-manifest.mjs`는 **실행하지 않는다** (이 wave 동안 의도적 red, Plan 07이 되돌린다)
</verification>

<success_criteria>
- 2-2 모듈 2편이 심화 형식으로 작성됐다
- 두 레슨 모두 설치 없이 `.html` 파일 하나와 브라우저만으로 완결된다 (D-74)
- `html`·`css`·`javascript` 신규 펜스 언어가 실제 빌드 산출물에서 하이라이팅된다
- 확대된 구조 게이트와 브랜드 게이트를 통과한다
</success_criteria>

<output>
Create `.planning/phases/05-step-2-3/05-03-SUMMARY.md` when done.

담을 것: `html`/`css`/`javascript` 펜스의 실제 하이라이팅 결과(폴백 여부), 두 레슨에서 쓴 실습 파일 이름, 확정된 해보기 개수, 게이트 통과 시점의 `hasContent: true` 총 개수.
</output>
