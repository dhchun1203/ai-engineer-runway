---
phase: 05-step-2-3
plan: 09
type: execute
wave: 4
depends_on: ["05-07"]
files_modified:
  - src/content/lessons/step-3/3-3-peft-lora-qlora.mdx
  - src/content/lessons/step-3/3-3-tuning-evaluation.mdx
autonomous: true
requirements: [CONT-05]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`/lesson/3-3-peft-lora-qlora`가 PEFT·LoRA·QLoRA를 **개요 깊이**로 렌더한다 — 학습 목표가 정의·용도·파이프라인 위치 세 요소로 쓰였고 `직접 튜닝할 수 있다`류 목표가 0건이다 (D-62)"
    - "`/lesson/3-3-tuning-evaluation`이 모델 튜닝 전후 성능 비교를 개요 깊이로 렌더한다 — 지표를 계산하게 하지 않고 결과를 읽고 판단하게 한다 (D-62, D-64)"
    - "두 레슨의 코드가 읽기용 스니펫 하나씩뿐이고 '지금 실행할 필요 없습니다' 취지의 안내가 붙어 있으며 설치·실행 명령이 0건이다 — GPU·데이터셋 환경 구축은 90분 예산 밖이다 (D-63)"
    - "두 레슨의 `### 해보기`가 판단·설계형이다 — 실행이나 실제 튜닝을 요구하지 않는다 (D-64)"
    - "두 레슨 모두 6단 헤딩 원문 유지 + `### 해보기` 2~3개 + 해보기 개수 + 2 이상의 접힌 정답 블록 + `**이 레슨의 단어**` 표 5~8행을 갖고 `node scripts/check-lesson-structure.mjs`를 통과한다 (D-65, D-69)"
    - "두 레슨의 프론트매터에서 `hasContent: false → true` 한 줄 외에는 어떤 필드도 바뀌지 않았다 (D-13)"
    - statement: "개요 레슨을 읽고 나서 개강 첫 시간에 'PEFT', 'LoRA', 'QLoRA', '베이스라인'을 들었을 때 무슨 얘기인지 알아들을 수 있다 (D-62)"
      verification: backstop
    - statement: "'튜닝 대신 프롬프트나 RAG로 푸는 게 맞는 경우'가 본문에 분명히 나와 있다 — 학습자가 튜닝을 만능으로 오해하지 않는다"
      verification: backstop
  artifacts:
    - src/content/lessons/step-3/3-3-peft-lora-qlora.mdx
    - src/content/lessons/step-3/3-3-tuning-evaluation.mdx
  key_links:
    - "`3-3-peft-lora-qlora` → `3-3-tuning-evaluation` — 앞 편이 '무엇을 하는가', 뒤 편이 '잘 됐는지 어떻게 아는가'다. 용어와 예시 시나리오를 이어 쓴다"
    - "`3-3-tuning-evaluation`의 평가 개념 → `3-6-prompt-versioning-eval`(Plan 12) — 같은 '평가' 개념이 프롬프트 쪽에서 다시 나온다. 용어를 일치시킨다"
    - "`2-7-promptops`(Plan 06)의 골든셋 실습 → 이 Plan의 평가셋 개념 — 학습자가 이미 손으로 해 본 것이므로 그 경험을 참조한다"
    - "이 Plan의 2편 → Plan 13의 매니페스트 실측"
  prohibitions:
    - statement: "레슨 본문·코드 주석·메타데이터 어디에도 교육기관명(`scripts/check-brand.mjs`의 금지 문자열)을 노출해서는 안 된다 (D-02)"
      status: unverified
    - statement: "Step 3 개요 레슨에 설치·실행 안내를 붙이거나 해보기 과제로 실행을 요구해서는 안 된다 — GPU·데이터셋 환경 구축은 90분 예산을 초과한다 (D-63)"
      status: unverified
    - statement: "개요 레슨의 학습 목표에 `직접 구현할 수 있다`·`직접 튜닝할 수 있다`·`성능을 비교 측정할 수 있다`를 써서는 안 된다 (D-62)"
      status: unverified
    - statement: "수식·하이퍼파라미터 표·논문 인용으로 개요를 심화로 팽창시켜서는 안 된다 — 코드 블록 하나, 30줄 이하가 상한이다 (RESEARCH Pitfall 2)"
      status: unverified
    - statement: "6단 헤딩 텍스트를 바꾸거나 단어 표·해보기 개수 규칙을 예외 처리해서는 안 된다 (D-65, D-69)"
      status: unverified
    - statement: "프론트매터의 `hasContent` 외 어떤 필드도 바꿔서는 안 된다 (D-13, D-31)"
      status: unverified
---

<objective>
Step 3 배치의 3-3 모듈 2편을 집필한다 — PEFT·LoRA·QLoRA 개념과 모델 튜닝 전후 성능 비교.

Purpose: Step 3에서 **개요 깊이 유지가 가장 어려운 모듈**이다. 튜닝은 실무에서 깊은 주제라 eli5로 설명하다 보면 하이퍼파라미터·수식으로 미끄러지기 쉽다(RESEARCH Pitfall 2). D-62의 "알아듣는다" 기준과 D-63의 읽기용 스니펫 제한이 그 방어선이며, GPU·데이터셋 환경 구축은 애초에 90분 예산 밖이다.

Output: 개요 형식 레슨 2편, `hasContent: true` 전환 2건.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-step-2-3/05-CONTEXT.md
@.planning/phases/05-step-2-3/05-PATTERNS.md
@.planning/phases/05-step-2-3/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces

**Plan 09 (이 Plan)이 만드는 것**
- `src/content/lessons/step-3/3-3-peft-lora-qlora.mdx` — 본문 신규, `hasContent: false → true`
- `src/content/lessons/step-3/3-3-tuning-evaluation.mdx` — 본문 신규, `hasContent: false → true`

**Phase 5 전체가 만들거나 바꾸는 것** — `05-01-PLAN.md` §Artifacts this phase produces 참조.
</artifacts_produced>

<manifest_gate_note>
**이 wave 동안 `node scripts/check-manifest.mjs`는 의도적으로 red다.** `hasContent`를 2건 켜면 실제 개수가 Plan 07이 맞춰 둔 상수를 넘어 Invariant 10이 실패한다. 상수는 Wave 3 마감 Plan 13이 실측으로 되돌린다(D-78). **실행자는 `check-manifest`를 verify에서 제외하고 상수를 손대지 않는다.**
</manifest_gate_note>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: `3-3-peft-lora-qlora` 집필 — PEFT·LoRA·QLoRA 개념 (개요 깊이)</name>
  <files>src/content/lessons/step-3/3-3-peft-lora-qlora.mdx</files>
  <read_first>
    - `src/content/lessons/step-3/3-3-peft-lora-qlora.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "PEFT·LoRA·QLoRA 개념"`, `stepId: 3`, `moduleId: "3-3"`, `order: 1`, `depth: "개요"`, `estimatedMinutes: 90`, `hasContent: false`, `slug: "3-3-peft-lora-qlora"`)을 확인한다.
    - `src/content/lessons/step-3/3-1-vector-search-basics.mdx` (Plan 01 완료본) — **개요 형식의 승인된 실물.** 학습 목표 3요소 형식, 읽기용 스니펫 안내 문구, 판단형 해보기의 실제 문장을 그대로 가져온다. 전체를 읽는다.
    - `src/content/lessons/step-1/1-5-ml-model-types.mdx`와 `1-5-ml-metrics-and-pipeline.mdx` — Step 1에서 머신러닝 모델·지표를 어디까지 다뤘는지 확인한다. 이 레슨은 그 위에서 시작하므로 학습·추론 기본 개념을 반복하지 않는다.
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md` — 체크포인트 항목 2의 Step 3 깊이 판정 결과(a/b/c)와 조정 사항.
    - `.planning/curriculum.md` §Step 3 §3-3 첫 불릿("PEFT, LoRA, QLoRA 개념 이해") — 이 레슨의 범위 원천. "개념 이해"라는 원문 표현이 깊이의 상한이다.
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩은 아래 6개뿐이며 문자열과 순서를 그대로 쓴다: `## 1. 학습 목표`, `## 2. 왜 배우나`, `## 3. 개념 설명`, `## 4. 실무 예제`, `## 5. 실무 팁`, `## 6. 핵심 정리 및 스스로 점검`(D-65).

- **① 학습 목표** — "이 레슨을 마치면 다음을 알아듣습니다." 뒤에 항목 3~4개. 각 항목은 (a) 용어 한 문장 정의, (b) 언제·왜, (c) 전체 파이프라인에서의 위치 중 하나의 형식이어야 한다. `직접 튜닝할 수 있다`류는 쓰지 않는다(D-62).
- **② 왜 배우나** — 최대 4문장 1문단. 프롬프트와 RAG로도 안 되는 문제가 남는다는 점(우리 도메인 말투·형식을 모델이 아예 모르는 경우), 그리고 튜닝이 비싸고 느리므로 **언제 쓰지 말아야 하는지를 아는 것**이 먼저라는 점.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 비유 먼저 정의 뒤, **수식 없이**. 다룰 것: 전체 파인튜닝(모델 전체를 다시 학습 — 비유: 건물을 통째로 다시 짓기)의 비용 문제, PEFT(Parameter-Efficient Fine-Tuning, 일부만 손대는 방식 — 비유: 건물은 두고 인테리어만 바꾸기), LoRA(원본 가중치는 얼리고 얇은 어댑터 층만 학습 — 비유: 옷 위에 덧입는 조끼), QLoRA(모델을 더 작은 숫자 표현으로 압축한 뒤 LoRA — 비유: 짐을 진공 압축해 작은 가방에 넣기), 그리고 **언제 튜닝 대신 프롬프트·RAG로 푸는가**를 판단 표로 정리(문제 유형 × 권장 접근). 파이프라인 위치는 `문제 정의 → 프롬프트로 해결 시도 → RAG로 지식 보강 → 그래도 안 되면 PEFT 튜닝 → 평가` 한 줄 도식으로 그린다(D-48). 하이퍼파라미터 이름을 나열하거나 논문을 인용하지 않는다.
- **④ 실무 예제** — `### 실무에서 이게 어떻게 쓰이나` 소제목으로 시작한다. 실제 사례를 2~3단락으로 서술한다: 고객 상담 답변의 말투·금지 표현을 조직 기준에 맞추려고 소규모 모델에 LoRA를 얹은 경우, 또는 특정 산업 용어가 많은 문서 요약을 개선한 경우. 그다음 인용문(`>`) 두 줄로 "아래 코드는 개념을 눈으로 보기 위한 예시입니다 — 지금 실행할 필요는 없습니다. / 개강 후 본 과정에서 실제로 돌려보게 됩니다." 취지의 안내를 넣는다(D-63, Plan 01과 같은 문구). 그 아래 `python` 펜스 **하나**에 LoRA 설정을 선언하는 10~20줄 스니펫을 넣는다 — 어떤 값이 "무엇을 얼마나 바꾸는지"를 주석으로 한 줄씩 붙여 읽기만 해도 뜻이 통하게 한다. 코드 블록은 이 하나뿐이며 30줄을 넘기지 않는다. **설치 명령·실행 명령·GPU 준비 안내를 붙이지 않는다.**
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 튜닝 전에 평가셋부터 만든다(다음 편으로 연결), 데이터 품질이 기법 선택보다 결과를 좌우한다, 어댑터는 작아서 여러 개를 갈아 끼울 수 있다는 실무적 장점.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표 5~8행(파인튜닝, PEFT, LoRA, 어댑터, 양자화, 가중치, 베이스 모델 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.
- **`### 해보기` 2~3개는 판단·설계형이다**(D-64). 소재: 문제 시나리오 3개를 주고 각각 프롬프트·RAG·튜닝 중 무엇으로 풀지 고르고 이유 적기, 우리 프로젝트에서 튜닝이 필요할 만한 지점이 있는지 찾아 적고 없다면 왜 없는지 쓰기, 전체 파인튜닝과 LoRA의 차이를 비유 하나로 직접 만들어 보기. 각 해보기 아래 `<details>` 정답은 "모범 답안 + 왜 그렇게 보는가" 형태.

90분 = 읽기 약 40분 + 판단형 해보기 약 50분(D-64). 용어는 첫 등장 시 "한글(English, 한 줄 풀이)" 형태로 풀고 이후는 한글만 쓴다(D-50).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `3-3-peft-lora-qlora`가 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent: false` → `hasContent: true` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`이고 순서가 1→6이며 `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며 빈 줄 규칙을 지킨다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - `grep -c '^```'`가 `2`다 (코드 블록 정확히 하나), 그 언어 태그가 `python`이며 본문이 30줄 이하다.
    - `grep -c 'pip install'`·`grep -c 'npm install'`·`grep -ci '실행 방법'`·`grep -ciE 'gpu.{0,10}(준비|필요합니다)'`가 모두 `0`이다 (D-63).
    - `## 1. 학습 목표` 절에 `구현할 수 있다`·`튜닝할 수 있다`·`비교 측정할 수 있다` 문자열이 0건이다 (D-62).
    - 코드 블록 바로 앞 5줄 안에 `실행할 필요는 없습니다` 취지의 인용문(`>`로 시작하는 줄)이 있다.
    - `## 3. 개념 설명` 구간에 프롬프트·RAG·튜닝 중 무엇을 고를지 정리한 마크다운 표가 1개 이상 있다.
    - `grep -ciE 'kant'`가 `0`이다.
    - `node -e "const l=require('./.velite/lessons.json');const p=l.find(x=>x.slug==='3-3-peft-lora-qlora');process.exit(/language-python/.test(p.code)?0:1)"`가 exit 0이다.
  </acceptance_criteria>
  <done>3-3 첫 편이 개요 깊이로 작성됐고, 읽기용 스니펫 하나와 판단형 해보기를 담으며 실행·GPU를 요구하지 않는다. "언제 튜닝하지 말아야 하는가"가 본문에 있다. 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: `3-3-tuning-evaluation` 집필 — 모델 튜닝 전후 성능 비교 (개요 깊이)</name>
  <files>src/content/lessons/step-3/3-3-tuning-evaluation.mdx</files>
  <read_first>
    - `src/content/lessons/step-3/3-3-tuning-evaluation.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "모델 튜닝 전후 성능 비교"`, `moduleId: "3-3"`, `order: 2`, `depth: "개요"`, `estimatedMinutes: 90`)을 확인한다.
    - `src/content/lessons/step-3/3-3-peft-lora-qlora.mdx` (Task 1 완료본) — 같은 모듈 앞 편. 튜닝 용어와 예시 시나리오를 이어 쓰고 이미 설명한 것을 반복하지 않는다.
    - `src/content/lessons/step-1/1-5-ml-metrics-and-pipeline.mdx` — Step 1에서 지표를 어디까지 다뤘는지 확인한다. 정확도·정밀도·재현율 정의를 반복하지 않고 그 위에서 LLM 특유의 평가로 넘어간다.
    - `src/content/lessons/step-2/2-7-promptops.mdx` (Plan 06 완료본) — 학습자가 골든셋과 자동 평가를 이미 손으로 해 봤다. 그 경험을 참조하고 용어를 일치시킨다.
    - `.planning/curriculum.md` §Step 3 §3-3 둘째 불릿("모델 튜닝 전후 성능 비교") — 이 레슨의 범위 원천.
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩 6개는 Task 1과 동일한 문자열·순서.

- **① 학습 목표** — "알아듣습니다" 형태 3~4개. 각 항목은 3요소 형식 중 하나. `성능을 비교 측정할 수 있다`는 쓰지 않는다(D-62의 명시 금지 목표). 대신 "튜닝 전후 결과 표를 읽고 좋아졌는지 판단하는 근거를 말할 수 있다" 형태로 쓴다.
- **② 왜 배우나** — 최대 4문장. 튜닝한 모델이 "좋아 보인다"와 "실제로 좋아졌다"는 다르다는 점, 그리고 개강 후 프로젝트에서 결과 표를 앞에 두고 판단해야 할 사람이 자기 자신이라는 점.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 다룰 것: 베이스라인(비교 기준, 튜닝 전 모델 — 비유: 다이어트 전 사진), 평가셋을 학습 데이터와 분리하는 이유(비유: 시험 문제를 미리 알려 주고 시험 보기), LLM 평가 지표의 종류를 성격별로 묶은 표(형식 통과율 같은 기계적 지표 / 정답과의 일치도 / 사람 평가 / 모델이 모델을 채점하는 방식)와 각각의 한계, 과적합(학습 데이터에만 잘 맞는 상태)의 신호. `튜닝 전 모델 + 평가셋 → 베이스라인 점수 → 튜닝 → 같은 평가셋 → 점수 비교 → 판단` 한 줄 도식으로 그린다(D-48).
- **④ 실무 예제** — `### 실무에서 이게 어떻게 쓰이나` 소제목으로 시작한다. 실제 사례를 2~3단락으로 서술한다: 상담 답변 톤 튜닝 후 형식 통과율은 올랐는데 사람 평가는 떨어진 경우처럼, **지표 하나만 보면 잘못 판단하는 상황**을 보여 준다. 그다음 인용문(`>`) 두 줄로 "지금 실행할 필요 없습니다" 취지의 안내(D-63). 그 아래 `json` 펜스 **하나**에 튜닝 전후 평가 결과를 담은 10~20줄 결과 객체를 넣는다 — 지표 3~4개 × 전/후 값, 그리고 케이스별 실패 사유 몇 개. 코드 블록은 이 하나뿐이며 30줄을 넘기지 않는다. 표를 읽는 법을 산문 2~3줄로 설명한다.
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 평가셋은 실제로 문제가 났던 입력부터 채운다(2-7-promptops의 골든셋과 같은 원칙), 지표 하나만 보고 배포하지 않는다, 튜닝 전후 비교는 같은 평가셋·같은 조건에서만 의미가 있다.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표 5~8행(베이스라인, 평가셋, 과적합, 형식 통과율, 사람 평가, 모델 채점, 회귀 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.
- **`### 해보기` 2~3개는 판단·설계형이다**(D-64). 소재: ④의 결과 표를 보고 "이 튜닝을 배포할 것인가"를 판단하고 근거 3줄로 쓰기, 우리 프로젝트에 맞는 평가 지표 3개를 고르고 각각 자동으로 잴 수 있는지 표로 구분하기, 평가셋 10문항을 어떤 기준으로 고를지 규칙을 문장으로 적기. 각 해보기 아래 `<details>` 정답은 "모범 답안 + 왜 그렇게 보는가".

90분 = 읽기 약 40분 + 판단형 해보기 약 50분(D-64). 용어 표기는 D-50을 따른다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `3-3-tuning-evaluation`이 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`, `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며 빈 줄 규칙을 지킨다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - `grep -c '^```'`가 `2`다 (코드 블록 정확히 하나), 그 언어 태그가 `json`이며 본문이 30줄 이하다.
    - `grep -c 'pip install'`·`grep -c 'npm install'`·`grep -ci '실행 방법'`가 모두 `0`이다 (D-63).
    - `## 1. 학습 목표` 절에 `구현할 수 있다`·`튜닝할 수 있다`·`비교 측정할 수 있다` 문자열이 0건이다 (D-62).
    - 코드 블록 바로 앞 5줄 안에 `실행할 필요는 없습니다` 취지의 인용문(`>`로 시작하는 줄)이 있다.
    - `## 3. 개념 설명` 구간에 평가 지표를 성격별로 묶은 마크다운 표가 1개 이상 있다.
    - `grep -ciE 'kant'`가 `0`이다.
    - `node -e "const l=require('./.velite/lessons.json');const s=new Set(l.filter(x=>x.hasContent).map(x=>x.slug));process.exit(s.has('3-3-peft-lora-qlora')&&s.has('3-3-tuning-evaluation')?0:1)"`가 exit 0이다.
  </acceptance_criteria>
  <done>3-3 모듈 2편이 개요 깊이로 작성됐고, 결과를 읽고 판단하게 하되 직접 측정을 요구하지 않는다. 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레슨 본문 → 공개 프로덕션 URL | 본문 문자열이 그대로 인터넷에 노출된다 |
| 읽기용 스니펫 → 학습자의 이해 | 실행하지 않는 코드이므로 실행 정확성보다 개념의 옳음이 중요하다 |
| 튜닝 데이터 안내 → 학습자의 실제 데이터 | 사내 데이터로 튜닝하는 습관이 개인정보 유출로 이어질 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | 레슨 본문 — 교육기관명·이메일 | high | mitigate | `node scripts/check-brand.mjs` 상시 검사 + acceptance criteria의 대소문자 무시 grep 0건 |
| T-05-23 | Information Disclosure | 사내·개인 데이터를 튜닝 데이터로 쓰는 습관 | medium | mitigate | ⑤ 실무 팁의 "데이터 품질" 항목에 "학습 데이터에 개인정보가 섞이면 모델이 그것을 기억한다"는 취지를 한 줄 포함시킨다. 3-6 모듈이 PII·거버넌스를 개념으로 다시 짚는다 |
| T-05-22 | Tampering | 개요 스니펫이 검증되지 않은 실무 관행을 사실처럼 제시 | medium | accept | D-63이 실행을 요구하지 않으므로 판단 기준은 개념의 옳음이다. 하이퍼파라미터 권장값·성능 수치를 단정하지 않고, 사례는 "이런 경우가 있다" 수준으로 서술한다 |
| T-05-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 저장소에 패키지를 설치하지 않고, Step 3 레슨은 설치 자체를 안내하지 않는다(D-63). acceptance criteria가 `pip install`·`npm install` 0건을 확인한다 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

- **CONT-05 edge 프로브가 `unclassified → unresolved`로 남아 있다** (SPEC.md 부재, spec-less fallback §A). 이 Plan의 두 레슨은 프로젝트 가이드가 아니므로 "재현 아님" 경계가 직접 걸리지 않는다. 대응되는 실패 모드(`hasContent: true`인데 6단이 덜 찬 레슨)는 구조 게이트가 막지만 프로즈 깊이는 자동 검증 불가다.
- **RESEARCH Pitfall 2의 위험이 이 모듈에서 가장 높다.** 튜닝은 실무에서 깊은 주제라 수식·하이퍼파라미터로 미끄러지기 쉽다. acceptance criteria가 코드 블록 1개·30줄 이하·설치 안내 0건·학습 목표 금지어 0건으로 팽창 경로를 막지만, **산문 깊이 자체는 자동 측정 불가**이며 Plan 01 체크포인트 항목 2에서 승인된 깊이를 기준으로 삼는다.
- **튜닝 도메인 지식이 이 세션에서 검증되지 않았다.** 리서치의 `## Sources` Tertiary가 3-6/3-7의 PII·인젝션 정책을 `[ASSUMED]`로 표시했는데, 3-3의 PEFT/LoRA 설명도 같은 성격이다. D-63이 실행을 요구하지 않으므로 코드 정확성보다 개념의 옳음이 판단 기준이며, **구체 수치·권장 하이퍼파라미터를 단정하지 않는 것**이 이 Plan의 방어 방식이다.
- **`check-manifest.mjs`는 이 wave 동안 의도적으로 red다** — `<manifest_gate_note>` 참조.
</flagged_assumptions>

<verification>
1. `npm run build`
2. `node scripts/check-lesson-structure.mjs` — 두 레슨 모두 오류 목록에 없음
3. `node scripts/check-brand.mjs`
4. `node scripts/check-manifest.mjs`는 **실행하지 않는다** (이 wave 동안 의도적 red, Plan 13이 되돌린다)
</verification>

<success_criteria>
- 3-3 모듈 2편이 개요 깊이로 작성됐고 실행·GPU·데이터셋을 요구하지 않는다 (D-62~D-65)
- "언제 튜닝하지 말아야 하는가"와 "지표 하나만 보면 안 되는 이유"가 본문에 있다
- 두 레슨 모두 6단 헤딩 원문을 유지하고 형식 예외가 없다
- 확대된 구조 게이트와 브랜드 게이트를 통과한다
</success_criteria>

<output>
Create `.planning/phases/05-step-2-3/05-09-SUMMARY.md` when done.

담을 것: 두 레슨의 스니펫 언어·길이, 개념 설명에 쓴 판단 표의 구성, 확정된 해보기 개수, `3-6` 모듈(Plan 12)과 맞춰야 할 평가 용어 목록, 게이트 통과 시점의 `hasContent: true` 총 개수.
</output>
