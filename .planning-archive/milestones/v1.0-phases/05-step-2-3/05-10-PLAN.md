---
phase: 05-step-2-3
plan: 10
type: execute
wave: 4
depends_on: ["05-07"]
files_modified:
  - src/content/lessons/step-3/3-4-multi-agent-structure.mdx
  - src/content/lessons/step-3/3-4-webhook-schedule-hitl.mdx
  - src/content/lessons/step-3/3-4-n8n-langgraph.mdx
autonomous: true
requirements: [CONT-05]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`/lesson/3-4-multi-agent-structure`가 여러 AI가 함께 일하는 구조를 **개요 깊이**로 렌더한다 — `.planning/curriculum.md` §3-4 첫 불릿 범위를 채운다 (D-62)"
    - "`/lesson/3-4-webhook-schedule-hitl`이 Webhook·스케줄·HITL 설계를 개요 깊이로 렌더한다 — §3-4 둘째 불릿 범위를 채운다"
    - "`/lesson/3-4-n8n-langgraph`가 n8n·LangGraph 기반 자동화를 **개요**로 렌더한다 — 제목의 '실습 개요'가 실습 안내가 아니라 개요임을 지킨다. 별도 인스턴스 구축 안내가 0건이다 (D-63)"
    - "세 레슨 모두 코드가 읽기용 스니펫 하나씩뿐이고 '지금 실행할 필요 없습니다' 취지의 안내가 붙어 있으며 설치·실행 명령이 0건이다 (D-63)"
    - "세 레슨의 `### 해보기`가 판단·설계형이다 — 워크플로 도구 설치나 실행을 요구하지 않는다 (D-64)"
    - "세 레슨 모두 6단 헤딩 원문 유지 + `### 해보기` 2~3개 + 해보기 개수 + 2 이상의 접힌 정답 블록 + `**이 레슨의 단어**` 표 5~8행을 갖고 `node scripts/check-lesson-structure.mjs`를 통과한다 (D-65, D-69)"
    - "세 레슨의 프론트매터에서 `hasContent: false → true` 한 줄 외에는 어떤 필드도 바뀌지 않았다 (D-13)"
    - statement: "개요 3편을 읽고 나서 개강 첫 시간에 '오케스트레이션', 'HITL', '멱등성', 'n8n', 'LangGraph'를 들었을 때 무슨 얘기인지 알아들을 수 있다 (D-62)"
      verification: backstop
    - statement: "세 편이 서로 용어와 예시 시나리오를 이어 써서 한 모듈로 읽힌다 — 같은 자동화 시나리오를 구조·트리거·도구 세 각도로 보는 구성이다"
      verification: backstop
  artifacts:
    - src/content/lessons/step-3/3-4-multi-agent-structure.mdx
    - src/content/lessons/step-3/3-4-webhook-schedule-hitl.mdx
    - src/content/lessons/step-3/3-4-n8n-langgraph.mdx
  key_links:
    - "3-4 세 편 → `3-5-project-orchestration`(Plan 11) — 프로젝트 가이드가 이 세 slug를 복습 포인터로 가리킨다. 용어를 일치시켜야 포인터가 실제로 작동한다"
    - "읽기용 스니펫 → D-63 안내 문구 — 안내가 빠지면 학습자가 n8n 인스턴스 구축에 90분을 다 쓴다. Step 3 실습 환경 구축은 CONTEXT.md Deferred로 명시 배제됐다"
    - "이 Plan의 3편 → Plan 13의 매니페스트 실측"
  prohibitions:
    - statement: "레슨 본문·코드 주석·메타데이터 어디에도 교육기관명(`scripts/check-brand.mjs`의 금지 문자열)을 노출해서는 안 된다 (D-02)"
      status: unverified
    - statement: "Step 3 개요 레슨에 설치·실행 안내를 붙이거나 해보기 과제로 실행을 요구해서는 안 된다 — n8n·LangGraph 인스턴스 구축은 90분 예산을 초과해 CONTEXT.md Deferred로 배제됐다 (D-63)"
      status: unverified
    - statement: "개요 레슨의 학습 목표에 `직접 구현할 수 있다`·`직접 튜닝할 수 있다`·`성능을 비교 측정할 수 있다`를 써서는 안 된다 (D-62)"
      status: unverified
    - statement: "`3-4-n8n-langgraph`의 제목에 '실습 개요'가 있다는 이유로 실습 안내를 넣어서는 안 된다 — 제목은 확정값이고 내용은 개요다 (D-13, D-63)"
      status: unverified
    - statement: "6단 헤딩 텍스트를 바꾸거나 단어 표·해보기 개수 규칙을 예외 처리해서는 안 된다 (D-65, D-69)"
      status: unverified
    - statement: "프론트매터의 `hasContent` 외 어떤 필드도 바꿔서는 안 된다 (D-13, D-31)"
      status: unverified
    - statement: "레슨 본문에 실제 API 키·웹훅 URL·토큰 형태의 문자열을 적어서는 안 된다 — 자리표시자만 쓴다 (T-05-01)"
      status: unverified
---

<objective>
Step 3 배치의 3-4 모듈 3편을 집필한다 — 여러 AI가 함께 일하는 구조, Webhook·스케줄·HITL 설계, n8n·LangGraph 자동화 개요.

Purpose: 오케스트레이션 모듈 전체다. 세 편이 **같은 자동화 시나리오를 세 각도**(구조 / 트리거 / 도구)로 보게 구성해 한 모듈로 읽히게 한다. Step 3 실습 환경 구축(n8n·LangGraph 인스턴스)은 CONTEXT.md Deferred로 명시 배제됐으므로, 세 편 모두 읽기용 스니펫만 쓰고 실행을 요구하지 않는다.

Output: 개요 형식 레슨 3편, `hasContent: true` 전환 3건.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-step-2-3/05-CONTEXT.md
@.planning/phases/05-step-2-3/05-PATTERNS.md
@.planning/phases/05-step-2-3/05-01-SUMMARY.md
</context>

<artifacts_produced>
## Artifacts this phase produces

**Plan 10 (이 Plan)이 만드는 것**
- `src/content/lessons/step-3/3-4-multi-agent-structure.mdx` — 본문 신규, `hasContent: false → true`
- `src/content/lessons/step-3/3-4-webhook-schedule-hitl.mdx` — 본문 신규, `hasContent: false → true`
- `src/content/lessons/step-3/3-4-n8n-langgraph.mdx` — 본문 신규, `hasContent: false → true`

**Phase 5 전체가 만들거나 바꾸는 것** — `05-01-PLAN.md` §Artifacts this phase produces 참조.
</artifacts_produced>

<manifest_gate_note>
**이 wave 동안 `node scripts/check-manifest.mjs`는 의도적으로 red다.** `hasContent`를 3건 켜면 실제 개수가 Plan 07이 맞춰 둔 상수를 넘어 Invariant 10이 실패한다. 상수는 Wave 3 마감 Plan 13이 실측으로 되돌린다(D-78). **실행자는 `check-manifest`를 verify에서 제외하고 상수를 손대지 않는다.**
</manifest_gate_note>

<shared_scenario>
세 Task가 **하나의 자동화 시나리오**를 공유한다: "고객 문의 메일이 도착하면 → 내용을 분류하고 → 관련 문서를 찾아 답변 초안을 만들고 → 담당자가 승인하면 → 발송하고 기록한다." Task 1은 이 시나리오를 **누가 무엇을 맡는가**(구조)로, Task 2는 **언제 시작되고 어디서 사람이 개입하는가**(트리거·HITL)로, Task 3은 **무엇으로 만드는가**(도구 선택)로 각각 본다. 세 레슨이 같은 시나리오를 쓰면 학습자가 매번 새 맥락을 세우지 않아도 되고, `3-5-project-orchestration` 준비 가이드가 이 시나리오를 그대로 이어받을 수 있다.
</shared_scenario>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: `3-4-multi-agent-structure` 집필 — 여러 AI가 함께 일하는 구조 (개요 깊이)</name>
  <files>src/content/lessons/step-3/3-4-multi-agent-structure.mdx</files>
  <read_first>
    - `src/content/lessons/step-3/3-4-multi-agent-structure.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "여러 AI가 함께 일하는 구조"`, `stepId: 3`, `moduleId: "3-4"`, `order: 1`, `depth: "개요"`, `estimatedMinutes: 90`, `hasContent: false`)을 확인한다.
    - `src/content/lessons/step-3/3-1-vector-search-basics.mdx` (Plan 01 완료본) — **개요 형식의 승인된 실물.** 학습 목표 3요소 형식, 읽기용 스니펫 안내 문구, 판단형 해보기 문장을 그대로 가져온다. 전체를 읽는다.
    - `.planning/phases/05-step-2-3/05-01-SUMMARY.md` — 체크포인트 항목 2의 Step 3 깊이 판정 결과와 조정 사항.
    - `.planning/curriculum.md` §Step 3 §3-4 첫 불릿("여러 AI가 함께 일하는 구조 설계") — 이 레슨의 범위 원천.
    - 이 Plan의 `<shared_scenario>` — 세 레슨이 공유할 자동화 시나리오.
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩은 아래 6개뿐이며 문자열과 순서를 그대로 쓴다: `## 1. 학습 목표`, `## 2. 왜 배우나`, `## 3. 개념 설명`, `## 4. 실무 예제`, `## 5. 실무 팁`, `## 6. 핵심 정리 및 스스로 점검`(D-65).

- **① 학습 목표** — "알아듣습니다" 형태 3~4개, 각 항목이 3요소 형식 중 하나. `직접 구현할 수 있다`류 금지(D-62).
- **② 왜 배우나** — 최대 4문장. 하나의 큰 프롬프트로 다 시키면 왜 실패가 잦아지는지, 그리고 개강 후 프로젝트 4·5가 이 구조 위에서 돌아간다는 자리매김.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 비유 먼저 정의 뒤. 다룰 것: 단일 에이전트의 한계(한 사람이 기획·집필·검수를 다 하면 생기는 일), 역할 분담 패턴(계획 세우는 쪽·실행하는 쪽·검수하는 쪽 — 비유: 편집국의 데스크·기자·교열), 연결 방식 3종을 표로(순차 / 병렬 / 감독자가 배분, 각각 언제 쓰나), 상태 공유(에이전트들이 무엇을 어떻게 주고받는가)와 무한 루프·비용 폭주 위험. `<shared_scenario>` 시나리오를 `문의 도착 → 분류 담당 → 검색 담당 → 초안 담당 → 검수 담당 → 승인 → 발송` 한 줄 도식으로 그린다(D-48 — 다이어그램 플러그인·이미지 없음).
- **④ 실무 예제** — `### 실무에서 이게 어떻게 쓰이나` 소제목으로 시작한다. `<shared_scenario>`를 실제 서비스 사례로 2~3단락 서술한다. 그다음 인용문(`>`) 두 줄로 "지금 실행할 필요 없습니다" 취지의 안내(D-63, Plan 01과 같은 문구). 그 아래 `python` 펜스 **하나**에 그래프 형태로 노드와 연결을 선언하는 10~20줄 스니펫을 넣는다 — 각 줄이 무엇을 뜻하는지 주석 한 줄씩. 코드 블록은 이 하나뿐이며 30줄을 넘기지 않는다. **설치·실행 안내를 붙이지 않는다.**
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 역할을 늘릴수록 비용과 지연이 곱해진다, 각 단계의 출력 형식을 먼저 정해야 다음 단계가 안 깨진다(2-7 구조화 출력과 연결), 루프에는 반드시 최대 반복 횟수를 둔다.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표 5~8행(에이전트, 오케스트레이션, 감독자, 노드, 상태, 도구 호출, 루프 제한 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.
- **`### 해보기` 2~3개는 판단·설계형**(D-64). 소재: `<shared_scenario>`를 역할 3~5개로 직접 쪼개고 각 역할의 입력·출력을 표로 적기, 순차·병렬·감독자 중 어느 연결이 맞는지 고르고 이유 쓰기, 이 구조가 무한 루프에 빠질 수 있는 지점을 하나 찾아 막을 방법 적기. 각 해보기 아래 `<details>` 정답은 "모범 답안 + 왜 그렇게 보는가".

90분 = 읽기 약 40분 + 판단형 해보기 약 50분(D-64). 용어 표기는 D-50을 따른다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `3-4-multi-agent-structure`가 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent: false` → `hasContent: true` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`이고 순서가 1→6이며 `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며 빈 줄 규칙을 지킨다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - `grep -c '^```'`가 `2`다 (코드 블록 정확히 하나), 언어 태그가 `python`이며 본문이 30줄 이하다.
    - `grep -c 'pip install'`·`grep -c 'npm install'`·`grep -ci '실행 방법'`가 모두 `0`이다 (D-63).
    - `## 1. 학습 목표` 절에 `구현할 수 있다`·`튜닝할 수 있다`·`비교 측정할 수 있다` 문자열이 0건이다 (D-62).
    - 코드 블록 바로 앞 5줄 안에 `실행할 필요는 없습니다` 취지의 인용문(`>`로 시작하는 줄)이 있다.
    - `## 3. 개념 설명` 구간에 연결 방식 3종을 정리한 마크다운 표가 1개 이상 있다.
    - `grep -ciE 'kant'`가 `0`이고 `grep -c 'sk-ant-'`가 `0`이다.
    - `node -e "const l=require('./.velite/lessons.json');const p=l.find(x=>x.slug==='3-4-multi-agent-structure');process.exit(/language-python/.test(p.code)?0:1)"`가 exit 0이다.
  </acceptance_criteria>
  <done>3-4 첫 편이 개요 깊이로 작성됐고 공유 시나리오를 구조 관점으로 다룬다. 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: `3-4-webhook-schedule-hitl` 집필 — Webhook·스케줄·HITL 설계 (개요 깊이)</name>
  <files>src/content/lessons/step-3/3-4-webhook-schedule-hitl.mdx</files>
  <read_first>
    - `src/content/lessons/step-3/3-4-webhook-schedule-hitl.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "Webhook·스케줄·HITL 설계"`, `moduleId: "3-4"`, `order: 2`, `depth: "개요"`, `estimatedMinutes: 90`)을 확인한다.
    - `src/content/lessons/step-3/3-4-multi-agent-structure.mdx` (Task 1 완료본) — 같은 모듈 앞 편. 공유 시나리오와 용어(에이전트·역할·상태)를 이어 쓰고 이미 설명한 것을 반복하지 않는다.
    - `src/content/lessons/step-2/2-5-express-rest-api.mdx` (Plan 04 완료본) — 학습자가 이미 HTTP 메서드·상태 코드를 손으로 다뤄 봤다. Webhook을 "남이 내 엔드포인트를 호출하는 것"으로 그 경험에 붙인다.
    - `.planning/curriculum.md` §Step 3 §3-4 둘째 불릿("Webhook, 스케줄, HITL 설계") — 이 레슨의 범위 원천.
    - 이 Plan의 `<shared_scenario>`.
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다. `##` 헤딩 6개는 Task 1과 동일한 문자열·순서.

- **① 학습 목표** — "알아듣습니다" 형태 3~4개, 3요소 형식. 금지 목표 없음(D-62).
- **② 왜 배우나** — 최대 4문장. 자동화는 "무엇을 하는가"보다 "언제 시작하고 어디서 멈추는가"가 더 자주 문제가 된다는 점, 그리고 사람이 개입하는 지점을 설계에서 빼면 나중에 못 끼워 넣는다는 점.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 다룰 것: 트리거 3종을 표로(이벤트 = Webhook / 시간 = 스케줄 / 사람 = 수동·승인, 각각 언제 쓰나), Webhook(남의 서비스가 내 주소로 알림을 보내 주는 것 — 비유: 택배 도착 문자)과 2-5에서 만든 엔드포인트와의 연결, 스케줄(주기 표현식의 개념만, 문법 암기 요구 없음), HITL(Human In The Loop, 사람이 중간에 승인·수정하는 지점 — 비유: 결재 라인)과 대기 상태를 어디에 저장하나, 재시도·멱등성(같은 알림이 두 번 와도 결과가 한 번만 반영되게 — 비유: 같은 주문서를 두 번 받아도 물건은 한 번만 보내기). `<shared_scenario>`를 `메일 도착(Webhook) → 처리 → 담당자 승인 대기(HITL) → 발송 / 매일 아침 미처리 건 재확인(스케줄)` 한 줄 도식으로 그린다(D-48).
- **④ 실무 예제** — `### 실무에서 이게 어떻게 쓰이나` 소제목으로 시작한다. 공유 시나리오를 실제 서비스 사례로 2~3단락 서술하되, **트리거와 승인 지점**에 초점을 둔다. 그다음 인용문(`>`) 두 줄로 "지금 실행할 필요 없습니다" 안내(D-63). 그 아래 `typescript` 펜스 **하나**에 Webhook 수신 핸들러의 뼈대 10~20줄 스니펫 — 서명 검증 자리, 중복 알림 식별자 확인 자리, 처리 큐에 넣고 즉시 200 응답하는 흐름을 주석으로 표시한다. 코드 블록은 이 하나뿐이며 30줄 이하. 웹훅 URL·토큰은 자리표시자로만 쓴다. **설치·실행 안내를 붙이지 않는다.**
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 반드시 포함: Webhook은 누구나 호출할 수 있으므로 서명·토큰으로 발신자를 확인한다, 같은 알림이 여러 번 올 수 있으니 식별자로 중복을 걸러 낸다, 승인 대기가 영원히 남지 않도록 만료를 정한다.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표 5~8행(Webhook, 트리거, 스케줄, HITL, 멱등성, 재시도, 큐 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.
- **`### 해보기` 2~3개는 판단·설계형**(D-64). 소재: 자동화하고 싶은 업무 하나를 골라 어떤 트리거가 맞는지 고르고 이유 쓰기, 공유 시나리오에서 사람이 반드시 개입해야 하는 지점을 찾고 그 이유와 대기 만료 시간을 정하기, 같은 알림이 두 번 왔을 때 무슨 일이 생기는지 적고 막을 방법 쓰기. 각 해보기 아래 `<details>` 정답은 "모범 답안 + 왜 그렇게 보는가".

90분 = 읽기 약 40분 + 판단형 해보기 약 50분(D-64).
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `3-4-webhook-schedule-hitl`이 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent` 한 줄뿐이다.
    - `grep -c '^## '`가 `6`, `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며 빈 줄 규칙을 지킨다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - `grep -c '^```'`가 `2`다 (코드 블록 정확히 하나), 언어 태그가 `typescript`이며 본문이 30줄 이하다.
    - `grep -c 'npm install'`·`grep -ci '실행 방법'`가 모두 `0`이다 (D-63).
    - `## 1. 학습 목표` 절에 `구현할 수 있다`·`튜닝할 수 있다`·`비교 측정할 수 있다` 문자열이 0건이다.
    - 코드 블록 바로 앞 5줄 안에 `실행할 필요는 없습니다` 취지의 인용문이 있다.
    - `## 3. 개념 설명` 구간에 트리거 3종을 정리한 마크다운 표가 1개 이상 있다.
    - `grep -cE 'https://hooks\.[a-z]|https://[a-z0-9.-]+/webhook/[A-Za-z0-9]{8}'`가 `0`이다 (실제 웹훅 URL 없음).
    - `grep -ciE '멱등'`가 `2` 이상이다 (⑤ 실무 팁과 ③ 개념 설명 양쪽에 나온다).
    - `grep -ciE 'kant'`가 `0`이다.
    - `node -e "const l=require('./.velite/lessons.json');const p=l.find(x=>x.slug==='3-4-webhook-schedule-hitl');process.exit(/language-typescript/.test(p.code)?0:1)"`가 exit 0이다.
  </acceptance_criteria>
  <done>3-4 둘째 편이 개요 깊이로 작성됐고 공유 시나리오를 트리거·승인 관점으로 다룬다. 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: `3-4-n8n-langgraph` 집필 — n8n·LangGraph 자동화 개요 (인스턴스 구축 안내 없음)</name>
  <files>src/content/lessons/step-3/3-4-n8n-langgraph.mdx</files>
  <read_first>
    - `src/content/lessons/step-3/3-4-n8n-langgraph.mdx` — 스텁 현재 상태. 프론트매터 8줄(`title: "n8n·LangGraph 자동화 실습 개요"`, `moduleId: "3-4"`, `order: 3`, `depth: "개요"`, `estimatedMinutes: 90`)을 확인한다. **제목에 "실습 개요"가 들어 있지만 `depth`는 `개요`이고 제목은 확정값이다 — 실습 안내를 넣으라는 뜻이 아니다.**
    - `src/content/lessons/step-3/3-4-multi-agent-structure.mdx`와 `3-4-webhook-schedule-hitl.mdx` (Task 1·2 완료본) — 같은 모듈 앞 두 편. 공유 시나리오와 용어를 이어 쓰고 구조·트리거 설명을 반복하지 않는다.
    - `.planning/phases/05-step-2-3/05-CONTEXT.md` `<deferred>` — "Step 3 실습 환경(벡터DB·n8n) 실제 구축 안내"가 90분 예산 초과로 명시 배제됐다는 근거.
    - `.planning/curriculum.md` §Step 3 §3-4 셋째 불릿("n8n, LangGraph 기반 자동화 실습") — 이 레슨의 범위 원천. 본 과정에서 실습하는 것이지 사전학습에서 하는 것이 아니다.
  </read_first>
  <action>
프론트매터에서 `hasContent: false`를 `true`로 바꾼다. 나머지 7개 필드는 손대지 않는다 — **특히 `title`의 "실습 개요"를 고치지 않는다.** `##` 헤딩 6개는 Task 1과 동일한 문자열·순서.

이 레슨의 성격을 ② 왜 배우나에서 한 줄로 분명히 한다: **이 편은 도구를 설치해 돌려 보는 편이 아니라, 개강 후 실습할 두 도구가 무엇이고 언제 어느 쪽을 고르는지 미리 알아 두는 편이다.**

- **① 학습 목표** — "알아듣습니다" 형태 3~4개, 3요소 형식. 예: "n8n과 LangGraph가 각각 무엇인지 한 문장으로 말할 수 있다", "어떤 자동화에 노코드가 맞고 어떤 자동화에 코드가 맞는지 판단 기준을 말할 수 있다", "두 도구가 전체 자동화 파이프라인에서 어디에 놓이는지 안다". `직접 구현할 수 있다`류 금지(D-62).
- **② 왜 배우나** — 최대 4문장. 개강 후 이 두 도구로 실제 실습을 하는데, 도구 이름과 성격을 그때 처음 만나면 배울 게 두 배가 된다는 점. 그리고 **지금은 설치하지 않는다**는 점을 분명히 한다.
- **③ 개념 설명** — `###` 소제목 3~4개, 각 앞에 이모지 하나. 다룰 것: 노코드 워크플로 도구(n8n — 화면에서 블록을 이어 붙여 만드는 방식, 비유: 레고 블록)와 코드 프레임워크(LangGraph — 코드로 그래프를 선언하는 방식, 비유: 도면 그리기)의 성격 차이, 공통 개념인 노드와 연결(둘 다 "무엇을 하는 상자"와 "다음으로 가는 선"으로 이뤄진다는 점 — 이것이 Task 1의 구조 개념과 같다는 연결), **언제 어느 쪽을 고르는가**를 판단 표로(변경이 잦고 비개발자도 손대야 하면 노코드 / 조건 분기가 복잡하고 테스트가 필요하면 코드 / 둘을 섞는 경우), 그리고 어느 쪽이든 버전 관리와 실패 처리가 필요하다는 점. 자동화 흐름은 `트리거 → 노드 여러 개 → 분기 → 사람 승인 → 종료` 한 줄 도식으로 그린다(D-48).
- **④ 실무 예제** — `### 실무에서 이게 어떻게 쓰이나` 소제목으로 시작한다. 공유 시나리오를 두 도구로 각각 만들면 어떻게 달라지는지 2~3단락으로 대비해 서술한다. 그다음 인용문(`>`) 두 줄로 "지금 실행할 필요 없습니다 / 개강 후 본 과정에서 실제로 돌려보게 됩니다" 안내(D-63). 그 아래 `json` 펜스 **하나**에 노코드 워크플로의 노드·연결 구조를 담은 10~20줄 발췌를 넣는다 — 각 노드가 무엇인지 알아볼 수 있는 최소 형태. 코드 블록은 이 하나뿐이며 30줄 이하. **설치 명령·인스턴스 구축 안내·계정 생성 안내를 붙이지 않는다.**
- **⑤ 실무 팁** — 불릿 3~4개, 각 2문장 이내. 노코드도 결국 버전 관리가 필요하다(화면에서 고친 것을 기록으로 남기기), 워크플로가 커지면 하나를 여러 개로 쪼갠다, 도구가 무엇이든 실패했을 때 어디까지 됐는지 알 수 있어야 한다.
- **⑥ 핵심 정리 및 스스로 점검** — `**핵심 정리**` 불릿 4~5개 → `**이 레슨의 단어**` 표 5~8행(노코드, 워크플로, 노드, 연결, 그래프, 분기, 실행 이력 등) → `**스스로 점검**` 질문 2개 + 접힌 정답 블록.
- **`### 해보기` 2~3개는 판단·설계형**(D-64). **도구 설치나 계정 생성을 요구하지 않는다.** 소재: 자동화 후보 업무 3개를 주고 각각 노코드·코드 중 무엇이 맞는지 고르고 이유 적기, 공유 시나리오를 노드 6~8개로 쪼개 표로 적기(각 노드의 입력·출력), 두 도구의 성격을 대조표로 직접 채워 보기. 각 해보기 아래 `<details>` 정답은 "모범 답안 + 왜 그렇게 보는가".

90분 = 읽기 약 40분 + 판단형 해보기 약 50분(D-64). 용어 표기는 D-50을 따른다.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; node scripts/check-lesson-structure.mjs &amp;&amp; node scripts/check-brand.mjs</automated>
  </verify>
  <acceptance_criteria>
    - 세 게이트 명령이 모두 exit 0이고 `check-lesson-structure` 오류 목록에 `3-4-n8n-langgraph`가 나타나지 않는다.
    - `git diff`의 프론트매터 변경이 `hasContent` 한 줄뿐이다 — `title`이 diff에 나타나지 않는다.
    - `grep -c '^## '`가 `6`, `grep -c '^### 해보기$'`가 `2` 또는 `3`이다.
    - `<details>` · `<summary>정답 보기</summary>` · `</details>` 개수가 서로 같고 `### 해보기` 개수 + 2 이상이며 빈 줄 규칙을 지킨다.
    - `grep -c '^\*\*이 레슨의 단어\*\*$'`가 `1`이고 표 데이터 행이 5~8개다.
    - `grep -c '^```'`가 `2`다 (코드 블록 정확히 하나), 언어 태그가 `json`이며 본문이 30줄 이하다.
    - `grep -c 'npm install'`·`grep -c 'pip install'`·`grep -c 'docker'`·`grep -ci '설치하세요'`·`grep -ci '계정을 만드'`가 모두 `0`이다 (D-63, CONTEXT.md Deferred — 인스턴스 구축·계정 생성 안내 없음).
    - `## 1. 학습 목표` 절에 `구현할 수 있다`·`튜닝할 수 있다`·`비교 측정할 수 있다` 문자열이 0건이다.
    - 코드 블록 바로 앞 5줄 안에 `실행할 필요는 없습니다` 취지의 인용문이 있다.
    - `## 3. 개념 설명` 구간에 노코드·코드 선택 기준을 정리한 마크다운 표가 1개 이상 있다.
    - `## 2. 왜 배우나` 절에 지금은 설치하지 않는다는 취지의 문장이 있다.
    - `grep -ciE 'kant'`가 `0`이다.
    - `node -e "const l=require('./.velite/lessons.json');const s=new Set(l.filter(x=>x.hasContent).map(x=>x.slug));process.exit(['3-4-multi-agent-structure','3-4-webhook-schedule-hitl','3-4-n8n-langgraph'].every(x=>s.has(x))?0:1)"`가 exit 0이다.
  </acceptance_criteria>
  <done>3-4 모듈 3편이 모두 개요 깊이로 작성됐고, 제목의 "실습 개요"에도 불구하고 도구 설치·계정 생성 안내가 0건이다. 세 편이 하나의 공유 시나리오를 세 각도로 다룬다. 구조 게이트를 통과한다. 커밋 완료.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 레슨 본문 → 공개 프로덕션 URL | 본문 문자열이 그대로 인터넷에 노출된다 |
| Webhook 개념 → 학습자가 나중에 만드는 수신 엔드포인트 | 서명 검증 없는 예제는 그대로 공개 엔드포인트가 된다 |
| 읽기용 스니펫 → 학습자의 이해 | 실행하지 않는 코드이므로 개념의 옳음이 판단 기준이다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | 레슨 본문 — 교육기관명·이메일·웹훅 URL·토큰 | high | mitigate | `node scripts/check-brand.mjs` 상시 검사 + acceptance criteria의 대소문자 무시 grep 0건 + 실제 웹훅 URL 정규식 0건. 자리표시자만 쓴다 |
| T-05-24 | Spoofing | 발신자 확인 없는 Webhook 수신 습관 | high | mitigate | Task 2의 스니펫에 서명 검증 자리를 주석으로 명시하고, ⑤ 실무 팁에 "누구나 호출할 수 있으므로 서명·토큰으로 확인한다"를 필수 항목으로 넣는다 |
| T-05-25 | Tampering | 중복 알림으로 같은 작업이 두 번 실행 | medium | mitigate | 멱등성을 ③ 개념 설명과 ⑤ 실무 팁 양쪽에 넣고, acceptance criteria가 해당 용어 2회 이상 등장을 확인한다. 해보기 과제 하나가 이 상황을 직접 다룬다 |
| T-05-26 | Denial of Service | 무한 루프·비용 폭주하는 에이전트 구조 | medium | mitigate | Task 1의 ⑤ 실무 팁에 "루프에는 반드시 최대 반복 횟수를 둔다"를 필수 항목으로 넣고, 해보기 과제 하나가 루프 위험 지점을 찾게 한다 |
| T-05-SC | Tampering | npm/pip/cargo 설치 | low | accept | 이 Plan은 저장소에 패키지를 설치하지 않고, 레슨도 설치·인스턴스 구축을 안내하지 않는다(D-63). acceptance criteria가 설치·컨테이너 명령 0건을 확인한다 |
</threat_model>

<flagged_assumptions>
## Flagged Assumptions

- **CONT-05 edge 프로브가 `unclassified → unresolved`로 남아 있다** (SPEC.md 부재, spec-less fallback §A). 이 Plan의 세 레슨은 프로젝트 가이드가 아니지만, `3-5-project-orchestration`(Plan 11) 준비 가이드가 이 세 편의 용어와 공유 시나리오를 그대로 이어받으므로 **여기서 용어가 흔들리면 프로젝트 가이드의 복습 포인터가 무의미해진다.** `<shared_scenario>`가 그 방어 장치다.
- **RESEARCH Pitfall 2 — 개요 팽창 위험.** acceptance criteria가 코드 블록 1개·30줄 이하·설치 안내 0건·학습 목표 금지어 0건으로 팽창 경로를 막는다. 산문 깊이는 자동 측정 불가이며 Plan 01 체크포인트 항목 2에서 승인된 깊이를 기준으로 삼는다.
- **`3-4-n8n-langgraph`의 제목이 "실습 개요"라 오해 소지가 있다.** 제목은 Phase 1에서 확정된 불변값(D-13)이고 `depth`는 `개요`다. 이 Plan은 제목을 고치지 않고, ② 왜 배우나에 "지금은 설치하지 않는다"를 명시하고 acceptance criteria로 설치·계정 생성 안내 0건을 강제해 오해를 막는다.
- **오케스트레이션 생태계는 빠르게 변한다.** 리서치가 "D-63이 실행 코드를 요구하지 않으므로 콘텐츠 수명에 미치는 영향은 제한적"이라고 판단했다. 이 Plan은 도구별 구체 버전·화면 구성·API 표면을 단정하지 않고 성격과 선택 기준만 다뤄 그 판단을 따른다.
- **`check-manifest.mjs`는 이 wave 동안 의도적으로 red다** — `<manifest_gate_note>` 참조.
</flagged_assumptions>

<verification>
1. `npm run build`
2. `node scripts/check-lesson-structure.mjs` — 세 레슨 모두 오류 목록에 없음
3. `node scripts/check-brand.mjs`
4. `node scripts/check-manifest.mjs`는 **실행하지 않는다** (이 wave 동안 의도적 red, Plan 13이 되돌린다)
</verification>

<success_criteria>
- 3-4 모듈 3편이 개요 깊이로 작성됐고 도구 설치·인스턴스 구축 안내가 0건이다 (D-62~D-65, CONTEXT.md Deferred)
- 세 편이 하나의 공유 시나리오를 구조·트리거·도구 세 각도로 다뤄 한 모듈로 읽힌다
- Webhook 서명 검증과 멱등성이 개념·실무 팁 양쪽에 있다
- 확대된 구조 게이트와 브랜드 게이트를 통과한다
</success_criteria>

<output>
Create `.planning/phases/05-step-2-3/05-10-SUMMARY.md` when done.

담을 것: 실제로 쓴 공유 시나리오 문장(Plan 11의 `3-5-project-orchestration`이 그대로 이어받는다), 세 레슨의 스니펫 언어(`python`/`typescript`/`json`)와 길이, 세 편에서 확정된 용어 목록, 확정된 해보기 개수, 게이트 통과 시점의 `hasContent: true` 총 개수.
</output>
