---
phase: quick-260901-x62
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260901140000_create_inbox_item.sql
  - src/lib/inbox-store.ts
  - src/app/inbox-actions.ts
  - src/app/inbox/page.tsx
  - src/components/inbox-panel.tsx
  - src/lib/note-store.ts
  - src/app/notes/page.tsx
  - src/components/site-nav.tsx
  - scripts/check-route-rendering.mjs
autonomous: false
requirements:
  - notes-digest        # FINAL-REPORT 2단 "내 노트 모아보기(단권화)" (candidates B4)
  - question-inbox      # FINAL-REPORT 2단 "궁금한 것 인박스" (Matuschak writing inbox + 클로드 연결)

estimate:
  tokens: 68000
  raw_tokens: 42000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "/notes에서 잠금 해제하면, 노트를 적어둔 레슨들이 커리큘럼(모듈) 순서로 한 페이지에 모여 보이고 각 카드에서 그 레슨으로 이동할 수 있다"
    - "노트가 하나도 없으면 /notes는 빈 상태 안내를 보여준다"
    - "/inbox에서 새 질문을 적어 추가하면 목록에 나타나고 새로고침 후에도 남는다(Supabase 저장 — 기기 간 공유)"
    - "/inbox에서 질문의 완료(done)를 토글할 수 있고, 미완료가 먼저·최신순으로 정렬된다"
    - "/inbox의 각 질문에서 '클로드에 물어보기'로 질문을 클립보드에 복사할 수 있다"
    - "잠금 해제 전에는 /notes·/inbox 모두 노트/질문 데이터를 보여주지 않는다"
    - "내비에서 노트·질문함에 도달할 수 있다"
  artifacts:
    - supabase/migrations/20260901140000_create_inbox_item.sql
    - src/lib/inbox-store.ts
    - src/app/inbox-actions.ts
    - src/app/inbox/page.tsx
    - src/components/inbox-panel.tsx
    - src/app/notes/page.tsx
    - src/lib/note-store.ts
    - src/components/site-nav.tsx
    - scripts/check-route-rendering.mjs
  key_links:
    - "inbox-actions 모든 함수가 hasUnlockCookie() 재검증 → 입력 검증 → inbox-store 순서(위조 POST 방어, review-actions와 동형)"
    - "inbox-store → supabaseAdmin(service_role) 단일 접근 경로 (RLS 우회, default-deny 테이블)"
    - "/notes: readAllLessonNotes() Map ↔ getOrderedLessons() 조인 키가 lesson_id=slug로 일치"
    - "/notes·/inbox force-dynamic ↔ check-route-rendering.mjs EXPECTED_DYNAMIC_ROUTES 계약"
---

<objective>
연구 2단의 수집 그룹 2건을 붙인다: (1) 전 레슨 메모를 커리큘럼 순서로 한 페이지에 모아 보는 **내 노트 단권화 `/notes`**(읽기 전용 집계), (2) 곁가지 질문 전용 전역 리스트 + 클로드 연결인 **궁금한 것 인박스 `/inbox`**(추가·완료 토글·클립보드 복사).

두 페이지 모두 `/review`가 이미 증명한 아키텍처를 그대로 복제한다 — force-dynamic + hasUnlockCookie 게이트 → service_role(admin) 접근. 새 라이브러리·새 인증 경로 없음(그래서 tracer-first를 생략한다: round 260901-w04 /review가 같은 계층을 이미 관통 검증했고 얇은 슬라이스가 추가 정보를 주지 않는다).

Purpose: 마지막 주 "모듈별 내 요약 만들기" 슬롯과 세트인 단권화, 그리고 학습 곁가지 질문을 잃지 않고 클로드로 이어가는 인박스 — 사전학습 흐름의 마지막 수집 도구.
Output: /notes·/inbox 라우트, inbox 데이터 계층·서버 액션, note-store 전체 읽기 헬퍼, 내비·라우트 게이트 배선, inbox 마이그레이션 파일.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/CLAUDE.md

# 서버 액션 보안 순서의 정본 패턴 — 그대로 복제
@src/app/review-actions.ts
@src/lib/review-store.ts
@src/lib/supabase/admin.ts
@src/lib/auth.ts

# force-dynamic + 잠금 게이트 + EmptyState 페이지 패턴
@src/app/review/page.tsx

# 정적/수집 페이지 디자인(크림 종이·.panel/.chip) + 노트/헬퍼
@src/app/glossary/page.tsx
@src/lib/note-store.ts
@src/content/curriculum-helpers.ts
@src/content/modules.ts

# 클립보드 복사 패턴(클로드 연결) — navigator.clipboard 제스처 규약
@src/components/lesson-copy-prompt.tsx

# 배선 지점
@src/components/site-nav.tsx
@scripts/check-route-rendering.mjs

# 마이그레이션 헤더 스타일(RLS on·정책 0개=의도된 default-deny)
@supabase/migrations/20260901120000_create_lesson_review.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: 인박스 데이터 계층 — 마이그레이션 + inbox-store + inbox-actions</name>
  <files>supabase/migrations/20260901140000_create_inbox_item.sql, src/lib/inbox-store.ts, src/app/inbox-actions.ts</files>
  <precondition>라이브 Supabase에 public.inbox_item 테이블이 이미 존재한다(오케스트레이터가 생성). 이 태스크의 마이그레이션은 그 테이블과 일치하는 idempotent 기록용 파일이다.</precondition>
  <action>
  마이그레이션(20260901140000_create_inbox_item.sql): 라이브 테이블과 정확히 일치하는 idempotent 파일을 만든다 — public.inbox_item, 컬럼은 id uuid primary key default gen_random_uuid(), body text not null, lesson_id text(nullable), created_at timestamptz default now(), done boolean default false. `create table if not exists`로 쓰고 이어서 `alter table ... enable row level security`. lesson_review 마이그레이션과 같은 헤더 주석 원칙을 담는다: RLS는 켜되 정책(create policy)은 하나도 만들지 않으며 이것은 버그가 아니라 의도된 default-deny다 — 이 앱은 D-17에 따라 Supabase Auth를 쓰지 않고 service_role(admin.ts)로만 접근하므로 정책 0개가 정상 동작이다. `using (true)` 편의 정책을 추가하지 말라는 경고를 넣는다. 라이브엔 이미 적용됨을 명시.

  inbox-store.ts(server-only, supabaseAdmin 단일 접근 — review-store와 동형): 세 함수를 export한다. readInboxItems(): inbox_item에서 id, body, lesson_id, created_at, done을 select하고 미완료(done=false) 먼저·그 안에서 created_at 내림차순으로 정렬해 반환(order('done', ascending true) 후 order('created_at', descending) 또는 클라 정렬). ok/error 판별 유니온으로 반환({ ok: true, items: InboxItem[] } | { ok: false, error }). InboxItem 타입 export(id, body, lessonId, createdAt, done). addInboxItem(body, lessonId?): trim한 body가 비었거나 MAX_INBOX_LENGTH(5000) 초과면 한국어 오류를 throw, 아니면 insert. setInboxItemDone(id, done): update done where id. 실패 시 review-store처럼 문맥이 담긴 오류 throw. localStorage 아님 — Supabase 저장이 기기 간 공유의 핵심.

  inbox-actions.ts('use server' — review-actions.ts의 순서 계약을 복제): addInboxQuestion(body, lessonId?)와 toggleInboxDone(id, done) 두 서버 액션. 본문 순서가 보안 계약이다 — 각 함수는 무조건, 어떤 store 호출보다도 먼저 hasUnlockCookie()를 재검증하고 실패 시 throw('unauthorized')한다(Server Action은 컴파일된 POST라 캡처된 요청이 재전송될 수 있다). 그 다음 입력 검증: addInboxQuestion은 body가 문자열·trim 후 비지 않음·길이 상한 이내인지 확인하고, lessonId가 주어지면 getLessonBySlug로 존재 검증(없으면 throw 또는 null 취급 — throw 채택)한 뒤에만 addInboxItem 호출. toggleInboxDone은 id가 문자열 uuid 형태(정규식 또는 최소 길이 검증)이고 done이 boolean인지 확인한 뒤에만 setInboxItemDone 호출. 모든 검증 통과 후에만 store를 호출.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
  </verify>
  <done>tsc·lint 통과. 마이그레이션 파일이 라이브 스키마와 일치(create table if not exists + RLS on, 정책 0개). inbox-store 3함수·inbox-actions 2액션이 존재하고, 액션은 store 호출 전에 hasUnlockCookie() → 입력 검증 순서를 지킨다.</done>
</task>

<task type="auto">
  <name>Task 2: /inbox 페이지 + 클라이언트 상호작용(추가·완료 토글·클로드 복사)</name>
  <files>src/app/inbox/page.tsx, src/components/inbox-panel.tsx</files>
  <action>
  inbox/page.tsx(force-dynamic — /review/page.tsx 조립 순서 복제): 파일 상단에 `export const dynamic = "force-dynamic"`. 무조건 먼저 hasUnlockCookie()를 호출하고, 잠금 안 됐으면 /review와 동일한 EmptyState 컴포넌트로 "질문함은 잠금 해제 후 이용할 수 있어요. 홈에서 잠금을 해제해 주세요."를 렌더. 잠금됐으면 readInboxItems()를 호출하고 실패 시 EmptyState로 오류 안내, 성공 시 items를 inbox-panel에 prop으로 넘긴다. 페이지 셸은 glossary/review와 같은 크림 종이 레이아웃(main.max-w-3xl, panel-hero 헤더에 제목 "질문함"과 한 줄 설명: 공부하다 곁가지로 떠오른 질문을 여기 담아두고, 클로드에 물어볼 때 꺼내 쓴다는 취지). 인박스는 곁가지 질문 전용 전역 리스트이므로 이 페이지의 추가 폼은 lesson_id 없이(null) 항목을 만든다 — 레슨 페이지 진입점은 이번 범위에서 제외(최소 /inbox 필수 원칙, lesson_id 컬럼은 향후 진입점 대비 존재만).

  inbox-panel.tsx('use client'): props로 InboxItem[] 목록을 받는다. (1) 추가 폼 — textarea + 제출 버튼, 제출 시 addInboxQuestion(body) 서버 액션을 호출하고 성공하면 textarea를 비우고 next/navigation의 useRouter().refresh()로 force-dynamic 서버 페이지를 다시 읽어 목록을 갱신(단일 사용자 전제라 낙관적 UI 없이 refresh로 충분). 저장 실패 시 textarea 내용은 그대로 두고 한국어 실패 문구를 role=status/aria-live로 알린다(lesson-notepad의 실패 처리 원칙). 한글 IME 안전을 위해 onChange 핸들러에서는 값 검사 로직을 넣지 않는다. (2) 목록 — 각 항목을 .panel 카드로: 질문 본문, 완료 체크(버튼 또는 체크박스)로 toggleInboxDone(id, !done) 호출 후 refresh, 완료 항목은 시각적으로 흐리게(muted) 처리. (3) 각 항목에 "클로드에 물어보기" 버튼 — lesson-copy-prompt의 navigator.clipboard.writeText 규약(사용자 제스처 안에서 앞 await 없이 곧바로 호출, iPad Safari 제스처 컨텍스트 보존)을 그대로 따르고, 복사 내용은 질문 본문 + 클로드에게 건네는 짧은 한 줄 안내(레슨 전용 TEACHER_BRIEF 전체가 아니라 이 곁가지 질문 하나에 맞춘 가벼운 프레이밍). 복사 성공/실패를 아이콘 말고 글자로도 role=status로 알린다. 모든 상호작용 요소는 min-h-11(44px+) 터치 타깃, 아이패드 우선. ProgressProvider·useProgress는 쓰지 않는다(진도 불필요).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <human-check>내장 브라우저를 아이패드 뷰포트 크기로 열어 /inbox 잠금 해제 후: 질문 추가 → 목록에 나타남 → 새로고침 후에도 남음 → done 토글 동작 → "클로드에 물어보기"로 클립보드 복사 확인(붙여넣어 내용 확인).</human-check>
  </verify>
  <done>tsc·lint 통과. /inbox가 force-dynamic이고 잠금 게이트가 데이터보다 먼저 걸린다. 질문 추가·완료 토글·복사가 내장 브라우저에서 동작하고, 추가한 질문이 새로고침 후에도 유지된다.</done>
</task>

<task type="auto">
  <name>Task 3: /notes 단권화 페이지 + readAllLessonNotes 헬퍼</name>
  <files>src/lib/note-store.ts, src/app/notes/page.tsx</files>
  <action>
  note-store.ts에 readAllLessonNotes() 추가(기존 함수·스타일 유지, supabaseAdmin 사용): lesson_note에서 lesson_id, body를 전부 select하고, ok/error 판별 유니온으로 반환({ ok: true, notes: Map<string, string> } | { ok: false, error })한다 — readReviewStates가 Map을 만드는 방식과 동형. body가 빈 문자열인 행은 Map에서 제외(단권화는 실제 내용이 있는 노트만 대상). 조회 실패(error)와 "노트 없음"을 타입 수준에서 구분(review/note-store 원칙).

  notes/page.tsx(force-dynamic, 읽기 전용 집계): 상단에 `export const dynamic = "force-dynamic"`. /review와 동일하게 hasUnlockCookie()를 가장 먼저 호출 — 잠금 안 됐으면 EmptyState로 "노트 단권화는 잠금 해제 후 이용할 수 있어요. 홈에서 잠금을 해제해 주세요." 렌더. 잠금됐으면 readAllLessonNotes() 호출, 실패 시 EmptyState 오류 안내. 성공 시 getOrderedLessons()(step→모듈 order→레슨 order 정렬)를 순회하며 notes Map에 non-empty body가 있는 레슨만 남긴다. 렌더는 커리큘럼 순서 그대로 한 페이지에 카드로: 모듈이 바뀌는 지점마다 모듈 제목 헤딩(h2, @/content/modules의 modules에서 moduleId→title 조회)을 넣어 단권화 문서 느낌을 준다. 각 노트 카드(.panel)는 레슨 제목 + 노트 본문(줄바꿈 보존 — whitespace-pre-wrap) + 해당 레슨으로 가는 링크(lesson.permalink, .chip tap-feedback min-h-11). 노트가 하나도 없으면 EmptyState로 "아직 저장된 메모가 없어요. 레슨을 읽으며 하단 메모장에 적으면 여기 모여요." 안내. 이 페이지는 읽기 전용 집계다 — 여기서 노트를 편집하지 않는다(편집은 레슨 메모장). glossary/review와 같은 크림 종이 레이아웃, panel-hero 헤더에 제목 "노트"와 설명(모듈 순으로 모은 내 메모 단권화). 아이패드 우선, 코드 조각이 섞일 수 있으니 본문 컨테이너는 가로 스크롤 안전.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npm run lint</automated>
    <human-check>내장 브라우저를 아이패드 뷰포트로 열어 /notes 잠금 해제 후: 메모가 있는 레슨들이 모듈 순서로 모여 보이고, 각 카드의 레슨 링크로 이동됨. (노트가 없다면 빈 상태 안내가 뜨는지 확인.)</human-check>
  </verify>
  <done>tsc·lint 통과. readAllLessonNotes가 빈 노트를 제외한 Map을 ok/error 유니온으로 반환. /notes가 force-dynamic·잠금 게이트를 걸고, 노트가 있는 레슨을 모듈 순서로 링크와 함께 집계하며, 노트가 없으면 빈 상태를 보여준다.</done>
</task>

<task type="auto">
  <name>Task 4: 배선 — 내비 진입점 + 라우트 렌더 계약 등록 + 회귀 확인</name>
  <files>src/components/site-nav.tsx, scripts/check-route-rendering.mjs</files>
  <action>
  site-nav.tsx: NAV_ITEMS에 "노트"(href /notes)와 "질문함"(href /inbox) 두 항목을 "복습" 다음·"일정표" 앞에 추가한다 — 학습 흐름(오늘의 학습 → 커리큘럼 → 용어집 → 복습 → 노트 → 질문함 → 일정표 → 소개)에서 자연스러운 위치이고, 용어집·복습이 등장하며 내비에 추가된 것과 같은 판단이다. 내비는 flex-wrap이라 좁은 폭에서 두 줄로 접히고 헤더 높이 ResizeObserver가 이를 흡수하므로 8항목·짧은 라벨(노트·질문함)이 아이패드에서 문제없다(대안인 홈 보조 링크보다 안정적 도달 경로). 근거를 기존 주석 블록 형식으로 한 줄 남긴다(quick 260901-x62).

  check-route-rendering.mjs: EXPECTED_DYNAMIC_ROUTES 배열에 '/notes'와 '/inbox'를 추가한다(현재 ['/', '/schedule', '/review']). 두 라우트는 force-dynamic이라 prerender-manifest.json routes에 등장하면 안 되며, 이 게이트가 그 계약을 고정한다. 헤더 주석의 "동적 유지" 목록 설명에도 /notes·/inbox(quick 260901-x62 — 쿠키·노트/질문 상태를 읽는 라우트, /·/review와 동일 계약)를 반영한다.
  </action>
  <verify>
    <automated>npm run build</automated>
    <automated>node scripts/check-route-rendering.mjs</automated>
    <automated>npm run lint</automated>
    <human-check>내장 브라우저에서 내비에 "노트"·"질문함"이 보이고 각각 /notes·/inbox로 이동됨. 데스크톱 행과 640px 미만 햄버거 패널 양쪽에서 항목이 나타남.</human-check>
  </verify>
  <done>빌드 성공 후 check-route-rendering.mjs가 통과(/notes·/inbox가 동적 계약을 만족). lint 통과. 내비 두 곳(데스크톱·모바일 패널) 모두에 노트·질문함 진입점이 있고 라우트로 이동된다. 기존 라우트 계약(정적/동적) 회귀 없음.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>/notes(내 노트 단권화)·/inbox(궁금한 것 인박스) 두 수집 페이지, 내비 진입점, inbox 저장·서버 액션(Supabase, 기기 간 공유).</what-built>
  <how-to-verify>
    실제 아이패드(iPad Safari)로 배포/로컬 사이트에 접속해 홈에서 잠금 해제 후:
    1) 내비에서 "질문함"(/inbox) → 질문 하나 추가 → 목록에 뜨는지, 새로고침 후에도 남는지, done 토글이 되는지, "클로드에 물어보기"로 복사가 되는지(다른 앱에 붙여넣어 확인). 세로/가로 모드 모두, 터치 타깃이 충분한지.
    2) 내비에서 "노트"(/notes) → 그동안 레슨 메모장에 적어둔 메모가 모듈 순서로 모여 보이는지, 각 카드의 레슨 링크로 이동되는지. (메모가 없으면 빈 상태 안내.)
    3) 다른 기기(또는 브라우저)에서도 같은 질문이 보이는지(Supabase 공유 확인).
  </how-to-verify>
  <resume-signal>동작이 맞으면 "승인", 문제가 있으면 어느 페이지·어떤 동작에서 무엇이 어긋났는지 알려주세요.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Server Action (inbox-actions) | 질문 추가·완료 토글 요청의 body/id/done/lessonId가 신뢰 불가 입력으로 넘어온다 |
| client → force-dynamic page | 잠금 해제 여부에 따라 노트/질문 데이터 노출이 갈린다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-x62-01 | Elevation/Tampering | inbox-actions (add/toggle) | high | mitigate | 모든 액션이 store 호출 전에 hasUnlockCookie() 재검증(캡처된 POST 재전송 방어) — review-actions와 동형 순서 |
| T-x62-02 | DoS | inbox-store.addInboxItem / addInboxQuestion | medium | mitigate | body trim 후 비었거나 MAX_INBOX_LENGTH(5000) 초과 시 거부(note-store MAX_NOTE_LENGTH 원칙) |
| T-x62-03 | Tampering | toggleInboxDone(id) | medium | mitigate | id가 uuid 형태·done이 boolean인지 검증한 뒤에만 update, 존재하지 않는 id는 no-op |
| T-x62-04 | Information disclosure | /notes·/inbox 페이지 | high | mitigate | 어떤 조회보다 먼저 hasUnlockCookie() 게이트, 미잠금 시 EmptyState 반환·force-dynamic으로 캐시된 인증 응답 회피 |
| T-x62-05 | Spoofing | addInboxQuestion lessonId | low | mitigate | lessonId가 주어지면 getLessonBySlug로 존재 검증 후에만 저장(이번 범위엔 /inbox에서 null로만 생성) |
</threat_model>

<verification>
- `npx tsc --noEmit`·`npm run lint` 통과(전 태스크).
- `npm run build` 성공 후 `node scripts/check-route-rendering.mjs` 통과(/notes·/inbox 동적 계약, 기존 정적/동적 계약 회귀 없음).
- 기존 정적 게이트(design-tokens·brand·lesson-structure·check-review·check-pace 등) 회귀 없음 — 이 변경은 신규 라우트·신규 파일 중심이라 기존 게이트 입력을 건드리지 않으나, 실행자는 저장소의 게이트 스위트를 한 번 돌려 초록불 확인.
- 내장 브라우저(아이패드 뷰포트) 기능 확인: /inbox 추가·토글·복사, /notes 집계·링크·빈 상태.
- 인박스 마이그레이션 파일이 supabase/migrations/에 존재하고 라이브 스키마와 일치(idempotent).
</verification>

<success_criteria>
- /notes: 잠금 해제 시 노트가 있는 레슨만 모듈 순서로 링크와 함께 집계, 없으면 빈 상태. 읽기 전용(편집 없음).
- /inbox: 질문 추가(Supabase 영속)·완료 토글·클립보드 복사가 동작하고, 잠금 게이트가 데이터보다 먼저 걸린다.
- 내비에서 노트·질문함 도달 가능(데스크톱·모바일 패널 양쪽).
- 서버 액션이 hasUnlockCookie() 재검증 → 입력 검증 → store 순서를 지킨다.
- lint·tsc·build·route-rendering 게이트 초록불, 기존 게이트 회귀 없음.
</success_criteria>

<output>
Create `.planning/quick/260901-x62-notes-digest-question-inbox/260901-x62-SUMMARY.md` when done
</output>
